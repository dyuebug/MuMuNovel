# Alembic 数据库迁移指南

生产迁移当前由 Rust `migration-executor` 管理 PostgreSQL schema。SQLite
Alembic legacy profile 已退役，不再保留独立实现面。

## 📁 目录结构

```
backend/
├── alembic-postgres.ini       # PostgreSQL 配置文件
├── alembic/
│   ├── postgres/              # PostgreSQL 迁移脚本目录
│   │   ├── env.py
│   │   ├── script.py.mako
│   │   └── versions/          # PostgreSQL 迁移版本
│   │       ├── 20251226_1008_ee0a189f1532_初始数据库结构.py
│   │       └── 20251226_1102_e411428f00c0_初始化预置数据.py
```

## 🚀 使用方法

### ✅ 默认：Rust 执行迁移，Alembic 只做显式修订维护

生产和默认开发迁移由 Rust `migration-executor` 执行。Python
`scripts/migrate.py` 包装入口已删除；需要新增 Alembic revision 时，直接使用
PostgreSQL Alembic 配置。

```bash
cd backend
alembic -c alembic-postgres.ini revision --autogenerate -m "add_system_prompt_to_settings"
```

#### 使用建议
- **默认执行**：使用 Rust `migration-executor`
- **修订维护**：直接执行 `alembic -c alembic-postgres.ini revision ...`
- **命名约束**：`-m` 建议使用 ASCII slug，避免文件名乱码或跨平台编码痕迹

### 1. PostgreSQL 数据库

#### 配置环境变量
```bash
# .env 文件
DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/database_name
```

#### 生成迁移脚本
```bash
cd backend
alembic -c alembic-postgres.ini revision --autogenerate -m "add_system_prompt_to_settings"
```

#### 应用迁移
```bash
cargo run --manifest-path ../backend-rs/Cargo.toml -- migration-executor
```

部署环境默认通过 `docker compose -f ../docker-compose.strangler.yml run --rm db-migrator`
执行同一个 Rust `migration-executor`。`alembic -c alembic-postgres.ini upgrade ...`
仅保留为显式历史维护 / source-map 调试入口，不再作为默认升级命令。

#### 回退迁移
```bash
alembic -c alembic-postgres.ini downgrade -1
```

#### 查看迁移历史
```bash
alembic -c alembic-postgres.ini history
alembic -c alembic-postgres.ini current
```

## ⚙️ 关键配置差异

### PostgreSQL (alembic/postgres/env.py)
- `render_as_batch=False` - 直接支持 ALTER TABLE
- 使用 `server_default=sa.text('now()')` 

## 📝 注意事项

### PostgreSQL 优势
1. **高并发支持**：多用户同时读写
2. **完整的 ALTER TABLE 支持**
3. **高级特性**：全文搜索、JSON 操作符、数组类型等

## 🔄 切换数据库

只需修改 `.env` 文件中的 `DATABASE_URL`，然后使用对应的配置文件执行迁移：

```bash
# 切换到 PostgreSQL
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/db
cargo run --manifest-path ../backend-rs/Cargo.toml -- migration-executor
```

## 💡 最佳实践

1. **默认开发/部署**：使用 PostgreSQL + Rust `migration-executor`
2. **测试迁移**：默认验证 PostgreSQL/Rust migrator
3. **保持同步**：schema/source-map 变更要同时更新 Rust schema metadata tests 和 Alembic 健康检查

## 🐛 常见问题

### Q: 迁移脚本生成后可以通用吗？
A: 不行。PostgreSQL 和 SQLite 的迁移脚本是独立的，因为：
- SQL 语法差异（如 DEFAULT 值）
- 类型差异（如 JSON、BOOLEAN）
- ALTER TABLE 能力差异

### Q: 如何从 PostgreSQL 迁移数据到 SQLite？
A: 需要编写数据导出/导入脚本，不能直接复用迁移脚本。

### Q: 还保留 SQLite Alembic profile 吗？
A: 不保留。生产和默认开发迁移都走 PostgreSQL + Rust `migration-executor`。
