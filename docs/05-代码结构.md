# 05 - 代码结构

本文档详细介绍 MuMuAINovel 的代码组织结构，帮助你快速定位和理解各个模块的职责。

## 项目整体结构

```
MuMuAINovel/
├── backend/                 # 后端服务
│   ├── app/                # 应用核心代码
│   │   ├── api/           # API 路由层
│   │   ├── models/        # 数据库模型
│   │   ├── schemas/       # Pydantic 数据模型
│   │   ├── services/      # 业务逻辑层
│   │   ├── middleware/    # 中间件
│   │   ├── utils/         # 工具函数
│   │   ├── mcp/           # MCP 插件系统
│   │   ├── database.py    # 数据库配置
│   │   ├── logger.py      # 日志配置
│   │   └── main.py        # 应用入口
│   ├── alembic/           # 数据库迁移
│   ├── data/              # SQLite 数据文件
│   ├── tests/             # 测试文件
│   ├── requirements.txt   # Python 依赖
│   ├── .env.example       # 环境变量模板
│   └── CLAUDE.md          # 后端模块文档
│
├── frontend/              # 前端应用
│   ├── src/
│   │   ├── pages/        # 页面组件
│   │   ├── components/   # 通用组件
│   │   ├── services/     # API 调用服务
│   │   ├── stores/       # Zustand 状态管理
│   │   ├── types/        # TypeScript 类型定义
│   │   ├── utils/        # 工具函数
│   │   ├── hooks/        # 自定义 Hooks
│   │   ├── App.tsx       # 应用根组件
│   │   └── main.tsx      # 应用入口
│   ├── public/           # 静态资源
│   ├── package.json      # Node 依赖
│   ├── vite.config.ts    # Vite 配置
│   ├── tsconfig.json     # TypeScript 配置
│   └── CLAUDE.md         # 前端模块文档
│
├── docs/                 # 开发文档
├── images/               # 项目图片资源
├── docker-compose.yml    # Docker 编排配置
├── Dockerfile            # Docker 镜像构建
├── CLAUDE.md            # 项目架构文档
├── README.md            # 项目说明
└── LICENSE              # 开源协议
```

## 后端代码结构详解

### 1. app/api/ - API 路由层

**职责**：定义 HTTP 端点，处理请求和响应

**文件组织**：
```
api/
├── __init__.py
├── auth.py              # 认证相关 API
├── projects.py          # 项目管理 API
├── characters.py        # 角色管理 API
├── chapters.py          # 章节管理 API
├── foreshadowing.py     # 伏笔管理 API
├── prompts.py           # 提示词工坊 API
├── ai.py                # AI 服务 API
├── organizations.py     # 组织架构 API
├── levels.py            # 职业等级 API
└── settings.py          # 系统设置 API
```

**代码示例** (`api/projects.py`)：
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from typing import List

from app.database import get_db
from app.schemas.project import ProjectCreate, ProjectResponse
from app.services.project_service import ProjectService
from app.middleware.auth import get_current_user

router = APIRouter(prefix="/api/projects", tags=["projects"])

@router.get("/", response_model=List[ProjectResponse])
async def get_projects(
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user)
):
    """获取用户的所有项目"""
    service = ProjectService(db)
    return await service.get_user_projects(user_id)

@router.post("/", response_model=ProjectResponse)
async def create_project(
    project: ProjectCreate,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user)
):
    """创建新项目"""
    service = ProjectService(db)
    return await service.create_project(project, user_id)
```

**关键点**：
- 使用 `APIRouter` 组织路由
- 依赖注入获取数据库会话和用户信息
- 路由函数只负责参数验证和调用服务层
- 使用 Pydantic 模型进行数据验证

### 2. app/models/ - 数据库模型

**职责**：定义数据库表结构

**文件组织**：
```
models/
├── __init__.py
├── base.py              # 基础模型类
├── user.py              # 用户模型
├── project.py           # 项目模型
├── character.py         # 角色模型
├── chapter.py           # 章节模型
├── foreshadowing.py     # 伏笔模型
├── prompt.py            # 提示词模型
├── organization.py      # 组织模型
└── level_system.py      # 等级体系模型
```

**代码示例** (`models/project.py`)：
```python
from sqlalchemy import Column, String, Text, DateTime, ForeignKey
from sqlalchemy.orm import relationship
from datetime import datetime
import uuid

from app.models.base import Base

class Project(Base):
    __tablename__ = "projects"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id"), nullable=False)
    name = Column(String(200), nullable=False)
    description = Column(Text)
    genre = Column(String(50))
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    # 关系
    user = relationship("User", back_populates="projects")
    characters = relationship("Character", back_populates="project", cascade="all, delete-orphan")
    chapters = relationship("Chapter", back_populates="project", cascade="all, delete-orphan")
```

**关键点**：
- 继承 `Base` 类
- 使用 UUID 作为主键
- 定义外键关系
- 自动时间戳（created_at, updated_at）
- 级联删除（cascade）

### 3. app/schemas/ - Pydantic 模型

**职责**：定义 API 请求和响应的数据结构

**文件组织**：
```
schemas/
├── __init__.py
├── user.py              # 用户相关模型
├── project.py           # 项目相关模型
├── character.py         # 角色相关模型
├── chapter.py           # 章节相关模型
└── common.py            # 通用模型
```

**代码示例** (`schemas/project.py`)：
```python
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime

class ProjectBase(BaseModel):
    """项目基础模型"""
    name: str = Field(..., min_length=1, max_length=200)
    description: Optional[str] = None
    genre: Optional[str] = None

class ProjectCreate(ProjectBase):
    """创建项目请求模型"""
    pass

class ProjectUpdate(BaseModel):
    """更新项目请求模型"""
    name: Optional[str] = Field(None, min_length=1, max_length=200)
    description: Optional[str] = None
    genre: Optional[str] = None

class ProjectResponse(ProjectBase):
    """项目响应模型"""
    id: str
    user_id: str
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True  # 支持从 ORM 模型转换
```

**关键点**：
- 继承 `BaseModel`
- 使用 `Field` 进行数据验证
- 区分 Create、Update、Response 模型
- `Config.from_attributes = True` 支持 ORM 转换

### 4. app/services/ - 业务逻辑层

**职责**：实现核心业务逻辑

**文件组织**：
```
services/
├── __init__.py
├── project_service.py       # 项目服务
├── character_service.py     # 角色服务
├── chapter_service.py       # 章节服务
├── ai_service.py            # AI 服务
├── ai_providers/            # AI 提供商
│   ├── __init__.py
│   ├── base.py             # 基础提供商类
│   ├── openai_provider.py  # OpenAI 实现
│   ├── gemini_provider.py  # Gemini 实现
│   └── claude_provider.py  # Claude 实现
└── memory/                  # 长期记忆系统
    ├── __init__.py
    ├── memory_service.py
    └── vector_store.py
```

**代码示例** (`services/project_service.py`)：
```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from typing import List

from app.models.project import Project
from app.schemas.project import ProjectCreate, ProjectUpdate
from app.logger import get_logger

logger = get_logger(__name__)

class ProjectService:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def get_user_projects(self, user_id: str) -> List[Project]:
        """获取用户的所有项目"""
        try:
            result = await self.db.execute(
                select(Project).where(Project.user_id == user_id)
            )
            return result.scalars().all()
        except Exception as e:
            logger.error(f"获取项目列表失败: {e}")
            raise

    async def create_project(self, project_data: ProjectCreate, user_id: str) -> Project:
        """创建新项目"""
        try:
            project = Project(
                user_id=user_id,
                **project_data.model_dump()
            )
            self.db.add(project)
            await self.db.commit()
            await self.db.refresh(project)
            logger.info(f"创建项目成功: {project.id}")
            return project
        except Exception as e:
            await self.db.rollback()
            logger.error(f"创建项目失败: {e}")
            raise
```

**关键点**：
- 封装数据库操作
- 异常处理和日志记录
- 事务管理（commit/rollback）
- 业务逻辑与路由层分离

### 5. app/middleware/ - 中间件

**职责**：请求预处理和后处理

**文件组织**：
```
middleware/
├── __init__.py
├── auth.py              # 认证中间件
├── cors.py              # CORS 配置
├── logging.py           # 日志中间件
└── error_handler.py     # 错误处理中间件
```

**代码示例** (`middleware/auth.py`)：
```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from jose import JWTError, jwt
from datetime import datetime

from app.config import settings

security = HTTPBearer()

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security)
) -> str:
    """从 JWT Token 中获取当前用户 ID"""
    try:
        token = credentials.credentials
        payload = jwt.decode(
            token,
            settings.SECRET_KEY,
            algorithms=["HS256"]
        )
        user_id: str = payload.get("sub")
        if user_id is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="无效的认证凭证"
            )
        return user_id
    except JWTError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="无效的认证凭证"
        )
```

### 6. app/main.py - 应用入口

**职责**：应用初始化和配置

**代码结构**：
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.api import (
    auth, projects, characters, chapters,
    foreshadowing, prompts, ai
)
from app.database import engine, Base
from app.logger import get_logger

logger = get_logger(__name__)

# 创建 FastAPI 应用
app = FastAPI(
    title="MuMuAINovel API",
    description="AI 智能小说创作助手",
    version="1.3.5"
)

# CORS 配置
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
app.include_router(auth.router)
app.include_router(projects.router)
app.include_router(characters.router)
app.include_router(chapters.router)
app.include_router(foreshadowing.router)
app.include_router(prompts.router)
app.include_router(ai.router)

@app.on_event("startup")
async def startup_event():
    """应用启动事件"""
    logger.info("应用启动中...")
    # 初始化数据库
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    logger.info("应用启动完成")

@app.on_event("shutdown")
async def shutdown_event():
    """应用关闭事件"""
    logger.info("应用关闭中...")
    await engine.dispose()
    logger.info("应用已关闭")

@app.get("/health")
async def health_check():
    """健康检查"""
    return {"status": "healthy"}
```

## 前端代码结构详解

### 1. src/pages/ - 页面组件

**职责**：定义应用的各个页面

**文件组织**：
```
pages/
├── Home.tsx             # 首页
├── Login.tsx            # 登录页
├── ProjectList.tsx      # 项目列表
├── ProjectDetail.tsx    # 项目详情
├── CharacterManage.tsx  # 角色管理
├── ChapterEditor.tsx    # 章节编辑器
├── PromptWorkshop.tsx   # 提示词工坊
└── Settings.tsx         # 设置页面
```

**代码示例** (`pages/ProjectList.tsx`)：
```typescript
import { useState, useEffect } from 'react';
import { Button, Card, message } from 'antd';
import { useNavigate } from 'react-router-dom';
import { projectApi } from '../services/api';
import type { Project } from '../types';

export default function ProjectList() {
  const [projects, setProjects] = useState<Project[]>([]);
  const [loading, setLoading] = useState(false);
  const navigate = useNavigate();

  useEffect(() => {
    loadProjects();
  }, []);

  const loadProjects = async () => {
    try {
      setLoading(true);
      const data = await projectApi.getProjects();
      setProjects(data);
    } catch (error) {
      message.error('加载项目失败');
    } finally {
      setLoading(false);
    }
  };

  const handleCreateProject = () => {
    navigate('/projects/new');
  };

  return (
    <div className="project-list">
      <Button type="primary" onClick={handleCreateProject}>
        新建项目
      </Button>
      <div className="project-grid">
        {projects.map(project => (
          <Card
            key={project.id}
            title={project.name}
            onClick={() => navigate(`/projects/${project.id}`)}
          >
            {project.description}
          </Card>
        ))}
      </div>
    </div>
  );
}
```

### 2. src/components/ - 通用组件

**职责**：可复用的 UI 组件

**文件组织**：
```
components/
├── Layout/              # 布局组件
│   ├── Header.tsx
│   ├── Sidebar.tsx
│   └── Footer.tsx
├── Editor/              # 编辑器组件
│   ├── RichTextEditor.tsx
│   └── MarkdownEditor.tsx
├── Character/           # 角色相关组件
│   ├── CharacterCard.tsx
│   ├── CharacterForm.tsx
│   └── RelationshipGraph.tsx
├── AI/                  # AI 相关组件
│   ├── AIGenerateButton.tsx
│   └── StreamingText.tsx
└── Common/              # 通用组件
    ├── Loading.tsx
    ├── ErrorBoundary.tsx
    └── ConfirmDialog.tsx
```

**代码示例** (`components/Character/CharacterCard.tsx`)：
```typescript
import { Card, Tag, Avatar } from 'antd';
import type { Character } from '../../types';

interface CharacterCardProps {
  character: Character;
  onClick?: () => void;
}

export default function CharacterCard({ character, onClick }: CharacterCardProps) {
  return (
    <Card
      hoverable
      onClick={onClick}
      cover={
        <Avatar size={64} src={character.avatar}>
          {character.name[0]}
        </Avatar>
      }
    >
      <Card.Meta
        title={character.name}
        description={
          <>
            <Tag color="blue">{character.age}岁</Tag>
            <Tag color="green">{character.occupation}</Tag>
            {character.level && <Tag color="gold">{character.level}</Tag>}
          </>
        }
      />
      <p className="character-description">{character.description}</p>
    </Card>
  );
}
```

### 3. src/services/ - API 调用服务

**职责**：封装 API 请求

**文件组织**：
```
services/
├── api.ts               # API 客户端配置
├── projectApi.ts        # 项目 API
├── characterApi.ts      # 角色 API
├── chapterApi.ts        # 章节 API
├── aiApi.ts             # AI API
└── authApi.ts           # 认证 API
```

**代码示例** (`services/api.ts`)：
```typescript
import axios from 'axios';
import { message } from 'antd';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000';

// 创建 axios 实例
const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器
apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器
apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      message.error('登录已过期，请重新登录');
      localStorage.removeItem('token');
      window.location.href = '/login';
    } else {
      message.error(error.response?.data?.detail || '请求失败');
    }
    return Promise.reject(error);
  }
);

export default apiClient;

// 项目 API
export const projectApi = {
  getProjects: () => apiClient.get('/api/projects'),
  getProject: (id: string) => apiClient.get(`/api/projects/${id}`),
  createProject: (data: any) => apiClient.post('/api/projects', data),
  updateProject: (id: string, data: any) => apiClient.put(`/api/projects/${id}`, data),
  deleteProject: (id: string) => apiClient.delete(`/api/projects/${id}`),
};
```

### 4. src/stores/ - 状态管理

**职责**：全局状态管理（使用 Zustand）

**文件组织**：
```
stores/
├── useAuthStore.ts      # 认证状态
├── useProjectStore.ts   # 项目状态
├── useEditorStore.ts    # 编辑器状态
└── useSettingsStore.ts  # 设置状态
```

**代码示例** (`stores/useAuthStore.ts`)：
```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  token: string | null;
  user: User | null;
  isAuthenticated: boolean;
  login: (token: string, user: User) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      token: null,
      user: null,
      isAuthenticated: false,
      login: (token, user) => {
        localStorage.setItem('token', token);
        set({ token, user, isAuthenticated: true });
      },
      logout: () => {
        localStorage.removeItem('token');
        set({ token: null, user: null, isAuthenticated: false });
      },
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

### 5. src/types/ - TypeScript 类型定义

**职责**：定义数据类型

**代码示例** (`types/index.ts`)：
```typescript
export interface User {
  id: string;
  username: string;
  email?: string;
}

export interface Project {
  id: string;
  user_id: string;
  name: string;
  description?: string;
  genre?: string;
  created_at: string;
  updated_at: string;
}

export interface Character {
  id: string;
  project_id: string;
  name: string;
  age?: number;
  gender?: string;
  occupation?: string;
  level?: string;
  description?: string;
  avatar?: string;
}

export interface Chapter {
  id: string;
  project_id: string;
  title: string;
  content: string;
  order: number;
  created_at: string;
  updated_at: string;
}
```

## 数据流向

### 后端数据流

```
HTTP 请求
    ↓
API 路由层 (api/)
    ↓
中间件验证 (middleware/)
    ↓
业务逻辑层 (services/)
    ↓
数据库模型 (models/)
    ↓
PostgreSQL/SQLite
    ↓
返回数据
    ↓
Pydantic 序列化 (schemas/)
    ↓
HTTP 响应
```

### 前端数据流

```
用户操作
    ↓
页面组件 (pages/)
    ↓
API 服务 (services/)
    ↓
HTTP 请求 → 后端
    ↓
响应数据
    ↓
状态管理 (stores/)
    ↓
组件更新 (components/)
    ↓
UI 渲染
```

## 下一步

理解代码结构后，继续学习：
- **06-后端开发.md**：后端开发实践
- **07-前端开发.md**：前端开发实践
- **08-数据库设计.md**：数据库设计详解

---

**提示**：建议从一个完整的功能模块（如项目管理）入手，跟踪代码从前端到后端的完整流程。
