# Implementation Plan

## Execution Rule

Do not start implementation until the planning artifacts in this task are
reviewed and approved.

## Ordered Checklist

1. Inspect the current `backend-rs` state and select the highest-signal
   remaining seam for this round.
2. Load the relevant backend spec indexes before editing code.
3. Implement one low-risk slice only.
4. Add focused tests if the slice extracts or changes pure helper behavior.
5. Run validation with `cargo check` and any relevant targeted tests.
6. Reassess whether the next seam is still worth pursuing in this session.

## Latest Checkpoint

- 2026-05-30 09:18 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by closing the next
  owner gap behind batch quality-gate retry: Rust already owned
  `auto_repair -> retry same chapter`, but the retry attempt was still building
  research/provider input from the original session compat options instead of
  the runtime-restored compat options that carry the latest story-repair state.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now narrows retry input ownership so runtime-restored compat options feed the
  provider/research input path as well:
  - batch runtime still restores `resolved_compat_options` from the persisted
    runtime snapshot before each chapter attempt
  - prompt overrides already consumed those resolved compat options before this
    slice
  - the provider/research payload path now also consumes the same resolved
    compat options instead of the original `session.compat_options`

  A dedicated helper now owns that boundary:
  - `build_runtime_provider_payload_for_batch_generation_step(...)`
  - runtime passes:
    - current chapter target
    - current user id
    - runtime-restored compat options
  - this keeps provider/research assembly aligned with the same repaired
    runtime state that already shapes prompt overrides

  This is a real Python-to-Rust migration step because Rust now owns more than
  retry scheduling alone:
  when batch runtime re-enters the same chapter after a retryable quality gate,
  the next attempt now reuses the updated runtime repair context across both:
  - prompt override input
  - provider/research payload input

  In effect, Rust now owns the minimal retry-input loop for batch quality
  repair instead of only saying “retry again” while still partially sourcing
  generation inputs from the pre-repair request boundary.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 08:52 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by taking the next
  real step beyond retry labeling: batch generation no longer treats
  `quality_gate = auto_repair/repair` as read-side-only retry intent, because
  the Rust runtime now owns a minimal execution branch that actually retries
  the current chapter after follow-up analysis resolves a retryable quality
  gate.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now extends batch runtime step ownership with an explicit retry outcome:
  - `BatchGenerationStepOutcome` now carries
    `RetryCurrentChapter { next_retry_count }`
  - the chapter execution loop no longer only supports:
    - `Continue`
    - `Stop`
  - it now also supports:
    - stay on the same chapter
    - wait using the existing retry backoff contract
    - rerun the same chapter attempt under Rust runtime control

  Rust now reuses the previously completed owner pieces instead of inventing a
  parallel retry path:
  - retry-budget-aware quality-gate semantics from
    `retryable_repair_label_from_quality_context_with_retry_budget(...)`
  - existing runtime retry snapshot/task persistence from
    `persist_batch_generation_retry_attempt(...)`
  - existing retry backoff contract from
    `batch_generation_retry_backoff_seconds(...)`

  After follow-up analysis succeeds, Rust now resolves three execution branches
  in order:
  - `manual_review` -> stop task as failed
  - `auto_repair/repair` with remaining retry budget -> persist
    `repair_pending` runtime state and retry the current chapter
  - otherwise -> continue to the next chapter as before

  The retry branch now writes runtime-owned quality-gate retry state before
  sleeping/retrying:
  - `analysis_task_message`
  - `quality_gate_decision = "auto_repair"`
  - `quality_gate_label`
  - `phase = "repair_pending"`

  This is a real Python-to-Rust migration step because Rust now owns another
  true execution boundary from the Python quality-gate runtime:
  `auto_repair && retry_budget_available -> retry same chapter`
  is no longer only exposed through terminal/read-side semantics, but is now
  executed by the Rust runtime loop itself.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 08:06 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by extending the
  new quality-gate execution owner from batch generation into the single
  chapter generation runtime. Before this change, single generation still
  treated follow-up analysis as side-effect-only work: Rust could persist fresh
  chapter quality facts, but the runtime would still always finalize the task
  as `completed` after generation as long as analysis itself did not error.

  `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  now returns the freshly derived `quality_metrics` payload in the follow-up
  analysis result envelope:
  - the runtime no longer needs to re-query durable state just to inspect the
    freshly produced quality gate
  - the same minimal quality payload that already flows into
    `generation_history` can now directly feed the next runtime decision

  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  now owns a minimal single-generation `manual_review` quality-gate stop path:
  - Rust now resolves manual-review labels from the follow-up analysis
    `quality_metrics`
  - after generation succeeds, single-generation runtime no longer blindly
    completes the task when analysis returns a blocking gate
  - when follow-up analysis resolves `quality_gate.decision = manual_review`,
    Rust now:
    - writes runtime snapshot fields:
      - `quality_gate_decision = "manual_review"`
      - `quality_gate_label`
      - `phase = "quality_blocked"`
    - marks the single generation task as `failed`
    - persists a runtime-owned failure message instead of exposing the gate as
      inert metadata only

  This is a real Python-to-Rust migration step because single-chapter runtime
  now owns another true execution boundary from the quality-gate flow:
  `manual_review -> do not finalize as completed`
  is now enforced by the Rust runtime itself, not only surfaced later through
  read-side interpretation.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 07:24 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by turning one more
  quality-gate branch from read-side/status interpretation into a real Rust
  execution owner path: `manual_review` is no longer only a terminal label
  that Rust can surface in status/resume/SSE semantics after failure, because
  the batch follow-up runtime now actively stops execution when persisted
  quality context resolves to a manual-review gate.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now owns a minimal manual-review quality-gate execution stop:
  - Rust now resolves manual-review gate labels from the same persisted
    quality-context sources already used by the read-side/status owner:
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `latest_quality_metrics`
    - with retry-budget-aware fallback semantics
  - after follow-up analysis succeeds, Rust now evaluates that gate before
    advancing to the next chapter
  - when the gate resolves to `manual_review`, Rust now writes a runtime-owned
    snapshot payload carrying:
    - `quality_gate_decision = "manual_review"`
    - `quality_gate_label`
  - and then stops the task through a dedicated failed runtime persistence
    branch instead of letting execution continue as if the gate were only
    informational metadata

  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`
  now extends the runtime failure contract with a dedicated quality-gate
  failure kind:
  - `BatchGenerationFailureKind::QualityGateBlocked`
  - failed checkpoint messaging now distinguishes quality-gate manual-review
    blocking from generic analysis/runtime failure

  This is a real Python-to-Rust migration step because Rust now owns an actual
  execution boundary from the Python quality-gate runtime instead of only
  interpreting the outcome later:
  `manual_review -> stop batch progression`
  is now enforced by the Rust runtime path itself.

  Focused validation passed with:
  `cargo check --manifest-path "backend-rs/Cargo.toml"`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`

- 2026-05-30 03:36 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by moving one more
  failure-source seam from “Rust only interprets terminal data later” toward
  “Rust runtime writes its own typed failure facts” for batch generation.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now owns a reusable failed-chapter persistence path:
  - Rust now builds runtime-owned failed-chapter entries through a shared
    helper instead of leaving most failure branches with only task-level
    `error_message`
  - failure entries now consistently carry:
    - `chapter_id`
    - `chapter_number`
    - `title`
    - `error`
    - `retry_count`
  - the runtime persistence plan now appends those entries when the task stops
    in a failed state, instead of relying on later read-side layers to infer
    failure meaning from generic task fields

  This now covers the main Rust runtime stop branches already present in
  `execute_batch_generation_step(...)` and the follow-up analysis failure path:
  - missing chapter
  - load chapter error
  - generation/provider payload preparation error
  - chapter generation runtime error
  - follow-up analysis hard failure

  The analysis failure path was also simplified:
  - Rust no longer hand-updates `failed_chapters` and task terminal fields in a
    special-case block before calling the shared runtime persistence owner
  - snapshot-side analysis metadata remains in place, but task failure facts
    now flow through the same runtime failure contract as the other stop
    branches

  This is a real Python-to-Rust migration step because Rust now owns more of
  the Python batch failure source-of-truth contract:
  `failed_chapters` is starting to become runtime-authored data again, instead
  of mostly a Python-era shape that Rust status/read-side has to reinterpret
  after the fact.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 03:16 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by taking another
  real sub-branch from Python `resolve_quality_gate_execution_plan(...)`:
  failed batch tasks with `quality_gate = auto_repair/repair` and remaining
  retry budget are no longer collapsed into generic Rust `error` terminal
  semantics, but now resolve to a dedicated retryable-repair owner path across
  status/read-side and SSE stream semantics.

  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
  now extends shared terminal semantics with retry-intent ownership:
  - Rust still resolves:
    - explicit `manual_review`
    - Python-style `quality_blocked`
    - exhausted `auto_repair/repair -> manual_review`
  - Rust now also resolves:
    - `quality_gate.decision = auto_repair` or `repair`
    - with `current_retry_count < max_retries`
    as a retryable repair terminal branch
  - batch terminal payloads now emit:
    - `terminal_reason = retry`
    - `terminal_label = quality_gate_label` fallback
    - `review_required = false`
    - `can_resume = true`
    instead of collapsing directly to generic `error`

  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  now carries the same owner into stream-state assembly:
  - failed tasks with retryable repair context now resolve to
    `BatchGenerationStreamTerminalKind::Retry`
  - stream message fallback now prefers the retryable repair label before
    falling back to generic failed text

  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  now exposes the retry terminal branch through SSE:
  - retryable repair terminal events now emit:
    - `phase = repair_pending`
    - `terminal_reason = retry`
    - `review_required = false`
    - `can_resume = true`
    - HTTP-style code `409`
  - manual-review and generic failed branches remain unchanged

  This is a real Python-to-Rust migration step because Rust now owns one more
  execution-plan classification boundary from the Python runtime:
  `auto_repair && retry_count < max_retries -> retry`
  is no longer hidden behind a generic failed terminal surface on the Rust
  read/stream side, even though the full automatic retry runtime loop has not
  been migrated yet.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_stream_semantics_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_event_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 02:37 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by aligning one more
  Python `resolve_quality_gate_execution_plan(...)` branch with a real Rust
  owner path: `quality_gate = auto_repair/repair` no longer stops at
  “manual_review explicit label only”, because exhausted retry budget is now
  treated as a manual-review blocker across status/read-side, resume, and SSE
  terminal semantics.

  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
  now extends shared manual-review detection with retry-budget awareness:
  - Rust still supports:
    - explicit `quality_gate_decision == manual_review`
    - Python-style `phase = quality_blocked`
    - nested quality-context gate payloads
  - Rust now also treats:
    - `quality_gate.decision = auto_repair` or `repair`
    - with `current_retry_count >= max_retries`
    as a manual-review blocker even when the failed payload never wrote the
    terminal `manual_review` label explicitly
  - batch terminal payload assembly now consumes the retry-budget-aware owner
    before deciding:
    - `terminal_reason`
    - `terminal_label`
    - `review_required`
    - `can_resume`

  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  now consumes the same retry-budget-aware owner while resolving terminal
  stream semantics:
  - a failed batch with exhausted `auto_repair` budget now resolves to
    `BatchGenerationStreamTerminalKind::ManualReview`
    instead of a generic failed terminal state

  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now extend resume stop/go ownership:
  - resume command state now carries `current_retry_count`
  - resume preparation now blocks not only on explicit `manual_review` labels
    and `quality_blocked`, but also on exhausted `auto_repair/repair` retry
    budget carried by persisted quality context

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  test helpers were updated so resume-runtime fixtures now provide the new
  retry-count input explicitly.

  This is a real Python-to-Rust migration step because Rust now owns one more
  execution-plan boundary from the Python runtime:
  exhausted `auto_repair -> manual_review blocked` is no longer an implicit
  Python-only decision hidden behind post-hoc payload labels.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_stream_semantics_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 02:24 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by aligning Rust
  with the Python `quality_blocked -> manual_review` failure owner, so Rust no
  longer depends only on explicit `quality_gate_decision == manual_review`
  fields when deciding that a failed batch is blocked for manual review.

  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
  now extends shared manual-review detection:
  - Rust still supports:
    - `quality_gate_decision == manual_review`
    - nested quality-context gate payloads
  - Rust now also treats failed payloads with
    `phase = quality_blocked` as manual-review blockers
  - batch terminal status payloads now resolve:
    - `terminal_reason = manual_review`
    - `terminal_label = 需人工复核`
    - `review_required = true`
    - `can_resume = false`
    even when the failed-chapter record only carries the Python-style
    `quality_blocked` phase marker

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now inherits the same owner behavior automatically through the shared helper:
  - resume blocking now covers Python-style `quality_blocked` failed entries,
    not just explicit gate-decision labels

  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  now carries the Python-style failure phase through the SSE manual-review
  terminal envelope:
  - manual-review terminal error events now include
    `phase = quality_blocked`
  - the compatible error-event shape remains unchanged otherwise

  This is a real Python-to-Rust migration step because Rust now owns one more
  execution failure branch from the Python batch-generation runtime instead of
  only mirroring the outcome through partial field inspection.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_event_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 02:07 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by extending
  `manual_review` ownership from resume/status read-side into the batch SSE
  terminal stream, so Rust no longer stops resume and labels status payloads
  correctly while still collapsing the live terminal stream into a generic
  failed error event.

  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  now extends stream-state ownership:
  - Rust still supports the existing status-only stream fallback
  - Rust stream state now also accepts
    `BatchGenerationQualityStatusContext`
  - when batch status is `failed`, Rust now resolves manual-review labels from:
    - `failed_chapters`
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `quality_metrics_summary.quality_gate`
    - `latest_quality_metrics`
    - `latest_quality_metrics.quality_gate`
  - a failed batch with a persisted manual-review gate now resolves to
    `BatchGenerationStreamTerminalKind::ManualReview`
    instead of the generic `Failed`

  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
  now passes runtime/snapshot quality context into stream-state assembly:
  - initial stream load and polling reloads now build stream state from the
    same quality-context owner already used by resume and status payloads

  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  now emits explicit manual-review terminal semantics while preserving the
  existing SSE error envelope:
  - terminal stream still closes with an `error` event
  - but manual-review terminal events now include:
    - `terminal_reason = manual_review`
    - `terminal_label`
    - `review_required = true`
    - `can_resume = false`

  This is a real Python-to-Rust migration step because Rust now owns another
  live runtime behavior boundary:
  persisted story-repair quality context can now directly shape batch SSE
  terminal semantics, not just resume blocking or status/read-side payloads.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_stream_semantics_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_event_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 01:46 +08:00 checkpoint:
  this slice continued the same Phase 5 runtime migration by closing the
  batch terminal/read-side `manual_review` semantics gap, so Rust no longer
  stops resume through quality-context owner while still exposing a generic
  failed/error terminal payload to status readers.

  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
  now extends terminal payload ownership:
  - Rust still supports the existing
    `failed_chapters[0].quality_gate_decision == manual_review` contract
  - Rust terminal payload resolution now also consumes
    `BatchGenerationQualityStatusContext`
  - when `failed_chapters` does not carry the label, Rust now falls back to
    the same quality-context manual-review owner already used by resume:
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `quality_metrics_summary.quality_gate`
    - `latest_quality_metrics`
    - `latest_quality_metrics.quality_gate`

  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  now passes runtime/snapshot quality context into terminal payload assembly:
  - status/read-side payloads no longer derive `terminal_reason`,
    `terminal_label`, `review_required`, and `can_resume` from
    `failed_chapters` only
  - a failed batch with persisted runtime `manual_review` quality context now
    surfaces the same terminal semantics to readers even before
    `failed_chapters` is backfilled with that label

  This is a real Python-to-Rust migration step because Rust now owns another
  runtime behavior boundary instead of storing quality-gate data as inert
  metadata only:
  runtime/snapshot quality context can now directly shape batch read-side
  terminal semantics, not just resume stop/go behavior.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 02:18 +08:00 checkpoint:
  this slice continued the Phase 5 runtime migration by turning persisted
  story-repair quality-gate fields into a real Rust resume stop/go owner,
  instead of only storing `quality_gate_decision` in runtime/snapshot payloads
  while leaving resume blocking dependent on `failed_chapters` labels alone.

  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
  now extends shared manual-review semantics:
  - Rust still supports the existing
    `failed_chapters[0].quality_gate_decision == manual_review` contract
  - Rust now also resolves manual-review labels directly from quality context:
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `quality_metrics_summary.quality_gate`
    - `latest_quality_metrics`
    - `latest_quality_metrics.quality_gate`

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now consumes that shared quality-context blocker during resume preparation:
  - resume is still blocked when `failed_chapters` explicitly says
    `manual_review`
  - resume is now also blocked when workflow runtime state or persisted
    snapshot quality context already carries a manual-review gate, even if the
    failed-chapter list itself does not yet contain the label

  This is a real Python-to-Rust migration step because Rust now owns a true
  runtime decision boundary:
  persisted `quality_gate_decision` no longer acts as inert metadata only,
  but can actively stop batch resume through the Rust owner path.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_quality_status_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 01:49 +08:00 checkpoint:
  this slice continued the same Phase 5 story-repair migration by aligning
  the single-chapter recent-history fallback with the new shared Rust
  aggregation owner, instead of leaving the single path on a weaker
  “collect up to 3 summaries, then use only the first one” shortcut.

  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  now updates the recent-history fallback path:
  - Rust still looks back at up to the previous 3 chapters when the current
    chapter has no local quality metrics yet
  - but it no longer returns only the first available previous summary
  - the single-chapter recent-history fallback now reuses
    `aggregate_story_repair_quality_summaries(...)`
    from the shared story-repair quality-context owner

  This means the single-generation runtime seed can now inherit a richer
  aggregated recent-history summary before Rust restores:
  - `active_story_repair_payload`
  - `quality_metrics_summary`
  - `quality_history_context`
  - and then rehydrates compat options for the real execution input

  This is a real Python-to-Rust migration step because Rust now owns the same
  class of recent-history summary aggregation contract on both:
  - batch pre-run story-repair fallback
  - single-chapter pre-run story-repair fallback

  Focused validation target for this slice:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 01:36 +08:00 checkpoint:
  this slice continued the same Phase 5 story-repair migration by replacing
  the Rust batch pre-run recent-history fallback from a weak “take the first
  available previous summary” shortcut with a Rust-owned recent-history
  aggregation boundary that is closer to the Python batch runtime owner.

  `backend-rs/src/services/chapter_story_repair_quality_context_service.rs`
  now owns a reusable
  `aggregate_story_repair_quality_summaries(...)` helper:
  - Rust accepts multiple recent per-chapter quality summaries
  - Rust now merges recent:
    - `repair_targets`
    - `preserve_strengths`
    - `focus_areas`
  - Rust also emits a stable aggregated batch summary subset:
    - `chapter_count`
    - `overall_score`
    - `overall_score_delta`
    - `overall_score_trend`
    - `recent_focus_areas`
    - `recent_failed_metric_counts`
    - `quality_gate_counts`
    - `recent_manual_review_count`
    - `recent_auto_repair_count`
    - `quality_runtime_context.recent_metrics`
    - merged `repair_guidance`
    - latest `quality_gate`

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now uses that shared aggregation owner when seeding batch runtime state:
  - Rust still looks back at up to the previous 3 chapters
  - but it no longer stops at the first chapter with a usable summary
  - the batch runtime seed now derives its story-repair context from an
    aggregated recent-history batch summary before restoring:
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `quality_history_context`
  - that aggregated seed still flows into runtime execution input through the
    already-completed compat-option restore path

  This is a real Python-to-Rust migration step because Rust now owns more of
  the Python `resolve_generation_story_repair_state_for_batch(...)` recent
  history summary assembly contract instead of keeping only a one-summary
  placeholder fallback.

  Focused validation target for this slice:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 01:24 +08:00 checkpoint:
  this slice completed the most important missing link in the current
  story-repair migration wave: Rust no longer writes derived repair state into
  runtime seed only for snapshot persistence, but now also restores that seed
  back into the real execution input before runtime launch.

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now extends batch create dispatch:
  - Rust builds `runtime_state_payload` before task snapshot persistence
  - Rust restores compat options from that seeded payload through
    `resolve_batch_generation_runtime_compat_options_from_seed(...)`
  - the restored compat options now feed the real
    `BatchGenerationExecutionInput`

  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  now extends single create dispatch in the same direction:
  - Rust builds the single-generation runtime seed before dispatch
  - Rust restores compat options from that seed through
    `resolve_single_generation_runtime_compat_options_from_seed(...)`
  - the restored compat options now feed the real
    `SingleChapterGenerationExecutionInput`

  This is a real Python-to-Rust migration step because the derived
  story-repair state now changes actual runtime prompt input through the Rust
  owner path:
  `runtime seed -> restored compat options -> execution input -> prompt overrides`

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 00:58 +08:00 checkpoint:
  this slice continued the same single-chapter story-repair runtime migration
  by extending the Rust pre-run seed boundary from “current chapter quality”
  only to the Python fallback branch that reuses recent previous chapter
  history when the current chapter has no quality metrics yet.

  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  now extends the Rust-owned single-generation runtime seed assembly:
  - Rust still prefers current chapter quality context when the current
    chapter already has quality metrics
  - if the current chapter has no quality metrics, Rust now loads up to the
    previous 3 chapters in the same project and reuses the first available
    recent-history quality summary as a stable fallback
  - the single-generation runtime seed can now be built from either:
    - `current_chapter_quality`
    - `recent_history_summary`
  - Rust continues to merge explicit manual repair input with whichever derived
    quality branch is active

  After this slice, Rust has started owning both major stable branches of the
  Python `resolve_generation_story_repair_state_for_chapter(...)` pre-run
  contract:
  - current chapter quality-derived repair state
  - previous recent-history-derived repair state

  The single-generation runtime seed now carries the same stable field family
  across both branches:
  - `active_story_repair_payload`
  - `quality_metrics_summary`
  - `quality_history_context`

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 00:53 +08:00 checkpoint:
  this slice started the same story-repair runtime-state migration on the
  single-chapter path, so Rust no longer seeds background single-generation
  runtime state from manual request input only when current chapter quality
  context is already available locally.

  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  now introduces a Rust-owned single-generation runtime seed assembly
  boundary:
  - Rust loads the current chapter analysis read context before dispatching the
    background single-generation task
  - Rust derives `quality_metrics_summary` and `latest_quality_metrics` from
    that current chapter quality context
  - Rust builds the single-generation runtime seed from:
    - explicit manual story-repair request input
    - current chapter quality-derived story-repair payload
    - `quality_metrics_summary`
    - `quality_history_context`
  - when both manual repair input and current chapter quality-derived repair
    input are present, Rust now merges them and stamps:
    - `source = manual_plus_current_chapter_quality`

  `backend-rs/src/services/chapter_story_repair_quality_context_service.rs`
  now extends the shared merge helper so batch and chapter paths can reuse the
  same active-payload owner while still emitting source labels appropriate to:
  - `recent_history_summary`
  - `current_chapter_quality`

  This is a real Python-to-Rust migration step because Python
  `resolve_generation_story_repair_state_for_chapter(...)` previously owned
  the runtime rule that current chapter quality context should feed the active
  repair payload and companion quality state before a chapter generation run
  starts.

  After this slice, Rust has started owning the stable single-chapter pre-run
  runtime seed contract for the currently supported “current chapter quality”
  branch:
  - `active_story_repair_payload`
  - `quality_metrics_summary`
  - `quality_history_context`

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 23:10 +08:00 checkpoint:
  this slice continued the same Phase 5 batch story-repair migration by
  extending the Rust resume runtime-state seed boundary to carry forward the
  persisted quality summary context, instead of restoring only the active
  repair payload while silently dropping the companion quality owner fields.

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now extends `build_resume_runtime_state_seed(...)`:
  - Rust still restores `active_story_repair_payload` with the same priority
    order:
    - workflow runtime state payload
    - snapshot quality-context-derived payload
    - request-owned manual payload
  - Rust now also restores `quality_metrics_summary` directly from the loaded
    batch snapshot when present
  - Rust now additionally restores `quality_history_context` from
    `quality_metrics_summary.quality_runtime_context`

  This is a real Python-to-Rust migration step because Python batch
  story-repair runtime assembly previously owned not only the active repair
  payload, but also the companion quality-summary state that later runtime and
  prompt layers may depend on.

  After this slice, Rust resume preparation owns the stable recovery of all
  three persisted batch story-repair runtime fields:
  - `active_story_repair_payload`
  - `quality_metrics_summary`
  - `quality_history_context`

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 23:06 +08:00 checkpoint:
  this slice extended the same Phase 5 story-repair migration from the resume
  path into the batch create / pre-run runtime seed boundary, so Rust no longer
  seeds only a manual-request-shaped repair payload before batch execution.

  `backend-rs/src/services/chapter_story_repair_quality_context_service.rs`
  now owns two additional shared runtime-state helpers that mirror the stable
  Python batch behavior:
  - `merge_active_story_repair_payloads(...)`
    - merges explicit manual repair intent with recent-history-derived repair
      context instead of forcing a choose-one fallback
    - preserves manual summary / repair targets / preserve strengths as the
      higher-priority source while appending missing derived guidance items
    - stamps the merged runtime snapshot with
      `source = manual_plus_recent_history_summary`
  - `extract_quality_history_context(...)`
    - restores the stable `quality_runtime_context` subset from persisted
      `quality_metrics_summary`

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now extends the Rust-owned batch create runtime seed contract:
  - the pre-run seed builder first resolves the recent batch quality summary
    from previous chapter history
  - Rust then derives a batch repair payload from that summary
  - when the request also carries explicit story-repair input, Rust now merges
    the manual payload with the derived payload instead of discarding recent
    history context
  - the initial workflow runtime seed now persists all three owner fields:
    - `active_story_repair_payload`
    - `quality_metrics_summary`
    - `quality_history_context`

  This is a real Python-to-Rust migration step because Python
  `resolve_generation_story_repair_state_for_batch(...)` previously owned two
  runtime behaviors that create/pre-run batch execution depended on:
  - merging manual repair input with recent-history-derived batch repair state
  - attaching `quality_metrics_summary` and `quality_history_context` to the
    runtime seed alongside the active repair payload

  After this slice, Rust owns that stable batch create / pre-run runtime-state
  assembly contract for the currently supported quality-summary shape.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_story_repair_quality_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-30 00:18 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume story-repair migration by
  moving the stable “snapshot quality context -> active repair runtime payload”
  fallback behind a Rust owner, instead of restoring only prompt input while
  leaving the resumed runtime state's `active_story_repair_payload` to degrade
  back into a manual-request-shaped seed.

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now extends the Rust resume runtime-state seed boundary:
  - `build_resume_runtime_state_seed(...)` now accepts the loaded
    `batch_generation_snapshot`
  - when workflow runtime state already carries `active_story_repair_payload`,
    Rust still preserves it as highest priority
  - when runtime state does not carry that payload, Rust now rebuilds a richer
    `active_story_repair_payload` directly from persisted snapshot quality
    context before falling back to the request-owned manual payload

  The new Rust-owned fallback reconstruction currently restores the stable
  active repair payload subset that the snapshot already persists:
  - prompt-affecting repair input:
    - `summary`
    - `repair_targets`
    - `preserve_strengths`
  - stable guidance context:
    - `focus_areas` (trimmed, deduplicated, capped)
    - `weakest_metric_key`
    - `weakest_metric_label`
    - `weakest_metric_value`
  - stable quality-gate context:
    - `quality_gate`
    - `quality_gate_status`
    - `quality_gate_decision`
    - `quality_gate_label`
    - `quality_gate_summary`
    - `quality_gate_failed_metrics`
  - Rust also stamps the restored runtime payload with:
    - `source = recent_history_summary`
    - `source_label = Recent history summary`
    - `scope = batch`

  This is a real Python-to-Rust migration step because resumed batch runtime
  state no longer needs to collapse back to a `manual_request` seed when the
  active repair snapshot is absent. Rust now owns both:
  - the fallback restoration of executable repair prompt input
  - the fallback restoration of the richer active repair runtime payload shape
    that accompanies resumed batch execution state

  Scope guard:
  this slice only restores the stable active repair runtime payload from
  already-persisted batch snapshot quality context. It does not yet migrate
  the broader Python owner that freshly computes batch quality summaries from
  recent chapter history before generation starts.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 23:58 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume story-repair migration by
  moving the stable “snapshot quality context -> executable repair input”
  fallback behind a Rust owner, instead of leaving resumed repair restoration
  dependent on Python-only derived state when `active_story_repair_payload`
  is absent.

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now passes the loaded `batch_generation_snapshot` into the Rust resume
  preparation boundary, so resume preparation can use both:
  - workflow runtime state
  - persisted snapshot quality fields

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now extends `restore_story_repair_compat_options_from_active_snapshot(...)`
  with a Rust-owned fallback chain:
  - if request compat options already carry explicit story-repair input,
    Rust still keeps those values as highest priority
  - otherwise Rust still prefers `active_story_repair_payload` from runtime
    snapshot state
  - if that active payload is missing, Rust now falls back to persisted batch
    snapshot quality context in this order:
    - `quality_metrics_summary.repair_guidance`
    - `quality_metrics_summary.raw.repair_guidance`
    - `latest_quality_metrics.repair_guidance`
    - `latest_quality_metrics.raw.repair_guidance`
  - Rust then restores the same executable repair-input subset used by prompt
    overrides:
    - `summary`
    - `repair_targets`
    - `preserve_strengths`

  This is a real Python-to-Rust migration step because resumed batch execution
  no longer depends solely on Python-side derived repair reconstruction when
  the active repair snapshot is absent. Rust can now rehydrate prompt-affecting
  repair input directly from snapshot quality owner data that it already
  persists and reads itself, so the resumed batch prompt inputs change under a
  Rust-owned fallback contract.

  Scope guard:
  this slice only migrates the stable repair-input fallback from persisted
  quality context into resumed batch compat options. It does not yet migrate
  the broader Python owner that derives fresh batch repair state from recent
  chapter history, quality-summary aggregation, guidance derivation, and
  quality-history attachment.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 23:34 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume story-repair migration by
  moving the stable “active repair snapshot -> executable repair input” restore
  rule behind a Rust owner, instead of leaving that restoration solely inside
  Python `restore_story_repair_payload_from_active_snapshot(...)`.

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now restores story-repair input from `active_story_repair_payload` before the
  resumed batch execution input is finalized:
  - if request compat options already carry explicit
    `story_repair_summary / story_repair_targets / story_preserve_strengths`,
    Rust keeps those explicit values as the highest-priority source
  - otherwise Rust restores the same three stable fields from the active
    story-repair snapshot:
    - `summary`
    - `repair_targets`
    - `preserve_strengths`
  - the restored values are written back into the Rust-owned
    `BatchGenerationRequestRuntimeState`, so resumed batch execution reuses
    them through the same compat-options -> prompt-overrides path that normal
    batch runtime already consumes

  This is a real Python-to-Rust migration step because Python
  `restore_story_repair_payload_from_active_snapshot(...)` previously owned the
  runtime rule for turning an active repair snapshot back into executable
  repair intent. After this slice, Rust has started owning that restore rule
  for the stable repair-input subset used by resumed batch generation.

  Scope guard:
  this slice only restores the stable explicit repair-input subset from the
  active snapshot:
  - summary
  - repair targets
  - preserve strengths

  It does not yet migrate the broader Python derived-repair owner that still
  combines recent quality metrics, guidance derivation, and quality-history
  attachment into a richer resumed repair state.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 23:16 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume preparation migration by
  moving the first-remaining-chapter prerequisite gate behind a Rust owner,
  instead of leaving that stop/go rule inside Python resume preparation only.

  `backend-rs/src/services/chapter_generation_prerequisite_service.rs`
  now introduces a dedicated Rust prerequisite helper that mirrors the current
  Python `check_chapter_generation_prerequisites(...)` contract:
  - chapter 1 can always generate
  - for later chapters, Rust loads all previous chapters in chapter-number
    order
  - if any previous chapter has empty / missing content, Rust returns:
    - `can_generate = false`
    - error message `前置章节尚未完成: ... 章`
    - the ordered previous chapter list

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now uses that Rust helper during batch resume preparation:
  - after Rust derives the remaining resumable chapter slice
  - after Rust validates that the remaining chapters still exist
  - Rust now checks the first remaining chapter's prerequisite gate before
    allowing resume to proceed
  - when blocked, Rust returns
    `ResumeBatchGenerationDomainError::PrerequisitesBlocked(...)`
    with the stable detail format:
    `Resume blocked by prerequisites: ...`

  This is a real Python-to-Rust migration step because Python
  `prepare_batch_generation_resume(...)` previously owned the rule that the
  first remaining resume chapter must still satisfy chapter-generation
  prerequisite constraints before resume can be accepted.

  After this slice, Rust owns that prerequisite gate directly on the batch
  resume preparation path, immediately after remaining-slice and chapter
  existence validation.

  Scope guard:
  this slice only migrates the prerequisite stop/go rule for the first
  remaining chapter during batch resume preparation. It does not yet migrate
  the higher-level story-repair preparation branch that still resolves
  resumed repair payload and active repair snapshot state outside Rust.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prerequisite_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 22:58 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume preparation migration by
  moving the remaining-chapter existence check behind a Rust owner boundary,
  instead of leaving that validation solely inside Python resume preparation.

  `backend-rs/src/services/chapter_batch_generation_access_service.rs`
  now exposes a shared Rust helper:
  - `load_accessible_chapters_for_generation(...)`
  - it reuses the existing single-chapter access owner for each chapter id and
    returns ordered accessible chapter models for a requested resume slice

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now uses that helper before accepting a batch resume execution selection:
  - when batch resume still points at remaining chapters that no longer exist,
    or are no longer accessible, Rust now resolves the preparation boundary to
    an explicit `ResumeBatchGenerationDomainError::ChaptersUnavailable`
  - the stable user-facing detail message is
    `Some chapters no longer exist`

  This is a real Python-to-Rust migration step because Python
  `prepare_batch_generation_resume(...)` previously owned the validation that:
  - remaining chapter ids must still resolve to actual chapter rows
  - resume should fail before dispatch if some remaining chapters disappeared

  After this slice, Rust has started owning that remaining-chapter existence
  validation directly on the batch resume path, immediately after deriving the
  remaining resumable slice.

  Scope guard:
  this slice only migrates ordered chapter existence/access validation for the
  already-derived remaining batch slice. It does not yet migrate the broader
  Python prerequisite check for the first remaining chapter, nor the story
  repair state preparation that still depends on higher-level resume
  orchestration.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_semantics_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 22:44 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume migration by moving the
  first real piece of Python resume-preparation ownership into Rust: the
  decision of which remaining batch chapters are still resumable.

  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  now makes Rust own the stable `resume_start_index -> remaining_chapter_ids`
  decision for batch resume:
  - `resolve_execution_selection()` for batch tasks no longer returns the full
    original `chapter_ids` blindly
  - Rust now parses batch chapter ids once, then derives the remaining slice
    from:
    - `current_chapter_id` when it still points at a valid chapter in the
      original batch
    - otherwise `completed_chapters` as the fallback resume start index
  - when the computed start index is already beyond the last chapter, Rust
    now returns `None`, which keeps the existing `NoChaptersToResume` domain
    boundary but makes the underlying resumable-target decision Rust-owned

  This is a real Python-to-Rust migration step because Python
  `prepare_batch_generation_resume(...)` previously owned the canonical
  runtime rule for deciding the remaining batch chapter slice:
  - prefer `current_chapter_id` if it is still in the batch
  - otherwise fall back to `completed_chapters`
  - reject resume when there are no chapters left

  After this slice, Rust has started owning that resumable-slice decision
  directly inside the resume semantics boundary, which is upstream of both the
  resumed runtime dispatch path and the resumed response contract.

  Scope guard:
  this slice only migrates the stable remaining-chapter selection rule.
  It does not yet migrate the broader Python resume-preparation owner that
  still checks chapter existence / chapter loading / prerequisite validation
  before resuming execution.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_semantics_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 22:29 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume migration by moving the
  resumed runtime-checkpoint assembly itself behind a single Rust owner,
  instead of letting reset snapshot writes and resumed response payloads drift
  apart.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now exposes a dedicated Rust owner helper:
  - `build_batch_generation_resume_runtime_checkpoint(...)`
  - it starts from the shared `ResumeResetSemantics` checkpoint contract
  - then merges the Rust-owned resume runtime seed metadata into the resumed
    runtime snapshot shape
  - this keeps `resume_from_batch_id`, retry metadata, and optional
    `active_story_repair_payload` under the same checkpoint assembly boundary

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now reuses that same Rust-owned resume checkpoint when building the resumed
  response payload:
  - reset snapshot replacement and response checkpoint no longer depend on
    separate ad-hoc assembly paths
  - the resumed payload now inherits the same checkpoint contract that Rust
    persists for resume reset

  This is a real Python-to-Rust migration step because Python
  `build_resumed_batch_generation_runtime_snapshot(...)` and
  `build_resumed_batch_generation_response(...)` previously represented two
  closely related runtime/checkpoint assembly owners on the resume path.
  After this slice, Rust owns the shared checkpoint assembly contract for both
  persisted resume runtime state and resumed response payload projection.

  Scope guard:
  this slice only unifies the resumed checkpoint assembly owner inside Rust.
  It does not yet migrate the broader Python resume preparation owner that
  still decides remaining chapters / prerequisite checks / chapter existence
  validation outside the Rust seam.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 22:03 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-resume runtime-state migration
  by letting the Rust resume snapshot seed take back more of the stable
  runtime metadata that Python `build_resumed_batch_generation_runtime_snapshot(...)`
  used to imply.

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now keeps the Rust-owned resume seed contract explicit and test-covered:
  - `build_resume_runtime_state_seed(...)` always emits the stable resume
    metadata fields
    - `resume_from_batch_id`
    - `current_retry_count`
    - `max_retries`
  - when workflow snapshot state still carries
    `active_story_repair_payload`, Rust preserves it
  - when the workflow snapshot is missing that payload, Rust rehydrates the
    batch-scoped manual repair seed from request compat options
  - when there is no repair payload at all, Rust still emits the stable
    resume metadata object instead of returning `None`

  This is a real Python-to-Rust migration step because the batch resume lane
  no longer depends on Python-only snapshot construction to reintroduce these
  stable runtime-state fields. Rust now owns both:
  - the persisted resume metadata seed
  - the conditional repair-payload recovery path that rides alongside it

  Scope guard:
  this slice only tightens the Rust runtime-state owner boundary for batch
  resume snapshot seed construction. It does not yet migrate the full Python
  batch create/run initialization owner path, and it does not yet remove the
  remaining compatibility shells around broader batch orchestration.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_semantics_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 21:16 +08:00 checkpoint:
  this slice continued the same Phase 5 batch follow-up analysis migration by
  closing the remaining “started/fail only” runtime-state gap on the Rust side.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now makes the Rust-owned batch follow-up analysis snapshot lane converge on
  a real success terminal state as well:
  - introduced `build_batch_generation_analysis_completed_snapshot(...)`
  - when `execute_prepared_chapter_analysis_trigger(...)` succeeds, Rust now
    writes:
    - `analysis_task_message = 第 N 章分析完成`
    - `analysis_task_progress = 100`
    - `analysis_last_error = null`
    - stable retry metadata for the successful attempt
  - when the fallback direct analysis path
    `analyze_generated_chapter_follow_up(...)` succeeds, Rust now writes the
    same completed snapshot contract instead of returning success while leaving
    the batch runtime snapshot stuck at “analysis started”

  This is a real Python-to-Rust migration step because the batch analysis lane
  was already partially owned by Rust:
  - Rust owned analysis task creation / start metadata
  - Rust owned retry / final batch-failure contract
  - but Rust did not yet own the success-side snapshot convergence, which left
    the runtime state semantically incomplete after a successful analysis

  After this change, the Rust batch follow-up analysis lane now owns all three
  core observable phases of that seam:
  - started
  - completed
  - failed after retries

  Scope guard:
  this slice only tightens the batch follow-up analysis snapshot owner. It
  does not yet migrate the larger Python batch create/run initialization lane
  that still carries story-packet / quality-profile / story-repair runtime
  seed value in compatibility code.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 21:10 +08:00 checkpoint:
  this slice continued the same Phase 5 “real Python owner compression”
  direction by tightening the Rust-owned chapter-analysis execution boundary
  that batch follow-up analysis now depends on.

  `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  now splits the prepared-trigger execution path into:
  - `perform_prepared_chapter_analysis_trigger(...)` for the raw synchronous
    execution body
  - `execute_prepared_chapter_analysis_trigger(...)` as the true Rust owner
    boundary that always applies failure-state persistence for the analysis
    task before returning an `Err`

  That means the same Rust helper now owns the full lifecycle contract for a
  prepared analysis task across both call sites:
  - background dispatch still uses the same helper
  - batch follow-up analysis retries now also use the same helper
  - when synchronous execution fails, the analysis task is no longer left in a
    stale `running` / half-open state waiting for an outer caller to mark it
    failed

  This is a real Python-to-Rust migration step because the batch follow-up
  analysis lane was already being pulled into Rust, but its task lifecycle was
  still incomplete: the Rust retry/failure contract could fail a batch after
  three retries, yet the underlying analysis task status write-back was still
  effectively owned by the background wrapper path only. After this change,
  the prepared analysis execution boundary itself owns failure finalization,
  which removes another runtime-value reason for keeping outer compatibility
  orchestration around.

  Scope guard:
  this slice only tightens analysis-task lifecycle ownership for the prepared
  execution seam used by batch follow-up analysis and background dispatch.
  It does not yet remove the remaining Python compatibility entry shells, and
  it does not yet migrate broader batch create/run wiring that still exists
  outside the Rust runtime owner path.

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 19:11 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns analysis-driven state/relationship/organization write-back,
  while Python still owns analysis-driven career progression updates”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> career_changes` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  now extends the Rust-owned analysis character-state lane with direct career
  progression handling:
  - reads `career_changes` from each analysis `character_states` item
  - applies `main_career_stage_change` to the owned main
    `character_careers` row with `Career.max_stage` clamping
  - keeps `characters.main_career_stage` in sync with the main-career row
  - applies `sub_career_changes` by matching sub-careers through
    `careers.name + project_id + type='sub'`
  - updates both the `character_careers` stage and the character
    `sub_careers` JSON snapshot for owned sub-careers
  - handles `new_careers` by creating new `character_careers` rows
    for newly gained main/sub careers and synchronizing the character-side
    redundant fields
  - preserves the Python-side guardrails around “only one main career” and
    “at most two sub careers”, recording explicit skipped reasons instead of
    failing the whole analysis task

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/career_update_service.py` previously owned the runtime
  behavior of applying analysis-derived职业变化 into `character_careers` and
  the redundant career fields on `characters`. Before this change, Rust only
  persisted the raw `career_changes` payload inside analysis results; after
  this change, Rust has started owning the live main/sub/new career update
  path as part of the same analysis write-back boundary.

  Scope guard:
  this slice migrates the minimal high-signal analysis-driven career seam:
  - main career stage progression/regression
  - existing sub-career stage progression/regression
  - newly gained main/sub careers

  It does not yet claim parity for:
  - non-analysis Python owner paths that update careers through other entry
    points
  - broader career-system consistency repair beyond direct analysis write-back
  - any unrelated Python compatibility shells outside this runtime lane

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 19:06 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns analysis-driven character status writes, but Python still owns
  the survival-status side-effect cascade into relationships and organization
  memberships”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> survival_status cascade` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  now upgrades the Rust-owned survival-status write-back path so that
  supported analysis statuses (`deceased`, `missing`, `retired`) no longer
  stop at `characters.status/status_changed_chapter` only:
  - writes the organization-compatible display state
    `死亡/失踪/退场（第X章）` into `characters.current_state`
  - writes `state_updated_chapter` together with the status update
  - marks all active `character_relationships` involving the character as
    `status="past"` and writes `ended_at=第X章`
  - marks all active `organization_members` for the character as
    `deceased` or `retired`
  - writes the matching chapter note
    `[第X章] 角色死亡/失踪/退场`
  - keeps the existing newer-chapter guard so lower chapter analysis still
    cannot overwrite a newer status boundary

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/character_state_update_service.py` previously owned
  the runtime side effects of analysis-driven survival transitions. Before
  this change, Rust updated only the `characters` row while Python still
  owned the downstream relationship/member cascade; after this change, Rust
  has started owning that cascade as part of the same analysis write-back
  lane.

  Scope guard:
  this slice only migrates the direct survival-status cascade triggered by
  analysis output:
  - active relationship -> `past`
  - active organization membership -> `deceased/retired`

  It does not yet claim parity for:
  - non-analysis Python owner paths that may update the same tables through
    other entrypoints
  - broader consistency repair beyond direct analysis write-back
  - remaining non-analysis runtime owners elsewhere in the Python backend

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 19:03 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns analysis-driven character state, relationship, and organization
  membership write-back, while Python still owns top-level organization state
  synchronization from analysis output”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> organization_states` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  now adds a dedicated Rust-owned organization-state sync lane that:
  - reads top-level `organization_states` from the analysis payload
  - matches organization targets by organization-character name plus
    `organizations` detail record
  - applies `power_change` to `organizations.power_level`
  - applies `new_location` to `organizations.location`
  - applies `new_purpose` to `characters.organization_purpose`
  - applies `status_description` to the organization character
    `current_state/state_updated_chapter`
  - handles `is_destroyed=true` as a real destruction cascade:
    sets the organization character to `status="destroyed"`,
    writes `覆灭（第X章）`, zeros the organization power level,
    and retires all active `organization_members` with a destruction note
  - keeps skipped reasons explicit instead of failing the whole analysis task

  `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  now calls the Rust organization-state sync owner immediately after the
  existing foreshadow and character-state sync steps, preserving the same
  weak-fail boundary:
  - plot-analysis persistence remains the primary success boundary
  - organization-state sync errors are downgraded to `warn!`
  - the analysis task can still complete even if organization-state
    post-processing degrades

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/character_state_update_service.py` previously owned
  the runtime behavior of applying top-level `organization_states` into
  `characters`, `organizations`, and `organization_members`. Before this
  change, Rust only stored the raw top-level organization-state payload;
  after this change, Rust has started owning the live organization
  power/location/purpose/status update path and the destruction cascade as
  well.

  Scope guard:
  this slice migrates the minimal high-signal top-level organization-state
  seam only:
  - analysis-driven organization power/location/purpose/state updates
  - analysis-driven organization destruction cascade

  It does not yet claim parity for:
  - character survival `deceased/missing/retired` cascade into relationships
    and organization memberships
  - broader consistency repair beyond direct analysis write-back
  - any remaining Python-side non-analysis owner paths outside this lane

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 18:56 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns character state + relationship write-back from analysis, while
  Python still owns organization membership synchronization from analysis
  output”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> organization_members` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  now extends the Rust-owned analysis character-state seam with organization
  membership synchronization:
  - reads `organization_changes` from each analysis `character_states` item
  - matches organization targets by organization-character name plus
    `organizations` detail record
  - handles the core Python membership change kinds:
    `joined`, `left`, `expelled`, `betrayed`, `promoted`, `demoted`
  - updates or creates `organization_members` rows with `source="analysis"`
  - applies Rust-side loyalty keyword adjustments to mirror the Python
    loyalty-delta behavior
  - increments organization `member_count` when a new active membership is
    created from analysis output
  - keeps skipped reasons explicit instead of failing the whole analysis task

  The runtime boundary remains unchanged at the top level:
  `chapter_analysis_runtime_service.rs` already delegates post-persist
  character/relationship handling to this service, so organization membership
  sync is now part of the same Rust-owned weak-fail analysis write-back lane.

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/character_state_update_service.py` previously owned
  the runtime behavior of applying analysis-derived `organization_changes`
  into `organization_members`. Before this change, Rust only stored the raw
  analysis payload; after this change, Rust has started owning the live member
  join/leave/promote/demote update path as well.

  Scope guard:
  this slice only migrates the smallest high-signal part of the Python
  organization post-processing seam:
  - analysis-driven organization membership create/update
  - loyalty adjustments driven by change-description keywords

  It does not yet claim parity for:
  - top-level `organization_states` updates
  - organization destruction cascade
  - death/missing/retired cascade from character survival updates into
    organization membership status
  - broader consistency repair beyond direct analysis write-back

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 18:50 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns minimal `analysis -> character current_state/status` write-back,
  while Python still owns relationship synchronization from analysis output”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> character_relationships` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  now extends the Rust-owned analysis character-state seam with relationship
  synchronization:
  - reads `relationship_changes` from each analysis `character_states` item
  - normalizes both plain-string and object-shaped relationship change payloads
  - matches target characters by `target_character_name`
  - looks up existing relationships in either direction
  - updates existing relationship name / description / intimacy when the pair
    already exists
  - creates new `character_relationships` rows with `source="analysis"` when
    no relationship exists yet
  - reuses a Rust-side intimacy-delta keyword mapping to preserve the core
    Python behavior of “analysis wording influences relationship strength”

  The runtime boundary stays inside the same weak-fail owner path:
  `chapter_analysis_runtime_service.rs` already delegates post-persist
  character-state handling to this service, so relationship sync is now part
  of the Rust-owned analysis write-back lane without changing task completion
  semantics.

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/character_state_update_service.py` previously owned
  the runtime behavior of applying `relationship_changes` from analysis output
  into `character_relationships`. Before this change, Rust only persisted the
  raw `plot_analysis.character_states`; after this change, Rust has started
  owning the live relationship update/create path as well.

  Scope guard:
  this slice only migrates the smallest high-signal part of the Python
  relationship post-processing seam:
  - analysis-driven relationship update
  - analysis-driven relationship creation
  - intimacy adjustment from change-description keywords

  It does not yet claim parity for:
  - death/missing/retired cascade that marks active relationships as `past`
  - organization membership changes
  - organization state changes
  - relationship type-id inference beyond free-text relationship naming

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 18:46 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust owns `analysis -> foreshadow` post-processing only, while Python still
  owns the analysis-driven character state update boundary”
  to
  “Rust chapter analysis now also owns the minimal high-value
  `analysis -> character current_state/status` runtime post-processing seam”.

  `backend-rs/src/services/chapter_analysis_character_state_service.rs`
  is a new Rust-owned character-state post-processing seam that now:
  - normalizes `character_states` items from analysis payloads
  - matches project characters by `character_name`
  - updates `current_state` plus `state_updated_chapter` from `state_after`
  - updates `status` plus `status_changed_chapter` for supported survival
    states (`deceased` / `missing` / `retired`)
  - preserves the Python-side “newer chapter wins” guard so lower chapter
    analysis cannot overwrite a newer stored state/status
  - records skipped reasons instead of hard-failing the whole analysis task

  `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  now calls the Rust character-state sync owner immediately after
  `plot_analysis` persistence and after the foreshadow sync step. The runtime
  keeps the same weak-fail strategy used for the foreshadow migration:
  - analysis persistence remains the primary success boundary
  - character-state sync errors are downgraded to `warn!`
  - the analysis task can still complete even if character-state
    post-processing degrades

  This is a real Python-to-Rust migration step because Python
  `backend/app/services/character_state_update_service.py` previously owned
  the runtime behavior of applying analysis-derived character state changes to
  the `characters` table. Before this change, Rust only stored
  `plot_analysis.character_states`; after this change, Rust has started owning
  the first live write-back part of that runtime boundary.

  Scope guard:
  this slice only migrates the smallest high-signal part of the Python
  character-state post-processing seam:
  - `state_after -> current_state`
  - supported `survival_status -> status`

  It does not yet claim parity for:
  - analysis-driven relationship updates
  - organization membership changes
  - organization state changes
  - cascade effects when a character becomes deceased / missing / retired

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_character_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 18:40 +08:00 checkpoint:
  this slice advanced the Rust chapter-analysis lane from
  “Rust only persists `plot_analysis` rows while Python still owns the
  analysis-driven foreshadow post-processing step”
  to
  “Rust chapter analysis now also owns a real
  `analysis -> foreshadow_reminders` runtime post-processing seam for the
  minimal high-value path”.

  `backend-rs/src/services/foreshadow_service.rs`
  now upgrades `sync_from_analysis(...)` from a placeholder success response
  into a real Rust owner for the minimal analysis foreshadow sync contract:
  - accepts `chapter_id`, `chapter_number`, and `analysis_foreshadows`
  - creates or updates analysis-planted foreshadow rows using a stable
    `chapter_id + normalized content` key
  - resolves planted foreshadows by exact
    `reference_foreshadow_id` matching when analysis marks them as resolved
  - records skipped reasons instead of failing hard on unsupported or
    incomplete items

  `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  now calls the Rust foreshadow sync owner immediately after
  `plot_analysis` persistence. The runtime keeps Python-compatible weak-fail
  semantics:
  - analysis persistence remains the primary success boundary
  - foreshadow sync errors are downgraded to `warn!` log events
  - the analysis task can still complete even if foreshadow post-processing
    does not fully succeed

  `backend-rs/src/services/foreshadow_request_service.rs`
  now exposes a minimal constructor/accessor for
  `SyncForeshadowFromAnalysisRouteRequest` so the analysis runtime can build
  the route-compatible sync payload without reopening the request-body
  contract in multiple places.

  This is a real Python-to-Rust migration step because Python
  `backend/app/api/memories.py` previously owned the runtime behavior of
  “persist plot analysis, then automatically update foreshadow reminders from
  the analysis output”. Before this change, Rust stopped at inserting
  `plot_analysis`; after this change, Rust has started owning the same runtime
  post-processing boundary instead of leaving that owner entirely in Python.

  Scope guard:
  this slice only migrates the smallest high-signal part of the Python
  post-processing seam:
  - analysis `planted` foreshadow upsert
  - analysis `resolved` foreshadow exact-id resolve

  It does not yet claim parity for:
  - Python content-fallback matching for resolved foreshadows
  - analysis-driven character state updates
  - organization / relation / wider memory post-processing chains

  Focused validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_analysis_runtime_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  foreshadow_request_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  foreshadow_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 18:25 +08:00 checkpoint:
  this slice advanced the Rust single-chapter background lane from
  “single background generation builds a real Rust provider payload only at
  task creation time”
  to
  “single background generation now also persists the Rust runtime request
  state needed for resume, so resumed execution can rebuild the same Rust-owned
  provider/prompt semantics instead of silently falling back to defaults”.

  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  now seeds the batch-generation snapshot used by single background tasks with
  the same Rust-owned runtime request state shape that batch generation resume
  already consumes:
  - `compat_options` are captured from the prepared single-chapter execution
    input
  - `model_override` is persisted alongside them
  - the pending checkpoint payload is merged with
    `batch_request_runtime_state` before the snapshot is written

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now exposes `batch_generation_runtime_state_payload(...)` for reuse so single
  and batch background lanes serialize the same resume state shape rather than
  drifting into parallel implementations.

  This is a real Python-to-Rust migration step because the previous Rust single
  background lane only owned the provider payload at create-time. Once the task
  moved into the persisted/resumable runtime path, it could lose web research,
  prompt overrides, and model override semantics because resume parsing only
  knew how to restore the batch-owned runtime state schema. After this change,
  resumed single background generation can stay on the Rust-owned provider
  payload rebuild path instead of degrading to default runtime semantics.

  Scope guard:
  this slice closes the persisted resume-state seam for single background
  chapter generation only. It does not yet introduce a dedicated single-task
  snapshot schema; it deliberately reuses the existing batch runtime-state
  contract so the resume path stays unified.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 17:02 +08:00 checkpoint:
  this slice advanced the Rust chapter-memory lane from
  “Rust only writes SQL `story_memories` rows while Python still owns vector
  memory add/delete/search behavior”
  to
  “Rust now owns a real vector-memory index seam for the research lane and has
  started using it for write/delete/search runtime behavior”.

  `backend-rs/src/services/story_memory_vector_index_service.rs`
  is a new Rust-owned memory-vector seam that now:
  - persists project-scoped vector-memory records under the Rust-side runtime
    data directory instead of leaving all vector-memory ownership in Python
  - builds embeddings through the Rust AI/settings owner when possible
    (`/embeddings` via the current OpenAI-compatible config), with a stable
    fallback embedding path when provider-native embedding support is not
    available
  - supports project/chapter/type-scoped delete operations and a minimal
    cosine-similarity search path

  `backend-rs/src/services/chapter_generation_research_payload_service.rs`
  now upgrades the existing Rust `replace_chapter_research_memories(...)`
  seam from
  “delete SQL rows + insert SQL rows”
  to
  “delete SQL rows + delete vector records + insert SQL rows + upsert vector
  records”.

  This matters because Python `chapter_web_research_service.replace_memories()`
  does not stop at relational persistence; it also calls
  `memory_service.delete_memories_by_types(...)` and
  `memory_service.add_memory(...)`.
  After this change, Rust chapter research memory replacement no longer leaves
  the vector-memory side entirely in Python.

  `backend-rs/src/services/memories_write_workflow_service.rs`
  now removes chapter-scoped vector-memory records together with SQL
  `story_memories` rows when deleting chapter memories, instead of leaving
  vector records behind.

  `backend-rs/src/services/memories_query_service.rs`
  now starts using the Rust-owned vector index for
  `search_owned_project_memories_payload(...)`:
  - when a search query is present, Rust first attempts vector-memory
    similarity search
  - matching IDs are then resolved back through the existing `story_memories`
    table so response shape stays stable
  - if the vector lane has no hits, the code falls back to the existing SQL
    `LIKE` search path to preserve compatibility

  This is a real Python-to-Rust migration step because the Rust backend now
  owns part of the runtime behavior that Python `memory_service` previously
  owned alone:
  - vector-memory write during chapter research replacement
  - vector-memory delete during chapter memory cleanup
  - vector-backed search entry for memory retrieval

  Scope guard:
  this slice is still a minimal parity cut, not a full Python memory stack
  replacement. Rust does not yet claim parity for:
  - the full Python Chroma collection model
  - all memory categories beyond the currently migrated research lane
  - every higher-level retrieval heuristic in Python memory context assembly

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  story_memory_vector_index_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_research_payload_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  memories_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  memories_query_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  memories -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value real migration cut should continue pushing Python
  `memory_service` ownership into Rust rather than returning to wrapper-level
  cleanup:
  - expand vector-memory ownership beyond the research lane into more memory
    categories that still depend on Python add/delete/search behavior
  - or migrate higher-level context/retrieval consumers so they read the Rust
    vector-memory lane directly instead of relying on SQL fallback semantics

- 2026-05-29 16:18 +08:00 checkpoint:
  this slice advanced the Rust batch-generation lane from
  “batch create/runtime only carries a shared placeholder provider payload”
  to
  “batch generation now persists request compat state and rebuilds a real
  per-chapter research provider payload at runtime and resume time”.

  `backend-rs/src/api/chapter_batch_generation.rs`
  now passes the already-exposed batch route compat fields through to the Rust
  workflow request instead of dropping them at the API boundary.

  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  now:
  - extends `BatchGenerationCreateWorkflowRequest` to own the Python-compatible
    batch inputs needed by the research seam, including
    `enable_web_research`, `web_research_query`, `creative_mode`,
    `story_focus`, `plot_stage`, `story_creation_brief`,
    `quality_*`, and repair/preserve lists
  - derives a serializable `BatchGenerationRequestRuntimeState` from those
    request fields plus the resolved web-research default
  - stores that runtime state into the batch snapshot at create time
  - reloads the same runtime state on resume so model override and compat
    semantics are not lost when the task is restarted

  `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs`
  now preserves non-checkpoint runtime metadata when resetting a task for
  resume, instead of replacing the whole `workflow_runtime_state` payload with
  a checkpoint-only object.

  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  no longer reuses one shared batch-level `provider_payload` for every chapter.
  The runtime now:
  - carries `SingleChapterGenerationCompatOptions` in
    `BatchGenerationExecutionInput`
  - rebuilds a fresh `SingleChapterGenerationTarget` for each chapter during
    execution
  - calls `build_single_chapter_research_provider_payload(...)` per chapter
    before `generate_and_persist_chapter_content_with_provider_payload(...)`
  - fails the batch step explicitly if chapter-scoped research payload
    preparation fails

  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now forwards the restored compat options into both resumed single-chapter and
  resumed batch execution paths, removing the old `SingleChapterGenerationCompatOptions::default()`
  fallback for resumed batch-owned single chapter tasks.

  This is a real Python-to-Rust migration step because Python batch generation
  already performs chapter-scoped research collection/memory replacement before
  each chapter prompt is built. After this change, Rust batch generation no
  longer fakes the seam with a shared placeholder payload; it now owns the same
  per-chapter runtime boundary and keeps that boundary reconstructable across
  resume.

  Scope guard:
  this slice closes the batch research payload ownership seam only. It still
  does not claim parity for Python vector-memory service write-back
  (`memory_service.add_memory / delete_memories_by_types`) or the remaining
  non-generation backend surfaces.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_snapshot_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value real migration cut should stay inside runtime parity
  rather than compatibility cleanup:
  - add vector-memory parity behind the same Rust-owned research seam
  - or continue migrating remaining Python generation-adjacent lanes that still
    bypass the Rust research/memory owner

- 2026-05-29 15:19 +08:00 checkpoint:
  this slice advanced the Rust regeneration lane from
  “partial regeneration only carries a web-research note”
  to
  “partial regeneration now also reuses the Rust-owned chapter research
  provider payload and injects real external/reference assets into the
  rewrite prompt”.

  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`
  now:
  - adapts `PartialRegenerationStreamWorkflowRequest` into
    `SingleChapterGenerationCompatOptions` with the same settings fallback
    semantics used by the single-chapter and full-regeneration Rust lanes
  - reuses
    `build_single_chapter_research_provider_payload(...)`
    for partial regeneration instead of keeping a note-only compatibility path
  - upgrades `build_partial_regeneration_prompt(...)` and
    `prepare_partial_regeneration_input(...)` to surface
    `external_assets/reference_assets` alongside the existing research note

  This is a real Python-to-Rust migration step because Python partial
  regeneration already consumes chapter web research as prompt grounding
  instead of a mere flag. After this change, the Rust partial regeneration
  lane no longer stops at “联网说明”; it now shares the same Rust-owned
  research collector/archive/memory seam already used by single-chapter
  generation and full regeneration.

  Scope guard:
  this slice upgrades prompt/runtime research grounding for partial
  regeneration, but it still does not claim Python vector-memory service
  parity. The underlying persistence remains the Rust-side archive +
  SQL `story_memories` write-back seam.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_regeneration_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_regeneration_stream_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value real migration cut should leave “regeneration prompt
  grounding parity” and move deeper into remaining Python runtime ownership:
  - add true vector-memory parity behind the same Rust research owner
  - or migrate batch-generation research collection/memory seams onto the same
    Rust-owned provider path

- 2026-05-29 15:24 +08:00 checkpoint:
  this slice advanced the Rust regeneration lane from
  “full chapter regeneration accepts generic rewrite inputs only”
  to
  “full chapter regeneration now reuses the Rust-owned single-chapter
  research seam and can carry real web-research context into rewrite prompts”.

  `backend-rs/src/api/chapter_regeneration_routes.rs`
  now formally accepts `enable_web_research` and `web_research_query` for the
  full regeneration stream route, instead of keeping those fields limited to
  partial regeneration and single-chapter generation lanes.

  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`
  now:
  - extends `FullChapterRegenerationStreamRequest` to own
    `enable_web_research` and `web_research_query`
  - adapts that request into
    `SingleChapterGenerationCompatOptions` with the same settings fallback
    semantics already used by the single-chapter Rust lane
  - reuses
    `build_single_chapter_research_provider_payload(...)`
    with a regeneration-derived `SingleChapterGenerationTarget`
  - injects the resulting `external_assets/reference_assets` and a
    web-research note directly into the full regeneration prompt owner

  This is a real Python-to-Rust migration step because the Python regeneration
  context path already consumes chapter web research as live runtime input.
  After this change, Rust full regeneration no longer stops at a passive
  request flag; it now consumes the same Rust-owned research collector/archive
  seam that single-chapter generation uses, and passes the resulting research
  payload into the rewrite prompt path.

  Scope guard:
  this slice upgrades the full regeneration lane only. Partial regeneration in
  Rust still uses a lighter “web research note” path and does not yet consume
  the full provider payload / asset injection contract.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_regeneration_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_regeneration_routes -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value real migration cut should continue the same
  regeneration seam:
  - migrate partial regeneration from “web research note” to the same Rust
    provider payload / asset contract
  - or push the Rust-owned research memory seam toward vector-memory parity

- 2026-05-29 15:09 +08:00 checkpoint:
  this slice advanced the single-chapter Rust research lane from
  “real Exa collection + archive write + SQL chapter research memory write-back”
  to
  “single-chapter Rust now owns a real Exa + Grok provider fan-in cut behind
  the same research payload / archive / memory owner”.

  `backend-rs/src/services/chapter_generation_research_payload_service.rs`
  no longer stops at Exa-only collection. The Rust owner now:
  - composes both the existing single-chapter Exa query and a Grok-oriented
    real-time research query from the same request/chapter/story context seam
  - resolves Grok credentials, base URL, and model from Rust settings storage
  - reuses the existing Rust `OpenAIClient` as an OpenAI-compatible Grok
    transport instead of adding a parallel HTTP client stack
  - parses Grok JSON research summaries and source lists into the same
    prompt/runtime `assets` contract already used by the Exa lane
  - persists the merged provider result through the existing archive writer and
    SQL `story_memories` write-back seam
  - carries provider/query diagnostics in `mcp_references`, so downstream
    runtime owners can inspect which provider payloads contributed to the
    final research context

  This is a real Python-to-Rust migration step because the single-chapter Rust
  lane now owns both provider branches of the Python chapter research surface
  instead of only one external provider plus compatibility placeholders.
  It still does not claim parity with Python’s vector-memory service
  write-back (`memory_service.add_memory / delete_memories_by_types`), but it
  closes another concrete runtime responsibility inside the live chapter
  generation path.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_research_payload_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value real migration cut should stay on “Python runtime
  ownership, not cosmetic seams”:
  - add true vector-memory parity behind the same research owner
  - or reuse this Rust-owned research collector/memory seam in the chapter
    regeneration lane

- 2026-05-29 15:00 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from
  “real Exa collection + archive write” to
  “real Exa collection + archive write + chapter research memory write-back”.

  `backend-rs/src/services/chapter_generation_research_payload_service.rs`
  now owns a minimal Rust-side `replace_chapter_memories` parity cut for the
  single-chapter research lane. After collecting and normalizing Exa assets,
  the Rust owner now:
  - deletes existing `story_memories` rows for the same
    `project_id + chapter_id + memory_type=research_reference`
  - inserts the new research memories into the existing `story_memories` table
  - records `query`, `archive_path`, and the original asset payload into
    `full_context`
  - preserves the same memory-type identity (`research_reference`) used by the
    Python chapter research path

  This intentionally stops at the SQL / ORM memory layer and does not yet
  claim parity with Python’s vector-memory service write-back
  (`memory_service.add_memory / delete_memories_by_types`). The migration
  completed here is the first real Rust-owned chapter research memory
  persistence seam, not the full hybrid memory stack.

  Why this counts as a real migration step:
  the single-chapter Rust lane now not only collects external research and
  injects it into prompt/runtime payloads, but also persists chapter-scoped
  research references into the backend memory table that other backend flows
  can query. That closes another concrete Python runtime responsibility
  instead of only restructuring compatibility code.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_research_payload_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value migration cut should remain inside the same
  single-chapter research parity lane:
  - add the vector-memory service write-back parity
  - or add the Grok provider path behind the same collector / memory owner

- 2026-05-29 14:57 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from
  “runtime research payload owner + settings-aware enablement parity”
  to “single-chapter Rust now owns a real external research asset collection
  cut for the Exa lane”.

  `backend-rs/src/services/chapter_generation_research_payload_service.rs`
  was upgraded from a pure query-composition helper into a real single-chapter
  research owner. It now:
  - resolves the effective web-research settings from Rust settings storage
  - composes the single-chapter research query from request/chapter/story
    context
  - calls the Exa search API through Rust `reqwest`
  - normalizes Exa search results into prompt/runtime `assets`
  - writes a research archive JSON file under the Rust-side
    `data/web_research/<project_id>/<chapter_id>.json` path
  - returns a `PromptContextProviderPayload` carrying real
    `research_assets/external_assets/reference_assets`

  `backend-rs/src/services/settings_service.rs` now exposes
  `resolve_web_research_settings(...)` so the research owner can read the
  merged `web_research` preference payload once without duplicating settings
  parsing logic.

  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  now awaits the async research owner directly while building the prepared
  single-chapter execution input. This means the single-chapter Rust lane can
  perform real external research collection before runtime execution instead of
  only injecting a semantic note or query placeholder into the prompt path.

  Scope guard:
  this is still a deliberately small migration cut. Rust now owns a real Exa
  collection path plus archive write for the single-chapter lane, but it does
  not yet claim parity for:
  - Grok search collection
  - research-memory replacement / StoryMemory write-back
  - Python’s broader multi-lane reuse across wizard / outline /
    regeneration flows

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_research_payload_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next highest-value migration cut should stay in the same Rust-owned
  research lane:
  - add StoryMemory / memory write-back parity for collected chapter research
  - or add the Grok provider path behind the same collector boundary

- 2026-05-29 15:03 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from
  “request-level web-research runtime payload exists” to
  “single-chapter Rust now also matches the Python settings-fallback
  semantics for web-research enablement”.

  In Python, the single-chapter generation path does not hardcode
  `enable_web_research` to false when the request omits that field; it falls
  back to the user-level web-research setting through the research service
  runtime config. Rust previously diverged here because
  `SingleChapterGenerationRequest` normalized a missing
  `enable_web_research` to `false` at the prepare owner boundary, which meant
  the newly introduced Rust research payload owner would stay disabled even
  when the user settings default enabled web research.

  `backend-rs/src/services/settings_service.rs` now exposes a narrow
  `resolve_web_research_enabled(...)` owner helper that reads the merged
  `web_research` preference surface and resolves the effective
  `web_research_enabled` default without widening the rest of the settings
  contract.

  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  now uses that settings-owned default while normalizing
  `SingleChapterGenerationCompatOptions`, so the single-chapter Rust lane
  behaves like Python in the important compatibility case:
  request flag wins when explicitly present, otherwise the user settings
  default drives whether the Rust research payload owner activates.

  This is a real Python-to-Rust migration step because it removes a concrete
  runtime behavior mismatch in the live single-chapter generation path rather
  than only reshaping internal structs. After the earlier runtime research
  payload seam was introduced, this fallback behavior became part of the real
  migration surface and is now aligned on the Rust side.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  settings_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next high-value migration cut should keep following real runtime
  parity inside the single-chapter research lane. The best next candidates
  are:
  - introduce a Rust-owned external research asset collector / archive writer
    behind the existing payload contract
  - or migrate the chapter-regeneration research seam onto the same
    settings-aware research payload owner

- 2026-05-29 14:49 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from “request-level
  web-research compatibility fields affect the live prompt text” to
  “single-chapter Rust now owns a real runtime research payload contract”.

  `backend-rs/src/services/chapter_generation_prompt_context_provider_service.rs`
  now expands `PromptContextProviderPayload` beyond the old placeholder-only
  trio and explicitly carries:
  `research_query`, `research_assets`, `external_assets`,
  `reference_assets`, and `mcp_references`.
  This turns the provider payload into the owner contract for runtime
  research context instead of leaving web-research semantics trapped in
  route/request metadata and prompt-local notes.

  `backend-rs/src/services/chapter_generation_research_payload_service.rs`
  is the new single-chapter Rust owner for the minimal research payload seam.
  It composes a request-scoped `research_query` from either the explicit
  compatibility query or chapter/story context, and it materializes a
  compatibility-safe `external_assets/reference_assets` payload summary from
  that query without claiming external provider parity.

  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  now builds the single-chapter research provider payload at the prepare
  boundary and passes it through
  `prepare_generation_execution_config_with_provider_payload(...)`, so the
  request/workflow preparation boundary owns provider-payload construction
  once and downstream runtime/prompt boundaries only consume that explicit
  contract.

  `backend-rs/src/services/chapter_generation_prompt_service.rs`
  now consumes the provider payload’s research/runtime fields as real prompt
  params, including `external_assets`, `reference_assets`,
  `mcp_references`, and `quality_external_assets_block`. This means the
  single-chapter Rust lane no longer only knows that web research was
  requested; it now has a Rust-owned runtime payload that can be injected into
  the existing chapter-generation template surface.

  Scope guard:
  this slice still does not claim parity with Python
  `chapter_web_research_service` external asset collection, provider fanout,
  archive persistence, or memory replacement flows. The migration completed
  here is the runtime payload owner seam for single-chapter generation, not
  the full external research pipeline.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prompt_context_provider_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_research_payload_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prompt_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next high-value single-chapter Rust migration should build on this
  runtime payload owner rather than returning to request-only cleanup. The
  best next cuts are:
  - connect this owner to a real Rust-side external research asset collector
    when the slice can stay isolated from batch/wizard/global policy churn
  - or migrate the adjacent Python chapter regeneration research/runtime seam
    onto the same provider-payload contract

- 2026-05-29 14:37 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from “story-repair
  diagnostics affect the live prompt” to “the single-chapter web-research
  compatibility surface now also affects live Rust prompt semantics”.

  `backend-rs/src/services/chapter_generation_prompt_service.rs` now extends
  `ChapterGenerationPromptOverrides` with
  `web_research_enabled` and `web_research_query`, and the prompt owner now
  resolves a real `web_research_block` plus `web_research_query` contract.
  The current compatibility-safe implementation injects the web-research note
  into the existing prompt path by composing it into the prompt-owner block
  flow, so the single-chapter Rust generation lane now visibly consumes the
  request-level web-research toggle instead of carrying it as inert metadata.

  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  now maps `SingleChapterGenerationCompatOptions.web_research_enabled /
  web_research_query` into the shared prompt-owner override contract through
  the same explicit compat-to-prompt helper used by the other migrated single
  generation preferences.

  This is a real Python-to-Rust migration step rather than another transport
  seam because the legacy Python single-generation compatibility surface now
  changes the live Rust generation prompt for a second semantic lane beyond
  style/repair preferences.

  Scope guard:
  this slice intentionally does not claim parity with the Python
  `chapter_web_research_service` asset-collection / memory-write pipeline.
  Rust now owns the request-level semantic note that the chapter should
  incorporate web research, but external research asset collection remains a
  future cut if a dedicated Rust owner is introduced.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prompt_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next single-chapter Rust migration should prefer one of two explicit
  directions instead of reopening generic seam cleanup:
  - introduce a real Rust-owned external research asset owner, if the slice
    can stay independent from global settings/runtime policy churn
  - continue migrating additional Python single-generation semantics that can
    be consumed entirely inside the current prompt/runtime owner boundary

- 2026-05-29 14:24 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from “request-level
  generation preferences affect the live prompt” to “request-level
  story-repair diagnostics now also affect the live prompt-owner contract”.

  `backend-rs/src/services/chapter_generation_prompt_service.rs` now extends
  `ChapterGenerationPromptOverrides` with:
  `story_repair_summary`, `story_repair_targets`, and
  `story_preserve_strengths`.
  The single-chapter prompt owner now injects the following template params
  from the Rust-owned request override contract:
  `story_repair_summary`, `story_repair_targets`,
  `story_preserve_strengths`, `story_repair_target_block`, and
  `story_repair_diagnostic_block`.

  This is a real Python-to-Rust migration step rather than another transport
  seam because the Rust prompt owner now consumes a second high-value subset
  of the historical Python `ChapterGenerateRequest` compatibility surface in
  real generation semantics, instead of merely preserving those fields as
  metadata.

  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  now centralizes `SingleChapterGenerationCompatOptions ->
  ChapterGenerationPromptOverrides` mapping through an explicit helper and
  carries `story_repair_summary`, `story_repair_targets`, and
  `story_preserve_strengths` into the shared runtime generation owner. This
  keeps the single-chapter prepare -> runtime -> prompt lane aligned on one
  Rust-owned contract.

  Focused validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prompt_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next single-chapter Rust migration should keep following live
  semantics, not return to seam-only flattening. The best next candidate is
  `enable_web_research / web_research_query` ownership if it can be wired as
  an explicit runtime/provider-side contract without dragging unrelated global
  policy into the same slice.

- 2026-05-29 14:17 +08:00 checkpoint:
  this slice advanced the single-chapter Rust lane from “compatibility fields
  are carried through runtime input” to “a high-value subset of Python single
  generation preferences now affects real Rust generation semantics”.

  `backend-rs/src/services/chapter_generation_prompt_service.rs` now defines
  `ChapterGenerationPromptOverrides` as the prompt-owner contract for
  request-level generation preferences. The chapter prompt owner now resolves
  prompt-facing values by priority:
  1. single-chapter request override
  2. project default preference
  3. existing empty/default fallback

  The Rust prompt lane now explicitly injects:
  `narrative_perspective`, `creative_mode`, `story_focus`, `plot_stage`,
  `story_creation_brief`, `quality_preset`, and `quality_notes`
  into the chapter-generation template params, including the optional
  `creative_mode_block`, `story_focus_block`, and
  `story_creation_brief_block` prompt sections.

  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  now maps `SingleChapterGenerationCompatOptions` into
  `ChapterGenerationPromptOverrides` before calling the shared runtime
  generation owner, so a real subset of the Python
  `ChapterGenerateRequest` compatibility surface now influences the generated
  prompt instead of only being preserved as metadata.

  The shared runtime owner change was kept compatibility-safe for the batch
  lane:
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  now passes `ChapterGenerationPromptOverrides::default()` so batch generation
  behavior stays unchanged while the single-chapter lane gains request-level
  prompt ownership.

  This is a real Python-to-Rust migration step rather than seam-only cleanup
  because single-chapter Rust generation now consumes live chapter-generation
  preference semantics that previously only existed in the Python request
  model and compatibility shell.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_prompt_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next single-chapter Rust migration should continue from this semantic
  lane instead of returning to transport-only cleanup. The best next
  candidates are:
  - `story_repair_summary / story_repair_targets / story_preserve_strengths`
    prompt ownership
  - `enable_web_research / web_research_query` ownership, if it can be wired
    through an explicit provider-side contract without dragging unrelated
    settings/runtime policy into the same slice

- 2026-05-29 14:07 +08:00 checkpoint:
  this slice moved the single-chapter Rust lane from “route/request accepts the
  Python compatibility fields” to “prepare/runtime owners explicitly carry a
  Rust-owned compatibility contract”.

  `backend-rs/src/services/chapter_single_generation_prepare_service.rs` now
  defines `SingleChapterGenerationCompatOptions` as the prepare-owner
  projection for the Python `ChapterGenerateRequest` compatibility surface.
  Instead of leaving the legacy single-generation fields as inert struct
  members on `SingleChapterGenerationRequest`, the prepare owner now
  normalizes and owns them through explicit helper semantics:
  - boolean defaults are resolved for `enable_analysis`, `enable_mcp`, and
    `enable_web_research`
  - string/list compatibility fields such as `creative_mode`, `story_focus`,
    `plot_stage`, `story_creation_brief`, `quality_preset`, `quality_notes`,
    `story_repair_summary`, `story_repair_targets`, and
    `story_preserve_strengths` are projected into one reusable owner contract
  - `SingleChapterGenerationExecutionInput` now carries that compat contract
    alongside the runtime execution config instead of dropping it before the
    workflow/runtime boundary

  Downstream single-generation owners were updated to keep the same contract
  alive across the Rust lane:
  `chapter_single_generation_stream_workflow_service.rs`,
  `chapter_single_generation_write_workflow_service.rs`, and
  `chapter_single_generation_runtime_state_service.rs` now build and preserve
  the explicit compat-owned execution input contract in tests and launch
  wiring.

  The adjacent batch resume seam was also brought into the same contract:
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  now initializes the single-chapter resume branch with the same
  `SingleChapterGenerationCompatOptions` owner instead of constructing an
  older, narrower single-generation execution input shape.

  This is a real Python-to-Rust migration step rather than another internal
  seam flatten because the Rust single-chapter path now has an explicit owner
  for the legacy Python request compatibility surface after the route boundary,
  and that owner is preserved through prepare -> workflow -> runtime ->
  resume(single) instead of being silently accepted and then discarded.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_prepare_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_stream_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`

  Updated stop-rule assessment:
  the next single-generation Rust migration should stop treating these compat
  fields as transport-only metadata and start selectively consuming the highest
  value subset in real generation semantics, with `web_research` and
  `creative_mode/story_focus/quality_preset` as the best next candidates.

- 2026-05-29 13:54 +08:00 checkpoint:
  this slice moved the single-chapter Rust route closer to the Python
  compatibility surface by expanding
  `backend-rs/src/api/chapter_generation_routes.rs`
  to accept the broader `ChapterGenerateRequest`-style route payload shape.

  The Rust single-generation route now explicitly accepts the legacy
  single-generation compatibility fields that are still present in the Python
  request schema, including:
  `style_id`, `enable_analysis`, `enable_mcp`, `enable_web_research`,
  `web_research_query`, `narrative_perspective`, `creative_mode`,
  `story_focus`, `plot_stage`, `story_creation_brief`, `quality_preset`,
  `quality_notes`, `story_repair_summary`, `story_repair_targets`, and
  `story_preserve_strengths`.

  The current Rust owner still only forwards the already-supported owned
  fields (`target_word_count` and `model`) into the existing
  single-generation prepare/write/stream pipeline, but the route boundary now
  explicitly owns the compatibility acknowledgement instead of simply omitting
  most of the Python input surface.

  This is a real Python-to-Rust migration step rather than internal seam
  cleanup because the Rust route is now closer to being a drop-in transport
  replacement for the Python `ChapterGenerateRequest` entrypoint, which
  reduces the gap between “Rust has the core behavior” and “Rust can actually
  receive the legacy client payload shape”.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_routes -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_error_mapper -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapters -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the next single-generation migration moves should prefer turning these newly
  acknowledged compatibility fields into explicit Rust-owned request semantics
  or documented compat no-ops, rather than leaving them as silent transport
  placeholders indefinitely.

- 2026-05-29 13:51 +08:00 checkpoint:
  this slice continued the single-chapter route-group migration by removing
  two remaining mixed boundaries with the batch-generation lane:
  - introduced `backend-rs/src/api/chapter_generation_error_mapper.rs` so
    single-generation route errors no longer depend on the batch-generation
    error-mapper module
  - deleted the empty legacy placeholder
    `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
    after the real single-generation write/stream owners were already split
    out

  The single-chapter Rust lane is now cleaner at the ownership-map level:
  route group, route error mapping, write workflow, stream workflow, and
  runtime each have explicit single-generation owners instead of borrowing
  batch modules or keeping a fake legacy mixed-workflow shell alive.

  This is a real Python-to-Rust migration step rather than internal seam
  cleanup because it reduces the remaining conceptual overlap between the
  single-generation Rust route group and the batch-generation Rust route group,
  making the legacy Python `chapter_generation_routes.py` compatibility shell
  easier to compare against a dedicated Rust owner stack without batch-owned
  detours.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_error_mapper -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_routes -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_error_mapper -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the single-generation Rust lane is now largely detached from batch route
  ownership at the module level. Continue here only if the next move removes a
  remaining Python compatibility shell or external routing dependency; do not
  reintroduce batch-owned adapters for single-generation transport errors or
  workflow entrypoints.

- 2026-05-29 13:47 +08:00 checkpoint:
  this slice moved the single-chapter generation route group into its own Rust
  route owner by introducing `backend-rs/src/api/chapter_generation_routes.rs`
  and removing the single-generation endpoints from
  `chapter_batch_generation.rs`.

  `chapters.rs` now merges a dedicated `chapter_generation_routes::routes()`
  module, so Rust routing structure more closely matches the historical Python
  route-group split where `chapter_generation_routes.py` was distinct from
  batch-generation routes. Batch generation remains in
  `chapter_batch_generation.rs`, while single-chapter background/stream entry
  points now terminate in their own route owner and continue to delegate into
  the previously separated Rust write/stream workflow owners.

  This is a real Python-to-Rust migration step rather than internal seam
  cleanup because it upgrades the Rust side from “single-generation behavior
  exists somewhere under the batch route group” to an auditable dedicated route
  module aligned with the legacy Python ownership map, which makes route-group
  cutover and compatibility-shell retirement materially easier.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_generation_routes -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapters -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the single-generation Rust lane now has explicit route-group, write, stream,
  and runtime owners. Continue here only if the next move further reduces a
  concrete Python compatibility shell or aligns the remaining route-group
  ownership map; do not blur single-generation back into batch route modules.

- 2026-05-29 13:43 +08:00 checkpoint:
  this slice moved the single-chapter stream path into its own Rust stream
  workflow owner by introducing
  `chapter_single_generation_stream_workflow_service.rs`.

  The chapter-generation route now delegates stream generation to
  `create_single_generation_stream_workflow(...)` from the new stream owner,
  while the earlier single-chapter background write path remains in
  `chapter_single_generation_write_workflow_service.rs`. The previous
  `chapter_single_generation_workflow_service.rs` is now reduced to a legacy
  placeholder module rather than a mixed stream/write owner.

  This is another real Python-to-Rust migration step rather than seam-only
  cleanup because the single-generation lane now has explicit Rust owners for
  both write-side orchestration and stream-side orchestration, making the
  remaining Python compatibility route shell easier to reason about, compare,
  and eventually retire without one ambiguous Rust workflow module carrying
  both responsibilities.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_stream_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the single-chapter generation lane now has explicit Rust owners for
  `prepare -> write workflow -> stream workflow -> runtime`. Continue here
  only if the next move removes another concrete compatibility-shell or
  ambiguous owner boundary; do not rebuild a generic mixed workflow module
  after this split.

- 2026-05-29 13:39 +08:00 checkpoint:
  this slice moved the single-chapter background write path into its own Rust
  write-workflow owner by introducing
  `chapter_single_generation_write_workflow_service.rs`.

  The chapter-generation route now delegates background task creation to
  `start_owned_single_generation_background_write_workflow(...)`, while
  `chapter_single_generation_workflow_service.rs` narrows to the stream-side
  launch boundary only. The new write-workflow owner keeps the same prepared
  request loading, pending checkpoint persistence, task active-model insert,
  runtime snapshot upsert, and background runtime dispatch contract.

  This is a real Python-to-Rust migration step rather than seam-only
  flattening because it makes the Rust side own a clearer single-chapter write
  orchestration boundary analogous to the existing batch write workflow,
  reducing dependence on the historical Python write-side orchestration shape
  and making future compatibility-shell retirement easier to audit.

  Validation passed with:
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_write_workflow_service -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target';
  cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the single-chapter lane now has a clearer split between stream workflow and
  write workflow. Continue here only if the next move further concentrates a
  true single-generation owner boundary, such as prepare/write/runtime
  responsibilities, without re-mixing stream orchestration and write-side task
  creation into one service again.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the private `BatchGenerationExecutionInput::new(...)`
  constructor from `chapter_batch_generation_runtime_state_service.rs` and
  updated both runtime dispatch and test call sites to build the input
  struct directly.

  The runtime-state lane now owns less wrapper code while preserving the same
  dispatch contract, chapter-id ordering, target word count, and prepared
  execution config flow.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the runtime-state lane is thinner now, but the remaining code is real
  dispatch logic and state mutation. Continue only if another adjacent helper
  still wraps the same contract; do not keep flattening already-direct
  runtime state assembly for symmetry alone.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the private `BatchGenerationReadContext::new(...)`
  constructor and moved the snapshot-to-context assembly directly into
  `load_batch_generation_read_context_for_task(...)`.

  The read-context loader now owns the full construction boundary in one
  place, while the tests instantiate `BatchGenerationReadContext` directly
  with the same snapshot-derived runtime and quality-status fields. This
  keeps read-side payloads, optional snapshot semantics, and terminal status
  projection unchanged while removing one more thin internal constructor
  shell.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the read-context lane is thinner now, but the remaining public helpers are
  real loaders/projections rather than fake-shared wrappers. Continue only if
  another adjacent helper still wraps the same contract; do not keep
  flattening already-direct read payload code for symmetry alone.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the private shared payload-base helper
  `build_task_view_payload_base()` from
  `chapter_batch_generation_read_context_service.rs`.

  `into_status_task_payload()` and `into_active_task_payload()` now each own
  the real task-view payload assembly directly, while still reusing the same
  base task-view builder plus shared quality-status insertion. The read
  context lane is therefore one layer flatter without changing any payload
  fields, terminal status data, or read-side ownership semantics.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the read-context lane is thinner now, but the remaining functions are real
  payload projections rather than fake-shared wrappers. Continue only if
  another adjacent helper still wraps the same contract; do not keep
  flattening already-direct read payload code for symmetry alone.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the private terminal-event dispatcher
  `BatchGenerationStreamTerminalKind::build_events(...)` from
  `chapter_batch_generation_status_stream_event_service.rs`.

  `BatchGenerationStreamState::terminal_events()` now owns the terminal-event
  branch directly, so the stream event lane keeps one fewer private wrapper
  while preserving the same progress/result/error/done event payload shapes
  and cursor resolution behavior.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_event_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the event lane is thinner now, but the remaining helpers are still real
  stream/event constructors. Continue only if another adjacent helper still
  wraps the same contract; do not keep inlining already-direct event builders
  for symmetry alone.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the last remaining test-only wrapper
  `build_batch_generation_task_runtime_payload_from_task_state_with_checkpoint_override(...)`
  from `chapter_batch_generation_task_payload_base_service.rs`.

  The test suite now calls the real runtime-parts helper directly when it
  needs to assert checkpoint override behavior, so the task-payload base lane
  keeps one fewer fake-shared test shell while preserving the same runtime
  payload shape, checkpoint override semantics, and view payload behavior.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_payload_base_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the task-payload base lane is thinner now, but the remaining helpers are
  already real payload constructors rather than thin compatibility shells.
  Continue only if another adjacent helper still wraps the same contract;
  do not keep flattening already-direct payload builders for symmetry alone.

- 2026-05-28 01:29 +08:00 checkpoint:
  this slice removed the intermediate resume response-state owner from
  `chapter_batch_generation_resume_task_command_service.rs`.

  The resume command now keeps the reset semantics and final payload assembly
  in one owner boundary by returning the resumed execution plan, task id, and
  final response payload directly from `prepare_batch_generation_resume(...)`.
  The batch write workflow now consumes that payload directly instead of
  building or carrying a separate `ResumeBatchGenerationResponseState` shell.

  This keeps resume gating, reset semantics, task id selection, response
  payload shape, checkpoint metadata, and runtime dispatch behavior unchanged
  while removing one more fake-shared resume command owner from the batch
  command lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the resume command lane is thinner now. Continue in this area only if
  another helper still wraps the same response contract; do not keep
  flattening already-direct resume payload assembly once the next move would
  only move reset metadata around without reducing owner count.

- 2026-05-28 01:25 +08:00 checkpoint:
  this slice narrowed the batch cancel command lane by removing the
  single-caller internal helper `cancel_batch_generation_task(...)` from
  `chapter_batch_generation_cancel_service.rs`.

  The owned cancel workflow now performs terminal-status rejection, runtime
  cancellation finalization, and shared progress-summary assembly directly in
  `cancel_owned_batch_generation_task(...)` instead of routing through a
  second file-local command wrapper first.

  This keeps cancel error detail messages, cancel response payload shape,
  runtime cancelled checkpoint semantics, and API error mapping unchanged
  while removing one more fake-shared command shell from the batch command
  lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_cancel_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the cancel lane is thinner now. Continue in the batch command/adapter area
  only if another helper still wraps the same response or command contract;
  do not keep flattening already-direct finalize/summary calls once the next
  move would only inline explicit domain checks without reducing owner count.

- 2026-05-28 01:21 +08:00 checkpoint:
  this slice narrowed the single-chapter route/request seam in
  `chapter_batch_generation.rs` by moving the repeated
  `SingleChapterGenerationRouteRequest ->
  SingleChapterGenerationRequest` conversion into one route-owned helper
  `into_generation_request()`.

  The background generation route and the stream generation route now both
  forward the same converted request owner instead of each rebuilding
  `SingleChapterGenerationRequest::new(...)` inline from the same route body
  fields.

  This keeps single-chapter request payloads, request validation shape,
  background workflow dispatch, stream workflow dispatch, and error mapping
  unchanged while removing one more duplicate route-local request projection
  from the chapter-generation transport lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_workflow_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-29 02:36:21 +08:00 checkpoint:
  this slice removed the private runtime-state carrier
  `BatchGenerationRuntimeTaskContext` from
  `chapter_batch_generation_runtime_state_service.rs`.

  `BatchGenerationRuntimePersistencePlan` now directly owns the runtime
  persistence fields it immediately forwards to task-state mutation and
  checkpoint snapshot assembly:
  `current_chapter_id`, `current_chapter_number`, `completed_chapters`,
  `total_chapters`, and `error_message`.

  This keeps batch runtime lifecycle semantics, cancelled/failure checkpoint
  progression, task mutation behavior, and step outcome contracts unchanged
  while removing one more short-lived single-owner carrier from the runtime
  state lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the runtime-state lane is thinner now, but the remaining helpers mostly own
  real lifecycle or persistence boundaries. Continue here only if the next
  move still removes a short-lived internal carrier or wrapper without
  widening the cancel/resume/shared-runtime boundary.

- 2026-05-29 02:41:40 +08:00 checkpoint:
  this slice removed the private task-view response shells
  `ActiveBatchGenerationTaskListView` and
  `ActiveBatchGenerationQueryView` from
  `chapter_batch_generation_task_view_query_service.rs`.

  The task-view query lane now assembles its final list/query JSON payloads
  directly at the query owner boundary, and it also no longer routes active
  task payload loading through the single-hop helper
  `load_active_task_payload(...)`.

  This keeps active-task list payload shape, active-query response shape,
  read-context loading, and downstream batch route semantics unchanged while
  removing two more short-lived payload shells plus one local forwarding
  helper from the read/query lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the task-view query lane is thinner now, but the remaining helpers are
  closer to real read-context loading and project-access boundaries. Continue
  here only if the next move still removes a short-lived internal payload or
  forwarding shell without duplicating query semantics across owners.

- 2026-05-29 02:44:30 +08:00 checkpoint:
  this slice removed the private single-background launch carrier
  `SingleGenerationBackgroundLaunchPlan` from
  `chapter_single_generation_workflow_service.rs`.

  The prepared single-generation owner now dispatches background generation
  directly by assembling the checkpoint, background response payload, active
  task model, and runtime launch input in one owner boundary before
  persisting and spawning the runtime.

  This keeps single-chapter background response shape, pending checkpoint
  semantics, task insert defaults, and downstream single-generation runtime
  dispatch unchanged while removing one more short-lived single-owner launch
  shell from the single workflow lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the single background workflow lane is thinner now, but the remaining
  helpers are closer to real request preparation, SSE launch, and runtime
  execution boundaries. Continue here only if the next move still removes a
  short-lived internal launch or payload shell without widening the shared
  single-generation contract.

- 2026-05-29 02:47:49 +08:00 checkpoint:
  this slice removed the private runtime carrier
  `SingleGenerationRuntimeTaskContext` from
  `chapter_single_generation_runtime_state_service.rs`.

  `SingleGenerationRuntimeOutcome` and
  `SingleGenerationRuntimePersistencePlan` now directly own the runtime
  fields they immediately forward into task mutation and checkpoint snapshot
  persistence: `chapter_id`, `chapter_number`, `word_count`, and
  `error_message`.

  This keeps single-generation preparing/completed/failed task semantics,
  runtime checkpoint progression, and dispatch behavior unchanged while
  removing one more short-lived single-owner carrier from the single runtime
  lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the single runtime lane is thinner now, but the remaining helpers are
  mostly real runtime preparation, mutation, and snapshot boundaries.
  Continue here only if the next move still removes a short-lived internal
  carrier without collapsing the runtime lifecycle stages into one opaque
  block.

- 2026-05-29 02:51:10 +08:00 checkpoint:
  this slice removed the private read-to-stream forwarding shell
  `BatchGenerationReadContext::into_stream_state()` from
  `chapter_batch_generation_read_context_service.rs`.

  The batch status stream lane now calls
  `BatchGenerationStreamState::from_task_state(...)` directly at the stream
  owner boundary using the read context's `task` and
  `workflow_runtime_state`, instead of routing through an intermediate
  read-context method first.

  This keeps initial stream-state loading, polling refresh behavior, stream
  state fallback semantics, terminal stream semantics, and downstream SSE
  event resolution unchanged while removing one more short-lived forwarding
  shell from the batch read/stream boundary.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the batch read/stream boundary is thinner now, but the remaining helpers are
  close to real read loading, stream-state semantics, and SSE polling
  boundaries. Continue here only if the next move still removes a short-lived
  private forwarding shell without duplicating stream-state semantics across
  owners.

- 2026-05-29 02:55:14 +08:00 checkpoint:
  this slice removed the private resume launch carrier
  `ResumeRuntimeLaunchPlan` from
  `chapter_batch_generation_resume_task_command_service.rs`.

  The resume command lane now lets `ResumeExecutionPlan` own the direct
  single-vs-batch runtime dispatch decision with the prepared execution
  config, instead of first converting into an intermediate launch enum and
  matching on that short-lived shell.

  This keeps resume-domain validation, reset semantics, response payload
  shape, execution-config threading, and downstream single/batch runtime
  dispatch targets unchanged while removing one more short-lived launch shell
  from the resume command lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`

  Updated stop-rule assessment:
  the resume command lane is thinner now, but the remaining helpers are close
  to real resume-domain selection, reset, and shared response boundaries.
  Continue here only if the next move still removes a short-lived internal
  carrier without splitting the resume semantics across multiple owners.
  the single-chapter route request lane is thinner now. Continue in
  `chapter_batch_generation.rs` only if another route-local branch still owns
  duplicated transport conversion or fallback logic; do not keep moving
  already-direct request forwarding once the next move would only rename the
  same two fields without reducing boundary count.

- 2026-05-28 01:18 +08:00 checkpoint:
  this slice narrowed the single-chapter workflow launch lane by extracting
  one workflow-local helper
  `build_single_generation_runtime_launch_input(...)` in
  `chapter_single_generation_workflow_service.rs`.

  The stream workflow and the background workflow now both derive their
  `SingleGenerationRuntimeLaunchInput` from the same prepared-request owner
  boundary instead of each locally destructuring
  `PreparedSingleChapterGenerationRequest` and rebuilding the runtime launch
  input inline.

  This keeps single-chapter route payloads, request preparation, background
  task creation, stream launch behavior, runtime dispatch, and response
  payloads unchanged while removing one more duplicate workflow-local launch
  projection from the single-chapter generation lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_single_generation_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the single-chapter workflow launch lane is narrower now. Continue in this
  area only if another helper still splits the real request/workflow boundary;
  do not keep moving obvious task-payload assembly around once the next move
  would only reshuffle the same prepared request fields without reducing
  owner count.

- 2026-05-28 01:12 +08:00 checkpoint:
  this slice removed the file-local runtime step wrapper
  `BatchGenerationRuntimeStepOutcome` from
  `chapter_batch_generation_runtime_state_service.rs`.

  The batch runtime loop now performs its task gate and chapter lookup
  branches directly in `execute_batch_generation_runtime(...)`, then finalizes
  cancelled / missing-chapter / chapter-load-error paths through the existing
  runtime finalize helpers in place instead of routing through an intermediate
  control enum first.

  This keeps runtime checkpoint writes, snapshot persistence, cancelled-task
  handling, missing-chapter failure semantics, load-error failure semantics,
  and generation-error finalization unchanged while removing one more
  fake-shared control shell from the batch runtime lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the runtime loop is narrower now, but the remaining owners in this file are
  closer to real checkpoint/snapshot/task-mutation contracts. Continue here
  only if another helper still hides a genuine ownership split; do not flatten
  the surviving stage/finalize helpers if the next move would only inline
  explicit runtime state transitions without reducing boundary count.

- 2026-05-28 01:09 +08:00 checkpoint:
  this slice removed the file-local runtime launch wrapper
  `BatchGenerationRuntimeLaunchPlan` from
  `chapter_batch_generation_write_workflow_service.rs`.

  The batch create workflow now dispatches runtime work directly after
  preparing the explicit `BatchGenerationExecutionInput`, and the batch resume
  workflow now branches directly on `ResumeExecutionPlan` to dispatch either
  the batch runtime or the single-chapter runtime without creating an
  intermediate launch enum first.

  This keeps create/resume response payloads, execution-config preparation,
  runtime dispatch targets, and single-vs-batch resume behavior unchanged
  while removing one more fake-shared owner shell from the batch write
  workflow seam.

  Focused tests were updated to assert the real surviving contract
  (`BatchGenerationExecutionInput`) instead of the removed internal wrapper.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the write-workflow launch lane is noticeably narrower now. Continue in this
  file only if another helper still splits a real create/resume workflow
  contract; do not keep flattening straight-line request preparation or
  persistence steps once the next move would only trade one obvious local
  branch for another without reducing ownership count.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice tightened the active-task view query lane by removing
  `build_active_batch_generation_task_query(...)` from
  `chapter_batch_generation_task_view_query_service.rs`.

  The list-view query and the project-scoped active-task query now each own
  their local active-task filter stack directly:
  `user_id + active statuses + created_at desc`, with the project-scoped
  query layering `project_id` on top in place.

  This keeps active-task filtering, ordering, response payloads, and project
  access behavior unchanged while removing one more thin fake-shared query
  shell from the batch read/query seam.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the active-task query lane is thinner now. Continue here only if another
  helper still splits a real query/read contract; do not keep localizing
  already-obvious filter stacks if the next move would only duplicate the same
  semantics without further ownership gain.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed the final single-call internal helper
  `load_owned_batch_generation_read_context_inner(...)` from
  `chapter_batch_generation_read_context_service.rs`.

  The required read-context owner now directly performs:
  - owned task lookup
  - task-not-found mapping
  - snapshot-backed read-context construction

  This keeps batch status payloads, status-stream polling behavior, SSE
  not-found / timeout semantics, and route error mapping unchanged while
  removing one more internal forwarding layer from the batch read-context
  seam.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the read-context seam is now close to saturation. Continue here only if a
  remaining helper still owns a real compatibility-sensitive read contract,
  not if the next move would merely inline already-direct task/snapshot
  plumbing.

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice removed `BatchGenerationReadContext::into_stream_state(...)`
  and moved the stream-state projection back to the true stream consumers:
  `chapter_batch_generation_status_stream_service.rs` and the batch status
  route in `chapter_batch_generation.rs`.

  The read-context owner now stays focused on read payload assembly plus
  task/snapshot loading, while `BatchGenerationStreamState::from_task_state(...)`
  again clearly belongs to the stream lane that actually consumes it.

  This keeps batch status payloads, SSE polling behavior, not-found / timeout
  semantics, and route error mapping unchanged while removing one more thin
  projection split between read-context and stream-state ownership.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the read-context / status-stream seam is now thinner and cleaner. Continue
  here only if another remaining owner still duplicates a real stream/read
  contract; do not keep shuffling task-state projection code once the next
  move would be symmetry-only cleanup.

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice removed the now-unused public optional wrapper
  `load_owned_batch_generation_read_context(...)` from
  `chapter_batch_generation_read_context_service.rs` and tightened the
  required read-context path so it now owns both the task lookup and the
  snapshot-to-context conversion in one place.

  The status stream path now consumes that required read-context owner
  directly, so the batch status SSE flow keeps the same polling, not-found,
  timeout, and keep-alive behavior while shedding one more thin ownership
  split from the read-context lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_read_context_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the read-context lane is noticeably thinner now. Only continue if another
  nearby owner still duplicates a real compatibility-sensitive read or stream
  contract; do not keep flattening helpers once the next move would only move
  the same task/snapshot conversion around without reducing boundary count.

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved the batch status SSE polling boundary one step deeper into
  `chapter_batch_generation_status_stream_service.rs`. The stream owner no
  longer routes through `load_owned_batch_generation_read_context(...)` for
  the polling branch; it now loads the required owned task state directly,
  converts it to `BatchGenerationStreamState`, and continues with the same
  cursor/event resolution path.

  This keeps the batch status SSE payloads, polling behavior, not-found /
  timeout semantics, and keep-alive contract unchanged while removing one
  more thin read-context wrapper from the status lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the immediate status-stream wrapper is thinner now, and the remaining
  shared read-context entry should only be touched if another genuine owner
  split still exists nearby. Do not keep flattening direct SSE plumbing once
  the next move would only reorganize already-straight-line polling flow.

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved the batch status SSE entry boundary one step outward by
  removing the single-call wrapper
  `load_owned_batch_generation_status_stream(...)` from
  `chapter_batch_generation_status_stream_service.rs`. The route handler in
  `backend-rs/src/api/chapter_batch_generation.rs` now loads the required
  read context directly, converts it to `BatchGenerationStreamState`, and
  passes that initial state into
  `build_batch_generation_status_stream(...)` itself.

  This keeps the batch status SSE payloads, polling behavior, not-found /
  timeout semantics, and keep-alive contract unchanged while removing one
  more thin transport-style ownership split from the batch status lane.

  Validation passed with:
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`
  `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule assessment:
  the owned-task query wrapper lane is now saturated enough that the next
  move should only continue if another clearly shared owner split still exists
  in the nearby status/read/runtime surface. Do not keep flattening thin
  wrappers once the next change would only reorganize already-direct SSE
  plumbing.

## Candidate Execution Waves

### Wave 1: Batch-generation semantics hardening

Goal:

- tighten service-owned runtime or read-side semantics with helper extraction
  and regression protection

Likely candidates:

- progress/checkpoint helper cleanup
- stream fallback or event-status helper cleanup
- status view normalization

Validation:

- `cargo check`
- focused unit tests in touched service files

Completed checkpoint:

- `chapter_batch_generation_task_payload_base_service.rs`,
  `chapter_batch_generation_read_context_service.rs`, and
  `chapter_batch_generation_resume_task_command_service.rs`
  - `BatchGenerationTaskRuntimePayloadBase` has been removed as a struct owner
    from the shared batch task runtime payload lane
  - batch task runtime payload assembly now goes directly through shared helper
    functions:
    `build_batch_generation_task_runtime_payload(...)`,
    `build_batch_generation_task_runtime_payload_from_runtime_parts(...)`,
    `build_batch_generation_task_runtime_payload_from_task(...)`,
    `build_batch_generation_task_runtime_payload_from_task_state(...)`, and
    `build_batch_generation_task_view_payload_from_task_state(...)`
  - read-context task view payload assembly no longer re-enters the removed
    struct wrapper and now calls the shared direct view helper once
  - resume response payload assembly no longer builds
    `struct -> into_payload()` just to patch the checkpoint; it now goes
    straight through the shared runtime-parts helper before layering the
    command summary fields
  - focused tests were updated to protect the direct helper contract,
    including the checkpoint override path previously covered through the
    removed struct owner
  - this keeps the existing batch runtime payload shape, checkpoint metadata,
    read-side payload fields, and resume response envelope unchanged while
    removing one more same-shape helper wrapper from the Phase 5 batch command
    lane
  - `ResumeBatchGenerationResponseState::into_response_payload()` briefly
    regressed when it bypassed the runtime-parts helper directly; the fix
    restored `build_batch_generation_task_runtime_payload_from_runtime_parts(...)`
    so `stage_code`, `execution_mode`, and the runtime checkpoint metadata are
    again assembled through the shared contract before the summary fields are
    layered on
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_payload_base_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_read_context_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"` and
    `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this payload-base lane is now closer to saturation; the next seam should
    stay adjacent only if another compatibility-sensitive runtime payload
    owner still duplicates the same checkpoint/response contract, not if the
    next move would only rename already-direct helper forwarding
  - after the resume payload regression fix, keep the lane strict: only remove
    another helper if it still preserves the exact runtime payload envelope
    and checkpoint contract without changing how metadata is sourced

- `chapter_batch_generation_cancel_service.rs`
  - `CancelBatchGenerationCommandState` has been removed from the batch cancel
    command lane because it only re-projected `task.id/status/completed/total`
    without owning any distinct contract
  - cancel validation now reads terminal-status semantics directly from
    `batch_generation_task::Model`, and the shared
    `BatchGenerationCommandProgressSummary::from_task(...)` owner now supplies
    the response summary immediately after the runtime cancel finalize call
  - this keeps the existing terminal-status rejection behavior, cancel
    response payload shape, and shared summary fields unchanged while removing
    one more same-shape command wrapper from the Phase 5 batch command lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this cancel lane is now close to saturation; only return if another owner
    still duplicates a compatibility-sensitive cancel command or response
    contract, not for naming-only cleanup

- `chapter_batch_generation_write_workflow_service.rs`
  - `BatchGenerationCreatePlan` has been removed from the batch create
    write-workflow lane because it only wrapped `created_task_id` and no
    longer owned any distinct create/runtime contract
  - task persistence now returns the created task identifier directly through
    `create_batch_generation_task(...)`, and the prepared create owner
    dispatches runtime work from that direct identifier without re-entering an
    extra plan shell
  - this keeps the create response payload shape, persisted task/snapshot
    behavior, runtime launch input, and write-workflow error contract
    unchanged while removing one more same-shape wrapper from the Phase 5
    batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this create-plan lane is now closer to saturation; only continue here if
    another owner still duplicates a compatibility-sensitive create workflow
    or runtime-launch contract, not if the next move would just rename direct
    identifier forwarding
  - `dispatch_resume_workflow(...)` has also been removed from the same write
    lane because it only rewrapped `response_state -> payload` plus
    `execution -> launch_plan` without owning a distinct resume compatibility
    contract
  - the resume write-workflow owner now performs that final
    `response payload + runtime launch` closure inline after config
    preparation, keeping the launch decision and returned payload in one place
  - this keeps the resume response payload shape, execution-plan dispatch, and
    write-workflow error contract unchanged while removing one more same-shape
    wrapper from the Phase 5 batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    the batch write lane is now even closer to saturation; only stay here if a
    remaining owner still duplicates a compatibility-sensitive
    create/resume/runtime contract, not for generic helper flattening
  - `PreparedBatchGenerationCreateRequest::dispatch_create_workflow(...)` has
    now also been removed because it only rewrapped
    `create_response_payload + into_runtime_launch_plan + dispatch` with no
    distinct compatibility contract of its own
  - the create write-workflow owner now closes the same `response payload +
    runtime launch` flow inline after execution-config preparation, matching
    the already-simplified resume lane
  - this keeps the create response payload shape, launch input semantics, and
    write-workflow error contract unchanged while removing one more same-shape
    wrapper from the Phase 5 batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    the batch write lane is now effectively saturated for this task; the next
    seam should move to an adjacent lane only if it still removes a real
    compatibility-sensitive owner split, not if it merely flattens already
    direct workflow code
  - `BatchGenerationRuntimeLaunchPlan::dispatch(...)` has now also been
    removed because it only forwarded the already-owned launch-plan enum into
    one of two runtime dispatch functions and had no contract beyond that
    single `match`
  - the create and resume write-workflow owners now perform the final
    `launch plan -> runtime dispatch` branch inline, keeping the outward
    response payload and the last runtime handoff in the same workflow closure
  - this keeps create/resume payloads, launch-plan shape, and runtime dispatch
    behavior unchanged while removing one more one-hop wrapper from the Phase 5
    batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the batch write lane is now even closer to full saturation; only return if
    another owner still duplicates a compatibility-sensitive create/resume
    contract, not if the next move would only inline another straight-through
    branch with no ownership gain
  - `PreparedBatchGenerationCreateRequest` now also owns the collection
    projections it already semantically defines:
    `chapter_id_payload()` for persistence input and `chapter_payloads()` for
    create-response output
  - create-task persistence and create-response assembly no longer each rebuild
    the same `chapters_to_generate -> ids/json payload` mapping inline from the
    workflow owner
  - this keeps create payload shape, persisted chapter-id array order, and
    estimated-time semantics unchanged while removing one more duplicated
    projection owner from the Phase 5 batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the batch write lane is now very close to saturation; only continue here if
    the next slice removes another real compatibility-sensitive owner split,
    not if it merely centralizes cosmetic collection mapping with no further
    boundary gain

- `chapter_batch_generation_task_view_query_service.rs`
  - the active-project single-task read path no longer round-trips through the
    list read-context loader by converting `Option<task>` into a one-item `Vec`
  - `load_active_project_batch_generation_task_view_context(...)` now loads the
    single task read-context directly through
    `load_batch_generation_read_context_for_task(...)`, while the list loader
    remains the owner only for true multi-task list flows
  - this keeps the active-query payload shape, project-access error contract,
    and read-context semantics unchanged while removing one more fake shared
    path from the batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this task-view query lane now has less fake sharing pressure; only return
    if another owner still duplicates a compatibility-sensitive active-query
    or status-query contract, not for generic single/list helper symmetry
  - the active-task query contract is now owned through one shared
    `build_active_batch_generation_task_query(user_id)` helper, so both the
    active list view and the single active-project query consume the same
    `user/status/order` semantics instead of each rebuilding that filter stack
    inline
  - this keeps active-task selection, ordering, response payloads, and
    project-access behavior unchanged while removing one more duplicated query
    owner from the Phase 5 batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this read/query lane is thinner again; only continue here if another owner
    still interprets a compatibility-sensitive active/status query contract, not
    if the next move would only restyle already-local payload assembly

- `chapter_batch_generation_read_context_service.rs`
  - the read-context owner now builds its shared task-view payload base through
    one internal `build_task_view_payload_base()` helper instead of repeating
    the same `task view payload + quality status context` assembly in both the
    active-task and status-task branches
  - `into_active_task_payload()` now returns that shared base directly, while
    `into_status_task_payload()` only layers the status-only retry / failed /
    terminal fields afterward
  - this keeps active/status payload shapes, quality-status insertion, and
    terminal field behavior unchanged while removing one more duplicated
    read-side projection owner from the Phase 5 batch status/read lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_read_context_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the read-context lane is thinner now; only continue here if another owner
    still duplicates a compatibility-sensitive read projection or stream-state
    contract, not if the next move would only wrap the same payload base again

- `chapter_single_generation_workflow_service.rs`
  - `SingleGenerationBackgroundLaunchPlan` has been removed from the
    single-generation background workflow lane because it only wrapped
    `task/checkpoint/chapter_target/runtime_input` before an immediate
    `persist + dispatch` handoff
  - `PreparedSingleChapterGenerationRequest` now exposes direct background
    runtime parts, and the background workflow owner closes the
    `persist task + persist snapshot + dispatch runtime + build response`
    flow inline
  - this keeps the background create response payload, task/snapshot
    persistence behavior, runtime launch input, and workflow error contract
    unchanged while removing one more same-shape launch wrapper from the
    Phase 5 single-generation lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this single-generation background lane is now closer to saturation; only
    continue here if another owner still duplicates a compatibility-sensitive
    background/stream/runtime contract, not for generic workflow flattening
  - `OwnedSingleGenerationStreamTerminalEvent` has also been removed from the
    same service because it only rewrapped `generation result -> terminal SSE
    event emission` for one caller and did not own an externally meaningful
    compatibility contract
  - stream terminal emission now goes directly through
    `emit_owned_single_generation_stream_terminal_event(...)`, keeping the
    success/error SSE behavior in one helper without an extra enum shell
  - this keeps the single-generation stream success/error/done event behavior
    unchanged while removing one more same-shape wrapper from the Phase 5
    single-generation stream lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    the single-generation workflow lane is now even closer to saturation; the
    next seam should move only if another owner still duplicates a real
    compatibility-sensitive stream/background/runtime contract

- `chapter_batch_generation_resume_task_command_service.rs`
  - `ResumeBatchGenerationResponseState::from_command_state_with_summary(...)`
    has been removed because it only rewrapped direct field projection from
    `ResumeBatchGenerationCommandState` and no longer owned a distinct resume
    compatibility contract
  - `from_command_state(...)` and `reset_for_resume(...)` now construct the
    response state directly, while the tests that need a custom summary also
    build the state inline instead of going through an extra helper wrapper
  - this keeps resume response payload shape, reset semantics, and domain
    error behavior unchanged while removing one more same-shape projection
    helper from the Phase 5 batch resume lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    the batch resume command lane is thinner now; only stay here if another
    owner still duplicates a compatibility-sensitive resume response or reset
    contract, not for generic projection flattening

- `chapter_batch_generation_runtime_state_service.rs`
  - `BatchGenerationTaskStage::persist_for_task(...)` has been removed from
    the runtime-state lane because it only rewrapped
    `resolve_mutation_plan(...).persist_for_task(...)` without owning an
    externally meaningful contract
  - runtime task persistence now goes directly through the mutation plan for
    preparing, cancelling, starting, succeeding, and failing stages, so the
    stage enum no longer keeps an extra persistence wrapper
  - this keeps runtime mutation behavior, checkpoint writes, and persisted
    task semantics unchanged while removing one more same-shape wrapper from
    the Phase 5 runtime lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - stop-rule update:
    this runtime-state lane is thinner now; only stay here if another owner
    still duplicates a compatibility-sensitive stage/persistence contract, not
    for generic helper flattening
  - the remaining snapshot-side `BatchGenerationSnapshotStage::persist_for_task(...)`
    wrapper has also been removed in the latest follow-up because it only
    forwarded directly to `upsert_batch_generation_runtime_snapshot(...)` plus
    `build_batch_generation_runtime_checkpoint_for_stage(...)` without owning
    a distinct compatibility contract
  - batch runtime state helpers now write task mutations and snapshot
    checkpoints directly at the call sites, so the runtime-state lane no longer
    keeps an extra stage-level persistence owner for either task or snapshot
    writes
  - this keeps runtime mutation behavior, checkpoint writes, and persisted
    task semantics unchanged while removing another same-shape wrapper from the
    Phase 5 runtime lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_runtime_state_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this runtime-state lane is now thinner; the next seam should be chosen
    only if another owner still duplicates a real compatibility-sensitive
    write or checkpoint contract, not for more direct builder forwarding
  - follow-up cleanup: the now-inlined `resolve_generation_result(...)` test
    shell was removed together with the dead `Succeeded` outcome branch, so the
    runtime-state file now only keeps the step outcomes that still exist in the
    production loop
  - focused validation passed again after the cleanup:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_runtime_state_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this runtime-state lane is effectively saturated for this follow-up; only
    revisit it if another owner still duplicates a compatibility-sensitive
    checkpoint or runtime-write contract, not for more local test-shape cleanup

- `chapter_batch_generation_status_view_service.rs`
  - `BatchGenerationStreamState` now owns `event_status`
  - progress-event payload construction no longer remaps status locally
  - focused tests added for failed/cancelled/unknown fallback semantics
  - `cargo check` passed after the slice

- `chapter_batch_generation_status_stream_service.rs`
  - the thin optional/required stream-state loaders have been removed because
    they only re-mapped `BatchGenerationReadContext` into
    `BatchGenerationStreamState` without owning a distinct contract
  - the status stream owner now performs that read-context-to-stream-state
    mapping directly at the call sites, so the file keeps only the real polling
    and event-stream orchestration
  - this keeps stream access behavior and error mapping unchanged while
    removing another same-shape wrapper from the batch status lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this status-stream lane is thinner; the next seam should be chosen only if
    another owner still duplicates a compatibility-sensitive stream/read
    contract, not for more direct read-context mapping
  - `BatchGenerationStreamEventResolution::events()` has now also been removed
    from the adjacent stream event lane because it only exposed the already
    owned `events` vector back to the single stream owner without adding any
    extra contract or policy
  - `chapter_batch_generation_status_stream_service.rs` now consumes
    `Continue { events, next_cursor }` and `Close { events }` directly, so the
    final `event batch -> SSE send / cursor advance / close` ownership stays in
    the stream polling loop where those branch decisions are actually made
  - this keeps emitted SSE event order, cursor advancement, close semantics,
    and timeout/not-found behavior unchanged while removing one more one-hop
    wrapper from the Phase 5 batch status-stream lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_event_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the stream lane is thinner again; only continue here if another helper
    still splits a compatibility-sensitive polling/event contract, not if the
    next move would only inline cursor bookkeeping with no boundary reduction
  - `BatchGenerationStreamCursor` now resolves event-batch change detection and
    `next_cursor` construction inline inside `resolve_event_batch(...)`, so the
    cursor owner keeps the whole polling delta decision in one place instead of
    splitting it across `from_state()` / `has_changed()` / `resolve_event_batch()`
  - this keeps progress/terminal SSE behavior, cursor advancement, and
    unchanged-state suppression exactly the same while removing one more
    internal helper split from the Phase 5 batch status-stream event lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_event_service -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the cursor lane is now close to saturation; only continue here if another
    helper still represents a real compatibility-sensitive polling contract,
    not if the next move is only collapsing already-local equality logic again
  - the owned stream access boundary now sits fully inside
    `chapter_batch_generation_status_stream_service.rs` again through
    `load_owned_batch_generation_status_stream(...)`
  - the route no longer assembles `load read context -> into_stream_state ->
    build stream` inline; it now delegates the whole owned stream
    initialization flow to the status-stream service and keeps only transport
    concerns plus error mapping
  - this keeps stream access control, initial state semantics, SSE payloads,
    and route error mapping unchanged while removing one more cross-file owner
    split from the Phase 5 batch status-stream lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_error_mapper -- --nocapture`
    and
    `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    the stream route/service seam is now much thinner; only continue here if a
    remaining helper still owns a real compatibility-sensitive access or poll
    boundary, not if the next move would only relocate already-local stream
    setup code

- `chapter_batch_generation_task_view_query_service.rs`
  - the active-query payload wrapper helper has been removed because it only
    rewrapped `Option<BatchGenerationReadContext>` into the existing
    `has_active_task/task` JSON shape without any extra ownership
  - the active-project query now builds that small response inline at the call
    site, while keeping the same payload contract and route error mapping
  - this keeps active-query behavior unchanged while removing another
    same-shape wrapper from the batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this query lane is thinner; the next seam should only target another real
    duplicated read/query contract, not another single-call payload branch
  - the owned status-task view helper has also been removed because it only
    transformed `BatchGenerationReadContext -> status payload` without adding
    any extra policy beyond the already-owned context conversion
  - the route-facing status lookup now composes the shared read-context loader
    and payload conversion directly, while keeping the same 404 / internal
    error mapping and payload shape
  - this keeps status-query behavior unchanged while removing another
    same-shape wrapper from the batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this query lane is now thinner again; only continue here if a future audit
    finds another real duplicated read/query contract, not another single-step
    payload conversion
  - the active list payload wrapper has also been removed because it only
    converted `Vec<BatchGenerationReadContext>` into the same `total/items`
    JSON shape that the caller already had enough information to build inline
  - the list view now assembles `items` and `total` directly from the loaded
    contexts, keeping the same payload contract while removing one more
    same-shape wrapper from the batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this query lane is approaching saturation; the next move should only target
    another real duplicated read/query contract, not a list-shape helper that is
    already fully owned by the caller
  - the intermediate `load_batch_generation_read_contexts_for_tasks(...)`
    helper has also been removed because it only looped over task models and
    forwarded them into `load_batch_generation_read_context_for_task(...)` for
    a single caller
  - the active-user list context loader now owns that task-to-context loop
    directly, keeping behavior unchanged while removing one more same-lane
    forwarding shell from the batch read/query lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_view_query_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this read/query lane is now very close to saturation; the next move should
    only proceed if another helper still duplicates a real ownership boundary,
    not if it only saves one local loop or one local JSON branch

- `chapter_batch_generation_status_stream_service.rs` and
  `chapter_batch_generation.rs`
  - the route now loads the required read context directly, converts it to
    `BatchGenerationStreamState`, and passes that state into
    `build_batch_generation_status_stream(...)`
  - the stream service now keeps only the polling cadence, cursor progression,
    SSE send orchestration, and event handling, without a separate one-line
    owned status-stream loader wrapper
  - this keeps status SSE behavior, not-found handling, and polling/event
    sequencing unchanged while removing one more same-shape route/service
    wrapper from the batch status-stream lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this status-stream lane is thinner now; only continue if a future audit
    finds another real ownership boundary, not another single-call loader
    wrapper
  - a follow-up scan of the neighboring stream/event files did not surface a
    similarly safe single-call wrapper to remove without starting to reshape
    real event semantics, so this lane is now effectively at the
    “true-owner” boundary for this follow-up task

- `chapter_batch_generation_write_workflow_service.rs`
  - the prepared-request payload helpers have been tightened so the create
    response and route-facing chapter-id payload assembly are built directly
    from the already-owned chapter targets instead of bouncing through separate
    one-line wrapper methods
  - this keeps create/workflow response shape, chapter ordering, and runtime
    launch behavior unchanged while removing another same-shape payload wrapper
    from the batch write lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this write-workflow lane is still productive, but only if the next move
    removes a real owner split or payload duplication boundary, not another
    helper that simply turns one owned vector into JSON

- `chapter_batch_generation_runtime_state_service.rs`
  - pending batch snapshot semantics now flow through the same
    `BatchGenerationSnapshotStage` / `BatchGenerationSnapshotPlan` owner as
    the running, completed, cancelled, and failed runtime states
  - queued/resumed checkpoint construction no longer owns a separate pending
    stage/plan branch; it reuses the unified snapshot plan and preserves the
    existing payload shape
  - focused tests now cover the unified queued/resume plan and checkpoint
    outputs
  - `cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
    passed
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_status_stream_service.rs`
  - terminal event selection now goes through
    `build_terminal_batch_generation_events()`
  - focused tests added for completed/failed/cancelled/non-terminal branches
  - `cargo check` passed after the slice
- `chapter_batch_generation_status_stream_event_service.rs`
  - SSE stream change detection plus terminal-close decision now flow through
    one event-owner helper:
    `resolve_batch_generation_stream_event_batch()`
  - `chapter_batch_generation_status_stream_service.rs` now keeps only
    polling + send orchestration and no longer carries its own parallel
    change-detection / terminal-dispatch contract
  - this preserves the existing progress/result/error/done payloads and
    stream stop behavior while making the read-side SSE contract more
    auditable in one owner module
  - focused tests passed:
    `cargo test chapter_batch_generation_status_stream_event_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
    and
    `cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_status_payload_adapter_service.rs`
  - shared payload field assembly now goes through `base_task_payload()`
  - focused tests added to protect the difference between
    `task_status_payload()` and `active_task_payload()`
  - `cargo check` passed after the slice
- `chapter_batch_generation_status_semantics_service.rs`
  - `task_execution_mode()` no longer carries a redundant single/batch match
    branch; the shared execution-mode contract is now expressed as one
    immutable literal owner
  - `task_execution_mode()` also no longer accepts a useless `task`
    argument, which removes a fake dependency from the shared payload owner
  - focused tests now also cover malformed-single input while keeping the
    existing `"interactive"` contract stable
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_semantics_service -- --nocapture` passed
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_task_payload_base_service.rs` and
  `chapter_batch_generation_command_payload_adapter_service.rs`
  - resume response payload assembly no longer patches
    `checkpoint.chapter_id` directly in the response adapter
  - nested checkpoint overrides are now owned by the shared task-payload base
    helper, so the response layer only selects the override and adds the outer
    resume metadata
  - the resume response envelope is now also assembled through a dedicated
    helper, so the adapter no longer hand-builds the outer resume message and
    chapter totals inline
  - cancel and resume task-progress summaries now reuse one shared task-summary
    helper for `batch_id/message/completed_chapters/total_chapters`, so those
    response fields no longer drift across separate adapter branches
  - focused tests now cover the checkpoint override helper and the resume
    response adapter
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_task_payload_base_service -- --nocapture` passed
  - `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_command_payload_adapter_service -- --nocapture`
    passed
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_status_view_service.rs` and
  `chapter_batch_generation_status_stream_event_service.rs`
  - stream terminal-kind semantics are now resolved once while building
    `BatchGenerationStreamState`
  - the stream event builder now consumes `state.terminal_kind` instead of
    re-parsing `state.status` for terminal-event selection
  - `chapter_batch_generation_status_view_service.rs` no longer performs a
    second terminal-kind lookup after calling
    `resolve_batch_generation_stream_semantics()`; the semantics owner now
    returns `terminal_kind` directly
  - focused tests passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_status_stream_event_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_active_query_service.rs`
  - active batch query now owns the project-access gate plus
    `ProjectNotFoundOrAccessDenied` mapping instead of splitting that read-side
    contract across `*_active_query_workflow_service.rs` and the query owner
  - `chapter_batch_generation_active_query_workflow_service.rs` now remains a
    thin compatibility delegate and no longer carries a parallel access-check
    decision path
  - this preserves the existing active-query response shape and error mapping
    while making the read-side access boundary auditable in one owner module
  - focused validation passed:
    `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
    and
    `cargo test chapter_batch_generation_active_query_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_single_generation_workflow_service.rs`
  - the background create response payload no longer lives behind a one-call
    helper; the workflow now assembles that stable JSON envelope directly at
    the single production call site
  - the background task chapter payload helper was also removed, and the active
    model builder now inlines the same single-chapter payload JSON directly
    where it already owns the task-construction contract
  - this keeps the single-chapter background create response shape, chapter
    ordering, task defaults, and runtime dispatch behavior unchanged while
    removing two more same-shape wrappers from the Phase 5 single-generation
    lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_workflow_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this single-generation workflow lane is thinner now; only continue if the
    next move removes another real compatibility-sensitive owner split, not if
    it only shifts JSON construction between nearby lines
  - the workflow-local initial SSE sequence wrapper was then removed as well:
    `OwnedSingleGenerationInitialEvent` only represented the fixed
    preparing/generating startup pair for one workflow owner, so the stream
    launcher now emits those two events directly instead of routing through an
    extra enum/sequence helper
  - this keeps the single-generation stream startup order, progress values,
    and terminal emission behavior unchanged while removing one more
    same-shape wrapper from the Phase 5 single-generation workflow lane
  - focused validation passed again after the cleanup:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_workflow_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this workflow lane is approaching saturation; only continue if the next
    move removes another real owner split, not if it merely flattens the fixed
    startup event pair again
- `chapter_single_generation_prepare_service.rs`
  - `SingleChapterGenerationTarget::from_model(...)` has been removed because
    it only reprojected the loaded chapter model into the already-owned target
    contract without any extra validation or branching
  - the prepare owner now constructs `SingleChapterGenerationTarget` inline
    after loading the chapter and continues to own the normalized
    request/preparation contract for background and stream entrypoints
  - this keeps chapter projection, target-word normalization, execution-config
    assembly, and prepared-request behavior unchanged while removing one more
    same-shape wrapper from the Phase 5 single-generation prepare lane
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_prepare_service -- --nocapture`
    and `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - stop-rule update:
    this prepare lane is thinner now; only continue if the next move removes
    another real request-owned projection shell, not if it just restates the
    same `chapter -> target` field copy
- `chapter_batch_generation_status_query_service.rs`
  - status-query error ownership no longer duplicates the same
    `TaskNotFound/Internal` contract already owned by
    `LoadBatchGenerationTaskViewContextError`
  - `LoadBatchGenerationStatusQueryError` is now a direct alias of the view
    context error type, so the status query keeps only
    `load context -> build payload` flow instead of maintaining a parallel
    one-to-one error remap layer
  - this preserves the existing 404 / internal error mapper behavior while
    reducing another read-side owner split
  - focused validation passed:
    `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
    and
    `cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_active_query_service.rs` and
  `chapter_batch_generation_active_list_query_service.rs`
  - active-query and active-list response envelopes now belong to their
    respective query-owner modules instead of being assembled inside
    `chapter_batch_generation_status_view_service.rs`
  - `chapter_batch_generation_status_view_service.rs` now narrows to shared
    view-context loading, active-task item payload assembly, and stream-state
    semantics, rather than mixing shared context ownership with multiple
    route-specific query envelope shapes
  - this preserves `has_active_task/task` and `total/items` payload shapes
    while further reducing read-side owner overlap across batch query files
  - focused validation passed:
    `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
    and
    `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
  - updated stop-rule assessment:
    this batch read-side lane is now nearing saturation; the next step should
    stay here only if another concrete persisted/read-contract owner split is
    still visible, otherwise switch to the next higher-signal Phase 5 seam
    instead of forcing more thin-wrapper cleanup
- `chapter_single_generation_write_workflow_service.rs`
  - single-generation write-workflow entrypoints now share one
    `build_single_generation_write_workflow_request()` helper for the
    route-payload -> domain-request conversion
  - background and stream write workflows no longer maintain parallel request
    conversion branches before delegating to the shared workflow owner
  - this preserves the existing route payload contract, response shape, and
    runtime launch behavior while reducing one more write-lane owner split
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_write_workflow_service.rs` and
  `chapter_batch_generation_runtime_launch_service.rs`
  - batch create/resume write workflows now prepare the explicit
    `PreparedGenerationExecutionConfig` before runtime launch
  - runtime launch no longer loads execution config internally; it now only
    assembles the launch payload and dispatches it
  - this aligns the batch write lane with the single-generation workflow
    boundary and removes one more implicit owner split in runtime-launch
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_runtime_launch_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_single_generation_write_workflow_service.rs`,
  `chapter_batch_generation_write_workflow_service.rs`, and
  `api/chapter_batch_generation.rs`
  - single-generation write workflows no longer keep a local
    `build_single_generation_write_workflow_request()` pass-through helper;
    route payload normalization now goes directly through
    `build_single_chapter_generation_request_from_route_payload()` at the
    write-workflow boundary
  - batch-generation create routes no longer delegate through the
    route-only `start_batch_generation_write_workflow_from_route()` wrapper;
    the route now prepares `BatchGenerationCreateWorkflowRequest` explicitly
    and calls `start_owned_batch_generation_write_workflow()` directly
  - this keeps the existing route payload contract, error mapper behavior,
    and runtime launch semantics while removing two more pure forwarding
    seams from the chapter-generation write lane
  - focused validation passed:
    `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_single_generation_write_workflow_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_regeneration_stream_request_service.rs`
  - `build_full_chapter_regeneration_stream_request_from_route_payload_typed()`
    no longer round-trips through `serde_json::json!` before building the
    typed request
  - typed route payload fields are now mapped directly into
    `FullChapterRegenerationStreamRequest`, keeping the same defaulting and
    filtering behavior while removing one unnecessary intermediate payload
    reconstruction
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_regeneration_stream_request_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_regeneration -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_analysis_trigger_service.rs`
  - `PreparedChapterAnalysisTrigger` no longer carries the prepared payload;
    dispatch and response assembly now derive the payload from the task/chapter
    identifiers at the workflow boundary, so the prepared trigger contract only
    keeps the fields the background dispatch actually needs
  - this shrinks the prepared trigger contract without changing the
    chapter-analysis background dispatch behavior or the returned payload
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_analysis_trigger_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_analysis_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice
- `chapter_batch_generation_create_task_command_service.rs`,
  `chapter_batch_generation_runtime_launch_service.rs`, and
  `chapter_batch_generation_write_workflow_service.rs`
  - batch create task planning no longer keeps the final response payload on
    `BatchGenerationCreatePlan`; the plan now only carries dispatch inputs
  - the workflow boundary now owns the create response payload via a
    `BatchGenerationCreateOutcome` wrapper, keeping the returned JSON shape
    stable while narrowing the plan / runtime contract
  - runtime launch now only dispatches create work and returns a sentinel
    value, so response assembly is no longer split across plan and runtime
    helpers
  - focused validation passed:
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_runtime_launch_service -- --nocapture`
    and
    `cargo test --manifest-path "backend-rs/Cargo.toml"
    chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    slice

### Wave 2: Provider seam tightening

Goal:

- remove any remaining route-local default provider assembly when the service
  boundary can own it without behavior change

Validation:

- `cargo check`
- targeted grep/sanity checks on the affected call path

Candidate focus:

- identify any remaining route-local or caller-local default provider payload
  assembly in batch-generation / single-generation paths
- move ownership only if the prepared request/workflow boundary can hold the
  default once without changing behavior

Completed checkpoint:

- `chapter_generation_runtime_service.rs`
  - removed the default provider-payload wrapper entrypoint
  - kept only the explicit `generate_and_persist_chapter_content_with_provider_payload()`
    boundary
  - `cargo check` passed after the slice
- `chapter_batch_generation_access_service.rs`
  - added `prepare_generation_execution_config()` so `AIConfig` plus default
    provider payload are prepared once in a shared helper
  - `chapter_batch_generation_create_workflow_service.rs`,
    `chapter_batch_generation_resume_service.rs`, and
    `chapter_single_generation_request_service.rs` now consume that helper
    instead of repeating local assembly
  - `cargo check` passed after the slice

### Wave 3: Route seam compression

Goal:

- keep route files transport-only once adjacent service semantics are stable

Validation:

- `cargo check`
- targeted payload-shape sanity review if route shaping changes

Current priority:

- lower than Wave 2 unless a no-risk route-only delegation cleanup is clearly
  isolated from behavior-sensitive files

### Wave 4: Phase 5 governance asset hardening

Goal:

- enter Phase 5 through executable governance assets, not by removing Python
  fallback prematurely

Execution rule:

- keep this wave limited to owner / smoke / rollback evidence
- do not couple Phase 5 work to new Rust business behavior or schema changes
- only promote a route-group slice when it can be validated through the
  gateway with stable expectations

Sub-waves:

1. P0 route-group smoke isolation
   - split `chapters` / `projects` / `wizard-stream` / `settings` /
     `memories` into a dedicated `phase5-p0` smoke profile
   - keep `deploy`, `route-groups`, and `business` profiles unchanged as
     broader control-plane / owner evidence lanes
2. P0 stronger business/SSE evidence
   - add one stronger business or SSE smoke slice for the highest-signal P0
     groups when the assertion can stay stable through the gateway
   - prefer structure/content-type assertions over time-sensitive values
3. rollback asset capture
   - turn the current route-group checklist rollback notes into executable or
     operator-ready per-group steps, starting with P0 groups
4. P1 route-group follow-up
   - extend stable owner/business smoke to `auth`, `users`, `characters`,
     `outlines`, and `book_import` only after P0 evidence is solid

Validation:

- `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`
- `python backend/tools/run_strangler_gateway_smoke.py --manifest deploy/strangler-gateway-probes.json --profile <profile> --validate-manifest-only`
- live gateway smoke only when the target environment is up and the probe is
  stable enough to avoid false positives

Current priority:

- start with P0 route-group smoke isolation and stronger owner evidence
- defer Python fallback removal until smoke + rollback assets are complete
- now that the P0/P1 governance assets are executable, it is also valid to
  continue low-risk `backend-rs` seam tightening in parallel, as long as each
  slice stays behavior-preserving and independently verifiable

Completed checkpoint:

- `chapter_single_generation_background_workflow_service.rs`
  - moved single-chapter background generation orchestration out of
    `chapter_batch_generation.rs`
  - route now delegates `prepare request + create task plan` to a dedicated
    workflow service before dispatching runtime work
  - `cargo check` passed after the slice
- `chapter_single_generation_stream_workflow_service.rs`
  - moved single-chapter stream orchestration out of
    `chapter_batch_generation.rs`
  - route now delegates `prepare request + build stream` to a dedicated
    workflow service before returning SSE transport state
  - `cargo check` passed after the slice
- `chapter_batch_generation_dispatch_service.rs`
  - added `dispatch_resume_generation_runtime()` so
    `chapter_batch_generation.rs` no longer owns `ResumeExecutionPlan`
    branching before runtime dispatch
  - `cargo check` passed after the slice
- `chapter_batch_generation_task_command_service.rs`
  - removed local task type / stage / execution-mode semantics helpers
  - now reuses shared `chapter_batch_generation_status_semantics_service`
    helpers while keeping the existing payload shape
  - `cargo check` passed after the slice
  - targeted `cargo test chapter_batch_generation_task_command_service`
    still fails due to pre-existing test compile errors outside this slice
- validation unblock checkpoint
  - fixed the stale `terminal_semantics` test import in
    `chapter_batch_generation_status_view_service.rs`
  - added the missing `serde_json::Value` test import in
    `chapter_quality_metrics_source_service.rs`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the fix
  - targeted `cargo test chapter_batch_generation_task_command_service` no
    longer fails on those source-level test import issues; the remaining
    failure is a Rust test compilation memory/stack-overrun problem in this
    environment
- cancel/runtime consistency checkpoint
  - `chapter_batch_generation_task_command_service.rs` now calls the existing
    cancelled runtime finalizer after explicit cancel
  - this keeps `task.status=cancelled` aligned with persisted
    `workflow_runtime_state/checkpoint` instead of leaving stale
    `generating/running` state behind
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the fix
- resume/runtime snapshot consistency checkpoint
  - `chapter_batch_generation_task_command_service.rs` now rebuilds the resume
    snapshot instead of merge-updating the old runtime checkpoint payload
  - batch-task resume clears stale `current_chapter_*`, resets checkpoint
    `completed/total` progress to a fresh pending state, and clears stale
    `failed_chapters`
  - resume snapshot replacement also clears stale
    `latest_quality_metrics/quality_metrics_history/quality_metrics_summary`
    so pending tasks do not keep exposing the previous terminal quality state
  - single-chapter resume still preserves the current chapter pointer
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the fix
  - `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml"`
    passed with the focused resume-checkpoint tests
- runtime/read-side semantics dedupe checkpoint
  - removed the stale status/read-side payload helpers from
    `chapter_batch_generation_runtime_state_service.rs`
  - runtime state service now keeps runtime snapshot/checkpoint execution
    helpers only, while status vocabulary and response payload assembly stay
    owned by `chapter_batch_generation_status_semantics_service.rs`,
    `chapter_batch_generation_quality_status_service.rs`, and
    `chapter_batch_generation_status_payload_adapter_service.rs`
  - this reduces the chance of future drift between runtime internals and
    status/query responses without changing HTTP or SSE payloads
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    cleanup
  - focused tests passed:
    `chapter_batch_generation_runtime_state_service`,
    `chapter_batch_generation_task_command_service`, and
    `chapter_batch_generation_status_payload_adapter_service`
- manual-review semantics dedupe checkpoint
  - `manual_review_label()` is now owned by
    `chapter_batch_generation_quality_status_service.rs`
  - `chapter_batch_generation_task_command_service.rs` reuses that helper for
    resume blocking instead of carrying a second copy of the same quality-gate
    parsing logic
  - focused tests now protect the shared manual-review label behavior used by
    both status payloads and resume blocking
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused tests passed:
    `chapter_batch_generation_task_command_service`,
    `chapter_batch_generation_status_view_service`, and
  - recent audit note:
    `backend/app/api/chapters.py` is now treated as a mixed compatibility
    shell rather than a fresh migration target; the next useful seam should
    stay on Rust-owned batch-generation semantics or route-only delegation
    cleanup
    `chapter_batch_generation_status_payload_adapter_service`
- chapter-regeneration stream workflow checkpoint
  - `chapter_regeneration_stream_workflow_service.rs` now owns the full and
    partial regeneration stream workflow assembly
  - `chapter_regeneration_routes.rs` delegates access loading, prepare-service
    calls, and stream input construction to that workflow service, leaving the
    route with HTTP/SSE boundary and error mapping only
  - this follows the Phase 3 route-to-workflow seam from
    `docs/architecture/chapter-api-gateway-seams.zh-CN.md` without changing
    prompt construction, SSE event shape, or generated result payloads
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_regeneration --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests and still
    reports the pre-existing `utils/sse.rs` unused `Event` warnings
- analysis/regeneration query ownership checkpoint
  - `chapter_analysis_query_service.rs` now owns access loading for the
    chapter analysis view and chapter quality metrics query helpers
  - `chapter_analysis_routes.rs` now delegates those two read routes to owned
    query helpers instead of loading the chapter in the route and then passing
    it through
  - `chapter_regeneration_query_service.rs` now owns access loading and limit
    normalization for regeneration task history queries
  - `chapter_regeneration_routes.rs` now delegates regeneration task history
    lookup to the owned query helper, keeping route-local logic to transport
    parsing and response mapping
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused filters compiled successfully:
    `cargo test chapter_analysis --manifest-path "backend-rs/Cargo.toml"` and
    `cargo test chapter_regeneration --manifest-path "backend-rs/Cargo.toml"`;
    both filters currently match no tests and still report the pre-existing
    `utils/sse.rs` unused `Event` warnings
- analysis draft workflow/query ownership checkpoint
  - `chapter_analysis_draft_service.rs` now owns the access loading, request
    parsing, and payload delegation for auto-revision draft load/apply and
    candidate draft load/apply routes
  - `chapter_analysis_routes.rs` now keeps those four draft endpoints as
    transport-only handlers that pass query/body data to owned service helpers
    and map typed service errors
  - `chapter_analysis_draft_error_mapper.rs` now maps the owned draft helper
    errors while reusing the existing draft error response semantics, including
    the history/attempt-id-specific 404 messages and stale/preview conflict
    responses
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_analysis --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests and still
    reports the pre-existing `utils/sse.rs` unused `Event` warnings
- partial-regeneration apply ownership checkpoint
  - `chapter_regeneration_apply_service.rs` now owns access loading and route
    default normalization for partial-regeneration apply requests
  - `chapter_regeneration_routes.rs` delegates `apply-partial-regenerate` to
    `apply_owned_partial_regenerate_payload()` and no longer loads the chapter
    or expands missing body fields locally
  - the existing `ApplyPartialRegenerateError` mapping remains unchanged, so
    empty text, workflow meta text, invalid range, not found, and internal
    failures preserve their HTTP response semantics
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_regeneration --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests and still
    reports the pre-existing `utils/sse.rs` unused `Event` warnings
- analysis trigger dispatch ownership checkpoint
  - `chapter_analysis_trigger_service.rs` now owns dispatching the prepared
    chapter-analysis background runtime
  - `chapter_analysis_routes.rs` now prepares the trigger, delegates dispatch,
    and returns the existing payload without directly spawning the background
    runtime task
  - task creation, runtime execution, response payload, and
    `PrepareChapterAnalysisTriggerError` mapping remain unchanged
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_analysis --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests and still
    reports the pre-existing `utils/sse.rs` unused `Event` warnings
- active batch task list query ownership checkpoint
  - `chapter_batch_generation_active_list_query_service.rs` now owns the
    active-task list limit default and clamp semantics
  - `chapter_batch_generation.rs` delegates the raw optional query limit to
    the owned query helper instead of normalizing it in the route
  - the default remains `20` and the allowed range remains `1..=100`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests and still
    reports the pre-existing `utils/sse.rs` unused `Event` warnings
- SSE validation cleanup checkpoint
  - `backend-rs/src/utils/sse.rs` tests now explicitly ignore the returned
    `Event` values in the no-panic smoke test
  - this removes the recurring unused `Event` warnings from focused Rust test
    runs without changing runtime SSE event builders or payload shape
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test sse --manifest-path "backend-rs/Cargo.toml"` passed with 5
    tests and no unused `Event` warnings
- single-chapter compatibility field ownership checkpoint
  - `chapter_single_generation_request_service.rs` now owns consumption of the
    background single-chapter generation compatibility-only `enable_analysis`
  field
  - `chapter_batch_generation.rs` delegates that compatibility field handling
    to the request service instead of swallowing it in the route handler
  - this preserves request compatibility without changing generation
    execution fields, provider payload ownership, response payloads, or SSE
    behavior
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - `cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml"`
    compiled successfully; the filter currently matches no tests
- single-chapter request builder ownership checkpoint
  - `chapter_single_generation_request_service.rs` now owns one
    `build_single_chapter_generation_request()` helper for the standard
    single-chapter request shape plus compat-only field consumption
  - `chapter_batch_generation.rs` no longer assembles the single-chapter
    request struct inline for background vs stream separately
  - this keeps transport/auth/workflow behavior unchanged while shrinking
    route-local request ownership
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused `cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 3 tests
- regeneration default-semantics explicitness checkpoint
  - `chapter_regeneration_query_service.rs` now owns
    `normalize_regeneration_tasks_limit()` for regeneration task history query
    defaults and clamping
  - `chapter_regeneration_stream_workflow_service.rs` now owns explicit
    helpers for partial-regeneration `context_chars` and
    `enable_web_research` defaults
  - this keeps the existing defaults unchanged (`limit` default `10`, clamp
    `1..=50`; `context_chars` default `500`; web research default `false`)
    while making the cross-layer default semantics testable instead of leaving
    them embedded in call expressions
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused tests passed:
    `chapter_regeneration_query_service` (1 test) and
    `chapter_regeneration_stream_workflow_service` (2 tests)
- active batch task list default-semantics explicitness checkpoint
  - `chapter_batch_generation_active_list_query_service.rs` now owns
    `normalize_active_batch_generation_task_list_limit()` for active batch
    task list query defaults and clamping
  - this keeps the existing default unchanged (`20`) and allowed range
    unchanged (`1..=100`) while making the query boundary default semantics
    testable
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 1 test
- batch generation create default-semantics explicitness checkpoint
  - `chapter_batch_generation_create_workflow_service.rs` now owns
    `normalize_batch_generation_enable_analysis()` and
    `normalize_batch_generation_max_retries()` for batch-create workflow
    defaults
  - this keeps the existing defaults unchanged (`enable_analysis=false`,
    `max_retries=3`) while making create-workflow default semantics testable
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    change
  - focused `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- generation target-word-count default-semantics test checkpoint
  - added focused tests for the existing
    `normalize_batch_generation_target_word_count()` helper in
    `chapter_batch_generation_create_service.rs`
  - added focused tests for the existing
    `normalize_single_chapter_generation_target_word_count()` and
    `load_chapter_generation_target()` helpers in
    `chapter_single_generation_request_service.rs`
  - this does not change runtime behavior; it protects the shared default
    target word count (`3000`), minimum clamp (`1`), and explicit target value
    preservation used by batch and single-chapter generation
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused tests passed:
    `chapter_batch_generation_create_service` (1 test) and
    `chapter_single_generation_request_service` (2 tests)
- analysis draft request default-semantics test checkpoint
  - added focused tests for `chapter_analysis_draft_request_service.rs`
    request parsing helpers
  - coverage now protects auto-revision vs candidate draft field ownership,
    trimmed/empty ID handling, and `allow_stale=false` default semantics
  - this does not change runtime behavior; it adds regression protection for
    the service-owned draft request boundary used by analysis draft routes
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_analysis_draft_request_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 4 tests
- regeneration prepare default-semantics test checkpoint
  - added focused tests for `chapter_regeneration_prepare_service.rs` pure
    helpers
  - coverage now protects partial-regeneration length-mode defaults,
    target-word calculation, override selected text handling, context
    extraction, invalid range rejection, and empty-selection rejection
  - this does not change runtime behavior; it adds regression protection for
    the service-owned partial-regeneration preparation boundary used by SSE
    workflows
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 5 tests
- regeneration prepare prompt/boundary default-semantics test checkpoint
  - added focused tests for `chapter_regeneration_prepare_service.rs` prompt
    and partial-regeneration boundary helpers
  - coverage now protects full-regeneration prompt defaults
    (`target_word_count=3000`, `preserve_structure=false`,
    `preserve_character_traits=true`), explicit prompt field projection, blank
    selected-text fallback to chapter content, edge context extraction, and
    partial-regeneration `max_tokens` floor/cap semantics
  - this does not change runtime behavior; it adds regression protection for
    the service-owned prompt/preparation contract used by regeneration SSE
    workflows
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 9 tests
- analysis query error-code semantics test checkpoint
  - added focused tests for `chapter_analysis_query_service.rs`
    `classify_analysis_error_code()` mapping
  - coverage now protects retrying, JSON parse/format failures, empty AI
    response, stream interruption, timeout/startup timeout, empty chapter,
    missing project, unknown-error fallback, and missing-error `None`
    semantics
  - this does not change runtime behavior or payload shape; it adds regression
    protection for the read-side analysis task status `error_code` field
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- partial-regeneration apply preparation seam checkpoint
  - `chapter_regeneration_apply_service.rs` now owns a pure
    `prepare_partial_regenerate_apply()` helper for sanitized replacement
    text validation, range validation, new-content assembly, and old word-count
    capture before the DB update boundary
  - `apply_partial_regenerate_payload()` now delegates preparation to that
    helper and keeps the existing `ChapterService::update()` response/error
    shape unchanged
  - focused tests protect successful content replacement, empty generated text,
    meta-only generated text becoming empty after sanitization, and invalid
    range rejection
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    seam extraction
  - focused `cargo test chapter_regeneration_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 4 tests
- batch status checkpoint metadata semantics test checkpoint
  - added focused tests for
    `chapter_batch_generation_status_payload_adapter_service.rs`
    `checkpoint_with_runtime_metadata()`
  - coverage now protects empty-runtime checkpoint metadata insertion and
    existing checkpoint field preservation while current `stage_code` /
    `execution_mode` override stale runtime metadata
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the status/read-side checkpoint adapter contract
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 4 tests
- regeneration text normalization/finalize semantics test checkpoint
  - added focused tests for `chapter_regeneration_text_service.rs`
    output normalization and finalization helpers
  - coverage now protects rewrite-prefix stripping, quote/bracket unwrapping,
    partial-regeneration result payload fields, full-chapter regeneration
    payload fields, and meta-only generated text becoming `EmptyContent` after
    sanitization
  - this does not change runtime behavior or SSE payload shape; it adds direct
    regression protection for the regeneration stream finalization contract
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_regeneration_text_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 4 tests
- batch quality status context semantics test checkpoint
  - added focused tests for
    `chapter_batch_generation_quality_status_service.rs`
    `active_story_repair_payload_from_runtime_state()` and
    `build_quality_status_context()`
  - coverage now protects object-only active story repair payload extraction,
    non-object payload rejection, snapshot quality metric/summary propagation,
    and empty-context defaults
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the batch read-side quality status context used
    by status view payload assembly
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_batch_generation_quality_status_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 4 tests
- analysis checker fragments semantics test checkpoint
  - added focused tests for `chapter_analysis_checker_query_service.rs`
    `build_chapter_analysis_checker_fragments()`
  - coverage now protects selecting the first valid
    `chapter_text_checker_v1` history, ignoring unrelated/invalid/missing
    checker-result histories, formatting checker creation time, and preserving
    `checker_result` when a matching history has no `created_at`
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the analysis view read-side checker fragment
    contract
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_analysis_checker_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 3 tests
- regeneration query datetime semantics test checkpoint
  - added focused tests for `chapter_regeneration_query_service.rs`
    `datetime_to_string()`
  - coverage now protects regeneration task history timestamp formatting
    (`%Y-%m-%dT%H:%M:%S`) and `None` propagation
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the regeneration task history read-side payload
    contract
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_regeneration_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- batch request compat projection semantics test checkpoint
  - added focused tests for
    `chapter_batch_generation_request_compat_service.rs`
    `project_batch_generation_request_compat_fields()`
  - coverage now protects projection of owned compatibility fields into
    borrowed view fields, including optional strings, booleans, vector slices,
    and all-default `None` semantics
  - this does not change runtime behavior or request compatibility; it adds
    direct regression protection for the service-owned legacy request field
    consumption boundary
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- analysis view payload adapter semantics test checkpoint
  - added focused tests for `chapter_analysis_view_payload_adapter_service.rs`
    `value_or_null()` and `build_chapter_analysis_view_payload()`
  - coverage now protects JSON null fallback, analysis field projection,
    memory projection and `is_foreshadow` boolean conversion, checker/draft
    fragment projection, quality metrics/summary projection, and top-level
    `created_at`
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the analysis view read-side response adapter
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_analysis_view_payload_adapter_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- chapter quality metrics payload adapter semantics test checkpoint
  - added focused tests for
    `chapter_quality_metrics_payload_adapter_service.rs`
    `build_chapter_quality_metrics_payload()`
  - coverage now protects `has_metrics`, `latest_metrics` /
    `latest_quality_metrics` compatibility duplication, history/timestamp
    projection, summary projection, and null/default response fields when no
    metrics are available
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the chapter quality metrics read-side response
    adapter
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_quality_metrics_payload_adapter_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- batch generation status semantics test checkpoint
  - added focused tests for
    `chapter_batch_generation_status_semantics_service.rs`
    `task_type()`, `task_stage_code()`, and `task_execution_mode()`
  - coverage now protects single-vs-batch task classification, malformed
    single-task fallback, status-to-stage-code mapping, unknown-status pending
    fallback, and current `interactive` execution-mode compatibility
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for shared batch status vocabulary consumed by
    status payloads and task commands
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_batch_generation_status_semantics_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 3 tests
- chapter quality query fragments semantics test checkpoint
  - added focused tests for `chapter_quality_query_service.rs`
    `build_chapter_analysis_quality_fragments()` and
    `build_chapter_quality_metrics_fragments()`
  - coverage now protects analysis-view quality summary construction without
    runtime context, metrics-view quality summary construction with runtime
    context, history id/timestamp projection, raw metrics preservation, and
    empty-fragment defaults when no metrics source is available
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for chapter quality read-side fragment assembly
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_quality_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 3 tests
- active batch generation query response semantics test checkpoint
  - added focused tests for `chapter_batch_generation_active_query_service.rs`
    `build_active_batch_generation_query_response()`
  - coverage now protects the no-active-task read-side response contract:
    `has_active_task=false` and `task=null`
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for the active batch query adapter empty-state
    response
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 1 test
- analysis runtime pure-helper semantics test checkpoint
  - added focused tests for `chapter_analysis_runtime_service.rs` pure helper
    semantics
  - coverage now protects JSON integer default/clamp behavior, non-finite
    float filtering, analysis task status normalization, composed analysis
    report section formatting, and empty-section fallback
  - this does not change runtime behavior or payload shape; it adds direct
    regression protection for analysis-runtime persistence normalization
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 5 tests
- chapter CRUD payload adapter semantics test checkpoint
  - added focused tests for `chapter_crud_payload_adapter_service.rs`
    response adapter helpers
  - coverage now protects compatible list payload duplication
    (`success`/`data`/`items`/`total`), project-path list payload shape, and
    compatible single-chapter payload top-level plus nested `data` projection
  - this does not change runtime behavior or response payload shape; it adds
    direct regression protection for chapter CRUD compatibility adapters
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test additions
  - focused `cargo test chapter_crud_payload_adapter_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 3 tests
- batch single-task chapter payload semantics test checkpoint
  - added a focused test for
    `chapter_batch_generation_chapter_payload_service.rs`
    `single_task_chapter_payload()`
  - coverage now protects the single-task batch chapter snapshot contract:
    only `id`, `chapter_number`, and `title` are projected into the stored
    chapter payload
  - this does not change runtime behavior or task payload shape; it adds
    direct regression protection against leaking chapter body/status fields
    into batch task chapter snapshots
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_batch_generation_chapter_payload_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 1 test
- chapter query payload helper checkpoint
  - extracted pure payload helpers in `chapter_query_service.rs` for chapter
    navigation and can-generate query responses
  - coverage now protects `previous` / `current` / `next` navigation response
    projection and boolean `can_generate` response projection
  - this preserves the existing async DB/service lookup and error mapping;
    only response JSON assembly moved into focused pure helpers
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests
- chapter annotations payload helper checkpoint
  - extracted a pure `annotations_payload()` helper in
    `chapter_annotation_query_service.rs`
  - coverage now protects the existing empty annotations response contract:
    `chapter_id`, `annotations=[]`, and `memory_mapping=[]`
  - this preserves the existing access check through `ChapterService::get()`
    and only moves response JSON assembly into a focused helper
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_annotation_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 1 test
- chapter draft apply payload helper checkpoint
  - extracted pure payload helpers in `chapter_draft_apply_service.rs` for
    candidate-draft generated-content payloads, candidate apply responses,
    auto-revision apply history payloads, and auto-revision apply responses
  - coverage now protects candidate quality-metrics null fallback, candidate
    apply response fields, auto-revision issue-count fallback/preservation,
    stale/allow-stale flags, timestamp formatting, and user-facing success
    messages
  - this preserves the existing DB transaction, stale validation, narrative
    sanitization, workflow-meta rejection, history insert, and route error
    mapping; only JSON assembly moved into focused pure helpers
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 5 tests
- chapter draft apply text preparation checkpoint
  - extracted `sanitize_apply_draft_text()` in
    `chapter_draft_apply_service.rs` so candidate-draft apply and
    auto-revision apply share the same service-owned generated-text
    sanitation boundary
  - coverage now protects successful candidate text cleanup, empty candidate
    text rejection, and auto-revision meta-prefix stripping before apply
  - this preserves the existing cleaner semantics: meta-only text becomes
    empty and is rejected, while removable meta prefix lines are stripped
    before the cleaned draft text is applied
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 8 tests
- chapter draft apply stale-validation checkpoint
  - extracted `validate_apply_draft_staleness()` in
    `chapter_draft_apply_service.rs` so candidate-draft apply and
    auto-revision apply share the same service-owned stale/allow-stale
    validation boundary
  - coverage now protects fresh draft application, stale candidate rejection
    when `allow_stale=false`, and stale auto-revision application when
    `allow_stale=true`
  - this preserves the existing `is_draft_stale()` semantics, typed
    `Stale` errors, response payload `stale_applied` field, DB transaction,
    and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 11 tests
- chapter draft apply word-count checkpoint
  - extracted `ApplyDraftWordCounts` and `apply_draft_word_counts()` in
    `chapter_draft_apply_service.rs` so candidate-draft apply and
    auto-revision apply share the same old/new word-count calculation boundary
  - coverage now protects old word-count clamping at zero and synchronized
    `i32` / `usize` new word-count projections used by DB updates, response
    payloads, and auto-revision history payloads
  - this preserves existing character-count semantics, response
    `old_word_count` / `word_count` fields, auto-revision history
    `new_word_count`, DB transaction behavior, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 13 tests
- chapter draft apply history-field checkpoint
  - extracted draft apply history model constants and prompt helpers in
    `chapter_draft_apply_service.rs`
  - coverage now protects the candidate draft apply history model/prompt and
    auto-revision draft apply history model/prompt, including the
    `chapter_text_reviser_apply_v1` log type used by the auto-revision history
    payload
  - this preserves existing generation-history `prompt`, `model`, and
    `log_type` values while keeping transaction and insert behavior unchanged
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 15 tests
- chapter draft apply history-model checkpoint
  - extracted candidate and auto-revision
    `generation_history::ActiveModel` assembly into focused helpers in
    `chapter_draft_apply_service.rs`
  - coverage now protects the persisted history-row field projection:
    `id`, `project_id`, `chapter_id`, `prompt`, `generated_content`,
    `model`, `tokens_used`, `generation_time`, and `created_at`
  - this preserves UUID/timestamp generation timing, DB transaction scope,
    insert order, response payloads, stale validation, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 17 tests
- chapter draft apply chapter-update-model checkpoint
  - extracted draft apply chapter update `chapter::ActiveModel` assembly into
    `draft_apply_chapter_update_model()` in
    `chapter_draft_apply_service.rs`
  - coverage now protects the shared chapter update field projection:
    `id`, `project_id`, `content`, `word_count`, `updated_at`, `title`, and
    `status`
  - this preserves DB transaction timing, update order, generated text
    sanitation, stale validation, history insertion, response payloads, and
    route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 18 tests
- chapter draft apply text-prepare checkpoint
  - extracted candidate and auto-revision draft apply text preparation into
    `prepare_candidate_draft_apply_text()` and
    `prepare_auto_revision_draft_apply_text()` in
    `chapter_draft_apply_service.rs`
  - coverage now protects complete candidate preview application,
    candidate preview-only rejection, and auto-revision missing
    `revised_text` rejection
  - this preserves generated-text sanitation, workflow-meta rejection,
    preview-only semantics, stale validation, DB transaction timing, history
    insertion, response payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 21 tests
- chapter draft apply issue-count checkpoint
  - extracted auto-revision apply issue-count normalization into
    `AutoRevisionApplyIssueCounts` and `auto_revision_apply_issue_counts()` in
    `chapter_draft_apply_service.rs`
  - coverage now protects priority issue-count fallback
    (`critical_count + major_count`) and `applied_issue_count` fallback from
    `applied_critical_count`, plus explicit value preservation
  - this preserves auto-revision history payload fields, generated-content
    serialization, DB transaction timing, history insertion, response
    payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 23 tests
- chapter draft query auto-revision count checkpoint
  - extracted auto-revision draft-view count normalization into
    `AutoRevisionDraftViewCounts` and `auto_revision_draft_view_counts()` in
    `chapter_draft_query_service.rs`
  - coverage now protects priority issue-count fallback
    (`critical_count + major_count`), applied issue-count fallback from
    `applied_critical_count`, revised word-count fallback from `revised_text`,
    and explicit value preservation
  - this preserves auto-revision draft view payload fields, full-text
    inclusion behavior, stale detection, datetime formatting, query behavior,
    response payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper extraction
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 2 tests

- chapter draft query candidate full-content checkpoint
  - added focused tests for `extract_candidate_draft_full_content()` in
    `chapter_draft_query_service.rs`
  - coverage now protects candidate full-content extraction precedence,
    `content_complete=true` preview promotion, word-count-matched preview
    promotion, and preview-only rejection
  - this preserves candidate draft view payload fields, apply eligibility
    semantics, full-text inclusion behavior, query behavior, response
    payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 6 tests

- chapter draft query candidate item normalization checkpoint
  - added focused tests for `normalize_candidate_items()` in
    `chapter_draft_query_service.rs`
  - coverage now protects trimming, deduplication, empty/unsupported value
    filtering, supported object field precedence, and limit truncation
  - this preserves candidate draft highlights, risk points, recommended
    actions, preserved strengths, response payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 9 tests

- chapter draft query datetime/staleness checkpoint
  - added focused tests for `format_datetime()` and `is_draft_stale()` in
    `chapter_draft_query_service.rs`
  - coverage now protects timezone-free datetime formatting, `None`
    passthrough, stale detection when the chapter update timestamp is newer
    than the draft timestamp, and non-stale equality/missing-timestamp cases
  - this preserves draft view `created_at` formatting, stale marker
    semantics, response payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 11 tests

- chapter draft query reviser-history parse checkpoint
  - added focused tests for `parse_reviser_result_from_history()` in
    `chapter_draft_query_service.rs`
  - coverage now protects successful `chapter_text_reviser_v1` parsing,
    `reviser_result` object extraction, and rejection of missing content,
    invalid JSON, wrong `log_type`, missing reviser result, and non-object
    reviser result payloads
  - this preserves auto-revision draft discovery, query fallback behavior,
    response payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 13 tests

- chapter draft query auto-revision payload checkpoint
  - added focused tests for `build_auto_revision_draft_payload()` in
    `chapter_draft_query_service.rs`
  - coverage now protects preview fallback from trimmed revised text,
    `include_full_text` gating, `history_id`/`created_at` null handling,
    stale marker projection, unresolved-issues defaults, and explicit
    unresolved issue preservation
  - this preserves auto-revision draft view payload shape, full-text
    inclusion behavior, stale marker semantics, response payloads, and route
    error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 15 tests

- chapter draft query candidate payload checkpoint
  - added focused tests for `build_candidate_draft_payload()` in
    `chapter_draft_query_service.rs`
  - coverage now protects full-content projection, `include_full_text`
    behavior, stale marker projection, quality/repair field projection,
    repair-summary precedence, failed-metric projection, repair-payload
    fallback highlights/risks, and preview-only non-applyable payloads
  - this preserves candidate draft view payload shape, apply eligibility
    semantics, full-text inclusion behavior, response payloads, and route
    error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 17 tests

- chapter draft query analysis-view fragments checkpoint
  - added focused tests for `build_chapter_draft_analysis_view_fragments()` in
    `chapter_draft_query_service.rs`
  - coverage now protects selection of the first valid auto-revision
    `chapter_text_reviser_v1` history after invalid entries, candidate draft
    fragment projection, exclusion of full text from analysis-view fragments,
    stale marker projection, and empty-input fragment shape
  - this preserves chapter analysis view draft fragment aggregation, response
    payloads, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 19 tests

- chapter draft apply response/null timestamp checkpoint
  - added focused regression tests for draft apply response/history payload
    compatibility in `chapter_draft_apply_service.rs`
  - coverage now protects candidate and auto-revision apply response shape
    when `draft_created_at` is absent, auto-revision apply history payload
    shape when source/applied timestamps are absent, default issue-count
    projection in that missing-timestamp branch, and direct revised-text
    preparation from `revised_text`
  - this preserves draft apply response payloads, history payloads,
    null timestamp projection, text sanitation behavior, DB transaction
    ordering, UUID/timestamp generation timing, and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 27 tests

- chapter draft query candidate fallback checkpoint
  - added focused regression tests for candidate draft query payload fallback
    compatibility in `chapter_draft_query_service.rs`
  - coverage now protects `created_at: null` projection for candidate draft
    payloads, non-stale behavior when draft timestamp is absent, summary-preview
    fallback into display `content_preview`, preservation of raw
    `summary_preview`, and exclusion of full candidate text from analysis-view
    fragments when `include_full_text=false`
  - this preserves candidate draft query payload shape, preview fallback
    semantics, full-text gating, stale marker semantics, response payloads,
    and route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    test addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 22 tests

- chapter draft query candidate compat fields checkpoint
  - restored Python/frontend-compatible candidate draft response fields in
    `chapter_draft_query_service.rs`: `repair_targets`,
    `preserve_strengths`, and `focus_areas`
  - the fields reuse existing candidate item normalization and preserve the
    Python source precedence: `repair_payload` before `repair_guidance` for
    repair/preserve fields, and `repair_guidance` before `quality_gate` for
    focus areas
  - added focused tests for compat field projection, fallback precedence, and
    empty-array defaults in preview-only payloads
  - this preserves candidate draft response adapter compatibility with
    frontend `ChapterCandidateDraft`, while keeping existing
    `recommended_actions` / `preserved_strengths` fields intact
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 24 tests

- chapter draft query failed-metric compat checkpoint
  - restored Python/frontend-compatible `failed_metrics[].repair_target`
    projection in `chapter_draft_query_service.rs`
  - aligned missing failed-metric numeric fields with Python behavior by
    defaulting `value`, `threshold`, and `gap` to `0.0` instead of `null`
  - added focused tests for explicit failed-metric field projection and
    missing-number defaults
  - this preserves candidate draft response adapter compatibility with
    frontend `ChapterCandidateDraftFailedMetric` and Python
    `_build_candidate_draft_payload`
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter addition
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 25 tests

- chapter draft query candidate display-field parity checkpoint
  - aligned candidate draft query display fields with Python
    `_build_candidate_draft_payload`
  - `source` and `attempt_state` are now trimmed in the response adapter,
    `summary_preview` is trimmed before projection, and `word_count` falls
    back to full-content length when the stored draft word count is zero
  - added focused tests for source/state trimming, summary-preview trimming,
    and fallback word-count projection
  - this preserves frontend-facing `ChapterCandidateDraft` display semantics
    while keeping full-text gating and draft apply eligibility unchanged
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 26 tests

- chapter draft query candidate apply-risk parity checkpoint
  - added `candidate_apply_risk_payload()` in
    `chapter_draft_query_service.rs` to match Python fallback semantics for
    candidate draft `apply_risk`
  - explicit `apply_risk` payloads are still preserved; when absent, the
    adapter now derives warning items from quality-highlight missing items,
    quality-gate failed metric labels, and warning/manual-review gate state
    or action
  - added focused helper tests plus a `build_candidate_draft_payload()`
    integration-style unit test for generated fallback `apply_risk`
  - this preserves frontend `candidateDraft.apply_risk` behavior without
    changing DB queries, full-text gating, or apply eligibility
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 30 tests

- chapter draft query candidate string-normalization parity checkpoint
  - added `trimmed_string_or_null()` in `chapter_draft_query_service.rs` for
    Python-compatible candidate draft string projection
  - `failed_metrics[].key` and `failed_metrics[].label` now trim surrounding
    whitespace, while blank `focus_area` / `repair_target` values project as
    `null` instead of blank strings
  - `repair_summary` now follows Python `_build_candidate_draft_payload`
    semantics by trimming the selected repair/guidance summary and projecting
    blank values as `null`
  - added focused regression coverage for blank failed-metric fields and blank
    repair summaries without changing DB queries, full-text gating, apply
    eligibility, or route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 31 tests

- chapter draft query numeric-string parity checkpoint
  - added `json_i64()` and `json_f64()` in
    `chapter_draft_query_service.rs` so Rust response adapters accept numeric
    JSON strings in the same places Python builders accept `int(...)` /
    `float(...)` coercion
  - auto-revision draft view counts now preserve numeric-string
    `critical_count`, `major_count`, `priority_issue_count`,
    `applied_critical_count`, `applied_issue_count`, and
    `revised_word_count` values instead of falling back to zero/default counts
  - candidate draft `failed_metrics[].value`, `threshold`, and `gap` now
    preserve numeric-string values instead of falling back to `0.0`
  - added focused regression coverage for auto-revision numeric-string counts
    and candidate failed-metric numeric strings without changing DB queries,
    full-text gating, apply eligibility, or route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 32 tests

- chapter draft apply numeric-string parity checkpoint
  - added `json_i64()` in `chapter_draft_apply_service.rs` so
    auto-revision apply history issue-count assembly accepts numeric JSON
    strings in the same places Python builders accept `int(...)` coercion
  - auto-revision apply history now preserves numeric-string `critical_count`,
    `major_count`, `priority_issue_count`, `applied_critical_count`, and
    `applied_issue_count` values instead of falling back to zero/default counts
  - added focused regression coverage for numeric-string issue counts without
    changing DB transactions, route error mapping, stale validation, text
    sanitation, or history insert ordering
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 28 tests

- chapter draft numeric parser reuse checkpoint
  - promoted `chapter_draft_query_service::json_i64()` to `pub(crate)` and
    reused it from `chapter_draft_apply_service.rs`
  - removed the duplicate apply-local JSON integer parser so draft query view
    counts and auto-revision apply history issue counts share the same
    numeric-string coercion semantics
  - no response fields, DB writes, stale validation, text sanitation, or route
    error mapping changed in this slice
  - targeted `rustfmt --edition 2021 --check` passed for both touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    helper reuse
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 28 tests
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 32 tests

- chapter draft candidate item scalar parity checkpoint
  - aligned `normalize_candidate_items()` in `chapter_draft_query_service.rs`
    with Python `_normalize_candidate_draft_items()` for scalar values
  - standalone JSON number / bool values and array number / bool values now
    normalize to strings instead of being dropped, preserving frontend
    candidate list and apply-risk item compatibility when stored payloads use
    scalar values
  - object-field extraction remains intentionally constrained to the existing
    supported keys and does not stringify arbitrary nested objects
  - added focused regression coverage for scalar number/bool normalization
    without changing DB queries, response field names, full-text gating, apply
    eligibility, or route error mapping
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 33 tests

- chapter draft candidate item object-scalar parity checkpoint
  - extended `normalize_candidate_items()` so supported array-object fields
    (`label`, `name`, `value`, `item`) can also project JSON number / bool
    values through the existing scalar string conversion path
  - this keeps array-object extraction aligned with standalone-object
    extraction while still avoiding arbitrary object stringification
  - updated focused scalar normalization coverage for array objects with
    numeric `value` and boolean `label`, preserving the existing decision to
    ignore unsupported `summary` inside array objects
  - no response field names, DB queries, full-text gating, apply eligibility,
    stale validation, or route error mapping changed
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 33 tests

- chapter draft candidate item Python-truthiness parity checkpoint
  - tightened `normalize_candidate_items()` scalar conversion to follow
    Python `_normalize_candidate_draft_items()` truthiness semantics more
    closely
  - JSON `false` and numeric zero values are now dropped like Python
    `str(value or "")`, while JSON `true` projects as `True`
  - updated focused scalar normalization coverage for standalone and array
    false/zero values plus true casing
  - no response field names, DB queries, full-text gating, apply eligibility,
    stale validation, or route error mapping changed
  - targeted `rustfmt --edition 2021 --check` passed for the touched Rust file
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 33 tests

- chapter draft numeric bool-coercion parity checkpoint
  - extended shared `json_i64()` and query-local `json_f64()` to accept JSON
    bools with Python-compatible numeric coercion (`true` => `1` / `1.0`,
    `false` => `0` / `0.0`)
  - auto-revision draft view counts, auto-revision apply issue counts, and
    candidate failed-metric numeric projections now share this bool coercion
    behavior
  - added focused query coverage for bool-based auto-revision counts and
    failed-metric threshold/gap values, plus focused apply coverage for
    bool-based auto-revision issue counts
  - no response field names, DB queries/writes, full-text gating, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - targeted `rustfmt --edition 2021 --check` passed for both touched Rust
    files
  - `cargo check --manifest-path "backend-rs/Cargo.toml"` passed after the
    adapter alignment
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 34 tests
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 29 tests

- chapter draft failed-metric scalar-string parity checkpoint
  - extracted shared `python_truthy_scalar_text()` for Python-compatible
    scalar string projection across candidate item normalization and candidate
    failed-metric string fields
  - candidate failed metrics now project non-string scalar `key`, `label`,
    `focus_area`, and `repair_target` values like Python
    `str(value or "").strip()` without stringifying arrays or objects
  - preserved Python label fallback semantics by falling back from falsey or
    blank `label` to `key`, while optional focus/repair fields still return
    `null` for falsey or blank values
  - added focused query coverage for numeric/bool scalar failed-metric fields
    and label fallback behavior
  - no response field names, DB queries/writes, full-text gating, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 35 tests

- chapter draft repair-summary truthiness parity checkpoint
  - aligned candidate draft `repair_summary` with Python fallback semantics:
    use `repair_payload.summary` only when it is truthy after scalar
    conversion, otherwise fall back to `repair_guidance.summary`
  - reused the same Python-truthy scalar conversion used by candidate items
    and failed-metric string fields, and removed the now-dead string-only
    helper
  - added focused query coverage for falsey repair-payload summary falling
    back to numeric guidance summary
  - no response field names, DB queries/writes, full-text gating, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 36 tests

- chapter draft compat-item fallback parity checkpoint
  - added `normalize_candidate_items_with_fallback()` so candidate compat
    list fields use the primary source only when it normalizes to non-empty
    items, matching Python `primary or fallback` behavior for falsey payloads
  - aligned candidate `repair_targets`, `preserve_strengths`, and
    `focus_areas` fallback behavior when repair/guidance fields are `false`,
    `0`, or empty arrays
  - preserved `preserved_strengths` as the repair-payload-only field and did
    not change quality-highlight, apply-risk, full-text, stale, DB, route,
    SSE, or runtime semantics
  - added focused query coverage for falsey primary compat fields falling
    back to guidance / quality-gate sources
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 37 tests

- chapter draft apply-risk failed-metric label parity checkpoint
  - added `candidate_failed_metric_labels()` so fallback `apply_risk` quality
    gate warning text is derived only from `failed_metrics[].label`, matching
    the Python builder instead of normalizing arbitrary failed-metric object
    fields
  - preserved Python truthiness for scalar labels, so numeric/bool labels are
    still normalized while missing labels do not fall back to `key`, `value`,
    `name`, or `item`
  - added focused query coverage for label-only failed-metric apply-risk
    fallback behavior
  - no response field names, DB queries/writes, full-text gating, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 38 tests

- chapter draft auto-revision revised-text whitespace parity checkpoint
  - aligned auto-revision draft `revised_text` handling with Python by
    preserving the raw string instead of trimming before deriving
    `revised_text_preview`, `has_full_text`, and optional full-text payload
  - updated existing preview-default coverage and added focused whitespace
    coverage for full-text inclusion plus whitespace-only revised text
  - no response field names, DB queries/writes, candidate draft fields, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 39 tests

- chapter draft auto-revision revised-word-count payload parity checkpoint
  - aligned auto-revision draft `revised_word_count` payload projection with
    Python by preserving an explicit `revised_word_count` value as-is instead
    of coercing it to an integer in the response payload
  - kept the existing integer parser for internal count/default semantics, so
    missing `revised_word_count` still falls back to the untrimmed
    `revised_text` character count
  - added focused query coverage for explicit string/bool
    `revised_word_count` values and missing-value fallback
  - no response field names, DB queries/writes, candidate draft fields, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 40 tests

- chapter draft auto-revision falsey-count fallback parity checkpoint
  - added shared `python_truthy_json_i64()` for Python-compatible
    `value or fallback` numeric fields where false/zero should trigger the
    fallback rather than become the final value
  - aligned auto-revision draft view `priority_issue_count` and
    `applied_issue_count` fallback behavior with Python
  - aligned auto-revision apply-history issue-count fallback behavior with the
    same helper so query/apply adapters do not drift
  - added focused query/apply coverage for falsey count fallback semantics
  - no response field names, DB queries/writes, candidate draft fields, apply
    eligibility, stale validation, route error mapping, or SSE/runtime
    semantics changed
  - focused `cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 41 tests
  - focused `cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml"`
    passed with 30 tests

## Handoff Note

- Current stop point: Wave 1 read-side/status hardening, Wave 2 provider seam
  tightening, and the high-signal Wave 3 route-to-workflow slices have
  completed. The latest slices moved chapter-regeneration full/partial stream
  workflow assembly into a dedicated workflow service, moved analysis /
  regeneration read-route access/query ownership into service helpers, and
  moved analysis draft load/apply plus partial-regeneration apply
  orchestration into service-owned helpers. The chapter-analysis trigger
  route now also delegates runtime dispatch to the trigger service. The
  active batch task list route also delegates limit normalization to the query
  service. The final tail cleanup moved single-chapter background-generation
  compatibility field consumption into the single-chapter request service. The
  latest regeneration cleanup made query/stream workflow defaults explicit and
  covered them with focused pure-helper tests. The latest batch query cleanup
  also made active task list limit defaults explicit and covered them with a
  focused pure-helper test. The latest batch-create cleanup made
  `enable_analysis` and `max_retries` defaults explicit and covered them with
  focused pure-helper tests. The latest test-only cleanup added regression
  coverage for batch and single-chapter target-word-count defaults. The latest
  analysis draft request cleanup added regression coverage for draft request
  parsing defaults and ID ownership. The regeneration prepare cleanup added
  regression coverage for partial-regeneration length and selection
  preparation semantics. The latest regeneration prepare cleanup added
  regression coverage for full-regeneration prompt defaults, explicit prompt
  field projection, selected-text fallback/context edges, and `max_tokens`
  clamp semantics. The latest analysis query cleanup added regression coverage
  for analysis task status `error_code` classification. The latest
  partial-regeneration apply cleanup extracted a pure preparation helper and
  covered replacement/range/sanitization semantics. The latest batch status
  payload cleanup added direct regression coverage for checkpoint runtime
  metadata insertion/override semantics. The latest regeneration text cleanup
  added regression coverage for output normalization and finalization payloads.
  The latest batch quality status cleanup added regression coverage for
  read-side quality context extraction/defaults. The latest analysis checker
  cleanup added regression coverage for checker history fragment selection and
  timestamp semantics. The latest regeneration query cleanup added regression
  coverage for task-history datetime formatting. The latest batch request
  compat cleanup added regression coverage for legacy request field projection
  into borrowed view fields. The latest analysis view payload cleanup added
  regression coverage for response adapter field/null/fragment projection. The
  latest chapter quality metrics payload cleanup added regression coverage for
  quality metrics response adapter compatibility fields. The latest batch
  status semantics cleanup added regression coverage for shared task type /
  stage / execution-mode vocabulary. The latest chapter quality query cleanup
  added regression coverage for quality fragment summary/runtime-context
  semantics. The latest active batch query cleanup added regression coverage
  for empty active-task response shape. The latest analysis runtime cleanup
  added regression coverage for JSON normalization, analysis status
  normalization, and analysis-report section assembly. The latest chapter CRUD
  payload adapter cleanup added regression coverage for list/single response
  compatibility payload shapes. The latest batch chapter payload cleanup added
  regression coverage for the single-task chapter snapshot projection. The
  latest chapter query cleanup extracted focused payload helpers for
  navigation and can-generate responses. The latest annotations query cleanup
  extracted the empty annotations payload helper and covered its response
  shape. The latest chapter draft apply cleanup extracted focused payload
  helpers for candidate and auto-revision draft apply response/history JSON
  assembly without changing transaction or validation behavior. The latest
  chapter draft apply cleanup also extracted the shared draft apply text
  sanitation helper and covered empty-text rejection plus meta-prefix stripping
  semantics. The latest chapter draft apply cleanup also extracted the shared
  stale/allow-stale validation helper and covered fresh, rejected-stale, and
  explicitly-allowed-stale branches. The latest chapter draft apply cleanup
  also extracted the shared old/new word-count calculation helper and covered
  negative old-count clamping plus synchronized new-count projections. The
  latest chapter draft apply cleanup also extracted history model constants
  and prompt helpers for candidate and auto-revision draft apply history rows.
  The latest chapter draft apply cleanup also extracted candidate and
  auto-revision generation-history ActiveModel assembly helpers and covered
  the persisted history-row field projection directly. The latest chapter
  draft apply cleanup also extracted the shared chapter update ActiveModel
  assembly helper and covered the chapter update field projection directly.
  The latest chapter draft apply cleanup also extracted candidate and
  auto-revision draft apply text preparation helpers and covered complete
  preview application, preview-only rejection, and missing revised-text
  rejection semantics. The latest chapter draft apply cleanup also extracted
  auto-revision issue-count normalization and covered fallback plus explicit
  value preservation semantics. The latest chapter draft query cleanup also
  extracted auto-revision draft-view count normalization and covered fallback
  plus explicit value preservation semantics. The latest chapter draft query
  cleanup also added direct regression coverage for candidate draft
  full-content extraction precedence and preview-completeness semantics. The
  latest chapter draft query cleanup also added direct regression coverage for
  candidate item normalization used by highlights, risk points, recommended
  actions, and preserved strengths. The latest chapter draft query cleanup
  also added direct regression coverage for datetime formatting and stale
  detection semantics shared by draft query/apply flows. The latest chapter
  draft query cleanup also added direct regression coverage for auto-revision
  reviser-history payload parsing and invalid payload rejection. The latest
  chapter draft query cleanup also added direct regression coverage for
  auto-revision draft payload projection, full-text gating, stale markers, and
  unresolved-issue defaults. The latest chapter draft query cleanup also
  added direct regression coverage for candidate draft payload projection,
  apply eligibility, repair-field fallbacks, and preview-only response shape.
  The latest chapter draft query cleanup also added direct regression coverage
  for chapter analysis-view draft fragment aggregation and empty-input shape.
  The latest chapter draft apply cleanup also added direct regression coverage
  for candidate/auto-revision apply response null timestamp projection,
  auto-revision apply history null timestamp/default issue-count projection,
  and direct revised-text apply preparation.
  The latest chapter draft query cleanup also added direct regression coverage
  for candidate draft missing-created-at projection, summary-preview fallback,
  raw summary preservation, and analysis-view full-text exclusion.
  The latest chapter draft query cleanup also restored Python/frontend
  compatible candidate draft `repair_targets`, `preserve_strengths`, and
  `focus_areas` fields with focused source-precedence coverage.
  The latest chapter draft query cleanup also restored failed-metric
  `repair_target` projection and Python-compatible missing-number defaults.
  The latest chapter draft query cleanup also aligned candidate draft display
  fields with Python by trimming source/state/summary and using full-content
  length when stored draft word count is zero.
  The latest chapter draft query cleanup also aligned candidate draft
  `apply_risk` fallback generation with Python quality-highlight /
  quality-gate semantics while preserving explicit payloads.
  The latest chapter draft query cleanup also aligned candidate failed-metric
  string projection and repair-summary blank handling with Python by trimming
  labels/keys and returning `null` for blank optional strings.
  The latest chapter draft query cleanup also aligned numeric-string coercion
  with Python for auto-revision draft counts and candidate failed metric
  numbers.
  The latest chapter draft apply cleanup also aligned auto-revision apply
  history issue-count numeric-string coercion with the query-side adapter.
  The latest chapter draft cleanup also removed the duplicate apply-local
  numeric parser by reusing the query-side `json_i64()` helper for both draft
  view and apply-history count adapters.
  The latest chapter draft query cleanup also aligned candidate item
  normalization with Python for scalar number/bool payloads while keeping
  object extraction constrained to supported fields.
  The latest chapter draft query cleanup also reused the same scalar
  conversion for supported fields inside array objects without stringifying
  unsupported object payloads.
  The latest chapter draft query cleanup also tightened scalar conversion to
  Python truthiness semantics: false/zero values are dropped and true projects
  as `True`.
  The latest chapter draft cleanup also aligned numeric bool coercion with
  Python for shared integer counts and failed-metric floating-point fields.
  The latest chapter draft query cleanup also reused that Python-truthy scalar
  conversion for candidate failed-metric `key`, `label`, `focus_area`, and
  `repair_target` fields while keeping array/object values ignored.
  The latest chapter draft query cleanup also aligned candidate `repair_summary`
  fallback with Python truthiness and removed the unused string-only helper.
  The latest chapter draft query cleanup also aligned candidate compat list
  fallback semantics for `repair_targets`, `preserve_strengths`, and
  `focus_areas` after empty/falsey primary normalization.
  The latest chapter draft query cleanup also aligned fallback `apply_risk`
  failed-metric warning text with Python by reading only
  `failed_metrics[].label`.
  The latest chapter draft query cleanup also aligned auto-revision draft
  `revised_text` whitespace handling with Python so preview fallback and
  `has_full_text` are derived from the untrimmed raw text.
  The latest chapter draft query cleanup also aligned auto-revision draft
  `revised_word_count` payload projection with Python by preserving explicit
  values and only using the computed count as the missing-value fallback.
  The latest chapter draft cleanup also aligned falsey numeric fallback
  semantics for auto-revision `priority_issue_count` and `applied_issue_count`
  across query and apply-history adapters.
- Validation status: `cargo check` passed after the latest slice; touched-file
  rustfmt check passed; targeted `chapter_analysis` / `chapter_regeneration`
  test filtering compiled successfully but currently has no matching tests.
  `cargo test sse` now passes with 5 tests and no recurring unused `Event`
  warnings. The latest `chapter_single_generation_request_service` focused
  test filter also compiles successfully but currently matches no tests. The
  latest regeneration default-semantics tests pass for query and stream
  workflow helpers. The latest active batch task list default-semantics test
  also passes. The latest batch-create default-semantics tests also pass. The
  latest target-word-count default-semantics tests also pass. The latest
  analysis draft request parsing/default-semantics tests also pass. The latest
  regeneration prepare prompt/boundary default-semantics tests pass with 9
  focused tests. The latest analysis query error-code tests pass with 2
  focused tests. The latest partial-regeneration apply tests pass with 4
  focused tests. The latest batch status payload adapter tests pass with 4
  focused tests. The latest regeneration text tests pass with 4 focused tests.
  The latest batch quality status context tests pass with 4 focused tests. The
  latest analysis checker query tests pass with 3 focused tests. The latest
  regeneration query tests pass with 2 focused tests. The latest batch request
  compat projection tests pass with 2 focused tests. The latest analysis view
  payload adapter tests pass with 2 focused tests. The latest chapter quality
  metrics payload adapter tests pass with 2 focused tests. The latest batch
  status semantics tests pass with 3 focused tests. The latest chapter quality
  query tests pass with 3 focused tests. The latest active batch query tests
  pass with 1 focused test. The latest analysis runtime helper tests pass with
  5 focused tests. The latest chapter CRUD payload adapter tests pass with 3
  focused tests. The latest batch single-task chapter payload adapter test
  passes with 1 focused test. The latest chapter query payload helper tests
  pass with 2 focused tests. The latest annotations payload helper test passes
  with 1 focused test. The latest chapter draft apply payload helper tests
  pass with 5 focused tests. The latest chapter draft apply text preparation
  tests pass as part of the same focused filter, which now passes with 8
  focused tests. The latest chapter draft apply stale-validation tests pass as
  part of the same focused filter, which now passes with 11 focused tests. The
  latest chapter draft apply word-count tests pass as part of the same focused
  filter, which now passes with 13 focused tests. The latest chapter draft
  apply history-field tests pass as part of the same focused filter, which now
  passes with 15 focused tests. The latest chapter draft apply history-model
  tests pass as part of the same focused filter, which now passes with 17
  focused tests. The latest chapter draft apply chapter-update-model test
  passes as part of the same focused filter, which now passes with 18 focused
  tests. The latest chapter draft apply text-prepare tests pass as part of the
  same focused filter, which now passes with 21 focused tests. The latest
  chapter draft apply issue-count tests pass as part of the same focused
  filter, which now passes with 23 focused tests. The latest chapter draft
  query auto-revision, candidate full-content, and candidate item
  normalization plus datetime/staleness, reviser-history parse, and
  auto-revision/candidate payload and analysis-view fragment tests pass with
  19 focused tests. The latest chapter draft apply payload compatibility tests
  pass with 27 focused tests. The latest chapter draft query candidate
  fallback tests pass with 22 focused tests. The latest chapter draft query
  candidate compat-field tests pass with 24 focused tests. The latest chapter
  draft query failed-metric compat tests pass with 25 focused tests. The
  latest chapter draft query display-field parity tests pass with 26 focused
  tests. The latest chapter draft query apply-risk parity tests pass with 30
  focused tests. The latest chapter draft query string-normalization parity
  tests pass with 31 focused tests. The latest chapter draft query
  numeric-string parity tests pass with 32 focused tests. The latest chapter
  draft apply numeric-string parity tests pass with 28 focused tests. The
  latest draft numeric parser reuse validation kept query at 32 focused tests
  and apply at 28 focused tests. The latest chapter draft query scalar-item
  parity tests pass with 33 focused tests. The latest chapter draft query
  object-scalar item parity validation kept query at 33 focused tests. The
  latest Python-truthiness scalar-item parity validation kept query at 33
  focused tests. The latest numeric bool-coercion parity validation raises
  query to 34 focused tests and apply to 29 focused tests. The latest
  failed-metric scalar-string parity validation raises query to 35 focused
  tests. The latest repair-summary truthiness parity validation raises query
  to 36 focused tests. The latest compat-item fallback parity validation
  raises query to 37 focused tests. The latest apply-risk failed-metric label
  parity validation raises query to 38 focused tests. The latest
  auto-revision revised-text whitespace parity validation raises query to 39
  focused tests. The latest auto-revision revised-word-count payload parity
  validation raises query to 40 focused tests. The latest falsey-count
  fallback parity validation raises query to 41 focused tests and apply to 30
  focused tests.
- Recommended next step: prefer final quality review and broader validation
  before more structural refactor. Remaining route code is mostly transport
  glue; continue only if a concrete compatibility-safe seam is found, not just
  because a file can be made smaller.
- First files to inspect next session, if continuing:
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  (read-only unless a concrete compatibility-safe seam is proven).
- Keep avoiding broad cross-file dedupe with
  `chapter_batch_generation_runtime_state_service.rs` until a concrete
  compatibility-safe seam is identified.
- Query/status routes are already thin enough; do not reopen them unless a new
  cross-route transport helper clearly removes non-trivial duplication.
- Treat `chapter_batch_generation_dispatch_service.rs` and
  `chapter_batch_generation_runtime_state_service.rs` as high-skepticism areas:
  do not refactor them further without a seam that reduces compatibility risk,
  not just local duplication.
- `chapter_batch_generation.rs` is nearing a route-thinning stop point; prefer
  leaving remaining code in place unless a new workflow boundary removes more
  than one transport-local branch or repeated request assembly step.
- `chapter_batch_generation_runtime_state_service.rs` and
  `chapter_batch_generation_task_command_service.rs` should also be treated as
  near-stop unless a future slice clearly removes semantic drift without
  reopening snapshot/checkpoint write ownership.
- validation is now effectively blocked by test compilation resource limits
  rather than by obvious local source errors in this task's touched area.
- focused validation for `chapter_batch_generation_task_command_service`
  currently passes again in this environment after the resume-consistency fix;
  broader test limits should still be treated as environment-sensitive until a
  wider pass is rerun.
- the remaining batch-generation seams now lean strongly toward write-path
  ownership changes, so further refactor should stop unless a concrete
  user-visible inconsistency or multi-consumer drift is found first.
- after the runtime/read-side helper cleanup, the remaining route and runtime
  moves are even closer to diminishing returns; prefer quality review and
  final validation over additional structure-only changes.
- current route/runtime stop review found no remaining route-local default
  provider assembly, checkpoint/snapshot writes, SSE payload semantics, or
  duplicated status/quality/checkpoint semantics worth moving. Treat the
  route-to-service refactor as at the Phase 3 stop point unless a future
  user-visible inconsistency is proven.
- 2026-05-19 13:12 +08:00 checkpoint:
  chapter draft auto-revision scalar text parity now matches Python
  `str(value or "")` semantics for query/apply adapters. The shared
  `python_truthy_scalar_text()` helper is crate-visible and is reused by
  auto-revision draft query payload construction and auto-revision draft apply
  text preparation. Focused regression coverage now confirms numeric/truthy
  boolean `revised_text` and `revised_text_preview` are projected like Python,
  while falsey scalar `revised_text` remains empty and rejected on apply.
  Validation: touched-file rustfmt check passed; `cargo test
  chapter_draft_query_service --manifest-path backend-rs/Cargo.toml` passed
  with 42 tests; `cargo test chapter_draft_apply_service --manifest-path
  backend-rs/Cargo.toml` passed with 31 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed.
- 2026-05-19 13:12 +08:00 checkpoint:
  candidate draft full-content extraction now also reuses the shared Python
  truthy scalar projection for `repair_payload.candidate_full_content`.
  Numeric/truthy scalar full content is preserved consistently in query
  payloads and candidate apply preparation, while falsey scalar full content
  still falls through to existing preview completeness rules. Validation:
  touched-file rustfmt check passed; `cargo test chapter_draft_query_service
  --manifest-path backend-rs/Cargo.toml` passed with 43 tests; `cargo test
  chapter_draft_apply_service --manifest-path backend-rs/Cargo.toml` passed
  with 32 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed.
- 2026-05-19 13:12 +08:00 checkpoint:
  candidate draft `repair_payload.content_complete` now follows Python
  truthiness instead of Rust bool-only parsing. Non-empty strings, non-zero
  numbers, non-empty arrays/objects, and `true` can mark preview content as
  complete, while falsey JSON values still fall through to the existing
  preview/word-count checks. Validation: touched-file rustfmt check passed;
  `cargo test chapter_draft_query_service --manifest-path backend-rs/Cargo.toml`
  passed with 44 tests; `cargo test chapter_draft_apply_service --manifest-path
  backend-rs/Cargo.toml` passed with 32 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed.
- 2026-05-19 13:13 +08:00 checkpoint:
  candidate draft `content_preview` display fallback now matches Python
  `content_preview or summary_preview or ""` semantics. An explicitly empty
  `content_preview` no longer suppresses a non-empty `summary_preview`, while
  existing whitespace trimming and full-content preview fallback remain
  unchanged. Validation: touched-file rustfmt check passed; `cargo test
  chapter_draft_query_service --manifest-path backend-rs/Cargo.toml` passed
  with 45 tests; `cargo test chapter_draft_apply_service --manifest-path
  backend-rs/Cargo.toml` passed with 32 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed.
- 2026-05-19 13:14 +08:00 checkpoint:
  candidate fallback `apply_risk` now mirrors Python `quality_gate_decision or
  quality_gate.decision` semantics. An explicitly empty route/model decision
  no longer suppresses a non-empty `quality_gate.decision`, and gate status /
  decision fallback text uses the shared Python truthy scalar projection.
  Validation: touched-file rustfmt check passed; `cargo test
  chapter_draft_query_service --manifest-path backend-rs/Cargo.toml` passed
  with 46 tests; `cargo test chapter_draft_apply_service --manifest-path
  backend-rs/Cargo.toml` passed with 32 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed.
- 2026-05-19 13:15 +08:00 checkpoint:
  explicit empty candidate `apply_risk` objects no longer survive into the
  Rust payload. This now matches Python, which only returns a populated risk
  object or `None`; the frontend will no longer treat `{}` as a truthy
  warning card and render the fallback risk summary by mistake. Validation:
  touched-file rustfmt check passed; `cargo test chapter_draft_query_service
  --manifest-path backend-rs/Cargo.toml` passed with 47 tests; `cargo test
  chapter_draft_apply_service --manifest-path backend-rs/Cargo.toml` passed
  with 32 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed.
- 2026-05-19 13:16 +08:00 checkpoint:
  explicit empty candidate `quality_highlights` objects now also collapse to
  `null` for Python parity. This keeps the candidate comparison modal from
  receiving a truthy-but-empty highlights object, while preserving existing
  non-empty highlight payloads and derived `highlight_points`. Validation:
  touched-file rustfmt check passed; `cargo test chapter_draft_query_service
  --manifest-path backend-rs/Cargo.toml` passed with 48 tests; `cargo test
  chapter_draft_apply_service --manifest-path backend-rs/Cargo.toml` passed
  with 32 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed.
- 2026-05-19 13:44 +08:00 checkpoint:
  legacy-only candidate `apply_risk` payloads now split cleanly between the
  UI-facing `apply_risk` object and the compat-only `risk_points` list. Rust
  now matches Python by collapsing `apply_risk` to `null` when the source
  object only carries legacy `risk_points`, while still preserving those
  points for older consumers. Validation: touched-file rustfmt check passed;
  `cargo test chapter_draft_query_service --manifest-path backend-rs/Cargo.toml`
  passed with 51 tests; `cargo test chapter_draft_apply_service
  --manifest-path backend-rs/Cargo.toml` passed with 32 tests; `cargo check
  --manifest-path backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/services/chapter_draft_query_service.rs backend-rs/src/services/chapter_draft_apply_service.rs`
  passed.
- 2026-05-19 13:44 +08:00 checkpoint:
  legacy-only candidate `quality_highlights` payloads now also split cleanly
  between the UI-facing `quality_highlights` facet object and the compat-only
  `highlight_points` list. Rust now keeps `quality_highlights=null` unless
  the payload contains meaningful `continuity` or `foreshadow` facet data,
  while still preserving legacy `highlight_points` for compatibility. This
  keeps the frontend comparison modal aligned with its typed facet contract
  without dropping the legacy summary list. Validation: touched-file rustfmt
  check passed; `cargo test chapter_draft_query_service --manifest-path
  backend-rs/Cargo.toml` passed with 51 tests; `cargo test
  chapter_draft_apply_service --manifest-path backend-rs/Cargo.toml` passed
  with 32 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed;
  `git diff --check -- backend-rs/src/services/chapter_draft_query_service.rs backend-rs/src/services/chapter_draft_apply_service.rs`
  passed.
- 2026-05-19 13:50 +08:00 checkpoint:
  `chapter_batch_generation` status SSE route now also follows the existing
  route-to-workflow seam. `chapter_batch_generation.rs` no longer performs the
  local `stream access gate + stream builder` orchestration; that ownership is
  moved into the new
  `chapter_batch_generation_stream_workflow_service.rs` helper, matching the
  already-adopted single-generation and regeneration stream workflow pattern
  without changing keep-alive behavior, error mapping, or event payload
  semantics. Validation: touched-file rustfmt check passed; `cargo test
  chapter_batch_generation_status_stream_service --manifest-path
  backend-rs/Cargo.toml` passed with 3 tests; `cargo test
  chapter_batch_generation --manifest-path backend-rs/Cargo.toml` passed with
  35 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed;
  `git diff --check -- backend-rs/src/api/chapter_batch_generation.rs backend-rs/src/services/chapter_batch_generation_stream_workflow_service.rs backend-rs/src/services/mod.rs`
  passed.
- 2026-05-19 13:54 +08:00 checkpoint:
  `chapter_batch_generation` resume route now also keeps only transport
  concerns. `chapter_batch_generation.rs` no longer owns the local
  `prepare resume request + dispatch runtime + return response payload`
  orchestration; that sequence is now wrapped by
  `resume_owned_batch_generation_task()` in
  `chapter_batch_generation_resume_service.rs`, so the existing resume
  prepare helper becomes the internal workflow boundary instead of a
  route-visible intermediate step. This preserves the current error mapping,
  response payload, and runtime dispatch semantics while shrinking another
  behavior-sensitive route branch. Validation: touched-file rustfmt check
  passed; `cargo test chapter_batch_generation_resume_service --manifest-path
  backend-rs/Cargo.toml` compiled successfully with the current filter
  matching 0 tests; `cargo test chapter_batch_generation --manifest-path
  backend-rs/Cargo.toml` passed with 35 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/services/chapter_batch_generation_resume_service.rs backend-rs/src/api/chapter_batch_generation.rs`
  passed.
- 2026-05-19 13:57 +08:00 checkpoint:
  the remaining low-risk `chapter_batch_generation` route orchestration has
  now been compressed into workflow-owned dispatch boundaries. Both
  `create_batch_generate` and `generate_chapter_content_background` keep their
  request parsing and compat-field consumption in the route, but the local
  `create workflow + dispatch runtime + return response payload` sequencing is
  now owned by
  `start_owned_batch_generation_workflow()` and
  `start_owned_single_generation_background_workflow()` in the corresponding
  workflow services. This aligns batch-create and single-background generation
  with the same thin-route pattern already used by batch status stream and
  resume, without changing response payloads or runtime dispatch semantics.
  Validation: touched-file rustfmt check passed; `cargo test
  chapter_batch_generation --manifest-path backend-rs/Cargo.toml` passed with
  35 tests; `cargo test chapter_single_generation_background_workflow_service
  --manifest-path backend-rs/Cargo.toml` compiled successfully with the
  current filter matching 0 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs backend-rs/src/services/chapter_single_generation_background_workflow_service.rs backend-rs/src/api/chapter_batch_generation.rs`
  passed.
- 2026-05-19 14:01 +08:00 checkpoint:
  chapter-domain route registration is now pulled back under a single gateway
  entrypoint. `backend-rs/src/api/chapters.rs` now also merges
  `chapter_batch_generation::routes()`, and `backend-rs/src/api/router.rs`
  no longer registers batch-generation routes separately at the top-level API
  router. This does not change any route path or handler behavior; it only
  aligns route ownership with the existing chapter gateway split so chapter
  CRUD, analysis, regeneration, and batch-generation routes are once again
  grouped under one chapter-domain aggregator. Validation: touched-file
  rustfmt check passed; `cargo test chapter_batch_generation --manifest-path
  backend-rs/Cargo.toml` passed with 35 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapters.rs backend-rs/src/api/router.rs`
  passed.
- 2026-05-19 14:06 +08:00 checkpoint:
  chapter-generation SSE routes now reuse a shared keep-alive helper instead
  of rebuilding the same 10-second keep-alive configuration inline. The new
  helpers in `backend-rs/src/utils/sse.rs` centralize the default 10-second
  keep-alive and the named `"keep-alive"` variant, and the chapter batch
  generation plus chapter regeneration routes now call those helpers instead
  of carrying route-local `KeepAlive::new().interval(...)` copies. This keeps
  SSE event payloads and stream behavior unchanged while reducing boundary
  drift across chapter-domain streaming routes. Validation: touched-file
  rustfmt check passed; `cargo test chapter_batch_generation --manifest-path
  backend-rs/Cargo.toml` passed with 35 tests; `cargo test
  chapter_regeneration --manifest-path backend-rs/Cargo.toml` passed with 21
  tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/utils/sse.rs backend-rs/src/api/chapter_batch_generation.rs backend-rs/src/api/chapter_regeneration_routes.rs`
  passed.
- 2026-05-19 14:11 +08:00 checkpoint:
  chapter-domain API error mappers now reuse shared detail-response helpers
  instead of repeating the same JSON/status projection inline. The shared
  helper set in `backend-rs/src/api/chapters_error_mapper.rs` now owns the
  generic `detail_error`, `internal_detail_error`, and the stable
  `chapter/project not found or access denied` responses, and chapter
  analysis draft/query plus regeneration query error mappers now delegate to
  those helpers. This keeps status codes and response messages unchanged while
  reducing repeated error-shape logic across chapter-domain routes. Validation:
  touched-file rustfmt check passed; `cargo test chapter_analysis
  --manifest-path backend-rs/Cargo.toml` passed with 17 tests; `cargo test
  chapter_regeneration --manifest-path backend-rs/Cargo.toml` passed with 21
  tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/api/chapters_error_mapper.rs backend-rs/src/api/chapter_analysis_query_error_mapper.rs backend-rs/src/api/chapter_analysis_draft_error_mapper.rs backend-rs/src/api/chapter_regeneration_query_error_mapper.rs`
  passed.
- 2026-05-19 14:16 +08:00 checkpoint:
  refactor progress was re-aligned against the planning documents before the
  next implementation wave. The active follow-up task is now explicitly
  treated as Phase 3 (`Rust` internal boundary shrink) work from
  `docs/architecture/rust-strangler-refactor-plan-2026-05-17.zh-CN.md`, not
  as Phase 1 deployment hardening or Phase 2 schema-ownership work. The
  review also confirmed that the low-risk `chapter_batch_generation` route
  seam has effectively reached its stop point, so further safe work should
  stay in shared helper / read-side reuse instead of entering
  `runtime_state/status_view/task_command` semantics. As part of that next
  safe lane, `chapter_batch_generation_error_mapper.rs` now also delegates its
  repeated `detail` JSON plus stable chapter/project access-denied responses
  to the shared helper set in `chapters_error_mapper.rs`, keeping status codes
  and messages unchanged while reducing another drift-prone adapter copy.
  Validation: touched-file rustfmt check passed; `cargo test
  chapter_batch_generation --manifest-path backend-rs/Cargo.toml` passed with
  35 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/api/chapter_batch_generation_error_mapper.rs backend-rs/src/api/chapters_error_mapper.rs`
  passed.
- 2026-05-19 14:20 +08:00 checkpoint:
  chapter CRUD error mapping now has one less local drift point. The repeated
  `success:false/message` response branches for stable
  `project/chapter not found or access denied` cases and internal-message
  passthroughs were consolidated inside
  `backend-rs/src/api/chapter_crud_error_mapper.rs` via focused local helpers,
  while preserving the existing CRUD-specific compat response shape instead of
  forcing it into the chapter-domain `detail` JSON pattern. This keeps CRUD
  HTTP semantics and payload structure unchanged while reducing the repeated
  adapter code in a low-risk file. Validation: touched-file rustfmt check
  passed; `cargo test chapter_crud --manifest-path backend-rs/Cargo.toml`
  passed with 3 tests; `cargo check --manifest-path backend-rs/Cargo.toml`
  passed; `git diff --check -- backend-rs/src/api/chapter_crud_error_mapper.rs`
  passed.
- 2026-05-19 14:31 +08:00 checkpoint:
  the Phase 3 safe-lane helper reuse continued with one smaller cleanup in
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs`. The owned
  chapter-analysis view error path still keeps its special-case
  `"Chapter analysis not found"` -> `404` behavior, but the final
  `(status, {"detail": ...})` projection now delegates to the shared
  `detail_error()` helper from `chapters_error_mapper.rs` instead of building
  the same JSON shape inline again. This keeps HTTP semantics and payload
  shape unchanged while removing another tiny route-adjacent mapper drift
  point without re-entering batch runtime/status semantics. Validation:
  touched-file rustfmt check passed; `cargo test chapter_analysis
  --manifest-path backend-rs/Cargo.toml` passed with 17 tests; `cargo check
  --manifest-path backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapter_analysis_query_error_mapper.rs`
  passed.
- 2026-05-19 14:39 +08:00 checkpoint:
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` now also
  removes one more local drift point inside its single-chapter lane. The
  request-preparation and background-workflow error mappers used to carry two
  copies of the same `ChapterNotFound / ChapterNotFoundOrAccessDenied /
  Config / Internal` projection logic; they now both delegate to one focused
  local `map_single_chapter_generation_error()` helper in the same file.
  This keeps all existing status codes and `detail` messages unchanged while
  reducing another adapter-only duplication point in the Phase 3 safe lane,
  without touching batch runtime/status semantics or route payload shapes.
  Validation: touched-file rustfmt check passed; `cargo test
  chapter_batch_generation --manifest-path backend-rs/Cargo.toml` passed with
  35 tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  passed.
- 2026-05-19 14:50 +08:00 checkpoint:
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` now pushes more
  of its stable `detail`-response projection through the shared chapter-domain
  helper set instead of rebuilding `Json({"detail": ...})` inline for every
  fixed-message branch. The auto-revision and candidate draft load/apply
  mappers still preserve the same `404 / 400 / 409 / 500` status codes and
  the same Chinese `detail` messages, but the route-adjacent mapper now
  reuses `detail_error()` for those constant branches while continuing to use
  `internal_detail_error()` for passthrough internal failures. This keeps the
  externally observable API behavior unchanged while removing another small
  duplication cluster inside the Phase 3 safe lane. Validation: touched-file
  rustfmt check passed; `cargo test chapter_analysis --manifest-path
  backend-rs/Cargo.toml` passed with 17 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapter_analysis_draft_error_mapper.rs`
  passed.
- 2026-05-19 14:58 +08:00 checkpoint:
  the same `chapter_analysis_draft_error_mapper.rs` slice was tightened one
  step further by removing its local `chapter_not_found_error()` wrapper,
  which only forwarded to the shared
  `chapter_not_found_or_access_denied_error()` helper. The mapper now calls
  the shared chapter-domain helper directly for analysis task status and the
  owned auto-revision / candidate draft access-denied branches, keeping all
  existing `404` payload shapes and messages unchanged while trimming one more
  route-adjacent alias layer from the Phase 3 safe lane. Validation:
  touched-file rustfmt check passed; `cargo test chapter_analysis
  --manifest-path backend-rs/Cargo.toml` passed with 17 tests; `cargo check
  --manifest-path backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapter_analysis_draft_error_mapper.rs`
  passed.
- 2026-05-19 15:06 +08:00 checkpoint:
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs` was also
  tightened by removing the local `map_chapter_quality_metrics_query_error()`
  alias, which had become a pure wrapper around the shared
  `internal_detail_error()` helper after the owned-query migration. The owned
  quality-metrics mapper now routes its internal failure branch directly to
  `internal_detail_error()` while keeping the existing access-denied `404`
  path and all external payload shapes unchanged. This trims one more tiny
  route-adjacent alias layer in the Phase 3 safe lane without touching query
  ownership or analysis payload semantics. Validation: touched-file rustfmt
  check passed; `cargo test chapter_analysis --manifest-path
  backend-rs/Cargo.toml` passed with 17 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapter_analysis_query_error_mapper.rs`
  passed.
- 2026-05-19 15:13 +08:00 checkpoint:
  the analysis lane was tightened once more by removing the local
  `map_batch_analysis_task_status_query_error()` alias from
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs`. That helper had
  become a pure wrapper around the shared `internal_detail_error()` response
  mapper, so `backend-rs/src/api/chapter_analysis_routes.rs` now sends the
  batch analysis status route's internal failure branch directly to
  `internal_detail_error()` instead. This keeps the same `500 {"detail":
  ...}` behavior while trimming one more route-adjacent alias layer from the
  Phase 3 safe lane without changing batch analysis query ownership or route
  payload semantics. Validation: touched-file rustfmt check passed; `cargo
  test chapter_analysis --manifest-path backend-rs/Cargo.toml` passed with 17
  tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/api/chapter_analysis_query_error_mapper.rs backend-rs/src/api/chapter_analysis_routes.rs`
  passed.
- 2026-05-19 15:20 +08:00 checkpoint:
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs` was tightened
  one more step by inlining the single-use `map_chapter_analysis_view_error()`
  helper back into `map_owned_chapter_analysis_view_error()`. The special
  `"Chapter analysis not found"` -> `404` branch and the default
  internal-error `500 {"detail": ...}` branch remain exactly the same, but
  the mapper no longer carries a one-call wrapper just to compute the status
  before delegating to `detail_error()`. This trims another small local alias
  layer from the Phase 3 safe lane without changing analysis view ownership or
  payload semantics. Validation: touched-file rustfmt check passed; `cargo
  test chapter_analysis --manifest-path backend-rs/Cargo.toml` passed with 17
  tests; `cargo check --manifest-path backend-rs/Cargo.toml` passed; `git
  diff --check -- backend-rs/src/api/chapter_analysis_query_error_mapper.rs`
  passed.
- 2026-05-19 15:28 +08:00 checkpoint:
  the analysis lane also removed one more duplicate mapper implementation by
  switching the analysis task-status route to the shared
  `map_load_analysis_task_status_error()` helper in
  `backend-rs/src/api/chapters_error_mapper.rs`. The local
  `map_analysis_task_status_error()` copy was deleted from
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs`, and
  `backend-rs/src/api/chapter_analysis_routes.rs` now maps
  `load_analysis_task_status_payload()` failures through the shared helper
  instead. This keeps the same `404` and `500 {"detail": ...}` behavior while
  removing a true duplicate implementation from the Phase 3 safe lane without
  changing analysis task-status payload semantics. Validation: touched-file
  rustfmt check passed; `cargo test chapter_analysis --manifest-path
  backend-rs/Cargo.toml` passed with 17 tests; `cargo check --manifest-path
  backend-rs/Cargo.toml` passed; `git diff --check -- backend-rs/src/api/chapter_analysis_draft_error_mapper.rs backend-rs/src/api/chapter_analysis_routes.rs`
  passed.
- 2026-05-19 16:57 +08:00 checkpoint:
  the follow-up execution temporarily shifted from Rust-internal seam
  shrinking into Phase 5 strangler governance asset hardening so the current
  gateway owner map is backed by executable evidence instead of stale Nginx
  comments or prior assumptions. `backend/tools/run_strangler_gateway_smoke.py`
  now supports manifest `profiles`, `deploy-strangler.ps1` explicitly runs the
  `deploy` profile only, and `deploy/strangler-gateway-probes.json` now also
  carries a `route-groups` profile with through-gateway owner probes for
  `auth/config`, `changelog`, `settings`, `projects`, `wizard-stream`, and
  `memories`. Running the new route-group profile against the current gateway
  on `http://127.0.0.1:8005` also falsified an older planning assumption:
  the currently exposed `projects`, `wizard-stream`, and `/api/memories/*`
  API paths already resolve to Rust-owned auth/public boundaries, so the Phase
  5 docs were corrected away from the previous blanket `mixed` label and now
  treat the remaining issue as stale fallback configuration / owner
  documentation drift. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py` passed (6
  tests); `python backend/tools/run_strangler_gateway_smoke.py --profile
  deploy --validate-manifest-only` passed; `python
  backend/tools/run_strangler_gateway_smoke.py --profile route-groups
  --validate-manifest-only` passed; live `route-groups` probe run against
  `127.0.0.1:8005` passed with 9 probes; `git diff --check -- backend/tools/run_strangler_gateway_smoke.py backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json deploy-strangler.ps1 docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md`
  passed with only existing line-ending warnings from Git.
- 2026-05-19 17:00 +08:00 checkpoint:
  the Phase 5 executable owner evidence was expanded into the chapter gateway,
  which is the highest-risk Rust-owned API domain in the strangler plan.
  `deploy/strangler-gateway-probes.json` now adds five low-precondition
  `route-groups` probes for the `chapters` surface:
  `GET /api/chapters?project_id=...`, `GET /api/chapters/{id}/analysis`,
  `POST /api/chapters/analysis/status/batch`,
  `GET /api/chapters/batch-generate/active-tasks`, and
  `GET /api/chapters/{id}/regeneration/tasks`. A live run against
  `http://127.0.0.1:8005` confirmed all five currently resolve to the Rust
  auth boundary with the stable `401 {"detail":"未登录，请先登录"}` shape,
  which means the chapter CRUD, analysis, batch, and regeneration lanes are
  now all represented in the route-group smoke evidence chain. The Phase 5
  docs were updated to record that `chapters` now has first-pass owner probes
  in place, while explicitly leaving stronger stream/business smoke for a
  later slice. Validation target for this checkpoint: live `route-groups`
  smoke should now pass with 14 probes total.
- 2026-05-19 17:39 +08:00 checkpoint:
  the Phase 5 gateway-smoke asset now has enough expressiveness to carry the
  next business/SSE wave instead of being limited to status-code plus JSON
  subset assertions. `backend/tools/run_strangler_gateway_smoke.py` now
  accepts per-probe `headers`, `json_body` / `body`,
  `expected_text_startswith`, and `expected_text_contains`, while preserving
  the existing `expected_json` and `expected_content_type_contains`
  assertions. Focused pytest coverage now locks the new request forwarding,
  conflicting-body validation, and text-prefix/contains checks. As a first
  low-risk consumer of that capability, the existing route-group POST owner
  probes for `POST /api/chapters/analysis/status/batch` and
  `POST /api/wizard-stream/outline` now send minimal JSON request bodies plus
  a dedicated smoke header, so those through-gateway owner checks are closer
  to real call shapes without changing the default deploy profile. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py`
  passed with 9 tests; `python backend/tools/run_strangler_gateway_smoke.py
  --manifest deploy/strangler-gateway-probes.json --profile route-groups
  --validate-manifest-only` passed; live `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile route-groups --base-url
  http://127.0.0.1:8005` passed with 14 probes; `git diff --check -- backend/tools/run_strangler_gateway_smoke.py backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json`
  passed with the existing Git line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 19:24 +08:00 checkpoint:
  the Phase 5 smoke manifest now has a separate `business` profile for public
  JSON/HTML assertions, so the stable control-plane probes can stay isolated
  from the default deploy slice. `deploy/strangler-gateway-probes.json` now
  marks the shared root probe, `GET /api/auth/config`, and
  `GET /api/changelog` as `business` probes. The root probe asserts the page
  title and `lang="zh-CN"` marker, `auth/config` locks the stable
  `linuxdo_enabled=false` plus `local_auth_enabled=true` JSON shape, and
  `changelog` now checks only the stable JSON field names/content markers
  instead of a time-sensitive cache value. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py`
  passed with 10 tests; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile business --validate-manifest-only`
  passed; live `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile business --base-url
  http://127.0.0.1:8005` passed with 3 probes; `git diff --check -- backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing Git line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 19:32 +08:00 checkpoint:
  the default deploy smoke slice gained one more stable Rust-owned health
  probe: `GET /health/db-sessions`. The endpoint returns a fixed JSON
  structure with zeroed session counters and a nullable warning field, so it
  is a safe low-risk addition to the `deploy` / `route-groups` profile and
  does not depend on login state or business data. Focused tests now cover
  the new manifest shape, and the live deploy smoke on
  `http://127.0.0.1:8005` passed with four probes total
  (`/health`, `/readyz`, `/health/db-sessions`, `/`). Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py`
  passed with 11 tests; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile deploy --validate-manifest-only`
  passed; live `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile deploy --base-url
  http://127.0.0.1:8005` passed with 4 probes; `git diff --check -- backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json`
  passed with the existing Git line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 19:38 +08:00 checkpoint:
  the deploy smoke slice now also covers `GET /livez`, so the Rust health
  surface is represented by three fixed JSON probes (`/health`, `/livez`,
  `/readyz`) plus the structured `/health/db-sessions` check. This keeps the
  default deploy profile fully on Rust-owned control-plane endpoints while
  staying independent of login state and mutable business data. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py`
  passed with 12 tests; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile deploy --validate-manifest-only`
  passed; live `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile deploy --base-url
  http://127.0.0.1:8005` passed with 5 probes; `git diff --check -- backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json`
  passed with the existing Git line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 19:44 +08:00 checkpoint:
  the route-group ownership and parity docs were updated to match the current
  health smoke truth instead of the earlier two-probe shorthand. The health
  route group is now documented as `health / livez / readyz`, with
  `rust-health`, `rust-livez`, `rust-readiness`, and
  `rust-health-db-sessions` explicitly listed as stable smoke evidence.
  The parity matrix now also names the current
  `deploy/strangler-gateway-probes.json` coverage for `/health`, `/livez`,
  `/readyz`, and `/health/db-sessions`. Validation for this doc-only update:
  `git diff --check -- docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed.
- 2026-05-19 19:55 +08:00 checkpoint:
  the gateway smoke manifest now supports `expected_json_has_keys`, allowing
  structural JSON assertions without pinning time-sensitive values. This was
  used to tighten `GET /api/changelog` from a text-substring fallback to a
  stable key-structure assertion over `commits`, `cached`, and `cache_time`,
  while keeping the existing content-type check. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py`
  passed with 14 tests; live `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile business --base-url
  http://127.0.0.1:8005` passed with 3 probes; `git diff --check -- backend/tools/run_strangler_gateway_smoke.py backend/tests/test_tools/test_run_strangler_gateway_smoke.py deploy/strangler-gateway-probes.json`
  passed with the existing Git line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 21:48 +08:00 checkpoint:
  the Phase 5 wave moved from stronger-smoke evaluation into rollback asset
  capture for the P0 route groups because the current `settings`,
  `projects`, `chapters`, `wizard-stream`, and `memories` gateway probes still
  skew toward auth-boundary owner evidence rather than stable public business
  or SSE assertions. `backend/tools/run_strangler_gateway_smoke.py` now
  supports repeated `--probe-name` filtering on top of the existing `profile`
  selection, which made it possible to validate a single route-group rollback
  step before re-running the whole `phase5-p0` slice. The first operator-ready
  rollback asset now lives in
  `docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md`,
  and the Phase 5 ownership/parity docs now reference that runbook as the
  current rollback source of truth for `settings`, `projects`, `chapters`,
  `wizard-stream`, and `memories`. Validation target for this checkpoint:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --probe-name
  settings-auth-guard-rust --validate-manifest-only`, and `git diff --check`
  over the touched smoke/doc files should all pass.
- 2026-05-19 21:52 +08:00 checkpoint:
  the P0 rollback asset is now one step more executable because the smoke
  manifest gained explicit `route_group` ownership labels for `settings`,
  `projects`, `chapters`, `wizard-stream`, and `memories`, while
  `backend/tools/run_strangler_gateway_smoke.py` now supports repeated
  `--route-group` filtering before any optional `--probe-name` narrowing. This
  removes the need for operators to manually translate a route group back into
  a probe-name list when validating a rollback step, and the new runbook now
  uses `--route-group` as the primary command shape while keeping
  `--probe-name` as an escape hatch for single-probe diagnosis. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`
  passed with 20 tests; `python backend/tools/run_strangler_gateway_smoke.py
  --manifest deploy/strangler-gateway-probes.json --profile phase5-p0
  --route-group chapters --validate-manifest-only` passed with `probe_count=5`;
  `git diff --check -- backend/tools/run_strangler_gateway_smoke.py
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:11 +08:00 checkpoint:
  the next Phase 5 slice stayed on governance assets and did not expand the
  smoke runner again. Instead, the P0 rollback runbook now records a first
  matrix of Python fallback success clues for `settings`, `projects`,
  `chapters`, `wizard-stream`, and `memories`, so operators have a concrete
  owner-change checklist after a gateway rollback even before a dedicated
  fallback smoke profile exists. The documented clues intentionally stay at
  the transport/auth-boundary level: Python `settings` should fall back to
  `401 {"detail":"需要登录"}`, `projects` / `memories` to
  `401 {"detail":"未登录"}`, chapter read/query routes mostly to
  `401 {"detail":"未登录"}` while Python batch-generation active-task listing
  keeps the older English `401 {"detail":"Not logged in"}`, and
  `wizard-stream/outline` should re-enter the Python dependency/auth path
  before the SSE body is produced. The docs also now call out that
  `GET /api/chapters?project_id=...` is not a valid Python fallback success
  probe because the Python list route shape is `/api/chapters/project/{id}`.
  Validation target for this checkpoint: `git diff --check` over the touched
  Phase 5 docs should pass.
- 2026-05-19 22:15 +08:00 checkpoint:
  the documented Python fallback clues are now partially executable instead of
  staying doc-only. `deploy/strangler-gateway-probes.json` now defines a
  dedicated `phase5-p0-fallback` profile that covers the lowest-precondition
  P0 fallback checks for `settings`, `projects`, `chapters`,
  `wizard-stream`, and `memories`, using the Python-side auth-boundary
  differences already captured in the runbook: `401 {"detail":"需要登录"}` for
  `settings` and `wizard-stream/outline`, `401 {"detail":"未登录"}` for
  `projects`, `memories`, chapter analysis/status/regeneration queries, and
  `401 {"detail":"Not logged in"}` for the Python batch-generation
  `active-tasks` listing. The profile intentionally excludes
  `GET /api/chapters?project_id=...` because that Rust query-shape list route
  is not the Python fallback path. Focused pytest now locks the new profile
  selection behavior, and the Phase 5 docs now describe `phase5-p0-fallback`
  as the first executable Python fallback slice rather than a future idea.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group chapters --validate-manifest-only`, and `git diff --check`
  over the touched smoke/doc files should pass.
- 2026-05-19 22:33 +08:00 checkpoint:
  the `chapters` portion of `phase5-p0-fallback` now covers the Python list
  primary path instead of stopping at query/status/task side probes only.
  `deploy/strangler-gateway-probes.json` now includes
  `chapters-project-list-auth-guard-python-fallback` for
  `GET /api/chapters/project/{project_id}` with the Python auth-boundary
  expectation `401 {"detail":"未登录"}`. This is intentionally distinct from
  the Rust owner probe `GET /api/chapters?project_id=...`, because that query
  shape is not the Python fallback route contract. With this addition, the
  `chapters` fallback slice now covers project-path list, analysis, batch
  analysis status, batch active-tasks, and regeneration-task reads, and the
  Phase 5 docs can treat that coverage as executable fallback smoke rather
  than a remaining manual verification note. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group chapters --validate-manifest-only`, and `git diff --check`
  over the touched smoke/doc/task files should pass.
- 2026-05-19 22:28 +08:00 checkpoint:
  the next Phase 5 governance slice improved smoke-result usability rather
  than expanding probe semantics. `backend/tools/run_strangler_gateway_smoke.py`
  now emits manifest-level `owner_counts`, `route_group_counts`, and
  `route_group_probe_names` alongside the selected probe list, so
  `tmp/smoke/*.json` can be used directly as a rollback/fallback evidence
  artifact without manually regrouping probes after the run. Focused tests now
  lock both the inventory summarizer and the summary payload shape, and the P0
  rollback docs now explicitly call out these rollups as the preferred report
  skeleton for route-group operations. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`
  passed with 23 tests; `python backend/tools/run_strangler_gateway_smoke.py
  --manifest deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group chapters --validate-manifest-only` now reports both
  `probe_count=5` and the new `owner_counts` / `route_group_counts` /
  `route_group_probe_names` fields; `git diff --check -- backend/tools/run_strangler_gateway_smoke.py
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  passed.
- 2026-05-19 22:33 +08:00 checkpoint:
  Phase 5 has now started a first P1 route-group executable skeleton instead
  of leaving `auth` / `users` only in the checklist. The smoke manifest now
  defines `phase5-p1` with two low-precondition Rust owner probes:
  `auth-config-public-rust` is now also labeled as `route_group="auth"` and
  selected by `phase5-p1`, while `users-current-auth-guard-rust` adds the
  first `users` route-group probe on `GET /api/users/current` with the stable
  Rust auth-boundary response `401 {"detail":"未登录，请先登录"}`. This is
  intentionally only a starter slice, not a full auth-flow or admin-privilege
  business verification set, but it moves P1 from doc-only planning into a
  runnable profile with structured owner/route-group rollups. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`
  passed with 24 tests; `python backend/tools/run_strangler_gateway_smoke.py
  --manifest deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` passed with `probe_count=2`, `owner_counts={"rust":2}`,
  and route-group coverage for `auth` plus `users`; `git diff --check -- 
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:36 +08:00 checkpoint:
  the first P1 profile has been strengthened with a public auth action probe
  instead of staying at one config read plus one auth-boundary read. `phase5-p1`
  now also includes `auth-logout-public-rust` on `POST /api/auth/logout`,
  asserting the stable Rust JSON response `{"success": true, "message": "已登出"}`
  without depending on a live logged-in session. This keeps the slice
  low-precondition while moving slightly closer to real auth behavior than a
  pure config read. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  24 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` now reports `probe_count=3`,
  `route_group_counts={"auth":2,"users":1}`, and
  `route_group_probe_names.auth=["auth-config-public-rust","auth-logout-public-rust"]`;
  `git diff --check -- deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:39 +08:00 checkpoint:
  this wave advanced two Phase 5 fronts in parallel without touching runtime
  semantics. On the P1 side, `phase5-p1` now includes
  `auth-linuxdo-url-misconfig-rust` on `GET /api/auth/linuxdo/url`, asserting
  the stable Rust misconfiguration branch `400 {"detail":"LinuxDO OAuth 未配置"}`.
  That gives the `auth` route group a third low-precondition public/business
  signal beyond config read and logout. On the P0 side, `settings` no longer
  stops at the root-path auth guard only: `settings-models-auth-guard-rust`
  now covers `GET /api/settings/models?...`, proving that the route-group asset
  has started to move into sub-route ownership checks instead of a single
  top-level endpoint. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  24 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group settings
  --validate-manifest-only` now reports `probe_count=2` for `settings`; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` now reports `probe_count=4` with
  `route_group_counts={"auth":3,"users":1}`; `git diff --check -- 
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:44 +08:00 checkpoint:
  the next Phase 5 wave kept the same dual-track pattern and pushed both sides
  one level deeper into protected/business route ownership. On the `settings`
  side, `phase5-p0` now also includes `settings-fetch-models-auth-guard-rust`
  on `POST /api/settings/fetch-models` with a real JSON body, so the route
  group now has root-path, GET sub-route, and POST sub-route auth-boundary
  probes instead of only read-side coverage. On the `auth` side, `phase5-p1`
  now also includes `auth-user-auth-guard-rust` on `GET /api/auth/user`,
  adding a protected current-user read probe next to the existing public
  config/logout/LinuxDO misconfig checks. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  24 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group settings
  --validate-manifest-only` now reports `probe_count=3` with
  `route_group_probe_names.settings=["settings-auth-guard-rust","settings-models-auth-guard-rust","settings-fetch-models-auth-guard-rust"]`;
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` now reports `probe_count=5` with
  `route_group_counts={"auth":4,"users":1}`; `git diff --check -- 
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:48 +08:00 checkpoint:
  this Phase 5 slice continued the same P0/P1 parallel pattern and brought
  both route groups closer to the coverage shape described in the planning
  docs. On the `users` side, `phase5-p1` now also includes
  `users-list-auth-guard-rust` on `GET /api/users`, so the route group is no
  longer represented only by the current-user read and now has an explicit list
  read auth-boundary probe that matches the checklist wording. On the
  `settings` side, `phase5-p0` now also includes
  `settings-test-auth-guard-rust` on `POST /api/settings/test` with a real API
  probe payload, which extends the route-group asset from root path plus
  settings-model fetch helpers into the first connection-test style business
  endpoint. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  24 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group settings
  --validate-manifest-only` now reports `probe_count=4` with
  `route_group_counts={"settings":4}`; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` now reports `probe_count=6` with
  `route_group_counts={"auth":4,"users":2}`; `git diff --check -- 
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 22:51 +08:00 checkpoint:
  this wave continued to avoid runtime-risky success assertions while still
  moving both route groups deeper into business-adjacent protected endpoints.
  On the `settings` side, `phase5-p0` now also includes
  `settings-check-function-calling-auth-guard-rust` on
  `POST /api/settings/check-function-calling` with a realistic API probe body,
  so the route-group asset now covers root settings read, model list lookup,
  model fetch, connection test, and function-calling test entrypoints at the
  auth-boundary level. On the `auth` side, `phase5-p1` now also includes
  `auth-password-status-auth-guard-rust` on `GET /api/auth/password/status`,
  extending the protected read coverage beyond `/api/auth/user`. Validation:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`
  passed with 24 tests; `python backend/tools/run_strangler_gateway_smoke.py
  --manifest deploy/strangler-gateway-probes.json --profile phase5-p0
  --route-group settings --validate-manifest-only` now reports `probe_count=5`
  with `route_group_counts={"settings":5}`; `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1
  --validate-manifest-only` now reports `probe_count=7` with
  `route_group_counts={"auth":5,"users":2}`; `git diff --check -- 
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-19 23:21 +08:00 checkpoint:
  this Phase 5 slice strengthened the smoke infrastructure itself instead of
  only adding more route entries. `backend/tools/run_strangler_gateway_smoke.py`
  now records response headers and supports a new per-probe
  `expected_header_contains` assertion, which is intentionally minimal but
  sufficient for cookie/header presence checks. The first direct consumer is
  `auth-logout-public-rust`: it no longer only proves the JSON body
  `{"success":true,"message":"已登出"}`, but also asserts that the response
  carries a `Set-Cookie` header containing `token=` so the probe is closer to
  the real logout contract. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  26 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --probe-name
  auth-logout-public-rust --validate-manifest-only` passed with `probe_count=1`;
  `git diff --check -- backend/tools/run_strangler_gateway_smoke.py
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with the existing line-ending warning on
  `deploy/strangler-gateway-probes.json`.
- 2026-05-20 00:33 +08:00 checkpoint:
  the next Phase 5 slice tightened the credibility of the new header-level
  smoke before expanding it further. The initial `expected_header_contains`
  implementation recorded response headers via `dict(headers.items())`, which
  would silently collapse repeated headers and therefore weaken any future
  `Set-Cookie`-based assertions. `backend/tools/run_strangler_gateway_smoke.py`
  now preserves repeated response headers by joining duplicate values with
  newlines, and the focused tests now lock both the helper and the
  header-assertion behavior for multi-value `Set-Cookie` scenarios. This keeps
  `auth-logout-public-rust` trustworthy as a stronger business smoke rather
  than a fragile body-only probe. Validation: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q` passed with
  27 tests; `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --probe-name
  auth-logout-public-rust --validate-manifest-only` passed with `probe_count=1`;
  `git diff --check -- backend/tools/run_strangler_gateway_smoke.py
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
  passed with line-ending warnings only.
- 2026-05-20 01:03 +08:00 checkpoint:
  this Phase 5 slice shifted from pure owner smoke expansion into P1 rollback
  governance. `deploy/strangler-gateway-probes.json` now defines the first
  `phase5-p1-fallback` profile, intentionally scoped only to `auth` where the
  Python fallback contract is stable enough to automate: `POST /api/auth/logout`
  now distinguishes Rust `{"success":true,"message":"已登出"}` from Python
  `{"message":"退出登录成功"}` while also checking a cookie-clearing header, and
  `/api/auth/user` plus `/api/auth/password/status` now encode the stable
  Python unauthenticated response `401 {"detail":"未登录"}`. In parallel, a new
  P1 rollback runbook documents why `users` is not yet in the fallback profile:
  Rust `GET /api/users/current` does not map 1:1 to a Python owner path, so
  blindly probe-izing it would create false automation confidence. The Phase 5
  checklist and parity matrix now reflect this split: `auth` has executable
  fallback evidence, while `users` currently has explicit path-shape caveats
  and rollback guidance instead of synthetic parity claims. Validation target
  for this checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --validate-manifest-only`, and `git diff --check -- deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:20 +08:00 checkpoint:
  this Phase 5 slice returned to low-risk P1 owner expansion after the auth
  fallback governance checkpoint. `deploy/strangler-gateway-probes.json` now
  extends `phase5-p1` with a second starter wave for `characters`,
  `outlines`, and `book_import`: `GET /api/characters/project/{project_id}`,
  `GET /api/outlines/project/{project_id}`, and
  `GET /api/book-import/tasks/{task_id}` now all assert the stable Rust auth
  boundary `401 {"detail":"未登录，请先登录"}` through the gateway. This keeps
  the slice low-precondition and avoids synthetic fallback claims, while still
  moving three more Rust-owned route groups from checklist-only status into
  executable owner evidence. The Phase 5 checklist and parity matrix now call
  out that these groups have only starter owner smoke so far; upload, generate,
  and SSE-heavy flows remain for later waves. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  characters --validate-manifest-only`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  outlines --validate-manifest-only`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  book_import --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:36 +08:00 checkpoint:
  this Phase 5 slice expanded P1 fallback governance beyond `auth` without
  touching business runtime code. `deploy/strangler-gateway-probes.json` now
  extends `phase5-p1-fallback` with three additional same-path Python fallback
  probes: `GET /api/characters/project/{project_id}`,
  `GET /api/outlines/project/{project_id}`, and
  `GET /api/book-import/tasks/{task_id}`. All three intentionally use the
  stable unauthenticated Python response `401 {"detail":"未登录"}` to contrast
  the Rust owner boundary `401 {"detail":"未登录，请先登录"}` on the exact same
  paths, which makes them safe low-precondition rollback clues rather than
  speculative semantic parity claims. The P1 rollback runbook now documents
  these groups as having first-stage executable fallback evidence, while still
  keeping `users` out of the fallback profile because `/api/users/current`
  remains path-misaligned with Python ownership. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group characters --validate-manifest-only`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group outlines --validate-manifest-only`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group book_import --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:52 +08:00 checkpoint:
  this Phase 5 slice deepened the same P1 route groups without leaving the
  low-precondition read-side lane. `deploy/strangler-gateway-probes.json` now
  adds a second same-path pair for each of `characters`, `outlines`, and
  `book_import`: `GET /api/characters?project_id=...`,
  `GET /api/outlines?project_id=...`, and
  `GET /api/book-import/tasks/{task_id}/preview` now exist in both
  `phase5-p1` (Rust owner) and `phase5-p1-fallback` (Python fallback), using
  the same unauthenticated owner split as the earlier starter probes. This
  makes the evidence less dependent on a single route shape per group and moves
  the asset from one-path starter coverage toward a slightly stronger read-side
  matrix, while still deliberately avoiding upload/generation/SSE success-path
  assertions. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  characters --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group characters --validate-manifest-only`, and the same pair for
  `outlines` plus `book_import`, followed by `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:08 +08:00 checkpoint:
  this Phase 5 slice advanced one step past pure read-side probing without
  introducing request-body or SSE validation risk. `deploy/strangler-gateway-probes.json`
  now adds `DELETE /api/book-import/tasks/{task_id}` to both `phase5-p1`
  (Rust owner) and `phase5-p1-fallback` (Python fallback), using the same
  unauthenticated owner split as the existing status/preview probes. This gives
  `book_import` the first lightweight write-side rollback clue in P1 while
  still avoiding upload payloads or stream success semantics. Validation target
  for this checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  book_import --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group book_import --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:19 +08:00 checkpoint:
  this Phase 5 slice continued to deepen `book_import` while staying inside a
  low-risk auth-boundary envelope. `deploy/strangler-gateway-probes.json` now
  adds `POST /api/book-import/tasks/{task_id}/apply` and
  `POST /api/book-import/tasks/{task_id}/retry-stream` to both `phase5-p1`
  (Rust owner) and `phase5-p1-fallback` (Python fallback), using minimal legal
  JSON payloads so the request gets past shape validation and lands on the
  authentication owner boundary instead of failing for schema reasons. This
  gives `book_import` a more complete pre-success matrix across status,
  preview, cancel, apply, and retry entrypoints without needing a real upload
  or successful import flow. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  book_import --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group book_import --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:16 +08:00 checkpoint:
  this Phase 5 slice extended the same `book_import` governance lane one step
  further into the streaming submit boundary without leaving the low-risk auth
  envelope. `deploy/strangler-gateway-probes.json` now adds
  `POST /api/book-import/tasks/{task_id}/apply-stream` to both `phase5-p1`
  (Rust owner) and `phase5-p1-fallback` (Python fallback), reusing the same
  minimal legal JSON payload as `apply` so the request crosses schema shape
  validation and lands on the auth owner boundary instead of failing earlier.
  This gives `book_import` a fuller pre-success matrix across status, preview,
  cancel, apply, retry-stream, and apply-stream while still avoiding upload
  creation or success-path SSE assertions. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  book_import --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group book_import --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:17 +08:00 checkpoint:
  this Phase 5 slice advanced `book_import` into the multipart upload boundary
  without moving into success-path semantics. `backend/tools/run_strangler_gateway_smoke.py`
  now supports a minimal `multipart_form` manifest payload shape with focused
  tests, and `deploy/strangler-gateway-probes.json` now adds
  `POST /api/book-import/tasks` to both `phase5-p1` (Rust owner) and
  `phase5-p1-fallback` (Python fallback). The probe uses a tiny `.txt`
  in-memory upload plus the existing `append` / `create_new_project=true`
  form fields so the request reaches the auth owner boundary instead of
  failing on transport shape. This gives `book_import` a fuller pre-success
  matrix across create, status, preview, cancel, apply, retry-stream, and
  apply-stream while still avoiding login-dependent or successful import
  assertions. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  book_import --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group book_import --validate-manifest-only`, and `git diff --check -- 
  backend/tools/run_strangler_gateway_smoke.py
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:24 +08:00 checkpoint:
  this Phase 5 slice pushed `characters` and `outlines` one step beyond the
  read-side-only lane without entering success-path assertions. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/characters/generate-stream` and
  `POST /api/outlines/generate-stream` to both `phase5-p1` (Rust owner) and
  `phase5-p1-fallback` (Python fallback), using the smallest route-legal JSON
  payloads for each side. The Rust owner expectation remains the middleware
  boundary `401 {"detail":"未登录，请先登录"}`. The Python fallback expectation
  intentionally differs from the list routes: because both generate-stream
  endpoints depend on `get_user_ai_service()`, the stable unauthenticated
  Python signal is `401 {"detail":"需要登录"}` instead of `未登录`. This makes
  the probes useful rollback clues for the same paths without pretending that
  AI generation success, SSE event ordering, or downstream persistence are
  already covered. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  characters --validate-manifest-only`, the same command for `outlines`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group characters --validate-manifest-only`, the same command for
  `outlines`, and `git diff --check -- deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:39 +08:00 checkpoint:
  this Phase 5 slice deepened `outlines` beyond the first generate-stream
  boundary while staying inside low-noise auth/fallback governance. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/outlines/batch-expand-stream` and
  `POST /api/outlines/{outline_id}/create-chapters-from-plans` to both
  `phase5-p1` (Rust owner) and `phase5-p1-fallback` (Python fallback).
  `batch-expand-stream` uses a minimal route-legal project payload, while
  `create-chapters-from-plans` uses a single fully shaped `chapter_plans`
  item so the request crosses Python body validation and lands on the same
  authentication boundary instead of failing early with a 422/400. The Rust
  owner expectation remains `401 {"detail":"未登录，请先登录"}`. The Python
  fallback expectation intentionally matches the existing outlines write-side
  dependency behavior: both routes resolve `get_user_ai_service()` before
  business work, so the stable fallback signal is
  `401 {"detail":"需要登录"}`. This gives `outlines` a fuller pre-success
  matrix across list, generate, batch-expand, and create-from-plans entry
  points without asserting successful SSE output or real chapter creation.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  outlines --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group outlines --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:48 +08:00 checkpoint:
  this Phase 5 slice returned to `characters` and chose the lowest-noise
  remaining same-path governance seam instead of forcing success-path or
  data-dependent CRUD probes. `deploy/strangler-gateway-probes.json` now adds
  `POST /api/characters/export` and `POST /api/characters/import` to both
  `phase5-p1` (Rust owner) and `phase5-p1-fallback` (Python fallback).
  `export` uses the smallest legal JSON payload with one placeholder
  `character_id`, while `import` uses a tiny multipart `.json` payload plus
  the required `project_id` query parameter so the request shape is valid
  enough to reach the auth boundary instead of failing earlier for transport
  reasons. The Rust owner expectation remains
  `401 {"detail":"未登录，请先登录"}`. The Python fallback expectation for
  these two routes is the direct request-state guard
  `401 {"detail":"未登录"}`, which intentionally differs from
  `characters/generate-stream` because `export/import` do not depend on
  `get_user_ai_service()`. `characters/validate-import` remains intentionally
  out of this slice because the current Rust route behaves more like a public
  validator and would not produce a clean same-path auth-boundary comparison.
  This gives `characters` a fuller pre-success matrix across list, generate,
  export, and import entry points without asserting real file semantics or
  successful entity creation. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group
  characters --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group characters --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 01:58 +08:00 checkpoint:
  this Phase 5 slice stopped forcing more `book_import` auth-boundary probes
  once the same-path lane was effectively saturated, then promoted the first
  explicit asymmetric-interface governance asset instead. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/characters/validate-import` under a new
  `phase5-p1-asymmetric` profile with two paired expectations:
  Rust owner uses the smallest legal multipart `.json` file and must return a
  stable public-validator success payload (`200`, `valid=true`, empty-data
  warning), while Python fallback must return the direct request-state auth
  guard `401 {"detail":"未登录"}` on the same path. This intentionally does
  not join `phase5-p1-fallback`, because the route is same-path but not
  same-boundary: Rust currently exposes a public validator, whereas Python
  treats the same endpoint as a login-required import precheck. The rollback
  runbook, parity matrix, and route-group checklist now record this class as
  a separate governance lane so future Phase 5 work can distinguish
  auth-boundary parity from public-vs-protected asymmetry without creating
  false automation confidence. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-asymmetric
  --route-group characters --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:03 +08:00 checkpoint:
  this Phase 5 slice returned to the `auth` route-group and deepened the
  low-precondition protected write-side matrix instead of chasing noisier
  session-success behavior. `deploy/strangler-gateway-probes.json` now adds
  `POST /api/auth/password/set` and `POST /api/auth/password/initialize` to
  both `phase5-p1` (Rust owner) and `phase5-p1-fallback` (Python fallback),
  using the same smallest legal JSON body `{ "password": "test123456" }`.
  The Rust owner expectation remains
  `401 {"detail":"未登录，请先登录"}`. The Python fallback expectation is the
  direct `require_request_user()` guard `401 {"detail":"未登录"}` for both
  routes. This gives `auth` a broader write-side auth-boundary evidence set
  beyond `logout` and `password/status`, while still avoiding real password
  initialization semantics, session success-path assertions, or time-sensitive
  refresh behavior. The rollback runbook, parity matrix, and route-group
  checklist now record these two protected password endpoints as part of the
  P1 fallback lane. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group auth
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group auth --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:11 +08:00 checkpoint:
  this Phase 5 slice kept moving along the `auth` governance lane and filled
  the remaining low-precondition session-refresh boundary instead of jumping
  to success-path session semantics. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/auth/refresh` to both `phase5-p1` (Rust owner) and
  `phase5-p1-fallback` (Python fallback). The Rust owner expectation remains
  `401 {"detail":"未登录，请先登录"}` via the shared auth middleware. The
  Python fallback expectation is the route-local `require_request_user()`
  message `401 {"detail":"未登录，无法刷新会话"}`, which makes this probe more
  informative than a generic auth-boundary read probe because it also encodes
  the current Python session-refresh contract. This extends the P1 auth asset
  set from public config/logout plus password boundaries into the refresh
  boundary without requiring any real session, cookie-expiry clock control, or
  success-path cookie assertions. The rollback runbook, parity matrix, and
  route-group checklist now record `auth/refresh` as part of the P1 fallback
  lane. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group auth
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group auth --validate-manifest-only`, and `git diff --check -- 
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:17 +08:00 checkpoint:
  this Phase 5 slice returned to the `projects` route-group and intentionally
  stopped adding more low-value `401` probes. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/projects/validate-import` as a paired same-path public
  validator asset: `projects-validate-import-public-rust` enters
  `phase5-p0` plus `business`, while
  `projects-validate-import-public-python-fallback` enters
  `phase5-p0-fallback`. Both sides use the same smallest legal multipart JSON
  import file and both return `200 valid=true`, but the result shapes are
  intentionally different in stable ways: Rust exposes the narrower
  `statistics.memories`-based schema with no warnings, while Python exposes
  `organization_members` / `character_careers` / `story_memories` /
  `has_default_style=false` plus the empty-project warnings
  `项目没有章节数据` and `项目没有角色数据`. This makes `projects` the first
  P0 group with a stronger through-gateway public/business owner probe and a
  same-path public-success fallback clue, instead of relying only on auth
  boundaries. The rollback runbook, route-group checklist, and parity matrix
  now record this lane explicitly. Validation target for this checkpoint:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  projects --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group projects --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:29 +08:00 checkpoint:
  this Phase 5 slice stayed with the same `projects` route-group and added the
  next-lowest-noise write-side governance asset instead of jumping to heavier
  export or consistency-repair flows. `deploy/strangler-gateway-probes.json`
  now adds `POST /api/projects/import` to both `phase5-p0` (Rust owner) and
  `phase5-p0-fallback` (Python fallback), reusing the same smallest legal
  multipart JSON file as `validate-import`. The difference is intentional:
  `validate-import` proves same-path public/business success semantics,
  whereas `import` proves the multipart write-side auth boundary after the
  request shape is already valid. Rust must still land on the shared auth
  middleware `401 {"detail":"未登录，请先登录"}`; Python must still land on the
  route-local `request.state.user_id` guard `401 {"detail":"未登录"}` before any
  import business logic runs. This gives `projects` a tighter P0 evidence set
  across read-side auth, public validator success, and multipart write-side
  auth, without needing a real logged-in import success path. The rollback
  runbook, route-group checklist, and parity matrix now record that expanded
  lane explicitly. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  projects --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group projects --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:38 +08:00 checkpoint:
  this Phase 5 slice kept the same `projects` route-group focus and chose the
  next stable JSON write-side boundary instead of jumping to repair/report
  success assertions. `deploy/strangler-gateway-probes.json` now adds
  `POST /api/projects/{project_id}/export-data` to both `phase5-p0` (Rust
  owner) and `phase5-p0-fallback` (Python fallback), using the smallest legal
  JSON body `{}` so the request clears request decoding and lands on the owner
  auth boundary rather than failing for payload shape. Rust must still return
  the shared middleware `401 {"detail":"未登录，请先登录"}`; Python must still
  return the route-local `request.state.user_id` guard
  `401 {"detail":"未登录"}` before any export business logic runs. This gives
  `projects` a broader P0 governance set across read-side auth,
  same-path public-success validation, multipart write-side auth, and JSON
  write-side auth, without requiring a real project, login, or export result.
  The rollback runbook, route-group checklist, and parity matrix now record
  that expanded lane explicitly. Validation target for this checkpoint:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  projects --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group projects --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:47 +08:00 checkpoint:
  this Phase 5 slice moved off the now-dense `projects` group and returned to
  the P0 `wizard-stream` lane to add a second same-family SSE auth-boundary
  probe. `deploy/strangler-gateway-probes.json` now adds
  `POST /api/wizard-stream/world-building/{project_id}/regenerate` to both
  `phase5-p0` (Rust owner) and `phase5-p0-fallback` (Python fallback), using
  the smallest legal JSON body `{}` so the request clears body parsing and
  lands on the owner auth boundary instead of failing for payload shape. Rust
  must still return the shared middleware `401 {"detail":"未登录，请先登录"}`.
  Python must still return the `get_user_ai_service()` login dependency signal
  `401 {"detail":"需要登录"}` before any SSE generation work starts. This gives
  `wizard-stream` a more credible P0 pair across `outline` and world-building
  regenerate, rather than relying on a single top-level SSE endpoint to prove
  owner/fallback behavior. The rollback runbook, route-group checklist, and
  parity matrix now record that expanded lane explicitly. Validation target
  for this checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  wizard-stream --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group wizard-stream --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.

- 2026-05-20 02:40 +08:00 checkpoint:
  this Phase 5 slice moved to the `memories` route-group once `projects`
  and `wizard-stream` had already gained denser same-group evidence. The next
  lowest-noise lane was `POST /api/memories/projects/{project_id}/search`,
  because Rust accepts a generic `Json<Value>` body while Python requires the
  `query` term as a query parameter rather than inside JSON. The manifest now
  adds `memories-search-auth-guard-rust` and
  `memories-search-auth-guard-python-fallback`, both using the same path
  `/api/memories/projects/test-project-id/search?query=test` plus the minimal
  JSON body `{}` so the request shape is valid enough on both sides to cross
  transport parsing and land on the auth boundary instead of failing early
  with a 422. Rust must still return the shared middleware
  `401 {"detail":"未登录，请先登录"}`. Python fallback must still return
  `401 {"detail":"未登录"}` from `verify_project_access()`. This upgrades
  `memories` from a single `stats` auth-boundary clue into a two-probe query
  lane (`stats + search`) without requiring any real project, login, or
  search result data. The rollback runbook, route-group checklist, and parity
  matrix now record that expanded lane explicitly. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  memories --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group memories --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:48 +08:00 checkpoint:
  this Phase 5 slice stayed in the P0 governance lane but shifted to the
  sparsest Python fallback group instead of adding more low-yield owner-only
  evidence. `settings` already had five Rust owner probes in `phase5-p0`,
  but only one Python fallback clue at `GET /api/settings`. The manifest now
  adds `settings-fetch-models-auth-guard-python-fallback` under
  `phase5-p0-fallback`, reusing the same minimal JSON body as the existing
  Rust owner probe for `POST /api/settings/fetch-models`. This is a lower-noise
  lane than `test` or `check-function-calling` because the body is smaller
  and Python reaches `Depends(require_login)` before any network probe logic.
  Python fallback must therefore return the stable login guard
  `401 {"detail":"需要登录"}` on the same path and request shape. This
  upgrades `settings` from a single root-path fallback clue into a two-probe
  fallback lane (`/api/settings + /api/settings/fetch-models`) without
  asserting any live provider success path. The rollback runbook, route-group
  checklist, and parity matrix now record that expanded lane explicitly.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group settings --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:50 +08:00 checkpoint:
  this Phase 5 slice stayed with the same `settings` fallback lane and added
  the next-lowest-noise sub-route instead of jumping to success-path probe
  logic. The manifest now adds `settings-test-auth-guard-python-fallback`
  under `phase5-p0-fallback`, reusing the same minimal JSON body as the
  existing Rust owner probe for `POST /api/settings/test`. This keeps the
  request shape realistic enough to cross transport parsing while still
  stopping at Python's `Depends(require_login)` before any external provider
  connectivity probe runs. Python fallback must therefore return the stable
  login guard `401 {"detail":"需要登录"}` on the same path and request
  shape. This upgrades `settings` from a two-probe fallback lane
  (`/api/settings + /api/settings/fetch-models`) into a three-probe lane that
  now also covers the connection-test endpoint, without asserting any live
  provider success or failure semantics. The rollback runbook, route-group
  checklist, and parity matrix now record that expanded lane explicitly.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group settings --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 02:54 +08:00 checkpoint:
  this Phase 5 slice stayed with the same `settings` fallback lane and filled
  the last low-precondition probe sibling before moving on to other groups.
  The manifest now adds
  `settings-check-function-calling-auth-guard-python-fallback` under
  `phase5-p0-fallback`, reusing the same minimal JSON body as the existing
  Rust owner probe for `POST /api/settings/check-function-calling`. This
  keeps the request shape realistic enough to cross transport parsing while
  still stopping at Python's `Depends(require_login)` before any tool-calling
  capability probe runs. Python fallback must therefore return the stable
  login guard `401 {"detail":"需要登录"}` on the same path and request
  shape. This upgrades `settings` from a three-probe fallback lane
  (`/api/settings + /api/settings/fetch-models + /api/settings/test`) into a
  four-probe lane that now also covers the function-calling probe endpoint,
  without asserting any live provider capability semantics. The rollback
  runbook, route-group checklist, and parity matrix now record that expanded
  lane explicitly. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group settings --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:00 +08:00 checkpoint:
  this Phase 5 slice moved off the now-complete low-precondition `settings`
  fallback lane and returned to the thinner P1 `users` group. During route
  verification, the previous rollback docs turned out to be stale: Python
  still exposes same-path `GET /api/users/current` and `GET /api/users`
  routes in `backend/app/api/users.py`, so `users/current` no longer needs to
  be treated as a path-mismatch-only operator clue. The manifest now adds
  `users-current-auth-guard-python-fallback` and
  `users-list-auth-guard-python-fallback` under `phase5-p1-fallback`. Both
  should return the stable Python login guard `401 {"detail":"需要登录"}`
  after a route-group rollback. For `/api/users`, this still proves only the
  same-path auth boundary, not full admin-list semantic parity, because the
  Python endpoint also enforces admin privileges after login. The P1 rollback
  runbook, route-group checklist, and parity matrix now explicitly distinguish
  “same-path auth-boundary ownership clue” from “full business parity proof”
  for the `users` group. Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group users --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:05 +08:00 checkpoint:
  this Phase 5 slice stayed with the now-validated `users` fallback lane and
  added the next two lowest-noise same-path write boundaries instead of
  stopping at read-only ownership clues. The manifest now adds
  `users-set-admin-auth-guard-python-fallback` and
  `users-reset-password-auth-guard-python-fallback` under
  `phase5-p1-fallback`, using the smallest legal JSON bodies for each route.
  In both Python handlers, the request should still stop at
  `require_login("需要登录")` before any admin-only or password-reset business
  branch executes. That means these probes extend `users` from read-side
  fallback clues into write-side auth-boundary clues, while still not claiming
  full semantic parity for admin mutations. The P1 rollback runbook, route-group
  checklist, and parity matrix now explicitly record that distinction. Validation
  target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group users --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:09 +08:00 checkpoint:
  this Phase 5 slice stayed with the same `users` route-group and filled the
  matching Rust owner side for the two newly added fallback write boundaries.
  The manifest now adds `users-set-admin-auth-guard-rust` and
  `users-reset-password-auth-guard-rust` under `phase5-p1`, using the same
  smallest legal JSON bodies as the Python fallback probes. This keeps the
  owner/fallback matrix symmetric at the transport/auth-boundary layer without
  asserting successful admin mutation behavior. Rust should still return the
  shared middleware `401 {"detail":"未登录，请先登录"}` on both paths, while
  Python fallback keeps returning `401 {"detail":"需要登录"}`. The P1 rollback
  runbook, route-group checklist, and parity matrix now record `users` as a
  route-group that already has both read-side and write-side same-path auth
  boundary assets, even though full business parity remains out of scope.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group users
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group users --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:21 +08:00 checkpoint:
  this Phase 5 slice returned to the still-thinnest `auth` group, but avoided
  adding another ordinary auth-boundary `401` probe. The lowest-noise missing
  lane was the public callback error boundary at `GET /api/auth/callback`
  without query parameters. Rust and Python both expose the same path, yet
  they stop at different stable local validation branches before any external
  OAuth call or transient `state` handling is involved. The manifest now adds
  `auth-callback-missing-code-rust` under `phase5-p1` and
  `auth-callback-missing-code-python-fallback` under
  `phase5-p1-fallback`. Rust must return
  `400 {"detail":"缺少 code 参数"}` because the Axum handler checks `code`
  before `state`. Python fallback must return
  `400 {"detail":"缺少 code 或 state 参数"}` because `_handle_callback()`
  rejects either missing value through one shared branch. This upgrades
  `auth` from config/logout/url/error plus login-boundary evidence into a
  slightly richer public-error matrix that now covers the callback entrypoint
  itself, without requiring any live LinuxDO configuration or real callback
  state. The P1 rollback runbook, route-group checklist, and parity matrix now
  record that new owner/fallback clue explicitly. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group auth
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group auth --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:34 +08:00 checkpoint:
  this Phase 5 slice stayed with the `auth` group, but moved from callback
  parameter validation to the local-login public failure surface so the lane
  would gain a more business-shaped owner clue instead of another transport
  boundary. The manifest now adds
  `auth-local-login-invalid-credentials-rust` under `phase5-p1` and
  `auth-local-login-invalid-credentials-python-fallback` under
  `phase5-p1-fallback`, both using the same explicit invalid credentials body
  on `POST /api/auth/local/login`. This probe depends on the current
  deployment assumption `local_auth_enabled=true`, which is already evidenced
  by the existing `auth-config-public-rust` probe, but it does not require a
  real user, session, or OAuth callback state. Rust owner must return
  `401 {"success": false, "message": "用户名或密码错误"}` because the Axum
  login handler wraps invalid credentials in a compat-style `{success,message}`
  JSON body. Python fallback must return
  `401 {"detail":"用户名或密码错误"}` because FastAPI raises an
  `HTTPException` on the same invalid-credentials branch. This upgrades
  `auth` from callback/config/logout/error plus login-boundary evidence into a
  slightly richer public/business failure matrix that now includes the actual
  local-login entrypoint structure, without needing a successful login or
  live LinuxDO setup. The P1 rollback runbook, route-group checklist, and
  parity matrix now record that new owner/fallback clue explicitly. Validation
  target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group auth
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group auth --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:43 +08:00 checkpoint:
  this Phase 5 slice stayed on the same `auth` public-business lane and added
  the sibling bound-account login entrypoint instead of jumping back to
  generic auth-boundary `401` probes. The manifest now adds
  `auth-bind-login-invalid-credentials-rust` under `phase5-p1` and
  `auth-bind-login-invalid-credentials-python-fallback` under
  `phase5-p1-fallback`, both using the same explicit invalid credentials body
  on `POST /api/auth/bind/login`. This probe is slightly lower-noise than the
  previous `local/login` one because Python does not first gate it on
  `local_auth_enabled`; it goes straight into the bound-account credential
  branch, yet still remains low-precondition because no real user, session, or
  OAuth state is required. Rust owner must again return
  `401 {"success": false, "message": "用户名或密码错误"}` because the Axum
  bind-login route reuses the compat-style local login handler. Python
  fallback must again return `401 {"detail":"用户名或密码错误"}` because the
  FastAPI route raises `HTTPException` on the same invalid-credentials path.
  This upgrades `auth` from one real login-entry public failure clue
  (`local/login`) to a two-entry login matrix (`local/login + bind/login`),
  while still avoiding successful-login state setup or external OAuth
  dependencies. The P1 rollback runbook, route-group checklist, and parity
  matrix now record that new owner/fallback clue explicitly. Validation target
  for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1 --route-group auth
  --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p1-fallback
  --route-group auth --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p1-route-group-rollback-runbook-2026-05-20.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 03:55 +08:00 checkpoint:
  this Phase 5 slice moved back to the P0 `settings` group after the `auth`
  lane had already become denser than the remaining low-precondition settings
  fallback surface. The smallest missing same-path clue was
  `GET /api/settings/api-key`, because both Rust and Python expose the exact
  same path, require no request body, and stop at the login boundary before
  any provider probe or settings lookup semantics can diverge. The manifest
  now adds `settings-api-key-auth-guard-rust` under `phase5-p0` and
  `settings-api-key-auth-guard-python-fallback` under
  `phase5-p0-fallback`. Rust must return the shared middleware
  `401 {"detail":"未登录，请先登录"}`. Python fallback must return the
  stable dependency guard `401 {"detail":"需要登录"}`. This upgrades
  `settings` from root-path plus provider-probe sub-route evidence into a
  slightly more complete read-side matrix that now also covers the stored API
  key retrieval entrypoint, without relying on any live provider connectivity
  or saved user settings. The P0 rollback runbook, route-group checklist, and
  parity matrix now record that owner/fallback clue explicitly. Validation
  target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  settings --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group settings --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 04:09 +08:00 checkpoint:
  this Phase 5 slice initially tried to close the last visible `settings`
  fallback count gap by adding `/api/settings/models`, but route inspection
  showed that would be incorrect. Rust and Python both expose the same path,
  yet they do not share the same boundary type: Rust owner requires auth first,
  while Python fallback keeps `/api/settings/models` public and proceeds into
  provider probing. Instead of polluting `phase5-p0-fallback` with a fake
  auth-boundary clue, the manifest now adds a new asymmetric pair under
  `phase5-p0-asymmetric`:
  `settings-models-auth-guard-rust-asymmetric` and
  `settings-models-public-network-error-python-fallback`. Both use the same
  minimal query shape
  `/api/settings/models?provider=openai&api_key=test-key&api_base_url=http://127.0.0.1:9/v1`.
  Rust must return `401 {"detail":"未登录，请先登录"}` from the shared auth
  middleware. Python fallback must return
  `400 {"detail":"无法连接到 API: All connection attempts failed"}` because
  the public endpoint reaches `httpx` connection failure before any auth gate.
  The P0 rollback runbook, route-group checklist, and parity matrix now record
  `settings/models` as the first P0 asymmetric sample, analogous to the
  existing P1 `characters/validate-import` pattern. Validation target for this
  checkpoint: `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-asymmetric
  --route-group settings --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 05:02 +08:00 checkpoint:
  this Phase 5 slice returned to the P0 `wizard-stream` group instead of
  continuing to overfit `settings`. Route inspection confirmed
  `POST /api/wizard-stream/cleanup/{project_id}` is implemented on Rust but
  not on the current Python `wizard_stream` router, so it must not be modeled
  as a fake `phase5-p0-fallback` auth-boundary clue. The manifest now adds
  `wizard-stream-cleanup-auth-guard-rust` under `phase5-p0`, using the same
  minimal JSON body `{}` on
  `POST /api/wizard-stream/cleanup/test-project-id`. Rust owner must return
  `401 {"detail":"未登录，请先登录"}` from the shared auth middleware before
  the cleanup SSE handler starts. This gives `wizard-stream` a third explicit
  through-gateway owner probe across `outline`, `world-building regenerate`,
  and `cleanup`, while the rollback runbook, route-group checklist, and parity
  matrix now explicitly record that `cleanup` is owner-only evidence until
  Python gains a real same-path route. Validation target for this checkpoint:
  `pytest backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`,
  `python backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  wizard-stream --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 05:19 +08:00 checkpoint:
  this Phase 5 slice kept working in the P0 `wizard-stream` group, but moved
  from owner-only evidence back to a real owner/fallback pair. Route
  inspection confirmed `POST /api/wizard-stream/career-system` exists on both
  Rust and Python, and both sides hit auth before any deeper SSE business
  logic: Rust stops at the shared middleware with
  `401 {"detail":"未登录，请先登录"}`, while Python stops inside
  `get_user_ai_service -> require_login()` with
  `401 {"detail":"需要登录"}`. The manifest now adds
  `wizard-stream-career-system-auth-guard-rust` under `phase5-p0` and
  `wizard-stream-career-system-auth-guard-python-fallback` under
  `phase5-p0-fallback`, both using the same minimal JSON body
  `{"projectId":"test-project-id"}` on
  `POST /api/wizard-stream/career-system`. This upgrades `wizard-stream` from
  a two-entry fallback matrix (`outline + world-building regenerate`) to a
  three-entry one (`outline + world-building regenerate + career-system`),
  while still keeping `cleanup` explicitly documented as owner-only evidence.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  wizard-stream --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group wizard-stream --validate-manifest-only`, and
  `git diff --check -- deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 05:31 +08:00 checkpoint:
  this Phase 5 slice stayed on the same P0 `wizard-stream` lane and extended
  the newly re-established real fallback matrix one step further. Route
  inspection confirmed `POST /api/wizard-stream/characters` exists on both
  Rust and Python, and both sides again stop at auth before any deeper SSE
  workflow logic: Rust returns
  `401 {"detail":"未登录，请先登录"}` from the shared middleware, while Python
  returns `401 {"detail":"需要登录"}` from
  `get_user_ai_service -> require_login()`. The manifest now adds
  `wizard-stream-characters-auth-guard-rust` under `phase5-p0` and
  `wizard-stream-characters-auth-guard-python-fallback` under
  `phase5-p0-fallback`, both using the same minimal JSON body
  `{"projectId":"test-project-id"}` on
  `POST /api/wizard-stream/characters`. This upgrades `wizard-stream` from a
  three-entry real fallback matrix
  (`outline + world-building regenerate + career-system`) to a four-entry one
  (`outline + world-building regenerate + career-system + characters`), while
  still keeping `cleanup` explicitly documented as owner-only evidence.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  wizard-stream --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group wizard-stream --validate-manifest-only`, and
  `git diff --check -- deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 06:09 +08:00 checkpoint:
  this Phase 5 slice moved from the now-denser `wizard-stream` group back to
  the P0 `chapters` lane, but deliberately avoided inventing another low-value
  auth-only probe. Route inspection confirmed
  `GET /api/chapters/batch-generate/{batch_id}/status` is a real same-path
  asymmetric endpoint: Rust requires auth first and therefore returns
  `401 {"detail":"未登录，请先登录"}`, while the current Python status query
  route does not read login state and instead returns
  `404 {"detail":"Batch generation task not found"}` when the batch id is
  missing. The manifest now adds
  `chapters-batch-status-auth-guard-rust-asymmetric` and
  `chapters-batch-status-task-not-found-python-fallback` under
  `phase5-p0-asymmetric`, both using the same path
  `/api/chapters/batch-generate/test-batch-id/status`. This makes
  `chapters` the second P0 route-group after `settings` to have an explicit
  asymmetric sample, and more importantly establishes the first batch
  generation status-query asymmetry record so future work does not
  accidentally pollute `phase5-p0-fallback` with a fake auth-boundary clue.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-asymmetric
  --route-group chapters --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 06:22 +08:00 checkpoint:
  this Phase 5 slice stayed in the P0 `chapters` lane and continued following
  the same rule: do not confuse a stronger stream/status clue with a fake
  asymmetric sample. Route inspection confirmed
  `GET /api/chapters/batch-generate/{batch_id}/stream` is actually a real
  same-path fallback probe, not another asymmetric one. Rust still stops at
  shared auth first and returns `401 {"detail":"未登录，请先登录"}`. Python
  stream access validation also stops before any SSE event wiring, but it uses
  the older short message `401 {"detail":"未登录"}` when `request.state.user_id`
  is missing. The manifest now adds
  `chapters-batch-stream-auth-guard-rust` under `phase5-p0` and
  `chapters-batch-stream-auth-guard-python-fallback` under
  `phase5-p0-fallback`, both using the same path
  `/api/chapters/batch-generate/test-batch-id/stream`. This upgrades
  `chapters` fallback coverage from five entries to six, extending it from
  read/status endpoints into the same-path batch stream query boundary while
  keeping `batch-generate/{batch_id}/status` in `phase5-p0-asymmetric`.
  Validation target for this checkpoint: `pytest
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py -q`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0 --route-group
  chapters --validate-manifest-only`, `python
  backend/tools/run_strangler_gateway_smoke.py --manifest
  deploy/strangler-gateway-probes.json --profile phase5-p0-fallback
  --route-group chapters --validate-manifest-only`, and `git diff --check --
  deploy/strangler-gateway-probes.json
  backend/tests/test_tools/test_run_strangler_gateway_smoke.py
  docs/architecture/rust-phase5-p0-route-group-rollback-runbook-2026-05-19.zh-CN.md
  docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md
  docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md
  .trellis/tasks/05-18-backend-chapter-generation-refactor-followup/implement.md`
  should pass, allowing only the existing line-ending warning on the manifest
  file if it persists.
- 2026-05-20 11:32 +08:00 checkpoint:
  this Phase 5 slice returned to real `backend-rs` seam tightening instead of
  expanding governance assets again. The first half of the slice moved the
  project batch-generation request assembly into
  `chapter_batch_generation_create_workflow_service.rs` so
  `chapter_batch_generation.rs` no longer consumes compat-only batch request
  fields and assembles `BatchGenerationCreateWorkflowRequest` inline. The new
  `build_batch_generation_create_workflow_request()` helper now owns compat
  field consumption plus the standard workflow request shape, mirroring the
  already-landed single-chapter request builder pattern and keeping route
  handlers transport-oriented. The second half of the slice tightened the
  owned-task access seam by adding
  `load_required_owned_task()` plus `LoadOwnedBatchGenerationTaskError` in
  `chapter_batch_generation_owned_task_query_service.rs`, then reusing that
  shared required-owned-task boundary in
  `chapter_batch_generation_cancel_service.rs`,
  `chapter_batch_generation_resume_service.rs`, and
  `chapter_batch_generation_stream_access_service.rs` instead of repeating
  local `load_owned_task(...).ok_or(TaskNotFound)` conversions. This keeps the
  existing error mapping and HTTP/SSE behavior unchanged while narrowing where
  “owned task must exist” semantics are defined. Validation target for this
  checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"` and
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filter
  `cargo test chapter_batch_generation_owned_task_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  also completed successfully but currently matches 0 tests, so this slice's
  executable regression protection remains the new create-workflow helper test
  plus the repository-wide compile check.
- 2026-05-20 11:36 +08:00 checkpoint:
  this Phase 5 slice kept working inside the `chapter_batch_generation`
  read-side seam and narrowed two small ownership boundaries without touching
  route handlers or transport payload shapes. First,
  `chapter_batch_generation_status_view_service.rs` now owns the required
  status-query view boundary via
  `load_required_batch_generation_task_view_context()` plus
  `LoadBatchGenerationTaskViewContextError`, so
  `chapter_batch_generation_status_query_service.rs` no longer performs its
  own `Option -> TaskNotFound` conversion after loading an optional context.
  This aligns the status-query read path with the previously tightened
  required-owned-task access pattern while keeping the existing
  `TaskNotFound/Internal` error mapping unchanged. Second, the empty active
  batch response wrapper is now owned by
  `chapter_batch_generation_status_payload_adapter_service.rs` through
  `build_empty_active_batch_generation_response()`, and
  `chapter_batch_generation_active_query_service.rs` reuses that shared
  adapter instead of owning a second local `{has_active_task:false,task:null}`
  payload shape. This keeps the no-active-task response identical while
  shrinking one more read-side wrapper out of the query service. Validation
  target for this checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filter
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  also completed successfully but currently matches 0 tests, so direct
  regression protection for the status-query wrapper still comes from the
  shared view/payload tests plus the compile check.
- 2026-05-20 11:41 +08:00 checkpoint:
  this Phase 5 slice stayed on the same `chapter_batch_generation` stream
  seam and finished moving stream-specific event ownership out of the shared
  status-view helper. `chapter_batch_generation_status_stream_service.rs` now
  owns the batch stream event payload builders
  (`build_batch_generation_progress_event()`,
  `build_batch_generation_result_event()`,
  `build_batch_generation_failed_event()`,
  `build_batch_generation_cancelled_event()`,
  `build_batch_generation_not_found_event()`,
  `build_batch_generation_timeout_event()`) plus
  `build_batch_generation_terminal_events()`, while
  `chapter_batch_generation_status_view_service.rs` keeps only stream-state
  loading and fallback semantics. This makes the stream owner responsible for
  stream event shape and terminal sequencing without changing the emitted SSE
  payloads, terminal conditions, or polling cadence. The same slice also
  tightened the stream orchestration boundary by adding
  `create_owned_batch_generation_status_stream()` in
  `chapter_batch_generation_status_stream_service.rs`, so
  `chapter_batch_generation_stream_workflow_service.rs` no longer performs its
  own “ensure access then build stream” choreography and instead delegates the
  full owned-stream creation boundary to the stream service. Validation target
  for this checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filter
  `cargo test chapter_batch_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  also completed successfully but currently matches 0 tests, so direct
  regression protection for the workflow wrapper still comes from the owned
  stream service tests plus the compile check.
- 2026-05-20 11:45 +08:00 checkpoint:
  this Phase 5 slice stayed on wrapper/read-side hardening and targeted two
  previously thin seams that had little or no direct focused test coverage.
  First, `chapter_batch_generation_status_query_service.rs` now owns an
  explicit `build_batch_generation_status_query_result()` helper instead of
  constructing the response wrapper inline after loading the required task
  view context. The same file now carries focused tests for
  `TaskNotFound/Internal` error mapping and for the final status-query payload
  wrapper built from a concrete `BatchGenerationTaskViewContext`. Second,
  `chapter_batch_generation_stream_access_service.rs` dropped the no-payload
  `BatchGenerationStatusStreamAccessGate` marker struct and now exposes the
  access boundary as `Result<(), BatchGenerationStatusStreamAccessError>`,
  which better matches the actual semantics of the helper while keeping the
  external workflow behavior unchanged. That file also now has focused tests
  for owned-task `TaskNotFound/Internal` error mapping, so the stream access
  seam is no longer a filter that compiles but matches zero tests. Validation
  target for this checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_stream_access_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and `cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  This checkpoint intentionally prioritizes executable seam protection over
  adding another broader ownership move, so the next round can keep shrinking
  wrappers without reopening the earlier “0 matched tests” validation gap.
- 2026-05-20 11:52 +08:00 checkpoint:
  this Phase 5 slice returned to stronger read-side ownership tightening after
  the wrapper-seam tests were stabilized. The `chapter_batch_generation`
  query/read lane no longer keeps three separate local `response_payload`
  wrapper structs for active-project, active-task-list, and task-status
  queries. Instead,
  `chapter_batch_generation_status_payload_adapter_service.rs` now owns one
  shared `BatchGenerationQueryResult` plus dedicated constructors:
  `build_active_batch_generation_query_result()`,
  `build_active_batch_generation_task_list_query_result()`, and
  `build_task_status_query_result()`. The query services
  `chapter_batch_generation_active_query_service.rs`,
  `chapter_batch_generation_active_list_query_service.rs`, and
  `chapter_batch_generation_status_query_service.rs` now delegate their final
  response wrapping to that shared payload owner instead of each defining a
  duplicate `*QueryResult { response_payload }` shell locally. This keeps all
  HTTP payload shapes unchanged while making the payload adapter the single
  owner of both read-side JSON shape and query-result wrapping for this route
  group. Focused tests were extended in the payload adapter to cover the new
  shared query-result constructors, while the existing active/status query
  tests continue to validate the resulting payloads from concrete contexts.
  Validation target for this checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 12:08 +08:00 checkpoint:
  this Phase 5 slice continued the read-side ownership tightening with a
  narrower semantics-owner move instead of adding another new payload layer.
  `chapter_batch_generation_status_semantics_service.rs` now owns the shared
  active-task status vocabulary through
  `active_batch_generation_statuses()` and
  `is_active_batch_generation_status()`, so the route-group no longer repeats
  the `"pending" / "running"` active-state definition locally. Building on
  that, `chapter_batch_generation_status_view_service.rs` now uses a shared
  `build_active_batch_generation_task_query()` helper for the active-project
  and active-user read paths. This keeps the existing project/user filters,
  sort order, and payload behavior unchanged while making the active-task
  query semantics auditable from one owner boundary instead of two partially
  duplicated query chains. Focused tests were added in
  `chapter_batch_generation_status_semantics_service.rs` to protect the active
  status vocabulary explicitly, and the existing
  `chapter_batch_generation_status_view_service.rs` tests still validate the
  downstream stream/manual-review/read-side behavior after the query helper
  move. Validation target for this checkpoint passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_semantics_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 12:34 +08:00 checkpoint:
  this Phase 5 slice advanced two parallel low-risk ownership moves without
  widening the compatibility surface. First, the single-chapter generation
  route no longer assembles the standard request object locally before calling
  the background or stream workflow. The request owner boundary moved one step
  inward: `chapter_single_generation_request_service.rs` now consumes the
  compat-only `enable_analysis` field directly in
  `build_single_chapter_generation_request()`, and both
  `chapter_single_generation_background_workflow_service.rs` and
  `chapter_single_generation_stream_workflow_service.rs` now build the
  request internally from transport values (`target_word_count`, `model`,
  `enable_analysis`). This keeps auth, workflow semantics, runtime dispatch,
  and response payloads unchanged while making
  `chapter_batch_generation.rs` thinner and less aware of request-shape
  details. Second, the batch-generation status-stream route no longer goes
  through `chapter_batch_generation_stream_workflow_service.rs`, which had
  become a pure one-line forwarder after the owned stream boundary moved into
  `chapter_batch_generation_status_stream_service.rs`. The route now calls
  `create_owned_batch_generation_status_stream()` directly, and the empty
  workflow wrapper module was removed. Validation target for this checkpoint
  passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filters
  `cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  still currently match 0 tests, so regression protection for those workflow
  shells remains indirect through the request-service tests plus compile
  validation.
- 2026-05-20 12:48 +08:00 checkpoint:
  this Phase 5 slice stayed on the single-chapter generation lane and
  tightened the request-owner boundary one step further. The previous move had
  already removed request assembly from `chapter_batch_generation.rs`, but the
  two workflow shells still repeated the same transport-to-request-to-prepared
  chain locally. `chapter_single_generation_request_service.rs` now owns that
  full conversion through
  `prepare_single_chapter_generation_transport_request()`, which consumes the
  transport fields (`target_word_count`, `model`, `enable_analysis`), builds
  the compatibility-preserving request object once, and then prepares the
  owned execution inputs. Both
  `chapter_single_generation_background_workflow_service.rs` and
  `chapter_single_generation_stream_workflow_service.rs` now delegate that
  chain to the request service instead of repeating the same
  `build_single_chapter_generation_request() + prepare_single_chapter_generation_request()`
  sequence. This keeps the same error types, runtime dispatch, SSE payload
  shape, and chapter access/config semantics, while making the request service
  the clearer owner of the transport-to-domain conversion. Focused tests were
  extended in `chapter_single_generation_request_service.rs` to protect the
  compat-field consumption entrypoint, and validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_single_generation_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The `chapter_single_generation_stream_service` filter still matches 0 tests,
  so direct regression protection for the stream shell remains compile-level
  only; this was judged acceptable for this slice because the real ownership
  move happened in the request service, which now has explicit focused
  coverage.
- 2026-05-20 13:06 +08:00 checkpoint:
  this Phase 5 slice returned to `chapter_batch_generation_task_command_service.rs`
  and tightened the shared status/checkpoint ownership across the write-side
  resume path. Before this slice, the service still carried two small drift
  risks: queued-vs-resume pending runtime checkpoints were assembled through
  separate local JSON shapes, and the resume response re-declared
  `stage_code` / `execution_mode` / `checkpoint` metadata inline instead of
  reusing the shared checkpoint metadata owner used on the read side. The
  service now centralizes pending runtime checkpoint assembly through
  `build_pending_runtime_checkpoint()`, which is reused by both the queued
  snapshot writers and the resume checkpoint builder. In addition,
  `build_resume_batch_generation_response_payload()` now owns the resume
  response payload assembly and reuses
  `checkpoint_with_runtime_metadata()` from
  `chapter_batch_generation_status_payload_adapter_service.rs` so the resume
  response no longer hand-assembles a second copy of the same
  stage/execution metadata. This keeps the HTTP response fields, resume
  semantics, checkpoint defaults, and persisted task status unchanged while
  reducing drift risk between write-side resume responses and read-side status
  payloads. Focused tests were added in
  `chapter_batch_generation_task_command_service.rs` for the new queued
  pending-checkpoint helper and the shared-metadata resume response helper.
  Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 13:24 +08:00 checkpoint:
  this Phase 5 slice kept the scope narrow but tightened two more ownership
  seams around active/read-side context loading and task-command response
  payload assembly. First,
  `chapter_batch_generation_status_view_service.rs` now owns shared
  single-or-many task-to-view-context conversion helpers through
  `build_optional_batch_generation_task_view_context()` and
  `build_batch_generation_task_view_contexts()`. The active-project,
  active-user, and owned-task read paths no longer each carry their own local
  `Option -> Some(context)` or `Vec -> push context in a loop` conversion
  pattern; they now delegate that shape to one owner boundary while keeping
  query filters, payload shape, and error behavior unchanged. Second,
  `chapter_batch_generation_task_command_service.rs` now owns dedicated
  response helpers for batch create, single background create, and cancel
  payloads, plus a shared
  `estimate_batch_generation_task_minutes()` helper. This removes another set
  of scattered inline JSON payload declarations and keeps the estimated-time
  convention in one place instead of embedding `2` / `total.max(1) * 2`
  directly in multiple command branches. These moves intentionally stop short
  of broader route/runtime refactors; the gain here is drift reduction rather
  than changing behavior. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 13:39 +08:00 checkpoint:
  this Phase 5 slice focused on a smaller but high-signal drift point:
  multiple `chapter_batch_generation` services were each locally projecting
  `LoadOwnedBatchGenerationTaskError` into their own domain-specific
  `TaskNotFound/Internal` enums with repeated `match` blocks. The owned-task
  query boundary now exposes a shared
  `map_owned_batch_generation_task_error()` helper in
  `chapter_batch_generation_owned_task_query_service.rs`, and the adjacent
  callers now delegate their not-found/internal projection to that helper
  instead of carrying four separate local mappings. The moved callers were
  `chapter_batch_generation_status_view_service.rs`,
  `chapter_batch_generation_cancel_service.rs`,
  `chapter_batch_generation_resume_service.rs`, and
  `chapter_batch_generation_stream_access_service.rs`. This does not change
  any outward error enum, response payload, or route mapping; it only reduces
  the chance that future owned-task error handling drifts between read-side,
  command-side, and stream access boundaries. Focused tests were added on the
  new shared mapper and the existing stream-access tests were updated to use
  the shared helper. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_owned_task_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_stream_access_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 13:52 +08:00 checkpoint:
  this Phase 5 slice targeted the remaining ultra-thin workflow/result shells
  on the batch-generation path and removed wrappers that no longer carried
  independent semantics. `chapter_batch_generation_create_workflow_service.rs`
  and `chapter_batch_generation_resume_service.rs` no longer wrap their final
  response in one-field `Start*WorkflowResult` / `*WorkflowResult` structs
  when the only exported field was `response_payload`. The start/create path
  now returns the response `Value` directly after dispatch setup, and the
  resume workflow path now likewise returns the cloned response payload
  directly once dispatch has been scheduled. On the cancel path,
  `chapter_batch_generation_cancel_service.rs` now returns the existing
  `CancelBatchGenerationResult` from the task-command owner instead of
  introducing a second cancel-specific workflow wrapper with the same single
  field. `chapter_batch_generation.rs` was updated only at the transport call
  sites to consume those slimmer return shapes; route payloads and error
  mapping stay unchanged. This slice intentionally stopped before touching
  `PreparedBatchGenerationResumeRequest`, because that struct still owns
  multiple values (`response_payload`, `ai_config`, `provider_payload`,
  `execution`) and is not a pure wrapper yet. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filter
  `cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  still matches 0 tests, which is consistent with the fact that this slice
  only removed a thin wrapper shell and kept the actual resume semantics
  protected through compile validation plus the task-command tests.
- 2026-05-20 14:07 +08:00 checkpoint:
  this Phase 5 slice continued the same thin-wrapper reduction on the
  read/query side by removing the now-redundant `BatchGenerationQueryResult`
  shell from `chapter_batch_generation_status_payload_adapter_service.rs` and
  its direct consumers. The adapter-owned `build_*_query_result()` helpers now
  return `serde_json::Value` directly instead of wrapping that payload in a
  one-field struct, and the adjacent query services
  `chapter_batch_generation_status_query_service.rs`,
  `chapter_batch_generation_active_query_service.rs`, and
  `chapter_batch_generation_active_list_query_service.rs` now return `Value`
  directly as well. `chapter_batch_generation.rs` was updated only at the
  route call sites to consume the slimmer return shape, with no response
  field changes and no error-mapping changes. This keeps the payload adapter
  as the owner of JSON shape while removing one more internal wrapper layer
  that no longer encoded meaningful behavior. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:21 +08:00 checkpoint:
  this Phase 5 slice stayed on the same batch-generation read/status lane and
  removed three more no-behavior query wrappers from
  `chapter_batch_generation_status_payload_adapter_service.rs`:
  `build_task_status_query_result()`,
  `build_active_batch_generation_query_result()`, and
  `build_active_batch_generation_task_list_query_result()`. The adjacent
  query services
  `chapter_batch_generation_status_query_service.rs`,
  `chapter_batch_generation_active_query_service.rs`, and
  `chapter_batch_generation_active_list_query_service.rs` now call the real
  response builders directly:
  `build_task_status_response()`,
  `build_active_batch_generation_response()`,
  `build_empty_active_batch_generation_response()`, and
  `build_active_batch_generation_task_list_response()`. This keeps JSON
  payload shape, task-not-found/error mapping, and status semantics unchanged
  while deleting one more adapter-only forwarding layer. Validation passed
  with `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:24 +08:00 checkpoint:
  this Phase 5 slice tightened runtime snapshot ownership by deleting the
  duplicated snapshot-loader and new-task snapshot initialization helpers from
  `chapter_batch_generation_task_command_service.rs`, then reusing the
  existing owner functions in
  `chapter_batch_generation_runtime_state_service.rs` instead. In the same
  pass, `chapter_batch_generation_status_view_service.rs` stopped carrying its
  own copy of `load_batch_generation_snapshot()` and now imports that read
  helper from the runtime owner as well. Resume-specific snapshot replacement
  semantics stay local to the task-command owner, so this slice only removes
  duplication and clarifies that general snapshot load/upsert/init behavior
  belongs to the runtime state service. No HTTP payloads, status semantics,
  or resume-reset behavior changed. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:26 +08:00 checkpoint:
  this Phase 5 slice continued the same thin-wrapper reduction on the
  task-command lane by deleting the one-field
  `CancelBatchGenerationResult` shell from
  `chapter_batch_generation_task_command_service.rs`. The owned
  `cancel_batch_generation_task()` entrypoint now returns the final response
  `Value` directly after persisting the cancelled task state and reusing the
  shared runtime finalizer, and
  `chapter_batch_generation_cancel_service.rs` now forwards that payload
  without an extra unwrap/rewrap hop. No cancel payload fields, task status
  semantics, or runtime checkpoint behavior changed; this only narrows one
  more internal service boundary that no longer encoded business meaning.
  Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_cancel_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:30 +08:00 checkpoint:
  this Phase 5 slice returned to the query lane and removed two more
  no-behavior helper hops. In
  `chapter_batch_generation_status_query_service.rs`, the local
  `build_batch_generation_status_query_result()` wrapper was deleted and the
  query entrypoint now returns `build_task_status_response(context)` directly.
  In `chapter_batch_generation_active_list_query_service.rs`, the intermediate
  `load_active_batch_generation_task_list_query()` helper was removed because
  its only caller already owned limit normalization and only forwarded the
  resulting contexts into `build_active_batch_generation_task_list_response()`.
  The route and service contracts stay the same: task-not-found/internal
  mapping, active-list limit normalization, and all status/list JSON payload
  fields remain unchanged. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:33 +08:00 checkpoint:
  this Phase 5 slice narrowed the task-command to workflow contract by
  shrinking three plan carriers down to the minimum data their callers
  actually consume. `BatchGenerationCreatePlan` and
  `SingleGenerationBackgroundCreatePlan` in
  `chapter_batch_generation_task_command_service.rs` no longer expose the full
  persisted `created_task` model; they now return only `created_task_id`
  alongside the already-owned `response_payload`, `chapter_ids`, and
  `target_word_count`. In the same pass, `ResumeBatchGenerationPlan` stopped
  exposing `updated_task` because the resume workflow only needs the
  `response_payload` plus `execution` plan after the task-command owner
  persists the reset state internally. The adjacent workflow owners
  `chapter_batch_generation_create_workflow_service.rs` and
  `chapter_single_generation_background_workflow_service.rs` were updated to
  consume the new `created_task_id` field directly. This keeps all create,
  background, and resume payloads plus runtime dispatch semantics unchanged
  while reducing the amount of database-model knowledge leaked across the
  service boundary. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:36 +08:00 checkpoint:
  this Phase 5 slice kept shrinking workflow-only carriers that had only one
  local consumer and no independent behavior. In
  `chapter_batch_generation_resume_service.rs`, the private
  `PreparedBatchGenerationResumeRequest` shell and its
  `prepare_batch_generation_resume_request()` helper were removed; the public
  `resume_owned_batch_generation_task()` entrypoint now performs the same
  owned-task load, task-command resume preparation, config preparation, and
  runtime dispatch inline before returning the unchanged response payload. In
  `chapter_single_generation_background_workflow_service.rs`, the private
  `CreateSingleGenerationBackgroundWorkflowResult` carrier and its
  `create_single_generation_background_workflow()` helper were removed for the
  same reason, so the workflow entrypoint now directly sequences request
  preparation, task-plan creation, runtime dispatch, and final payload
  forwarding. No create/resume/background payload fields, error mapping, or
  dispatch semantics changed; this slice only removes two more single-use
  internal wrappers from the workflow lane. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 14:41 +08:00 checkpoint:
  this Phase 5 slice tightened two more adjacent orchestration seams. First,
  `chapter_batch_generation_status_stream_service.rs` absorbed the tiny
  `chapter_batch_generation_stream_access_service.rs` access wrapper: the
  stream-access error enum and owned-task permission check now live directly
  next to `create_owned_batch_generation_status_stream()`, and the standalone
  wrapper module plus its `services/mod.rs` export were removed. Second,
  `chapter_batch_generation_create_workflow_service.rs` no longer uses the
  private single-consumer `CreateBatchGenerationWorkflowResult` carrier.
  `start_owned_batch_generation_workflow()` now performs request building,
  access verification, prepared create lookup, config preparation, task-plan
  creation, runtime dispatch, and final payload forwarding in one owner
  boundary. No stream-access error mapping, create payload fields, config
  defaults, or runtime dispatch semantics changed; this slice only removes one
  dead helper module and one single-use workflow carrier. Validation passed
  with `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and a follow-up literal search confirmed there are no remaining
  `chapter_batch_generation_stream_access_service` references under
  `backend-rs/src`.
- 2026-05-20 14:45 +08:00 checkpoint:
  this Phase 5 slice focused on internal contract slimming in the remaining
  batch-create support helpers. In
  `chapter_batch_generation_request_compat_service.rs`, the intermediate
  `BatchGenerationRequestCompatView` projection and its
  `project_batch_generation_request_compat_fields()` helper were removed
  because the codebase no longer consumes that projected view anywhere; the
  compat-consumption boundary now reads the original
  `BatchGenerationRequestCompatFields` directly and preserves the same
  placeholder compatibility sink behavior. In
  `chapter_batch_generation_create_service.rs`, the
  `PreparedBatchGenerationCreateRequest` contract was narrowed by removing the
  unused `end_chapter_number` field, and the duplicated count validation inside
  `prepare_batch_generation_create_request()` was dropped because
  `load_chapters_for_batch_generation_range()` already owns that check. The
  normalized target-word-count behavior, create-workflow orchestration, and
  batch chapter lookup semantics remain unchanged; this slice only deletes one
  dead projection layer and one unused prepared field while relying on the
  existing validation owner. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_create_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 12:37 +08:00 checkpoint:
  this Phase 5 slice returned to the batch-create lane and moved one more
  transport-to-domain ownership seam inward without changing route behavior.
  `chapter_batch_generation.rs` no longer builds a
  `BatchGenerationCreateWorkflowRequest` locally before calling the workflow
  owner. Instead,
  `chapter_batch_generation_create_workflow_service.rs` now keeps
  `build_batch_generation_create_workflow_request()` private and
  `start_owned_batch_generation_workflow()` directly accepts the transport
  fields plus `BatchGenerationRequestCompatFields`, consumes the compat-only
  fields, and assembles the owned workflow request internally before calling
  the existing create/dispatch path. This keeps HTTP payload fields, route
  error mapping, task-plan creation, default `enable_analysis` /
  `max_retries` semantics, and runtime dispatch unchanged while making the
  route more strictly transport-only and aligning the batch-create path with
  the earlier single-generation request-owner move. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"` and
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 12:41 +08:00 checkpoint:
  this Phase 5 slice removed two more internal one-field response wrappers on
  adjacent create/cancel paths without widening the compatibility surface.
  `chapter_single_generation_background_workflow_service.rs` no longer returns
  `StartSingleGenerationBackgroundWorkflowResult { response_payload }` from
  `start_owned_single_generation_background_workflow()`; after dispatching the
  owned runtime, it now returns the response `Value` directly. In parallel,
  `chapter_batch_generation_cancel_service.rs` no longer unwraps and rewraps
  `CancelBatchGenerationResult` from the task-command owner just to forward
  the same payload shape; `cancel_owned_batch_generation_task()` now returns
  that owned response `Value` directly. `chapter_batch_generation.rs` was
  updated only at the transport call sites to consume those slimmer return
  values, so the HTTP payload fields and error mapping remain unchanged while
  the route group loses two more pieces of internal `.response_payload`
  knowledge. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"` and
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The focused filter
  `cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  still matches 0 tests, so direct protection for that workflow shell remains
  compile-level only; this is acceptable here because the moved seam is a
  pure wrapper reduction and the owned single-generation request/task-command
  helpers still carry the meaningful focused coverage.
- 2026-05-20 12:44 +08:00 checkpoint:
  this Phase 5 slice stayed entirely inside the existing workflow owners and
  tightened their internal visibility instead of changing any outward
  contract. Three workflow-only result/request carriers are now private to
  their owner files:
  `BatchGenerationCreateWorkflowRequest` and
  `CreateBatchGenerationWorkflowResult` inside
  `chapter_batch_generation_create_workflow_service.rs`,
  `PreparedBatchGenerationResumeRequest` inside
  `chapter_batch_generation_resume_service.rs`, and
  `CreateSingleGenerationBackgroundWorkflowResult` inside
  `chapter_single_generation_background_workflow_service.rs`.
  The corresponding helper functions
  `create_batch_generation_workflow()`,
  `prepare_batch_generation_resume_request()`, and
  `create_single_generation_background_workflow()` are also now file-private,
  because their call sites never crossed a module boundary. While doing that,
  the workflow entrypoints now destructure those internal carriers directly
  and stop clone-forwarding `response_payload`, which makes the owner
  boundary narrower without changing route payloads, dispatch semantics,
  status/checkpoint defaults, or error mapping. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 12:47 +08:00 checkpoint:
  this Phase 5 slice removed a now-dead aggregation facade instead of moving
  more behavior across boundaries. `chapter_batch_generation_service.rs` had
  become a pure `pub use` surface for adjacent task/runtime/query helpers, and
  the remaining codebase no longer referenced that module at all. The facade
  file was removed and `services/mod.rs` no longer exports
  `chapter_batch_generation_service`. This reduces one more public module
  boundary without changing runtime behavior, route ownership, or any payload
  shape. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"` and
  `cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  A follow-up literal search for `chapter_batch_generation_service` in
  `backend-rs/src` returned no matches, confirming there are no residual
  internal consumers of the deleted facade.
- 2026-05-20 12:50 +08:00 checkpoint:
  this Phase 5 slice applied the same facade-cleanup pattern to two adjacent
  chapter-generation compatibility layers. `chapter_analysis_quality_service.rs`
  was a dead one-line re-export of
  `chapter_quality_query_service::load_chapter_quality_metrics_payload` and no
  longer had any internal consumers, so the facade file and its `services/mod.rs`
  export were removed. `chapter_generation_service.rs` had become a single
  re-export of
  `chapter_generation_runtime_service::generate_and_persist_chapter_content_with_provider_payload`;
  its remaining internal callers in
  `chapter_batch_generation_runtime_state_service.rs` and
  `chapter_single_generation_stream_service.rs` now import the runtime owner
  directly, and the compatibility facade file plus its `services/mod.rs`
  export were removed as well. This further narrows the Rust service module
  surface without changing generation behavior, payload shape, or runtime
  sequencing. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_generation_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  A follow-up literal search for `chapter_analysis_quality_service` and
  `chapter_generation_service` in `backend-rs/src` returned no matches,
  confirming there are no residual internal consumers of the deleted facades.
- 2026-05-20 12:53 +08:00 checkpoint:
  this Phase 5 slice continued the same surface-reduction work on the
  regeneration lane. `chapter_regeneration_service.rs` had become a dead
  compatibility facade that only re-exported apply/prepare/text helpers and no
  longer had any internal consumers, so the facade file and its `services/mod.rs`
  export were removed. In parallel,
  `chapter_regeneration_text_service.rs` no longer re-exports
  `contains_chapter_workflow_meta_text` and
  `sanitize_generated_narrative_text` from
  `chapter_narrative_cleaner_service`; it now imports those cleaner helpers
  privately for its own finalize logic. Adjacent callers that truly need the
  cleaner behavior, such as `chapter_regeneration_apply_service.rs`, already
  import the cleaner owner directly. This keeps regeneration payload shapes
  and text-finalization behavior unchanged while shrinking one more public
  compatibility surface and clarifying that narrative cleaning belongs to the
  cleaner owner, not to the regeneration text adapter. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_regeneration_text_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_regeneration_apply_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  A follow-up literal search for `chapter_regeneration_service` in
  `backend-rs/src/services` returned no matches, confirming there are no
  residual internal consumers of the deleted facade.
- 2026-05-20 12:55 +08:00 checkpoint:
  this Phase 5 slice applied the same dead-facade cleanup to the CRUD lane.
  `chapter_crud_service.rs` only re-exported payload workflow functions and
  error enums from `chapter_crud_workflow_service.rs`, and the remaining
  Rust callers were limited to `chapter_crud_routes.rs` and
  `chapter_crud_error_mapper.rs`. Those call sites now import the CRUD
  workflow owner directly, so the compatibility facade file and its
  `services/mod.rs` export were removed. This leaves CRUD route behavior,
  payload shape, and error mapping unchanged while shrinking one more public
  service module surface. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_crud_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_crud_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The CRUD workflow filter still matches 0 tests in this environment, so
  direct regression protection for this slice remains compile-level plus the
  payload-adapter tests; that is acceptable here because the change was a pure
  facade removal with no workflow logic edits. A follow-up literal search for
  `chapter_crud_service` in `backend-rs/src` returned no matches, confirming
  there are no residual internal consumers of the deleted facade.
- 2026-05-20 12:59 +08:00 checkpoint:
  this Phase 5 slice returned to the batch-generation read/status lane and
  tightened two small internal visibility seams without changing status
  semantics or payload shape. First,
  `chapter_batch_generation_status_view_service.rs` no longer re-exports
  payload-adapter or status-semantics helpers through `pub use`; it now
  imports only the owner functions it actually consumes internally, and its
  tests import payload helpers directly from
  `chapter_batch_generation_status_payload_adapter_service.rs`. Second,
  `chapter_batch_generation_status_query_service.rs` made
  `build_batch_generation_status_query_result()` file-private because the
  helper is only used by the local query path plus local unit tests. This
  keeps the same task-not-found/internal mapping and the same task-status JSON
  shape while shrinking one more layer of historical transitive surface from
  the status owner modules. Validation passed with
  `cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
- 2026-05-20 13:38 +08:00 checkpoint:
  this Phase 5 slice stayed inside the batch-generation runtime owner boundary
  and pulled two remaining pieces of runtime-state infrastructure out of
  `chapter_batch_generation_task_command_service.rs` without changing resume
  orchestration. `chapter_batch_generation_runtime_state_service.rs` now owns
  `build_pending_batch_generation_runtime_checkpoint()` for pending batch-style
  checkpoint assembly and also owns
  `replace_batch_generation_runtime_snapshot_for_resume()` for the strict
  resume snapshot-reset write path that clears stale quality fields and
  replaces, rather than merges, the persisted runtime checkpoint. The
  task-command owner still decides resume eligibility, execution branching, and
  task-row reset semantics, but it now reuses those runtime-state helpers
  instead of carrying a second local checkpoint builder plus a duplicate
  snapshot write implementation. This keeps response payloads, checkpoint
  defaults, manual-review resume blocking, and `ResumeExecutionPlan`
  dispatching unchanged while making the runtime owner more complete. This
  slice also moved the queued pending-checkpoint unit assertion to the runtime
  owner so the extracted helper keeps direct focused coverage. Validation
  passed with `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Current stop-rule assessment: the most obvious remaining seams in this lane
  now sit much closer to true resume/dispatch orchestration boundaries, so the
  next Phase 5 slice should only proceed when it can prove the same
  behavior-preserving profile as this runtime-owner move.
- 2026-05-20 15:48 +08:00 checkpoint:
  this Phase 5 slice moved back to the single-chapter request ownership lane
  and removed two now-thin transport wrappers from
  `chapter_single_generation_request_service.rs` without changing route or
  runtime behavior. The service no longer carries
  `build_single_chapter_generation_request()` or
  `prepare_single_chapter_generation_transport_request()` because both were
  single-consumer helpers that only consumed the compat-only
  `enable_analysis` field and then forwarded the same minimal
  `{ target_word_count, model }` request contract into the real owner entrypoint.
  `chapter_single_generation_background_workflow_service.rs` and
  `chapter_single_generation_stream_workflow_service.rs` now consume the
  compat-only `enable_analysis` field locally, assemble the minimal
  `SingleChapterGenerationRequest`, and call
  `prepare_single_chapter_generation_request()` directly. This keeps HTTP
  payload fields, single-chapter access checks, target-word normalization,
  provider-payload preparation, background task creation, runtime dispatch,
  and SSE stream construction unchanged while making the request owner surface
  narrower and clarifying that the workflow boundary, not the request service,
  owns transport-only compat consumption. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The two workflow-focused filters still match 0 tests in this environment, so
  direct protection for the moved workflow seam remains compile-level plus the
  request-service unit coverage; that is acceptable here because the slice was
  a pure contract-thinning move with no runtime logic edits.
- 2026-05-20 15:55 +08:00 checkpoint:
  this Phase 5 slice returned to the batch-create lane and applied two
  back-to-back contract-thinning moves inside the existing create owner path
  without changing transport behavior or runtime dispatch. First,
  `chapter_batch_generation_create_workflow_service.rs` no longer keeps the
  internal `BatchGenerationCreateWorkflowRequest` carrier or the
  `build_batch_generation_create_workflow_request()` helper. The workflow
  entrypoint now consumes the compat-only request fields locally, keeps
  project-access verification at the same boundary, and passes the explicit
  transport fields directly into create preparation, execution-config loading,
  task-plan creation, and runtime dispatch. Second,
  `chapter_batch_generation_create_service.rs` no longer carries the
  single-consumer `BatchGenerationCreateRequest` input struct; its owner
  entrypoint now directly accepts
  `(start_chapter_number, count, target_word_count)` as the minimal create
  contract while preserving the same count validation, chapter-range loading,
  and target-word normalization behavior. Together these two moves keep HTTP
  payload fields, project-access checks, chapter selection semantics,
  default `enable_analysis` / `max_retries` behavior, provider-payload
  preparation, task-plan creation, and background runtime dispatch unchanged
  while shrinking one more internal request layer from the batch-create path.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Current stop-rule assessment: the obvious low-risk request/contract wrappers
  in the batch-create and single-chapter lanes are now much thinner than
  before, so the next Phase 5 slice should prefer read-side/helper ownership
  seams or another clearly single-consumer carrier, not orchestration-heavy
  resume/dispatch changes.
- 2026-05-20 16:00 +08:00 checkpoint:
  this Phase 5 slice moved back to the batch-generation read side and narrowed
  the payload-adapter surface so query-only response envelopes now belong to
  the query owners instead of the shared adapter module. In
  `chapter_batch_generation_status_payload_adapter_service.rs`, the shared
  adapter now only owns the core payload builders
  (`task_status_payload`, `active_task_payload`, task-list response helpers)
  and no longer carries the query-specific wrappers
  `build_task_status_response()`,
  `build_active_batch_generation_response()`, or
  `build_empty_active_batch_generation_response()`. That envelope assembly now
  lives locally in `chapter_batch_generation_status_query_service.rs` via
  `build_batch_generation_status_query_response()` and in
  `chapter_batch_generation_active_query_service.rs` via
  `build_active_batch_generation_query_response()`, so each query owner keeps
  its own transport-facing `Some/None` response shape while still reusing the
  shared payload builders for the actual task/status content. This keeps task
  status JSON fields, active-task envelope fields, quality/status metadata, and
  empty-state behavior unchanged while making the shared adapter less
  transitive and clarifying ownership between core payload projection and query
  response assembly. During verification, a duplicate `#[test]` attribute in
  `chapter_batch_generation_status_payload_adapter_service.rs` surfaced as
  warning-only noise, so that redundant attribute was removed to keep future
  focused test output clean. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Current stop-rule assessment: the remaining obvious Phase 5 seams are now
  increasingly concentrated in behavior-sensitive status/view/runtime owners,
  so the next move should only proceed if it cleanly separates another helper
  or single-consumer carrier without pulling true orchestration or status
  semantics across boundaries.
- 2026-05-20 16:08 +08:00 checkpoint:
  this Phase 5 slice continued the same read-side owner-tightening pattern on
  the active-task-list query path. `chapter_batch_generation_status_payload_adapter_service.rs`
  no longer carries `build_active_batch_generation_task_list_response()`;
  the shared adapter now only owns the item-level payload builders
  (`task_status_payload`, `active_task_payload`) plus supporting metadata
  helpers. The `total/items` list-response envelope is now owned locally by
  `chapter_batch_generation_active_list_query_service.rs` via
  `build_active_batch_generation_task_list_query_response()`, so the query
  owner controls its own response shape while still reusing the shared
  per-item payload projection. This keeps `items` payload fields, `total`
  semantics, quality/status metadata, and empty-list behavior unchanged while
  further reducing the transitive surface of the shared adapter module. During
  the move, two pieces of stale test coupling were also cleaned up: a
  cross-owner test dependency from `chapter_batch_generation_status_view_service.rs`
  to the active-list query helper was removed instead of widening that helper
  back to a public surface, and leftover unused imports/warning noise were
  cleared so focused tests remain signal-rich. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Current stop-rule assessment: the remaining Phase 5 opportunities are now
  mostly clustered inside behavior-sensitive status-view/runtime/task-command
  owners, so further acceleration should stay selective and only take seams
  that remove a clearly isolated helper or carrier without moving status logic
  or orchestration semantics.
- 2026-05-20 16:15 +08:00 checkpoint:
  this Phase 5 slice stayed inside the same batch-generation resume/runtime
  lane and completed one more owner-tightening move without changing resume
  eligibility or dispatch sequencing.
  `chapter_batch_generation_runtime_state_service.rs` now owns the remaining
  resume runtime reset helpers
  `build_resume_batch_generation_runtime_checkpoint()` and
  `resolve_resume_batch_generation_runtime_position()`, so the runtime-state
  owner fully controls how a resumed task clears or preserves chapter pointer
  plus pending checkpoint progress before the snapshot reset write.
  `chapter_batch_generation_task_command_service.rs` still owns domain
  validation (failed/cancelled gate, manual-review blocker, execution-plan
  branching, task-row reset), but it now delegates the resume checkpoint
  projection to the runtime-state owner instead of carrying a second local copy
  of that runtime-reset shape. Focused unit coverage for the single-chapter
  resume checkpoint and batch resume position/progress reset moved with the
  helper into the runtime-state owner, while task-command tests keep coverage
  for response payload and resume gating behavior. This keeps resume response
  payload fields, snapshot reset semantics, chapter pointer preservation for
  single-generation tasks, batch pointer clearing, and `ResumeExecutionPlan`
  dispatch behavior unchanged while making the runtime owner more complete.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: remaining obvious seams in this lane are now
  even closer to true resume orchestration, task-row semantics, or read-side
  status behavior, so the next Phase 5 slice should only continue if it can
  isolate another helper/carrier with the same behavior-preserving profile.
- 2026-05-20 16:19 +08:00 checkpoint:
  this Phase 5 slice accelerated with two additional low-risk surface-slimming
  moves that stay outside batch-generation orchestration and keep all external
  behavior unchanged. First,
  `chapter_batch_generation_request_compat_service.rs` now only keeps the
  `BatchGenerationRequestCompatFields` carrier; the single-consumer no-op
  helper `consume_batch_generation_request_compat_fields()` and its dedicated
  tests were removed because the compat-only fields are still consumed exactly
  once at the batch-create workflow boundary. `chapter_batch_generation_create_workflow_service.rs`
  now reads that compat carrier locally, preserves the same placeholder field
  touch for compatibility, and also narrows
  `normalize_batch_generation_enable_analysis()` plus
  `normalize_batch_generation_max_retries()` to file-private visibility because
  they are only used by the local workflow owner and its unit tests.
  Second, `chapter_batch_generation_status_stream_service.rs` reduced another
  small transitive surface by making its event-construction helpers private:
  progress/result/failed/cancelled/not-found/timeout builders,
  terminal-event assembly, and the internal stream constructor are now
  file-owned helpers because the public transport boundary is only
  `create_owned_batch_generation_status_stream()`. This keeps SSE event JSON
  shape, terminal ordering, polling cadence, and access-control behavior
  unchanged while shrinking one more internal helper surface from the stream
  owner. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: after removing the compat no-op wrapper and
  private stream-helper leakage, the remaining obvious Phase 5 seams are now
  even more concentrated in behavior-sensitive `status_view`, `runtime_state`,
  `task_command`, or API transport boundaries. Further acceleration should
  continue only when a candidate seam is clearly a single-consumer helper,
  carrier, or internal visibility tightening move with no task/status semantic
  drift risk.
- 2026-05-20 16:23 +08:00 checkpoint:
  this Phase 5 slice continued with one more pair of low-risk visibility /
  transport-seam tightening moves, again without changing any external request,
  response, or runtime behavior. On the service side,
  `chapter_batch_generation_access_service.rs` narrowed
  `build_user_ai_config()` to file-private visibility because it is only used
  by the local `prepare_generation_execution_config()` owner and its value is
  not a standalone cross-module contract. In parallel,
  `chapter_batch_generation_active_list_query_service.rs` narrowed
  `normalize_active_batch_generation_task_list_limit()` to file-private
  visibility because the limit normalization contract is only consumed by the
  active-list query owner plus local unit tests.
  On the API transport side,
  `backend-rs/src/api/chapter_batch_generation.rs` now uses one route-local
  helper `build_batch_generation_request_compat_fields()` to assemble the
  compat carrier before delegating to
  `start_owned_batch_generation_workflow()`. This keeps the route transport-
  oriented while reducing inline request-field copying and making the compat
  carrier assembly explicit at one local boundary. No request fields, defaults,
  workflow inputs, query behavior, or background dispatch behavior changed.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: the remaining useful Phase 5 seams now sit even
  closer to truly shared read-side builders (`status_view`) or to behavior-
  sensitive command/runtime owners. Further acceleration should stay selective
  and prefer only private-helper tightening or unmistakably transport-only
  extraction moves that do not alter task/status/checkpoint semantics.
- 2026-05-20 16:28 +08:00 checkpoint:
  this Phase 5 slice aligned the single-generation lane with the same
  compatibility-surface slimming pattern already applied to batch generation.
  `chapter_single_generation_request_service.rs` no longer carries the no-op
  compat helper `consume_single_chapter_generation_request_compat_fields()`;
  the compat-only `enable_analysis` field is still consumed exactly once at the
  workflow boundary, but it now stays local to
  `chapter_single_generation_background_workflow_service.rs` and
  `chapter_single_generation_stream_workflow_service.rs` via a direct
  placeholder read instead of flowing through a dedicated wrapper function.
  This keeps the single-generation request contract minimal and makes it
  explicit that `enable_analysis` is still compatibility-only in this lane.
  In parallel, `chapter_batch_generation_status_view_service.rs` narrowed
  `build_batch_generation_task_view_context()` to file-private visibility
  because that builder is only used by local load/query helpers inside the same
  owner module; no other module consumes it as a cross-owner contract. These
  moves keep single-generation request fields, background/stream workflow
  inputs, status-view payload assembly, runtime state loading, and read-side
  semantics unchanged while shrinking one more set of internal helper surfaces.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The single-generation stream workflow filter still matches 0 tests in this
  environment, so direct protection there remains compile-level plus the
  request-service tests; that is acceptable for this slice because the move was
  limited to removing a no-op compat wrapper and keeping the placeholder read
  local to the workflow boundary.
  Updated stop-rule assessment: Phase 5 now has even fewer obvious low-risk
  wrappers left. The remaining candidates are mostly private-helper tightening
  inside `status_view`, or true behavior-sensitive owners where further moves
  should stop unless the seam is unmistakably single-consumer and
  behavior-preserving.
- 2026-05-20 16:32 +08:00 checkpoint:
  this Phase 5 slice tightened the remaining crate-internal carrier and
  dispatch boundaries across the batch-generation command/access lanes without
  changing any runtime behavior. In
  `chapter_batch_generation_access_service.rs`, the generation-preparation
  contract moved from public module surface to crate-internal surface:
  `LoadAccessibleChapterForGenerationError`,
  `PreparedGenerationExecutionConfig`, and the access/preparation helpers
  `prepare_generation_execution_config()`, `verify_project_access()`, and
  `load_accessible_chapter_for_generation()` now use `pub(crate)` visibility
  because they are only consumed inside the Rust backend crate.
  In `chapter_batch_generation_dispatch_service.rs`, the concrete dispatch
  helpers for single, batch, and resume execution are also now `pub(crate)`,
  matching their real usage as internal runtime-launch boundaries rather than
  external contracts. In
  `chapter_batch_generation_request_compat_service.rs`, the compat carrier
  `BatchGenerationRequestCompatFields` is now crate-internal with crate-
  internal fields, reflecting that it only flows from the route module into the
  batch-create workflow owner. Finally,
  `chapter_batch_generation_task_command_service.rs` narrowed the command-side
  plan and execution carriers to crate-internal visibility:
  `BatchGenerationCreatePlan`,
  `SingleGenerationBackgroundCreatePlan`,
  `ResumeBatchGenerationPlan`,
  `ResumeExecutionPlan`,
  `create_batch_generation_task_plan()`,
  `create_single_generation_background_task_plan()`,
  `prepare_batch_generation_resume()`, and `cancel_batch_generation_task()` are
  now explicitly crate-internal, while `parse_batch_task_chapter_ids()` was
  further narrowed to file-private because only the local owner and its tests
  consume it. During validation, a `private_interfaces` warning exposed that
  `start_owned_batch_generation_workflow()` still accepted the now
  crate-internal compat carrier while remaining `pub`; the function visibility
  was aligned to `pub(crate)` so the carrier and workflow boundary now match.
  This keeps route behavior, workflow inputs, dispatch behavior, resume
  branching, response payloads, and task/checkpoint semantics unchanged while
  making the remaining Phase 5 boundary much more honest about what is truly
  internal to the Rust backend crate.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  The resume/background workflow filters still match 0 tests in this
  environment, so direct protection there remains compile-level plus adjacent
  task-command/create coverage; that is acceptable here because the slice was a
  pure visibility-tightening move with no behavior edits.
  Updated stop-rule assessment: most remaining Phase 5 opportunities are now
  inside genuinely behavior-sensitive owners rather than obvious overexposed
  crate surfaces. Further acceleration should prefer only final private-helper
  tightening or stop when the next move would primarily reshuffle semantics
  instead of shrinking a clear boundary.
- 2026-05-20 16:36 +08:00 checkpoint:
  this Phase 5 slice completed a matching crate-internal visibility pass across
  the batch-generation read-side owners. In
  `chapter_batch_generation_status_view_service.rs`, the read-side context and
  stream-state contracts are now crate-internal:
  `BatchGenerationTaskViewContext`,
  `LoadBatchGenerationTaskViewContextError`,
  `load_required_batch_generation_task_view_context()`,
  `load_active_project_batch_generation_task_view_context()`,
  `load_active_user_batch_generation_task_view_contexts()`,
  `BatchGenerationStreamState`, and
  `load_batch_generation_stream_state()` now use `pub(crate)` visibility,
  reflecting that they are consumed only by other Rust backend modules. During
  the same pass, the now-unused optional task-view loader
  `load_batch_generation_task_view_context()` was removed entirely because no
  remaining module or test consumed it after the earlier status-query
  refactor. In the adjacent query/stream owners,
  `LoadBatchGenerationStatusQueryError`,
  `LoadActiveBatchGenerationQueryError`,
  `LoadActiveBatchGenerationTaskListQueryError`,
  `BatchGenerationStatusStreamAccessError`, and their corresponding load/stream
  entrypoints were aligned to `pub(crate)` visibility as well.
  This immediately surfaced one more honest-boundary cleanup in
  `chapter_batch_generation_error_mapper.rs`: the error-mapper functions for
  active-query, active-list, status-query, and status-stream access are now
  also `pub(crate)` so their visibility matches the crate-internal error types
  they map. Together these moves keep status JSON fields, active-query payloads,
  SSE polling/event behavior, access control, and error mapping semantics
  unchanged while finishing one more meaningful boundary-tightening wave on the
  read side and eliminating the dead optional task-view loader.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: after the read-side crate-internal pass, the
  remaining unsimplified surfaces are now overwhelmingly concentrated in true
  behavior owners such as `runtime_state` and `task_command`, or else in route-
  level transport glue whose remaining code is mostly explicit wiring rather
  than accidental boundary bloat. That means the next meaningful move is likely
  no longer a cheap Phase 5 seam-tightening slice. Unless a clearly isolated
  private helper still appears, this follow-up should treat Phase 5 as
  approaching a stage-complete checkpoint rather than continuing mechanical
  boundary churn.
- 2026-05-20 16:40 +08:00 checkpoint:
  this Phase 5 slice finished one more crate-internal visibility alignment pass
  across the remaining workflow-entry and request-preparation boundaries that
  still sat above their actual usage scope. In the batch-generation workflow
  lane, `CreateBatchGenerationWorkflowDomainError`,
  `CreateBatchGenerationWorkflowError`,
  `CancelBatchGenerationWorkflowError`, and
  `PrepareBatchGenerationResumeRequestError` are now crate-internal, matching
  the fact that their only consumers are the batch-generation route module and
  its local error mapper. In the single-generation lane,
  `CreateSingleGenerationBackgroundWorkflowError`,
  `start_owned_single_generation_background_workflow()`, and
  `create_single_generation_stream_workflow()` are now crate-internal as well.
  The single-generation request-preparation layer was tightened further so
  `SingleChapterGenerationRequest`,
  `PrepareSingleChapterGenerationRequestError`,
  `PreparedSingleChapterGenerationRequest`, and
  `prepare_single_chapter_generation_request()` are crate-internal, while the
  target-word helper pair
  `normalize_single_chapter_generation_target_word_count()` and
  `load_chapter_generation_target()` now stay file-private because only the
  local request owner and its tests consume them. To keep boundary visibility
  honest end-to-end, the remaining batch-generation error-mapper entrypoints
  for create/cancel/resume plus the single-generation request/background
  mappers were also aligned to `pub(crate)` visibility. These moves preserve
  HTTP payloads, route behavior, workflow branching, request preparation,
  dispatch behavior, and all task/status/checkpoint semantics while completing
  the last obvious crate-internal surface reduction wave around the chapter
  generation follow-up.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  No new visibility warnings remained after this pass.
  Updated stop-rule assessment: the remaining Phase 5 opportunities now look
  exhausted enough that further work would mainly reshuffle true behavior
  owners (`runtime_state`, `task_command`, status semantics) instead of
  shrinking accidental boundaries. Unless a newly discovered isolated private
  helper appears, this follow-up should treat Phase 5 as a stage-complete
  refactor checkpoint and avoid additional mechanical boundary churn.
- 2026-05-20 16:47 +08:00 checkpoint:
  this Phase 5 slice completed one last narrow visibility-tightening pass in
  `chapter_batch_generation_runtime_state_service.rs`, after a fresh audit
  confirmed that the remaining exported runtime helpers were still consumed
  only inside the Rust backend crate. The snapshot loader
  `load_batch_generation_snapshot()` and the runtime entrypoints for single-
  chapter and batch execution now use `pub(crate)` visibility, matching their
  real ownership as crate-internal runtime boundaries shared only with the
  adjacent dispatch, task-command, and read-side owners. The remaining
  persistence and runtime-progression helpers that are not consumed outside the
  file, including the snapshot upsert helper plus the generic runtime
  checkpoint builders, now stay file-private. The batch/single resume and
  pending-checkpoint helpers remain crate-internal because they are still
  intentionally shared with adjacent owner modules and focused tests. This
  move preserves task creation, runtime execution, resume behavior, checkpoint
  payload fields, snapshot reset semantics, and SSE/read-side behavior while
  making the runtime-state owner boundary more honest about which helpers are
  truly reusable contracts versus internal implementation detail.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"` ,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this was the last clearly isolated
  crate-internal seam discovered by the current audit. After this point, most
  remaining unsimplified surfaces sit directly inside behavior-sensitive
  runtime/task/status owners or route transport wiring whose remaining code is
  largely explicit orchestration rather than accidental surface area. Phase 5
  should therefore be treated as effectively stage-complete, and further
  acceleration should shift toward planning or opening the next Rust migration
  phase instead of continuing mechanical boundary-tightening for its own sake.
- 2026-05-20 16:52 +08:00 checkpoint:
  this Phase 5 slice completed one more narrow read-side visibility pass after
  a local file-system audit replaced an earlier stale semantic-search lead.
  In `chapter_batch_generation_status_payload_adapter_service.rs`, the shared
  read-side helpers `to_iso()`, `checkpoint_with_runtime_metadata()`,
  `task_status_payload()`, and `active_task_payload()` are now `pub(crate)`,
  matching their real usage as crate-internal payload adapters consumed only by
  the adjacent query/view/task-command owners. In
  `chapter_batch_generation_status_semantics_service.rs`, the shared read-side
  helpers `active_batch_generation_statuses()`, `task_type()`,
  `task_stage_code()`, and `task_execution_mode()` are also now `pub(crate)`,
  reflecting that no external module boundary consumes them outside the Rust
  backend crate. During validation, this tightening surfaced one honest cleanup:
  `is_active_batch_generation_status()` was no longer used outside tests, so it
  was removed entirely and the semantics tests were adjusted to assert against
  `active_batch_generation_statuses()` directly. These moves preserve active-
  task query payloads, status payload assembly, read-side metadata adaptation,
  status vocabulary, execution-mode defaults, and all route/SSE/task/checkpoint
  semantics while shrinking one more small set of accidental read-side export
  surfaces.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_semantics_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this confirms that only a very small number of
  isolated crate-internal read-side seams were still available after the prior
  runtime-state pass. Phase 5 can still accept similarly narrow helper/
  visibility cleanups if they are immediately obvious and independently
  verifiable, but the remaining surface area is now overwhelmingly concentrated
  in true behavior owners or explicit transport orchestration. Further
  acceleration should therefore bias toward planning the next Rust migration
  phase instead of repeatedly searching for ever-smaller mechanical boundary
  churn.
- 2026-05-20 16:56 +08:00 checkpoint:
  this Phase 5 slice completed one more narrow owner-boundary tightening pass
  in `chapter_batch_generation_create_service.rs`. A local usage audit
  confirmed that the create-request preparation owner still exposed a small
  cluster of request-preparation helpers and carriers more broadly than their
  actual usage required. The create-request error type
  `PrepareBatchGenerationCreateRequestError`, the prepared carrier
  `PreparedBatchGenerationCreateRequest`, and the workflow-facing entrypoint
  `prepare_batch_generation_create_request()` are now crate-internal, matching
  the fact that they are only consumed by the adjacent batch-create workflow
  owner. The count validation helper, target-word normalization helper, and
  chapter-range loader now stay file-private because they are only reused
  inside the create-request owner and its focused tests. These moves preserve
  batch-create validation semantics, normalized target-word defaults, chapter
  range loading behavior, workflow dispatch behavior, HTTP payloads, and all
  task/checkpoint/runtime semantics while shrinking one more isolated create-
  request export surface that no longer needs module-wide visibility.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this confirms that the remaining viable Phase 5
  slices are now limited to small owner-boundary cleanups in request/query/
  adapter lanes where usage scope is immediately provable and validation is
  cheap. The broad direction remains unchanged: if the next candidate touches
  runtime-write semantics, transport contracts, or task-status behavior instead
  of an isolated helper/export surface, that work should be treated as the next
  migration phase rather than more Phase 5 mechanical tightening.
- 2026-05-20 16:59 +08:00 checkpoint:
  this Phase 5 slice completed one more crate-internal query/access boundary
  tightening pass in `chapter_batch_generation_owned_task_query_service.rs`.
  A local usage audit confirmed that the shared owned-task error type
  `LoadOwnedBatchGenerationTaskError`, the optional and required owned-task
  loaders `load_owned_task()` and `load_required_owned_task()`, and the generic
  mapper `map_owned_batch_generation_task_error()` are only consumed by
  adjacent batch-generation owners inside the Rust backend crate, including the
  cancel, resume, status-view, and status-stream lanes. These shared query/
  access helpers are now `pub(crate)`, matching their real ownership as
  crate-internal support boundaries rather than public module exports. This
  move preserves owned-task lookup behavior, access control semantics, not-
  found/internal error mapping, status-stream access checks, and all route/
  status/task semantics while shrinking one more isolated shared helper surface
  that no longer needs full module visibility.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_owned_task_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this reinforces the current Phase 5 shape: the
  only remaining credible slices are narrow shared helper/export tightenings in
  query/access/adapter owners where usage scope is immediately provable and
  verification is cheap. If the next candidate cannot be justified at that
  level of isolation, the work should stop and roll into the next Rust
  migration phase rather than continue searching for mechanical churn.
- 2026-05-20 17:02 +08:00 checkpoint:
  this Phase 5 slice completed one final ultra-narrow stream-boundary
  tightening pass in `chapter_batch_generation_status_stream_service.rs`.
  A local usage audit confirmed that the type alias
  `BatchGenerationStatusStream` is only used inside the Rust backend crate as
  the shared concrete stream return type for the batch-generation status-stream
  owner and its adjacent route-facing constructor. The alias is now
  `pub(crate)`, matching its real role as crate-internal transport plumbing
  rather than a public module export. This move preserves the concrete stream
  type, SSE event construction, terminal-event behavior, access checks, and all
  route/status semantics while shrinking one more tiny export surface whose
  visibility exceeded its actual usage.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this confirms that Phase 5 has effectively
  reached the end of the “cheap, obvious, behavior-preserving export tightening”
  lane. Further continuation should only proceed if another similarly isolated
  seam is immediately visible from local usage evidence; otherwise the correct
  next move is to stop searching for mechanical churn and switch to the next
  Rust migration phase.
- 2026-05-20 17:07 +08:00 checkpoint:
  this Phase 5 slice completed a final module-boundary alignment pass across
  the batch-generation and adjacent single-generation owners. After the prior
  helper/type-alias tightening waves, a local module-usage audit confirmed that
  the corresponding owner modules in `backend-rs/src/services/mod.rs` are also
  only consumed inside the Rust backend crate. The batch-generation support
  modules (`chapter_batch_generation_*`) plus the adjacent
  `chapter_single_generation_*` workflow/request/stream modules are now
  declared as `pub(crate) mod`, aligning the module boundary with the already
  crate-internal functions, carriers, and helpers they expose. On the API side,
  `chapter_batch_generation` and `chapter_batch_generation_error_mapper` in
  `backend-rs/src/api/mod.rs` are now also `pub(crate) mod`, matching their
  real usage as crate-internal route and transport-mapping owners that are only
  merged by neighboring API modules inside the same binary crate. These moves
  preserve all route registration, workflow dispatch, request preparation,
  stream behavior, payload mapping, and task/checkpoint/runtime semantics while
  bringing the owner-module declarations into line with the crate-internal
  boundary model established by the earlier Phase 5 slices.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this completes the highest-signal remaining
  crate-internal boundary alignment work that could be justified without
  touching behavior-sensitive owners. Phase 5 should now be treated as
  effectively exhausted for mechanical Rust seam tightening. Further work
  should move to the next Rust migration phase unless a newly introduced,
  immediately obvious isolated seam appears during unrelated edits.
- 2026-05-20 17:10 +08:00 checkpoint:
  this Phase 5 slice completed a final route-owner visibility alignment pass
  across the chapter route composition stack. A local route-usage audit
  confirmed that the route entrypoints `routes()` in
  `chapter_batch_generation.rs`, `chapters.rs`, `chapter_analysis_routes.rs`,
  `chapter_crud_routes.rs`, and `chapter_regeneration_routes.rs` are only
  consumed by neighboring API modules inside the Rust backend crate
  (`chapters.rs` and `router.rs`) and do not serve as external library-facing
  entrypoints. These route-owner constructors are now `pub(crate)`, aligning
  the route composition layer with the earlier helper/type/module boundary
  tightening waves. This move preserves route registration order, route merge
  behavior, batch-generation endpoints, chapter route aggregation, stream and
  background route exposure, and all HTTP/SSE/task semantics while removing the
  last obvious overexposed route-owner surfaces in the current Phase 5 scope.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test router --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this should be treated as the formal end of the
  current Phase 5 mechanical boundary-tightening track. The remaining work now
  sits in behavior-sensitive runtime/task/status owners or in genuine Rust
  migration concerns, not in accidental export surface area. Further
  acceleration should therefore switch to the next migration phase rather than
  continue searching for additional Phase 5 seams.
- 2026-05-20 17:13 +08:00 checkpoint:
  this Phase 5 slice completed a final API module-boundary cleanup across the
  remaining chapter route/error-mapper owners in `backend-rs/src/api/mod.rs`.
  A local usage audit confirmed that the chapter-analysis, chapter-crud,
  chapter-regeneration, and shared `chapters` / `chapters_error_mapper` modules
  are only consumed by neighboring API modules inside the same Rust binary
  crate and do not form public library-facing boundaries. These chapter-related
  API modules are now declared as `pub(crate) mod`, aligning the API module
  declarations with the earlier crate-internal function/type/module/route-owner
  tightening work already completed in this follow-up. This move preserves
  chapter route aggregation, route wiring, error mapping semantics, analysis/
  CRUD/regeneration endpoint exposure, and all HTTP/SSE/task semantics while
  removing the last obvious overexposed API-module surfaces in the current
  Phase 5 scope.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test router --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this should be treated as the final meaningful
  Phase 5 boundary-alignment slice on the Rust chapter-generation path. The
  accidental export-surface cleanup track is now exhausted enough that further
  acceleration should move directly into the next Rust migration phase instead
  of continuing to search for more mechanical tightening opportunities.
- 2026-05-20 17:16 +08:00 checkpoint:
  this Phase 5 slice completed the crate-root boundary alignment pass in
  `backend-rs/src/main.rs`. Because the Rust backend is currently built as a
  binary crate with `main.rs` and no public `lib.rs` boundary, the top-level
  declarations `pub mod ai`, `pub mod api`, `pub mod config`, `pub mod db`,
  `pub mod mcp`, `pub mod middleware`, `pub mod models`, `pub mod services`,
  `pub mod tasks`, and `pub mod utils` were broader than needed. They are now
  private `mod` declarations, which aligns the crate root with the earlier
  crate-internal helper/type/module/route-owner cleanup work already completed
  across the chapter-generation path. This move preserves all runtime startup,
  router build, background task bootstrap, and chapter-generation behavior
  while removing the final crate-root export surface that was still implying a
  wider library boundary than the current binary actually provides.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test router --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  This pass also surfaced a set of pre-existing unused-import / dead-code
  warnings that had been masked by the previous top-level public module shape;
  these warnings were not introduced by behavioral refactor logic and should be
  treated as separate cleanup work rather than part of the chapter-generation
  seam-tightening scope. Updated stop-rule assessment: Phase 5 mechanical
  boundary tightening is now fully exhausted. Further work should switch to the
  next Rust migration phase or to an explicit repository-wide warning cleanup
  effort, not continue searching for more chapter-generation export seams.
- 2026-05-20 17:21 +08:00 checkpoint:
  this final Phase 5 cleanup slice removed the last directly adjacent
  chapter-generation dead code that was surfaced during the crate-root
  visibility pass. A local usage audit confirmed that
  `backend-rs/src/services/chapter_access_http_service.rs` was completely
  unreferenced, so the module was removed from `services/mod.rs` and the file
  was deleted. In `chapter_access_service.rs`, the unused helper
  `check_accessible_chapter_exists()` was removed because no route or service
  consumes that boolean wrapper. In `chapter_generation_prompt_service.rs`, the
  no-longer-needed wrapper `build_prompt()` was removed and the focused tests
  now assert directly through `build_prompt_with_provider_payload()` plus the
  placeholder provider payload helper. These moves preserve chapter access
  semantics, prompt construction behavior, batch/single generation flow, and
  all route/runtime behavior while finishing the last clearly provable
  chapter-generation dead-code cleanup that still fit the current Phase 5
  scope.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_prompt_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test router --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: with the crate-root boundary pass and this last
  chapter-generation dead-code cleanup complete, the current Phase 5 follow-up
  should be treated as fully exhausted. The remaining warnings are now broader
  repository hygiene concerns, not chapter-generation seam work. Further
  acceleration should move to the next Rust migration phase or to an explicit
  repo-wide warning cleanup task, not continue mining this task for more Phase
  5 slices.
- 2026-05-20 17:36 +08:00 checkpoint:
  this slice is the first post-Phase-5 Rust migration step rather than another
  mechanical boundary-tightening pass. A local ownership audit showed that
  `chapter_batch_generation_task_command_service.rs` was still mixing task
  persistence / execution-plan semantics with command response payload
  adaptation for create, cancel, resume, and single-chapter background create
  flows. That response-shaping logic now lives in the new
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  while `chapter_batch_generation_task_command_service.rs` keeps task record
  writes, resume gating, chapter-id parsing, and runtime snapshot reset
  behavior. This preserves all existing HTTP payload fields, task/checkpoint
  defaults, resume execution branching, and single/batch runtime dispatch
  semantics while making the command owner boundary closer to “write model +
  execution plan” and the payload owner boundary closer to “response
  adaptation”.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_command_payload_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this confirms there is still productive work
  available after the Phase 5 seam-tightening track, but it now sits in
  behavior-preserving ownership clarification inside true command/runtime/query
  owners rather than in visibility churn. The next slice should continue in
  that direction, preferably by tightening one more internal semantic boundary
  around resume/runtime-state or status-view ownership without changing HTTP,
  SSE, or task-checkpoint contracts.
- 2026-05-20 18:02 +08:00 checkpoint:
  this slice continued the post-Phase-5 Rust migration work by moving the
  resume-time task reset semantics deeper into
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`.
  A local ownership audit showed that
  `chapter_batch_generation_task_command_service.rs` was still directly owning
  the `ActiveModel` reset for resume (`pending` status, cleared error state,
  cleared failed chapter list, zeroed retry/progress counters, and
  single-vs-batch current chapter position reset) even though the adjacent
  runtime-state owner already owned the corresponding resume checkpoint and
  snapshot-replacement semantics. The new
  `reset_batch_generation_task_for_resume()` runtime-state helper now owns that
  write-side reset plus the follow-up snapshot replacement, while
  `prepare_batch_generation_resume()` keeps only resume eligibility checks and
  execution-plan selection. This preserves resume response payloads, task type
  branching, pending checkpoint defaults, snapshot clearing behavior, and all
  HTTP/SSE/task contracts while tightening one more real owner boundary in the
  resume path.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"` ,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: this confirms the remaining productive slices
  are still available, but they are now concentrated in true behavior owners
  such as status-view/query assembly and runtime/write-side helpers rather than
  broad command surfaces. The next slice should likely target one more
  internal semantic boundary in `status_view` or an adjacent query owner,
  unless a separate repo-wide warning cleanup task is started explicitly.
- 2026-05-20 18:07 +08:00 checkpoint:
  this slice continued the post-Phase-5 Rust migration work on the read side
  by moving the batch-generation status/active/list response assembly back into
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`.
  A local ownership audit showed that
  `chapter_batch_generation_status_query_service.rs`,
  `chapter_batch_generation_active_query_service.rs`, and
  `chapter_batch_generation_active_list_query_service.rs` were still each
  carrying small amounts of view-payload assembly even though
  `status_view_service` already owned the task view context and adjacent
  read-side semantics. The status-view owner now exposes
  `build_batch_generation_status_query_response()`,
  `build_active_batch_generation_query_response()`, and
  `build_active_batch_generation_task_list_query_response()`, while the query
  owners keep access checks, context loading, limit normalization, and error
  mapping only. This preserves all existing payload fields, stage/checkpoint
  metadata, quality metrics exposure, and active-task wrapper shapes while
  making the query layer closer to pure orchestration and the view owner closer
  to the single read-side response assembly boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_list_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: the remaining productive seams are now even
  more concentrated in the true behavior owners. A sensible next slice would
  be either another narrow read-side normalization inside `status_view` /
  stream semantics, or an explicit separate task for repo-wide warnings if the
  goal shifts from refactor ownership to hygiene.
- 2026-05-20 18:11 +08:00 checkpoint:
  this slice continued the read-side ownership tightening by moving the
  batch-generation stream event payload assembly into
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  alongside the already-owned stream-state projection semantics. A local audit
  showed that `chapter_batch_generation_status_stream_service.rs` still owned
  the JSON event builders for progress, terminal result, failure, cancelled,
  not-found, and timeout events even though those events were derived entirely
  from `BatchGenerationStreamState`, which is already projected by the
  neighboring status-view owner. The status-view owner now exposes
  `build_batch_generation_progress_event()`,
  `build_batch_generation_result_event()`,
  `build_batch_generation_failed_event()`,
  `build_batch_generation_cancelled_event()`,
  `build_batch_generation_not_found_event()`,
  `build_batch_generation_timeout_event()`, and
  `build_batch_generation_terminal_events()`, while
  `chapter_batch_generation_status_stream_service.rs` keeps only stream-access
  checks, change detection cursor logic, polling cadence, and SSE transport
  sending. This preserves event types, payload fields, timeout/not-found/error
  codes, terminal event ordering, and all SSE transport behavior while making
  the stream owner closer to pure polling/transport orchestration and the
  status-view owner closer to the single read-side event assembly boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`.
  Updated stop-rule assessment: the remaining seams are now increasingly
  narrow and concentrated in the deepest behavior owners. Further acceleration
  should either continue with another similarly small owner clarification on
  the batch-generation path, or deliberately switch to a separate repository
  hygiene task if the main bottleneck becomes warning noise rather than
  ownership ambiguity.
- 2026-05-20 18:20 +08:00 checkpoint:
  this slice continued the post-Phase-5 Rust owner clarification work on the
  write side by moving the batch-cancel terminal task write fully into
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`.
  A local audit showed that
  `chapter_batch_generation_task_command_service.rs` still directly mutated the
  task row to `cancelled` and set `completed_at` before delegating adjacent
  cancelled checkpoint persistence to the runtime-state owner. That split left
  the cancel path with mixed ownership: command-level eligibility plus partial
  terminal state write in one file, and snapshot finalization in another. The
  runtime-state owner now sets the cancelled task status, writes
  `completed_at`, and builds the cancelled runtime checkpoint through the new
  shared `build_cancelled_batch_generation_runtime_checkpoint()` helper, while
  `cancel_batch_generation_task()` keeps only cancel eligibility checks and
  response payload return. This preserves cancel response payload fields,
  cancelled checkpoint shape, terminal progress semantics, runtime-loop
  cancelled handling, and all HTTP/SSE/task contracts while making the cancel
  write path closer to a single runtime-state owner boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_cancel_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: productive seams still exist, but they are now
  extremely narrow and should only continue where one owner still clearly mixes
  command/query/runtime responsibility. The next likely slice is either one
  more write-side clarification around batch failure/finalization semantics, or
  an explicit decision to stop this refactor lane and switch to a separate Rust
  migration or repository-hygiene task.
- 2026-05-20 18:23 +08:00 checkpoint:
  this slice continued the post-Phase-5 Rust owner clarification work on batch
  failure finalization by moving checkpoint-facing failure message selection
  fully into
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`.
  A local audit showed that `execute_batch_generation_runtime()` still chose
  terminal checkpoint copy at each failure callsite even though the adjacent
  runtime-state owner already owned the failed task write, failed checkpoint
  persistence, and terminal progress semantics. The runtime-state owner now
  exposes `BatchGenerationFailureKind` plus the shared
  `checkpoint_message_for_batch_generation_failure()` helper, and
  `finalize_batch_generation_failure()` now accepts a typed failure kind
  instead of a route/runtime-loop-provided checkpoint message string. The
  runtime loop keeps only source-specific task error text (`Chapter not found`
  / DB load error / generation error) plus chapter position context, while the
  runtime-state owner decides the stable checkpoint-facing message for missing
  chapter, chapter load failure, and generation failure terminal states. This
  preserves task `error_message`, failed checkpoint shape, SSE/read-side
  terminal behavior, and all HTTP/task contracts while making batch failure
  finalization closer to a single runtime-state owner boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_cancel_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: remaining seams on this path are now very close
  to diminishing returns. Continue only if another slice can still collapse a
  clearly duplicated behavior owner boundary without touching response fields,
  SSE timing, or task/checkpoint defaults; otherwise switch to a separate Rust
  migration slice or explicit repository-hygiene task.
- 2026-05-20 18:42 +08:00 checkpoint:
  this slice tightened the batch success finalization boundary by collapsing
  the duplicated “is this terminal completion or still-running progress” logic
  into one runtime-state plan. A local audit showed that
  `finalize_batch_generation_success()` still decided terminal-vs-running task
  state twice: once while writing the task row (`completed` vs `running`,
  whether `completed_at` should be set) and again in
  `resolve_batch_generation_success_checkpoint()` while choosing checkpoint
  phase/progress/status/event/message. The runtime-state owner now lets
  `BatchGenerationSuccessCheckpointPlan` carry both task-facing terminal
  semantics (`task_status`, `should_complete_task`) and checkpoint-facing
  semantics, and `finalize_batch_generation_success()` consumes that single
  plan instead of re-deriving completion state locally. This preserves
  completed/running task status, `completed_at` write timing, checkpoint
  progress/event/message semantics, SSE terminal behavior, and all HTTP/task
  contracts while removing one more behavior-sensitive duplicate decision table
  from the runtime loop boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the remaining `chapter_batch_generation`
  ownership seams are now extremely close to exhaustion. Further work on this
  task should proceed only if another slice still removes a concrete duplicated
  behavior decision across command/query/runtime owners; otherwise this lane
  should stop and the next acceleration step should move to a separate Rust
  migration target or an explicit hygiene task.
- 2026-05-20 18:46 +08:00 checkpoint:
  this slice deliberately switched from the nearly exhausted
  `chapter_batch_generation` runtime lane to the still-valid secondary seam
  around single-chapter request ownership. A local audit confirmed that the
  route itself was already thin, but both
  `chapter_single_generation_stream_workflow_service.rs` and
  `chapter_single_generation_background_workflow_service.rs` were still each
  reconstructing the same internal `SingleChapterGenerationRequest` from loose
  route/workflow parameters (`target_word_count`, `model`, compat-only
  `enable_analysis`) before delegating to
  `prepare_single_chapter_generation_request()`. The request owner now exposes
  `build_single_chapter_generation_request()` in
  `backend-rs/src/services/chapter_single_generation_request_service.rs`, and
  both workflows now delegate that internal request shaping to the request
  service owner instead of open-coding it locally. This preserves route payload
  fields, target-word-count normalization, provider-payload preparation,
  single-chapter stream/background workflow behavior, and all HTTP/SSE/task
  contracts while reducing one more duplicated request-shaping seam on the
  single-chapter path.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `chapter_batch_generation` itself is now close
  enough to exhaustion that further acceleration should come from adjacent
  low-risk seams like single-chapter request/workflow ownership, unless a new
  duplicated behavior decision is discovered in the batch runtime lane.
- 2026-05-20 18:49 +08:00 checkpoint:
  this slice completed the next step of the single-chapter request ownership
  lane by closing the internal request contract all the way from route to
  workflow. After the previous slice, the request owner already knew how to
  build a `SingleChapterGenerationRequest`, but
  `backend-rs/src/api/chapter_batch_generation.rs` still passed loose internal
  fields (`target_word_count`, `model`, `enable_analysis`) directly into both
  single-chapter workflows, and each workflow still encoded that loose-params
  contract in its own function signature. The route now builds the internal
  request through
  `backend-rs/src/services/chapter_single_generation_request_service.rs`, and
  both
  `chapter_single_generation_stream_workflow_service.rs` and
  `chapter_single_generation_background_workflow_service.rs` now consume one
  `SingleChapterGenerationRequest` value instead of three separate params. This
  preserves route payload fields, request normalization, stream/background
  behavior, and all HTTP/SSE/task contracts while making the internal
  single-chapter contract owner explicit across route → workflow →
  request-preparation boundaries.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the best remaining acceleration on this task is
  now more likely to come from adjacent single-chapter request/workflow/stream
  seams than from squeezing one more micro-slice out of
  `chapter_batch_generation`, unless a fresh duplicated behavior decision is
  discovered there.
- 2026-05-20 19:55 +08:00 checkpoint:
  this slice clarified the single-chapter request contract by splitting the
  compat-only `enable_analysis` flag away from the real internal generation
  request. A local audit showed that
  `backend-rs/src/services/chapter_single_generation_request_service.rs`
  already owned the normalized internal request shape, but the request
  contract still exposed `enable_analysis` even though that field no longer
  participated in any internal request-preparation or execution decision. The
  request owner now keeps `SingleChapterGenerationRequest` minimal
  (`target_word_count`, `model`) and exposes
  `SingleChapterGenerationRequestCompatFields` plus
  `consume_single_chapter_generation_request_compat_fields()` for the
  compatibility-only `enable_analysis` field. The route explicitly consumes
  that compat field before building the real internal request, and both
  single-chapter workflows now operate only on the minimal internal contract.
  This preserves request compatibility, route payload fields, target-word-count
  normalization, stream/background behavior, and all HTTP/SSE/task contracts
  while making the difference between compat-only transport fields and real
  internal request semantics explicit.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
- 2026-05-20 19:55 +08:00 checkpoint:
  this slice continued the single-chapter owner-clarification lane by
  collapsing the scattered execution-input contract into one explicit internal
  owner. A local audit showed that
  `chapter_single_generation_background_workflow_service.rs`,
  `chapter_single_generation_stream_workflow_service.rs`,
  `chapter_single_generation_stream_service.rs`,
  `chapter_batch_generation_dispatch_service.rs`, and
  `chapter_batch_generation_runtime_state_service.rs` were still passing the
  same prepared execution fields (`chapter_id`, `target_word_count`,
  `ai_config`, `provider_payload`) as loose parameters across each boundary
  even though they all came from one prepared single-chapter request. The
  request owner now exposes `SingleChapterGenerationExecutionInput`, the
  prepared request carries that execution input as one owned value, and the
  stream builder, background dispatch, and runtime executor all consume that
  explicit contract instead of re-spelling the same field bundle in separate
  signatures. This preserves route payloads, SSE payload shape, task creation
  behavior, runtime generation semantics, provider-payload ownership, and all
  HTTP/SSE/task contracts while making single-chapter execution input ownership
  explicit across request preparation → workflow → dispatch/stream → runtime.
  Validation pending for this slice in the current session; run targeted
  `chapter_single_generation_*` tests plus `cargo check` before taking another
  seam.
- 2026-05-20 20:22 +08:00 checkpoint:
  this slice tightened the adjacent single-chapter background task-plan seam by
  removing one remaining fake owner hop around `target_word_count`. A local
  audit showed that
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`
  still returned `target_word_count` from
  `SingleGenerationBackgroundCreatePlan`, and
  `chapter_single_generation_background_workflow_service.rs` immediately wrote
  that same value back into the already-owned
  `SingleChapterGenerationExecutionInput`. No task-plan owner decision was
  happening there: the normalized target word count was already decided by the
  prepared request owner and then persisted into the task row, so returning it
  again from the task-plan contract only preserved a redundant internal seam.
  `SingleGenerationBackgroundCreatePlan` now returns only `created_task_id`
  plus `response_payload`, and the background workflow forwards the prepared
  execution input directly to dispatch after task creation. This preserves task
  creation payloads, persisted target word count, execution input semantics,
  runtime behavior, and all HTTP/SSE/task contracts while making the task-plan
  boundary closer to pure task-record creation plus compat response shaping.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the remaining productive seams on this
  single-chapter lane are getting narrow. Continue only if the next slice still
  removes a concrete duplicated owner decision rather than just shortening
  parameter lists; otherwise stop this follow-up lane and switch to a new Rust
  migration target.
- 2026-05-20 20:26 +08:00 checkpoint:
  this slice tightened the last obvious route-adjacent request seam on the
  single-chapter path by moving the combined “consume compat-only field, then
  build the real internal request” step fully into the request owner. A local
  audit showed that
  `backend-rs/src/api/chapter_batch_generation.rs` still repeated the same
  two-step handler logic in both `generate_chapter_content_background()` and
  `generate_chapter_content_stream()`: first explicitly consume the
  compat-only `enable_analysis` field, then build the minimal
  `SingleChapterGenerationRequest`. That was already a shared request-owner
  semantic, but the route was still spelling it twice. The request owner now
  exposes `build_single_chapter_generation_request_with_compat_fields()` in
  `backend-rs/src/services/chapter_single_generation_request_service.rs`, and
  both single-chapter route handlers now delegate that combined compat/request
  shaping step to the service instead of open-coding it in transport code.
  This preserves request compatibility, route payloads, stream/background
  workflow behavior, and all HTTP/SSE/task contracts while making the route
  closer to pure transport wiring.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_background_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this likely exhausts the productive
  single-chapter request/workflow lane for this follow-up task. Continue only
  if a future audit finds another concrete duplicated owner decision; otherwise
  stop this lane and move to a new Rust migration target instead of continuing
  route-local mechanical cleanup.
- 2026-05-20 20:29 +08:00 checkpoint:
  this slice deliberately switched away from the now-nearly-exhausted
  single-chapter request lane and reopened a productive batch-generation seam
  on the SSE read path. A local audit showed that
  `backend-rs/src/api/chapter_batch_generation.rs` had already adopted the
  route -> workflow pattern for single-chapter stream/background generation and
  batch create/resume paths, but the batch status stream endpoint still called
  `chapter_batch_generation_status_stream_service.rs` directly even though that
  service owned two distinct responsibilities: stream access/orchestration and
  the lower-level polling/SSE transport loop. The new
  `backend-rs/src/services/chapter_batch_generation_status_stream_workflow_service.rs`
  now owns the route-adjacent stream workflow boundary, performing access
  validation before delegating to the existing polling/transport builder, while
  `chapter_batch_generation_status_stream_service.rs` keeps the owned access
  helper plus SSE polling/event transport primitives. This preserves status
  stream endpoint behavior, SSE event payloads, terminal event ordering,
  timeout/not-found handling, and all HTTP/SSE/task contracts while restoring a
  consistent route -> workflow -> transport layering on the batch stream path.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this confirms there are still a few
  behavior-preserving `chapter_batch_generation` owner seams left when they
  re-establish an existing layering pattern instead of just shortening
  parameter lists. The next slice should still satisfy the same bar: concrete
  owner clarification with stable HTTP/SSE/task behavior, or else stop.
- 2026-05-20 20:33 +08:00 checkpoint:
  this slice continued the reopened `chapter_batch_generation` read-side lane
  by restoring the same route -> workflow -> query layering for the active-task
  query path. A local audit showed that the batch status stream endpoint now
  had a dedicated workflow owner, but the adjacent active-task endpoint still
  called `chapter_batch_generation_active_query_service.rs` directly even
  though that query service mixed two responsibilities: project access gating
  and owned read-side query assembly. The new
  `backend-rs/src/services/chapter_batch_generation_active_query_workflow_service.rs`
  now owns the route-adjacent workflow boundary for active-task lookup,
  performing project access validation before delegating to the existing
  read-side query service, while
  `chapter_batch_generation_active_query_service.rs` keeps only the owned
  view-context load plus payload assembly. This preserves active-task endpoint
  payloads, not-found/access-denied behavior, and all HTTP/task contracts while
  making the active query path structurally consistent with the already-owned
  batch status stream path.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the `chapter_batch_generation` read-side lane
  still has value when it is used to restore consistent owner layering across
  sibling endpoints. Keep going only while each slice removes a concrete mixed
  responsibility boundary; if the next move is merely cosmetic, stop.
- 2026-05-20 20:42 +08:00 checkpoint:
  this slice tightened one remaining mixed-responsibility seam inside the
  reopened `chapter_batch_generation` read-side lane by separating stream-event
  ownership from the HTTP/status view owner. A local audit showed that
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  still owned two adjacent but distinct responsibilities: read-side task view
  context loading / status payload assembly, and batch status-stream event /
  terminal-event payload construction. That meant the same service sat across
  both the query/read boundary and the SSE transport-facing event boundary even
  after the route -> workflow layering had been restored. The new
  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  now owns `BatchGenerationStreamState` plus the progress / result / failed /
  timeout / terminal-event builders, while
  `chapter_batch_generation_status_view_service.rs` keeps the owned task-view
  context loading, read-side payload assembly, and stream-state semantics
  resolution only. `chapter_batch_generation_status_stream_service.rs` now
  depends on the dedicated stream-event owner for SSE payload emission without
  changing polling cadence, event ordering, timeout behavior, or payload
  fields. This preserves status stream behavior, terminal event sequencing,
  status query payloads, and all HTTP/SSE/task contracts while making the
  stream transport payload owner explicit and leaving the view service closer
  to a pure read-side query/state adapter.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_view_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_event_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this likely exhausts the productive
  `chapter_batch_generation` read-side lane for this follow-up task. The next
  move should switch to a new low-risk Rust migration target unless a fresh
  audit finds another real mixed owner boundary that changes more than file
  placement.
- 2026-05-20 20:46 +08:00 checkpoint:
  this slice switched away from the now-nearly-exhausted batch read-side lane
  and reopened the adjacent route-adjacent request-owner lane on batch create.
  A local audit showed that
  `backend-rs/src/api/chapter_batch_generation.rs` still passed the batch
  create workflow contract as a long list of loose internal params
  (`start_chapter_number`, `count`, `style_id`, `target_word_count`,
  `enable_analysis`, `max_retries`, `model`, plus compat fields) directly into
  `chapter_batch_generation_create_workflow_service.rs` even though that
  service already owned the workflow preparation, access check, normalization,
  task-plan creation, and runtime dispatch boundary. The workflow owner now
  exposes `BatchGenerationCreateWorkflowRequest`, and the route delegates the
  batch create internal contract as one explicit request object instead of
  re-spelling the workflow signature at the transport boundary. This preserves
  batch-create request payload fields, compat-field passthrough,
  enable-analysis/max-retries normalization, provider-config preparation,
  task-plan creation, runtime dispatch behavior, and all HTTP/task contracts
  while making the batch create internal request owner explicit in the same
  way the single-chapter workflow lanes were already tightened earlier.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: there is still some value in the route-adjacent
  request/workflow lane when a slice removes a duplicated internal contract
  rather than just shortening code. Continue only if the next move closes a
  similarly concrete owner boundary; otherwise stop and switch to a different
  Rust migration target.
- 2026-05-20 20:50 +08:00 checkpoint:
  this slice continued the reopened batch-create request-owner lane by
  separating compat-only transport fields away from the real batch create
  workflow contract. A local audit showed that after the previous slice,
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`
  already owned an explicit `BatchGenerationCreateWorkflowRequest`, but that
  internal request contract still carried
  `BatchGenerationRequestCompatFields` even though those compat fields were not
  used by any create-workflow preparation, normalization, task-plan creation,
  or runtime dispatch decision. The compat owner now lives in
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`:
  it exposes the minimal real `BatchGenerationCreateWorkflowRequest`, plus
  `consume_batch_generation_request_compat_fields()` and
  `build_batch_generation_create_workflow_request_with_compat_fields()` so the
  route can continue accepting the old transport payload while the workflow
  consumes only the fields that actually participate in internal behavior. This
  preserves batch-create request compatibility, compat-field passthrough,
  enable-analysis/max-retries normalization, provider-config preparation,
  task-plan creation, runtime dispatch behavior, and all HTTP/task contracts
  while making batch-create compat-only transport fields explicit in the same
  way the single-chapter lane already treats its compat-only request fields.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the batch-create request/workflow lane still
  has some value while it is removing concrete fake owner hops around internal
  request semantics. Continue only if the next slice still removes a real
  duplicated owner decision; otherwise stop and switch to a different Rust
  migration target.
- 2026-05-20 20:53 +08:00 checkpoint:
  this slice continued the same batch-create request-owner lane by moving the
  remaining internal default-value semantics fully into the request/compat
  owner. A local audit showed that after the previous slice,
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  already owned the difference between compat-only transport fields and the
  real batch create workflow request, but the real
  `BatchGenerationCreateWorkflowRequest` still carried
  `enable_analysis: Option<bool>` and `max_retries: Option<i32>`, leaving
  `chapter_batch_generation_create_workflow_service.rs` to normalize those
  defaults (`false`, `3`) locally. Those defaults are not workflow behavior
  orchestration; they are part of the internal request contract shape. The
  request/compat owner now normalizes them when building the real internal
  request, and `BatchGenerationCreateWorkflowRequest` now carries only the
  finalized internal values (`enable_analysis: bool`, `max_retries: i32`).
  This preserves batch-create request compatibility, compat-field passthrough,
  defaulting behavior, task-plan creation, provider-config preparation, runtime
  dispatch behavior, and all HTTP/task contracts while making the batch create
  workflow service closer to a pure consumer of an already-shaped internal
  request contract.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this lane is now close to exhaustion. Continue
  only if the next slice removes another concrete duplicated owner decision in
  batch create/resume request shaping; if the next move is just contract
  grooming or code shortening, stop and switch to a different Rust migration
  target.
- 2026-05-20 21:10 +08:00 checkpoint:
  this slice found one more concrete owner seam adjacent to the batch
  create/resume request lane by collapsing the repeated batch runtime execution
  contract into one explicit internal owner. A local audit showed that
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  `chapter_batch_generation_resume_service.rs`,
  `chapter_batch_generation_dispatch_service.rs`, and
  `chapter_batch_generation_runtime_state_service.rs` were still spelling the
  same execution bundle as loose fields across boundaries:
  `user_id`, `chapter_ids`, `target_word_count`, `ai_config`, and
  `provider_payload`. Those fields all describe one prepared batch runtime
  execution request, just like the single-chapter lane previously converged on
  an explicit execution-input owner. The batch dispatch owner now exposes
  `BatchGenerationExecutionInput`, batch create now packages that execution
  input once before dispatch, batch resume now dispatches the same owner shape
  on the batch branch, and the runtime executor now consumes that explicit
  contract instead of re-spelling the field bundle in its own signature. This
  preserves batch-create task creation, batch-resume behavior, provider-config
  preparation, runtime iteration semantics, checkpoint progression, and all
  HTTP/task contracts while making batch runtime execution input ownership
  explicit across workflow -> dispatch -> runtime boundaries.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the batch create/resume execution-input lane
  still has value only while it removes concrete duplicated owner decisions
  like this one. If the next move cannot eliminate another real shared internal
  contract or fake owner hop, stop and switch to a different Rust migration
  target.
- 2026-05-20 21:14 +08:00 checkpoint:
  this slice continued the now-narrow batch resume lane by removing one
  remaining fake owner hop around execution-input translation. A local audit
  showed that after `BatchGenerationExecutionInput` was introduced,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs` still
  owned two separate responsibilities on the resume path: dispatching prepared
  runtime work, and translating `ResumeExecutionPlan + ai_config +
  provider_payload` into the actual single-chapter or batch execution input
  owners. That translation is resume-workflow semantics, not dispatch
  semantics. `chapter_batch_generation_resume_service.rs` now owns that branch
  translation and calls either `dispatch_single_chapter_resume_runtime()` or
  `dispatch_batch_generation_runtime()` with already-shaped execution input,
  while `chapter_batch_generation_dispatch_service.rs` now keeps only dispatch
  concerns for explicit execution input values. This preserves resume
  eligibility checks, response payloads, provider-config preparation, batch
  vs single runtime behavior, and all HTTP/task contracts while making the
  resume workflow boundary closer to a pure owner of “what execution should
  run” and the dispatch layer closer to a pure owner of “run this prepared
  execution input”.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this likely brings the batch
  create/resume/dispatch lane very close to exhaustion. Continue only if a new
  audit finds another concrete owner seam with real semantic duplication;
  otherwise stop this lane and switch to a different Rust migration target.
- 2026-05-20 21:22 +08:00 checkpoint:
  this slice stopped digging further into the now-near-exhausted batch
  create/resume lane and instead audited adjacent Rust chapter routes for a
  new real owner seam. The highest-signal remaining seam was in
  `backend-rs/src/api/chapter_regeneration_routes.rs`, where the route still
  translated transport DTOs into two different internal service/workflow
  request shapes: partial-regenerate apply input and partial-regenerate stream
  workflow input. That route-local translation duplicated the request-owner
  pattern already tightened in the chapter analysis draft lane, the
  single-chapter generation lane, and the batch create lane. The refactor now
  introduces `backend-rs/src/services/chapter_regeneration_request_service.rs`
  as the explicit owner for those internal request contracts. The route keeps
  transport parsing only, then calls
  `build_apply_partial_regenerate_request()` or
  `build_partial_regeneration_stream_workflow_request()` and forwards the
  result. `chapter_regeneration_apply_service.rs` now consumes the owned apply
  request contract instead of a route-defined borrowed struct, and
  `chapter_regeneration_stream_workflow_service.rs` now consumes the owned
  partial-stream workflow request and reads normalized defaults through owner
  methods instead of re-normalizing route fields locally. This preserves HTTP
  payload shape, SSE behavior, range validation, default `context_chars=500`,
  default `enable_web_research=false`, and all regeneration runtime semantics
  while removing one more fake route owner hop and making request ownership
  consistent with the rest of the Rust chapter refactor.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_apply_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_stream_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the follow-up task still has value only when
  a route or workflow boundary is demonstrably re-owning a shared internal
  request/response contract. If the next audit only finds cosmetic route
  shortening or parameter grooming, stop this task and switch to a new Rust
  migration target.
- 2026-05-20 21:26 +08:00 checkpoint:
  this slice continued the route-boundary audit and found one more valid
  request-owner seam in `backend-rs/src/api/chapter_crud_routes.rs`. Although
  the CRUD lane is lower-risk than generation or batch runtime semantics, the
  route still owned three independent internal contract assemblies:
  chapter-create input, chapter-update input, and expansion-plan update input.
  That meant the transport layer was still spelling the workflow request shape
  directly instead of forwarding an explicit service-owned owner contract, the
  same fake owner hop already removed from single-chapter generation,
  batch-generation create, analysis draft handling, and regeneration routes.
  The refactor now introduces
  `backend-rs/src/services/chapter_crud_request_service.rs` as the internal
  request owner for `CreateChapterRequest`, `UpdateChapterRequest`, and
  `UpdateExpansionPlanRequest`. `chapter_crud_routes.rs` now builds those
  request objects once and forwards them to
  `chapter_crud_workflow_service.rs`, while the workflow service now consumes
  those explicit owner contracts instead of a loose parameter list. This
  preserves HTTP payload shape, response payload shape, access semantics, and
  CRUD persistence behavior while making the route boundary thinner and the
  workflow boundary the single owner of “what a chapter create/update request
  means” inside the Rust backend.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_crud_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: after this move, the remaining chapter-route
  seams are likely to be much lower signal. Continue this follow-up only if
  a new audit finds another concrete shared internal contract or duplicated
  owner decision. If the next move would only replace route-local field
  forwarding with wrappers that add no semantic ownership, stop this task and
  switch to a different Rust migration target.
- 2026-05-20 21:30 +08:00 checkpoint:
  this slice revisited `backend-rs/src/api/chapter_batch_generation.rs` and
  found one last route-level request assembly seam that still had concrete
  owner value. Earlier waves had already moved the internal batch-create
  contract and the single-chapter internal request contract into service-owned
  builders, but the route still owned two duplicated translation patterns:
  (1) spelling the batch compat-only field bundle locally before calling the
  batch request owner, and (2) spelling the same single-chapter compat
  wrapper twice for background and stream generation. The refactor now lets
  the request-owner services own those route-facing assemblies directly:
  `chapter_batch_generation_request_compat_service.rs` now exposes
  `build_batch_generation_create_workflow_request_from_route()`, and
  `chapter_single_generation_request_service.rs` now exposes
  `build_single_chapter_generation_request_from_route()`. The route now only
  forwards transport DTO fields once and no longer reconstructs compat-owner
  structs inline. This preserves batch create behavior, single-chapter
  background and stream behavior, compat field consumption, normalized default
  semantics, and all HTTP/SSE/task contracts while making the route boundary
  closer to pure transport orchestration.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this likely exhausts the remaining
  behavior-preserving chapter-route request-owner seams in the current follow-up.
  Continue only if a new audit finds another explicit shared internal contract
  or semantically duplicated owner decision. If the next move is only route
  wrapper compression or helper indirection without a real ownership change,
  stop this task and switch to a different Rust migration target.
- 2026-05-20 21:32 +08:00 checkpoint:
  a stricter stop-rule audit was run against the remaining “risky files”
  listed for this follow-up:
  `chapter_batch_generation_task_command_service.rs`,
  `chapter_batch_generation_runtime_state_service.rs`,
  `chapter_batch_generation_status_view_service.rs`, and
  `api/chapters.rs`. The result is that this continuation task should stop
  here. The remaining duplicated-looking code is no longer duplicated owner
  policy in the sense used by this refactor:
  `task_command_service.rs` now consumes shared quality semantics through
  `manual_review_label()` and shared queued checkpoint semantics through
  `build_pending_batch_generation_runtime_checkpoint()`;
  `runtime_state_service.rs` is the actual runtime-checkpoint/snapshot owner
  and the remaining helpers there are part of that owner boundary rather than
  fake hops; `status_view_service.rs` already owns the read-side stream/status
  fallback semantics through `resolve_batch_generation_stream_semantics()`;
  and `api/chapters.rs` is only a router merge surface with no business-owner
  duplication left. Any further edits in those files would now be wrapper
  compression, helper shuffling, or style-level deduplication without a real
  ownership change, which violates the follow-up stop rule. Therefore this
  task should be treated as implementation-complete for the current
  chapter-generation seam-tightening scope, and the next engineering step
  should switch to a different Rust migration target rather than continuing to
  mine this lane.
- 2026-05-20 22:05 +08:00 checkpoint:
  before opening a brand-new Phase 5 lane, the already-present uncommitted
  Rust batch-generation workflow split in this worktree was audited and
  validated as a real follow-up slice instead of context drift. The route now
  delegates active-task owner checks through
  `chapter_batch_generation_active_query_workflow_service.rs`, status-stream
  access through
  `chapter_batch_generation_status_stream_workflow_service.rs`, and stream
  event JSON assembly through
  `chapter_batch_generation_status_stream_event_service.rs`, while
  `chapter_batch_generation_active_query_service.rs` and
  `chapter_batch_generation_status_stream_service.rs` keep the pure query /
  polling concerns. To keep this seam independently reviewable, the active
  query workflow boundary now also owns explicit, unit-tested access-outcome
  mapping helpers instead of leaving that owner decision implicit in the async
  function body. This preserves route payloads, active-task response shape,
  batch status stream behavior, and SSE event payload shape while making the
  new route -> workflow -> query split less likely to regress back into route
  or query-local access policy.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_event_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_active_query_workflow_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this closes the “already in worktree” batch
  workflow split to a documented and test-backed checkpoint. The next round
  should not keep mining chapter-route seam compression; it should switch to a
  new Rust migration target under Phase 5 governance or a clearly identified
  schema/default owner seam.
- 2026-05-20 22:34 +08:00 checkpoint:
  this slice switched the task onto a real runtime-table ownership lane from
  the Phase 5 planning docs instead of continuing route-wrapper cleanup. A
  local audit of `batch_generation_snapshots` showed one small but concrete
  owner/default seam in
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`:
  both the generic snapshot upsert path and the resume-specific snapshot
  replacement path were spelling the same quality-field reset semantics inline
  (`latest_quality_metrics`, `quality_metrics_history`,
  `quality_metrics_summary` all reset to `None` on new snapshot creation, and
  on resume snapshot replacement), while also duplicating the ActiveModel
  construction for a fresh snapshot row. The refactor now makes that default
  contract explicit through `apply_empty_batch_generation_quality_fields()` and
  `build_new_batch_generation_snapshot_active_model()`. This preserves the
  current schema contract, runtime checkpoint payloads, resume reset semantics,
  and quality-field clearing behavior while making `batch_generation_snapshots`
  field ownership more explicit and testable as a first runtime-table slice.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this runtime-table lane still has value only
  while each move makes a real shared field/default contract explicit without
  changing task/checkpoint behavior. The next worthwhile moves should keep
  targeting concrete snapshot/default owner seams or another documented Phase 5
  schema/default candidate rather than drifting back to cosmetic chapter-route
  compression.
- 2026-05-20 22:46 +08:00 checkpoint:
  this slice continued the same `batch_generation_snapshots` runtime-table
  lane by making the `workflow_runtime_state` merge contract explicit instead
  of leaving it embedded in the database upsert branch. Previously
  `upsert_batch_generation_runtime_snapshot()` carried a local match block that
  implicitly encoded one important owner rule: when both current and incoming
  runtime states are JSON objects, the owner should merge keys; otherwise the
  incoming value replaces the old state entirely. That rule is part of the
  persisted runtime-state contract, not just a storage detail. The refactor
  now exposes it through `merge_batch_generation_runtime_state()` with focused
  tests for object-merge, stale non-object replace, and incoming non-object
  replace semantics. This preserves checkpoint payload shape, runtime snapshot
  persistence behavior, and resume/reset semantics while making one more
  `batch_generation_snapshots` owner/default rule explicit and reviewable.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this runtime-table lane remains productive
  while changes continue converting real persisted-state semantics into
  explicit helpers with targeted tests. The next step should either keep
  tightening concrete runtime snapshot/reset owner rules or move to the next
  documented schema/default candidate table, but should not revert to generic
  route/service wrapper reshuffling.
- 2026-05-20 23:01 +08:00 checkpoint:
  this slice kept pushing the same `batch_generation_snapshots` lane one level
  deeper by making the snapshot write mode itself explicit. After the earlier
  quality-field reset helper and runtime-state merge helper were extracted, the
  remaining owner rule was still split across two write paths:
  `upsert_batch_generation_runtime_snapshot()` implicitly meant “merge runtime
  state and preserve quality fields”, while
  `replace_batch_generation_runtime_snapshot_for_resume()` implicitly meant
  “replace runtime state and clear quality fields first”. Those are not just
  storage details; they are persisted snapshot write-mode semantics. The
  refactor now introduces `BatchGenerationSnapshotWriteMode` plus
  `resolve_batch_generation_snapshot_runtime_state_for_write()` and
  `apply_batch_generation_snapshot_write_mode()` so the owner contract is
  explicit: merge mode preserves quality fields and merges object state,
  replace mode clears quality fields and replaces runtime state. This
  preserves runtime checkpoint shape, resume reset behavior, quality-field
  clearing semantics, and task/read-side expectations while making one more
  persisted-state rule auditable and test-backed.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the `batch_generation_snapshots` lane is still
  valid as long as each step keeps extracting a concrete persisted-state owner
  contract like this one. The next move should either target another snapshot
  write/reset rule with similar semantic value or switch to the next documented
  runtime-table candidate instead of reopening generic chapter refactor churn.
- 2026-05-20 23:18 +08:00 checkpoint:
  this slice stayed inside the same runtime-table owner lane but tightened the
  single-generation snapshot progression contract instead of opening a new
  table. The remaining duplication was no longer about snapshot write mode; it
  was about who owns the persisted phase/status/message/progress semantics for
  single-chapter generation checkpoints. Several write sites in
  `chapter_batch_generation_runtime_state_service.rs` were spelling closely
  related stage data inline, which made the contract reviewable only by
  comparing multiple branches. The refactor now introduces
  `SingleGenerationSnapshotStage`, `SingleGenerationSnapshotPlan`,
  `resolve_single_generation_snapshot_plan()`, and
  `build_single_generation_runtime_checkpoint_for_stage()` so one explicit
  helper owns how pending / preparing / generating / finalizing / completed /
  failed snapshots map to persisted checkpoint fields. The touched write paths
  now delegate to that stage owner instead of rebuilding the same contract
  locally in `persist_new_single_generation_task_snapshot()`,
  `prepare_single_generation_runtime()`,
  `persist_single_generation_finalizing_snapshot()`,
  `finalize_single_generation_success()`, and
  `finalize_single_generation_failure()`. This preserves checkpoint payload
  shape, task progression behavior, and snapshot persistence semantics while
  making one more persisted-state contract explicit and test-backed without
  changing the external task lifecycle.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this lane still has signal while each move
  turns a real persisted-state progression rule into one explicit owner helper
  with focused tests. The next move should stay on concrete
  `batch_generation_snapshots` progression/reset semantics rather than falling
  back to generic route-wrapper cleanup.
- 2026-05-20 23:47 +08:00 checkpoint:
  this slice kept the task on the same `batch_generation_snapshots`
  runtime-table lane and pushed the progression owner seam from the
  single-generation path into the true batch path. The remaining duplication
  was spread across `prepare_batch_generation_runtime()`,
  `mark_batch_generation_chapter_started()`,
  `finalize_batch_generation_success()`,
  `finalize_batch_generation_failure()`, and
  `finalize_batch_generation_cancelled()`: each site was locally spelling a
  related combination of persisted checkpoint `phase`, `progress`, `status`,
  `last_event`, and `last_message`. That is not wrapper noise; it is the batch
  runtime progression contract for `batch_generation_snapshots`. The refactor
  now makes that contract explicit through `BatchGenerationSnapshotStage`,
  `BatchGenerationSnapshotPlan`,
  `resolve_batch_generation_snapshot_plan()`,
  `build_batch_generation_runtime_checkpoint_from_plan()`, and
  `build_batch_generation_runtime_checkpoint_for_stage()`. The prepare /
  chapter-start / chapter-success / cancelled / failed write paths now
  delegate to that owner helper instead of assembling checkpoint progression
  fields inline. This preserves task status updates, checkpoint payload shape,
  per-stage event/message semantics, and terminal behavior while making the
  persisted batch progression table auditable and unit-tested in one place.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (21 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: the current runtime-table lane still has value
  while it continues extracting concrete snapshot/default/progression ownership
  rules. The next worthwhile move should stay with real persisted-state seams
  inside `batch_generation_snapshots` or an equally concrete Phase 5 runtime
  table target, rather than drifting back to route-only cleanup.
- 2026-05-21 00:08 +08:00 checkpoint:
  this slice stayed on the same `batch_generation_snapshots` table but shifted
  from running/terminal progression to pending-entry semantics. After the
  earlier stage-owner extraction, the queued-snapshot path
  (`persist_new_batch_generation_task_snapshot()`) and the resume-reset path
  (`build_resume_batch_generation_runtime_checkpoint()`) were still sharing a
  generic pending builder while leaving the real owner rule scattered across
  call sites: queued tasks must always expose `queued` + the create-time
  waiting message with zero totals, while resumed tasks must expose `resume` +
  the resume waiting message and preserve chapter pointer only for the
  single-generation case, with totals shown only for multi-chapter tasks. That
  is part of the persisted pending-snapshot contract, not just helper
  plumbing. The refactor now makes that contract explicit through
  `BatchGenerationPendingSnapshotStage`,
  `BatchGenerationPendingSnapshotPlan`,
  `resolve_batch_generation_pending_snapshot_plan()`,
  `build_pending_batch_generation_runtime_checkpoint_from_plan()`, and
  `build_pending_batch_generation_runtime_checkpoint_for_stage()`. Both the
  queued snapshot write path and the resume checkpoint path now delegate to
  this owner helper instead of spelling their pending semantics inline. This
  preserves checkpoint payload shape, queued/resume event vocabulary, chapter
  pointer behavior, and multi-chapter total display while making another real
  pending-state rule explicit and unit-tested.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (23 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this runtime-table lane is still productive
  while each move converts a concrete pending/reset/progression owner rule into
  one explicit helper with focused tests. The next slice should keep looking
  for similarly concrete persisted-state seams before switching tables.
- 2026-05-21 00:26 +08:00 checkpoint:
  this slice kept the work inside
  `chapter_batch_generation_runtime_state_service.rs` but widened the owner
  focus from snapshot rows to the adjacent `batch_generation_task` lifecycle
  contract for single-generation execution. After the pending/progression
  snapshot rules were extracted, the single-chapter task mutation path was
  still spelling status/timestamp/error/chapter-pointer writes inline across
  `prepare_single_generation_runtime()`,
  `finalize_single_generation_success()`, and
  `finalize_single_generation_failure()`. Those assignments are not merely ORM
  plumbing: they define the persisted task-table contract for single-chapter
  runtime lifecycle transitions. The refactor now makes that contract explicit
  through `ModelFieldUpdate`, `TaskTimestampUpdate`,
  `SingleGenerationTaskStage`, `SingleGenerationTaskMutationPlan`,
  `resolve_single_generation_task_mutation_plan()`, and
  `apply_single_generation_task_mutation_plan()`. The prepare / completed /
  failed task-table write paths now delegate to that owner helper instead of
  mutating `batch_generation_task::ActiveModel` field-by-field inline. This
  preserves task status progression, started/completed timestamp semantics,
  error clearing/filling behavior, completed chapter count, and chapter pointer
  updates while making the single-generation task lifecycle contract explicit
  and unit-tested next to the snapshot owner rules.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (25 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this runtime-state lane still has signal as
  long as each move captures a concrete persisted task/snapshot lifecycle rule
  in one owner helper with focused tests. The next slice can continue into the
  batch task-table lifecycle path if similar value remains, otherwise switch to
  the next Phase 5 runtime-table candidate instead of route-only cleanup.
- 2026-05-21 00:42 +08:00 checkpoint:
  this slice stayed in the same runtime-state file and completed the next
  symmetrical move after the single-generation task lifecycle extraction:
  `batch_generation_task` writes for the true batch path are now owned
  explicitly instead of being spelled inline at each transition. Before this
  change, `prepare_batch_generation_runtime()`,
  `mark_batch_generation_chapter_started()`,
  `finalize_batch_generation_success()`,
  `finalize_batch_generation_failure()`, and
  `finalize_batch_generation_cancelled()` each carried overlapping field-level
  mutations on `batch_generation_task::ActiveModel` for status, timestamps,
  error text, completed-chapter counters, current chapter pointer, retry
  state, and total-chapter progress. Those writes are not generic ORM noise;
  they define the persisted task-table lifecycle contract for batch execution.
  The refactor now makes that contract explicit through
  `BatchGenerationTaskStage`, `BatchGenerationTaskMutationPlan`,
  `resolve_batch_generation_task_mutation_plan()`, and
  `apply_batch_generation_task_mutation_plan()`. The prepare / chapter-start /
  chapter-success / failed / cancelled task-table write paths now delegate to
  that owner helper instead of mutating the task model field-by-field inline.
  This preserves running/completed/failed/cancelled task status semantics,
  start/complete timestamp behavior, error clearing/filling, completed chapter
  counts, total chapter tracking, and current chapter pointer updates while
  making the batch task lifecycle contract auditable and unit-tested next to
  the snapshot owner seams.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (27 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this file still has value while it continues
  converting concrete task/snapshot owner rules into explicit helpers with
  focused tests. The next step should only stay here if another persisted-state
  rule remains concrete and non-cosmetic; otherwise move to the next Phase 5
  runtime-table candidate rather than stretching this seam into generic
  reshuffling.
- 2026-05-21 00:54 +08:00 checkpoint:
  this slice stayed on the same `chapter_batch_generation_runtime_state_service.rs`
  lane and finished one more real persisted-state rule that had remained
  outside the new batch task lifecycle owner: resume reset. Even after the
  prepare / chapter-start / success / failure / cancelled task writes were
  extracted, `reset_batch_generation_task_for_resume()` still carried its own
  field-by-field reset logic for `status`, timestamps, error state, completed
  counters, retry count, and resumed chapter pointer while separately reusing
  only the `failed_chapters` reset payload. That is part of the
  `batch_generation_task` persisted resume contract, not generic ORM setup. The
  refactor now extends `BatchGenerationTaskStage` with `ResumeReset` so the
  existing `BatchGenerationTaskMutationPlan` /
  `resolve_batch_generation_task_mutation_plan()` /
  `apply_batch_generation_task_mutation_plan()` owner also governs resume reset
  semantics. `reset_batch_generation_task_for_resume()` now delegates its task
  table mutation through the same helper instead of keeping a parallel inline
  reset branch. This preserves pending status restoration, cleared
  started/completed timestamps, error reset, completed counter reset, retry
  reset, and the single-vs-batch chapter pointer behavior already defined by
  `ResumeBatchGenerationTaskResetPlan`, while making the batch task resume
  contract more auditable and consistent with the rest of the task lifecycle
  owner model.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (27 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this file is approaching diminishing returns.
  Any further work here should only continue if another concrete task/snapshot
  persistence rule is still split across owners; otherwise the next round
  should switch to the next documented Phase 5 runtime-table candidate instead
  of forcing more cosmetic reshuffling.
- 2026-05-21 01:12 +08:00 checkpoint:
  this slice intentionally stopped mining
  `chapter_batch_generation_runtime_state_service.rs` and moved to the next
  documented Phase 5 runtime-table candidate with a real Rust write path:
  `analysis_tasks`. A local audit confirmed that Rust already owns multiple
  persisted lifecycle writes for this table across both runtime and query-side
  recovery boundaries: task creation in
  `create_chapter_analysis_task()`, transition to running in
  `mark_analysis_task_running()`, terminal failure in
  `mark_analysis_task_failed()`, terminal success in
  `persist_chapter_analysis_result()`, and timeout/startup auto-recovery in
  `recover_analysis_task_if_needed()`. Before this change, those writes were
  partially duplicated across `chapter_analysis_runtime_service.rs` and
  `chapter_analysis_query_service.rs`, which meant `analysis_tasks`
  status/progress/timestamp/error semantics were still split across owners.
  The refactor now introduces
  `chapter_analysis_task_state_service.rs` with
  `AnalysisTaskStage`, `build_analysis_task_active_model()`, and
  `apply_analysis_task_state()`. Runtime task creation/running/failure/
  completion and query-side auto-recovered-as-failed writes now delegate to the
  same helper instead of mutating `analysis_task::ActiveModel` inline in
  separate services. This preserves pending/running/completed/failed lifecycle
  semantics, default progress values, created/started/completed timestamps, and
  error reset/fill behavior while making `analysis_tasks` persistence ownership
  explicit and test-backed as the next table-level Phase 5 slice.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_task_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this confirms the current task has now moved off
  the saturated batch runtime-state lane onto the next Phase 5 runtime-table
  target. The next step should continue only where Rust already has a concrete
  persisted write owner seam like this one, rather than reopening low-signal
  cleanup in the earlier batch snapshot/task file.
- 2026-05-21 01:24 +08:00 checkpoint:
  this slice kept the work on `analysis_tasks` and tightened the other real
  persisted-state rule that was still embedded in query-side code: auto
  recovery. After `chapter_analysis_task_state_service.rs` unified create /
  running / failed / completed / auto-recovered-as-failed task writes, the
  exact timeout/startup-timeout decision model was still encoded inline inside
  `recover_analysis_task_if_needed()` in
  `chapter_analysis_query_service.rs`. That logic is not only a query concern;
  it defines when a persisted `analysis_tasks` row should be rewritten from
  stale `pending`/`running` into `failed`, with which timeout window, which
  terminal error message, and which progress reset. The refactor now makes that
  contract explicit through `AnalysisTaskAutoRecoveryPlan` and
  `resolve_analysis_task_auto_recovery_plan()`, and
  `recover_analysis_task_if_needed()` now applies the returned plan instead of
  encoding the timeout/rewrite rules inline. This preserves the existing
  10-minute running timeout, 15-minute retry timeout, 3-minute startup timeout,
  failed-state rewrite behavior, completed timestamp backfill, and progress
  reset semantics while making the `analysis_tasks` auto-recovery contract
  unit-tested and reviewable as its own owner seam.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_task_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `analysis_tasks` still has signal while changes
  continue extracting concrete persisted timeout/recovery rules like this one.
  The next slice should stay only if another write-owner or default-owner seam
  is still concrete; otherwise move to the next runtime-table candidate rather
  than turning this lane into generic cleanup.
- 2026-05-20 23:13 +08:00 checkpoint:
  this slice kept the work on `analysis_tasks`, but used the runtime-table
  audit result to tighten the stop-rule before opening any new lane. A focused
  Rust search confirmed that the two next documented candidates do not yet have
  enough write-path signal for a real owner-seam move: `regeneration_tasks`
  currently has Rust query ownership plus project-delete cleanup only, and
  `chapter_draft_attempts` currently has Rust query/apply consumption plus
  project-delete cleanup only, without a Rust-owned insert/update lifecycle
  contract to consolidate. Rather than forcing a cosmetic migration, the
  refactor therefore returned to the still-valid `analysis_tasks` lane and
  tightened one more persisted failure-state contract. After the earlier
  extraction of `AnalysisTaskAutoRecoveryPlan`, the query-side recovery helper
  was still carrying `completed_at` and `progress` inside the recovery plan,
  which duplicated failure-state persistence semantics that already belong to
  `chapter_analysis_task_state_service.rs`. The refactor now narrows
  `AnalysisTaskAutoRecoveryPlan` so it only decides whether recovery should
  happen and which terminal error message should be written, while
  `AnalysisTaskStage::AutoRecoveredAsFailed` once again owns the failed-state
  persisted fields (`status`, `progress`, `error_message`, `completed_at`) end
  to end. In the same pass,
  `chapter_analysis_task_state_service.rs` now keeps the mutation plan simpler
  by storing the concrete `Option<String>` error payload directly instead of a
  second field-update enum just for `error_message`. This preserves the
  existing running/pending timeout windows, failed-state rewrite behavior, and
  task-status payload shape while making the `analysis_tasks` recovery write
  contract more obviously owned in one place.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_task_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: do not force Phase 5 onto
  `regeneration_tasks` or `chapter_draft_attempts` until Rust owns a real
  persisted insert/update lifecycle seam there. The next worthwhile move should
  either find one more concrete `analysis_tasks` write/default owner split, or
  record a saturation checkpoint for this lane and wait for a higher-signal
  Rust migration entrypoint.
- 2026-05-20 23:34 +08:00 checkpoint:
  this slice stayed on `analysis_tasks` and confirmed there was still one more
  concrete write-owner split worth removing before saturation. A full Rust
  write-path audit across `chapter_analysis_runtime_service.rs`,
  `chapter_analysis_query_service.rs`, and
  `chapter_analysis_task_state_service.rs` showed that create / running /
  failed / auto-recovered-as-failed writes were already routed through the
  shared state owner, but the completed path in
  `persist_chapter_analysis_result()` still performed a local follow-up status
  rewrite after calling `apply_analysis_task_state(... Completed ...)`. That
  extra `active.status = Set(normalize_analysis_status("completed"))` branch
  was not adding a new business rule; it was a residual parallel owner for the
  same persisted completed-state contract. The refactor removes that local
  override and drops the now-unused runtime-local
  `normalize_analysis_status()` helper and test, so `AnalysisTaskStage::Completed`
  once again fully owns the persisted `status/progress/error_message/
  completed_at` contract for analysis-task completion. This preserves the
  completed task payload shape and timestamp behavior while making the runtime
  completion path consistent with the rest of the shared `analysis_tasks`
  mutation model.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_task_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `analysis_tasks` still has a little signal, but
  it is now in the tail phase where only true persisted-state owner leftovers
  should be touched. If the next audit does not reveal another comparable local
  completed/failed/default override, mark this lane as near saturation rather
  than stretching it into generic runtime cleanup.
- 2026-05-21 09:25 +08:00 checkpoint:
  this slice moved away from the near-saturated `analysis_tasks` lane and used
  the remaining batch-generation signal instead. A fresh audit of
  `chapter_batch_generation_task_command_service.rs` showed that the two batch
  create entrypoints were still duplicating the same persisted pending/default
  contract inline: `status=pending`, `completed_chapters=0`,
  `failed_chapters=[]`, `current_retry_count=0`, `created_at=now`,
  `started_at=nil`, `completed_at=nil`, and `error_message=nil`, plus the
  matching single-vs-batch differences for `chapter_ids`, `style_id`,
  `current_chapter_id`, `current_chapter_number`, `enable_analysis`, and
  `max_retries`. That is a real write-owner seam, not just route plumbing, so
  the refactor introduced
  `build_batch_generation_task_active_model()` as the shared owner helper for
  batch task creation. Both
  `create_batch_generation_task_plan()` and
  `create_single_generation_background_task_plan()` now delegate the
  persisted-state construction to that helper instead of assembling the same
  defaults in two separate `ActiveModel` literals. The change preserves the
  existing batch-create and single-background-create response payloads,
  chapter id payload shape, and runtime snapshot writes while making the
  creation-state contract auditable in one place. Added focused test coverage
  now verifies the helper's default pending state and timestamp behavior.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `analysis_tasks` should remain on the
  saturation watchlist unless a new leak appears, while `batch_generation_tasks`
  still has enough real creation-state signal for a few more small helper
  consolidations if they remain similarly concrete.
- 2026-05-21 09:25 +08:00 checkpoint:
  this slice continued the batch-generation line and confirmed the next
  concrete seam was still in task creation defaults, not in a new runtime-table
  lane. The runtime/task-command audit showed that the remaining batch
  transition helpers were already built around shared task-state mutation
  helpers (`resolve_*_task_mutation_plan()` /
  `apply_*_task_mutation_plan()`), so there was no fresh ownership split worth
  forcing there. The higher-signal residual was in
  `chapter_batch_generation_task_command_service.rs`: the two create
  entrypoints were still spelling the same pending/default persisted-state
  contract inline. The refactor introduced
  `build_batch_generation_task_active_model()` to own that shared pending
  contract and route both `create_batch_generation_task_plan()` and
  `create_single_generation_background_task_plan()` through it. This preserves
  the existing response payloads and runtime snapshot initialization while
  removing duplicated creation-state ownership. Focused test coverage now
  verifies the helper's default pending state, chapter/task identity fields,
  and timestamp/nullability behavior.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: batch creation defaults still have room for a
  couple of similarly concrete helper consolidations, but `analysis_tasks`
  should stay on the saturation watchlist and `chapter_generation_runtime_service`
  is not a better next target than the current batch seam.
- 2026-05-21 10:13 +08:00 checkpoint:
  this slice stayed inside the current Phase 5 Rust chapter-generation lane
  but deliberately avoided reopening the already low-signal route/request
  wrapper seams. A fresh audit showed that the more meaningful residual split
  was a shared default contract that still had multiple owners:
  chapter-generation `target_word_count` normalization was duplicated across
  `chapter_batch_generation_create_service.rs` and
  `chapter_single_generation_request_service.rs`, while batch resume in
  `chapter_batch_generation_task_command_service.rs` separately re-applied only
  the lower-bound half of the same semantic with `task.target_word_count.max(1)`.
  Those paths all define the same cross-workflow meaning of chapter generation
  target sizing: default to 3000 when missing, and never allow values below 1.
  The refactor therefore introduced
  `chapter_generation_target_word_count_service.rs` with
  `normalize_chapter_generation_target_word_count()` as the shared owner of
  that default/minimum contract. Batch create preparation, single-chapter
  request preparation, and batch/single resume execution planning now all
  delegate to that helper instead of carrying partially duplicated normalization
  rules. This preserves existing HTTP/SSE payloads, task status semantics, and
  runtime behavior while making the target-word-count default contract auditable
  in one place for the current Rust migration lane.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_target_word_count_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (7 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: this is a valid shared-default owner split, but
  it is also another sign that the current batch-generation lane is entering a
  finer-grained cleanup phase. The next slice should continue only if it finds
  another concrete persisted/read/default contract owner split of comparable
  signal; otherwise Phase 5 should pivot to the next documented Rust migration
  seam instead of stretching this lane into cosmetic consolidation.
- 2026-05-21 10:35 +08:00 checkpoint:
  this slice treated the current `batch_generation` lane as near saturation
  and deliberately pivoted to the next higher-signal Rust task-state seam
  instead of forcing another read-side cleanup. A focused audit across
  `chapter_analysis_runtime_service.rs`,
  `chapter_analysis_query_service.rs`, and
  `chapter_analysis_task_state_service.rs` showed that `analysis_tasks`
  still had one concrete persisted-state ownership split left: runtime and
  query recovery paths were all independently repeating the same
  `find_by_id(task_id) -> into ActiveModel -> apply stage -> update`
  write sequence for `Running`, `Failed`, `Completed`, and
  `AutoRecoveredAsFailed`. That sequence is not mere wrapper plumbing; it is
  the shared write boundary for task lifecycle persistence by id. The refactor
  therefore introduced `apply_analysis_task_state_by_id()` in
  `chapter_analysis_task_state_service.rs` as the shared owner for id-based
  lifecycle rewrites. Runtime task start/fail/complete and query-side
  auto-recovery now all delegate to that helper instead of each service
  carrying its own `find/update` persistence path. This preserves the
  existing `analysis_tasks` status/progress/error/timestamp semantics and
  analysis payload behavior while making the id-based lifecycle write contract
  auditable from one state owner module.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_task_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `batch_generation` should now be treated as
  a watchlist lane that only reopens for a clearly comparable contract leak.
  The active Phase 5 execution lane can continue around `analysis_tasks` or
  the next documented neighboring Rust seam where lifecycle/read owner splits
  remain concrete and behavior-preserving.
- 2026-05-21 10:42 +08:00 checkpoint:
  this slice stayed on the `analysis_tasks` lane and removed one more real
  shared response-contract split without changing any outer HTTP envelope.
  A focused audit of `chapter_analysis_trigger_service.rs`,
  `chapter_analysis_runtime_service.rs`, and the `memories` route confirmed
  that analysis task creation still had two parallel owners for the same inner
  payload contract: both `PreparedChapterAnalysisTrigger.payload` and
  `enqueue_chapter_analysis_task()` were assembling the identical
  `{"task_id","chapter_id","status":"pending","message":"章节分析任务已创建"}`
  object inline. That is a concrete read-side contract duplication, not route
  transport glue, because both creation entrypoints are responsible for the
  same task-create response body consumed by callers. The refactor introduced
  `build_chapter_analysis_task_create_payload()` in
  `chapter_analysis_runtime_service.rs` as the single owner of that payload
  shape, then routed both the trigger preparation path and the runtime enqueue
  path through it. This preserves the existing response fields, pending status
  default, and create-message text while making the analysis-task create
  payload auditable in one place. Focused test coverage now locks the shared
  payload contract directly.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `analysis_tasks` still has some signal, but it
  is now in the tail phase where only another clearly shared lifecycle/read
  contract owner should justify more work. If the next audit finds only route
  wrapper cleanup or message-string interpretation churn, record lane
  saturation and pivot to the next documented Phase 5 Rust migration seam.
- 2026-05-21 10:47 +08:00 checkpoint:
  this slice kept the focus on the near-tail `analysis_tasks` lane and only
  proceeded because one more concrete cross-layer contract split remained.
  A targeted audit of `chapter_analysis_query_service.rs` and
  `chapter_analysis_query_error_mapper.rs` showed that the chapter-analysis
  view query path was still using a stringly-typed not-found contract:
  the service returned `"Chapter analysis not found"` as a generic internal
  error string, and the route mapper then re-interpreted that exact string to
  decide whether to emit a 404 or 500. That is a real owner split for the same
  query error semantic, not harmless plumbing, because the service and route
  were both partially defining the same “analysis record missing” boundary.
  The refactor introduced
  `LoadChapterAnalysisViewPayloadError::AnalysisNotFound` and routed
  `load_chapter_analysis_view_payload()` to return that typed variant directly
  when the latest `plot_analysis` row is absent. The route error mapper now
  maps the typed variant to the same 404 payload without comparing error
  strings, while true internal failures still map to 500 through the existing
  detail path. This preserves the external response status and `"Chapter analysis not found"`
  message exactly, but makes the missing-analysis query contract auditable from
  one shared typed owner instead of a service-string plus route-string check.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated stop-rule assessment: `analysis_tasks` is now very close to lane
  saturation. The next audit should continue here only if it finds another
  comparable typed lifecycle/read contract split. If the residual work is
  mostly wrapper compression, route-level forwarding cleanup, or broader
  message-code reinterpretation, Phase 5 should record saturation for this
  lane and pivot to the next documented Rust migration seam.
- 2026-05-21 10:54 +08:00 checkpoint:
  this slice applied the stop-rule and deliberately pivoted away from the
  nearly saturated `analysis_tasks` lane instead of forcing one more weak
  cleanup. A fresh audit across the single-chapter generation path in
  `chapter_batch_generation.rs`,
  `chapter_single_generation_background_workflow_service.rs`, and
  `chapter_single_generation_request_service.rs` showed that the next concrete
  contract split was no longer in request field assembly itself, but in the
  request-preparation error boundary: stream generation already surfaced
  `PrepareSingleChapterGenerationRequestError` directly, while background
  generation copied that same prepare contract into a second
  `CreateSingleGenerationBackgroundWorkflowError` enum and the route layer then
  maintained a second near-identical mapper for the duplicate error shape.
  That is a real owner split for the same request-prepare semantic, because
  background and stream are two transport entrypoints for one shared
  single-chapter generation request boundary, yet only one of them was using
  the canonical error type end to end. The refactor removed the duplicate
  background workflow error enum, made
  `start_owned_single_generation_background_workflow()` return
  `PrepareSingleChapterGenerationRequestError` directly, and routed the
  background endpoint through the existing
  `map_single_chapter_generation_request_error()` mapper. This preserves the
  external 404/400/500 response shapes and all background task creation /
  dispatch behavior while making both stream and background single-generation
  entrypoints share the same typed request-prepare error owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (7 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `analysis_tasks` should now be treated as saturated
  unless a clearly comparable lifecycle/read contract leak reappears. The
  active Phase 5 execution lane can continue around `chapter_generation` /
  `chapter_batch_generation` single-entry request and error ownership, as long
  as each slice stays behavior-preserving and avoids reopening runtime-write
  semantics.
- 2026-05-21 11:01 +08:00 checkpoint:
  this slice stayed on the reactivated `chapter_batch_generation` lane and
  picked a small typed-owner cleanup that matches the earlier status-query
  direction instead of reopening runtime-write logic. A focused audit of
  `chapter_batch_generation_status_stream_service.rs`,
  `chapter_batch_generation_status_query_service.rs`, and
  `chapter_batch_generation_error_mapper.rs` showed that batch task access
  semantics were still slightly split between query and stream:
  status query had already collapsed its access boundary to the shared
  `LoadBatchGenerationTaskViewContextError` alias, but status stream access was
  still carrying a second local `BatchGenerationStatusStreamAccessError` enum
  with the same `TaskNotFound/Internal` contract only to rewrap the
  `load_required_owned_task()` result. That is a real typed-owner duplication,
  not just naming noise, because the same owned-task access semantic was still
  being defined twice for two neighboring read-side entrypoints. The refactor
  therefore collapsed `BatchGenerationStatusStreamAccessError` into a type
  alias of `LoadOwnedBatchGenerationTaskError` and let
  `ensure_batch_generation_status_stream_access()` return the shared owned-task
  access error directly. The route mapper keeps the same 404 / 500 transport
  behavior, but the stream-access contract is now auditable from the same
  owner type that already governs required owned-task lookup. Focused tests now
  assert the alias boundary directly, and the route error mapper also has a
  dedicated regression for internal stream-access failures.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (7 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the active Phase 5 seam can keep advancing around
  `chapter_batch_generation` read/stream semantics if each next step removes a
  similarly concrete shared contract split. Avoid drifting into cosmetic route
  compression or reopening checkpoint/snapshot write ownership unless a much
  stronger seam appears.
- 2026-05-21 11:08 +08:00 checkpoint:
  this slice stayed in the same `chapter_batch_generation` read-side lane and
  tightened one more neighboring typed-owner boundary without reopening
  runtime-write semantics. A focused audit of
  `chapter_batch_generation_status_view_service.rs`,
  `chapter_batch_generation_status_query_service.rs`, and
  `chapter_batch_generation_status_stream_service.rs` showed that the required
  owned-task access contract was still slightly split across adjacent read-side
  owners: status query had already collapsed to the shared
  `LoadBatchGenerationTaskViewContextError` alias, and stream access had
  already collapsed to `LoadOwnedBatchGenerationTaskError`, but the view
  context owner itself was still carrying a local
  `LoadBatchGenerationTaskViewContextError` enum shape only to rewrap the same
  required owned-task `TaskNotFound/Internal` result from
  `load_required_owned_task()`. That is a real typed-owner duplication because
  the same required owned-task access semantic was still being defined twice
  for the same read-side context boundary. The refactor therefore collapsed
  `LoadBatchGenerationTaskViewContextError` into a type alias of
  `LoadOwnedBatchGenerationTaskError` and let
  `load_required_batch_generation_task_view_context()` return the shared owned
  task access error directly, while still mapping snapshot-load failures to the
  same internal variant. This preserves the existing status query / status
  stream 404 and 500 transport behavior, but makes status query, status stream
  access, and status view context all share one auditable owned-task access
  contract.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_status_stream_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_batch_generation` read-side ownership is
  now closer to saturation, so the next slice should continue here only if it
  removes another similarly concrete read/default contract split in an
  adjacent owner. If the residual work is mostly naming cleanup or thin-wrapper
  movement, Phase 5 should pivot to the next documented Rust seam instead of
  stretching this lane further.
- 2026-05-21 11:34 +08:00 checkpoint:
  this slice explicitly applied the stop-rule to the nearly saturated
  `chapter_batch_generation` read-side lane and pivoted to the next nearby
  Phase 5 Rust seam instead of forcing another thin-wrapper cleanup. A focused
  audit across `chapter_regeneration_query_service.rs`,
  `chapter_regeneration_query_error_mapper.rs`, and the shared chapter access
  boundary showed that regeneration task history was still carrying a parallel
  typed error owner for the same access semantic: the query service defined
  `LoadRegenerationTasksPayloadError` with the same
  `NotFoundOrAccessDenied/Internal` contract already owned by
  `LoadAccessibleChapterError`, then the route mapper redefined the same 404 /
  500 mapping for that duplicated wrapper type. That is a concrete typed-owner
  split, not cosmetic churn, because the same chapter-access boundary was
  still being described twice for one read-side query path. The refactor
  therefore collapsed `LoadRegenerationTasksPayloadError` into a type alias of
  `LoadAccessibleChapterError`, let
  `load_owned_regeneration_tasks_payload()` return the shared chapter-access
  error directly, and routed
  `map_regeneration_tasks_query_error()` through the existing
  `map_load_accessible_chapter_error()` helper. This preserves the existing
  regeneration-task history payload shape and 404 / 500 transport behavior,
  while making the owned chapter-access contract auditable from one shared
  owner in the regeneration query lane.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_query_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_batch_generation` read-side semantics
  should now stay on a saturation watchlist unless another comparably strong
  contract leak appears, while the active Phase 5 seam can continue around
  neighboring `chapter_regeneration` and `chapter_generation` typed/default
  owner boundaries as long as each slice remains behavior-preserving and
  independently verifiable.
- 2026-05-21 11:49 +08:00 checkpoint:
  this slice stayed on the `chapter_regeneration` lane and removed one more
  concrete shared access-contract split adjacent to the previous regeneration
  query cleanup. A focused audit of
  `chapter_regeneration_apply_service.rs`,
  `chapter_regeneration_routes.rs`, and
  `chapters_error_mapper.rs` showed that partial-regeneration apply was still
  flattening the same chapter-access boundary into a local wrapper type:
  `apply_owned_partial_regenerate_payload()` converted
  `LoadAccessibleChapterError` into local `NotFound/Internal` variants, while
  the route mapper then re-expressed the same 404 / 500 contract with
  `chapter_not_found_or_access_denied_error()` and `internal_detail_error()`.
  That is a real typed-owner duplication, not route noise, because the same
  chapter-access semantic was still being defined twice in one apply path.
  The refactor therefore changed `ApplyPartialRegenerateError` so the access
  branch is now `Chapter(LoadAccessibleChapterError)`, let
  `apply_owned_partial_regenerate_payload()` return the shared chapter-access
  error directly, and routed `map_apply_partial_regenerate_error()` through the
  existing `map_load_accessible_chapter_error()` helper for that branch. The
  apply-specific domain errors (`EmptyContent`, `WorkflowMetaText`,
  `InvalidRange`) and internal update failures remain owned by the apply
  service, so the external 400 / 404 / 500 response semantics and message text
  stay unchanged while the access boundary becomes auditable from one shared
  owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_apply_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_regeneration` still has enough signal to
  continue if the next slice removes another similarly concrete access/default
  owner split around stream workflow or request normalization. Do not reopen
  `chapter_batch_generation` read-side cleanup unless a new comparably strong
  contract leak appears.
- 2026-05-21 12:18 +08:00 checkpoint:
  this slice stayed on the same `chapter_regeneration` lane but moved from
  access-error ownership into a shared default-contract seam inside partial
  regeneration preparation. A focused audit of
  `chapter_regeneration_prepare_service.rs` showed that the
  `length_mode` default contract was still being defined twice in parallel:
  both `build_partial_length_requirement()` and
  `calculate_partial_target_words()` independently interpreted
  `length_mode.unwrap_or("similar")` and the `"custom"` fallback behavior.
  That is a real shared-default owner split, not harmless helper plurality,
  because those two functions together define one cross-workflow semantic:
  how partial regeneration should default and target length when callers omit
  or partially specify length controls. The refactor introduced
  `normalize_partial_regeneration_length_mode()` plus
  `resolve_partial_regeneration_length_plan()` as the shared owner for that
  contract. `build_partial_length_requirement()` and
  `calculate_partial_target_words()` now both delegate to the same length plan
  instead of each re-encoding the fallback rules. This preserves the existing
  prompt text, target-word behavior, and max-token calculation while making the
  partial regeneration length semantics auditable from one owner helper.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_regeneration` still has actionable signal,
  but it is now shifting from access-error cleanup toward smaller shared-default
  semantics. Continue here only for similarly concrete owner splits; if the
  remaining work degrades into cosmetic helper reshuffling, pivot to the next
  neighboring `chapter_generation` seam instead of stretching this lane.
- 2026-05-21 12:33 +08:00 checkpoint:
  this slice deliberately pivoted from the shrinking `chapter_regeneration`
  default-contract lane into the neighboring `chapter_generation` access seam
  once the next stronger contract leak appeared there. A focused audit of
  `chapter_generation_context_service.rs` and
  `chapter_batch_generation_access_service.rs` showed that generation context
  loading still carried its own parallel chapter-access owner: it queried the
  chapter directly, loaded the project again, and manually enforced the
  `user_id` access rule, even though
  `load_accessible_chapter_for_generation()` already owned that same
  chapter-generation access boundary for the current Phase 5 lane. That is a
  concrete owner split, not mere helper overlap, because two adjacent services
  were independently defining the same chapter existence / access contract for
  generation flows. The refactor therefore changed
  `load_generation_context()` to delegate chapter-access loading to
  `load_accessible_chapter_for_generation()`, while keeping a small local
  mapper so the existing `"Chapter not found"` and
  `"Chapter not found or access denied"` error text still flows outward exactly
  as before. Project lookup and previous-chapter lookup remain local to the
  context owner, so the change narrows the seam without broadening
  responsibilities. This preserves runtime behavior and visible error strings
  while making the generation access boundary auditable from one shared owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_context_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_generation` now has the stronger active
  signal for the next slice if another adjacent access/default contract split
  remains. `chapter_regeneration` can still continue for comparably concrete
  seams, but it should no longer be stretched just to avoid pivoting.
- 2026-05-21 12:48 +08:00 checkpoint:
  this slice stayed on the now-active `chapter_generation` lane and tightened a
  more concrete runtime-result owner split instead of drifting back into prompt
  helper cleanup. A focused audit of
  `chapter_generation_runtime_service.rs` and
  `chapter_batch_generation_runtime_state_service.rs` showed that generated
  chapter result semantics were still split across two owners: the generation
  runtime service assembled a JSON payload with `chapter_id`, `chapter_number`,
  `title`, `content`, and `word_count`, but the single-generation runtime path
  then re-parsed `chapter_id/chapter_number/word_count` back out of that JSON
  to update snapshots and final task state. That is a real owner split because
  the same generation-result contract was being defined once as JSON assembly
  and a second time as downstream JSON field decoding. The refactor introduced
  `GeneratedChapterResult` as the typed owner for that contract inside
  `chapter_generation_runtime_service.rs`. Persistence now returns the typed
  result, `response_payload()` is derived from that owner when SSE/result JSON
  is needed, and the single-generation runtime path now consumes the typed
  fields directly instead of re-decoding them from `Value`. This preserves the
  existing SSE result payload shape and final snapshot/task update behavior
  while making generated chapter result semantics auditable from one owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_runtime_state_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (27 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_generation` remains the highest-signal
  active seam as long as the next move removes another similarly concrete
  generation runtime/request/result contract split. `chapter_regeneration`
  stays available as a secondary lane, but no longer needs to be preferred by
  default.
- 2026-05-21 13:39 +08:00 checkpoint:
  this slice stayed on the active `chapter_generation` lane and removed one
  more concrete typed-owner split in the single-chapter request boundary
  instead of drifting into prompt-helper reshuffling. A focused audit of
  `chapter_single_generation_request_service.rs` and
  `chapter_batch_generation_error_mapper.rs` showed that single-generation
  request access semantics were still being defined twice: the request service
  flattened `LoadAccessibleChapterForGenerationError` into a second
  `PrepareSingleChapterGenerationRequestError::{ChapterNotFound, ChapterNotFoundOrAccessDenied, Internal}`
  shape, and the route mapper then maintained a second local
  `SingleChapterGenerationError` enum to restate the same 404 / 500 transport
  contract before mapping config errors. That is a real owner split because the
  same chapter-access semantic for one request-preparation boundary was still
  being redefined in both service and API layers. The refactor changed
  `PrepareSingleChapterGenerationRequestError` so the access branch is now
  `Chapter(LoadAccessibleChapterForGenerationError)`, let
  `prepare_single_chapter_generation_request()` return the shared access error
  directly, and made `map_single_chapter_generation_request_error()` map that
  shared owner without the extra local enum. The config branch remains local to
  the request owner, so the external `Chapter not found`,
  `Chapter not found or access denied`, bad-request config detail, and internal
  error response semantics all stay unchanged while the access contract becomes
  auditable from one shared owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (7 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (8 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_generation` still has usable signal, but
  the remaining work around prompt/template/default ownership should be
  audited carefully for true contract duplication before another slice starts.
  If the next candidate is only thin-wrapper movement or helper relocation,
  pivot to a comparably concrete neighboring seam instead of forcing more
  cleanup in this lane.
- 2026-05-21 11:46 +08:00 checkpoint:
  this slice stayed adjacent to the active `chapter_generation` / generation
  runtime lane and tightened one more concrete execution-contract split around
  batch resume dispatch instead of drifting into prompt helper cleanup. A
  focused audit of `chapter_batch_generation_resume_service.rs`,
  `chapter_batch_generation_dispatch_service.rs`, and
  `chapter_batch_generation_task_command_service.rs` showed that resume
  execution semantics were still being defined twice across neighboring
  owners: `prepare_batch_generation_resume()` already produced the typed
  `ResumeExecutionPlan::{SingleChapter,Batch}` owner for the restored runtime
  shape, but `resume_owned_batch_generation_task()` then re-matched that same
  single/batch plan and rebuilt the runtime dispatch input locally before
  calling the dispatch layer. That is a real owner split, not just a wrapper,
  because the same "how resumed work turns into runtime dispatch input"
  contract was still being restated in both the resume service and the dispatch
  owner. The refactor introduced
  `dispatch_resume_generation_runtime()` in
  `chapter_batch_generation_dispatch_service.rs` as the single owner for
  converting `ResumeExecutionPlan` plus prepared execution config into the
  actual single-chapter or batch runtime dispatch call. The resume service now
  keeps only `load task -> prepare plan -> prepare execution config ->
  dispatch` flow and no longer redefines the single/batch runtime input
  assembly itself. This preserves the existing resume response payload,
  restored task semantics, target-word-count normalization, and runtime spawn
  behavior while making the resume execution boundary auditable from one
  dispatch owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_task_command_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `chapter_generation` neighborhood
  still has some signal, but the remaining candidates now need stronger proof
  of duplicated execution/default/result ownership. If the next move is only
  constructor extraction or helper relocation, pivot back to another nearby
  seam with a clearer shared contract leak.

## Risky Files / Review Points

- `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
- `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
- `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`
- `backend-rs/src/api/chapter_batch_generation.rs`
- `backend-rs/src/api/chapters.rs`

Escalate review before continuing if a slice would:

- change response fields
- change SSE terminal timing or event naming
- change task status/checkpoint defaults
- require touching unrelated model/config layers just to complete the move

## Start Gate

Before `task.py start`, confirm:

- `prd.md` reflects the narrowed continuation scope
- `design.md` captures the continuation model and stop rule
- `implement.md` identifies the next execution wave
- the user has approved moving from planning to implementation

- 2026-05-21 11:53 +08:00 checkpoint:
  this slice pivoted from the nearly-saturated `chapter_generation` lane into
  the adjacent full-regeneration prepare boundary and collapsed one more real
  typed-owner duplication seam without changing external regeneration
  semantics. A focused audit of
  `chapter_regeneration_prepare_service.rs`,
  `chapter_regeneration_stream_workflow_service.rs`, and
  `chapters_error_mapper.rs` showed that full regeneration stream config
  errors were still being defined twice by neighboring owners:
  `build_regeneration_ai_service()` already owned the typed
  `BuildRegenerationAiServiceError::InvalidConfig` contract, but
  `prepare_chapter_regeneration_stream()` rewrapped that same config failure
  into a second single-variant
  `PrepareChapterRegenerationStreamError::InvalidConfig` enum only for the
  mapper to unwrap it again. That wrapper did not add route-specific policy,
  payload shaping, or alternate fallback semantics, so it was a true owner
  split rather than a useful compatibility boundary. The refactor collapsed
  `PrepareChapterRegenerationStreamError` into a type alias of
  `BuildRegenerationAiServiceError`, updated
  `prepare_chapter_regeneration_stream()` to return the shared owner directly,
  and made `chapters_error_mapper.rs` match the shared config owner without an
  intermediate local enum. This preserves the existing bad-request mapping,
  response detail payload shape, SSE construction timing, and regeneration
  workflow boundary while making the config error contract auditable from one
  shared owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_regeneration` still has signal only when a
  remaining candidate proves the same level of duplicated access/default/error
  ownership across prepare/request/workflow boundaries. If the next move is
  only request helper relocation, prompt assembly reshuffling, or route-only
  compression, stop this lane and pivot instead of forcing another cosmetic
  slice.

- 2026-05-21 11:57 +08:00 checkpoint:
  a second regeneration slice stayed in the same prepare/workflow boundary and
  removed a broader local owner-flattening seam in partial regeneration
  stream preparation. A focused audit of
  `chapter_regeneration_prepare_service.rs`,
  `chapter_regeneration_stream_workflow_service.rs`, and
  `chapters_error_mapper.rs` showed that
  `PreparePartialRegenerationStreamError` was still restating three lower
  owners as a route-facing flat enum:
  `PreparePartialRegenerationError::{InvalidRange,EmptySelectedText}`,
  `LoadPartialStyleContentError::InvalidStyle`, and
  `BuildRegenerationAiServiceError::InvalidConfig`.
  That meant the prepare boundary was locally redefining input/style/config
  ownership instead of carrying the real typed owners through one composite
  error boundary. The refactor changed
  `PreparePartialRegenerationStreamError` into a nested owner enum with
  `Input(...)`, `Style(...)`, and `Config(...)` variants, updated
  `prepare_partial_regeneration_stream()` to forward those typed owners
  directly, and moved the user-facing bad-request message mapping into
  `chapters_error_mapper.rs` without altering response payload shape. This
  keeps the existing `400` semantics for invalid range, empty selection, style
  failure, and config failure while making the prepare boundary auditable from
  the actual underlying owners rather than a duplicated flat contract.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `chapter_regeneration` prepare lane
  is now close to saturation. Further work in this area should require
  concrete proof of duplicated access/default/result ownership across request,
  workflow, and stream builders; otherwise stop here and pivot to another
  higher-signal Rust seam instead of compressing routes or relocating helpers.

- 2026-05-21 12:03 +08:00 checkpoint:
  this slice pivoted back into the `chapter_batch_generation` create lane and
  removed one more duplicated prepare/domain owner split at the workflow
  boundary. A focused audit of
  `chapter_batch_generation_create_service.rs`,
  `chapter_batch_generation_create_workflow_service.rs`, and
  `chapter_batch_generation_error_mapper.rs` showed that batch-create request
  preparation already owned the typed
  `PrepareBatchGenerationCreateRequestError::{InvalidCount,ChaptersNotFound,Internal}`
  contract, but the workflow layer restated that same prepare contract as a
  second local `CreateBatchGenerationWorkflowDomainError` plus a custom
  `map_prepare_error()` translation before the route mapper converted it back
  into the same `400`/`404`/`500` transport semantics. That extra workflow
  domain layer did not add a distinct policy boundary; it only duplicated the
  prepare owner. The refactor removed
  `CreateBatchGenerationWorkflowDomainError`, changed
  `CreateBatchGenerationWorkflowError` to carry
  `Prepare(PrepareBatchGenerationCreateRequestError)`, and updated
  `chapter_batch_generation_error_mapper.rs` to map the shared prepare owner
  directly while preserving the existing invalid-count bad-request detail,
  chapters-not-found not-found detail, and internal error behavior. This keeps
  route semantics stable while making the batch-create prepare contract
  auditable from one owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (10 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_create_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_batch_generation` neighborhood still
  has selective signal, but only where a workflow or query layer is provably
  re-owning an existing typed prepare/access/view contract. If the next move
  degrades into generic route cleanup, helper extraction, or broad naming
  churn, stop and pivot instead of forcing more refactor volume.

- 2026-05-21 12:07 +08:00 checkpoint:
  this slice stayed in the `chapter_batch_generation` neighborhood and
  tightened the cancel workflow boundary by removing one more locally restated
  owned-task access contract. A focused audit of
  `chapter_batch_generation_owned_task_query_service.rs`,
  `chapter_batch_generation_cancel_service.rs`, and
  `chapter_batch_generation_error_mapper.rs` showed that cancel still
  redefined the same owned-task lookup owner at the workflow boundary:
  `load_required_owned_task()` already owned the typed
  `LoadOwnedBatchGenerationTaskError::{TaskNotFound,Internal}` contract, but
  `CancelBatchGenerationWorkflowError` flattened that lookup contract into its
  own `TaskNotFound` plus an unreachable extra `Internal(String)` branch
  before the route mapper translated it back into the same `404`/`500`
  transport behavior. That flattening did not create a meaningful policy
  boundary, so it was another real owner split. The refactor changed
  `CancelBatchGenerationWorkflowError` to carry
  `Task(LoadOwnedBatchGenerationTaskError)` directly, mapped the shared owned
  task error in `chapter_batch_generation_error_mapper.rs`, and removed the
  dead local `Internal` variant from the cancel workflow owner. This preserves
  the existing task-not-found response, domain bad-request behavior, and
  internal error detail mapping while making the cancel access contract
  auditable from the shared owner.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_cancel_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the remaining `chapter_batch_generation` signal is
  now narrower and should mostly be limited to places where query/workflow
  owners still restate an existing typed access or view-owner contract. If the
  next candidate needs broad route cleanup or introduces a new local wrapper
  just to move code around, stop this lane and pivot.

- 2026-05-21 12:10 +08:00 checkpoint:
  this slice continued the same `chapter_batch_generation` access-owner lane
  and tightened the resume workflow boundary to match the cancel workflow
  shape. A focused audit of
  `chapter_batch_generation_owned_task_query_service.rs`,
  `chapter_batch_generation_resume_service.rs`, and
  `chapter_batch_generation_error_mapper.rs` showed that resume still
  restated the same owned-task lookup owner locally:
  `load_required_owned_task()` already owned the typed
  `LoadOwnedBatchGenerationTaskError::{TaskNotFound,Internal}` contract, but
  `PrepareBatchGenerationResumeRequestError` flattened that lookup contract
  into `NotFound` plus a local `Internal(String)` branch before the route
  mapper converted it back into the same `404`/`500` behavior. That local
  flattening did not add a distinct resume-specific access policy, so it was
  another real owner split. The refactor changed
  `PrepareBatchGenerationResumeRequestError` to carry
  `Task(LoadOwnedBatchGenerationTaskError)` directly, updated the route mapper
  to map the shared owner, and preserved the existing domain/config
  bad-request behavior without changing any response payload shape. This keeps
  the batch-resume access contract auditable from the shared owner while
  preserving the current not-found and internal transport semantics.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (12 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_resume_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the remaining signal in this neighborhood is now
  extremely narrow and mostly limited to tiny dead wrappers or query owners
  that still restate a shared typed access/view contract. If the next move is
  not of that exact class, pivot instead of stretching the lane.

- 2026-05-21 12:11 +08:00 checkpoint:
  a tiny follow-up cleanup immediately fell out of the cancel/resume access
  seam work and was worth taking because it removed dead compatibility code
  rather than introducing a fresh abstraction move. After the cancel and
  resume workflows started carrying `LoadOwnedBatchGenerationTaskError`
  directly, `chapter_batch_generation_owned_task_query_service.rs` no longer
  had any production callers for the generic helper
  `map_owned_batch_generation_task_error()`. Keeping that helper would leave a
  dead local translation utility plus tests for a translation boundary that no
  longer exists anywhere in the runtime path. The cleanup removed the helper
  and its now-orphaned unit tests, leaving the owned-task query module with
  only the actual shared owner contract (`load_owned_task()` /
  `load_required_owned_task()` and `LoadOwnedBatchGenerationTaskError`). This
  did not change any external behavior; it only removed dead code exposed by
  the previous seam tightening.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (12 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this is very close to the stop rule for the current
  `chapter_batch_generation` lane. Further work here should only continue if a
  remaining owner split is as concrete as the create/cancel/resume seams; if
  not, pivot instead of mining more tiny cleanup.

- 2026-05-21 12:15 +08:00 checkpoint:
  this wave explicitly pivoted away from the now-near-saturated
  `chapter_batch_generation` lane into the neighboring analysis query
  ownership surface and completed one more concrete typed access-owner seam.
  A focused audit of
  `chapter_analysis_query_service.rs` and
  `chapter_analysis_query_error_mapper.rs` showed that the analysis query
  layer still carried two adjacent access-owner splits:
  `load_accessible_chapter()` already owned the typed
  `LoadAccessibleChapterError::{NotFoundOrAccessDenied,Internal}` contract,
  but `LoadChapterAnalysisViewPayloadError` flattened that access contract into
  a local `NotFoundOrAccessDenied` variant before later reintroducing the same
  route semantics, and `LoadChapterQualityMetricsPayloadError` was effectively
  just the same access contract plus an internal string branch for the payload
  query. The refactor changed
  `LoadChapterAnalysisViewPayloadError` to carry
  `Chapter(LoadAccessibleChapterError)` alongside its real local
  `AnalysisNotFound` domain branch, collapsed
  `LoadChapterQualityMetricsPayloadError` into the shared access owner alias,
  and updated the query error mapper to reuse
  `map_load_accessible_chapter_error()` directly for the shared access branch.
  This preserves the existing analysis-view not-found/access-denied `404`,
  analysis-missing `404`, and internal `500` semantics while making the access
  boundary auditable from one shared owner instead of a route-adjacent local
  flattening.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  A tiny post-change cleanup also removed the now-unused temporary import in
  `chapter_analysis_query_error_mapper.rs`, and the focused mapper test plus
  `cargo check` were rerun successfully after that cleanup.
  Updated lane assessment: the current high-signal pivot target is now the
  broader analysis/regeneration query/access surface, but only where a service
  still restates `LoadAccessibleChapterError` or another shared typed owner
  without adding a real local domain branch. If the next candidate does not
  meet that bar, pivot again instead of broadening this lane into generic
  cleanup.

- 2026-05-21 12:22 +08:00 checkpoint:
  this slice stayed in the adjacent analysis access-owner lane and removed one
  more concrete local flattening around draft query/apply ownership. A focused
  audit of `chapter_analysis_draft_service.rs` and
  `chapter_analysis_draft_error_mapper.rs` showed that the owned draft helpers
  were still restating the shared chapter-access owner locally:
  `load_accessible_chapter()` already owned the typed
  `LoadAccessibleChapterError::{NotFoundOrAccessDenied,Internal}` contract,
  but all four draft helper errors flattened that same access boundary into
  local `ChapterNotFoundOrAccessDenied` plus `Internal(String)` variants
  before the mapper translated them back into the same `404` / `500`
  transport semantics. That flattening did not add a draft-specific policy
  boundary; the real local ownership remains the draft-domain branch carrying
  `AutoRevisionDraftError` or `CandidateDraftError` together with the
  explicit-id context bit. The refactor changed
  `LoadOwnedAutoRevisionDraftPayloadError`,
  `ApplyOwnedAutoRevisionDraftPayloadError`,
  `LoadOwnedCandidateDraftPayloadError`, and
  `ApplyOwnedCandidateDraftPayloadError` to carry
  `Chapter(LoadAccessibleChapterError)` directly, updated the draft error
  mapper to reuse `map_load_accessible_chapter_error()` for that shared owner,
  and preserved the existing history/attempt-id-sensitive `404` messages plus
  stale, preview-only, empty-content, and workflow-meta conflict/bad-request
  semantics for the real draft-domain branches. This keeps the access
  boundary auditable from one shared owner without changing draft payload
  shape or route behavior.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  A tiny post-change cleanup also removed the now-unused
  `LoadAccessibleChapterError` import from
  `chapter_analysis_draft_error_mapper.rs`, and the focused mapper test plus
  `cargo check` were rerun successfully after that cleanup.
  Updated lane assessment: the immediate analysis draft access lane is now
  much narrower. Continue here only if another neighboring service is still
  provably restating `LoadAccessibleChapterError` or another shared typed
  owner without adding a real domain branch; otherwise pivot instead of
  mining thin wrappers.

- 2026-05-21 12:34 +08:00 checkpoint:
  this slice stayed in the neighboring analysis trigger lane and removed one
  more real typed-owner split around background analysis task creation. A
  focused audit of `chapter_analysis_trigger_service.rs`,
  `chapter_analysis_runtime_service.rs`, and `chapters_error_mapper.rs` showed
  that `prepare_chapter_analysis_trigger()` was still flattening two existing
  owners into a local route-facing enum:
  `load_accessible_chapter()` already owned the typed
  `LoadAccessibleChapterError::{NotFoundOrAccessDenied,Internal}` contract,
  and `create_chapter_analysis_task()` already owned the typed
  `CreateChapterAnalysisTaskError::{ChapterEmpty,ProjectMissing,Internal}`
  contract, but `PrepareChapterAnalysisTriggerError` restated those same
  boundaries as local `ChapterNotFoundOrAccessDenied`, `ChapterEmpty`,
  `ProjectMissing`, and `Internal(String)` variants before the mapper rebuilt
  the same `404` / `400` / `500` transport semantics. That wrapper did not
  add trigger-specific policy; it only duplicated the adjacent access and
  create-task owners. The refactor changed
  `PrepareChapterAnalysisTriggerError` to carry
  `Chapter(LoadAccessibleChapterError)` and
  `Create(CreateChapterAnalysisTaskError)` directly, updated
  `prepare_chapter_analysis_trigger()` and
  `build_prepared_chapter_analysis_trigger()` to forward those typed owners
  without local remapping, and taught `chapters_error_mapper.rs` to reuse
  `map_load_accessible_chapter_error()` for the shared access owner while
  preserving the existing `chapter empty`, `project missing`, and internal
  trigger response semantics for the create-task owner. This keeps the
  trigger payload, route dispatch timing, and background runtime behavior
  unchanged while making the trigger preparation boundary auditable from the
  real neighboring owners instead of a duplicated flattened contract.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_trigger_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (filter matched 0 tests; compile and execution succeeded),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  A tiny supporting compile fix also added `#[derive(Debug)]` to
  `CreateChapterAnalysisTaskError` so the shared create-task owner can sit
  inside the trigger error enum without reintroducing a local string-only
  wrapper. Updated lane assessment: the immediate analysis trigger/access lane
  is now close to saturation. Continue only if another neighboring service is
  still clearly flattening the same access or create-task owner without
  adding a real domain branch; otherwise pivot rather than compressing thin
  wrappers.

- 2026-05-21 12:49 +08:00 checkpoint:
  this slice pivoted back into the neighboring single-generation runtime lane
  and removed one more local flattening of a shared generation-access owner
  without changing runtime behavior. A focused audit of
  `chapter_generation_context_service.rs` and
  `chapter_generation_runtime_service.rs` showed that
  `load_generation_context()` was still collapsing the shared
  `LoadAccessibleChapterForGenerationError::{ChapterNotFound,ChapterNotFoundOrAccessDenied,Internal}`
  owner directly into string messages before the runtime boundary consumed the
  result. That meant the context helper was locally re-owning the same access
  contract as a string-only compatibility layer even though the only caller
  that needs the final `"Chapter not found"` / `"Chapter not found or access denied"` /
  `"Project not found"` messages is the outer runtime boundary that already
  returns `Result<_, String>`. The refactor introduced
  `LoadGenerationContextError::{Chapter(...),ProjectNotFound,Internal(String)}`
  in `chapter_generation_context_service.rs`, changed
  `load_generation_context()` to carry the shared chapter owner plus the real
  local `ProjectNotFound` branch directly, and moved the final string
  compatibility mapping into `chapter_generation_runtime_service.rs` through
  `map_load_generation_context_error()`. This preserves the existing single-
  generation background and stream runtime error strings, SSE error payload
  detail, and generation-runtime call shape while making the context helper
  auditable from the actual neighboring typed owners instead of a premature
  string flattening.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_context_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_generation_runtime_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this runtime/context seam is now materially cleaner,
  but the immediate neighborhood should continue only when another caller is
  still demonstrably flattening the same shared access owner or re-owning the
  same compatibility string boundary. If the next move is only helper
  relocation or route-side request assembly cleanup, pivot instead of forcing
  more refactor volume here.

- 2026-05-21 12:57 +08:00 checkpoint:
  this slice pivoted into the nearby CRUD read-side query lane and removed one
  more duplicate chapter-access owner flattening. A focused audit of
  `chapter_annotation_query_service.rs` and `chapters_error_mapper.rs` showed
  that the annotations query did not own any real domain branch of its own: it
  only needed to prove chapter existence/access and then return the existing
  empty annotations envelope. However, it still restated the same access
  contract locally as `LoadAnnotationsPayloadError::{NotFound,Internal}` even
  though `load_accessible_chapter()` already owned the typed
  `LoadAccessibleChapterError::{NotFoundOrAccessDenied,Internal}` boundary and
  the route mapper immediately converted the local enum back into the same
  `404` / `500` chapter-access semantics. The refactor collapsed
  `LoadAnnotationsPayloadError` into a type alias of
  `LoadAccessibleChapterError`, changed the query helper to call
  `load_accessible_chapter()` directly before returning the existing payload,
  and updated `chapters_error_mapper.rs` to reuse
  `map_load_accessible_chapter_error()` for annotations just like the other
  access-owned chapter query paths. This preserves the existing annotations
  payload shape and chapter-not-found/internal HTTP behavior while making the
  query boundary auditable from one shared access owner instead of a duplicate
  local flat enum.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_annotation_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this CRUD-adjacent access lane still has some
  neighboring read helpers, but continue only where a helper is clearly
  restating `LoadAccessibleChapterError` or another existing shared owner
  without adding a real query-specific branch. If the next candidate depends
  on `ChapterService::navigation()` or other route-specific domain behavior,
  do not force it into the same pattern.

- 2026-05-21 13:09 +08:00 checkpoint:
  this slice returned to the neighboring analysis query lane and removed one
  more local flattening of the shared chapter-access owner from the single
  chapter status query path. A focused audit of
  `chapter_analysis_query_service.rs`,
  `chapter_analysis_service.rs`, and `chapters_error_mapper.rs` showed that
  `load_analysis_task_status_payload()` still loaded chapter access through
  `ChapterService::get()` and then restated the same access failure as a local
  `LoadAnalysisTaskStatusError::ChapterNotFound` branch before the route
  mapper translated it back into the same `"Chapter not found or access denied"`
  `404` semantics used elsewhere. That branch did not add a real status-query
  policy boundary; it was another duplicate access flattening sitting next to
  the already-shared `load_accessible_chapter()` owner. The refactor changed
  `LoadAnalysisTaskStatusError` to carry
  `Chapter(LoadAccessibleChapterError)` plus its existing real local
  `Internal(String)` branch, updated
  `load_analysis_task_status_payload()` to gate on
  `load_accessible_chapter()` directly, and taught
  `chapters_error_mapper.rs` to reuse `map_load_accessible_chapter_error()`
  for the shared access owner while keeping the existing internal error
  behavior. This preserves the single-chapter analysis status payload shape,
  `404` detail, and auto-recovery semantics, while leaving the batch status
  helper untouched because that path still has a separate compatibility rule
  for inaccessible chapter ids.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (7 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-chapter analysis query surface is now
  close to saturation. Continue only where another helper is still clearly
  re-owning `LoadAccessibleChapterError` without adding a real domain branch;
  do not broaden this pattern onto the batch status helper unless its special
  missing-chapter compatibility semantics are intentionally redesigned.

- 2026-05-21 13:18 +08:00 checkpoint:
  this slice pivoted into the neighboring CRUD workflow lane and removed one
  more repeated owner split inside the same module without changing any route
  response shapes. A focused audit of
  `chapter_crud_workflow_service.rs` and
  `chapter_crud_error_mapper.rs` showed that the workflow layer was still
  restating the exact same two error contracts multiple times:
  `ProjectNotFound/Internal` was defined separately for create/list/list-by-
  project-path, and `ChapterNotFound/Internal` was defined separately for
  get/update/delete/update-expansion-plan. Those local enums were not adding
  route-specific policy or distinct domain branches; they were seven copies of
  two owners inside one file, immediately funneled into the mapper layer. The
  refactor introduced shared workflow owners
  `ProjectCrudPayloadError` and `ChapterCrudPayloadError`, aliased the
  route-facing operation-specific error names to those owners, updated the
  workflow functions to construct the shared owners directly, and adjusted
  `chapter_crud_error_mapper.rs` to match the shared owners while preserving
  the existing split between `success/message` compat errors and `detail`
  errors for project-path routes. This keeps every CRUD payload, HTTP status,
  and error envelope unchanged while making the workflow-owner contracts
  auditable from two shared enums instead of seven duplicated copies.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_crud_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate CRUD workflow lane is now cleaner, but
  further work here should stop unless another candidate clearly removes a
  duplicated owner contract without forcing a broader rewrite of the legacy
  compat response shapes.

- 2026-05-21 13:31 +08:00 checkpoint:
  this slice stayed in the adjacent read-side query lane and removed one more
  duplicate owner split without changing any payload or route behavior. A
  focused audit of `chapter_query_service.rs` and
  `chapters_error_mapper.rs` showed that the navigation and can-generate
  helpers were still defining two separate local enums with the exact same
  owner contract: both `LoadNavigationPayloadError` and
  `LoadCanGeneratePayloadError` only represented `NotFound` plus
  `Internal(String)`, and neither helper added any route-specific or
  domain-specific branch beyond that shared read-side boundary. The mapper
  layer immediately translated both enums back into the same `404` chapter
  not found/access denied semantics and `500` internal detail semantics, so
  the split was not carrying a real domain seam. The refactor introduced a
  single shared owner `ChapterQueryPayloadError { NotFound, Internal(String) }`,
  aliased both operation-specific error names to that owner, updated
  `load_navigation_payload()` and `load_can_generate_payload()` to construct
  the shared owner directly, and taught `chapters_error_mapper.rs` to match
  the shared owner for both route helpers. This preserves the navigation
  payload, can-generate payload, and existing `404` / `500` route semantics
  while making the read-side owner boundary auditable from one shared enum
  instead of two flat duplicates in the same module.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (9 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this immediate read-side query lane is now close to
  the stop-rule. Continue only if another neighboring helper is still clearly
  duplicating an already-shared typed owner without adding a real local domain
  branch; otherwise pivot instead of forcing route-side cleanup or wrapper
  compression.

- 2026-05-21 14:06 +08:00 checkpoint:
  this slice returned to the batch-generation route mapper lane and removed one
  last repeated route-side flattening of the same owned-task owner without
  changing any workflow or payload behavior. A focused audit of
  `chapter_batch_generation_error_mapper.rs` showed that four neighboring route
  helpers were still matching the exact same
  `LoadOwnedBatchGenerationTaskError::{TaskNotFound,Internal}` contract
  separately: cancel, resume, status query, and status stream access all
  translated `TaskNotFound` into the same `"Batch generation task not found"`
  `404` response and `Internal(String)` into the same `500` detail response.
  Those matches did not add route-specific policy; they were duplicate
  transport mappings of one already-shared owner. The refactor introduced one
  local helper `map_owned_batch_generation_task_route_error()`, rewired the
  four neighboring route mappers to delegate to that helper, and added focused
  tests proving the shared `404` and `500` behavior stays unchanged. This keeps
  batch cancel/resume/status HTTP behavior, SSE access rejection behavior, and
  task lookup ownership exactly as before while making the route-side contract
  auditable from one shared transport mapping instead of four repeated
  match blocks.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (14 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate batch-generation route-mapper lane is
  now at the stop-rule. Continue only if another candidate still removes a
  real duplicated owner contract rather than just compressing wrappers,
  renaming helpers, or reformatting transport code.

- 2026-05-21 14:18 +08:00 checkpoint:
  this slice pivoted into the nearby CRUD route-mapper lane and removed one
  more repeated transport mapping of already-shared workflow owners without
  changing any response envelopes. A focused audit of
  `chapter_crud_error_mapper.rs` and
  `chapter_crud_workflow_service.rs` showed that the mapper layer was still
  restating the same two shared success-message contracts multiple times after
  the workflow layer had already been deduplicated:
  `ProjectCrudPayloadError::{ProjectNotFound,Internal}` was matched separately
  for create and list, and
  `ChapterCrudPayloadError::{ChapterNotFound,Internal}` was matched separately
  for get, update, delete, and update-expansion-plan, all producing the same
  compat `success/message` error envelopes. Those repeated matches did not add
  route-specific policy; the only neighboring exception was the
  `list_by_project_path` route, which intentionally keeps its distinct `detail`
  error shape and therefore stayed separate. The refactor introduced
  `map_project_crud_success_message_error()` and
  `map_chapter_crud_success_message_error()`, rewired the compat success-message
  routes to delegate to those helpers, preserved the project-path `detail`
  branch unchanged, and added focused tests proving the shared not-found shapes
  remain stable. This keeps CRUD HTTP status codes and envelope compatibility
  unchanged while making the success-message transport mapping auditable from
  the actual shared owners instead of six repeated match blocks.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_crud_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (5 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate CRUD route-mapper lane is now also at
  the stop-rule. Continue only if another candidate removes a clearly duplicated
  owner contract while preserving an existing compat boundary; do not broaden
  this into generic mapper compression or response-shape rewrites.

- 2026-05-21 14:31 +08:00 checkpoint:
  this slice returned to the shared chapter route-mapper lane and removed one
  more duplicate transport mapping of an already-deduplicated read-side owner.
  A focused audit of `chapters_error_mapper.rs` and
  `chapter_query_service.rs` showed that
  `LoadNavigationPayloadError` and `LoadCanGeneratePayloadError` had already
  been collapsed into the shared `ChapterQueryPayloadError`, but the route
  mapper layer still matched that same `NotFound/Internal` contract twice:
  once in `map_load_navigation_payload_error()` and once in
  `map_load_can_generate_payload_error()`, both translating it into the same
  `"Chapter not found or access denied"` `404` response and the same `500`
  internal detail response. Those matches did not add route-specific policy;
  they were duplicate transport mappings of a single shared owner. The
  refactor introduced `map_chapter_query_payload_error()`, rewired both query
  route helpers to delegate to it, and added focused tests proving the shared
  `404` / `500` semantics remain stable. This keeps navigation/can-generate
  payloads and HTTP behavior unchanged while making the shared chapter-query
  transport contract auditable from one mapper instead of two repeated match
  blocks.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate shared chapter route-mapper lane is
  now also close to the stop-rule. Continue only where another candidate still
  removes a clearly duplicated shared-owner transport mapping without broadening
  into generic consolidation or behavior-adjacent response rewrites.

- 2026-05-21 14:46 +08:00 checkpoint:
  this slice stayed in the neighboring analysis task transport lane and removed
  one more duplicate mapping of an already-shared domain owner across route
  files. A focused audit of `chapters_error_mapper.rs` and `memories.rs`
  showed that `CreateChapterAnalysisTaskError::{ChapterEmpty,ProjectMissing,Internal}`
  was already the shared task-creation owner used by both the chapter analysis
  trigger path and the memory-triggered analysis path, but the transport layer
  still mapped it twice: once nested inside
  `map_prepare_chapter_analysis_trigger_error()` and once locally inside
  `analyze_chapter_memories()`, both producing the same `400` / `404` / `500`
  detail responses. Those mappings did not add route-specific policy; they were
  duplicate transport translations of one shared owner. The refactor introduced
  `map_create_chapter_analysis_task_error()` in `chapters_error_mapper.rs`,
  rewired `map_prepare_chapter_analysis_trigger_error()` to delegate its create
  branch to that helper, and updated `memories.rs` to reuse the same mapper for
  `enqueue_chapter_analysis_task()` failures. This keeps chapter analysis
  trigger behavior, memories analysis trigger behavior, and all existing error
  payloads unchanged while making the task-creation transport contract auditable
  from one shared mapper instead of two separate match blocks.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (12 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate analysis task transport lane is now
  also near the stop-rule. Continue only if another candidate still duplicates
  an already-shared owner mapping across neighboring route boundaries; do not
  broaden this into unrelated route normalization or access-policy cleanup.

- 2026-05-21 15:04 +08:00 checkpoint:
  this slice pivoted back into the analysis draft service lane and removed one
  more duplicate owner definition inside the same neighboring module without
  changing mapper behavior or payload semantics. A focused audit of
  `chapter_analysis_draft_service.rs` and
  `chapter_analysis_draft_error_mapper.rs` showed that the owned draft service
  layer was still defining four local error enums even though they collapsed
  into only two actual owner contracts:
  auto-revision load/apply both carried
  `Chapter(LoadAccessibleChapterError)` plus
  `Draft(AutoRevisionDraftError, explicit_id_provided)`, and candidate
  load/apply both carried
  `Chapter(LoadAccessibleChapterError)` plus
  `Draft(CandidateDraftError, explicit_id_provided)`. Those pairs were not
  adding action-specific owner branches; the action-specific behavior already
  lives in the mapper functions that interpret the shared draft-domain error
  together with the load/apply context. The refactor introduced
  `OwnedAutoRevisionDraftPayloadError` and
  `OwnedCandidateDraftPayloadError`, aliased the existing load/apply error
  names to those shared owners, and added focused tests proving the load/apply
  call sites still see the same shared contract. This keeps all draft route
  payloads, chapter access handling, explicit-id-sensitive `404` messaging, and
  stale/preview/workflow-meta behavior unchanged while making the owned draft
  service contract auditable from two shared enums instead of four duplicated
  definitions.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate analysis draft service lane is now
  also close to the stop-rule. Continue only if another candidate still removes
  a clearly duplicated owner contract, not if it merely compresses mapper
  helpers or load/apply wrappers.

- 2026-05-21 15:18 +08:00 checkpoint:
  this slice stayed in the neighboring regeneration transport lane and removed
  one more duplicate mapper translation of an already-shared owner without
  changing any HTTP error semantics. A focused audit of
  `chapters_error_mapper.rs` showed that
  `BuildRegenerationAiServiceError::InvalidConfig(detail)` was already the
  shared config owner used by both the full-regeneration stream path and the
  partial-regeneration stream `Config(...)` branch, but the transport layer was
  still translating that same owner twice into the same `400 detail` response:
  once through `map_prepare_chapter_regeneration_stream_error()` and once in
  the `PreparePartialRegenerationStreamError::Config(...)` match arm. Those
  mappings did not add route-specific policy; they were duplicate transport
  translations of one shared owner. The refactor introduced
  `map_build_regeneration_ai_service_error()`, rewired
  `map_prepare_chapter_regeneration_stream_error()` to delegate directly to
  that helper, and updated the partial-regeneration config branch to reuse the
  same mapper. This keeps
  `PrepareChapterRegenerationStreamError::InvalidConfig(detail)` and
  `PreparePartialRegenerationStreamError::Config(InvalidConfig(detail))`
  returning the same `400` detail payloads as before while making the
  regeneration config transport contract auditable from one shared mapper
  instead of two repeated translations.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapters_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (13 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate regeneration transport lane is now
  also close to the stop-rule. Continue only if another neighboring candidate
  still removes a clearly duplicated shared-owner transport mapping; do not
  broaden this lane into helper compression or route-only cleanup.

- 2026-05-21 16:02 +08:00 checkpoint:
  this slice pivoted back into the batch-generation request-owner lane and
  removed one more route-local owner flattening without changing any create
  payload semantics. A focused audit of
  `chapter_batch_generation.rs` and
  `chapter_batch_generation_request_compat_service.rs` showed that the batch
  create route was still expanding a large compat request shape field-by-field
  into `build_batch_generation_create_workflow_request_from_route(...)`, even
  though the actual ownership of that route-compatible request contract already
  belonged in the adjacent request compat service. The route did not add any
  business policy with those 18 arguments; it was just re-owning the transport
  payload shape locally before handing it back to the same service lane. The
  refactor introduced `BatchGenerationCreateRouteRequest` plus
  `build_batch_generation_create_workflow_request_from_route_payload()` inside
  `chapter_batch_generation_request_compat_service.rs`, moved compat-field
  consumption and normalized request construction behind that owner boundary,
  and updated `chapter_batch_generation.rs` to deserialize directly into the
  shared route-payload type and delegate request assembly as one unit. This
  keeps batch create defaults, compat-only ignored fields, and downstream
  workflow behavior unchanged while making the route-compatible request shape
  auditable from one service owner instead of a large route-local flattening
  call.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_request_compat_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this batch create request-owner lane still has some
  remaining neighboring route/request boundaries, but continue only if the next
  move still removes a real route-local contract owner. Do not broaden it into
  wrapper-only compression or signature churn once the standard route payload
  shape is already owned by the compat service.

- 2026-05-21 16:19 +08:00 checkpoint:
  this slice stayed in the neighboring regeneration request-owner lane and
  removed one more route-local payload owner without changing partial
  regeneration behavior. A focused audit of
  `chapter_regeneration_routes.rs` and
  `chapter_regeneration_request_service.rs` showed that the partial apply route
  and partial stream route were still each holding their own transport payload
  structs and then flattening those fields back into the adjacent request
  builders. Those route-local payload owners did not add any business policy;
  they were just re-stating the standard route-compatible request shape before
  handing it to the same request service lane. The refactor introduced
  `ApplyPartialRegenerateRouteRequest`,
  `PartialRegenerationStreamRouteRequest`,
  `build_apply_partial_regenerate_request_from_route_payload()`, and
  `build_partial_regeneration_stream_workflow_request_from_route_payload()`
  inside `chapter_regeneration_request_service.rs`, then updated
  `chapter_regeneration_routes.rs` to deserialize directly into those shared
  route-payload types and delegate request construction as one unit. This keeps
  partial apply defaults, partial stream context/style/research fields, and all
  downstream workflow behavior unchanged while making the route-compatible
  request shapes auditable from one request owner instead of two route-local
  payload definitions plus field-by-field flattening calls.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate regeneration request-owner lane is now
  much closer to the stop-rule. Continue only if another neighboring move still
  removes a real route-local request contract owner; do not broaden this lane
  into wrapper-only request helpers or route signature churn once the standard
  payload shapes are already owned by the request service.

- 2026-05-21 16:34 +08:00 checkpoint:
  this slice pivoted into the neighboring CRUD request-owner lane and removed
  three more route-local payload owners without changing any CRUD behavior. A
  focused audit of `chapter_crud_routes.rs` and
  `chapter_crud_request_service.rs` showed that the create, update, and
  expansion-plan endpoints were still each holding their own transport payload
  structs in the route file and then flattening those fields back into the
  adjacent request builders. Those route-local payload owners did not add any
  business policy; they were just re-stating the standard route-compatible
  request shapes before handing them to the same request service lane. The
  refactor introduced `CreateChapterRouteRequest`,
  `UpdateChapterRouteRequest`, and `UpdateExpansionPlanRouteRequest` plus their
  corresponding `*_from_route_payload()` builders inside
  `chapter_crud_request_service.rs`, then updated
  `chapter_crud_routes.rs` to deserialize directly into those shared payload
  types and delegate request construction as one unit. This keeps create/update
  field semantics, optional expansion-plan behavior, and downstream CRUD
  workflow behavior unchanged while making the route-compatible request shapes
  auditable from one request owner instead of three route-local payload
  definitions plus field-by-field flattening calls.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_crud_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate CRUD request-owner lane is now much
  closer to the stop-rule. Continue only if another neighboring move still
  removes a real route-local request contract owner; do not broaden this lane
  into wrapper-only request helpers or route signature churn once the standard
  CRUD payload shapes are already owned by the request service.

- 2026-05-21 16:47 +08:00 checkpoint:
  this slice returned to the single-generation request-owner lane and removed
  one more shared route-local payload owner without changing background or
  stream behavior. A focused audit of `chapter_batch_generation.rs` and
  `chapter_single_generation_request_service.rs` showed that the
  `generate-background` and `generate-stream` endpoints were still sharing one
  local `ChapterGenerateRequest` transport payload struct inside the route file
  before flattening its fields back into the adjacent request builder. That
  route-local payload owner did not add any business policy; it was just
  re-stating the standard route-compatible single-generation request shape for
  two neighboring handlers before handing it to the same request service lane.
  The refactor introduced `SingleChapterGenerationRouteRequest` plus
  `build_single_chapter_generation_request_from_route_payload()` inside
  `chapter_single_generation_request_service.rs`, then updated
  `chapter_batch_generation.rs` to deserialize both handlers directly into the
  shared request-owner payload and delegate request construction as one unit.
  This keeps target-word-count normalization, model override wiring,
  compat-only `enable_analysis` consumption, and downstream background/stream
  workflow behavior unchanged while making the shared route-compatible request
  shape auditable from one request owner instead of one route-local payload
  definition plus two neighboring flattening calls.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (8 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate single-generation request-owner lane is
  now much closer to the stop-rule. Continue only if another neighboring move
  still removes a real shared route-local contract owner; do not broaden this
  lane into wrapper-only helper compression once the standard route payload
  shape is already owned by the request service.

- 2026-05-21 17:27 +08:00 checkpoint:
  this slice returned to the neighboring analysis draft request-owner lane and
  finished the typed-route-contract move cleanly by removing the obsolete raw
  parser path that had become dead after the previous request-owner extraction.
  A focused audit of
  `chapter_analysis_draft_request_service.rs`,
  `chapter_analysis_draft_service.rs`, and
  `chapter_analysis_routes.rs` showed that the four draft endpoints were
  already delegating through typed route query/body contracts owned by the
  request service, but the old `HashMap<String, String>` / `serde_json::Value`
  parser helpers were still present beside the new owners and had started to
  produce fresh dead-code warnings. Those functions no longer represented a
  real compatibility lane; they were stale route-adjacent owners left behind
  after the typed contract move. The refactor removed the obsolete parser
  functions plus their helper/default-parsing tests, kept the typed route
  builder coverage, and preserved the existing trim-and-empty normalization of
  `history_id` / `attempt_id` together with the `allow_stale=false` default.
  This keeps all four analysis draft endpoints transport-compatible while
  making `chapter_analysis_draft_request_service.rs` the single owner of the
  route-facing request contract instead of a dual typed/raw boundary.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate analysis draft request-owner lane is
  now at the stop-rule. Continue only if another candidate still removes a
  real shared route-local contract owner; do not reopen this lane for parser
  shape cleanup, helper moves, or typed-wrapper normalization.

- 2026-05-21 17:46 +08:00 checkpoint:
  this slice pivoted into the neighboring `memories` route group and removed
  one more still-valid cluster of route-local request owners without changing
  any query execution or response payload semantics. A focused audit of
  `backend-rs/src/api/memories.rs` showed that this route group still held
  three standard route-facing contracts locally:
  `MemoryListQuery`, `ForeshadowQuery`, and the search body parsing logic built
  inline from raw `Json<Value>`. Unlike the already-near-stop-rule batch /
  CRUD / single-generation lanes, this was still a small cluster of
  route-local request normalization in one neighboring module rather than a
  single thin wrapper. The refactor introduced
  `memories_request_service.rs` with
  `MemoryListRouteQuery`, `ForeshadowListRouteQuery`,
  `SearchMemoriesRouteRequest`, and the corresponding
  `build_*_from_route_query()` / `build_*_from_route_payload()` owners. The
  route now delegates query/body normalization through that service while
  keeping the actual SeaORM query execution and existing compat response
  payloads in place. Search still accepts weakly typed `Json<Value>` at the
  HTTP boundary so legacy payload leniency is preserved, but the trim/default
  semantics for `query`, `limit`, `min_importance`, and `memory_types` now
  belong to one request-owner service instead of being rebuilt inline in the
  route. This keeps memory list limit clamping, search defaults, foreshadow
  filtering, and response envelopes unchanged while making the route-facing
  contract auditable from one owner file.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test memories_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the broad Phase 5 request-owner line is now very
  close to the stop-rule. The most visible remaining raw route body on the
  adjacent chapter lanes is the full `regenerate-stream` single-endpoint
  `Json<Value>` boundary, which is no longer a strong grouped seam. Continue
  only if the next move still removes a clearly repeated/shared contract owner;
  otherwise pivot to verification/spec work or a different higher-signal
  Phase 5 lane.

- 2026-05-21 18:19 +08:00 checkpoint:
  this slice stayed in the neighboring `memories` route group but moved one
  layer deeper than the earlier request-owner pass by removing a still-valid
  cluster of route-local read-side access/query owners without changing any
  payload shapes or write-path behavior. A focused audit of
  `backend-rs/src/api/memories.rs` showed that, even after request contract
  extraction, five read endpoints still owned their own project-access gate
  plus SeaORM query assembly inline:
  project memories list, chapter analysis view, search memories, unresolved
  foreshadows, and memory stats. That was still a real neighboring route
  seam, because the route layer was mixing transport wiring with access checks,
  query composition, and payload construction across multiple handlers in the
  same route group. The refactor introduced
  `backend-rs/src/services/memories_query_service.rs`, moved the shared
  project-access ownership and those five read-side query/payload flows behind
  owned service helpers, and updated `backend-rs/src/api/memories.rs` so those
  handlers now keep request parsing and route-local error mapping only. The
  existing Chinese compat `detail` messages for
  `项目不存在或无权限` and `该章节还未进行分析` were preserved locally in the
  route mapper rather than being rewritten through a generic chapter/project
  mapper with different wording. Write-side handlers
  (`analyze_chapter_memories` and `delete_chapter_memories`) were intentionally
  left in place so this slice stayed in one low-risk read-side lane.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test memories_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed).
  Updated lane assessment: the immediate `memories` read-side lane is now much
  closer to the stop-rule as well. The remaining write-side pieces are no
  longer a clean grouped read/query seam, and the chapter-regeneration full
  stream raw-body boundary remains only a single endpoint. Continue only if
  the next move still removes a concrete shared behavior owner rather than
  forcing one-off route compression.

- 2026-05-21 18:42 +08:00 checkpoint:
  this slice returned to the chapter-regeneration main line and tightened the
  remaining full-stream raw-body contract without changing the public HTTP
  boundary or SSE behavior. A focused audit of
  `chapter_regeneration_routes.rs`,
  `chapter_regeneration_stream_workflow_service.rs`, and
  `chapter_regeneration_prepare_service.rs` showed that the route had already
  been narrowed to one transport-only `Json<Value>` entrypoint, but the
  adjacent workflow and prepare layers were still carrying that same weakly
  typed payload through multiple internal boundaries and re-reading the
  standard regeneration fields with repeated `body.get(...)` access. That was
  still a real owner seam even though only one endpoint remained, because the
  standard full-regeneration request contract was not yet owned anywhere in
  the service lane. The refactor introduced
  `FullChapterRegenerationStreamRequest` plus
  `build_full_chapter_regeneration_stream_request_from_route_payload()` inside
  `chapter_regeneration_request_service.rs`, updated the route to normalize the
  weakly typed body once at the HTTP boundary, and changed the workflow /
  prepare services to consume the typed internal request owner instead of raw
  `serde_json::Value`. Compatibility semantics were preserved intentionally:
  the HTTP handler still accepts `Json<Value>`, `selected_suggestion_indices`
  still keeps only numeric values, focus/repair/preserve lists still keep only
  strings, `target_word_count` still defaults to `3000`, and the generated
  prompt text remains unchanged for equivalent inputs.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (8 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_prepare_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (11 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the chapter-regeneration request/prepare boundary is
  now also near the stop-rule. The remaining route-visible raw `Json<Value>`
  is only a weak transport compatibility shell, and forcing it into a stricter
  HTTP type would risk changing request-validation behavior rather than
  removing a shared internal owner.

- 2026-05-21 19:02 +08:00 checkpoint:
  this slice stayed in the neighboring `memories` read-side transport lane and
  removed one more real shared-owner duplication without touching write-path
  behavior or changing any existing Chinese compat messages. A focused audit of
  `backend-rs/src/api/memories.rs` after the earlier request-owner and
  read/query-owner passes showed that five read-side route mappers were still
  translating the same `LoadProjectAccessError` owner into the same transport
  response contract locally:
  `项目不存在或无权限` for
  `NotFoundOrAccessDenied` plus `internal_detail_error(...)` for
  `Internal(...)`. That was still a real neighboring seam rather than thin
  wrapper churn, because the same shared owner mapping was duplicated across
  project memories, chapter analysis, search memories, unresolved
  foreshadows, and memory stats. The refactor introduced
  `backend-rs/src/api/memories_error_mapper.rs`, moved the shared project
  access transport mapping there, preserved the route-specific
  `该章节还未进行分析` branch for analysis-not-found, and updated
  `backend-rs/src/api/memories.rs` to reuse the owned mapper helpers instead
  of re-translating the same access owner five times inline. This keeps the
  existing HTTP status codes, JSON `detail` payload shape, and Chinese compat
  wording unchanged while making the `memories` read-side transport contract
  auditable from one owner file instead of a repeated cluster in the route.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test memories_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test memories_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `memories` read-side transport lane is
  now also at or very near the stop-rule. The remaining write-side handlers do
  not form another clean grouped shared-owner seam, so the next move should
  pivot to verification/spec work or a different route group instead of
  forcing more local compression in `memories`.

- 2026-05-21 19:15 +08:00 checkpoint:
  this slice pivoted out of the now-near-stop-rule `chapters` lanes and
  tightened one still-valid shared owner in the neighboring `projects` route
  group without changing export payloads, filenames, or import/consistency
  behavior. A focused audit of `backend-rs/src/api/projects.rs` showed that
  the two export endpoints, `export_project_data` and `export_project_txt`,
  were still each owning the same project-access plus ordered-chapter-load
  context locally before diverging into their route-specific response formats.
  That was still a real grouped seam rather than cosmetic cleanup, because the
  shared export context contract lived twice in the route layer and could drift
  independently while both handlers were supposed to export the same accessible
  project snapshot. The refactor introduced
  `backend-rs/src/services/project_export_query_service.rs` with
  `ProjectExportContext`,
  `load_project_export_context()`, and
  `load_project_export_context_with_non_empty_chapters()`, then updated
  `backend-rs/src/api/projects.rs` so both export handlers reuse that owned
  service context and a small route mapper for
  `ProjectNotFound` / `ProjectHasNoChapters` / `Internal(...)`. This keeps the
  existing `Project not found` and `Project has no chapters` detail messages,
  ordered chapter export behavior, JSON export shape, TXT export content, and
  filename logic unchanged while making the shared export-access boundary
  auditable from one owner module instead of two repeated route-local flows.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test project_export_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `projects` export lane is now much
  closer to the stop-rule as well. Continue here only if another neighboring
  move still removes a real shared project-access or export/import contract
  owner; do not broaden this lane into large CRUD/service migration or mixed
  import workflow cleanup in the same batch.

- 2026-05-21 19:23 +08:00 checkpoint:
  this slice stayed outside the now-near-stop-rule `chapters` lanes and
  tightened one still-valid shared runtime-config owner in the neighboring
  `settings` route group without changing any API response shape, provider
  fetch flow, or preset storage behavior. A focused audit of
  `backend-rs/src/api/settings.rs` showed that four neighboring endpoints,
  `get_available_models`, `test_api_connection`, `fetch_models_endpoint`, and
  `check_function_calling`, were all still carrying the same route-local
  effective-settings resolution contract:
  stored settings lookup, explicit override precedence, API key requirement,
  provider-specific base URL normalization, default model fallback, and default
  temperature/max-token behavior. That was still a real grouped seam rather
  than helper churn, because the same runtime-config rules sat in the route
  file while adjacent provider/default logic also existed around
  `settings_service.rs`, creating a drift risk for probe behavior over time.
  The refactor introduced
  `backend-rs/src/services/settings_runtime_config_service.rs` with
  `EffectiveSettingsOverrides`,
  `ResolvedEffectiveSettings`, and
  `resolve_effective_runtime_settings()`, moved the shared provider/base-url/
  model normalization there, exposed the OpenAI-compatible base URL helper for
  the existing Grok probe branch, and updated the four endpoints in
  `backend-rs/src/api/settings.rs` to consume the owned runtime-config result
  instead of resolving those rules inline. This keeps the existing
  `API key is required` bad-request behavior, OpenAI-compatible `/v1`
  normalization, Anthropc/Gemini default base URLs, and runtime default model
  choices unchanged while making the effective runtime-settings contract
  auditable from one owner module instead of a repeated route-local flow.
  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test settings_runtime_config_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed)
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `settings` runtime-config lane is now
  significantly tighter as well. Continue here only if another neighboring move
  still removes a real shared settings contract owner; do not broaden this lane
  into preset persistence redesign, provider fetch rewrites, or whole-route
  service migration in the same batch.

- 2026-05-21 20:10 +08:00 checkpoint:
  this slice stayed inside the neighboring `projects` route group and removed
  one more real grouped owner from the consistency lane without changing any
  existing response payload shape, detail wording, or fix semantics. A focused
  audit of `backend-rs/src/api/projects.rs` showed that the three adjacent
  consistency endpoints, `fix_project_organizations`,
  `fix_project_member_counts`, and `check_project_consistency`, still shared
  the same route-local project-access boundary:
  verify the project exists for the current user, map that owner into the same
  `Project not found` / internal `detail` transport contract, and in the
  no-auto-fix branch of the checker also own the same project-scoped
  organization totals inline. That was still a valid grouped seam rather than
  helper churn, because the same access/stat-count contract lived across three
  neighboring handlers and could drift independently while all three were meant
  to operate on the same project consistency snapshot. The refactor introduced
  `backend-rs/src/services/project_consistency_query_service.rs` with
  `LoadProjectConsistencyContextError`,
  `ProjectConsistencyCounts`,
  `ensure_project_consistency_access()`, and
  `load_project_consistency_counts()`, then updated
  `backend-rs/src/api/projects.rs` so the two direct fix endpoints reuse the
  owned access boundary and the checker reuses the owned counts for the
  `auto_fix=false` path instead of re-querying those project totals inline.
  This keeps the existing `Project not found` detail message, JSON response
  shape for all three endpoints, and auto-fix behavior unchanged while making
  the consistency access/count contract auditable from one owner module instead
  of a repeated cluster in the route file. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test project_consistency_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `projects` consistency lane is now
  also much closer to the stop-rule. Continue here only if another neighboring
  move removes a real grouped project-consistency owner; do not broaden this
  lane into import workflow cleanup or larger project CRUD/service migration in
  the same batch.

- 2026-05-21 20:36 +08:00 checkpoint:
  this slice pivoted to the neighboring `wizard` route group and removed one
  still-valid grouped request/invocation owner that had been split across both
  interactive SSE routes and background-task execution paths without changing
  any HTTP request shape, SSE event timing, or task-sync behavior. A focused
  audit of `backend-rs/src/api/wizard.rs` plus
  `backend-rs/src/api/background_tasks.rs` showed that five related wizard
  flows, `world_building`, `career_system`, `characters`, `outline`, and
  `regenerate_world_building`, were each still carrying the same route-local
  or runner-local invocation boundary:
  deserialize the same wizard request contract, resolve effective `user_id`,
  normalize world-building genre transport input, and fan those fields into the
  same `wizard_service::*` call signatures with repeated provider/model
  passthrough. That was still a real grouped seam rather than SSE helper churn,
  because the same request/invocation owner lived twice across interactive and
  background execution lanes and could drift independently while both were
  supposed to execute the same wizard semantics. The refactor introduced
  `backend-rs/src/services/wizard_request_service.rs` with the shared wizard
  request structs, `resolve_effective_user_id()`,
  `normalize_genre_input()`, and the narrow execution helpers
  `execute_*_request()`, then updated
  `backend-rs/src/api/wizard.rs` so the SSE routes keep owning only channel
  creation and spawn behavior while `backend-rs/src/api/background_tasks.rs`
  reuses the same invocation owner for captured task execution. This keeps the
  existing request aliases, provider/model passthrough, world-building genre
  normalization behavior, background-task payload compatibility, and SSE/result
  synchronization unchanged while making the shared wizard request boundary
  auditable from one owner module instead of two repeated clusters. Validation
  passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test wizard_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `wizard` request/invocation lane is
  now materially tighter, but not obviously at the stop-rule yet. Continue here
  only if the next move still removes a grouped route-facing request or stream
  contract owner without collapsing into generic SSE wrapper compression or
  changing event semantics.

- 2026-05-21 20:49 +08:00 checkpoint:
  this slice stayed in the same neighboring `wizard` execution lane and
  extended the newly-owned request/invocation boundary to adjacent legacy
  routes that were still bypassing it, without changing any legacy endpoint
  shape, captured result payload, or SSE transport behavior. A focused audit of
  `backend-rs/src/api/careers.rs` and `backend-rs/src/api/outlines.rs` after
  the previous `wizard_request_service` pass showed that two neighboring entry
  points were still carrying compatible copies of the same invocation owner:
  the legacy career-system SSE route still resolved effective `user_id` and
  provider/model passthrough inline before calling
  `wizard_service::generate_career_system()`, while the outline generation
  route still cloned the same route-facing outline execution fields into a
  local batch of variables before calling `wizard_service::generate_outline()`
  through a captured `SseChannel`. That was still a real grouped seam rather
  than opportunistic cleanup, because both routes were using the same wizard
  invocation contracts already owned by `wizard_request_service`, but had not
  yet converged on that owner and could still drift from the interactive
  wizard/background-task paths. The refactor extended
  `backend-rs/src/services/wizard_request_service.rs` with
  `legacy_career_system_query_to_request()` and
  `outline_generate_request_to_wizard_request()`, added focused adapter tests,
  updated `backend-rs/src/api/careers.rs` so the legacy SSE route now reuses
  the owned effective-user and career-system request boundary, and updated
  `backend-rs/src/api/outlines.rs` so the captured outline generation path now
  reuses the owned outline request/invocation boundary instead of carrying a
  local field-clone cluster. This keeps the legacy query contract, provider/
  model passthrough, outline result-capture behavior, and response payload
  shape unchanged while making the wizard invocation contract more consistently
  auditable from one owner module. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test wizard_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `wizard` invocation lane is now
  meaningfully closer to the stop-rule. Continue here only if the next move
  still removes a grouped route-facing request/result contract owner; do not
  broaden this lane into generic outline flow redesign, route consolidation,
  or SSE utility abstraction in the same batch.

- 2026-05-21 21:18 +08:00 checkpoint:
  this slice pivoted out of the now-near-stop-rule `wizard` lane and tightened
  one still-valid grouped generation owner in the neighboring `characters`
  route group without changing request shape, response payload shape, or
  background-task behavior. A focused audit of
  `backend-rs/src/api/characters.rs` after the `wizard_request_service` work
  showed that the synchronous `generate_character` route was still carrying a
  full copy of the same character-generation semantics already owned by
  `generate_character_task()` and already reused by
  `backend-rs/src/api/background_tasks.rs` for `character_generate`:
  project access, prompt-template/context assembly, provider/model runtime
  resolution, JSON clean/parse flow, career payload validation, persisted extra
  field updates, career sync, and final enriched payload load. That remained a
  real grouped seam rather than helper churn, because one generation owner was
  still duplicated across the interactive HTTP route and the background task
  execution path and could drift independently while both were supposed to
  produce the same generated character payload. The refactor kept the owner in
  `backend-rs/src/api/characters.rs` for now but narrowed it into an explicit
  shared boundary by introducing `GenerateCharacterTaskError`,
  `map_generate_character_task_error()`, and making `generate_character_task()`
  the single generation implementation used by both the background task path
  and the synchronous route. The route now owns only transport mapping, while
  `backend-rs/src/api/background_tasks.rs` bridges the shared owner error back
  to the existing task-string contract with `to_string()`. This keeps the
  existing `项目不存在或无权限` / `角色不存在或无权限` behavior, bad-request /
  bad-gateway / internal status mapping, and generated payload shape unchanged
  while making the character-generation semantics auditable from one owner
  function instead of two parallel copies. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test generate_character_task --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (0 focused tests matched, compile+test harness passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `characters` generation lane is now
  materially tighter. Continue here only if another neighboring move removes a
  grouped character/organization generation owner; do not broaden this lane
  into prompt-template redesign, full organization-generation service
  extraction, or generic AI generation abstractions in the same batch.

- 2026-05-21 21:33 +08:00 checkpoint:
  this slice stayed in the neighboring character/organization generation lane
  but moved one level outward to remove a still-valid grouped background-task
  request-adapter owner, without changing any task payload contract, generated
  request shape, or downstream generation behavior. A focused audit of
  `backend-rs/src/api/background_tasks.rs` after the character-generation owner
  pass showed that `run_character_generate()` and `run_organization_generate()`
  were still each carrying the same local payload-adaptation pattern:
  build a tiny `json!({...})` object from the background task payload, inject
  `record.project_id`, deserialize it into a typed generation request, and map
  the same deserialization failure into task-local Chinese error text. That was
  still a real grouped seam rather than helper churn, because the same
  task-payload-to-request contract lived twice in one route group and sat right
  beside two already-ownerized generation entry points,
  `generate_character_task()` and `generate_organization_task()`. The refactor
  introduced
  `backend-rs/src/services/generation_task_request_adapter_service.rs` with
  `adapt_character_generation_task_request()` and
  `adapt_organization_generation_task_request()`, added focused adapter tests,
  and updated `backend-rs/src/api/background_tasks.rs` so both generation task
  runners now reuse the same request adapter owner instead of each building and
  deserializing their own small JSON object inline. This keeps the existing
  background task payload keys, `record.project_id` injection, Chinese invalid-
  parameter wording, and downstream generation invocation behavior unchanged
  while making the generation task request-adapter contract auditable from one
  owner module instead of two repeated clusters in the route file. Validation
  passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test generation_task_request_adapter_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate generation task-adapter lane is now
  much tighter as well. Continue here only if the next move still removes a
  grouped generation request/transport owner; do not broaden this lane into
  full organization-generation extraction, prompt assembly unification, or
  generic task payload frameworks in the same batch.

- 2026-05-21 16:08 +08:00 checkpoint:
  this slice pivoted to the neighboring `outline expansion` lane and removed
  one still-valid duplicated runtime/provider owner without changing any HTTP
  payload shape, background-task payload contract, or expansion result
  semantics. A focused audit of `backend-rs/src/api/outlines.rs`,
  `backend-rs/src/api/background_tasks.rs`, and
  `backend-rs/src/services/plot_expansion_service.rs` showed that the two
  route-facing entry points, `expand_outline_to_chapters_compat` and
  `batch_expand_outlines_compat`, plus the two background-task runners,
  `run_outline_expand()` and `run_outline_batch_expand()`, were already acting
  as explicit runtime-injection boundaries by resolving
  `SettingsService::build_ai_config(...)`, constructing `AIService`, and then
  handing that runtime into `create_plot_expansion_service(&ai_service)`.
  However, both service-owned execution owners, `expand_outline()` and
  `batch_expand_outlines()`, still immediately rebuilt the same
  `AIConfig -> AIService -> PlotExpansionService` chain internally before
  calling `analyze_outline_for_chapters()`. That was still a real grouped seam
  rather than local cleanup, because the same runtime/provider ownership lived
  twice across four transport callers and two service execution owners, making
  the service boundary look injected while still hiding a second runtime
  bootstrap path internally. The refactor removed the internal
  `SettingsService::build_ai_config(...)` and nested `PlotExpansionService`
  reconstruction from `backend-rs/src/services/plot_expansion_service.rs`, so
  both `expand_outline()` and `batch_expand_outlines()` now reuse
  `self.ai_service` and keep runtime/provider ownership at the outer explicit
  injection boundary only. This keeps the existing outline access checks,
  provider/model passthrough, chapter-plan generation flow, auto-create
  behavior, batch skip/error aggregation, and JSON response shapes unchanged
  while making the outline-expansion runtime contract auditable from one owner
  boundary instead of a duplicated outer-plus-inner chain. Validation passed
  with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test plot_expansion_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (0 focused tests matched, compile+test harness passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `outline expansion` runtime/provider
  lane is now materially tighter and closer to the stop-rule. Continue here
  only if the next move still removes a real grouped request/runtime owner
  across the route/task seam; do not broaden this lane into outline-flow
  redesign, service-splitting for its own sake, or generic AI runtime
  abstraction in the same batch.

- 2026-05-21 16:18 +08:00 checkpoint:
  this slice stayed in the same neighboring `outline expansion` lane and moved
  one level outward to remove a still-valid grouped request/execution owner
  shared across both compat HTTP routes and background-task runners, without
  changing any request field names, default-value behavior, task payload
  contract, or expansion result shape. A focused audit of
  `backend-rs/src/api/outlines.rs` and
  `backend-rs/src/api/background_tasks.rs` after the runtime-owner pass showed
  that four related entry points, `expand_outline_compat`,
  `batch_expand_outlines_compat`, `run_outline_expand()`, and
  `run_outline_batch_expand()`, were still each carrying the same local
  request/execution boundary:
  parse the same compat payload defaults for expansion strategy, auto-create,
  scene-analysis flag, provider/model passthrough, and batch-size/outline-id
  lists, then resolve the same `AIConfig -> AIService ->
  create_plot_expansion_service(...)` chain before calling the same outline
  expansion owners. That was still a real grouped seam rather than helper
  churn, because the same execution contract lived four times across route and
  task lanes and could drift independently while both lanes were supposed to
  drive the same expansion semantics. The refactor introduced
  `backend-rs/src/services/outline_expansion_request_service.rs` with
  `build_outline_expand_execution_request()`,
  `build_outline_batch_expand_execution_request()`,
  `execute_outline_expand_request()`, and
  `execute_outline_batch_expand_request()`, then updated both
  `backend-rs/src/api/outlines.rs` and
  `backend-rs/src/api/background_tasks.rs` so those four entry points now
  reuse one owned request/default/runtime-injection boundary instead of each
  parsing and executing the same contract inline. This keeps the existing
  `outline_id` required behavior in the background-task runner, compat route
  defaults for `balanced` / `false` / `batch_size=5`, project-id passthrough,
  provider/model override semantics, and JSON response payloads unchanged while
  making the outline-expansion request/execution contract auditable from one
  owner module instead of four repeated clusters. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test outline_expansion_request_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `outline expansion` request/execution
  lane is now substantially tighter and very close to the stop-rule. Continue
  here only if the next move still removes a real grouped outline-expansion
  contract shared by multiple entry points; do not broaden this lane into
  outline access redesign, batch-flow rewrites, or generic execution-framework
  abstraction in the same batch.

- 2026-05-21 16:26 +08:00 checkpoint:
  this slice pivoted out of the now-near-stop-rule `outline expansion` lane
  and tightened one still-valid shared error-contract owner in the neighboring
  `organizations` generation lane, without changing any generated payload
  shape, SSE event shape, or background-task error wording. A focused audit of
  `backend-rs/src/api/organizations.rs` plus
  `backend-rs/src/api/background_tasks.rs` showed that
  `generate_organization_task()` had already become the single generation owner
  reused by both the legacy SSE route `generate_org_stream_legacy` and the
  background-task runner `run_organization_generate()`, but that shared owner
  still collapsed every failure into a bare `String`. That was still a real
  grouped seam rather than cosmetic typing, because the same domain failure
  space was being consumed by two transport lanes with different needs:
  the SSE route should keep sending the same `error` payload text, while the
  background-task lane should keep bridging to the existing task-string
  contract, and both were relying on an unstructured string source that could
  drift as more failure branches accumulate. The refactor introduced the
  explicit domain error enum `GenerateOrganizationTaskError` inside
  `backend-rs/src/api/organizations.rs`, updated
  `generate_organization_task()` to map project access, runtime-config,
  AI-call, invalid-JSON, and persistence failures into owned error variants,
  kept `generate_org_stream_legacy` on the same SSE `channel.error(...)`
  payload by bridging through `error.to_string()`, and updated
  `backend-rs/src/api/background_tasks.rs` so
  `run_organization_generate()` also bridges the owned error back into the
  existing string task contract with `to_string()`. This keeps the current
  `项目不存在或无权限` wording, invalid-JSON message text, SSE terminal error
  payload shape, and background-task failure text unchanged while making the
  organization-generation error contract auditable from one shared owner
  instead of an untyped string flow. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test organizations --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `organizations` generation lane is now
  materially tighter and also approaching the stop-rule. Continue here only if
  the next move still removes a real grouped generation contract shared by
  multiple entry points; do not broaden this lane into prompt-template
  redesign, CRUD service migration, or generic AI-generation abstractions in
  the same batch.

- 2026-05-21 16:38 +08:00 checkpoint:
  this slice pivoted back into the core `chapter generation` execution lane
  and removed one still-valid shared execution owner duplicated across the
  single-chapter SSE stream path and the single-chapter background runtime
  path, without changing any request payload shape, SSE event shape, generated
  chapter payload, or task finalization semantics. A focused audit of
  `backend-rs/src/services/chapter_single_generation_stream_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that both entry points were already consuming the same
  `SingleChapterGenerationExecutionInput`, but each still owned the same local
  execution body: destructure that input, construct `AIService`, call
  `generate_and_persist_chapter_content_with_provider_payload(...)`, and then
  continue with transport- or task-state-specific wrapping. That was still a
  real grouped seam rather than cosmetic helper extraction, because one core
  single-chapter generation contract lived twice across the interactive SSE
  lane and the background task lane and could drift independently even though
  both were supposed to execute the same chapter-generation semantics from the
  same prepared execution input. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_execution_service.rs` with
  `describe_single_chapter_generation_execution()` and
  `execute_single_chapter_generation()`, updated
  `backend-rs/src/services/chapter_single_generation_stream_service.rs` so the
  stream path now owns only progress/result/error SSE wrapping around the owned
  execution call, and updated
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  so the background runtime now owns only task snapshot/finalization handling
  around the same owned execution call. This keeps the existing SSE progress
  cadence, generated chapter response payload shape, runtime error string, and
  single-task success/failure finalization behavior unchanged while making the
  shared single-chapter execution contract auditable from one owner module
  instead of two parallel implementations. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_single_generation_execution_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate single-chapter generation execution
  lane is now materially tighter as well. Continue here only if the next move
  still removes a real grouped execution contract shared by multiple entry
  points; do not broaden this lane into generic runtime frameworks, stream
  choreography redesign, or batch-runtime rewrites in the same batch.

- 2026-05-21 16:44 +08:00 checkpoint:
  this slice stayed in the same `single chapter generation` neighborhood but
  moved one layer outward to remove a still-valid shared workflow error-owner
  gap on the background path, without changing any request payload shape,
  generated response payload shape, or route-facing error detail wording. A
  focused audit of
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` showed that the
  background route `generate_chapter_content_background` had already diverged
  from the stream route at the workflow boundary, but the background workflow
  entry `start_owned_single_generation_background_workflow()` was still
  returning the lower-level `PrepareSingleChapterGenerationRequestError`
  directly. That was still a real grouped seam rather than naming cleanup,
  because the route layer was consuming a request-preparation error from one
  lane and a workflow-specific error shape from adjacent batch create/cancel/
  resume lanes, so the background workflow itself did not yet own its own error
  boundary even though it already owned task-plan creation and runtime
  dispatch. The refactor introduced
  `CreateSingleGenerationBackgroundWorkflowError` plus `map_prepare_error()` in
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  updated `start_owned_single_generation_background_workflow()` to return that
  owned workflow error instead of the lower-level prepare error, added the
  dedicated route mapper
  `map_create_single_generation_background_workflow_error()` in
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`, and updated
  `backend-rs/src/api/chapter_batch_generation.rs` so the background route now
  consumes the workflow-specific error owner while the stream route keeps using
  the request-prepare mapper. This keeps the current `Chapter not found`,
  `Chapter not found or access denied`, and config bad-request detail wording
  unchanged while making the single-background workflow boundary auditable from
  its own owner instead of leaking a lower-level request error contract through
  the route. Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_batch_generation_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (16 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`,
  and `cargo fmt --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate single-background workflow lane is now
  materially tighter and also approaching the stop-rule. Continue here only if
  the next move still removes a real grouped workflow contract shared by
  multiple entry points; do not broaden this lane into batch/single workflow
  unification for its own sake or generic route-helper extraction in the same
  batch.

- 2026-05-21 16:56 +08:00 checkpoint:
  this slice moved into the neighboring `chapter regeneration` SSE execution
  lane and removed one still-valid shared execution owner duplicated across the
  full-chapter and partial-chapter regeneration stream services, without
  changing any route payload shape, SSE event category, chunk emission order,
  final response payload shape, or terminal error wording. A focused audit of
  `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`,
  `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`, and
  `backend-rs/src/services/chapter_regeneration_text_service.rs` showed that
  the two stream builders still each owned the same AI streaming execution
  contract: call `AIService::generate_text_stream(...)`, append chunk content
  into `full_content`, emit `chunk` SSE frames, map upstream stream errors to
  the same `500` SSE error payload, and then translate the same terminal
  sanitize/meta-text failures into the same English error strings before the
  route-visible `result`/`done` tail. That was still a real grouped seam
  rather than helper churn, because one regeneration execution contract lived
  twice across two user-facing SSE workflows and could drift independently even
  though both lanes were supposed to share the same stream/error semantics and
  only differ in progress cadence plus payload finalization. The refactor
  introduced
  `backend-rs/src/services/chapter_regeneration_stream_execution_service.rs`
  with `execute_regeneration_text_stream(...)`,
  `describe_regeneration_finalize_error(...)`, and
  `emit_regeneration_finalize_error(...)`, then updated both
  `chapter_regeneration_full_stream_service.rs` and
  `chapter_regeneration_partial_stream_service.rs` so they now reuse one owned
  execution boundary for streaming chunk accumulation and terminal error
  mapping while still keeping their own progress-event cadence and
  full-vs-partial finalize logic. This keeps the existing `Rewrite result is
  empty after sanitization` / `Rewrite result still contains workflow meta
  text` wording, partial every-5-chunk progress updates, full-stream fixed
  generation progress event, and result payload differences unchanged while
  making the regeneration stream execution contract auditable from one owner
  module instead of two parallel implementations. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_stream_execution_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (1 test passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_regeneration_text_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (6 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `chapter regeneration` stream
  execution lane is now materially tighter as well. Continue here only if the
  next move still removes a real grouped workflow or request boundary shared
  by multiple regeneration entry points; do not broaden this lane into
  generic SSE frameworks, stream choreography unification for its own sake, or
  route compression that does not reduce compatibility risk in the same batch.

- 2026-05-21 17:04 +08:00 checkpoint:
  this slice pivoted to the neighboring `book import` apply-workflow lane and
  removed one still-valid shared persistence owner duplicated across the normal
  apply path and the SSE apply path, without changing any route payload shape,
  apply response payload shape, SSE progress event kinds, or wizard-generation
  sequencing. A focused audit of
  `backend-rs/src/api/book_import.rs` plus
  `backend-rs/src/services/book_import_service.rs` showed that the two entry
  points `apply_import()` and `apply_import_stream()` were still each owning
  the same import-setup contract: parse the same `project_suggestion` fields
  with the same defaults, build the same `CreateProjectParams`, create the same
  project record, update the same `imported_project_id` task field, and then
  import the same outline and chapter payloads before diverging into
  non-stream return assembly vs stream progress reporting and wizard AI
  enrichment. That was still a real grouped seam rather than helper churn,
  because one persisted import workflow contract lived twice across two
  user-facing apply modes and could drift independently even though both lanes
  were supposed to share the same project/bootstrap import semantics and only
  differ in transport feedback plus post-import AI orchestration. The refactor
  introduced
  `backend-rs/src/services/book_import_apply_execution_service.rs` with
  `read_book_import_project_suggestion(...)`,
  `build_book_import_create_project_params(...)`,
  `create_book_import_project(...)`,
  `import_book_import_outlines(...)`, and
  `import_book_import_chapters(...)`, then updated
  `book_import_service.rs` so both apply modes now reuse one owned import
  execution boundary for suggestion parsing, project creation, imported
  project-id persistence, and outline/chapter import while still keeping their
  own JSON return assembly, SSE progress cadence, and wizard-generation
  branches. This keeps the existing fallback title/description defaults,
  `append` / `overwrite` outline mode passthrough, apply-stream progress
  messages, chapter/outline insertion order, and normal apply result payload
  unchanged while making the book-import apply persistence contract auditable
  from one owner module instead of two parallel implementations. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test book_import_apply_execution_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the immediate `book import` apply-workflow lane is
  now materially tighter. Continue here only if the next move still removes a
  real grouped import workflow or retry contract shared by multiple entry
  points; do not broaden this lane into wizard-generation redesign, task-store
  persistence migration, or generic SSE/import frameworks in the same batch.

- 2026-05-21 18:03 +08:00 checkpoint:
  this slice stayed in the same `book import` lane and advanced from the
  already-extracted apply-persistence seam into the shared AI step execution
  seam, removing one still-valid grouped workflow contract duplicated across
  `apply_import_stream()`, `retry_stream()`, and `run_wizard_generation()`
  without changing any HTTP payload shape, SSE event kind, SSE terminal timing,
  failed-step payload shape, or the existing Chinese warning text. A focused
  audit of `backend-rs/src/services/book_import_service.rs` showed that these
  three entry points were still supposed to share the exact same AI-step
  contract for `world_building`, `career_system`, and `characters`, but
  `apply_import_stream()` still owned its own copy of prompt parameter
  assembly, template formatting, AI retry/JSON cleaning, and persistence
  side-effects even after the adjacent non-stream and retry lanes had already
  started collapsing onto the new owner. That was still a real grouped seam
  rather than helper churn, because one user-visible import-generation
  contract lived in parallel across three entry points and could drift
  independently even though all three were supposed to differ only in
  transport/progress/result wrapping. The refactor kept
  `backend-rs/src/services/book_import_ai_generation_service.rs` as the owner
  boundary for shared prompt-param assembly, AI execution, JSON cleaning, and
  persistence side-effects, updated its focused prompt-param tests so the
  fixture matches the current `project::Model` schema, then updated
  `book_import_service.rs` so `apply_import_stream()` now delegates all three
  AI steps through `execute_book_import_ai_generation_step(...)` while keeping
  the existing step names (`world_building`, `career_system`, `characters`),
  labels (`世界观生成`, `职业体系生成`, `角色与组织生成`), progress percentages,
  warning payloads, and success result wrapping unchanged. The local duplicate
  `ai_call_with_retry(...)` in `book_import_service.rs` was also removed, so
  the AI execution contract now has one real owner instead of a leftover
  stream-only copy. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test book_import_ai_generation_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `book import` AI-step contract is now materially
  tighter and the three entry points are aligned on one shared execution seam.
  The next move in this lane should only continue if it removes another real
  grouped contract, such as a still-shared retry/apply orchestration boundary
  or a neighboring import-analysis seam; do not broaden this lane into task
  persistence redesign or generic AI workflow frameworks in the same batch.

- 2026-05-21 18:19 +08:00 checkpoint:
  this slice stayed in the same `book import` lane and tightened the adjacent
  AI-step metadata seam after the execution owner had already been unified,
  removing one still-valid grouped semantic contract duplicated across
  `apply_import_stream()`, `retry_stream()`, and `run_wizard_generation()`
  without changing any HTTP payload shape, SSE progress percentage, warning
  text, retry result payload, or missing-template behavior. A focused audit of
  `backend-rs/src/services/book_import_service.rs` plus
  `backend-rs/src/services/book_import_ai_generation_service.rs` showed that
  although prompt assembly / AI execution / persistence side-effects were now
  owned by `BookImportAiStepKind` plus
  `execute_book_import_ai_step(...)`, the three entry points still kept their
  own parallel copies of the same step metadata contract:
  `world_building / career_system / characters` string names, Chinese labels,
  template-key presence checks, missing-template messages, retry string-to-kind
  parsing, and the per-step “AI正在生成...” progress text. That was still a
  real grouped seam rather than cosmetic helper churn, because one
  user-visible semantic contract for the same three import AI steps was still
  split between the owner module and transport/workflow entrypoints and could
  drift independently even though the business meaning of those steps should be
  defined exactly once. The refactor extended
  `BookImportAiStepKind` with shared step metadata owners:
  `ALL`, `step_name()`, `step_label()`, `ai_progress_message()`, and
  `from_step_name(...)`, then updated `book_import_service.rs` so retry/apply/
  wizard flows now consume that owner instead of maintaining local string
  branches and a leftover `step_label(...)` helper. This keeps the existing
  retry validation, missing-template error wording, progress text, and step
  payload fields unchanged while making the entire book-import AI-step semantic
  contract auditable from one enum owner instead of a split enum-plus-route
  string table. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test book_import_ai_generation_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (3 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `book import` AI-step lane is now tighter both
  at execution ownership and at semantic metadata ownership. Continue here only
  if the next move removes another real grouped import contract; otherwise it
  is reasonable to pivot back to the higher-signal `chapter analysis` or
  neighboring Phase 5 seams instead of forcing more local cleanup.

- 2026-05-21 18:44 +08:00 checkpoint:
  this slice pivoted back to the neighboring `chapter analysis draft` lane and
  tightened the route-facing error owner boundary shared across
  `auto-revision` / `candidate` plus `load` / `apply`, removing one still-valid
  grouped contract split between the draft owner service and the route error
  mapper without changing any route payload shape, status code, or existing
  Chinese / English error wording. A focused audit of
  `backend-rs/src/services/chapter_analysis_draft_service.rs` and
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` showed that the
  four owned draft entry points had already moved chapter access plus draft
  load/apply orchestration into a dedicated owner service, but their public
  route-facing error enums still leaked the lower-level
  `LoadAccessibleChapterError` type as a `Chapter(...)` variant. That meant the
  draft route mapper still needed to understand the lower-level chapter-access
  owner and translate it separately, so the “owned draft workflow” boundary was
  not yet really owning its own route contract even though the route itself had
  already become transport-only. The refactor changed
  `OwnedAutoRevisionDraftPayloadError` and `OwnedCandidateDraftPayloadError` so
  they now expose only owner-level route semantics:
  `ChapterNotFoundOrAccessDenied`, `Draft(...)`, and `Internal(String)`. The
  service now normalizes `LoadAccessibleChapterError` into those owner-level
  variants before returning, and
  `chapter_analysis_draft_error_mapper.rs` now depends only on the draft-owner
  enums instead of also depending on the lower-level chapter-access mapper.
  This keeps the existing `Chapter not found or access denied`, stale-draft,
  preview-only, empty-content, workflow-meta-text, and generic load-failure
  responses unchanged while making the draft workflow error contract auditable
  from one owner service instead of a split owner-plus-lower-layer translation
  chain. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_error_mapper --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_analysis_draft_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis draft` route/error boundary is
  now materially tighter. Continue here only if the next move removes another
  real grouped query/apply contract shared by multiple draft or analysis
  entrypoints; do not broaden this lane into general error-framework cleanup
  or route compression that does not reduce compatibility risk in the same
  batch.

- 2026-05-21 19:03 +08:00 checkpoint:
  this slice stayed in the same `chapter analysis draft` lane and advanced from
  the route-facing error boundary into the neighboring shared draft-source seam,
  removing one still-valid grouped source contract split between the draft
  query and draft apply owners without changing any route payload shape,
  draft-detail payload field, stale detection behavior, or draft apply result
  wording. A focused audit of
  `backend-rs/src/services/chapter_draft_query_service.rs` and
  `backend-rs/src/services/chapter_draft_apply_service.rs` showed that the
  query owner was still carrying not only query-payload assembly and
  query-specific normalization, but also the shared draft-source contract that
  both query and apply flows depended on: optional datetime formatting,
  stale-draft detection, reviser-history parsing, candidate full-content
  extraction, truthy scalar / integer coercion helpers, and the actual
  source-loading helpers `load_candidate_draft_attempt(...)` and
  `load_latest_reviser_history(...)`. That was still a real grouped seam rather
  than generic utility churn, because one source-access and source-semantics
  contract was still owned by the query module while a second workflow owner
  (`chapter_draft_apply_service.rs`) depended on it directly, so source
  semantics could still drift if the query file changed for read-side reasons.
  The refactor introduced
  `backend-rs/src/services/chapter_draft_source_service.rs` as a focused shared
  owner for that draft-source contract, migrated the shared helpers and focused
  tests into that module, registered it in `services/mod.rs`, and updated both
  `chapter_draft_query_service.rs` and `chapter_draft_apply_service.rs` to
  consume the new source owner instead of sharing implementation through the
  query module. This keeps the existing `has_full_content`, `is_stale`,
  `draft_created_at`, `revised_text_preview`, and apply-time stale / preview /
  empty-content behaviors unchanged while making the draft-source contract
  auditable from one owner instead of a split query-owner-plus-apply-consumer
  relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_draft_source_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (4 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_draft_query_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (51 tests passed),
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test chapter_draft_apply_service --manifest-path "backend-rs/Cargo.toml" -- --nocapture`
  (32 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis draft` lane is now tighter at
  both route-facing error ownership and shared source ownership. Continue here
  only if the next move removes another real grouped query/apply semantic
  contract; otherwise it is reasonable to pivot back to a higher-signal Phase 5
  seam instead of forcing more local extraction.

- 2026-05-21 18:03 +08:00 checkpoint:
  this slice pivoted back to the higher-signal `chapter_batch_generation` lane
  and tightened one still-valid SSE read-side seam, removing a shared stream
  status contract split between the stream-state loader and the stream-event
  builder without changing SSE event names, terminal timing, payload shape, or
  fallback wording. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  showed that the current read-side stream contract was still defined in two
  owners at once: `status_view_service.rs` resolved task-status plus checkpoint
  fallback semantics into stream progress / last-message / `event_status`,
  while `status_stream_event_service.rs` separately reinterpreted the same task
  status values to decide terminal-kind and close behavior. That was still a
  real grouped seam rather than helper churn, because one transport-facing SSE
  status meaning for the same batch task could still drift between state
  loading and event emission even though it should be audited from one owner.
  The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  as a focused shared owner for SSE read-side status semantics, moving
  `BatchGenerationStreamState`, checkpoint-backed progress / message fallback
  resolution, `event_status` mapping, and terminal-kind classification into one
  module. `chapter_batch_generation_status_view_service.rs` now depends on that
  owner when loading stream state, and
  `chapter_batch_generation_status_stream_event_service.rs` now consumes the
  same terminal-kind owner instead of keeping its own status branching. This
  keeps the existing `pending/running/completed/failed/cancelled` fallback
  progress values, Chinese default messages, `processing/success/error`
  progress-event status values, and completed / failed / cancelled close-event
  behavior unchanged while making the SSE stream status contract auditable from
  one service boundary instead of a split loader-plus-event-builder
  relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_view_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_batch_generation` SSE read-side lane is
  now tighter both at state-loading semantics and at event-emission semantics.
  The next high-signal move should stay in `chapter_batch_generation` only if
  it removes another real grouped read-side or command-side status contract
  shared across stream / status / command owners; otherwise it is reasonable to
  pivot to the next Phase 5 Rust migration seam instead of forcing narrower
  local extraction.

- 2026-05-21 18:10 +08:00 checkpoint:
  this slice stayed in the same `chapter_batch_generation` lane and advanced
  from SSE read-side semantics into workflow-result assembly consolidation,
  removing one still-valid shared runtime-metadata contract split between
  command-side resume payload assembly and read-side status payload assembly
  without changing any HTTP response field names, checkpoint defaults, task
  metadata values, or resume/status payload shapes. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  showed that both owners were still assembling the same batch task runtime
  metadata contract in parallel: `batch_id`, `task_type`, `project_id`,
  `status`, `stage_code`, `execution_mode`, `current_chapter_id`, checkpoint
  metadata injection, and `created_at`, while each side then layered its own
  command-only or status-only fields on top. That was still a real grouped seam
  rather than harmless duplication, because one user-visible task-runtime
  payload contract for the same batch task could still drift between resume
  command responses and query/read-side payloads even though their shared base
  meaning should be defined exactly once. The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_task_payload_base_service.rs`
  as a focused shared owner for task runtime metadata assembly, moving
  `to_iso(...)`, `checkpoint_with_runtime_metadata(...)`, and the shared base
  payload builder into one module. `chapter_batch_generation_status_payload_adapter_service.rs`
  now builds status/active payloads on top of that base owner, and
  `chapter_batch_generation_command_payload_adapter_service.rs` now also reuses
  the same owner for resume response assembly before adding command-only
  `message`, `total_chapters`, and `completed_chapters` fields. This keeps the
  existing resume/status payload field set, checkpoint `stage_code` /
  `execution_mode` defaults, `task_type` semantics, and created-at formatting
  unchanged while making the shared task-runtime payload contract auditable
  from one owner instead of a split status-adapter-plus-command-adapter
  relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service`
  (5 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_batch_generation` lane is now tighter
  both at SSE read-side semantics and at shared task-runtime payload assembly.
  The next high-signal move should continue here only if it removes another
  real grouped command-side or read-side status contract; otherwise it is
  reasonable to pivot to the next Phase 5 Rust migration seam instead of
  forcing narrower payload-local cleanup.

- 2026-05-21 18:33 +08:00 checkpoint:
  this slice stayed in the same `chapter_batch_generation` lane and advanced
  from shared task-runtime payload assembly into runtime checkpoint/progress
  semantics ownership, removing one still-valid grouped batch runtime contract
  split between runtime snapshot writes and checkpoint/progress semantics
  without changing any HTTP payload shape, SSE event kind, terminal timing, or
  existing Chinese checkpoint wording. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that it still owned both snapshot write / merge / replace behavior and
  the batch-task checkpoint/progress semantic contract:
  failure-kind wording, queued/resumed pending checkpoint behavior, running
  progress calculation, and stage-to-runtime-checkpoint mapping. That was
  still a real grouped seam rather than helper churn, because one persisted
  batch runtime checkpoint contract for the same task lifecycle could still
  drift if write-path changes and semantic-stage changes continued to live in
  the same file even though only the latter belongs to a reusable runtime
  semantics owner. The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`
  as the focused owner for batch runtime checkpoint/progress semantics,
  migrating `BatchGenerationFailureKind`,
  `compute_batch_running_progress(...)`,
  `checkpoint_message_for_batch_generation_failure(...)`,
  `build_pending_batch_generation_runtime_checkpoint(...)`,
  `build_batch_generation_runtime_checkpoint_for_stage(...)`, and
  `build_resume_batch_generation_runtime_checkpoint(...)` into that module.
  `chapter_batch_generation_runtime_state_service.rs` now keeps snapshot write
  / merge / replace ownership plus task mutation ownership, while
  `chapter_batch_generation_task_command_service.rs` reuses the dedicated
  checkpoint owner for queued/resume pending checkpoints. This keeps the
  existing persisted checkpoint field set, `completed/total` behavior,
  running-progress floor/cap semantics, resumed batch pending semantics, SSE
  payload inputs, and Chinese messages unchanged while making the batch runtime
  checkpoint contract auditable from one owner instead of a split write-owner
  plus semantic helper relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service`
  (6 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service`
  (21 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service`
  (4 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_batch_generation_runtime_state_service.rs`
  is now closer to a pure snapshot-write owner. Continue in this lane only if
  the next move removes another real grouped runtime semantic contract instead
  of thin wrapper churn.

- 2026-05-21 18:33 +08:00 checkpoint:
  this slice stayed in the same `chapter_batch_generation` lane and advanced
  one step further by removing the adjacent single-generation runtime
  checkpoint/progress semantic contract from the runtime snapshot write owner,
  without changing task lifecycle semantics, persisted checkpoint fields,
  `word_count` projection, or the existing Chinese status messages. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  after the batch checkpoint extraction showed that it still mixed two owner
  classes in one file: snapshot write / merge / replace behavior, and the full
  single-generation runtime checkpoint contract used by task creation,
  preparing, generating, finalizing, completion, and failure transitions.
  That remained a real grouped seam rather than cosmetic extraction, because
  the single-generation runtime checkpoint contract is the same kind of
  reusable lifecycle semantics owner as the just-extracted batch checkpoint
  contract, yet it still lived beside persistence-write concerns in the runtime
  state module. The refactor extended
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`
  so it now also owns `SingleGenerationSnapshotStage`,
  `resolve_single_generation_snapshot_plan(...)`, and
  `build_single_generation_runtime_checkpoint_for_stage(...)`. The runtime
  state owner now imports those helpers instead of carrying a second local copy
  of single-generation checkpoint progress/status semantics, and the focused
  tests for that semantic contract now live with the checkpoint owner rather
  than the snapshot-write owner. This keeps pending / preparing / generating /
  finalizing / completed / failed checkpoint shapes, `queued` / `progress` /
  `done` / `error` event semantics, and optional `word_count` projection
  unchanged while making both batch and single-generation runtime checkpoint
  semantics auditable from one service boundary. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service`
  (8 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service`
  (19 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service`
  (4 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_batch_generation` runtime lane is now
  materially tighter, and `chapter_batch_generation_runtime_state_service.rs`
  is closer to a pure snapshot-write/task-mutation owner. The next move should
  stay here only if a new audit finds another real grouped runtime semantic
  contract; otherwise it is reasonable to pivot to the next Phase 5 Rust
  migration seam instead of forcing thinner local extraction.

- 2026-05-21 18:42 +08:00 checkpoint:
  this slice deliberately pivoted away from the now-lower-signal
  `chapter_batch_generation` runtime lane and reopened a stronger neighboring
  `chapter analysis` read-side seam, removing one still-valid grouped batch
  status-query contract without changing route payload shape, chapter status
  item fields, empty-response defaults, or the existing 500 error behavior on
  batch query failure. A focused audit of
  `backend-rs/src/services/chapter_analysis_query_service.rs` showed that it
  still mixed two adjacent but distinct query-owner responsibilities:
  single-chapter analysis task status lookup, and the batch analysis status
  query contract used by `/chapters/analysis/status/batch`. The batch path was
  still locally owning chapter-id normalization (`trim`/dedupe/limit 200),
  empty response defaults, project-id selection, and per-chapter status
  aggregation on top of the shared `latest_analysis_task(...)` and
  `analysis_task_status_payload(...)` primitives. That remained a real grouped
  seam rather than cosmetic helper churn, because one route-facing batch
  read-side contract was still living inside the single-chapter query owner
  even though the two paths have different owner scope and different natural
  test surfaces. The refactor introduced
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs` as a
  focused owner for the batch status-query contract, moving
  `normalize_batch_analysis_status_chapter_ids(...)`,
  `empty_batch_analysis_task_status_payload(...)`,
  `build_batch_analysis_task_status_payload(...)`, and
  `load_batch_analysis_task_status_payload(...)` into that module. The route in
  `backend-rs/src/api/chapter_analysis_routes.rs` now delegates the batch
  endpoint to the dedicated batch owner, while
  `chapter_analysis_query_service.rs` narrows back to single-chapter status
  query plus analysis/quality read-side owners. This keeps the existing empty
  response contract (`project_id=\"\"`, `total=0`, `items={}`), chapter-id
  dedupe/trim behavior, per-item task payload shape, and current
  `internal_detail_error(...)` mapping unchanged while making the batch status
  query contract auditable from one owner instead of a mixed single-plus-batch
  query service. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_batch_status_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis` read-side lane now has a
  cleaner split between single-item and batch query ownership. Continue here
  only if the next move removes another real grouped analysis/draft contract;
  otherwise reassess adjacent Phase 5 Rust seams instead of forcing narrower
  query-local extraction.

- 2026-05-21 18:50 +08:00 checkpoint:
  this slice stayed in the neighboring `chapter analysis draft` lane and
  tightened one still-valid grouped workflow contract shared across all four
  draft entrypoints (`auto-revision load/apply` plus `candidate load/apply`),
  without changing route payload shape, stale behavior, not-found wording, or
  chapter access semantics. A focused audit of
  `backend-rs/src/services/chapter_analysis_draft_service.rs` and
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` showed that the
  route-facing draft owner had already absorbed chapter access and typed draft
  errors, but it still carried the same selection workflow contract four times
  in parallel: load chapter, parse route request, derive whether the selector
  (`history_id` or `attempt_id`) was explicit, pass the selector through to
  query/apply, then forward a raw boolean into the error-mapping layer so
  not-found behavior could decide between “latest draft missing” and “explicit
  draft missing”. That remained a real grouped seam rather than cosmetic
  de-duplication, because one route-facing owner contract for draft selection
  mode and explicit-id semantics was still split across four service entry
  points plus the draft error mapper, and continued to rely on ad-hoc `bool`
  wiring instead of an explicit owner-level type. The refactor introduced a
  typed owner contract in
  `backend-rs/src/services/chapter_analysis_draft_service.rs`:
  `OwnedDraftSelectionMode` plus `OwnedDraftPayloadRequest`, and a shared
  `prepare_owned_draft_payload_context(...)` boundary that now owns chapter
  access, selector propagation, selection-mode classification (`Latest` vs
  `Explicit`), and `allow_stale` carriage for all four draft entry points.
  The route-facing owned draft error enums now carry
  `OwnedDraftSelectionMode` instead of a raw boolean, and
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` now maps
  not-found behavior from that typed selection owner rather than from ad-hoc
  `history_id_provided` / `attempt_id_provided` booleans. This keeps the
  existing “指定的…不存在或不可用” vs “该章节暂无…” message split, stale
  conflict responses, preview-only conflict response, and chapter-not-found
  mapping unchanged while making the draft selection workflow contract
  auditable from one explicit owner instead of four parallel bool-based flows.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis draft` owner now has a
  cleaner, typed route-facing selection contract. Continue here only if the
  next move removes another real grouped draft/query/apply contract; otherwise
  reassess adjacent Phase 5 Rust seams instead of forcing narrower draft-local
  abstraction.

- 2026-05-21 19:04 +08:00 checkpoint:
  this slice stayed in the same neighboring `chapter analysis draft/query`
  lane and tightened one still-valid grouped draft-view payload contract
  shared between draft detail queries and the chapter analysis view, without
  changing route payload shape, analysis embedding keys, stale semantics, or
  candidate/auto-revision preview visibility. A focused audit of
  `backend-rs/src/services/chapter_draft_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_query_service.rs`, and
  `backend-rs/src/services/chapter_analysis_view_payload_adapter_service.rs`
  showed that the query owner still mixed two distinct responsibilities:
  database load / outer response-envelope ownership for draft detail routes,
  and the full draft-view payload contract used both by detail responses
  (`load_auto_revision_draft_payload(...)`,
  `load_candidate_draft_payload(...)`) and by
  `build_chapter_draft_analysis_view_fragments(...)` when embedding draft
  previews inside the chapter analysis view. That remained a real grouped seam
  rather than cosmetic helper reuse, because one user-visible draft-view
  contract for `auto_revision_draft` and `candidate_draft` could still drift
  between detail query payloads and analysis-page fragments even though both
  surfaces should be audited from one owner boundary. The refactor introduced
  `backend-rs/src/services/chapter_draft_view_payload_service.rs` as the
  focused owner for draft-view payload assembly, moving
  `ChapterDraftAnalysisViewFragments`,
  `build_auto_revision_draft_payload(...)`,
  `build_candidate_draft_payload(...)`, and
  `build_chapter_draft_analysis_view_fragments(...)` plus their focused tests
  into that module. `chapter_draft_query_service.rs` now narrows back to
  `load_*` query ownership and outer response-envelope assembly only, while
  `chapter_analysis_query_service.rs` and
  `chapter_analysis_view_payload_adapter_service.rs` now depend on the shared
  draft-view payload owner instead of importing those contracts from the query
  file. This keeps the existing `chapter_id` envelope shape, analysis-view
  `auto_revision_draft` / `candidate_draft` keys, preview-only omission of
  full text, candidate quality fields, stale detection, and Chinese fallback
  wording unchanged while making the shared draft-view contract auditable from
  one service boundary instead of a split query-owner plus analysis-fragment
  relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_draft_view_payload_service`
  (22 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_draft_query_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_payload_adapter_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis draft/query` lane now has a
  cleaner split between draft-query data loading and shared draft-view payload
  ownership. The next move should stay here only if another real grouped
  analysis-view composition contract still spans multiple owners; otherwise
  reassess adjacent Phase 5 Rust seams instead of forcing narrower local
  extraction.

- 2026-05-21 19:09 +08:00 checkpoint:
  this slice stayed in the neighboring `chapter analysis` read-side lane and
  tightened one still-valid chapter-analysis-view query contract, without
  changing route payload shape, error mapping, embedded draft/checker/quality
  fragments, or chapter access semantics. A focused audit of
  `backend-rs/src/services/chapter_analysis_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_view_payload_adapter_service.rs`,
  `backend-rs/src/api/chapter_analysis_routes.rs`, and
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs` showed that the
  existing query owner still mixed two different read-side scopes in one file:
  analysis task status query ownership, and the full chapter analysis view
  query contract that loads `plot_analysis`, `story_memory`,
  `generation_history`, the latest `chapter_draft_attempt`, then composes
  checker/draft/quality fragments into the final analysis-view payload. That
  remained a real grouped seam rather than local helper churn, because one
  route-facing analysis-view query boundary still lived beside task-status and
  quality-metrics query concerns even though it has a distinct error surface,
  distinct data-loading contract, and distinct natural caller (`GET
  /chapters/:id/analysis`). The refactor introduced
  `backend-rs/src/services/chapter_analysis_view_query_service.rs` as the
  focused owner for the chapter analysis view query boundary, moving
  `LoadChapterAnalysisViewPayloadError`,
  `load_chapter_analysis_view_payload(...)`, and
  `load_owned_chapter_analysis_view_payload(...)` into that module. The route
  in `backend-rs/src/api/chapter_analysis_routes.rs` and the route-facing
  error mapper in
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs` now depend on
  the dedicated analysis-view query owner, while
  `chapter_analysis_query_service.rs` narrows back to analysis task status
  query plus owned quality-metrics query helpers. This keeps the existing
  analysis-view envelope, `Chapter analysis not found` 404 mapping,
  access-denied behavior, and fragment composition semantics unchanged while
  making the full analysis-view read-side contract auditable from one owner
  instead of a mixed status-query plus analysis-view-query service. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter analysis` lane now has a cleaner split
  between task-status query ownership, analysis-view query ownership, and
  quality-metrics query ownership. The next move should continue here only if
  another real grouped read-side contract still spans multiple owners;
  otherwise it is reasonable to pivot to the next higher-signal Phase 5 Rust
  seam instead of forcing narrower composition-local extraction.

- 2026-05-21 19:17 +08:00 checkpoint:
  this slice stayed in the broader `chapter analysis / quality` read-side lane
  and tightened one still-valid chapter-scope quality query contract without
  changing project quality-trend payloads, chapter quality-metrics payload
  shape, analysis-view quality fragment fields, or route-facing access/error
  semantics. A focused audit of
  `backend-rs/src/services/chapter_quality_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_query_service.rs`, and
  `backend-rs/src/services/chapter_analysis_view_query_service.rs` showed that
  the quality lane still mixed two distinct scopes across adjacent owners:
  project-scope quality-trend query ownership
  (`/chapters/project/:project_id/quality-trend`) and chapter-scope quality
  query ownership used by both `GET /chapters/:id/analysis/quality-metrics`
  and the chapter analysis view's embedded `quality_metrics` /
  `quality_metrics_summary` fragments. That remained a real grouped seam
  rather than helper churn, because one chapter-scoped quality query contract
  still lived partly in the project-trend owner and partly in the analysis
  query owner even though it has a shared source-resolution boundary, shared
  summary-shaping rules, and shared route-facing chapter access semantics. The
  refactor introduced
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs` as the
  focused owner for chapter-scope quality query contracts, moving
  `ChapterAnalysisQualityFragments`,
  `build_chapter_analysis_quality_fragments(...)`,
  `build_chapter_quality_metrics_fragments(...)`,
  `load_chapter_quality_metrics_payload(...)`, and
  `load_owned_chapter_quality_metrics_payload(...)` into that module together
  with their focused tests. `chapter_quality_query_service.rs` now narrows
  back to project-scope `LoadQualityTrendPayloadError` plus
  `load_quality_trend_payload(...)` only, while
  `chapter_analysis_view_query_service.rs`,
  `chapter_analysis_view_payload_adapter_service.rs`,
  `chapter_analysis_routes.rs`, and
  `chapter_analysis_query_error_mapper.rs` now depend on the dedicated
  chapter-quality query owner for chapter-scoped fragments and quality-metrics
  route handling. This keeps the existing project trend list payload, chapter
  quality-metrics envelope, runtime-context inclusion rules, access-denied
  behavior, and analysis-view quality fragment keys unchanged while making the
  chapter-scope quality query contract auditable from one owner boundary
  instead of a split project-quality owner plus analysis-query bridge. 
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_query_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the chapter-quality lane now has a cleaner split
  between project-scope quality-trend ownership and chapter-scope quality
  metrics / analysis-fragment ownership. The next move should stay in this
  neighborhood only if another real shared read-side contract still spans
  project/chapter/analysis owners; otherwise it is reasonable to pivot to the
  next higher-signal Phase 5 seam instead of forcing narrower query-local
  cleanup.

- 2026-05-21 19:27 +08:00 checkpoint:
  this slice pivoted away from the already-tightened `chapter analysis /
  quality` lane and returned to the higher-signal Phase 5
  `chapter batch generation / single chapter generation` boundary, tightening
  one still-valid shared workflow-prepare contract without changing single
  chapter route payload shape, stream/background task behavior, target word
  count defaults, compat-only request field handling, or error response
  semantics. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_request_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  and `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`
  showed that the single-chapter route group already had a thin transport
  boundary and a shared route-facing request owner, but one workflow-facing
  prepare contract was still split awkwardly across owners: the request module
  still carried `SingleChapterGenerationExecutionInput`,
  `PrepareSingleChapterGenerationRequestError`, and
  `prepare_single_chapter_generation_request(...)` even though those values are
  not route-facing request shape concerns at all and are instead jointly owned
  by both `generate-background` and `generate-stream` workflow entry points,
  their execution/dispatch consumers, and the route-facing error mapper. That
  remained a real grouped seam rather than helper churn, because one shared
  workflow contract for chapter access, normalized target-word resolution,
  execution config assembly, provider-payload carriage, and downstream error
  surface was still anchored to the route-request owner instead of one focused
  workflow-prepare owner. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs` as
  the dedicated owner for that shared workflow-prepare boundary, moving
  `SingleChapterGenerationExecutionInput`,
  `PrepareSingleChapterGenerationRequestError`,
  `PreparedSingleChapterGenerationRequest`, and
  `prepare_single_chapter_generation_request(...)` into that module together
  with focused target-normalization tests. The original
  `chapter_single_generation_request_service.rs` now narrows back to
  route-facing request/compat ownership only, while background workflow,
  stream workflow, execution/dispatch/runtime consumers, and
  `chapter_batch_generation_error_mapper.rs` now depend on the dedicated
  prepare owner for the shared workflow contract. This keeps the existing
  single-chapter background response payload, stream SSE transport behavior,
  `enable_analysis` compat consumption, chapter not found / access denied
  mapping, and config error mapping unchanged while making the shared
  single-chapter prepare contract auditable from one owner boundary instead of
  a mixed request-owner plus workflow-consumer relationship. Validation passed
  with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service`
  (6 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_execution_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-chapter generation lane now has a
  cleaner split between route-facing request/compat ownership and the shared
  workflow-prepare contract consumed by both background and stream entry
  points. The next move should continue nearby only if another real grouped
  single-chapter or batch-generation workflow contract still spans multiple
  owners; otherwise reassess the next Phase 5 seam instead of forcing smaller
  workflow-local extractions.

- 2026-05-21 19:34 +08:00 checkpoint:
  this slice stayed in the same high-signal `chapter batch generation /
  single chapter generation` neighborhood and tightened one still-valid
  background runtime-launch contract shared across batch create, batch resume,
  and single-chapter background generation, without changing route payload
  shape, task-id creation timing, runtime dispatch target, provider payload
  defaults, or error/response semantics. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`
  showed that the neighboring workflow owners had already separated access,
  request preparation, and task-plan persistence reasonably well, but they
  still each carried a parallel copy of the final runtime-launch contract:
  unpack task-plan output, keep the route-facing response payload alive, shape
  background execution input, select the right dispatch entrypoint, and then
  launch runtime work while returning the preserved payload. That remained a
  real grouped seam rather than helper churn, because one workflow-facing
  contract for “persisted task plan/result plus explicit runtime execution
  launch” was still split across three background entry owners even though all
  three should be auditable from one owner boundary when checking launch
  semantics, task-id carriage, or provider-payload propagation. The refactor
  introduced
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  as the dedicated owner for that shared launch contract, adding
  `OwnedGenerationRuntimeLaunch` plus focused builders for batch create,
  batch resume, and single-chapter background launch, together with one
  `dispatch_owned_generation_runtime(...)` boundary that now owns dispatch
  selection and response-payload return. The three workflow owners now narrow
  back to their own access / prepare / plan concerns and delegate the shared
  launch contract to the new owner instead of each manually wiring
  `response_payload`, `created_task_id`, execution input construction, and
  runtime dispatch. This keeps the existing batch-create response payload,
  batch-resume response payload, single-background task payload, background
  launch timing, resume dispatch branching, and config/provider payload
  semantics unchanged while making the shared runtime-launch contract auditable
  from one service boundary instead of a three-owner parallel wiring pattern.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the neighboring batch/single background workflow
  lane now has a cleaner split between owner-specific access/prepare/plan
  logic and the shared runtime-launch contract. The next move should stay here
  only if another real grouped execution or stream-facing contract still spans
  multiple workflow owners; otherwise reassess the next Phase 5 seam instead
  of forcing smaller launch-local cleanup.

- 2026-05-21 19:40 +08:00 checkpoint:
  this slice stayed in the adjacent `chapter batch generation` read-side lane
  and tightened one still-valid task-view query contract shared across status
  detail, active-project query, and active-task list surfaces, without
  changing route payload shape, task access semantics, active-task filtering,
  checkpoint/runtime metadata fields, stream event semantics, or error
  mapping. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  showed that the read-side lane still mixed two distinct owner boundaries in
  one module: the shared task-view query contract that loads owned/active task
  models, snapshot-backed runtime state, quality-summary context, and
  `BatchGenerationTaskViewContext` for three different route-facing queries;
  and the narrower stream-state contract that resolves
  `BatchGenerationStreamState` for SSE polling. That remained a real grouped
  seam rather than helper churn, because one shared read-side contract for
  task-view context loading and payload shaping still lived beside stream-only
  state loading even though status detail, active-project, and active-task
  list all depend on the same grouped query owner and should drift together.
  The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  as the dedicated owner for the shared read-side task-view query boundary,
  moving `BatchGenerationTaskViewContext`,
  `LoadBatchGenerationTaskViewContextError`,
  `build_active_batch_generation_task_view_payload(...)`,
  `build_batch_generation_status_query_response(...)`,
  `load_required_batch_generation_task_view_context(...)`,
  `load_active_project_batch_generation_task_view_context(...)`, and
  `load_active_user_batch_generation_task_view_contexts(...)` into that module
  together with the focused terminal/manual-review tests. The original
  `chapter_batch_generation_status_view_service.rs` now narrows back to the
  stream-facing `load_batch_generation_stream_state(...)` responsibility only,
  while status query, active-project query, and active-task list query owners
  now depend on the shared task-view query owner instead of importing mixed
  read-side contracts from the stream-adjacent module. This keeps the existing
  `has_active_task` envelope, active task item payloads, status detail payload
  fields, manual-review terminal semantics, and stream polling behavior
  unchanged while making the shared task-view read-side contract auditable
  from one owner boundary instead of a mixed status-view plus stream-state
  relationship. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch-generation read-side lane now has a
  cleaner split between shared task-view query ownership and stream-state
  ownership. The next move should stay nearby only if another real grouped
  read-side or stream-facing contract still spans multiple owners; otherwise
  reassess the next Phase 5 seam instead of forcing smaller query-local
  cleanup.

- 2026-05-21 21:32 +08:00 checkpoint:
  this slice reassessed the weakening `batch status stream` lane, chose not to
  force a smaller stream-local cleanup there, and instead pivoted to a
  higher-signal neighboring `chapter regeneration` route-facing seam,
  tightening one still-valid stream request contract without changing SSE
  payload shape, full/partial regeneration workflow behavior, apply-partial
  mutation payloads, prompt-building logic, or route error semantics. A
  focused audit of
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  `backend-rs/src/services/chapter_regeneration_request_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  and `backend-rs/src/services/chapter_regeneration_prepare_service.rs`
  showed that the regeneration lane still mixed two distinct route-facing
  contracts in one owner: stream request/workflow request shaping for
  `regenerate-stream` plus `partial-regenerate-stream`, and mutation request
  shaping for `apply-partial-regenerate`. That remained a real grouped seam
  rather than helper churn, because one shared stream-facing request contract
  for full/partial regeneration route payload normalization, defaulting,
  web-research flags, and workflow request handoff was still anchored to the
  same owner as the unrelated apply-mutation contract even though the two
  route groups have different natural consumers and drift independently. The
  refactor introduced
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`
  as the dedicated owner for stream-facing regeneration request contracts,
  moving `FullChapterRegenerationStreamRequest`,
  `build_full_chapter_regeneration_stream_request_from_route_payload(...)`,
  `PartialRegenerationStreamWorkflowRequest`,
  `PartialRegenerationStreamRouteRequest`,
  `build_partial_regeneration_stream_workflow_request(...)`,
  `build_partial_regeneration_stream_workflow_request_from_route_payload(...)`,
  plus the focused normalization tests into that module. The original
  `chapter_regeneration_request_service.rs` now narrows back to the
  `ApplyPartialRegenerateRequest` / route-payload mutation contract only,
  while `chapter_regeneration_routes.rs`,
  `chapter_regeneration_stream_workflow_service.rs`, and
  `chapter_regeneration_prepare_service.rs` now depend on the dedicated stream
  request owner for full/partial regeneration stream request shaping. This
  keeps the existing full-regeneration default target word count, partial
  regeneration context/web-research defaults, apply request defaults, Chinese
  route-facing validation semantics, and stream execution behavior unchanged
  while making the shared stream request contract auditable from one owner
  boundary instead of a mixed stream-request plus apply-request relationship.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_request_service`
  (6 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_request_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service`
  (11 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper`
  (13 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter regeneration` lane now has a cleaner
  split between stream-facing request ownership and apply-mutation request
  ownership. The next move should stay here only if another real grouped
  stream workflow or execution contract still spans multiple owners; otherwise
  reassess the next Phase 5 seam instead of forcing smaller request-local
  cleanup.

- 2026-05-21 21:42 +08:00 checkpoint:
  this slice stayed in the `chapter regeneration` lane and tightened one more
  still-valid stream-facing owner boundary shared across both full-chapter and
  partial regeneration SSE paths, without changing route payload shape, chunk /
  progress event categories, finalize error messages, result payload shape, or
  done-event semantics. A focused audit of
  `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`,
  `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_stream_execution_service.rs`
  showed that the lane still mixed two distinct responsibilities in the
  full/partial stream owners: each file still owned its route-specific input
  contract and finalize payload differences, but both also duplicated the same
  stream launch lifecycle for tracker start/preparing/generating emission,
  `execute_regeneration_text_stream(...)` dispatch, finalize-error emission,
  and `result` / `done` completion handoff. That remained a real grouped seam
  rather than helper churn because the duplicated lifecycle is the
  stream-facing execution contract that both regeneration modes must keep
  behaviorally aligned, while only their initial event plan, progress cadence,
  and final payload projection should drift independently. The refactor
  introduced
  `backend-rs/src/services/chapter_regeneration_stream_launch_service.rs`
  as the dedicated owner for that shared launch lifecycle, moving channel /
  tracker startup, initial event replay, shared text-stream execution,
  finalize-error emission, and result/done completion into one auditable
  owner. `chapter_regeneration_full_stream_service.rs` now narrows back to the
  full-regeneration-specific initial event plan plus chapter payload finalize
  rule, while `chapter_regeneration_partial_stream_service.rs` now narrows
  back to the partial-specific initial event plan, chunk-progress cadence, and
  partial payload finalize rule. This keeps the existing “Building rewrite
  prompt...”, “Rewriting chapter...”, partial rewrite preparation sequence,
  5-chunk progress cadence, and full/partial payload shape differences
  unchanged while making the shared stream launch/execution contract auditable
  from one owner boundary instead of two parallel stream owners. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_full_stream_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_partial_stream_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_execution_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper`
  (13 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter regeneration` lane now has a cleaner
  split between stream request ownership, shared stream launch ownership, and
  apply-mutation ownership. The next move should stay here only if another
  real grouped stream workflow or finalize contract still spans multiple
  owners; otherwise reassess the next Phase 5 seam instead of forcing smaller
  stream-local cleanup.

- 2026-05-21 21:54 +08:00 checkpoint:
  this slice deliberately stopped short of forcing thinner `chapter
  regeneration` workflow cleanup and instead pivoted to a stronger adjacent
  runtime-write seam, tightening one still-valid single-generation runtime
  owner boundary without changing batch-task payload shape, pending/running/
  finalizing/completed/failed snapshot semantics, single-generation task
  status transitions, dispatch signatures, or background/stream execution
  behavior. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`
  showed that the batch runtime-state owner still mixed two distinct write-side
  contracts: batch-generation runtime checkpoint/task mutation ownership for
  multi-chapter execution, and the full single-generation runtime write path
  for pending snapshot creation, preparing/finalizing/completed/failed
  checkpoint writes, task mutation plans, and runtime execute/finalize flow.
  That remained a real grouped seam rather than helper churn because the
  single-generation runtime write path already forms its own owner boundary
  with one chapter-scoped execution contract and one task/snapshot lifecycle,
  while the batch runtime owner should drift independently with multi-chapter
  progress, cancel, retry, and failure semantics. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  as the dedicated owner for the single-generation runtime write boundary,
  moving `persist_new_single_generation_task_snapshot(...)`,
  `prepare_single_generation_runtime(...)`,
  `persist_single_generation_finalizing_snapshot(...)`,
  `finalize_single_generation_success(...)`,
  `finalize_single_generation_failure(...)`,
  `execute_single_generation_runtime(...)`, plus the focused single-generation
  task-mutation tests into that module. The original
  `chapter_batch_generation_runtime_state_service.rs` now narrows back to the
  shared snapshot-write primitives plus batch-generation runtime reset /
  prepare / chapter-progress / cancel / failure ownership, while dispatch and
  task-plan owners now depend on the dedicated single-generation runtime-state
  owner for single-generation background/resume write-side lifecycle. This
  keeps the existing single-generation pending snapshot shape, start/finalize
  status transitions, resolved chapter-number persistence, runtime failure
  handling, and batch resume semantics unchanged while making the
  single-generation runtime write contract auditable from one owner boundary
  instead of a mixed batch-plus-single runtime-state module. Validation passed
  with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service`
  (17 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation lane now has a cleaner split
  between request preparation, stream/background transport/workflow, execution,
  and runtime-state ownership. The next move should stay nearby only if
  another real grouped single-generation execution/transport contract still
  spans multiple owners; otherwise reassess the next Phase 5 seam instead of
  forcing smaller runtime-helper cleanup.

- 2026-05-21 22:06 +08:00 checkpoint:
  this slice continued the single-generation Phase 5 seam by splitting the
  remaining stream launch contract out of the workflow-only path without
  changing route behavior, prepare-time validation, SSE payload shape, or the
  underlying execution/runtime semantics. A focused audit of
  `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that single-generation background dispatch already had a dedicated
  launch owner, while the stream path still kept one grouped transport-facing
  contract spread across the workflow and a single-use stream owner: the
  workflow prepared `SingleChapterGenerationExecutionInput`, but the next
  boundary still directly owned the stream launch handoff, initial SSE
  progress emission, and execution dispatch in a module that had no other
  consumer. That remained a real grouped seam rather than helper churn because
  it was the single-generation equivalent of the launch-owner pattern already
  used by background generation and chapter-regeneration stream launch.
  The refactor introduced
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  as the dedicated owner for the prepared execution -> SSE stream launch
  contract, moving the stream type alias plus launch/build/dispatch path into
  one auditable owner boundary. `chapter_single_generation_stream_workflow_service.rs`
  now narrows to `prepare request -> build launch -> delegate launch`, while
  the old `chapter_single_generation_stream_service.rs` was removed because it
  no longer owned an independent boundary. This keeps the existing
  `Chapter Generation` tracker label, preparing/generating/complete SSE
  events, result payload, done event, and error event behavior unchanged while
  aligning single-generation stream transport ownership with the background
  launch pattern already in use nearby. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (3 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation lane is now cleaner across
  request preparation, task create plan, background launch, stream launch,
  execution, and runtime-state ownership. The next move should stay in this
  neighborhood only if another real grouped execution/transport contract still
  spans multiple owners; otherwise switch to the next higher-signal Phase 5
  seam instead of forcing thinner stream-local cleanup.

- 2026-05-21 22:12 +08:00 checkpoint:
  this slice finished another real single-generation launch-owner split by
  removing the last single-background launch contract from the batch launch
  owner without changing route behavior, task creation semantics, runtime
  dispatch signatures, or response payload shape. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_resume_service.rs`
  showed that the batch launch owner still mixed two unrelated launch
  contracts: batch create/resume dispatch, and single-chapter background
  launch/response handoff. That remained a valid grouped seam rather than
  helper churn because the single-background path already has its own request
  preparation owner, task-create-plan owner, and runtime-state owner, so its
  final launch/dispatch boundary should be independently auditable instead of
  remaining embedded in a batch-focused launch module. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`
  as the dedicated owner for the single-background `launch + dispatch +
  response payload` contract. `chapter_single_generation_background_workflow_service.rs`
  now builds and delegates through that owner, while
  `chapter_batch_generation_runtime_launch_service.rs` narrows back to batch
  create/resume launch ownership only. This keeps the existing background
  task id / response payload shape, prepared execution input handoff, runtime
  dispatch behavior, and batch resume/create behavior unchanged while making
  the batch launch owner truly batch-only. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_service`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation lane is now close to local
  saturation across request prepare, task create plan, background launch,
  stream launch, execution, and runtime-state ownership. The next move should
  switch to another higher-signal Phase 5 seam unless a new cross-owner
  contract is first demonstrated nearby, rather than continuing with thinner
  single-generation wrapper cleanup.

- 2026-05-21 22:18 +08:00 checkpoint:
  this slice switched away from the near-saturated single-generation lane and
  continued the batch write-side owner split by extracting the batch create
  task-command contract from the mixed create/cancel/resume command owner
  without changing route behavior, batch create response payload shape,
  pending snapshot initialization, or resume/cancel semantics. A focused audit
  of
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the remaining batch task-command owner still mixed two distinct
  write-side boundaries: batch create plan ownership
  (`build_batch_generation_task_active_model`, `BatchGenerationCreatePlan`,
  `create_batch_generation_task_plan`) and the separate cancel/resume command
  path (`cancel_batch_generation_task`, `prepare_batch_generation_resume`,
  resume execution parsing). That remained a real grouped seam rather than
  helper churn because batch create now forms its own independent create-plan
  boundary used by batch create workflow plus runtime launch, while resume and
  cancel should continue drifting with task-state recovery semantics. The
  refactor introduced
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  as the dedicated owner for the batch create task-command contract, including
  the shared active-model builder that single-generation background task
  creation also reuses. `chapter_batch_generation_create_workflow_service.rs`
  and `chapter_batch_generation_runtime_launch_service.rs` now depend on the
  new create owner, while
  `chapter_batch_generation_task_command_service.rs` narrows to cancel/resume
  command ownership only. This keeps the existing batch task row defaults,
  create response payload, snapshot persistence, runtime launch inputs,
  single-generation shared active-model defaults, and resume/cancel behavior
  unchanged while making batch create planning auditable from one owner
  boundary instead of a mixed command service. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch write-side lane now has a cleaner split
  between create task-command ownership, runtime launch ownership, and
  cancel/resume command ownership. The next move should stay in this batch
  neighborhood only if another cross-owner write/read contract still spans
  multiple modules; otherwise reassess the next higher-signal Phase 5 seam
  instead of forcing thinner command-wrapper cleanup.

- 2026-05-21 22:25 +08:00 checkpoint:
  this slice continued the same batch write-side command lane by extracting
  the resume task-command contract from the remaining mixed cancel/resume
  owner without changing resume eligibility rules, resumed response payload
  shape, resume execution-plan semantics, or downstream runtime dispatch
  behavior. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that after the batch-create extraction, the old task-command owner
  still mixed two different command boundaries: cancel command ownership and
  the full resume recovery contract
  (`ResumeBatchGenerationPlan`, `ResumeExecutionPlan`,
  `prepare_batch_generation_resume`, plus chapter-id parsing and manual-review
  gating). That remained a real grouped seam rather than helper churn because
  resume now represents its own recovery/write-side boundary that is consumed
  by resume service, launch assembly, and runtime dispatch, while cancel is a
  single terminal command path with different lifecycle drift. The refactor
  introduced
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  as the dedicated owner for resume task-command semantics, including execution
  plan assembly, chapter-id parsing, manual-review blocking, and response
  payload handoff after snapshot reset. `chapter_batch_generation_resume_service.rs`,
  `chapter_batch_generation_runtime_launch_service.rs`, and
  `chapter_batch_generation_dispatch_service.rs` now depend on that new resume
  owner, while
  `chapter_batch_generation_task_command_service.rs` narrows further to the
  cancel command only. This keeps the existing single-chapter vs batch resume
  branching, normalized target-word-count behavior, snapshot reset semantics,
  and runtime dispatch inputs unchanged while making the batch command lane
  auditable as three separate owners: create, resume, and cancel. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch write-side command lane is now close to
  local saturation, with create, resume, and cancel boundaries split apart.
  The next move should stay in the batch neighborhood only if another
  cross-owner runtime/query contract is first demonstrated; otherwise switch
  to the next higher-signal Phase 5 seam instead of forcing thinner command
  cleanup.

- 2026-05-21 22:33 +08:00 checkpoint:
  this slice stopped extending the now near-saturated batch command lane and
  instead extracted the shared generation execution-config contract from the
  mixed access owner without changing access rules, prepared AI config
  defaults, default provider payload semantics, batch create/resume launch
  inputs, or single-generation prepare behavior. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_access_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_service.rs`,
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that the previous access owner still mixed two unrelated contracts:
  project/chapter accessibility checks and the shared execution-config
  assembly boundary (`PreparedGenerationExecutionConfig`,
  `prepare_generation_execution_config`, plus the default provider payload
  preparation). That remained a real cross-owner seam rather than helper churn
  because the same prepared execution config is consumed by batch create,
  batch resume, single-chapter prepare, and runtime launch, while access
  verification should remain independently auditable. The refactor introduced
  `backend-rs/src/services/chapter_generation_execution_config_service.rs` as
  the dedicated owner for shared generation execution-config assembly.
  `chapter_batch_generation_create_workflow_service.rs`,
  `chapter_batch_generation_resume_service.rs`,
  `chapter_single_generation_prepare_service.rs`, and
  `chapter_batch_generation_runtime_launch_service.rs` now depend on that new
  owner, while
  `chapter_batch_generation_access_service.rs` narrows back to access-only
  responsibilities (`verify_project_access` and
  `load_accessible_chapter_for_generation`). This keeps the existing access
  error mapping, AI config defaults, default provider payload fallback, batch
  launch inputs, and single-generation prepare semantics unchanged while
  making the shared execution-config boundary auditable from one owner module.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_execution_config_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_access_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_workflow_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch command lane should stay paused because
  create/resume/cancel plus shared execution-config ownership are now split.
  The next move in this neighborhood should only proceed if another concrete
  cross-owner runtime/query seam is demonstrated first; otherwise continue
  phase5 from a different higher-signal Rust owner boundary instead of
  returning to thin wrapper cleanup.

- 2026-05-21 23:02 +08:00 checkpoint:
  this slice continued phase5 Rust owner tightening by extracting the shared
  batch snapshot persistence contract from the mixed runtime-state owner
  without changing batch runtime checkpoint payloads, single-generation
  snapshot writes, batch create pending snapshot initialization, or read-side
  task view / stream loading semantics. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  showed that the old runtime-state owner still mixed two different shared
  contracts: batch task status progression / runtime execution, and the
  cross-owner `batch_generation_snapshot` read/write boundary used by batch
  runtime updates, single-generation runtime updates, create-time snapshot
  initialization, and read-side status/stream loaders. That remained a real
  owner split rather than helper churn because snapshot persistence is now a
  shared storage contract consumed across write-side batch runtime, write-side
  single runtime, and read-side task/stream query modules, while task-state
  mutation and runtime execution should remain independently auditable. The
  refactor introduced
  `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs` as
  the dedicated owner for snapshot loading, merge-vs-replace runtime-state
  writes, resume snapshot replacement, and initial queued snapshot creation.
  `chapter_batch_generation_runtime_state_service.rs` now narrows back to task
  mutation / runtime execution ownership, while single-generation runtime,
  batch create task command, batch task view query, and batch stream state
  loading all consume the new shared snapshot owner. This keeps the existing
  checkpoint JSON shape, quality-field clearing-on-replace semantics, read-side
  quality/status derivation, and batch/single runtime progress semantics
  unchanged while making the shared snapshot persistence boundary auditable
  from one owner module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service`
  (9 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service`
  (8 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_view_service`
  (0 tests matched; compile path passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch runtime lane now has a cleaner split
  between runtime checkpoint semantics, snapshot persistence, and task-state
  execution. The next step should prefer another concrete cross-owner seam
  such as shared read-side view-context loading or a different phase5 route
  group, rather than re-entering thin runtime helper cleanup.

- 2026-05-21 23:09 +08:00 checkpoint:
  this slice first updated the phase5 planning baseline before continuing Rust
  migration work. The refactor plan at
  `docs/architecture/rust-strangler-refactor-plan-2026-05-17.zh-CN.md` now
  records the corrected remaining-migration interpretation: phase5 must
  distinguish true Python owners from Rust-owned route groups that still keep
  Python fallback, and `/api/wizard/` must no longer be treated as a confirmed
  standalone Python API group because the real registered router on both
  Python and Rust sides remains `/api/wizard-stream/*`. After that planning
  correction, this slice continued the `chapters` neighborhood with a real
  batch read-side owner split by extracting the shared `task + snapshot +
  runtime/quality derived fields` assembly contract from the mixed status-view
  and task-view owners. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  showed that batch read-side context loading was still split across two
  owners: stream/status loading rebuilt `owned task + snapshot + stream
  semantics` locally, while task/status/active query loading separately rebuilt
  `task + snapshot + quality/runtime derived payload context`. That remained a
  real cross-owner seam rather than helper churn because both lanes depend on
  the same shared snapshot/read-side state while serving different transport
  adapters, and future changes to runtime-state-derived fields would otherwise
  drift across stream/status/query owners. The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  as the dedicated owner for loading owned/read-side batch context from task
  rows plus snapshot state and derived quality/runtime fields.
  `chapter_batch_generation_status_view_service.rs` now consumes that shared
  owner for stream-state loading, while
  `chapter_batch_generation_task_view_query_service.rs` now builds payload
  contexts from the same shared read-context boundary instead of loading
  snapshots and derived fields itself. This keeps existing stream status
  resolution, task status payload shape, active-task payload shape,
  manual-review semantics, and snapshot-derived quality fields unchanged while
  making batch read-side context assembly auditable from one owner module.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_view_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch read-side lane now has a cleaner split
  between shared read-context loading, payload adapters, and stream semantics.
  The next step should prefer another concrete shared seam such as active/status
  query access workflow consolidation or a stronger phase5 route-group smoke
  slice, instead of returning to thin read-side wrapper cleanup.

- 2026-05-21 23:28 +08:00 checkpoint:
  this slice continued the same batch read-side lane and completed the next
  concrete seam suggested by the previous checkpoint by consolidating the
  route-facing read workflows into one dedicated owner without changing any
  route payload shape, access decision, stream access behavior, or transport
  error mapping. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`,
  plus the previous thin workflow wrappers
  `chapter_batch_generation_active_query_workflow_service.rs` and
  `chapter_batch_generation_status_stream_workflow_service.rs`
  showed that the batch read-side route contract was still inconsistent:
  some endpoints called query services directly, some went through thin
  workflow wrappers, and the route surface no longer exposed a single owner for
  `status query + status stream + active project query + active task list`
  orchestration. That was a real owner-boundary cleanup rather than wrapper
  churn because the API layer now has one auditable read-workflow lane for all
  batch read-side route contracts instead of mixing direct query ownership with
  two partial workflow shims. The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_read_workflow_service.rs`
  as the dedicated owner for the route-facing batch read workflows:
  `load_batch_generation_status_read_workflow`,
  `create_batch_generation_status_stream_read_workflow`,
  `load_active_batch_generation_read_workflow`, and
  `load_active_batch_generation_task_list_read_workflow`.
  `backend-rs/src/api/chapter_batch_generation.rs` now depends on that single
  owner for all batch read-side endpoints, while the previous split workflow
  wrappers were removed. This keeps existing active/status JSON payloads,
  stream access gating, `404` / internal error mapping, keep-alive behavior,
  and downstream query/stream semantics unchanged while making the batch
  route-facing read workflow lane consistent and auditable from one owner
  module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_workflow_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch read-side route lane is now cleaner across
  route -> read workflow -> query/view/stream boundaries. The next step should
  prefer either a stronger `chapter_batch_generation` business/read-side smoke
  slice or another cross-owner seam outside this now cleaner read workflow
  surface, rather than reintroducing new thin workflow wrappers nearby.

- 2026-05-21 23:35 +08:00 checkpoint:
  this slice switched to the adjacent single-generation lane and completed a
  shared route-facing workflow consolidation without changing request parsing,
  chapter access checks, launch payload shape, SSE launch behavior, or
  transport error mapping. A focused audit of
  `backend-rs/src/services/chapter_single_generation_background_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and the consuming route/error-mapper surface in
  `backend-rs/src/api/chapter_batch_generation.rs` plus
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the route-facing single-generation contract still split one
  shared prepare/launch owner across two separate workflow modules:
  background generation owned `prepare -> task plan -> background launch`,
  while stream generation separately owned `prepare -> stream launch`,
  even though both lanes already shared the same single-generation request,
  access/config preparation, and launch-boundary semantics. That remained a
  real owner consolidation rather than wrapper churn because the API layer now
  has one auditable single-generation workflow owner instead of two parallel
  route-facing workflow modules that each re-owned parts of the same prepare
  boundary. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  as the dedicated owner for the route-facing single-generation workflow lane.
  It now owns the shared preparation boundary and exports both
  `create_single_generation_stream_workflow` and
  `start_owned_single_generation_background_workflow`, while the previous split
  background/stream workflow modules were removed.
  `backend-rs/src/api/chapter_batch_generation.rs` now consumes that single
  owner for both `/generate-stream` and `/generate-background`, and
  `chapter_batch_generation_error_mapper.rs` now references the consolidated
  background workflow error type from the new module. This keeps existing
  single-generation request compat handling, chapter access/config failures,
  background response payload shape, stream launch contract, and route error
  mapping unchanged while making the route-facing single-generation workflow
  lane consistent and auditable from one owner module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation`
  (105 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation route-facing lane is now
  cleaner across route -> workflow -> launch boundaries. The next step should
  prefer either a stronger single/batch generation business smoke slice or a
  different concrete cross-owner seam, rather than recreating split workflow
  ownership around the same single-generation prepare boundary.

- 2026-05-21 23:43 +08:00 checkpoint:
  this slice returned to the `chapter_batch_generation` write-side lane and
  consolidated the route-facing `create + resume` orchestration into one
  dedicated owner without changing project access checks, owned-task access
  checks, request compatibility normalization, execution-config preparation,
  runtime launch payload shape, dispatch behavior, or transport error mapping.
  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the batch write-side route contract still split one shared
  route-facing owner lane across two workflow modules even though both paths
  already followed the same high-level contract:
  `access/owned task -> prepare/plan -> shared execution config -> launch ->
  dispatch`. That remained a real owner consolidation rather than thin-wrapper
  churn because the API layer now has one auditable batch write-workflow owner
  for route-facing create/resume semantics instead of two parallel modules
  that each re-owned part of the same write-side orchestration lane, while
  `cancel` continues to stay independent as a separate command boundary. The
  refactor introduced
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  as the dedicated owner for
  `start_owned_batch_generation_write_workflow` and
  `resume_owned_batch_generation_write_workflow`, removed the previous split
  `chapter_batch_generation_create_workflow_service.rs` and
  `chapter_batch_generation_resume_service.rs`, and updated
  `backend-rs/src/api/chapter_batch_generation.rs` plus
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` to depend on
  the consolidated owner and error types. This keeps existing create/resume
  response payloads, 404/bad-request/internal mapping, execution-config
  defaults, launch contracts, and runtime dispatch semantics unchanged while
  making the batch write-side route workflow lane consistent and auditable
  from one owner module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service`
  (0 tests matched; compile path passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation`
  (105 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch route-facing write lane is now cleaner
  across route -> write workflow -> task/runtime launch boundaries. The next
  step should prefer either a stronger chapter-generation business/smoke proof
  slice or another concrete cross-owner seam outside the now consolidated
  create/resume workflow surface, rather than forcing `cancel` into the same
  owner without stronger shared-boundary evidence first.

- 2026-05-21 23:49 +08:00 checkpoint:
  this slice continued phase5 Rust owner tightening in the adjacent
  `chapter_analysis` read-side lane by extracting the shared
  `generation_history + latest candidate draft attempt` query boundary from
  split view and quality-metrics owners without changing chapter access rules,
  analysis payload shape, quality-metrics payload shape, draft/checker/quality
  fragment semantics, or route error mapping. A focused audit of
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`, and
  `backend-rs/src/services/chapter_quality_metrics_query_context_service.rs`
  showed that the analysis read-side still duplicated one concrete storage
  contract: both the analysis view lane and the quality-metrics lane loaded
  the same latest `chapter_draft_attempt` plus recent `generation_history`
  rows before deriving checker, draft, and quality fragments. That remained a
  real cross-owner seam rather than helper churn because future changes to the
  history window, candidate selection rule, or shared fragment inputs would
  otherwise drift between two read-side owners serving different payload
  adapters. The refactor introduced
  `backend-rs/src/services/chapter_analysis_read_context_service.rs` as the
  dedicated owner for loading the shared analysis read context from
  `generation_history` and `chapter_draft_attempt`. The existing
  `chapter_quality_metrics_query_context_service.rs` now delegates to that new
  owner, and `chapter_analysis_view_query_service.rs` now builds checker,
  draft, and quality fragments from the same shared read-context boundary
  instead of re-querying the storage contract locally. This keeps the existing
  analysis view JSON payload, quality-metrics response shape, latest-candidate
  semantics, checker extraction behavior, and access/error mapping unchanged
  while making the analysis read-side query boundary auditable from one owner
  module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis`
  (43 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_analysis` read-side now has a cleaner
  split between shared read-context loading and payload-specific fragment
  assembly. The next step should prefer either another concrete analysis
  route-facing workflow consolidation or a stronger chapter-domain smoke/proof
  slice, rather than duplicating similar history/candidate query logic in a
  third read owner.

- 2026-05-21 23:55 +08:00 checkpoint:
  this slice continued in the same `chapter_analysis` neighborhood and
  consolidated the route-facing read lane into one dedicated owner without
  changing chapter access checks, analysis view payload shape, quality-metrics
  payload shape, single-chapter analysis-status semantics, batch-analysis
  status semantics, or transport error mapping. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_query_service.rs`, and
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`
  showed that the analysis route surface still exposed four separate read-side
  owners directly to the API layer: chapter analysis view, quality metrics,
  single analysis task status, and batch analysis task status. That remained a
  real owner-boundary cleanup rather than wrapper churn because the route layer
  now gains one auditable `analysis` read-workflow lane instead of directly
  mixing multiple query owners with partially overlapping semantics. The
  refactor introduced
  `backend-rs/src/services/chapter_analysis_read_workflow_service.rs` as the
  dedicated owner for
  `load_chapter_analysis_read_workflow`,
  `load_chapter_quality_metrics_read_workflow`,
  `load_analysis_task_status_read_workflow`, and
  `load_batch_analysis_task_status_read_workflow`.
  `backend-rs/src/api/chapter_analysis_routes.rs` now depends on that single
  workflow owner for the four read endpoints, while draft load/apply and
  trigger dispatch remain independent boundaries. This keeps existing 404 /
  internal error mapping, chapter access behavior, batch-status response
  envelope, and downstream query semantics unchanged while making the
  `chapter_analysis` route-facing read lane consistent and auditable from one
  owner module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_read_workflow_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis`
  (46 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_analysis` route-facing read lane is
  now cleaner across route -> read workflow -> query/view/status boundaries.
  The next step should prefer either a concrete `chapter_analysis` write-side
  workflow consolidation or a stronger chapter-domain smoke/proof slice,
  rather than reintroducing direct route ownership over adjacent analysis read
  contracts.

- 2026-05-22 00:01 +08:00 checkpoint:
  this slice continued phase5 Rust migration in the same `chapter_analysis`
  neighborhood by splitting the route-facing draft workflow boundary away from
  the mixed draft core owner without changing chapter access rules, draft
  selection semantics, stale/preview conflict handling, apply/load payload
  shape, or transport error mapping. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/services/chapter_analysis_draft_service.rs`, and
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs`
  showed that the analysis draft lane still mixed two contracts inside one
  service owner: route-facing request parsing from route query/body types, and
  the core owned draft behavior of `load/apply -> access -> payload
  delegation`. That remained a real owner split rather than thin-wrapper churn
  because the API layer now gets one auditable route-facing workflow owner for
  auto-revision and candidate draft load/apply semantics, while the core draft
  owner narrows back to access normalization, selection mode handling, and
  draft payload delegation. The refactor introduced
  `backend-rs/src/services/chapter_analysis_draft_workflow_service.rs` as the
  dedicated owner for
  `load_auto_revision_draft_workflow`,
  `apply_auto_revision_draft_workflow`,
  `load_candidate_draft_workflow`, and
  `apply_candidate_draft_workflow`.
  `backend-rs/src/api/chapter_analysis_routes.rs` now depends on that
  workflow owner for the four draft endpoints, while
  `chapter_analysis_draft_service.rs` now accepts owned draft payload requests
  instead of route body/query types directly. This keeps existing draft error
  mapper behavior, chapter access mapping, selection-mode-specific 404
  messages, stale handling, preview-only candidate conflicts, and downstream
  apply/load payload semantics unchanged while making the draft route-facing
  write lane consistent and auditable from one owner module. Validation passed
  with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis`
  (48 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_analysis` draft route-facing write
  lane is now cleaner across route -> draft workflow -> owned draft core
  boundaries. The next step should prefer either consolidating the remaining
  trigger write boundary into a dedicated analysis write workflow owner or
  switching to stronger chapter-domain smoke/proof assets, rather than
  reintroducing route request parsing into the core draft owner.

- 2026-05-22 00:06 +08:00 checkpoint:
  this slice continued phase5 Rust migration in the same `chapter_analysis`
  neighborhood by consolidating the remaining route-facing trigger write lane
  into one dedicated workflow owner without changing chapter access checks,
  analysis task creation semantics, background dispatch behavior, response
  payload shape, or transport error mapping. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs` and
  `backend-rs/src/services/chapter_analysis_trigger_service.rs`
  showed that the analysis route surface still directly owned the two-step
  trigger orchestration of `prepare -> dispatch`, even after the adjacent
  draft write endpoints had already moved into a dedicated workflow boundary.
  That remained a real workflow consolidation rather than thin-wrapper churn
  because the API layer now gets one auditable `chapter_analysis` write owner
  for trigger dispatch semantics instead of mixing transport with background
  launch orchestration. The refactor introduced
  `backend-rs/src/services/chapter_analysis_write_workflow_service.rs`
  as the dedicated owner for
  `trigger_chapter_analysis_write_workflow`.
  `backend-rs/src/api/chapter_analysis_routes.rs` now delegates the analyze
  endpoint to that workflow owner, while
  `chapter_analysis_trigger_service.rs` remains focused on prepared trigger
  construction and runtime dispatch primitives. This keeps existing
  `PrepareChapterAnalysisTriggerError` mapping, task-create payload shape,
  background `tokio::spawn` dispatch timing, and route response body
  unchanged while making the `chapter_analysis` route-facing write lane
  consistent across `trigger` and `draft` flows. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_write_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis`
  (50 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_analysis` route-facing write lane is
  now cleaner across route -> write workflow -> trigger/runtime primitives,
  and this neighborhood has largely finished the high-signal workflow-owner
  tightening. The next step should prefer switching to stronger chapter-domain
  smoke/proof assets or identifying the next non-wrapper route/workflow seam
  outside `chapter_analysis`, rather than forcing another split in a now
  relatively stable owner boundary.

- 2026-05-22 00:12 +08:00 checkpoint:
  this slice continued phase5 Rust migration by leaving the now-stable
  `chapter_analysis` neighborhood and tightening the adjacent
  `chapter_crud` route-facing read lane into one dedicated workflow owner
  without changing chapter CRUD payload shape, navigation payload shape,
  annotations placeholder payload, quality-trend payload, can-generate
  payload, or the mixed success-message/detail transport error mapping already
  expected by callers. A focused audit of
  `backend-rs/src/api/chapter_crud_routes.rs`,
  `backend-rs/src/services/chapter_crud_workflow_service.rs`,
  `backend-rs/src/services/chapter_query_service.rs`,
  `backend-rs/src/services/chapter_annotation_query_service.rs`, and
  `backend-rs/src/services/chapter_quality_query_service.rs`
  showed that the chapter CRUD route surface still directly mixed four
  read-side owners: CRUD list/get payload assembly, chapter navigation query,
  annotations query, quality-trend query, and can-generate query. That
  remained a real route-facing workflow consolidation rather than thin-wrapper
  churn because the API layer now gets one auditable `chapter_crud` read owner
  instead of directly orchestrating several adjacent read contracts with
  different payload and error semantics. The refactor introduced
  `backend-rs/src/services/chapter_crud_read_workflow_service.rs`
  as the dedicated owner for
  `list_chapters_read_workflow`,
  `list_chapters_by_project_path_read_workflow`,
  `get_chapter_read_workflow`,
  `load_navigation_read_workflow`,
  `load_annotations_read_workflow`,
  `load_quality_trend_read_workflow`, and
  `load_can_generate_read_workflow`.
  `backend-rs/src/api/chapter_crud_routes.rs` now depends on that workflow
  owner for the chapter CRUD read endpoints, while create/update/delete and
  expansion-plan write behavior remain in their existing request/workflow
  owners. This keeps existing `chapter_crud_error_mapper` response shapes,
  `chapters_error_mapper` detail mapping, chapter/project access semantics,
  and downstream payload assembly unchanged while making the `chapter_crud`
  route-facing read lane consistent and auditable from one owner module.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_read_workflow_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_error_mapper`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud`
  (19 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_crud` route-facing read lane is now
  cleaner across route -> read workflow -> CRUD/query payload owners. The next
  step should prefer either a concrete `chapter_crud` write-side workflow
  consolidation if a real request/workflow seam still exists, or a stronger
  chapter-domain smoke/proof slice for `chapters`, rather than reintroducing
  multiple direct read-owner dependencies into the route layer.

- 2026-05-22 00:15 +08:00 checkpoint:
  this slice continued phase5 Rust migration in the same `chapter_crud`
  neighborhood by consolidating the remaining route-facing body-driven write
  lane into one dedicated workflow owner without changing create/update
  chapter payload shape, expansion-plan update payload shape, success-message
  error envelopes, chapter/project not-found semantics, or the existing
  delete behavior that already had no route-body transformation to extract. A
  focused audit of
  `backend-rs/src/api/chapter_crud_routes.rs`,
  `backend-rs/src/services/chapter_crud_request_service.rs`, and
  `backend-rs/src/services/chapter_crud_workflow_service.rs`
  showed that the chapter CRUD route surface still directly owned the route
  body -> owned request conversion for `create`, `update`, and
  `expansion-plan` writes, even after the read-side had already been moved
  into a dedicated workflow owner. That remained a real route-facing workflow
  consolidation rather than thin-wrapper churn because the API layer now gets
  one auditable `chapter_crud` write owner for body-driven write semantics,
  while the core CRUD workflow owner narrows back to payload assembly and
  service delegation. The refactor introduced
  `backend-rs/src/services/chapter_crud_write_workflow_service.rs`
  as the dedicated owner for
  `create_chapter_write_workflow`,
  `update_chapter_write_workflow`, and
  `update_expansion_plan_write_workflow`.
  `backend-rs/src/api/chapter_crud_routes.rs` now delegates those three write
  endpoints to the workflow owner, while `delete_chapter_payload` remains an
  acceptable direct dependency because there is no request-body conversion or
  adjacent route-facing workflow boundary to consolidate yet. This keeps
  existing `chapter_crud_error_mapper` response shapes, request-field
  semantics, chapter/project access behavior, and downstream CRUD payload
  assembly unchanged while making the `chapter_crud` body-driven write lane
  consistent across route -> write workflow -> core CRUD workflow boundaries.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_write_workflow_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_error_mapper`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud`
  (22 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_crud` route-facing write lane is now
  cleaner across route -> write workflow -> CRUD payload owner, and this
  neighborhood has largely finished the high-signal route/workflow tightening
  that does not devolve into wrappers. The next step should prefer either a
  stronger `chapters` smoke/proof slice or a new non-wrapper seam in
  `chapter_generation` / `chapter_regeneration`, rather than forcing a
  dedicated delete-only workflow layer with no meaningful boundary gain.

- 2026-05-22 00:19 +08:00 checkpoint:
  this slice continued phase5 Rust migration by leaving the now-cleaner
  `chapter_crud` neighborhood and tightening the adjacent
  `chapter_regeneration` body-driven write/stream lane into one dedicated
  workflow owner without changing partial-regenerate apply payload semantics,
  full regeneration SSE launch behavior, partial regeneration SSE launch
  behavior, regeneration task query semantics, or transport error mapping. A
  focused audit of
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  `backend-rs/src/services/chapter_regeneration_request_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`,
  `backend-rs/src/services/chapter_regeneration_apply_service.rs`, and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that the regeneration route surface still directly mixed three
  route-facing request conversions with downstream owners: partial apply route
  body -> owned apply request, full regenerate raw JSON -> typed stream
  request, and partial regenerate route body -> typed partial stream request.
  That remained a real workflow consolidation rather than thin-wrapper churn
  because the API layer now gets one auditable `chapter_regeneration` write
  owner for the body-driven write/stream semantics instead of mixing transport
  concerns with request normalization and workflow dispatch. The refactor
  introduced
  `backend-rs/src/services/chapter_regeneration_write_workflow_service.rs`
  as the dedicated owner for
  `apply_partial_regenerate_write_workflow`,
  `create_chapter_regeneration_stream_write_workflow`, and
  `create_partial_regeneration_stream_write_workflow`.
  `backend-rs/src/api/chapter_regeneration_routes.rs` now delegates those
  three body-driven endpoints to the workflow owner, while regeneration task
  read querying remains in its existing query owner and route-level SSE
  keep-alive wiring remains transport-local. This keeps existing
  `chapters_error_mapper` error shapes, request defaulting semantics,
  full/partial regeneration stream launch behavior, and downstream payload
  construction unchanged while making the `chapter_regeneration` route-facing
  write lane consistent across route -> write workflow -> apply/stream core
  owners. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_write_workflow_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration`
  (42 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper`
  (13 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `chapter_regeneration` body-driven write lane
  is now cleaner across route -> write workflow -> apply/stream owners, and
  the next step should prefer either a stronger `chapters` smoke/proof slice
  or the next non-wrapper seam in `chapter_generation`, rather than forcing
  another low-signal split inside already-focused regeneration core owners.

- 2026-05-22 00:23 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving from the freshly
  tightened `chapter_regeneration` lane into the remaining single-chapter
  generation seam inside `chapter_batch_generation` route ownership, without
  changing single-chapter background task creation semantics, single-chapter
  stream launch behavior, route payload shape, SSE keep-alive behavior, or
  existing transport error mapping. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_request_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that the route surface still directly owned the route body -> owned
  request conversion for the single-chapter generation background and stream
  endpoints, even though the surrounding batch-generation read/write lanes had
  already moved into dedicated workflow owners. That remained a real
  route-facing workflow consolidation rather than thin-wrapper churn because
  the API layer now gets one auditable owner for the body-driven single-chapter
  generation write semantics instead of mixing transport concerns with request
  normalization and downstream workflow dispatch. The refactor introduced
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  as the dedicated owner for
  `start_single_generation_background_write_workflow` and
  `create_single_generation_stream_write_workflow`.
  `backend-rs/src/api/chapter_batch_generation.rs` now delegates those two
  single-chapter generation endpoints to the workflow owner, while
  batch-generation create/resume, batch status reads, and SSE keep-alive
  wiring remain in their existing boundaries. This keeps existing
  `chapter_batch_generation_error_mapper` response shapes, compat-field
  consumption semantics, background launch behavior, and stream launch
  behavior unchanged while making the single-chapter generation route-facing
  write lane consistent across route -> write workflow -> single-generation
  core workflow owners. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation`
  (19 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-chapter generation body-driven write
  lane is now cleaner across route -> write workflow -> single-generation
  owners, and the next step should prefer either stronger `chapters` /
  generation smoke-proof assets or the next non-wrapper seam in adjacent
  generation surfaces, rather than reintroducing request normalization into
  the route layer.

- 2026-05-22 00:28 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  batch-create route-facing seam inside `chapter_batch_generation` without
  changing batch-create payload shape, compat-field handling, task launch
  semantics, downstream runtime dispatch behavior, or route-local transport
  concerns such as HTTP status/error mapping. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the batch-create route still directly owned the compat route
  payload -> owned workflow request conversion even after the surrounding
  read/write lanes had already moved into dedicated workflow owners. That
  remained a real workflow consolidation rather than thin-wrapper churn
  because the API layer now gets one auditable route-facing write owner for
  batch-create request normalization instead of mixing transport concerns with
  compat-field consumption and workflow dispatch. The refactor introduced
  `start_batch_generation_write_workflow_from_route(...)` in
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  so `backend-rs/src/api/chapter_batch_generation.rs` no longer builds a
  compat request locally before calling the owned workflow. Together with the
  earlier addition of
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`,
  the `chapter_batch_generation` route surface now keeps both batch-create and
  single-chapter body-driven generation writes aligned as
  route -> write workflow -> core owner boundaries, while preserving existing
  `chapter_batch_generation_error_mapper` response shapes and SSE keep-alive
  behavior. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper`
  (16 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation`
  (34 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_batch_generation` no longer keeps
  body-driven request normalization in the route layer for either batch-create
  or single-chapter generation writes, so the next high-signal move should
  prefer a stronger smoke/proof slice or another adjacent non-wrapper seam
  instead of forcing symmetry-only extractions.

- 2026-05-22 00:32 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening one remaining
  body-driven read seam in `chapter_analysis` without changing request body
  shape, batch analysis status payload structure, access/query behavior, or
  existing route-level error mapping. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/services/chapter_analysis_read_workflow_service.rs`, and
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`
  showed that `/chapters/analysis/status/batch` still kept its request-body
  schema in the route layer and extracted `chapter_ids` locally before
  delegating to the read workflow, even though the surrounding chapter
  analysis routes had already converged on route -> workflow -> query/service
  ownership. That remained a real seam because the API layer was still owning
  route-body normalization for a body-driven read endpoint. The refactor
  introduced
  `backend-rs/src/services/chapter_analysis_batch_status_request_service.rs`
  as the dedicated request owner for the batch-status route payload, and
  `load_batch_analysis_task_status_read_workflow(...)` now accepts the route
  request directly, normalizes/deduplicates `chapter_ids` through the request
  owner, and forwards the typed request to the query service. This keeps the
  existing `internal_detail_error` transport mapping and payload envelope
  unchanged while making the `chapter_analysis` batch-status path consistent
  with the rest of the route-facing workflow seams. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_batch_status_request_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_read_workflow_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_batch_status_query_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapters` route-group read/write body seams are
  now largely converged on service-owned request/workflow boundaries, so the
  next high-signal phase5 move should bias toward stronger smoke/proof assets
  or another adjacent route-group with visible route-local request ownership,
  not more symmetry-only extraction inside already-thin chapter routes.

- 2026-05-22 00:37 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening one remaining
  query-driven read seam in `chapter_batch_generation` without changing the
  `/chapters/batch-generate/active-tasks` query shape, limit semantics,
  response envelope, access behavior, or existing route-level error mapping.
  A focused audit of `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_read_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  showed that the `active-tasks` endpoint still kept its `limit` query schema
  in the route layer and passed `query.limit` directly into the read workflow,
  even though the surrounding batch-generation write/read seams had already
  converged on route -> workflow -> query/service ownership. That remained a
  real seam because the API layer was still owning a query-driven request
  contract and a normalization entrypoint for the active task list lane. The
  refactor introduced
  `backend-rs/src/services/chapter_batch_generation_active_list_request_service.rs`
  as the dedicated request owner for the `active-tasks` route query, and
  `load_active_batch_generation_task_list_read_workflow(...)` now accepts the
  route query directly, normalizes `limit` through the request owner, and
  forwards the typed request to the query service. This keeps the existing
  `map_active_batch_generation_task_list_query_error` transport mapping and
  `{ total, items }` payload envelope unchanged while making the
  `chapter_batch_generation` active-list path consistent with the other
  route-facing workflow seams. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_request_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_workflow_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service`
  (2 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_batch_generation` route-facing request
  boundaries are now close to saturation across body/query-driven create,
  single-generation, status-stream, and active-list seams, so the next
  high-signal phase5 move should prefer stronger smoke/proof assets or shift
  to another adjacent route-group with visible route-local ownership, rather
  than forcing additional symmetry-only extraction inside this lane.

- 2026-05-22 00:40 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening one remaining
  query-driven read seam in `chapter_regeneration` without changing the
  `/chapters/{chapter_id}/regeneration/tasks` query shape, limit semantics,
  response payload, chapter-access gate, or existing route-level error
  mapping. A focused audit of
  `backend-rs/src/api/chapter_regeneration_routes.rs` and
  `backend-rs/src/services/chapter_regeneration_query_service.rs`
  showed that the regeneration-task list endpoint still kept its `limit`
  query schema in the route layer and passed `query.limit` directly into the
  query owner, even though the surrounding regeneration write/stream seams had
  already converged on route -> workflow/request-owner -> core service
  boundaries. That remained a real seam because the API layer was still
  owning a query-driven request contract and a normalization entrypoint for a
  read path in an otherwise service-owned lane. The refactor introduced
  `backend-rs/src/services/chapter_regeneration_tasks_request_service.rs`
  as the dedicated request owner for the regeneration-task route query, and
  `load_owned_regeneration_tasks_payload(...)` now accepts a typed request
  instead of raw `Option<u64>`, so the request owner is solely responsible for
  `limit` normalization before the query service loads data. This keeps the
  existing `map_regeneration_tasks_query_error` transport mapping and
  `{ chapter_id, total, tasks }` payload unchanged while making the
  `chapter_regeneration` read path consistent with the route-facing workflow
  tightening already done in the chapter/generation lanes. Validation passed
  with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_tasks_request_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_query_error_mapper`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_regeneration` now has both its
  body-driven write/stream seams and its remaining query-driven read limit
  seam aligned behind service-owned request/workflow boundaries, so the next
  high-signal phase5 move should continue toward stronger smoke/proof assets
  or move to another adjacent route-group with visible route-local ownership,
  not reopen this lane for symmetry-only cleanup.

- 2026-05-22 00:44 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening a remaining raw
  body-driven request seam in `memories` without changing the
  `/memories/projects/{project_id}/search` payload shape, normalization
  semantics, query behavior, or existing route-level error mapping. A focused
  audit of `backend-rs/src/api/memories.rs` and
  `backend-rs/src/services/memories_request_service.rs` showed that the route
  still accepted `Json<Value>` and converted untyped JSON through
  `build_search_memories_request_from_value(&body)` before delegating to the
  query owner, even though the same lane already had a typed
  `SearchMemoriesRouteRequest -> SearchMemoriesRequest` builder. That remained
  a real seam because the API layer was still owning the raw payload contract
  for a body-driven search path, instead of delegating through the existing
  typed route request boundary. The refactor changed
  `backend-rs/src/api/memories.rs` so `search_memories` now accepts
  `Json<SearchMemoriesRouteRequest>` and delegates through
  `build_search_memories_request_from_route_payload(...)`, keeping the route
  on a typed contract rather than raw `serde_json::Value`. As a cleanup that
  reduced noise rather than changing behavior, the no-longer-used
  `build_search_memories_request_from_value(...)` helper and its fallback test
  were also removed from `memories_request_service.rs` so this slice would not
  leave a new dead-code warning behind. This keeps the existing
  `map_search_project_memories_payload_error` transport mapping and search
  response payload unchanged while making the `memories` search lane
  consistent with the other service-owned request boundaries. Validation
  passed with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_request_service`
  (3 tests passed after removing the unused value-based fallback case),
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper`
  (6 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories`
  (11 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `memories` still has additional room for request
  boundary tightening and possible workflow extraction, but the highest-signal
  raw-body seam on the search path is now gone, so the next move should again
  prefer either stronger smoke/proof assets or another clearly visible
  route-local ownership point instead of broad cleanup for its own sake.

- 2026-05-22 00:49 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  route-local write/business seam in `memories` for
  `/memories/projects/{project_id}/analyze-chapter/{chapter_id}` and
  `/memories/projects/{project_id}/chapters/{chapter_id}/memories`, without
  changing access policy, task-creation semantics, delete scope, success
  payload shape, or existing route-level error mapping. A focused audit of
  `backend-rs/src/api/memories.rs` showed that after the search-path request
  cleanup, the route layer still directly owned project-access gating, ORM
  deletion, and success-payload assembly for those two write paths, even
  though the broader phase5 direction is route -> workflow/request-owner ->
  service ownership. That remained a real seam because the API layer was still
  mixing transport concerns with write-side business orchestration and
  persistence work. The refactor introduced
  `backend-rs/src/services/memories_write_workflow_service.rs` as the
  dedicated write owner for
  `analyze_chapter_memories_write_workflow(...)` and
  `delete_chapter_memories_write_workflow(...)`. `backend-rs/src/api/memories.rs`
  now delegates both endpoints to the workflow owner, while
  `backend-rs/src/api/memories_error_mapper.rs` gained dedicated transport
  mapping for the new workflow error types so the existing Chinese detail
  strings and chapter-analysis task-create error mapping stay unchanged. As a
  small compile-only support fix rather than a behavior change,
  `LoadProjectAccessError` in `memories_query_service.rs` now derives `Debug`
  so the new workflow error enums can stay auditable without expanding trait
  requirements across unrelated legacy error types. This keeps the
  `章节分析任务已创建` response shape, delete-count message shape, project access
  semantics, and task creation behavior unchanged while making the `memories`
  write lane consistent with the workflow-style ownership already established
  across chapters/generation/regeneration. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_write_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper`
  (8 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories`
  (14 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `memories` now has its highest-signal raw-body and
  route-local write seams tightened behind service-owned request/workflow
  boundaries, so the next phase5 move should probably shift to stronger
  smoke/proof assets or another adjacent route-group with similarly visible
  transport/business overlap, instead of continuing broad cleanup inside this
  lane.

- 2026-05-22 00:57 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  route-local consistency/fix seam in `projects` for
  `/projects/{project_id}/fix-organizations`,
  `/projects/{project_id}/fix-member-counts`, and
  `/projects/{project_id}/check-consistency`, without changing the existing
  payload shapes, `auto_fix` query semantics, project-access policy, or
  transport-level HTTP error mapping. A focused audit of
  `backend-rs/src/api/projects.rs` and
  `backend-rs/src/services/project_consistency_query_service.rs` showed that
  the route layer still directly owned `auto_fix` query normalization,
  project-access gating, consistency repair execution, and success payload
  assembly, even though the surrounding phase5 direction has already converged
  on route -> request/workflow owner -> service boundaries in the chapter and
  memories lanes. That remained a real seam because the API layer was still
  mixing transport concerns with consistency orchestration and persistence
  operations. The refactor introduced
  `backend-rs/src/services/project_consistency_write_workflow_service.rs` as
  the dedicated owner for
  `fix_project_organizations_write_workflow(...)`,
  `fix_project_member_counts_write_workflow(...)`, and
  `check_project_consistency_write_workflow(...)`, plus a shared
  `normalize_project_consistency_auto_fix(...)` helper so the query-driven
  route boundary is now typed and explicit rather than re-decoded inline.
  `backend-rs/src/api/projects.rs` now delegates all three consistency/fix
  endpoints to the workflow owner and only keeps transport concern plus error
  translation; small mapper tests were added there to lock the unchanged
  `Project not found` and internal-detail HTTP behavior. This keeps the
  existing consistency response envelope, fixed/checked counters, and
  `auto_fix=false|0` semantics unchanged while aligning the `projects`
  consistency lane with the service-owned workflow pattern already established
  in `chapter_*` and `memories`. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" project_consistency_write_workflow_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" project_consistency_query_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" projects`
  (3 mapper tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `projects` now has its highest-signal
  consistency/fix route-local ownership pulled behind a service-owned workflow
  boundary, so the next phase5 move should prefer either another narrow
  project-facing seam with clear transport/business overlap or a stronger
  smoke/proof asset pass, rather than broad cleanup across the whole route
  module.

- 2026-05-22 01:04 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  import-oriented route-local ownership in `projects` for
  `/projects/validate-import` and `/projects/import`, without changing the
  multipart transport contract, JSON validation envelope, file-size limit,
  `Missing project field` failure, or successful import response shape. A
  focused audit of `backend-rs/src/api/projects.rs` showed that after the
  consistency/fix seam extraction, the route layer still directly owned import
  payload JSON decoding, validation/statistics assembly, size-limit guarding,
  chapter word-count normalization, project/chapter insert orchestration, and
  success payload assembly. That remained a real seam because the API layer
  was still mixing multipart transport handling with import-domain validation
  and write orchestration. The refactor introduced
  `backend-rs/src/services/project_import_workflow_service.rs` as the new
  service owner for both
  `validate_project_import_payload(...)` and
  `import_project_write_workflow(...)`. `backend-rs/src/api/projects.rs` now
  keeps multipart file extraction at the transport boundary but delegates
  validation envelope construction, import write orchestration, and response
  payload ownership to the workflow service, while dedicated mapper helpers
  preserve the existing invalid-JSON, payload-too-large, and missing-project
  HTTP behavior. Focused tests were added on both the workflow owner and the
  route mapper surface so this slice stays compatibility-auditable even before
  a full database-backed import integration test is added. This keeps the
  existing `valid/version/project_name/statistics/errors/warnings` validation
  payload, the `项目导入成功` response envelope, and the 50MB limit unchanged
  while aligning the `projects` import lane with the same route ->
  service-owned request/workflow boundary style already established in
  `chapter_*`, `memories`, and `projects consistency`. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" project_import_workflow_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" projects`
  (5 mapper tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `projects` now has both its consistency/fix seam
  and its highest-signal import validation/write seam pulled behind focused
  service owners, so the next phase5 move should likely prefer either a narrow
  export-side payload-ownership slice or a stronger smoke/proof pass instead
  of reopening the same route for broad cleanup.

- 2026-05-22 01:08 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  export-oriented route-local payload/text ownership in `projects` for
  `/projects/{project_id}/export-data` and `/projects/{project_id}/export`,
  without changing project-access policy, JSON export envelope, TXT export
  body format, filename normalization, content type, or attachment headers. A
  focused audit of `backend-rs/src/api/projects.rs` showed that after the
  import seam extraction, the route layer still directly owned export JSON
  payload assembly, TXT body formatting, and safe filename normalization even
  though query-side project/chapter loading was already delegated through
  `project_export_query_service`. That remained a real seam because the API
  layer was still mixing transport response building with export-domain view
  shaping. The refactor introduced
  `backend-rs/src/services/project_export_payload_adapter_service.rs` as the
  dedicated owner for
  `build_project_export_data_payload(...)`,
  `build_project_export_txt_content(...)`,
  `build_safe_project_export_json_filename(...)`, and
  `build_safe_project_export_txt_filename(...)`. `backend-rs/src/api/projects.rs`
  now delegates both export payload/text construction and filename
  normalization to that adapter service while keeping `Response`/header
  construction at the transport boundary. Focused tests were added on the
  adapter so the unchanged JSON envelope, TXT content shape, and filename
  normalization remain explicit and reviewable. This keeps the existing
  `version/export_type/project/chapters/statistics/options` export JSON shape,
  the `项目：...` / `第 N 章：...` TXT layout, and the attachment filename
  behavior unchanged while aligning the `projects` export lane with the same
  route -> query owner -> payload adapter boundary style already established
  across other phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" project_export_payload_adapter_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" projects`
  (5 mapper tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `projects` now has its highest-signal consistency,
  import, and export route-local ownership seams pulled behind focused service
  boundaries, so the next phase5 move should likely pivot to a different
  route-group or strengthen smoke/proof assets instead of continuing broad
  cleanup inside `projects.rs`.

- 2026-05-22 01:15 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving a narrow but real
  route-local payload/query seam out of `background_tasks`, without changing
  task execution behavior, deduplication semantics, task status values, SSE
  keep-alive behavior, or existing compat response shapes. A focused audit of
  `backend-rs/src/api/background_tasks.rs` showed that the route layer still
  directly owned task compat payload assembly, task-list response assembly,
  missing-task fallback payload assembly, stream connected-event assembly, and
  `statuses` query normalization, even though the actual task registry,
  checkpoint mutation, task execution, and stream fanout ownership already
  lived in dedicated task/runtime modules. That remained a real seam because
  the API layer was still mixing transport boundaries with reusable payload
  shaping and request normalization logic for a shared task domain. The
  refactor introduced
  `backend-rs/src/services/background_task_payload_adapter_service.rs` for
  `compatible_task_payload(...)`, `enrich_task_payload(...)`,
  `build_task_list_response(...)`, `build_missing_task_payload(...)`, and
  `build_connected_task_event(...)`, plus
  `backend-rs/src/services/background_task_request_service.rs` for
  `normalize_task_statuses_query(...)`. `backend-rs/src/api/background_tasks.rs`
  now delegates those compat payload and query-normalization concerns to the
  new service owners while keeping permission checks, task execution spawn,
  checkpoint mutation, and SSE response/keep-alive transport wiring in the
  route/runtime boundary. Focused tests were added on both new services so the
  unchanged `success/data/items/total` list payload, the cancelled-shaped
  missing-task payload, the connected SSE event contract, and the known-status
  filter semantics stay explicit and reviewable. This keeps the current
  background-task API surface stable while aligning the lane with the same
  route -> request/payload owner -> runtime/service ownership pattern used in
  the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" background_task_payload_adapter_service`
  (5 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" background_task_request_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" background_tasks`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `background_tasks` now has its most obvious
  route-local compat payload and query-normalization ownership extracted, so
  the next phase5 move should either continue into another adjacent route-group
  with similarly clear ownership overlap or strengthen smoke/proof assets
  rather than reopen this lane for symmetry-only cleanup.

- 2026-05-22 01:22 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the narrowest
  request/import helper ownership seam out of `prompt_workshop`, without
  changing cloud proxy transport behavior, workshop server/client branching,
  admin permission checks, local import write semantics, or existing success
  payload shapes. A focused audit of
  `backend-rs/src/api/prompt_workshop.rs` showed that the route layer still
  directly owned tag normalization, workshop user identifier assembly,
  required cloud item field extraction, and cloud-item-to-writing-style import
  workflow assembly even though those concerns were reused across request,
  import, and admin paths and were not inherently transport-specific. That
  remained a real seam because the API layer was still mixing HTTP/proxy
  boundaries with reusable request normalization and import preparation logic.
  The refactor introduced
  `backend-rs/src/services/prompt_workshop_request_service.rs` as the focused
  owner for `normalize_tags_value(...)`,
  `workshop_instance_id(...)`,
  `workshop_user_identifier(...)`,
  `required_workshop_text(...)`, and
  `create_writing_style_from_workshop_item(...)`.
  `backend-rs/src/api/prompt_workshop.rs` now delegates those helper concerns
  to the new request/workflow owner while keeping `proxy_workshop_request(...)`,
  route-level transport status mapping, and server/client mode branching in
  the API boundary. The migration also removed the now-unused
  `user_identifier(...)` / `get_user_identifier(...)` helper remnants from
  `prompt_workshop_service` so this seam does not leave fresh ownership drift
  behind. Focused tests were added on the new service so the unchanged CSV /
  JSON tags normalization behavior, instance-prefixed user identifier
  semantics, and required cloud item text-field validation stay explicit and
  reviewable. This keeps the existing prompt-workshop route surface stable
  while aligning the lane with the same route -> request/payload owner pattern
  established in the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop_request_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop`
  (the same 4 request-service tests matched via filter; command completed
  successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_workshop` now has its highest-signal
  route-local request/import helper ownership extracted, so the next phase5
  move should likely either stop before the larger cloud-proxy seam or choose
  another route-group with similarly narrow route-local semantics instead of
  pushing deeper into `proxy_workshop_request(...)` prematurely.

- 2026-05-22 01:28 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the shared
  route-local user/admin helper ownership seam out of `users` and `admin`,
  without changing admin-only access policy, user payload field names,
  default-password semantics, password hashing behavior, or existing
  create/update/delete/reset success shapes. A focused audit of
  `backend-rs/src/api/users.rs` and `backend-rs/src/api/admin.rs` showed that
  both route groups still directly owned the same helper concerns:
  `user_to_value(...)`, admin permission checks, password hashing, default
  password derivation, user lookup, and admin-count lookup. That remained a
  real seam because the API layer was still duplicating reusable request /
  payload / lookup logic across two separate transport surfaces even though
  the business rules themselves were already route-local and stable. The
  refactor introduced
  `backend-rs/src/services/user_admin_route_service.rs` as the focused owner
  for `api_error(...)`, `check_admin(...)`, `user_to_value(...)`,
  `hash_password(...)`, `default_password_for_username(...)`,
  `admin_count(...)`, and `find_user(...)`.
  `backend-rs/src/api/users.rs` and `backend-rs/src/api/admin.rs` now
  delegate those shared helper concerns to the new route-support service while
  keeping create/update/delete/reset policy checks, status transitions, and
  response transport assembly in the API boundary. This keeps the current
  `/users` and `/admin/users` behavior stable while reducing ownership drift
  between the two route groups and aligning them with the same route ->
  request/payload helper owner pattern used in the earlier phase5 slices.
  Focused tests were added on the new service so the unchanged forbidden
  admin-check contract, user JSON payload shape, default-password semantics,
  password hashing contract, and `detail` error envelope remain explicit and
  reviewable. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_route_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" users`
  (0 tests matched; command completed successfully),
  `cargo test --manifest-path "backend-rs/Cargo.toml" admin`
  (the same 4 user-admin service tests matched via filter; command completed
  successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `users/admin` now has its highest-signal shared
  route-local helper ownership extracted, so the next phase5 move should
  likely continue toward another duplicated request/payload seam rather than
  reopen this lane for symmetry-only cleanup.

- 2026-05-22 01:34 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the narrow auth
  password-status and password-write workflow seam out of `auth`, without
  changing OAuth flow behavior, LinuxDO callback semantics, cookie rendering,
  token refresh behavior, or the existing password status / set / initialize
  response shapes. A focused audit of `backend-rs/src/api/auth.rs` showed that
  the route layer still directly owned password status payload assembly,
  default-password derivation, password hashing reuse, password-row lookup,
  insert-vs-update branching for password writes, and initialize-only guard
  semantics even though those concerns were isolated to the `/auth/password/*`
  sub-surface and were not transport-specific. That remained a real seam
  because the API layer was still mixing auth transport wiring with reusable
  password workflow logic, while also carrying a second local password-hash
  owner after the new `user_admin_route_service` had already established a
  shared helper boundary for the same contract. The refactor introduced
  `backend-rs/src/services/auth_password_workflow_service.rs` as the focused
  owner for `load_password_status_workflow(...)`,
  `set_password_workflow(...)`, and
  `initialize_password_workflow(...)`, while reusing
  `hash_password(...)`, `default_password_for_username(...)`, and `find_user(...)`
  from `user_admin_route_service`.
  `backend-rs/src/api/auth.rs` now delegates password status and password
  write concerns to the new workflow owner while keeping OAuth route parsing,
  cookie I/O, response transport construction, and login/session boundaries in
  the API layer. Focused tests were added on the new workflow owner so the
  unchanged `has_password/has_custom_password/username/default_password`
  payload shape and the `success/message` password-write success envelope stay
  explicit and reviewable. This keeps the current auth surface stable while
  aligning the password lane with the same route -> request/workflow owner
  pattern established in the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" auth_password_workflow_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" auth`
  (10 tests passed, including the new auth-password workflow tests and
  existing cookie tests), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `auth` now has its highest-signal password
  route-local workflow ownership extracted, so the next phase5 move should
  favor another narrow request/payload seam instead of broadening into the
  larger OAuth/cookie/login flow unless a stronger compatibility proof need
  emerges.

- 2026-05-22 01:39 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the read-side
  relationship query and graph-payload ownership seam out of `relationships`,
  without changing create/update/delete behavior, project-access policy,
  graph payload field names, relationship-type ordering, or existing
  not-found/internal error envelopes. A focused audit of
  `backend-rs/src/api/relationships.rs` showed that after the write-side CRUD
  paths were already delegated through `RelationshipService`, the route layer
  still directly owned project-access checks for read-only routes,
  relationship-type query ordering, project-relationship list filtering, and
  graph node/link payload assembly for characters, relationships, and
  organization-membership edges. That remained a real seam because the API
  layer was still mixing transport/error boundaries with reusable read-side
  query and payload construction logic. The refactor introduced
  `backend-rs/src/services/relationship_query_service.rs` as the focused owner
  for `verify_relationship_project_access(...)`,
  `list_relationship_types(...)`,
  `list_project_relationship_models(...)`, and
  `build_relationship_graph_payload(...)`.
  `backend-rs/src/api/relationships.rs` now delegates those read-side access,
  query, and graph payload concerns to the new query service while keeping the
  transport-layer error mapping and write-side CRUD delegation in the route
  boundary. Focused tests were added on the new service so the unchanged graph
  `nodes/links` contract, member-edge `组织成员·...` relationship label, and
  link payload fields remain explicit and reviewable. This keeps the current
  relationship API surface stable while aligning the read lane with the same
  route -> query/payload owner pattern established in the earlier phase5
  slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" relationship_query_service`
  (1 test passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" relationships`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `relationships` now has its highest-signal
  read-side query and graph payload ownership extracted, so the next phase5
  move should likely prefer another narrow request/payload seam such as a
  lightweight request-normalization lane rather than reopen this route-group
  for symmetry-only cleanup.

- 2026-05-22 01:46 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the export-side prompt
  template payload/statistics ownership seam out of `prompt_templates`,
  without changing the `/prompt-templates/export` response shape, customized
  vs system-default classification, `system_content_hash` semantics, export
  `version`, or `export_time` field. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` showed that although template
  lookup already lived in `PromptTemplateService`, the route still directly
  owned export item assembly, duplicate system-default filtering,
  `parameters` serialization for system templates, and export statistics
  counting. That remained a real seam because the API layer was still mixing
  transport/error boundaries with reusable export payload construction logic.
  The refactor introduced
  `backend-rs/src/services/prompt_template_export_payload_adapter_service.rs`
  as the focused owner for
  `build_prompt_template_export_payload(...)`.
  `backend-rs/src/api/prompt_templates.rs` now delegates export payload
  assembly to that adapter while keeping user template query and HTTP error
  mapping in the route boundary. Focused tests were added on the new adapter
  so the unchanged mixed customized/system export list, duplicate
  system-default suppression, and `statistics.total/customized/system_default`
  contract remain explicit and reviewable. This keeps the current
  `templates/export_time/version/statistics` export surface stable while
  aligning the lane with the same route -> query owner -> payload adapter
  pattern already used in the earlier phase5 slices for `projects`,
  `background_tasks`, and `relationships`. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_export_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` still has additional payload /
  merge seams (`categories`, `sync-status`, `import`, `update merge`), but
  this export slice was the highest-signal low-risk cut. The next phase5 move
  should prefer either another narrow `prompt_templates` payload seam such as
  category aggregation or a similarly bounded request/payload seam in another
  route-group, rather than jumping into the broader import workflow in one
  pass.

- 2026-05-22 02:03 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the category-group
  prompt template payload ownership seam out of `prompt_templates`, without
  changing the `/prompt-templates/categories` response shape, user-template
  override precedence, system-default backfill behavior, category-group
  sorting, or the existing `未分类` fallback semantics. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` showed that after the export seam
  extraction, the route still directly owned category map construction,
  per-template JSON payload shaping, system-default suppression when a user
  override exists, synthetic system template timestamp filling, and grouped
  response sorting. That remained a real seam because the API layer was still
  mixing transport/error boundaries with reusable category aggregation payload
  construction. The refactor introduced
  `backend-rs/src/services/prompt_template_category_payload_adapter_service.rs`
  as the focused owner for
  `build_prompt_template_categories_payload(...)`.
  `backend-rs/src/api/prompt_templates.rs` now delegates category-group
  payload assembly to that adapter while keeping template sync, user template
  query, and HTTP error mapping in the route boundary. Focused tests were
  added on the new adapter so the unchanged grouped category payload, per-key
  sorting, user override precedence over system defaults, empty-category
  fallback, and system-template parameter serialization remain explicit and
  reviewable. This keeps the current category-group response stable while
  further aligning `prompt_templates` with the same route -> query owner ->
  payload adapter pattern already used across the earlier phase5 slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_category_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has both its export payload
  seam and category aggregation seam pulled behind focused adapters, so the
  next phase5 move should likely prefer either `sync-status` item aggregation
  as another bounded read-side payload/query seam or pivot to a different
  route-group with a similarly narrow request/payload owner split, rather than
  jumping straight into the broader `import` workflow or `update merge` path.

- 2026-05-22 01:55 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the read-side prompt
  template sync-status aggregation seam out of `prompt_templates`, without
  changing the `/prompt-templates/sync-status` response shape, managed-only
  filtering semantics, per-item `build_sync_status(...)` payload contract, or
  the pre-query managed-template auto-sync behavior. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` showed that after the export and
  category payload extractions, the route still directly owned four distinct
  read-side concerns: running managed-template sync, selecting the template
  key set from `managed_keys` vs all system templates, loading each user
  template row, and assembling the `total/managed_only/items` response shell.
  That remained a real seam because the API layer was still mixing
  transport/error boundaries with reusable sync-status query orchestration.
  The refactor introduced
  `backend-rs/src/services/prompt_template_sync_status_query_service.rs`
  as the focused owner for
  `load_prompt_template_sync_status_payload(...)`,
  plus internal helpers for response-shell assembly and managed/all key
  selection. `backend-rs/src/api/prompt_templates.rs` now delegates the whole
  sync-status read flow to that query owner while keeping only transport-layer
  extraction and HTTP error mapping in the route boundary. Focused tests were
  added on the new query service so the unchanged
  `total/managed_only/items` shell and the managed-only subset semantics stay
  explicit and reviewable. This keeps the current sync-status API surface
  stable while further aligning `prompt_templates` with the same route ->
  query owner -> payload/service owner pattern used across the earlier phase5
  slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_sync_status_query_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has its export payload,
  category aggregation, and sync-status query aggregation seams pulled behind
  focused owners, so the next phase5 move should likely either pivot to a
  different route-group with a similarly bounded request/payload seam or take
  a much narrower `prompt_templates` write-side cut, rather than jumping into
  the broader `import` workflow in one pass.

- 2026-05-22 01:59 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the remaining
  route-local request normalization seam out of
  `writing_styles/{style_id}/set-default`, without changing body-over-query
  precedence, the `project_id is required` bad-request contract, or the
  downstream `WritingStyleService::set_default_style(...)` call boundary. A
  focused audit of `backend-rs/src/api/writing_styles.rs` showed that the
  route was already thin overall, but it still directly owned one reusable
  request concern: merging `project_id` from optional JSON body and query
  params, then deciding whether the request should fail before entering the
  service layer. That remained a real seam because the API layer was still
  mixing transport extraction with reusable request normalization logic. The
  refactor introduced
  `backend-rs/src/services/writing_style_request_service.rs`
  as the focused owner for
  `build_set_default_style_project_id(...)`, plus a typed
  `BuildSetDefaultStyleRequestError::MissingProjectId` contract.
  `backend-rs/src/api/writing_styles.rs` now delegates that normalization and
  missing-field decision to the request service while keeping path/query/body
  extraction, HTTP status mapping, and the actual write-side service call in
  the route boundary. Focused tests were added on the new request service so
  the unchanged body-over-query precedence, query-only fallback, and
  missing-project rejection stay explicit and reviewable. This keeps the
  current `set-default` API surface stable while aligning `writing_styles`
  with the same route -> request owner -> workflow/service pattern used in the
  earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" writing_style_request_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" writing_styles`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `writing_styles` now has its most obvious remaining
  route-local request seam pulled behind a focused request owner, so the next
  phase5 move should likely pivot again to another route-group with a stronger
  payload/query seam rather than reopening `writing_styles` for symmetry-only
  cleanup.

- 2026-05-22 02:15 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the duplicated
  password-reset write workflow seam out of `users` and `admin`, without
  changing either route's success payload shape, default-password derivation,
  `user_passwords` upsert semantics, or the intentionally different handling
  of empty-string `new_password` between the two admin surfaces. A focused
  audit of `backend-rs/src/api/users.rs` and `backend-rs/src/api/admin.rs`
  showed that both routes still directly owned the same write workflow:
  loading the target user, deciding whether to fall back to the default
  password, hashing the chosen password, upserting `user_passwords`, and
  returning a success payload. That remained a real seam because the API layer
  was still mixing transport/policy boundaries with reusable password reset
  workflow logic. The refactor introduced
  `backend-rs/src/services/user_admin_password_reset_workflow_service.rs`
  as the focused owner for
  `reset_user_password_workflow(...)`,
  `build_user_reset_password_payload(...)`, and
  `build_admin_reset_password_payload(...)`, plus a
  `PasswordResetMode` contract that explicitly preserves the existing
  behavioral asymmetry:
  `/users/reset-password` falls back to the default password when
  `new_password` is missing or empty, while
  `/admin/users/{userId}/reset-password` only falls back when the field is
  missing and still treats an explicit empty string as a custom-password input
  to preserve current behavior.
  `backend-rs/src/api/users.rs` and `backend-rs/src/api/admin.rs` now keep
  permission checks, self-protection policy, path/body extraction, and HTTP
  error mapping in the route boundary while delegating the shared password
  write flow and response payload construction to the workflow owner. Focused
  tests were added on the new workflow service so the unchanged default-vs-
  custom decision semantics and the two route-specific success payload shapes
  remain explicit and reviewable. This keeps both admin password-reset API
  surfaces stable while reducing one more duplicated write-side owner split
  across the `users/admin` lane. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_password_reset_workflow_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" users`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `users/admin` now has both its shared helper seam
  and duplicated password-reset write workflow pulled behind focused owners,
  so the next phase5 move should likely pivot to another route-group with a
  similarly bounded request/payload/query seam rather than continue chasing
  smaller symmetry-only cleanup in the same lane.

- 2026-05-22 02:34 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the `settings` models
  payload / fallback ownership seam out of `settings`, without changing the
  `/settings/models` `provider/models/count` response contract, fallback
  `message` and `fallback_applied` semantics, the
  `/settings/fetch-models` `success/models/message/error/error_type` shell, or
  the existing `normalize_fetch_models_payload(...)` `id/owned_by`
  normalization behavior. A focused audit of
  `backend-rs/src/api/settings.rs` showed that the route group still directly
  owned five read-side payload concerns even after earlier phase5 slices:
  available-models success shell assembly, available-models fallback shell
  assembly, fetch-models success shell assembly, fetch-models fallback / failure
  shell assembly, and fetch-model payload normalization across `id`/`value`/
  `name`/`label` input shapes. That remained a real seam because the API layer
  was still mixing transport/runtime-setting resolution with reusable payload
  adaptation logic. The refactor introduced
  `backend-rs/src/services/settings_models_payload_adapter_service.rs`
  as the focused owner for
  `build_available_models_payload(...)`,
  `build_available_models_fallback_payload(...)`,
  `normalize_fetch_models_payload(...)`,
  `build_fetch_models_success_payload(...)`,
  `build_fetch_models_fallback_payload(...)`, and
  `build_fetch_models_failure_payload(...)`.
  `backend-rs/src/api/settings.rs` now keeps only effective-settings
  resolution, provider fetch invocation, and route-level success/fallback
  branching in `get_available_models(...)` and `fetch_models_endpoint(...)`,
  while delegating the JSON shell construction to the new adapter owner. Focused
  tests were added on the new adapter service so the unchanged success,
  fallback, failure, and normalization contracts remain explicit and
  reviewable. This keeps the current settings API surface stable while aligning
  the route group with the same route -> payload owner split used across the
  earlier phase5 seams. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_models_payload_adapter_service`
  (4 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings`
  (7 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `settings` now has one more pure read-side payload
  seam pulled behind a focused owner, so if this route group continues in
  phase5 the next move should stay at the same adapter/query level rather than
  expand into the provider probing workflow.

- 2026-05-22 03:02 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving two remaining narrow
  `settings` route-local seams behind focused owners, without changing the
  `/settings/api-key` trimmed `api_key/has_api_key` response contract,
  `create_preset_from_current(...)` query-over-body precedence for non-empty
  `name` and `description`, the default `"My Preset"` fallback, or the
  existing treatment of blank query values as missing while still allowing an
  empty-string body `description` to flow through unchanged. A focused audit of
  `backend-rs/src/api/settings.rs` showed that after the models payload
  extraction, the route group still directly owned two more compatibility-safe
  concerns: the stored-api-key response shell and the
  `create_preset_from_current(...)` request normalization that merged optional
  query/body fields before entering `SettingsService`. Those remained real
  seams because the API layer was still mixing transport/database access with
  reusable payload/request adaptation logic. The refactor introduced
  `backend-rs/src/services/settings_api_key_payload_adapter_service.rs`
  as the focused owner for `build_stored_api_key_payload(...)`, and
  `backend-rs/src/services/settings_preset_request_service.rs`
  as the focused owner for
  `build_create_preset_from_current_request(...)` plus the typed
  `CreatePresetFromCurrentRouteQuery`,
  `CreatePresetFromCurrentRouteBody`, and
  `CreatePresetFromCurrentRequest` contracts.
  `backend-rs/src/api/settings.rs` now keeps only settings loading, error
  mapping, and the downstream `SettingsService::create_preset_from_current(...)`
  call boundary, while delegating the stored-api-key payload shell and preset
  request merge semantics to the new service owners. Focused tests were added
  on both new modules so the unchanged trim/default/precedence contracts stay
  explicit and reviewable. This keeps the current settings API surface stable
  while continuing the same route -> payload/request owner split used across
  the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_api_key_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_preset_request_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings`
  (12 tests passed after both additions), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `settings` now has its obvious low-risk read-side
  payload seam and one lightweight request-normalization seam extracted, so the
  next phase5 move in this route group should continue only if another
  adapter/query-level cut exists; the provider probing / API test workflows
  should remain out of scope until a comparably narrow seam is identified.

- 2026-05-22 03:21 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the preset-probe request
  normalization seam out of `settings/test_preset(...)`, without changing the
  route's existing settings-not-found / preset-not-found error contracts, the
  provider key precedence (`api_provider` before legacy `provider`), the model
  key precedence (`llm_model` before legacy `model`), the existing numeric
  `temperature` pass-through semantics, or the `max_tokens` `u32` range guard.
  A focused audit of `backend-rs/src/api/settings.rs` showed that after the
  earlier payload and request slices, `test_preset(...)` still directly owned
  one more reusable request concern: translating a stored preset `config`
  object into the probe request shape consumed by `test_api_connection(...)`.
  That remained a real seam because the API layer was still mixing route-level
  transport/error handling with compat key selection and numeric-field
  normalization for a reusable config-to-request adapter. The refactor
  introduced
  `backend-rs/src/services/settings_test_preset_request_service.rs`
  as the focused owner for
  `build_test_preset_connection_request(...)` and the typed
  `TestPresetConnectionRequest` contract.
  `backend-rs/src/api/settings.rs` now keeps settings loading, preset lookup,
  and route-level error mapping in `test_preset(...)` while delegating the
  config-field merge and probe-request assembly to the new request owner before
  forwarding into `test_api_connection(...)`. Focused tests were added on the
  new service so the unchanged provider/model fallback order, numeric
  pass-through behavior, and invalid `max_tokens` rejection remain explicit and
  reviewable. This keeps the current test-preset API surface stable while
  continuing the same route -> request owner split already established in the
  `settings` lane. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_test_preset_request_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings`
  (15 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `settings` now has another lightweight request seam
  extracted from the preset/test flow, so the next phase5 move in this route
  group should still stay at the adapter/query boundary; broad provider probe
  workflow extraction remains intentionally out of scope.

- 2026-05-22 03:34 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the preset lookup query
  seam out of `settings/test_preset(...)`, without changing the route's
  existing `Settings not found` / `Preset not found` error contracts, the
  `api_presets` preferences storage shape, or the default `{}` config fallback
  when a preset entry exists but has no `config` object. A focused audit of
  `backend-rs/src/api/settings.rs` showed that after the earlier request
  adapter extraction, `test_preset(...)` still directly owned one more
  reusable read-side concern: parsing the stored `preferences` JSON, locating
  the requested preset inside `api_presets.presets`, and resolving its config
  payload before the probe request builder ran. That remained a real seam
  because the API layer was still mixing route-level transport/error handling
  with reusable preferences-query logic that already belongs conceptually with
  the preset read path. The refactor introduced
  `backend-rs/src/services/settings_preset_query_service.rs`
  as the focused owner for
  `find_preset_config(...)` plus the typed
  `FindPresetConfigError::PresetNotFound` contract, and promoted
  `get_api_presets(...)` in `settings_service` to a crate-visible helper so
  the new query owner can reuse the existing `api_presets` parsing semantics
  instead of re-implementing them. `backend-rs/src/api/settings.rs` now keeps
  only settings loading, route-level not-found mapping, and downstream probe
  delegation in `test_preset(...)`, while the new query owner handles
  preferences parsing, preset selection, and config extraction. Focused tests
  were added on the new query service so the unchanged preset-match behavior,
  empty-config fallback, and unknown-preset rejection remain explicit and
  reviewable. This keeps the current preset-test API surface stable while
  continuing the same route -> query owner -> request owner split already
  established in the `settings` lane. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_preset_query_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings`
  (18 tests passed), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `settings` now has both the preset lookup query seam
  and the preset-probe request seam pulled behind focused owners, so the next
  phase5 move in this route group should only continue if another similarly
  narrow adapter/query seam is still visible; provider probing and broader
  preset write workflows remain intentionally out of scope.

- 2026-05-22 03:49 +08:00 checkpoint:
  this slice continued phase5 Rust migration by pivoting from the now-thinned
  `settings` lane back to `users` and moving the remaining route-local
  `set_admin` request/payload seam behind a focused owner, without changing the
  existing `缺少 user_id` / `缺少 is_admin` bad-request contracts, the
  self-demotion guard, the last-admin protection semantics, or the success
  payload shape (`message/user_id/is_admin`) and grant-vs-revoke wording. A
  focused audit of `backend-rs/src/api/users.rs` showed that after the earlier
  shared-helper and password-reset workflow extractions, `set_admin(...)`
  still directly owned two compatibility-safe transport concerns: parsing the
  JSON body into the required `user_id/is_admin` request contract and building
  the final success shell once the database update completed. That remained a
  real seam because the API layer was still mixing guard/workflow orchestration
  with reusable request validation and payload formatting logic. The refactor
  introduced
  `backend-rs/src/services/user_admin_set_admin_route_service.rs`
  as the focused owner for
  `build_set_admin_request(...)`,
  `build_set_admin_payload(...)`, and the typed
  `SetAdminRequest` contract.
  `backend-rs/src/api/users.rs` now keeps admin permission checks, self/last-
  admin policy enforcement, user lookup, and the actual persistence update in
  `set_admin(...)`, while delegating the body parsing and success payload shell
  to the new route service owner. Focused tests were added on the new module so
  the unchanged required-field errors and grant/revoke response wording remain
  explicit and reviewable. This keeps the current users/admin API surface
  stable while continuing the same route -> request/payload owner split already
  used across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_set_admin_route_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" users`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `users/admin` now has its obvious `set_admin`
  request/payload seam extracted as well, so the next phase5 move should
  likely either pick another comparably narrow route-local seam in a different
  route group or, if returning to `users/admin`, only continue where a clearly
  bounded adapter/workflow cut still exists.

- 2026-05-22 04:03 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the remaining
  `toggle_user_status` route-local request/payload seam out of `admin`,
  without changing the self-disable guard (`不能禁用自己的账号`), the
  `用户不存在` not-found contract, the existing default behavior where a missing
  `is_active` field is treated as `false`, or the success payload shape
  (`success/message/is_active`) and enable-vs-disable wording. A focused audit
  of `backend-rs/src/api/admin.rs` showed that after the shared route helpers
  and password-reset workflow extraction, `toggle_user_status(...)` still
  directly owned two compatibility-safe transport concerns: normalizing the
  optional `is_active` flag from the JSON body and building the final success
  shell once the trust-level update completed. That remained a real seam
  because the API layer was still mixing policy/workflow orchestration with
  reusable request-defaulting and payload formatting logic. The refactor
  introduced
  `backend-rs/src/services/user_admin_toggle_status_route_service.rs`
  as the focused owner for
  `build_toggle_user_status_request(...)`,
  `build_toggle_user_status_payload(...)`, and the typed
  `ToggleUserStatusRequest` contract.
  `backend-rs/src/api/admin.rs` now keeps admin permission checks, self-
  protection policy, user lookup, and the actual trust-level persistence update
  in `toggle_user_status(...)`, while delegating the `is_active` defaulting and
  success payload shell to the new route service owner. Focused tests were
  added on the new module so the unchanged missing-field default behavior and
  enable/disable response wording remain explicit and reviewable. This keeps
  the current admin user-status API surface stable while continuing the same
  route -> request/payload owner split already used across the earlier phase5
  slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_toggle_status_route_service`
  (3 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" admin`
  (14 tests passed with the filtered admin lane set), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `users/admin` now has both its `set_admin` and
  `toggle_user_status` transport seams extracted, so the next phase5 move in
  this lane should only continue where a similarly bounded request/payload seam
  still exists; otherwise the better choice is to pivot to another route group
  with a higher-signal cut.

- 2026-05-22 04:17 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the `prompt_templates`
  list/system-defaults response shell seam out of `prompt_templates`, without
  changing the `/prompt-templates` `templates/total/categories` contract, the
  `/prompt-templates/system-defaults` `templates/total` contract, managed sync
  behavior, or the underlying
  `PromptTemplateService::list_user_templates(...)` /
  `all_system_templates()` query ownership. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` showed that after the earlier
  export payload, category payload, and sync-status query extractions, the
  route still directly owned two more read-side response shells: wrapping the
  user template list plus categories into the compat list payload and wrapping
  system defaults into the `templates/total` payload. That remained a real
  seam because the API layer was still mixing transport/error handling with
  reusable response-shell assembly logic that belongs with the prompt template
  read adapters. The refactor introduced
  `backend-rs/src/services/prompt_template_list_payload_adapter_service.rs`
  as the focused owner for
  `build_prompt_template_list_payload(...)` and
  `build_prompt_template_system_defaults_payload(...)`.
  `backend-rs/src/api/prompt_templates.rs` now keeps managed sync, query
  delegation, and transport error mapping in `get_all_templates(...)` and
  `get_system_defaults(...)`, while the new adapter owner handles the compat
  response shells. Focused tests were added so the unchanged `templates`,
  `total`, and `categories` payload structure remains explicit and reviewable.
  This keeps the current prompt-template read-side API surface stable while
  continuing the same route -> payload/query owner split used by earlier
  phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_list_payload_adapter_service`
  (2 tests passed),
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`
  (0 tests matched; command completed successfully), and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has export payload, category
  payload, sync-status query, and list/system-defaults payload seams extracted
  on the read side, so the next phase5 move in this lane should continue to
  prefer comparably narrow payload/query seams rather than entering the
  `import` main workflow.

- 2026-05-22 04:29 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the
  `prompt_templates` reset/sync success payload seam out of the route layer,
  without changing the `/prompt-templates/{template_key}/sync-to-default`
  success contract (`template_key/action/message/status`), the
  `/prompt-templates/{template_key}/reset` success contract
  (`message/template_key`), the `reset_to_system_default` vs
  `already_system_default` action wording, or the existing system-template
  existence checks and delete-before-fallback semantics. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` showed that after the read-side
  query/payload extractions, `sync_template_to_default(...)` and
  `reset_to_default(...)` still directly owned one more compatibility-safe
  transport concern: assembling the success JSON shells after route-level
  guards and persistence delegation completed. That remained a real seam
  because the API layer was still mixing template existence checks, delete
  workflow orchestration, and reusable success payload formatting logic. The
  refactor introduced
  `backend-rs/src/services/prompt_template_reset_payload_adapter_service.rs`
  as the focused owner for
  `build_prompt_template_sync_to_default_payload(...)` and
  `build_prompt_template_reset_payload(...)`.
  `backend-rs/src/api/prompt_templates.rs` now keeps system-template lookup,
  user-template deletion, sync-status assembly, and transport error mapping in
  the two route handlers, while the new adapter owner handles the compat
  success shells. Focused tests were added on the new adapter so the unchanged
  deleted-vs-already-default message variants and sync action wording remain
  explicit and reviewable. This keeps the current prompt-template reset/sync
  API surface stable while continuing the same route -> payload owner split
  already established in the phase5 lane. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_reset_payload_adapter_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now also has the reset/sync
  success shell extracted, so if this lane continues in phase5 it should still
  prefer similarly narrow payload/query cuts such as preview result shells,
  rather than entering the heavier `import` workflow.

- 2026-05-22 04:42 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the
  `prompt_templates` preview result shell out of the route layer, without
  changing the `/prompt-templates/{template_key}/preview` success contract
  (`success/rendered_content/parameters_used`), the error contract
  (`success/error/rendered_content`), the existing `PromptTemplateService::format_prompt(...)`
  rendering ownership, or the current behavior where preview formatting errors
  still return `200 OK` with `success: false` instead of a transport-level
  failure. A focused audit of `backend-rs/src/api/prompt_templates.rs` showed
  that after the list/system-defaults, reset/sync, export, category, and
  sync-status extractions, `preview_template(...)` still directly owned one
  more compatibility-safe transport concern: assembling the rendered-vs-error
  JSON result shell once the prompt-formatting service returned. That remained
  a real seam because the API layer was still mixing route boundary handling
  with reusable payload-shape logic for preview results. The refactor
  introduced
  `backend-rs/src/services/prompt_template_preview_payload_adapter_service.rs`
  as the focused owner for
  `build_prompt_template_preview_success_payload(...)` and
  `build_prompt_template_preview_error_payload(...)`.
  `backend-rs/src/api/prompt_templates.rs` now keeps request parsing and
  formatting delegation in `preview_template(...)`, while the new adapter owner
  handles the compat result shells for both render success and render failure.
  Focused tests were added on the new adapter so the unchanged rendered
  content field, parameter key projection, and compat error wording remain
  explicit and reviewable. This keeps the current prompt-template preview API
  surface stable while continuing the same route -> payload owner split
  already established in the phase5 lane. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_preview_payload_adapter_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has most of its narrow
  payload/query seams pulled behind focused owners, leaving the remaining
  `delete` success shell as a possible final light cut before the lane reaches
  diminishing returns and phase5 should pivot elsewhere.

- 2026-05-22 04:56 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the
  `prompt_templates` delete success shell out of the route layer, without
  changing the `/prompt-templates/{template_key}` delete success contract
  (`message/template_key`), the existing not-found error contract
  (`模板 {template_key} 不存在`), or the current behavior where the route first
  confirms the user template exists before delegating deletion. A focused audit
  of `backend-rs/src/api/prompt_templates.rs` showed that after the list,
  export, category, sync-status, reset/sync, and preview extractions,
  `delete_template(...)` still directly owned one last compatibility-safe
  transport concern in this lane: assembling the static delete success JSON
  shell after route-level existence checks and persistence delegation
  completed. That remained a real seam because the API layer was still mixing
  the guard/delete workflow with reusable payload-shape logic for the delete
  success response. The refactor extended
  `backend-rs/src/services/prompt_template_reset_payload_adapter_service.rs`
  so the same focused owner now carries
  `build_prompt_template_delete_payload(...)` alongside the existing reset/sync
  success builders.
  `backend-rs/src/api/prompt_templates.rs` now keeps template existence
  lookup, delete delegation, and transport error mapping in
  `delete_template(...)`, while the adapter owner handles the compat success
  shell. Focused tests were added on the adapter so the unchanged
  `message/template_key` delete response shape remains explicit and
  reviewable. This keeps the current prompt-template delete API surface stable
  while completing the remaining light payload cut in this route group.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_reset_payload_adapter_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_templates`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has its obvious narrow
  payload/query seams cleared, so further phase5 work in this lane would likely
  hit diminishing returns unless a new query/adapter boundary appears; the
  better next move is to pivot back to another route group with a higher-signal
  seam.

- 2026-05-22 05:14 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the `admin/create_user`
  route-local request/payload seam behind a focused owner, without changing
  the existing duplicate-username conflict contract (`用户名已存在`), the
  `admin_created_*` user id generation strategy, the default password fallback
  rule (`{username}@666` when no explicit password is provided), or the
  success response contract (`success/message/user/default_password`) and its
  current null-vs-default-password behavior. A focused audit of
  `backend-rs/src/api/admin.rs` showed that after the earlier admin password
  reset workflow extraction plus the `set_admin` / `toggle_user_status`
  route-service cuts, `create_user(...)` still directly owned two more
  compatibility-safe transport concerns: parsing the JSON body into the
  effective create-user request shape and assembling the final success shell
  after user/password persistence completed. That remained a real seam because
  the API layer was still mixing duplicate-check / persistence orchestration
  with reusable request-defaulting and payload formatting logic. The refactor
  introduced
  `backend-rs/src/services/user_admin_create_user_route_service.rs`
  as the focused owner for
  `build_create_user_request(...)`,
  `build_create_user_payload(...)`, and the typed
  `CreateUserRequest` contract.
  `backend-rs/src/api/admin.rs` now keeps admin permission checks, duplicate
  username detection, user/password persistence, and transport error mapping in
  `create_user(...)`, while the new route service owner handles body parsing,
  defaults, and the compat success shell. Focused tests were added on the new
  module so the unchanged required `username` contract, request defaults, and
  success payload shape remain explicit and reviewable. This keeps the current
  admin create-user API surface stable while continuing the same route ->
  request/payload owner split already used across the earlier phase5 slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_create_user_route_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" admin`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `admin/users` now has create-user, toggle-status,
  and password-reset route-local seams reduced behind focused owners, so the
  next phase5 move in this lane should only continue where another comparably
  narrow request/payload seam is still visible rather than pushing into a
  broader account lifecycle workflow.

- 2026-05-22 05:28 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the `admin/update_user`
  route-local request/payload seam behind a focused owner, without changing
  the existing last-admin protection contract (`不能取消最后一个管理员的权限`),
  the partial-update semantics for optional fields, the special `avatar_url`
  presence behavior where explicit `null` clears the field, or the success
  response contract (`success/message/user`). A focused audit of
  `backend-rs/src/api/admin.rs` showed that after the earlier create-user,
  toggle-status, and password-reset extractions, `update_user(...)` still
  directly owned three more compatibility-safe transport concerns: parsing the
  optional update fields from the route body, deciding whether the last-admin
  guard should activate based on that parsed request, and assembling the final
  success shell once persistence completed. That remained a real seam because
  the API layer was still mixing guard/persistence orchestration with reusable
  request normalization and payload formatting logic. The refactor introduced
  `backend-rs/src/services/user_admin_update_user_route_service.rs`
  as the focused owner for
  `build_update_user_request(...)`,
  `should_block_last_admin_removal(...)`, and
  `build_update_user_payload(...)`.
  `backend-rs/src/api/admin.rs` now keeps admin permission checks, target user
  lookup, the actual last-admin count check, persistence updates, and transport
  error mapping in `update_user(...)`, while the new route service owner
  handles optional field parsing, revoke-intent detection, and the compat
  success shell. Focused tests were added on the new module so the unchanged
  optional-field parsing, explicit-null avatar clearing semantics, last-admin
  revoke signal, and success payload shape remain explicit and reviewable.
  This keeps the current admin update-user API surface stable while continuing
  the same route -> request/payload owner split already used across the
  earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_update_user_route_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" admin`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `admin/users` now has create-user, update-user,
  toggle-status, and password-reset route-local seams reduced behind focused
  owners, leaving delete-user as the main obvious remaining light transport
  seam before this lane likely reaches diminishing returns.

- 2026-05-22 05:41 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the `admin/delete_user`
  success shell out of the route layer, without changing the self-delete guard
  (`不能删除自己的账号`), the last-admin protection contract
  (`不能删除最后一个管理员账号`), the current two-step password-row then user-row
  deletion workflow, or the success response contract (`success/message`). A
  focused audit of `backend-rs/src/api/admin.rs` showed that after the earlier
  create-user, update-user, toggle-status, and password-reset extractions,
  `delete_user(...)` still directly owned one final compatibility-safe
  transport concern in this lane: assembling the static delete success JSON
  shell after route-level guards and persistence delegation completed. That
  remained a real seam because the API layer was still mixing protection /
  deletion orchestration with reusable payload-shape logic for the delete
  success response. The refactor introduced
  `backend-rs/src/services/user_admin_delete_user_route_service.rs`
  as the focused owner for
  `build_delete_user_payload(...)`.
  `backend-rs/src/api/admin.rs` now keeps admin permission checks, self/last-
  admin protection, target lookup, delete execution, and transport error
  mapping in `delete_user(...)`, while the new route service owner handles the
  compat success shell. Focused tests were added on the new module so the
  unchanged `success/message` delete response shape remains explicit and
  reviewable. This keeps the current admin delete-user API surface stable
  while completing the remaining obvious light transport seam in the
  `admin/users` route group. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_delete_user_route_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" admin`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `admin/users` now has its obvious narrow route-local
  payload/request seams cleared, so further phase5 work in this lane would
  likely hit diminishing returns unless a new focused query/adapter boundary is
  identified; the better next move is to pivot to another route group with a
  higher-signal seam.

- 2026-05-22 05:55 +08:00 checkpoint:
  this slice continued phase5 Rust migration by pivoting from the now-thinned
  `admin/users` lane back to `users` and moving two remaining light transport
  seams behind focused owners, without changing the existing self-delete guard
  (`不能删除自己的账号`), the protected-admin delete contract
  (`无法删除该用户（用户不存在或为管理员）`), the user reset-password self-target
  guard (`不能重置自己的密码，请使用修改密码功能`), the required `user_id`
  contract (`缺少 user_id`), or the existing reset workflow behavior where
  empty or missing `new_password` falls back to the default password. A focused
  audit of `backend-rs/src/api/users.rs` showed that after the earlier
  `set_admin` and shared password-reset workflow extractions, two more
  compatibility-safe transport concerns still remained route-local: the
  `/users/{user_id}` delete success shell and the `/users/reset-password`
  request parsing for `user_id/new_password`. That remained a real seam because
  the API layer was still mixing guard/workflow orchestration with reusable
  request/payload shaping logic. The refactor reused
  `backend-rs/src/services/user_admin_delete_user_route_service.rs`
  for the shared delete success shell shape and introduced
  `backend-rs/src/services/user_password_reset_request_service.rs`
  as the focused owner for
  `build_user_reset_password_request(...)` and the typed
  `UserResetPasswordRequest` contract.
  `backend-rs/src/api/users.rs` now keeps admin permission checks, target
  lookup/protection guards, delete execution, reset workflow execution, and
  transport error mapping in the two route handlers, while the focused owners
  handle the compat delete payload and reset request parsing. Focused tests
  were added on the new request owner so the unchanged required-field contract
  remains explicit and reviewable. This keeps the current users/admin API
  surface stable while continuing the same route -> request/payload owner split
  already used across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_password_reset_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" users`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `users` now has its remaining obvious delete/reset
  transport seams reduced as well, so this route group should only continue in
  phase5 if another similarly narrow payload/query boundary is identified;
  otherwise the next move should pivot again to a higher-signal route group
  such as `prompt_workshop` or another compat-heavy read/write surface.

- 2026-05-22 06:09 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving two request/payload
  preparation seams out of `prompt_workshop`, without changing the cloud-proxy
  download payload contract (`instance_id/user_identifier`), the submit proxy
  payload contract (`instance_id/submitter_id/submitter_name/name/description/prompt_content/category/author_display_name/is_anonymous/tags`),
  the existing submitter-name fallback to the current user id, the normalized
  tags encoding semantics, or the split behavior where client mode proxies to
  the cloud service while server mode delegates to
  `PromptWorkshopService::submit_prompt(...)` locally. A focused audit of
  `backend-rs/src/api/prompt_workshop.rs` showed that after the earlier import
  helper extraction, the route still directly owned two more reusable
  transport-side concerns: building the duplicated download proxy payload in
  both `import_item(...)` and `record_download(...)`, and assembling the full
  submit proxy payload plus normalized request context in `submit_prompt(...)`.
  That remained a real seam because the API layer was still mixing
  proxy/workflow orchestration with reusable payload assembly and request
  normalization logic. The refactor extended
  `backend-rs/src/services/prompt_workshop_request_service.rs`
  as the focused owner for
  `build_workshop_download_payload(...)` and
  `prepare_submit_prompt_request(...)` plus the typed
  `PreparedSubmitPromptRequest` contract.
  `backend-rs/src/api/prompt_workshop.rs` now keeps transport branching,
  proxy dispatch, and service delegation in `import_item(...)`,
  `record_download(...)`, and `submit_prompt(...)`, while the request service
  owner handles the shared download payload and the submit proxy request
  preparation. Focused tests were added so the unchanged download payload
  shape, submitter-name fallback, normalized tags encoding, and submit proxy
  payload structure remain explicit and reviewable. This keeps the current
  prompt-workshop API surface stable while continuing the same route ->
  request/payload owner split already used across the earlier phase5 slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_workshop` now has one more batch of
  low-risk request/payload seams reduced, so if this lane continues in phase5
  the next moves should keep preferring comparably narrow proxy/request cuts
  around admin review/item updates rather than entering heavier cloud workflow
  rewrites.

- 2026-05-22 03:20 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the next two
  admin-side request-normalization seams out of `prompt_workshop`, without
  changing the admin review request contract
  (`action/review_note/category/tags`), the admin create-item request contract
  (`name/description/prompt_content/category/tags`), the existing default
  category behavior (`general`), the normalized tags encoding semantics, or
  the current ownership split where `prompt_workshop.rs` handles auth/transport
  branching while `PromptWorkshopService` still owns the actual review/create
  workflow. A focused audit of `backend-rs/src/api/prompt_workshop.rs` showed
  that after the earlier download/submit helper extraction, the route layer
  still directly owned two more reusable transport-side concerns:
  `admin_review_submission(...)` still parsed and normalized admin review tags
  inline, and `admin_create_item(...)` still combined typed route DTO parsing,
  default category injection, and tag normalization locally before delegating
  to the service layer. That remained a real seam because the API layer was
  still mixing admin transport orchestration with reusable request-shaping and
  normalization logic. The refactor extended
  `backend-rs/src/services/prompt_workshop_request_service.rs`
  as the focused owner for
  `PromptWorkshopAdminReviewRouteRequest`,
  `PreparedPromptWorkshopAdminReviewRequest`,
  `prepare_admin_review_submission_request(...)`,
  `PromptWorkshopAdminCreateItemRouteRequest`,
  `PreparedPromptWorkshopAdminCreateItemRequest`,
  `prepare_admin_create_item_request(...)`, and the shared
  `default_workshop_category()` owner used by both submit and admin create
  request DTOs.
  `backend-rs/src/api/prompt_workshop.rs` now keeps admin permission checks,
  route transport mapping, and service delegation in the two handlers, while
  the request service owner handles the typed DTO boundary, default category
  injection, and normalized tags preparation. Focused tests were added so the
  unchanged admin review tag normalization and admin create-item default
  category plus normalized-tags behavior remain explicit and reviewable. This
  keeps the current prompt-workshop admin API surface stable while continuing
  the same route -> request/payload owner split already used across the
  earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_workshop` now has its obvious admin
  review/create request seams reduced as well, so the remaining visible
  `admin_update_item(Json<Value>)` path should only be touched if we can prove
  a concrete owner split that preserves the current raw-update compatibility
  semantics; otherwise the next phase5 move should pivot to another higher-
  signal Rust seam instead of forcing thinner prompt-workshop cleanup.

- 2026-05-22 03:26 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the next request-owner
  seam out of `writing_styles`, without changing the current
  `create_style/update_style` compatibility fields (`preset_id/name/description/prompt_content/style_type/order_index`),
  the preset fallback behavior where missing name/description/prompt content
  can still be filled from a selected global preset, the required-field
  contract (`name 和 prompt_content 是必填字段`), the existing
  `style_type` defaulting rule (`preset` when `preset_id` is present,
  otherwise `custom`), or the current update semantics where content-field
  edits force the saved style back to `custom`. A focused audit of
  `backend-rs/src/api/writing_styles.rs` and
  `backend-rs/src/services/writing_style_service.rs` showed that after the
  earlier set-default request helper extraction, the route/service boundary
  still left one reusable transport-facing parsing seam unresolved:
  `create_style(...)` and `update_style(...)` accepted raw `Json<Value>` and
  the service layer directly read route payload fields inline before applying
  the existing preset/default semantics. That remained a real seam because the
  service owner was still mixing business workflow with route-body decoding and
  field projection logic. The refactor extended
  `backend-rs/src/services/writing_style_request_service.rs`
  as the focused owner for
  `CreateWritingStyleRequest`,
  `UpdateWritingStyleRequest`,
  `build_create_writing_style_request_from_route_payload(...)`, and
  `build_update_writing_style_request_from_route_payload(...)`.
  `backend-rs/src/api/writing_styles.rs` now keeps transport entrypoints and
  service delegation, while the request service owns raw body field decoding
  and the main `WritingStyleService` now consumes explicit typed requests
  instead of reading `serde_json::Value` directly. Focused tests were added so
  the unchanged optional-field capture, blank `style_type` handling, and
  partial update request projection remain explicit and reviewable. This keeps
  the current writing-styles API surface stable while continuing the same
  route/value -> request-owner -> service pattern already used across the
  earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" writing_style_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" writing_styles`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `writing_styles` now has its obvious create/update
  raw-body parsing seam reduced; the next worthwhile move in this group should
  only continue if a similarly concrete payload/adapter split remains visible,
  otherwise phase5 should pivot again to another higher-signal Rust seam such
  as `settings`, `outlines`, or another documented compat-heavy lane.

- 2026-05-22 03:31 +08:00 checkpoint:
  this slice continued phase5 Rust migration by moving the next request-owner
  seam out of `settings/presets`, without changing the current preset create
  and update compatibility fields (`name/description/config`), the existing
  generated preset id format (`preset_<timestamp_millis>`), the default create
  behavior where missing `config` still falls back to `{}`, the update behavior
  where `description` is only rewritten when the route payload explicitly
  carries that field, or the surrounding `settings.preferences -> api_presets`
  persistence workflow. A focused audit of `backend-rs/src/api/settings.rs`
  and `backend-rs/src/services/settings_service.rs` showed that although
  `create_preset_from_current(...)` and `test_preset(...)` already delegated
  request shaping to focused helpers, the plain `create_preset(...)` and
  `update_preset(...)` handlers still accepted raw `Json<Value>` and the main
  settings service directly decoded route payload fields inline before
  mutating the stored preset list. That remained a real seam because the
  service owner was still mixing preset persistence logic with transport-body
  decoding and partial-field projection semantics. The refactor extended
  `backend-rs/src/services/settings_preset_request_service.rs`
  as the focused owner for
  `CreateSettingsPresetRequest`,
  `UpdateSettingsPresetRequest`,
  `build_create_settings_preset_request_from_route_payload(...)`, and
  `build_update_settings_preset_request_from_route_payload(...)`.
  `backend-rs/src/api/settings.rs` now keeps transport entrypoints and service
  delegation for `create_preset(...)` / `update_preset(...)`, while the
  request owner handles route-payload decoding and explicit field-presence
  tracking for `description/config`. `backend-rs/src/services/settings_service.rs`
  now consumes typed preset requests instead of reading raw `serde_json::Value`
  for these two flows. Focused tests were added so the unchanged create shape,
  empty-config fallback, and update-time optional-field semantics remain
  explicit and reviewable. This keeps the current settings preset API surface
  stable while continuing the same route/value -> request-owner -> service
  pattern already used across the earlier phase5 slices. Validation passed
  with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_preset_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `settings/presets` now has its obvious create/update
  raw-body parsing seam reduced; phase5 should only stay in this lane if a
  similarly concrete request/payload split remains isolated from the broader
  runtime settings contract, otherwise the next move should pivot to another
  high-signal Rust seam such as `outlines` or `prompt_templates`.

- 2026-05-22 03:35 +08:00 checkpoint:
  this slice continued phase5 Rust migration by tightening the remaining
  compat request-owner boundary inside `outlines` expansion flows, without
  changing the current batch outline expansion compatibility fields
  (`project_id/chapters_per_outline/expansion_strategy/auto_create_chapters/enable_scene_analysis/provider/model/outline_ids`),
  the existing defaults for missing numeric/bool/string fields, the current
  outline-id filtering semantics for `outline_ids`, or the background-task
  runtime behavior where outline batch expansion still derives its owning
  project from the task record when the queued payload omitted `project_id`.
  A focused audit of `backend-rs/src/api/outlines.rs`,
  `backend-rs/src/services/outline_expansion_request_service.rs`, and the
  outline batch background-task entrypoint showed that the request service
  already owned almost all batch-expand compat decoding, but one transport-side
  field still leaked outside that boundary: `batch_expand_outlines_compat(...)`
  locally extracted `project_id` from raw JSON before calling the request
  builder, and the background-task caller still used a parallel signature that
  injected `record.project_id` out-of-band. That remained a real seam because
  the project-id ownership for the same compat request shape was split across
  multiple transport callers instead of one request owner. The refactor
  narrowed `build_outline_batch_expand_execution_request(...)` so it now owns
  `project_id` extraction from the payload together with the rest of the batch
  expansion compat fields. `backend-rs/src/api/outlines.rs` now keeps only
  transport delegation for `batch_expand_outlines_compat(...)`, and
  `backend-rs/src/api/background_tasks.rs` now preserves the current task-
  derived fallback by inserting `record.project_id` into the payload only when
  the queued payload omitted it before delegating to the same request owner.
  Focused tests were extended so the unchanged full contract and missing-
  `project_id` fallback-to-empty parsing behavior remain explicit and
  reviewable. This keeps the current outlines batch expansion compat surface
  stable while continuing the same route/value -> request-owner -> workflow
  split already used across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" outline_expansion_request_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" outlines`, and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `outlines` compat expansion request seam is now
  thinner and more uniform across HTTP plus background-task callers; the next
  worthwhile move in this group should only continue if another similarly
  concrete request/payload split remains isolated from the broader wizard /
  outline CRUD flow, otherwise phase5 should pivot to another high-signal Rust
  seam such as `prompt_templates`.

- 2026-05-22 03:42 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the next transport
  request seam inside `prompt_templates`, without changing the current create
  or update compatibility fields (`template_key/template_name/template_content/
  description/category/parameters/is_active`), the current create behavior
  where the route body is forwarded as-is to the upsert workflow, the current
  update semantics where route-supplied fields override the stored template
  while omitted fields keep the existing values, or the existing persistence
  owner boundary where `PromptTemplateService::upsert_template(...)` still
  applies the final insert/update behavior. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` and
  `backend-rs/src/services/prompt_template_service.rs` showed that after the
  earlier export/reset/preview payload-owner cleanup, the route layer still
  owned one concrete request seam: `create_or_update_template(...)` forwarded
  raw `Json<Value>` directly, and `update_template(...)` locally merged the
  stored model plus route body before calling the service. That remained a real
  seam because transport-body decoding and partial-update merge ownership for
  the same upsert contract were split between the route and the service-facing
  boundary instead of one focused request owner. The refactor introduced
  `backend-rs/src/services/prompt_template_request_service.rs`
  as the owner for
  `build_prompt_template_upsert_payload_from_route_body(...)` and
  `build_prompt_template_update_payload_from_route_body(...)`.
  `backend-rs/src/api/prompt_templates.rs` now keeps transport entrypoints and
  service delegation for `create_or_update_template(...)` and
  `update_template(...)`, while the request service owns route-body passthrough
  and existing-template merge assembly. Focused tests were added so the
  unchanged create payload shape, update-time existing-field preservation,
  route-field precedence, and non-object-body fallback behavior remain explicit
  and reviewable. This keeps the current prompt-template create/update API
  surface stable while continuing the same route/value -> request-owner ->
  service pattern already used across the earlier phase5 slices. Validation
  passed with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` create/update request ownership
  is now thinner and more consistent with the other phase5 request-owner
  migrations; the next worthwhile move in this lane should target the heavier
  `import_templates(...)` payload/decision seam only if it can be isolated
  without mixing import behavior changes into the current transport-compat
  boundary.

- 2026-05-22 03:46 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the next route-local
  transport seam inside `prompt_templates.import_templates(...)`, without
  changing the current import response shape (`message/statistics/
  converted_templates`), the existing import decision branches
  (`kept_system_default/created_or_updated/converted_to_custom`), the current
  meaning of `is_customized`, or the persistence owner boundary where
  `PromptTemplateService::upsert_template(...)` and
  `delete_user_template(...)` still apply the final write behavior. A focused
  audit of `backend-rs/src/api/prompt_templates.rs` showed that after the
  earlier create/update request-owner extraction, the import route still owned
  another concrete transport seam: it parsed the outer `templates` array,
  repeatedly decoded per-item fields (`template_key/template_name/
  is_customized/template_content`), and rebuilt near-identical upsert payloads
  inline across three import branches before delegating to the service. That
  remained a real seam because raw route-body decoding and import-item payload
  projection for the same transport contract were still duplicated inside the
  route instead of one focused request owner. The refactor extended
  `backend-rs/src/services/prompt_template_request_service.rs`
  with
  `BuildPromptTemplateImportRequestError`,
  `PromptTemplateImportRequest`,
  `PromptTemplateImportItemRequest`, and
  `build_prompt_template_import_request_from_route_body(...)`.
  `backend-rs/src/api/prompt_templates.rs` now keeps the existing import
  business decisions but delegates `templates` array validation, per-item field
  projection, trimmed imported-content extraction, and reusable upsert payload
  assembly to the request owner. Focused tests were added so the unchanged
  missing-templates rejection, import-item field projection, payload reuse, and
  non-object item fallback behavior remain explicit and reviewable. This keeps
  the current prompt-template import API surface stable while continuing the
  same route/value -> request-owner -> service migration pattern already used
  across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `prompt_templates` now has its obvious import-side
  transport payload seam reduced as well; the next worthwhile move in this lane
  should only continue if the remaining import decision matrix can be extracted
  as an isolated service-owned workflow without mixing transport cleanup with
  behavior changes, otherwise phase5 should pivot again to another higher-
  signal Rust seam.

- 2026-05-22 03:51 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the next transport
  request seam inside `book_import`, without changing the current apply/apply-
  stream/retry-stream route contracts, the existing default `import_mode`
  behavior (`append` when omitted), the current retry-step filtering semantics
  (ignore non-string step items), or the downstream import business flow owned
  by `BookImportService`. A focused audit of
  `backend-rs/src/api/book_import.rs` and
  `backend-rs/src/services/book_import_service.rs` showed that the route layer
  still owned one clear raw-JSON seam across three related endpoints:
  `apply_import(...)` and `apply_stream(...)` separately decoded
  `project_suggestion/chapters/outlines/import_mode`, while `retry_stream(...)`
  also parsed and filtered `steps` inline before delegating to the service.
  That remained a real seam because the same transport-body projection logic
  was split across multiple route entrypoints instead of one focused request
  owner. The refactor introduced
  `backend-rs/src/services/book_import_request_service.rs`
  as the owner for
  `BookImportApplyRequest`,
  `BookImportRetryRequest`,
  `build_book_import_apply_request_from_route_body(...)`, and
  `build_book_import_retry_request_from_route_body(...)`.
  `backend-rs/src/api/book_import.rs` now keeps transport entrypoints,
  status/error mapping, and spawn orchestration, while the new request service
  owns route-body field projection for `project_suggestion`, `chapters`,
  `outlines`, `import_mode`, and retry `steps`. Focused tests were added so the
  unchanged default values, body-field capture, and retry-step filtering remain
  explicit and reviewable. This keeps the current book-import API surface
  stable while continuing the same route/value -> request-owner -> service
  migration pattern already used across the earlier phase5 slices. Validation
  passed with `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" book_import_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `book_import` JSON apply/retry request seam is
  now thinner and shared across sync plus stream entrypoints; the next
  worthwhile move in this lane should only continue if multipart create-task
  parsing or downstream import workflow ownership can be isolated without
  widening the current compatibility-sensitive import pipeline.

- 2026-05-22 03:57 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the next transport
  request seam inside `foreshadows`, without changing the current create/
  update/plant/resolve route contracts, the existing default field semantics
  for create (`importance=0.5`, `strength=5`, `subtlety=5`,
  `auto_remind=true`, `remind_before_chapters=5`,
  `include_in_context=true`), the current update behavior where only present
  fields are applied, the current null-presence semantics for raw JSON fields
  such as `related_characters`, or the existing partial-resolve status
  behavior (`is_partial=true` -> `partially_resolved`, otherwise `resolved`).
  A focused audit of `backend-rs/src/api/foreshadows.rs` and
  `backend-rs/src/services/foreshadow_service.rs` showed that the route layer
  still passed raw `Json<Value>` through five behavior-sensitive entrypoints,
  while the service directly decoded and defaulted request-body fields inline
  across create/update/plant/resolve flows. That remained a real seam because
  transport-body projection and presence/default semantics for the same
  foreshadow contract were still mixed into the main service owner instead of
  one focused request layer. The refactor introduced
  `backend-rs/src/services/foreshadow_request_service.rs`
  as the owner for
  `CreateForeshadowRequest`,
  `UpdateForeshadowRequest`,
  `PlantForeshadowRequest`,
  `ResolveForeshadowRequest`,
  `build_create_foreshadow_request_from_route_body(...)`,
  `build_update_foreshadow_request_from_route_body(...)`,
  `build_plant_foreshadow_request_from_route_body(...)`, and
  `build_resolve_foreshadow_request_from_route_body(...)`.
  `backend-rs/src/api/foreshadows.rs` now keeps transport entrypoints and
  error mapping, while `backend-rs/src/services/foreshadow_service.rs` now
  consumes explicit typed requests instead of decoding raw JSON inline for
  create/update/plant/resolve. Focused tests were added so the unchanged
  create defaults, route-field capture, update presence flags, plant optional
  fields, and resolve partial-default behavior remain explicit and reviewable.
  This keeps the current foreshadows API surface stable while continuing the
  same route/value -> request-owner -> service migration pattern already used
  across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" foreshadow_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `foreshadows` write-side request seam is now
  thinner and more explicit; the next worthwhile move in this lane should only
  continue if `sync_from_analysis(...)` can be given a similarly isolated
  request/workflow owner or if query-side parameter ownership reveals another
  concrete duplicated seam, otherwise phase5 should pivot to another
  high-signal Rust lane.

- 2026-05-22 04:01 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the remaining
  multipart transport seam inside `book_import.create_task(...)`, without
  changing the current upload contract, the existing `.txt`-only restriction,
  the current `import_mode` whitelist (`append/overwrite`), the existing
  "only create-new-project import is supported" restrictions, the current file
  size limit (`50MB`), or the route-visible error message/status mapping for
  missing file, empty content, unsupported file type, invalid import mode,
  unsupported `project_id`, unsupported existing-project import, and oversized
  files. A focused audit of `backend-rs/src/api/book_import.rs` showed that
  after the earlier JSON apply/retry request-owner extraction, the multipart
  create-task route still owned another concrete transport seam: it parsed file
  bytes plus auxiliary multipart fields inline, normalized booleans/strings,
  and applied a stacked set of validation rules directly inside the route
  before delegating to `BookImportService::create_task(...)`. That remained a
  real seam because multipart field interpretation and compatibility validation
  for the same route contract were still mixed into the route instead of one
  focused request owner. The refactor extended
  `backend-rs/src/services/book_import_request_service.rs`
  with
  `BookImportCreateTaskRouteFields`,
  `BookImportCreateTaskRequest`,
  `BuildBookImportCreateTaskRequestError`, and
  `build_book_import_create_task_request_from_route_fields(...)`.
  `backend-rs/src/api/book_import.rs` still owns multipart I/O plus file-byte
  reading, but now delegates field normalization and validation to the request
  owner before calling the existing service. Focused tests were added so the
  unchanged happy path, default values, and each compat error branch remain
  explicit and reviewable together with the earlier JSON apply/retry request
  tests. This keeps the current book-import API surface stable while
  continuing the same route/value -> request-owner -> service migration
  pattern already used across the earlier phase5 slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" book_import_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `book_import` request boundary is now thinner
  across both multipart create and JSON apply/retry entrypoints; the next
  worthwhile move in this lane should only continue if downstream import or
  preview workflow ownership can be isolated without widening the current
  compatibility-sensitive import pipeline, otherwise phase5 should pivot to
  another high-signal Rust seam.

- 2026-05-22 04:07 +08:00 checkpoint:
  this slice continued phase5 Rust migration by reducing the remaining raw
  transport seam inside `chapter_regeneration` full-stream entrypoints,
  without changing the current full-chapter regeneration route contract, the
  existing default values and parsing behavior for `target_word_count`,
  `custom_instructions`, `focus_areas`, `selected_suggestion_indices`,
  `story_creation_brief`, `quality_notes`, `story_repair_summary`,
  `creative_mode`, `story_focus`, `quality_preset`, `story_repair_targets`,
  `story_preserve_strengths`, or the nested `preserve_elements` booleans and
  string-list filters. A focused audit of
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_write_workflow_service.rs`
  showed that `apply_partial_regenerate(...)` and
  `partial_regenerate_stream(...)` already used explicit typed route requests,
  but `regenerate_chapter_stream(...)` still accepted raw `Json<Value>` and
  the write workflow rebuilt the owned full-regeneration request from untyped
  JSON. That remained a real seam because one route in the same lane still
  carried raw transport decoding while the adjacent partial/apply paths had
  already moved to explicit request-owner contracts. The refactor introduced
  `FullChapterRegenerationStreamRouteRequest` and
  `build_full_chapter_regeneration_stream_request_from_route_payload_typed(...)`
  in `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`.
  `backend-rs/src/api/chapter_regeneration_routes.rs` now accepts
  `Json<FullChapterRegenerationStreamRouteRequest>` for
  `regenerate_chapter_stream(...)`, and
  `backend-rs/src/services/chapter_regeneration_write_workflow_service.rs`
  now forwards the typed request through the same request-owner module instead
  of rebuilding from raw `Value`. Focused tests were extended so the unchanged
  full-regeneration defaults, numeric/string list filtering, and nested
  `preserve_elements` behavior remain explicit for both the legacy raw-payload
  builder and the new typed route-request builder. This keeps the current
  chapter-regeneration API surface stable while further aligning the lane to
  the same route/request-owner/workflow pattern already used by the partial
  regeneration paths. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `chapter_regeneration` route request ownership is
  now consistent across full-stream, partial-stream, and partial-apply
  entrypoints; the next worthwhile move in this lane should only continue if a
  similarly isolated workflow or query-side owner split remains visible,
  otherwise phase5 should pivot again to another higher-signal Rust seam.

- 2026-05-22 04:11 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing one remaining raw
  request-field seam from the `admin` password-reset route without changing
  the current admin reset-password contract, default-password fallback
  behavior, or the route-specific empty-string semantics already protected by
  the shared password reset workflow tests. A focused audit of
  `backend-rs/src/api/admin.rs`,
  `backend-rs/src/services/user_password_reset_request_service.rs`, and
  `backend-rs/src/services/user_admin_password_reset_workflow_service.rs`
  showed that `create_user`, `update_user`, and `toggle_user_status` already
  delegated request ownership to focused helpers, but `reset_password(...)`
  still accepted raw `Json<Value>` and read `body.get("new_password")`
  inline before calling `reset_user_password_workflow(...)`. That remained a
  real seam because one route in the same admin lane still owned direct
  transport field extraction while the adjacent admin routes had already moved
  to explicit request-owner boundaries. The refactor extended
  `backend-rs/src/services/user_password_reset_request_service.rs` with
  `AdminResetPasswordRequest` and
  `build_admin_reset_password_request(...)`.
  `backend-rs/src/api/admin.rs` now builds the admin reset-password request
  once and forwards `request.new_password.as_deref()` to the existing
  workflow, rather than decoding the raw field locally. Focused tests were
  added so the unchanged missing-password -> default-password behavior,
  empty-string preservation, and normal custom-password contract remain
  explicit at the request-owner boundary. This keeps the current admin reset
  password API stable while continuing the same route/request-owner/service
  migration pattern used across the other phase5 Rust slices. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_password_reset_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `admin` route group now has consistent request
  owner boundaries across create, update, toggle-status, and reset-password;
  the next worthwhile phase5 move should pivot back to a higher-signal
  unresolved Rust seam such as `mcp_plugins`, `outlines`, or another isolated
  route/request-owner gap instead of forcing more thin-wrapper cleanup here.

- 2026-05-22 04:17 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the remaining raw
  transport seam from the `outlines` compat expand entrypoints without
  changing the current outline expand / batch-expand execution defaults,
  outline-id ownership, or the compatibility request semantics already owned
  by the outline expansion request service. A focused audit of
  `backend-rs/src/api/outlines.rs` and
  `backend-rs/src/services/outline_expansion_request_service.rs`
  showed that the lane had already extracted
  `build_outline_expand_execution_request(...)` and
  `build_outline_batch_expand_execution_request(...)`, but the compat routes
  `expand_outline_compat(...)` and `batch_expand_outlines_compat(...)` still
  accepted raw `Json<Value>` and rebuilt those request-owner payloads from
  untyped route bodies. That remained a real seam because one lane-specific
  request owner already existed, yet the route boundary still held direct raw
  transport decoding instead of forwarding a typed compat request. The
  refactor introduced `OutlineExpandRouteRequest`,
  `OutlineBatchExpandRouteRequest`,
  `build_outline_expand_execution_request_from_route_request(...)`, and
  `build_outline_batch_expand_execution_request_from_route_request(...)` in
  `backend-rs/src/services/outline_expansion_request_service.rs`.
  `backend-rs/src/api/outlines.rs` now accepts typed compat route requests for
  `expand_outline_compat(...)` and `batch_expand_outlines_compat(...)`, then
  forwards them through the same request-owner module instead of rebuilding
  from raw `Value`. Focused tests were extended so the unchanged defaults for
  missing `target_chapter_count`, `expansion_strategy`,
  `auto_create_chapters`, `enable_scene_analysis`, `provider`, `model`,
  `batch_size`, `project_id`, `chapters_per_outline`, and `outline_ids`
  remain explicit for both the legacy raw-payload builders and the new typed
  route-request builders. This keeps the current outlines compat API stable
  while aligning the lane with the same route/request-owner/service pattern
  used by the other phase5 Rust slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" outline_expansion_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `outlines` compat expand routes now match the
  existing request-owner boundary; the next worthwhile phase5 move should
  pivot to another unresolved Rust seam such as `mcp_plugins.update(...)` or
  another route still holding untyped request decoding, rather than forcing
  more low-signal cleanup in this lane.

- 2026-05-22 04:22 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the remaining raw
  partial-update field decoding from the `mcp_plugins.update(...)` lane
  without changing the current plugin update API shape, the existing
  "missing field means no update" contract, the string-field type-mismatch
  ignore behavior, or the current JSON-field persistence semantics where
  `headers`, `config`, `args`, and `env` are stored via `Value::to_string()`
  whenever the route payload explicitly includes those fields. A focused audit
  of `backend-rs/src/api/mcp_plugins.rs`,
  `backend-rs/src/services/mcp_plugin_service.rs`, and the surrounding request
  owner patterns in `backend-rs/src/services/` showed that
  `update_plugin(...)` still forwarded raw `Json<Value>` directly into
  `McpPluginService::update(...)`, and the service itself still owned all
  field-by-field untyped extraction for partial update behavior. That
  remained a real seam because this lane still mixed update request decoding
  with persistence mutation logic inside the service body instead of one
  focused request owner that made the compatibility contract explicit and
  testable. The refactor introduced
  `backend-rs/src/services/mcp_plugin_request_service.rs` with
  `McpPluginUpdateRequest` and
  `build_mcp_plugin_update_request(...)`.
  `backend-rs/src/api/mcp_plugins.rs` now builds the update request once at
  the route boundary, and `backend-rs/src/services/mcp_plugin_service.rs`
  now applies a typed partial-update request instead of parsing raw `Value`
  fields inline. Focused tests were added so the unchanged full update
  contract, string/sort-order type-mismatch ignore behavior, and explicit
  `null` preservation for `headers`, `config`, `args`, and `env` remain
  explicit at the request-owner boundary. This keeps the current MCP plugin
  update API stable while extending the same route/request-owner/service
  migration pattern used by the other phase5 Rust slices into a lane where the
  raw payload owner previously lived inside the service layer itself.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" mcp_plugin_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `mcp_plugins.update(...)` lane no longer keeps
  raw request-field extraction in the service body; the next worthwhile
  phase5 move should pivot to another unresolved Rust seam where either the
  route boundary still accepts untyped request payloads or a service still
  owns direct raw route-field decoding that can be isolated without changing
  observable behavior.

- 2026-05-22 04:26 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the remaining raw
  transport decoding from the `users` admin-command entrypoints without
  changing the current `set-admin` / `reset-password` contracts, the existing
  required-field validation messages, or the current user-password reset
  semantics where missing `new_password` falls back to the default password
  and explicit empty-string behavior remains preserved by the downstream
  workflow mode. A focused audit of `backend-rs/src/api/users.rs`,
  `backend-rs/src/services/user_admin_set_admin_route_service.rs`, and
  `backend-rs/src/services/user_password_reset_request_service.rs`
  showed that the lane already had focused request builders
  `build_set_admin_request(...)` and
  `build_user_reset_password_request(...)`, but the routes
  `set_admin(...)` and `reset_user_password(...)` still accepted raw
  `Json<Value>` and rebuilt those request-owner payloads from untyped bodies
  at the route boundary. That remained a real seam because the request-owner
  contract already existed, yet the route still held raw transport decoding
  instead of forwarding typed route payloads through the same owner module.
  The refactor introduced `SetAdminRouteRequest` plus
  `build_set_admin_request_from_route_payload(...)` in
  `backend-rs/src/services/user_admin_set_admin_route_service.rs`, and
  `UserResetPasswordRouteRequest` plus
  `build_user_reset_password_request_from_route_payload(...)` in
  `backend-rs/src/services/user_password_reset_request_service.rs`.
  `backend-rs/src/api/users.rs` now accepts typed route requests for
  `set_admin(...)` and `reset_user_password(...)`, then forwards them through
  the same request-owner modules instead of rebuilding from raw `Value`.
  Focused tests were extended so the unchanged required-field failures,
  standard happy path, and user reset-password route compatibility stay
  explicit for both the legacy raw-body builders and the new typed
  route-payload builders. This keeps the current users admin-command API
  stable while aligning the lane with the same route/request-owner/service
  pattern used by the other phase5 Rust slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_set_admin_route_service`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_password_reset_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `users` admin-command routes now match their
  existing request-owner boundaries; the next worthwhile phase5 move should
  pivot to a heavier unresolved seam such as `settings` or another route group
  where raw route payload decoding still lives in a broader service update
  path, rather than forcing more low-signal cleanup in this lane.

- 2026-05-22 04:33 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the broad raw route
  payload owner from the `settings` create/update lane without changing the
  current settings API shape, provider fallback behavior, placeholder API-key
  preservation rules, `clear_api_key` handling, `api_backup_urls` filtering
  semantics, provider-switch default-model behavior, or the web-research patch
  merge logic that was previously derived directly from the untyped request
  body inside `SettingsService::update(...)`. A focused audit of
  `backend-rs/src/api/settings.rs` and
  `backend-rs/src/services/settings_service.rs`
  showed that unlike the lighter seams completed earlier, this lane still kept
  nearly all compatibility-sensitive field decoding inside the service update
  body itself, while the routes `create_settings(...)` and `update_settings(...)`
  only forwarded raw `Json<Value>`. That remained a real seam because the main
  service path still mixed request decoding, compatibility fallbacks, and
  persistence mutation logic instead of one focused request owner that made the
  update contract explicit and testable. The refactor introduced
  `backend-rs/src/services/settings_update_request_service.rs` with
  `SettingsUpdateRequest`,
  `SettingsApiBackupUrlsField`, and
  `build_settings_update_request_from_route_body(...)`.
  `backend-rs/src/services/settings_service.rs` now consumes a typed settings
  update request instead of parsing raw `Value` fields inline, and
  `backend-rs/src/api/settings.rs` now builds that request once at the route
  boundary before calling `SettingsService::update(...)`. Focused tests were
  added so the unchanged scalar-field extraction rules, `api_backup_urls`
  compatibility shape, provider-switch detection, and web-research patch
  projection remain explicit at the request-owner boundary. This keeps the
  current settings API stable while moving one of the broader raw payload
  owners in phase5 out of the service mutation path and into a dedicated
  request module. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_update_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `settings` create/update lane no longer keeps
  the bulk of its raw route-field decoding inside `SettingsService::update(...)`;
  the next worthwhile phase5 move should pivot to another remaining route group
  whose service or route boundary still directly owns untyped request decoding,
  rather than reopening this lane unless a further isolated compatibility split
  is clearly visible.

- 2026-05-22 05:08 +08:00 checkpoint:
  this slice continued phase5 Rust migration by finishing the
  `prompt_workshop.admin_update_item(...)` request-owner split without changing
  the current partial-update contract, admin authorization flow, or the
  compatibility-sensitive `tags` persistence behavior that previously depended
  on raw `Json<Value>` plus inline `updates.get(...)` access inside
  `PromptWorkshopService::admin_update_item(...)`. A focused audit of
  `backend-rs/src/api/prompt_workshop.rs`,
  `backend-rs/src/services/prompt_workshop_request_service.rs`, and
  `backend-rs/src/services/prompt_workshop_service.rs`
  showed that most of the prompt workshop lane had already gained focused
  request owners, but `admin_update_item(...)` still accepted an untyped route
  payload and let the service body own field extraction for partial updates.
  That remained a real phase5 seam because the service mutation path still mixed
  transport decoding and persistence logic instead of consuming an explicit
  prepared request boundary. The refactor introduced
  `PromptWorkshopAdminUpdateItemRouteRequest`,
  `PreparedPromptWorkshopAdminUpdateItemRequest`, and
  `prepare_admin_update_item_request(...)` in
  `backend-rs/src/services/prompt_workshop_request_service.rs`.
  `backend-rs/src/api/prompt_workshop.rs` now accepts a typed route payload for
  `admin_update_item(...)` and prepares the request before calling the service,
  while `backend-rs/src/services/prompt_workshop_service.rs` now consumes the
  typed prepared request instead of reading raw fields through `updates.get(...)`.
  Focused tests were extended so the unchanged partial-update semantics remain
  explicit: string fields still update only when present as `Some(String)`,
  `null` does not introduce new clearing semantics, and `tags` still preserve
  the existing compatibility path by flowing through `Value` and persisting via
  `to_string()` when present. This keeps the current prompt workshop admin API
  stable while aligning the last broad update seam in this lane with the
  route/request-owner/service pattern used by the other phase5 Rust slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_workshop_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `prompt_workshop` admin update path no longer
  keeps raw route-field decoding in the service mutation body; the next
  worthwhile phase5 move should pivot to another route group whose request
  boundary still forwards untyped payloads into service-side decoding.

- 2026-05-22 05:34 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the raw route payload
  owner from the `foreshadows` command lane for
  `create(...)`, `update(...)`, `plant(...)`, and `resolve(...)` without
  changing the existing default values, partial-update behavior, or the
  compatibility-sensitive distinction between "field missing" and
  "field explicitly present as null" that the update flow already relied on for
  collection-like fields such as `related_characters`,
  `related_foreshadow_ids`, and `tags`. A focused audit of
  `backend-rs/src/api/foreshadows.rs`,
  `backend-rs/src/services/foreshadow_request_service.rs`, and
  `backend-rs/src/services/foreshadow_service.rs`
  showed that the service mutation layer was already consuming focused request
  structs, but the route boundary still accepted raw `Json<Value>` and rebuilt
  those request-owner payloads from untyped bodies. That remained a real phase5
  seam because the command routes still owned transport decoding even though the
  lane already had a dedicated request module and a stable service boundary.
  The refactor introduced typed route payloads
  `CreateForeshadowRouteRequest`,
  `UpdateForeshadowRouteRequest`,
  `PlantForeshadowRouteRequest`, and
  `ResolveForeshadowRouteRequest` in
  `backend-rs/src/services/foreshadow_request_service.rs`, together with
  `build_*_from_route_payload(...)` helpers that keep the previous defaults and
  null-handling semantics. `UpdateForeshadowRouteRequest` preserves the
  existing presence-sensitive contract by tracking whether
  `related_characters`, `related_foreshadow_ids`, and `tags` appeared at all,
  so the service still distinguishes omitted fields from explicit `null` when
  deciding whether to overwrite stored values. `backend-rs/src/api/foreshadows.rs`
  now accepts typed route payloads for those four command endpoints and
  prepares the service requests at the route boundary instead of forwarding raw
  `Value` bodies deeper into the lane. Focused tests were updated so the
  unchanged defaults, typed scalar extraction, and update presence flags remain
  explicit at the request-owner boundary. This keeps the current foreshadows
  API stable while aligning the main command routes with the same
  route/request-owner/service separation used by the other phase5 Rust slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" foreshadow_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `foreshadows` command routes no longer keep raw
  transport decoding at the route edge for create/update/plant/resolve; the
  next worthwhile phase5 move should target another remaining route group with
  untyped route payloads, or return to the heavier `sync_from_analysis`-style
  seams only after the lighter command-boundary splits are exhausted.

- 2026-05-22 05:52 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the raw `Json<Value>`
  transport owner from the `prompt_templates.import_templates(...)` route
  without changing the current import contract, the "missing templates means
  bad request" error, the per-item import semantics, or the existing
  compatibility behavior where import items still carry raw `template_name`
  and `template_content` through the request-owner helper so the service can
  keep the current merge/convert decisions unchanged. A focused audit of
  `backend-rs/src/api/prompt_templates.rs` and
  `backend-rs/src/services/prompt_template_request_service.rs` showed that the
  lane already had a dedicated request-owner helper for import parsing, but
  the route still accepted raw `Json<Value>` and rebuilt the request at the
  route edge. That remained a real seam because the route boundary still owned
  transport decoding even though the request module already encoded the import
  contract explicitly. The refactor introduced
  `PromptTemplateImportRouteRequest` and
  `build_prompt_template_import_request_from_route_payload(...)` in
  `backend-rs/src/services/prompt_template_request_service.rs`.
  `backend-rs/src/api/prompt_templates.rs` now accepts the typed route import
  payload and forwards it into the request-owner helper instead of rebuilding
  from raw `Value` inside the route. Focused tests were added so the unchanged
  missing-templates failure path and the item projection / `upsert_payload()`
  compatibility behavior remain explicit for both the legacy raw-body helper
  and the new typed route-payload helper. This keeps the current prompt
  templates import API stable while aligning one more route boundary with the
  same route/request-owner/service pattern used by the other phase5 Rust
  slices. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `prompt_templates.import_templates(...)`
  boundary no longer keeps raw request decoding at the route edge; the next
  worthwhile phase5 move should target another remaining route group with raw
  route payloads or a deeper service-side decode seam, rather than reopening
  this already-compatible import path.

- 2026-05-22 06:10 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the raw `Json<Value>`
  transport owner from the `prompt_templates.create_or_update_template(...)`
  and `prompt_templates.update_template(...)` routes without changing the
  existing template upsert / merge semantics, and while keeping the
  compatibility-sensitive request-owner helpers that still allow the service to
  operate on the same raw JSON payload shape once the route boundary has
  accepted it. A focused audit of `backend-rs/src/api/prompt_templates.rs` and
  `backend-rs/src/services/prompt_template_request_service.rs` showed that the
  lane already had route-adjacent helper functions for upsert and update, but
  the route functions still accepted raw `Json<Value>` and rebuilt the request
  boundaries locally. That remained a real seam because the route boundary was
  still carrying transport decoding for the main template write paths even
  though the request service already owned the transform contract. The refactor
  introduced `PromptTemplateUpsertRouteRequest` and
  `PromptTemplateUpdateRouteRequest` as typed route payload wrappers, plus
  `build_prompt_template_upsert_payload_from_route_payload(...)` and
  `build_prompt_template_update_payload_from_route_payload(...)` in
  `backend-rs/src/services/prompt_template_request_service.rs`. `backend-rs/src/api/prompt_templates.rs`
  now accepts typed route payloads for both write entrypoints and forwards them
  to the request-owner helpers instead of rebuilding from raw `Value` inline.
  Focused tests were added so the unchanged payload-shape passthrough and
  update merge behavior remain explicit for both the legacy raw-body helpers
  and the new typed route-payload wrappers. This keeps the current prompt
  templates write API stable while aligning the main write routes with the same
  route/request-owner/service pattern used by the other phase5 Rust slices.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `prompt_templates` write routes now avoid raw
  request decoding at the HTTP boundary; the remaining raw-body helpers in the
  request service are compatibility adapters and should only be revisited if a
  later phase needs to tighten that compatibility surface further.

- 2026-05-22 06:22 +08:00 checkpoint:
  this slice continued phase5 Rust migration by finishing the
  `prompt_templates` write-route seam with the same typed route ownership
  pattern applied to the other completed lanes. After the previous import-only
  split, the `create_or_update_template(...)` and `update_template(...)` routes
  still accepted raw `Json<Value>` and rebuilt their payloads locally, even
  though the request service already owned the compatibility-preserving merge
  and passthrough helpers. That remained a real seam because the route layer
  still owned transport decoding for the main template write paths. The refactor
  introduced `PromptTemplateUpsertRouteRequest` and
  `PromptTemplateUpdateRouteRequest` as typed route wrappers, plus
  `build_prompt_template_upsert_payload_from_route_payload(...)` and
  `build_prompt_template_update_payload_from_route_payload(...)` in
  `backend-rs/src/services/prompt_template_request_service.rs`. The route file
  now accepts typed payloads for both write endpoints and forwards them into
  the request-owner helpers instead of rebuilding from raw `Value` inline. The
  existing raw-body helper functions remain in the request service as
  compatibility adapters, so the observable payload shape and merge semantics
  stay unchanged while the HTTP boundary becomes thinner and easier to reason
  about. Focused tests were added so the unchanged payload passthrough and
  merge behavior stay explicit for both the legacy raw-body helpers and the new
  typed route-payload wrappers. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the prompt template write routes now match the
  route/request-owner/service direction used by the other phase5 slices; the
  remaining raw-body helpers are compatibility shims, not route-boundary
  owners, so the next worthwhile move should pivot to another route group with
  a deeper raw decode seam instead of reopening this lane again.

- 2026-05-22 06:34 +08:00 checkpoint:
  this slice completed the small follow-up hygiene pass on the
  `prompt_templates` write-route seam by removing an obsolete raw-body import
  from `backend-rs/src/api/prompt_templates.rs` after the route wrappers had
  already been converted to typed payloads and the validation run had proven
  the new route/request-owner boundary stable. This did not change behavior;
  it only cleaned up the route file so the new typed payload ownership is the
  only active path in the handler surface. Validation remained green with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" prompt_template_request_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the prompt templates lane is cleanly typed at the
  route edge now; future work should move to a different remaining seam rather
  than trying to squeeze more out of this already-stable write path.

- 2026-05-22 06:46 +08:00 checkpoint:
  this slice continued the phase5 Rust migration by tightening the
  `chapter_batch_generation_resume_task_command_service` seam, extracting the
  resume execution-plan selection into a dedicated helper without changing the
  existing resume preconditions, manual-review block, chapter-id parsing
  behavior, or the batch-vs-single execution plan shapes that downstream
  runtime dispatch relies on. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  showed that the command service still mixed plan-selection branching with the
  higher-level resume orchestration even after the surrounding route and
  workflow boundaries had already been narrowed. That remained a real seam
  because the command service still owned both the validation and the
  single/batch execution plan construction in one inline block. The refactor
  introduced `build_resume_execution_plan(...)` as a dedicated helper so the
  command path now separates validation, plan construction, and task reset /
  response assembly more cleanly. Focused tests were added so the unchanged
  parse behavior for mixed string/object chapter IDs, the shared manual review
  blocker, and the single-vs-batch execution-plan selection remain explicit.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the resume command service is now a little thinner
  and easier to reason about, but it is not yet a route-boundary seam; the
  next worthwhile move should target another remaining batch-generation helper
  with a clearer ownership split rather than reopening this already-stable
  resume flow immediately.

- 2026-05-22 13:03 +08:00 checkpoint:
  this slice continued the low-risk phase5 Rust migration in the
  `chapter_batch_generation` read-side lane by tightening payload ownership
  inside
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  without changing any HTTP status payload shape, active-task payload shape,
  checkpoint metadata, terminal semantics, or quality/repair fields exposed to
  existing consumers. A focused audit of
  `chapter_batch_generation_status_payload_adapter_service.rs`,
  `chapter_batch_generation_task_view_query_service.rs`, and the adjacent
  active/status query services showed that the broader read-side owner split
  completed earlier had already moved route and query envelopes outward, but
  the payload adapter itself still duplicated the same shared task-view field
  assembly in both `task_status_payload(...)` and `active_task_payload(...)`.
  That remained a real seam because the adapter still had two parallel owners
  for the same base view contract even after the higher-level query boundaries
  had been narrowed. The refactor introduced
  `build_shared_task_view_payload(...)` and
  `insert_shared_task_view_fields(...)` so checkpoint metadata, base task view
  fields, and shared quality/repair payload projection now flow through one
  local owner before the status-only fields are appended. Focused assertions
  were extended so both `task_status_payload(...)` and
  `active_task_payload(...)` still expose the unchanged
  `latest_quality_metrics`, `quality_metrics_summary`, and
  `active_story_repair_payload` values while preserving the existing absence
  of status-only fields on the active-task response. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: this was a worthwhile read-side owner cleanup, but
  the `chapter_batch_generation` payload-adapter lane is now close to
  diminishing returns again; the next worthwhile phase5 move should pivot to a
  remaining route or service boundary that still owns raw request decoding or
  a broader compatibility-sensitive contract, instead of forcing more
  fine-grained adapter-only reshuffling here.

- 2026-05-22 13:07 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the raw `Json<Value>`
  transport owner from the `writing_styles` create/update routes without
  changing the current compatibility-preserving field parsing, optional-field
  semantics, or the existing behavior where invalid scalar shapes are quietly
  ignored by the request-owner helper instead of becoming stricter route-level
  validation failures. A focused audit of
  `backend-rs/src/api/writing_styles.rs`,
  `backend-rs/src/services/writing_style_request_service.rs`, and
  `backend-rs/src/services/writing_style_service.rs`
  showed that the route layer still accepted raw `Json<Value>` for
  `create_style(...)` and `update_style(...)` even though this lane already had
  a dedicated request-owner module that normalized `style_type`, preserved
  optional string fields, and tolerated invalid `order_index` input by dropping
  it rather than rejecting the request. That remained a real phase5 seam
  because the HTTP boundary was still forwarding an untyped body directly into
  the route-local builder path instead of consuming a typed route wrapper first.
  The refactor introduced `CreateWritingStyleRouteRequest` and
  `UpdateWritingStyleRouteRequest` in
  `backend-rs/src/services/writing_style_request_service.rs`, together with
  `build_create_writing_style_request_from_typed_route_payload(...)` and
  `build_update_writing_style_request_from_typed_route_payload(...)`. Those
  typed wrappers intentionally keep each field as `Option<Value>` and then
  project back through the existing raw-body compatibility builder so the
  observable parsing contract remains unchanged while the route boundary no
  longer owns raw `Value` transport decoding for the main write paths.
  `backend-rs/src/api/writing_styles.rs` now accepts typed route payloads for
  create/update and forwards them into the request-owner helpers instead of
  passing raw `Json<Value>` through the handlers. Focused tests were extended
  so the unchanged create/update parsing behavior remains explicit for both the
  legacy raw-body builders and the new typed route wrappers, including trimmed
  `style_type` handling and ignored invalid `order_index` values. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" writing_style_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `writing_styles` write routes now follow the
  same route/request-owner/service direction as the other completed phase5
  slices; the next worthwhile move should pivot to another remaining route
  group with raw request transport ownership, such as `book_import`,
  `mcp_plugins`, or the remaining `settings` preset/test lanes, rather than
  reopening this now-stable writing-style path.

- 2026-05-22 13:11 +08:00 checkpoint:
  this slice continued phase5 Rust migration by taking down two more remaining
  raw transport-owner seams that already had request-owner foundations but
  still kept `Json<Value>` at the route boundary. A focused audit of
  `backend-rs/src/api/mcp_plugins.rs`,
  `backend-rs/src/services/mcp_plugin_request_service.rs`,
  `backend-rs/src/api/book_import.rs`, and
  `backend-rs/src/services/book_import_request_service.rs`
  showed that both lanes already encoded their compatibility-sensitive request
  parsing in dedicated request modules, but the route layer still accepted raw
  JSON bodies for the main update/apply/retry paths and forwarded untyped
  payloads into local builder calls. That remained a real phase5 seam because
  the HTTP boundary still owned raw transport decoding even though the request
  owner had already stabilized the field projection contracts.

  For `mcp_plugins`, the refactor introduced
  `McpPluginUpdateRouteRequest` plus
  `build_mcp_plugin_update_request_from_typed_route_payload(...)` in
  `backend-rs/src/services/mcp_plugin_request_service.rs`.
  The typed wrapper intentionally keeps route fields as `Option<Value>` and
  reprojects them through the existing raw-body compatibility builder, so the
  current update semantics remain unchanged: string-typed fields still ignore
  type mismatches, `sort_order` still ignores non-numeric input, and
  `headers/config/args/env` still preserve explicit `null` payloads when
  present. `backend-rs/src/api/mcp_plugins.rs` now accepts the typed route
  wrapper for `update_plugin(...)` instead of raw `Json<Value>`.

  For `book_import`, the refactor introduced
  `BookImportApplyRouteRequest`,
  `BookImportRetryRouteRequest`,
  `build_book_import_apply_request_from_route_payload(...)`, and
  `build_book_import_retry_request_from_route_payload(...)` in
  `backend-rs/src/services/book_import_request_service.rs`.
  These typed wrappers also intentionally keep the compatibility boundary
  loose by storing route fields as `Option<Value>` and then projecting through
  the existing raw-body builders, so `project_suggestion` still defaults to
  `null`, `chapters`/`outlines` still default to empty arrays when missing or
  invalid, `import_mode` still defaults to `append`, and retry `steps` still
  filter non-string entries rather than introducing stricter route validation.
  `backend-rs/src/api/book_import.rs` now accepts typed route wrappers for
  `apply_import(...)`, `apply_stream(...)`, and `retry_stream(...)` instead of
  raw `Json<Value>`.

  Focused tests were extended in both request services so the unchanged
  compatibility behavior remains explicit for the new typed route wrappers as
  well as the legacy raw-body builder paths. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" book_import_request_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" mcp_plugin_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: `book_import` apply/retry and `mcp_plugins` update
  now follow the same typed route wrapper pattern as the other completed
  phase5 slices; the next worthwhile move should pivot to one of the remaining
  `settings` preset/test seams, `admin` update routes, or another route group
  that still owns raw request transport decoding at the handler boundary.

- 2026-05-22 13:16 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing another cluster of
  raw handler-boundary transport owners across the `settings` preset write
  routes and the `admin` user-management write routes, while preserving the
  existing compatibility-sensitive parsing behavior already encoded in their
  request-owner helpers. A focused audit of
  `backend-rs/src/api/settings.rs`,
  `backend-rs/src/services/settings_preset_request_service.rs`,
  `backend-rs/src/api/admin.rs`,
  `backend-rs/src/services/user_admin_create_user_route_service.rs`,
  `backend-rs/src/services/user_admin_update_user_route_service.rs`,
  `backend-rs/src/services/user_admin_toggle_status_route_service.rs`, and
  `backend-rs/src/services/user_password_reset_request_service.rs`
  showed that these lanes had already split most of their route-local parsing
  into dedicated request helpers, but the handlers still accepted raw
  `Json<Value>` and manually invoked those builders at the transport boundary.
  That remained a real phase5 seam because the route surface still owned raw
  JSON decoding even though the compatibility contract had already been made
  explicit one layer deeper.

  For `settings`, the refactor introduced
  `CreateSettingsPresetRouteRequest`,
  `UpdateSettingsPresetRouteRequest`,
  `build_create_settings_preset_request_from_typed_route_payload(...)`, and
  `build_update_settings_preset_request_from_typed_route_payload(...)` in
  `backend-rs/src/services/settings_preset_request_service.rs`.
  The typed wrappers intentionally keep field values as `Option<Value>` and
  then reproject through the existing raw-body builders, so the current create
  and update semantics remain unchanged: `config` still defaults to `{}`,
  `description` still preserves `null`, and optional name/config fields still
  follow the previous partial-update behavior. `backend-rs/src/api/settings.rs`
  now accepts these typed route wrappers for `create_preset(...)` and
  `update_preset(...)` instead of raw `Json<Value>`.

  For `admin`, the refactor introduced
  `CreateUserRouteRequest` plus
  `build_create_user_request_from_route_payload(...)`,
  `UpdateUserRouteRequest` plus
  `build_update_user_request_from_route_payload(...)`,
  `ToggleUserStatusRouteRequest` plus
  `build_toggle_user_status_request_from_route_payload(...)`, and
  `AdminResetPasswordRouteRequest` plus
  `build_admin_reset_password_request_from_route_payload(...)`.
  These wrappers likewise preserve the current compatibility surface by keeping
  route fields as untyped JSON values until the existing request-owner helpers
  project them into the real admin contracts. That keeps the existing required
  field failures, avatar-url presence tracking, boolean/default semantics, and
  empty-string password behavior stable while removing raw `Json<Value>` from
  `create_user(...)`, `update_user(...)`, `toggle_user_status(...)`, and
  `reset_password(...)` in `backend-rs/src/api/admin.rs`.

  Focused tests were extended in all touched request services so the unchanged
  compatibility behavior remains explicit for both the legacy raw-body builder
  paths and the new typed route wrappers. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_preset_request_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_create_user_route_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_update_user_route_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_toggle_status_route_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" user_password_reset_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the `settings` preset create/update and `admin`
  create/update/toggle/reset lanes now match the same typed route wrapper /
  request-owner / service direction as the other completed phase5 slices; the
  next worthwhile move should pivot to the remaining `settings` create/update
  routes or any other route group that still exposes raw request transport
  ownership at the handler boundary.

- 2026-05-22 14:03 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the remaining raw
  transport-owner seam from the main `settings` create/update write routes
  while preserving the compatibility-sensitive parsing behavior already owned
  by `backend-rs/src/services/settings_update_request_service.rs`. A focused
  audit of `backend-rs/src/api/settings.rs` and
  `backend-rs/src/services/settings_update_request_service.rs` showed that the
  route layer still accepted raw `Json<Value>` for `create_settings(...)` and
  `update_settings(...)` even though the full compatibility contract already
  lived in the request-owner builder. That remained a real phase5 seam because
  the HTTP boundary still owned raw request decoding for provider switching,
  backup-url parsing, placeholder API-key preservation, and web-research patch
  projection.

  The refactor introduced `SettingsUpdateRouteRequest` plus
  `build_settings_update_request_from_typed_route_payload(...)` in
  `backend-rs/src/services/settings_update_request_service.rs`. The typed
  wrapper intentionally keeps every route-owned field as `Option<Value>` and
  reprojects only present fields back through the existing raw-body builder, so
  the current semantics remain unchanged:
  `api_backup_urls` still distinguishes missing vs invalid vs provided,
  provider-switch detection still keys off field presence rather than parsed
  value validity, invalid numeric/string scalar fields are still ignored
  instead of becoming route-level 422 failures, explicit `null` still remains
  observable when the previous contract allowed it, and web-research fields are
  still copied into the patch payload only when present on the incoming body.
  `backend-rs/src/api/settings.rs` now accepts this typed route wrapper for
  both `create_settings(...)` and `update_settings(...)` instead of raw
  `Json<Value>`.

  Focused tests in `settings_update_request_service.rs` were extended so the
  unchanged compatibility surface remains explicit for both the legacy raw-body
  builder path and the new typed route wrapper path, including the subtle
  missing-vs-null distinction that would have regressed `api_backup_urls`
  semantics if the typed projection had serialized absent fields as `null`.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" settings_update_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the main `settings` create/update routes now match
  the same typed route wrapper / request-owner / service direction as the
  other completed phase5 write slices; the next worthwhile move should pivot
  to the remaining true raw request-body seam under
  `backend-rs/src/api/foreshadows.rs` (`sync_from_analysis(...)`) or another
  route group where the handler still directly owns `Json<Value>` request
  decoding rather than just returning `Json<Value>` responses.

- 2026-05-22 14:18 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the remaining raw
  request-body transport owner from the `foreshadows` route group. A focused
  audit of `backend-rs/src/api/foreshadows.rs`,
  `backend-rs/src/services/foreshadow_request_service.rs`, and
  `backend-rs/src/services/foreshadow_service.rs` showed that most foreshadow
  write routes had already moved onto dedicated typed request-owner builders,
  but `sync_from_analysis(...)` still accepted raw `Json<Value>` and passed it
  straight into the service boundary. That remained a real phase5 seam even
  though the current service implementation is a compatibility placeholder,
  because the handler still owned raw transport decoding for that endpoint.

  The refactor introduced
  `SyncForeshadowFromAnalysisRouteRequest`,
  `SyncForeshadowFromAnalysisRequest`, and
  `build_sync_foreshadow_from_analysis_request_from_route_payload(...)` in
  `backend-rs/src/services/foreshadow_request_service.rs`, then updated
  `backend-rs/src/api/foreshadows.rs` so `sync_from_analysis(...)` now follows
  the same typed route wrapper -> request-owner -> service direction as the
  sibling foreshadow write routes. The typed wrapper intentionally uses custom
  `Deserialize` into a stored `serde_json::Value` rather than a stricter object
  schema so the previous compatibility surface remains unchanged:
  object payloads still pass through unchanged, non-object JSON bodies such as
  `null` or scalar values still deserialize successfully instead of becoming
  route-level 422 failures, and the service boundary still receives the raw
  body shape explicitly through the request owner contract. Because the current
  service implementation remains a stubbed empty-success placeholder, this
  slice changes ownership only and does not change runtime behavior.

  Focused tests in `foreshadow_request_service.rs` were extended to make the
  preserved arbitrary-body compatibility explicit for the new typed wrapper,
  and `ForeshadowService::sync_from_analysis(...)` now consumes the typed
  request contract instead of a naked raw body reference. Validation passed
  with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" foreshadow_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the previously identified true raw request-body seam
  in `foreshadows` is now closed, so the next worthwhile phase5 move should
  reassess the remaining route groups for handler-boundary raw decoding or move
  from request-owner seam cleanup toward a higher-signal Rust behavior owner
  migration slice.

- 2026-05-22 14:29 +08:00 checkpoint:
  this slice continued phase5 Rust migration by removing the last remaining
  direct raw request-body handler seam found by a focused audit of
  `backend-rs/src/api`. Exact search for route parameters shaped like
  `Json(body): Json<Value>` / `Json(_body): Json<Value>` narrowed the remaining
  true transport-owner seam to `backend-rs/src/api/wizard.rs`, where
  `cleanup_wizard_data(...)` still accepted a raw JSON body even though the
  endpoint did not inspect it at all and only retained the parameter for
  transport compatibility with existing callers.

  The refactor introduced `CleanupWizardDataRouteRequest`,
  `CleanupWizardDataRequest`, and
  `build_cleanup_wizard_data_request_from_route_payload(...)` in
  `backend-rs/src/services/wizard_request_service.rs`, then updated
  `backend-rs/src/api/wizard.rs` so `cleanup_wizard_data(...)` now follows the
  same typed route wrapper -> request-owner -> route orchestration pattern as
  the rest of the wizard route group. The typed wrapper intentionally uses
  custom `Deserialize` into a stored `serde_json::Value` so the previous
  compatibility surface remains unchanged:
  arbitrary object payloads still pass through untouched, `null` or scalar
  JSON bodies still deserialize successfully instead of becoming 422 failures,
  and the route continues to ignore the body semantically while holding the raw
  transport shape explicitly through the request-owner boundary. Because the
  endpoint behavior never depended on request-body contents, this slice is
  ownership-only and preserves the existing SSE cleanup flow and response
  payloads.

  Focused tests in `wizard_request_service.rs` were extended to make the
  preserved arbitrary-body compatibility explicit for the cleanup wrapper.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" wizard_request_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: with the `wizard` cleanup endpoint moved over, the
  focused phase5 pass for handlers that directly own raw `Json<Value>` request
  decoding is effectively exhausted in `backend-rs/src/api`; the next
  worthwhile move should pivot from transport-owner cleanup to higher-signal
  Rust behavior-owner migration seams or other cross-layer refactor goals that
  still reduce compatibility risk in a meaningful way.

- 2026-05-22 14:47 +08:00 checkpoint:
  this slice pivoted phase5 Rust migration away from now-exhausted raw
  transport cleanup and back toward a higher-signal behavior-owner seam on the
  batch generation write path. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  showed that the write workflow still owned repeated runtime-launch assembly
  steps after the actual create/resume task plans had already been computed:
  both create and resume performed their own execution-config preparation,
  launch construction, and runtime dispatch handoff before returning the compat
  response payload. That kept part of the runtime-launch contract split between
  the write workflow layer and the dedicated launch owner even though the
  launch owner already existed as a stable seam.

  The refactor moved this repeated create/resume runtime-launch assembly back
  into `chapter_batch_generation_runtime_launch_service.rs` by adding
  `launch_owned_batch_generation_create_runtime(...)` and
  `launch_owned_batch_generation_resume_runtime(...)`. The write workflow now
  keeps only the higher-level orchestration it should own:
  access validation, request preparation, task-plan / resume-plan derivation,
  and error-shape mapping. Execution-config resolution, launch construction,
  and runtime dispatch are now owned together by the dedicated launch service.
  This preserves the existing route-facing behavior, task creation/resume
  payloads, config error mapping, and runtime dispatch semantics while making
  the owner boundary closer to the already-cleaner single-generation pattern.

  Focused tests in `chapter_batch_generation_runtime_launch_service.rs` were
  extended to keep the launch response payload contract explicit after the
  owner move, and the existing workflow error-shape tests still protect the
  route-facing compatibility contract. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch write workflow is now thinner in a way
  that actually shrinks a behavior-sensitive owner boundary, not just file
  length. The next worthwhile move should continue in this class of seams:
  shared runtime-launch / dispatch / workflow-owner consolidation across the
  remaining chapter-generation write paths or another similarly high-signal
  behavior-owner boundary, rather than returning to low-value transport cleanup.

- 2026-05-22 15:02 +08:00 checkpoint:
  this slice continued the higher-signal behavior-owner migration by splitting
  mixed single-chapter runtime dispatch ownership out of the batch dispatch
  lane. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and the single-chapter prepare/runtime services showed that the batch
  dispatch module still owned two distinct responsibilities at once:
  batch runtime dispatch for create/resume, and single-chapter runtime dispatch
  wiring for both background generation and the single-chapter branch of batch
  resume. That left a cross-lane owner overlap where single-generation runtime
  construction still lived under a batch-named dispatch boundary even after the
  launch/workflow layers had become more explicit.

  The refactor introduced
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs` as the
  dedicated owner for single-chapter runtime dispatch, including both direct
  background dispatch and single-chapter resume dispatch input assembly.
  `chapter_single_generation_background_launch_service.rs` now depends on the
  dedicated single-dispatch owner, while
  `chapter_batch_generation_dispatch_service.rs` retains only the batch
  execution input contract plus batch create/resume dispatch routing and
  delegates its single-chapter resume branch to the new single owner. This
  preserves task execution behavior, runtime inputs, and route-facing payloads
  while making the owner boundary align better with the existing single vs
  batch generation split rather than continuing to mix them under one module.

  Focused tests were added so the split remains explicit:
  `chapter_single_generation_dispatch_service.rs` now protects the single
  execution-input shape it owns, `chapter_batch_generation_dispatch_service.rs`
  keeps the batch execution-input contract explicit, and the existing
  single-background launch test still verifies the launch payload handed to the
  new single dispatch owner. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: batch-vs-single generation ownership is now more
  coherent across workflow, launch, and dispatch seams. The next worthwhile
  move should keep following this direction by identifying another mixed owner
  boundary where single and batch logic still overlap in one module, or by
  tightening the corresponding runtime-state / resume path owners, rather than
  reopening low-signal route cleanup.

- 2026-05-22 15:16 +08:00 checkpoint:
  this slice continued the same owner-tightening direction on the resume/runtime
  seam by removing a duplicated single-vs-batch resume-position semantic that
  had started to drift across two runtime owners. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_status_semantics_service.rs`
  showed that the logic for deciding whether a resumed task should preserve a
  current chapter pointer (single-chapter generation) or clear it (true batch
  generation) existed in parallel local helpers inside both the runtime-state
  owner and the runtime-checkpoint owner. At the same time, other nearby code
  already used the shared `task_type(...)` semantic for the same single-vs-batch
  distinction. Keeping a second ad hoc `chapter_count == 1` branch in multiple
  places was a real compatibility-risk seam because single-task detection rules
  could drift again during future refactors.

  The refactor introduced
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  as the shared owner for `resume_runtime_position(...)`, with the single-vs-batch
  decision now delegated to the existing shared `task_type(...)` semantic
  instead of being re-encoded independently. Both
  `chapter_batch_generation_runtime_state_service.rs` and
  `chapter_batch_generation_runtime_checkpoint_service.rs` now consume this
  shared resume semantic owner. This preserves the current behavior:
  single-chapter resumed tasks still retain their current chapter id/number
  pointer, true batch resumed tasks still clear that pointer, and malformed
  “single” payloads that do not satisfy the existing `task_type(...)` contract
  still fall back to batch behavior. The change therefore removes a parallel
  decision path without changing the route-facing or persisted resume contract.

  Focused tests were added in the new resume semantics service to lock the
  single-vs-batch/malformed behavior explicitly, and the existing runtime-state
  plus runtime-checkpoint tests still passed unchanged, confirming that the
  shared owner preserved the previous semantics. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: single-vs-batch generation ownership is now tighter
  not only in workflow/launch/dispatch seams, but also in resume/runtime shared
  semantics. The next worthwhile move should continue this pattern by locating
  other duplicated single-vs-batch semantic branches near resume/runtime/status
  behavior and collapsing them behind one owner, rather than returning to
  cosmetic or transport-only cleanup.

- 2026-05-22 21:52 +08:00 checkpoint:
  this slice continued the same Phase 5 behavior-owner migration by closing the
  next duplicated single-vs-batch resume semantic adjacent to the newly shared
  resume-position owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that the resume/runtime lane still split one more compatibility-sensitive
  decision across owners: `runtime_checkpoint` still decided locally whether a
  resumed task should include pending `completed/total` progress totals via an ad
  hoc `task.chapter_count > 1` branch, while the already-shared resume-position
  owner was making a neighboring single-vs-batch decision through the canonical
  `task_type(...)` semantic. That left a real drift risk because malformed
  “single” tasks could continue to fall back to batch semantics for chapter
  pointers while still being judged differently for pending progress totals.

  The refactor expanded
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  into the shared owner for the full resumed runtime shape by adding
  `ResumeRuntimeSemantics` plus `resolve_resume_runtime_semantics(...)`. That
  shared semantic now owns three coupled decisions together:
  current chapter id, current chapter number, and whether resumed pending
  checkpoints should include progress totals. `resume_runtime_position(...)`
  now delegates to that shared struct, and
  `chapter_batch_generation_runtime_checkpoint_service.rs` now consumes the same
  owner instead of maintaining its own `chapter_count > 1` branch. This preserves
  the current behavior surface:
  proper single-chapter resumed tasks still retain their chapter pointer and omit
  totals, true batch resumed tasks still clear the pointer and include
  `completed=0/total=<n>`, and malformed “single” payloads still follow the
  existing `task_type(...)` fallback into batch behavior for both decisions
  instead of only one.

  Focused tests were extended in the resume semantics owner to lock the unified
  single/batch/malformed contract explicitly, and the existing runtime-checkpoint
  plus runtime-state tests still passed unchanged, confirming the shared owner
  preserved the persisted resume checkpoint behavior. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the resumed runtime shape is now owned in one place
  rather than split across adjacent checkpoint/state helpers, which reduces
  future semantic drift risk more than route-only cleanup would. The next
  worthwhile move should stay on this higher-signal lane by checking whether the
  resume response payload adapter or resume execution-plan owner still duplicates
  single-vs-batch semantics that should consume the same shared owner, instead of
  reopening transport-level refactors that have already reached diminishing
  returns.

- 2026-05-22 22:28 +08:00 checkpoint:
  this slice continued the same Phase 5 resume/runtime ownership tightening by
  closing the next adjacent single-vs-batch drift point in the resume command
  lane. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the runtime/checkpoint side was already using shared resume
  semantics, but the resume command owner still carried its own parallel
  single-vs-batch decision path when building `ResumeExecutionPlan`:
  it re-checked `task_type(...)`, parsed batch chapter ids locally, and decided
  the malformed-single fallback independently from the runtime/checkpoint lane.
  That remained a real compatibility-risk seam because malformed “single”
  payloads could drift again between “how resume checkpoint state is rebuilt”
  and “how resume execution work is dispatched”.

  The refactor expanded
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  again, this time with `ResumeExecutionTarget` plus
  `resolve_resume_execution_target(...)`. That shared owner now also decides
  whether a resumed task should target a single chapter or a batch chapter-id
  list, including the same malformed-single fallback behavior already enforced
  by `task_type(...)`. `chapter_batch_generation_resume_task_command_service.rs`
  no longer parses batch chapter ids or re-encodes the single-vs-batch branch
  locally; it only normalizes `target_word_count`, converts the shared target
  into `ResumeExecutionPlan`, and preserves the existing “no chapters to resume”
  error when the shared target resolves to an empty batch list or a missing
  single chapter id. This keeps the route-facing resume payload, launch
  contract, runtime dispatch behavior, and error strings unchanged while making
  resume execution selection follow the same owner as the resume runtime shape.

  Focused tests were extended in the shared resume semantics owner to lock the
  execution target contract for single/batch/malformed-single tasks, and new
  resume-task-command tests now protect the malformed-single fallback through
  the shared owner rather than through a local parser branch. Validation passed
  with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the resume lane now shares single-vs-batch semantics
  across runtime state, runtime checkpoint, and execution-plan selection rather
  than letting each neighbor keep its own fallback branch. The next worthwhile
  move should re-evaluate the resume response payload adapter or other
  status/read-side adapters for similarly duplicated resume semantics, but only
  if the change can keep the external payload contract fully stable.

- 2026-05-22 22:46 +08:00 checkpoint:
  this slice continued the same Phase 5 internal-owner tightening without
  changing any external payload shape by removing the last remaining
  string-comparison drift point from the shared batch-generation task-kind
  semantics. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_semantics_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`,
  and their downstream consumers showed that the codebase had already narrowed
  most single-vs-batch decisions to a small group of shared helpers, but those
  helpers still depended on repeating the external string contract
  `"chapter_single_generate"` / `"chapters_batch_generate"` for internal control
  flow. That left an unnecessary maintenance seam: the same owner both exposed
  user-facing string semantics and forced nearby internal services to branch on
  those strings instead of on a typed internal concept.

  The refactor introduced `BatchGenerationTaskKind` plus `task_kind(...)` in
  `backend-rs/src/services/chapter_batch_generation_status_semantics_service.rs`
  as the typed internal owner for this distinction. The existing
  `task_type(...)` function remains in place and still returns the same external
  string values for payload compatibility, but it now delegates to the enum
  owner. `task_execution_mode(...)` and the shared resume semantics owner now
  also branch on `BatchGenerationTaskKind` instead of re-checking those strings.
  This preserves the public `task_type` payload contract, keeps all existing
  status / resume behavior unchanged, and further reduces the chance that a
  future refactor updates one internal string check while missing another.

  Focused tests were extended so the typed task-kind owner and the legacy
  string-returning helper stay locked together for normal single, normal batch,
  and malformed-single fallback cases. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch-generation resume/status lane now shares a
  typed internal task-kind owner while still preserving the external string
  payload contract. The next worthwhile move should keep the same bar: either
  remove another real semantic drift seam without changing user-facing payloads,
  or stop before the work collapses into cosmetic cleanup.

- 2026-05-22 14:16 +08:00 checkpoint:
  this slice continued the same Phase 5 resume/status owner hardening by
  typing the internal resume domain-error contract without changing the
  external HTTP payload or detail strings. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` showed that
  the resume command lane had already centralized most single-vs-batch runtime
  semantics, but still passed domain failures around as raw `String`s. That
  left another owner split: the resume task-command service effectively knew the
  meaning of failures such as invalid status, manual-review block, and empty
  resume target, while the workflow / mapper layers only saw untyped text and
  therefore could not express the internal contract directly.

  The refactor introduced `ResumeBatchGenerationDomainError` as the typed
  internal owner for those resume-domain failures and moved the stable external
  detail-string contract behind `detail_message()`. Both
  `build_resume_execution_plan(...)` and
  `prepare_batch_generation_resume(...)` now return typed domain errors, while
  `ResumeBatchGenerationWriteWorkflowError::Domain(...)` carries the typed
  error instead of a raw string. The API mapper continues to emit the exact same
  `400` responses by calling `error.detail_message()`, so route-facing payload
  shape and user-visible messages remain unchanged even though the internal
  contract is now explicit and testable.

  Focused regression coverage was added to lock the typed-error seam in place:
  resume-task-command tests now protect malformed-single fallback and the
  stable detail strings, and the error-mapper test suite explicitly verifies
  that `NoChaptersToResume` still maps to the same `400 + detail` payload.
  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the resume write lane now exposes a typed internal
  domain-error owner while keeping the external HTTP error contract fully
  stable. The next worthwhile move should keep looking for similarly real owner
  drift in the adjacent resume/status payload adapters, but only if the change
  reduces semantic duplication rather than merely relocating response-shell
  code.

- 2026-05-22 14:22 +08:00 checkpoint:
  this slice continued the same Phase 5 adjacent-owner tightening by removing a
  repeated batch-task runtime payload assembly seam across the resume-command
  response adapter and the read-side status payload adapter. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_payload_base_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  showed that two neighboring adapters were each rebuilding the same internal
  base contract from task state: `task_stage_code(...)`,
  `task_execution_mode(...)`, `checkpoint_with_runtime_metadata(...)`, and the
  common runtime payload envelope. The observable payloads were different, but
  the owner of “how a batch task becomes its base runtime payload view” was
  still duplicated, which meant future changes to status semantics could drift
  between resume responses and read-side task views.

  The refactor introduced
  `build_batch_generation_task_runtime_payload_from_state(...)` in
  `chapter_batch_generation_task_payload_base_service.rs` as the shared owner
  for that internal assembly contract. Both the resume-command response adapter
  and the status payload adapter now consume the same helper instead of each
  recalculating stage/execution/checkpoint metadata locally. The resume adapter
  still keeps its route-facing special fields (`message`,
  `total_chapters`, `completed_chapters`, and `checkpoint.chapter_id`) and the
  status adapter still keeps its status-only or active-only decorations, so the
  external payload shapes remain unchanged while the duplicated internal
  semantic owner is reduced.

  Focused tests were extended in the shared base service to lock the new helper
  against the existing payload contract, and the adjacent adapter/query tests
  confirmed that resume payloads plus read-side active/status payloads kept the
  same fields and values after the owner move. Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_payload_adapter_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the resume/status payload lane is now slightly
  tighter because the base runtime-task view is owned once. The next move
  should again be held to the same standard: continue only if another seam
  reduces a real semantic owner split, not if it only shuffles payload-shell
  code around.

- 2026-05-22 14:30 +08:00 checkpoint:
  this slice continued the same Phase 5 read-side owner tightening by turning
  the batch-generation quality/status trio into one shared context owner across
  the adjacent read-context, task-view, and payload-adapter seams. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  showed that the codebase already had a local
  `BatchGenerationQualityStatusContext`, but the read-side lane still unpacked
  it back into three parallel fields
  (`latest_quality_metrics`, `quality_metrics_summary`,
  `active_story_repair_payload`) and then re-threaded those fields through the
  task-view and payload-adapter owners separately. That left another contract
  drift seam because the meaning of “quality/status read-side context” was
  owned in one place yet still transported as three independent values through
  neighboring owners.

  The refactor keeps `BatchGenerationQualityStatusContext` as the shared owner
  and now carries it intact across the read-side lane instead of exploding it
  into parallel fields. `BatchGenerationReadContext` and
  `BatchGenerationTaskViewContext` now store `quality_status_context`, and the
  status/active payload adapters consume that shared context directly when
  inserting quality metrics and story-repair fields. This preserves the
  existing active/status payload shapes and values while reducing another local
  owner split in the batch-generation read path.

  Focused tests were updated across the read-context, task-view, active-query,
  active-list-query, quality-status, and payload-adapter services so the shared
  context remains explicit without changing route-facing payloads. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch-generation read-side context is now less
  fragmented, and the remaining worthwhile Phase 5 work in this area should
  continue only if another seam shrinks a true behavior/context owner boundary
  rather than further repackaging already-stable payload shells.

- 2026-05-22 14:35 +08:00 checkpoint:
  this slice continued the same Phase 5 read-side owner reduction by removing a
  now-redundant duplicate context layer in the batch-generation query lane. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  showed that after the prior quality-context tightening, `BatchGenerationTaskViewContext`
  had become structurally identical to `BatchGenerationReadContext`: the same
  task model, the same workflow runtime state, and the same shared
  `quality_status_context`. The query lane was therefore still maintaining a
  second named owner for the exact same context contract, plus local conversion
  helpers that did nothing except rewrap one context type into the other.

  The refactor removed that duplicate owner by aliasing
  `BatchGenerationTaskViewContext` directly to `BatchGenerationReadContext` and
  deleting the intermediate conversion helpers. The task-view query service now
  consumes the read-context owner directly for required, active-project, and
  active-list query paths. This keeps active/status query payloads, error
  aliases, and route-facing behavior unchanged, while reducing one more local
  context drift seam inside the read-side lane.

  Focused tests across the task-view, active-query, active-list-query,
  status-query, and read-workflow services all continued to pass after the
  owner reduction, and shared-target `cargo check` stayed green. Validation
  passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch-generation read/query lane now has one
  fewer duplicate context owner. The next worthwhile Phase 5 move in this area
  should continue only if another seam removes real contract drift rather than
  simply wrapping existing helpers in thinner compatibility shells.

- 2026-05-22 14:41 +08:00 checkpoint:
  this slice continued the same Phase 5 stream/read owner tightening by
  collapsing a duplicated stream-access contract that had been split between an
  owned-task precheck and the first actual stream-state read. A focused audit
  of
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_read_workflow_service.rs`
  showed that the stream path still performed two adjacent owner steps for the
  same access/read contract: the read workflow first called
  `load_required_owned_task(...)` indirectly through
  `ensure_batch_generation_status_stream_access(...)`, and then the stream loop
  immediately performed its own read-context lookup through
  `load_batch_generation_stream_state(...)` to get the first usable stream
  state. That meant the stream lane still split “can this caller access the
  stream and what is its initial observable state?” across two owners even
  though the second owner already depended on the same task-ownership boundary.

  The refactor moved the initial stream-access/read contract into the
  stream-state owner by adding
  `load_required_batch_generation_stream_state(...)` in
  `chapter_batch_generation_status_view_service.rs`, which builds the same
  stream state from the required owned read-context boundary. The read workflow
  now loads that required initial state once and passes it into
  `build_batch_generation_status_stream(...)`, while the stream service uses
  that owned initial state for the first emission before falling back to the
  existing optional polling reads for subsequent iterations. This preserves the
  existing access error mapping, stream event payloads, timeout/not-found
  behavior after startup, and route-facing SSE contract, while removing one
  redundant precheck/read split from the stream lane.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_view_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch-generation stream/read path now shares one
  initial stream-state owner instead of splitting access and first-state
  loading across two adjacent helpers. Further work here should keep the same
  bar and stop if only thin wrapper cleanup remains.

- 2026-05-22 14:53 +08:00 checkpoint:
  this slice continued the same Phase 5 owner-tightening work by removing a
  crossed single-resume runtime-input seam between the batch resume launch lane
  and the single-generation dispatch lane. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`
  showed that single-chapter resume still rebuilt
  `SingleChapterGenerationExecutionInput` after the launch boundary had already
  resolved the runtime config and provider payload. The batch dispatch service
  exploded `ResumeExecutionPlan::SingleChapter` back into separate runtime
  parameters, and the single-generation dispatch service immediately
  reassembled those fields into the owned single-generation execution contract.
  That left one more real owner drift seam: the single-generation runtime input
  contract already existed, but it was not the value actually flowing across
  the resume launch/dispatch boundary.

  The refactor moved that owner boundary forward by introducing
  `ResumeGenerationExecutionInput` plus
  `build_resume_generation_execution_input(...)` in
  `chapter_batch_generation_dispatch_service.rs`. Batch resume launch now
  resolves the owned runtime input once from `ResumeExecutionPlan` plus the
  prepared `AIConfig` / provider payload, and
  `dispatch_resume_generation_runtime(...)` now only forwards either the owned
  batch execution input or the owned single-chapter execution input. The
  single-generation dispatch service no longer reconstructs resume execution
  input from exploded fields; it now only owns transport/runtime dispatch for
  an already-built `SingleChapterGenerationExecutionInput`. This preserves
  task dispatch behavior, runtime semantics, response payloads, and all
  route-facing contracts while reducing another adjacent internal owner split.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-chapter resume path now carries the same
  owned runtime-input contract that the rest of the single-generation lane
  already uses. The next worthwhile Phase 5 move should continue outward from
  this seam only if it removes another real owner split in single-generation
  launch/workflow boundaries, not if it merely renames or rewraps the now-thin
  dispatch shells.

- 2026-05-22 14:58 +08:00 checkpoint:
  this slice continued the same Phase 5 single-generation owner tightening by
  collapsing a duplicated runtime-launch owner across the background, stream,
  and batch-resume entry lanes. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that the codebase already had one stable single-generation runtime
  execution contract (`SingleChapterGenerationExecutionInput`), but the
  adjacent launch lanes still each carried a parallel `user_id +
  execution_input` owner shape locally. Background launch wrapped those two
  fields in one struct, stream launch wrapped the same two fields in another
  struct, and batch resume embedded the same pair inside its own resume enum
  branch. The dispatch layer therefore still had multiple neighboring owners
  for the exact same “single-generation runtime launch input” boundary.

  The refactor introduced `SingleGenerationRuntimeLaunchInput` plus
  `build_single_generation_runtime_launch_input(...)` in
  `chapter_single_generation_dispatch_service.rs` as the shared owner for that
  boundary. The single-generation dispatch function now accepts that owned
  runtime-launch input directly, background launch stores it as
  `runtime_input`, stream launch aliases its launch type to the same shared
  input, and batch resume now carries the shared runtime-launch owner in its
  single-chapter branch. This preserves task dispatch behavior, SSE flow,
  response payloads, and runtime execution semantics while reducing another
  local owner split inside the single-generation launch/dispatch lane.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation runtime launch boundary is now
  owned once across background, stream, and resume entry lanes. The next
  worthwhile Phase 5 move should keep looking for real workflow/plan owner
  splits around prepared single-generation requests or task-plan inputs, not
  merely rename the remaining thin wrappers.

- 2026-05-22 15:03 +08:00 checkpoint:
  this slice continued the same Phase 5 single-generation workflow tightening
  by moving the prepared-request to background-task-plan projection into the
  task-command owner itself. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  showed that the background workflow still unpacked
  `PreparedSingleChapterGenerationRequest` into separate fields before calling
  the task-plan owner: it passed `&prepared.chapter_model` and
  `prepared.execution_input.target_word_count` separately, then passed the full
  `prepared.execution_input` onward to the launch owner. That meant the
  workflow layer still partially redefined how a prepared single-generation
  request becomes a background task-create input instead of letting the
  task-command owner consume the prepared request directly.

  The refactor changed
  `create_single_generation_background_task_plan(...)` to accept
  `&PreparedSingleChapterGenerationRequest` and added
  `build_single_generation_background_task_active_model_from_prepared_request(...)`
  as the local projection owner inside
  `chapter_single_generation_task_command_service.rs`. The background workflow
  now forwards the prepared request directly to the task-plan owner instead of
  re-extracting `chapter_model` and `target_word_count` itself. This preserves
  background task payload shape, snapshot initialization, task defaults, and
  route-facing behavior while reducing one more internal owner split between
  prepare, workflow, and task-command boundaries.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the single-generation background workflow now
  forwards one prepared-request owner to the task-plan boundary instead of
  unpacking part of that contract itself. The next worthwhile Phase 5 move
  should continue only if it removes another real owner split in the remaining
  single-generation workflow/prepare lane rather than flattening already-thin
  transport wrappers.

- 2026-05-22 15:09 +08:00 checkpoint:
  this slice extended the same Phase 5 owner-tightening pattern from the
  single-generation lane into the batch-create workflow lane by moving the
  create-request to task-plan projection into the batch task-command owner. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  showed that the batch create workflow still unpacked both the normalized
  route request and the prepared create request into separate task-plan inputs:
  it passed `start_chapter_number`, `style_id`, `enable_analysis`,
  `max_retries`, `prepared.normalized_target_word_count`, and
  `&prepared.chapters_to_generate` independently even though those fields
  together already described one owned “batch create task input” boundary.

  The refactor changed `create_batch_generation_task_plan(...)` to accept
  `&BatchGenerationCreateWorkflowRequest` plus
  `&PreparedBatchGenerationCreateRequest`, and added
  `build_batch_generation_task_active_model_from_create_request(...)` as the
  local projection owner inside
  `chapter_batch_generation_create_task_command_service.rs`. The batch create
  workflow now forwards the normalized workflow request and prepared create
  request directly to the task-plan owner instead of re-extracting task fields
  itself. This preserves create response payloads, task defaults, snapshot
  initialization, and runtime launch behavior while reducing one more internal
  owner split between request-compat, prepare, workflow, and task-command
  boundaries.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch create workflow now forwards one normalized
  request owner plus one prepared create owner into the task-plan boundary
  instead of recomputing task-plan inputs itself. The next worthwhile Phase 5
  move should keep targeting real owner splits in the remaining batch create /
  launch / prepare adjacency, not cosmetic wrapper flattening.

- 2026-05-22 15:14 +08:00 checkpoint:
  this slice continued the same Phase 5 batch-generation owner tightening by
  collapsing a duplicated runtime-input owner across the batch create and batch
  resume launch lanes. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the batch lane still had two neighboring owners for the same
  runtime input shape. The create path built `BatchGenerationExecutionInput`
  inline in the launch service, while the resume path reconstructed the same
  shape in the dispatch service when `ResumeExecutionPlan::Batch` was selected.
  That meant the runtime launch contract for batch generation was still owned
  in two places even though the shape itself was already stable and shared.

  The refactor introduced `build_batch_generation_execution_input(...)` in
  `chapter_batch_generation_dispatch_service.rs` and made both the batch create
  launch and the batch resume launch use that one helper. `build_batch_generation_create_launch(...)`
  now delegates to the shared builder instead of assembling the same struct
  inline, and `build_resume_generation_execution_input(...)` now uses the same
  builder for the batch branch. This preserves batch launch behavior, runtime
  dispatch semantics, response payloads, and resume contract shape while
  reducing another local owner split inside the batch runtime launch lane.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Updated lane assessment: the batch runtime input boundary is now shared once
  across create and resume launch paths. The next worthwhile Phase 5 move
  should only continue if it removes another real owner split in nearby batch
  create/prepare/launch seams, not if it merely shortens already-thin wrappers.

- 2026-05-22 15:22 +08:00 checkpoint:
  this slice stayed within the same Phase 5 rule and removed two now-redundant
  compatibility wrappers instead of inventing a new boundary. A focused review
  of `backend-rs/src/services/chapter_single_generation_request_service.rs`
  showed that `build_single_chapter_generation_request_from_route(...)` was no
  longer a real owner after `SingleChapterGenerationRequestRouteFields` had
  become the route-field projection boundary. The route path already flowed
  through `build_single_chapter_generation_request_from_route_fields(...)`,
  so the older helper only duplicated the same projection step and was kept
  alive by one leftover test. The refactor removed that intermediate wrapper
  and its stale unit test, leaving route-field projection as the sole single-
  generation route owner for this request boundary.

  The same cleanup also removed
  `build_batch_generation_create_workflow_request_with_compat_fields(...)`
  from `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`.
  That helper had become a pure compatibility wrapper over
  `build_batch_generation_create_workflow_request(...)` and was no longer used
  by production code after the route-field owner split had landed. Removing it
  reduces API surface without changing payload shape, normalization rules, or
  route behavior. This round is intentionally conservative: it tightens the
  internal surface only where there is no remaining behavioral owner.

  Validation is next:
  run `cargo fmt`, targeted tests for the touched request/compat services, and
  a shared `cargo check` to make sure the dead wrapper removal does not disturb
  the surrounding chapter-generation lane.

- 2026-05-22 15:31 +08:00 checkpoint:
  this slice completed the conservative wrapper removal and left the route
  field owner as the only request projection boundary on the single-generation
  path. `build_single_chapter_generation_request_with_compat_fields(...)` was
  removed from `backend-rs/src/services/chapter_single_generation_request_service.rs`,
  and `build_single_chapter_generation_request_from_route_fields(...)` now
  consumes compat fields directly before building the final request. That keeps
  `enable_analysis` as a consumed compatibility field without carrying a second
  public helper that only rewrapped the same normalization step.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"``,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The remaining warnings are pre-existing workspace-level dead-code or
  re-export warnings outside this slice.

- 2026-05-22 16:02 +08:00 checkpoint:
  this slice continued the same Phase 5 single-generation owner tightening by
  extending the shared runtime-launch contract into the execution layer. A
  focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_execution_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the single-generation lane already owned one shared runtime
  launch boundary, `SingleGenerationRuntimeLaunchInput`, for background launch
  and batch-resume single-chapter dispatch. But the execution layer still
  split that same boundary back into `user_id + execution_input`, which meant
  both stream launch and runtime dispatch had to destructure and re-forward the
  same contract before actual execution started.

  The refactor moved execution ownership forward so
  `execute_single_chapter_generation(...)`,
  `describe_single_chapter_generation_execution(...)`, and
  `execute_single_generation_runtime(...)` now all consume the shared
  `SingleGenerationRuntimeLaunchInput` directly. The stream launch path no
  longer redefines the runtime boundary before execution, and the dispatch
  layer no longer has to unpack and re-pack the same owner before handing off
  to runtime state. This preserves SSE payload shape, background task runtime
  behavior, resume semantics, and generated chapter persistence while reducing
  another adjacent single-generation contract split.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_execution_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Note: the default `backend-rs/target` validation path is currently noisy on
  this machine due to Windows file-lock / access-denied errors in incremental
  artifacts, so the independent shared target directory was used for clean
  verification of this slice.

- 2026-05-22 16:09 +08:00 checkpoint:
  this slice continued the same Phase 5 single-generation contract tightening
  by collapsing a duplicated execution-input constructor that was still split
  across prepare and resume-dispatch lanes. A focused audit of
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that `SingleChapterGenerationExecutionInput` was still assembled in
  two places: the normal single-generation prepare path built it from chapter
  access plus prepared execution config, while the batch resume single-chapter
  branch rebuilt the same struct inline from resume plan fields plus prepared
  config payload. That meant the shared execution-input contract still had two
  adjacent constructors even after the runtime-launch owner itself had been
  unified.

  The refactor introduced
  `build_single_chapter_generation_execution_input(...)` in
  `chapter_single_generation_prepare_service.rs` and switched both the
  prepared-request path and the batch resume single-chapter branch to reuse
  that one builder. This preserves target-word-count normalization, provider
  payload flow, resume semantics, and downstream runtime behavior while
  reducing another small but real contract split between the single-generation
  prepare lane and the batch resume dispatch lane.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.

- 2026-05-23 00:00 +08:00 checkpoint:
  this slice stayed inside the same Phase 5 contract-tightening rule but moved
  to the batch-resume semantic lane instead of the already-thin read-side/status
  wrappers. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  showed that the resume target and runtime semantics helpers were each
  re-running the same `task_kind(task)` branch to decide whether a task was
  single-chapter or batch. That meant the same ownership decision was still
  being derived in two adjacent helpers even though the split itself was now
  stable and could be shared locally.

  The refactor introduced a private
  `ResumeBatchGenerationSemantics` owner inside
  `chapter_batch_generation_resume_semantics_service.rs` and made both
  `resolve_resume_runtime_semantics(...)` and
  `resolve_resume_execution_target(...)` consume that one shared decision. The
  behavior stays the same for single-chapter, batch, and malformed-single
  resume cases, but the branch logic now lives in one place instead of being
  re-evaluated separately by two helpers. This reduces one more small but real
  semantic drift seam in the batch resume lane without changing the external
  payload shape or resume policy.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"` ,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.

- 2026-05-23 00:10 +08:00 checkpoint:
  the latest planning audit moved from rough route counts to file-level
  migration mapping so the remaining Rust migration debt is easier to track.
  A direct comparison between `backend/app/api` and `backend-rs/src/api`
  showed that the obvious high-signal chapter-generation and batch-generation
  routes already have Rust counterparts, while the remaining Python files that
  still look like true unmigrated targets are concentrated in
  `chapter_annotation_routes.py`, `chapter_draft_routes.py`,
  `chapter_expansion_plan_routes.py`, and `chapter_quality_routes.py`.
  `backend/app/api/chapters.py` remains a mixed boundary that should be
  treated as seam cleanup rather than a fresh route migration target, and
  `chapter_route_helpers.py` is a helper file, not migration debt.

  Updated migration estimate:
  - route-count coverage is still roughly `223 / 228` or about `97.8%`
  - file-level API coverage is roughly `29 / 35` after excluding shared
    helpers, which leaves about `6` Python API files still worth a closer
    look
  - the remaining work is therefore mostly tail cleanup, not broad interface
    migration

  No code behavior changed in this checkpoint; the change is the planning map
  itself. This keeps the remaining scope explicit for the next Phase 5 slice
  and prevents us from treating already-migrated seams as open debt.

- 2026-05-23 00:20 +08:00 checkpoint:
  the latest mapping pass refined the remaining Python-vs-Rust list into an
  explicit file-level status table so the follow-up plan can distinguish
  migrated shells from genuinely open seams. The audit showed that
  `chapter_analysis_task_routes.py`, `chapter_annotation_routes.py`,
  `chapter_batch_generation_routes.py`, `chapter_draft_routes.py`,
  `chapter_expansion_plan_routes.py`, `chapter_generation_routes.py`,
  `chapter_partial_regeneration_routes.py`, `chapter_quality_routes.py`, and
  `wizard_stream.py` all already have Rust counterparts or equivalent Rust
  route/service owners. In other words, these Python files are now legacy
  compatibility shells rather than open Rust capability gaps. `chapters.py`
  remains the only clearly mixed boundary that still deserves seam-level
  analysis, while `chapter_route_helpers.py` stays excluded from migration
  accounting because it is a helper file, not a route interface.

  Planning implication:
  - the remaining migration debt is mostly shell removal and seam cleanup
  - the only high-signal open boundary still worth careful analysis in this
    lane is `backend/app/api/chapters.py`
  - future Phase 5 slices should only target a Python route file when the Rust
    owner already exists and the change reduces drift, not when it just
    shortens a wrapper

  No runtime behavior changed in this checkpoint; this is a planning
  refinement so the remaining work is explicit and we do not overcount legacy
  compatibility files as open migration debt.

- 2026-05-23 20:23 +08:00 checkpoint:
  this slice stayed inside the same Phase 5 behavior-preserving contract
  tightening rule and moved to a narrow `chapter_regeneration` helper seam
  instead of reopening already-thin route or batch read-side wrappers. A
  focused audit of
  `backend-rs/src/services/chapter_regeneration_text_service.rs`,
  `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`,
  `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`,
  and
  `backend-rs/src/services/chapter_partial_regeneration_stream_service.rs`
  showed that `FinalizedPartialRegenerationResult` and
  `FinalizedChapterRegenerationResult` still carried a `cleaned_text` field
  even though all production callers only consumed `result.payload`, and the
  externally observable SSE/result contract already lives in that payload
  object. The extra field was only asserted in local tests, which meant the
  finalized helper contract was wider than its real downstream usage.

  The refactor removed `cleaned_text` from both finalized result structs and
  kept `chapter_regeneration_text_service.rs` focused on one output owner:
  the final payload emitted to stream callers. This preserves full-chapter and
  partial-regeneration SSE payload shape, word-count semantics, and
  sanitization/meta-text rejection behavior while trimming another unused
  internal contract field from the regeneration lane.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_text_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The remaining warnings are pre-existing workspace-level dead-code /
  unused-item warnings outside this slice; this change itself did not add a
  new warning class.

- 2026-05-23 20:27 +08:00 checkpoint:
  this slice stayed in the same `chapter_regeneration` Phase 5 seam and
  continued the prepared-contract narrowing rule instead of switching into the
  paused `chapter_quality` lane. A focused audit of
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs` and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that `PreparedPartialRegenerationInput` still carried
  `selected_text`, `context_before`, and `context_after` even though those
  fields were only used locally to build the final prompt. Downstream stream
  workflow and launch owners only consumed the derived `prompt` plus numeric
  execution parameters (`original_word_count`, `target_words`, `max_tokens`),
  so the prepared contract remained wider than its production usage.

  The refactor removed those three prompt-construction-only fields from
  `PreparedPartialRegenerationInput` and updated the focused tests to assert
  the owned `prompt` content and derived numeric outputs instead. This
  preserves partial-regeneration request defaults, context extraction,
  prompt wording, max-token clamping, and downstream SSE/runtime behavior
  while trimming another unused internal prepared boundary from the
  regeneration lane.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  This slice also removed the previous dead-code warning on
  `PreparedPartialRegenerationInput.selected_text/context_before/context_after`;
  the remaining warnings are still pre-existing workspace-level items outside
  this seam.

- 2026-05-23 20:31 +08:00 checkpoint:
  this slice stayed in the quality-metrics source lane but only to remove an
  internal classification field, not to reopen the paused `chapter_quality`
  feature work. A focused audit of
  `backend-rs/src/services/chapter_quality_metrics_source_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`, and
  `backend-rs/src/services/chapter_quality_metrics_payload_adapter_service.rs`
  showed that `QualityMetricsSourceKind` and the `source_kind` field on
  `ResolvedQualityMetricsSource` were not part of the production query payload
  contract. The query and payload adapter paths only consume `metrics`,
  `source_history_id`, and `generated_at`, so the extra classification enum
  only widened the internal helper contract without affecting output shape.

  The refactor removed `QualityMetricsSourceKind` and the `source_kind` field
  from the source resolver, while keeping the resolved metrics/id/date values
  intact. The source owner now carries only the data the downstream query and
  payload adapter actually use, and the existing quality-metrics response
  shape remains unchanged.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_source_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The remaining warnings are pre-existing workspace-level items outside this
  seam; this change itself did not introduce a new warning source.

- 2026-05-23 20:36 +08:00 checkpoint:
  this slice stayed in the prompt-context owner lane and removed a thin default
  wrapper rather than introducing new behavior. A focused audit of
  `backend-rs/src/services/chapter_generation_prompt_context_provider_service.rs`
  and `backend-rs/src/services/chapter_generation_execution_config_service.rs`
  showed that `resolve_default_prompt_context_provider_payload()` was only a
  pass-through to `build_placeholder_prompt_context_provider_payload()`, and
  `prepare_generation_execution_config()` was the only production consumer.
  That meant the default wrapper did not own a distinct contract; it just
  duplicated the placeholder owner.

  The refactor deleted the redundant default wrapper and made execution config
  construction depend directly on the placeholder provider payload owner. The
  provider payload shape, execution-config shape, batch-create/resume runtime
  behavior, and prompt-context defaults remain unchanged, but the ownership
  graph is now one step flatter and easier to audit.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_context_provider_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  This slice removed the redundant default wrapper without changing the
  placeholder payload shape or the execution-config behavior.

- 2026-05-23 20:39 +08:00 checkpoint:
  this slice stayed in the batch-generation runtime owner lane and removed a
  thin constructor wrapper instead of changing the launch contract itself. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that `build_batch_generation_execution_input(...)` was only a pure
  struct-construction helper used twice inside the same runtime family:
  once for batch resume execution assembly, and once in the corresponding
  test. The helper did not own validation, normalization, or side effects; it
  just re-packaged fields already available at the call site.

  The refactor removed the helper and had the runtime-launch / resume path
  construct `BatchGenerationExecutionInput` directly. The batch create/resume
  runtime launch behavior, prompt-context flow, AI config propagation, and
  response payload shape remain unchanged, but the batch execution boundary is
  one layer flatter and easier to audit.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The only remaining warnings are pre-existing workspace-level items outside
  this seam.

- 2026-05-23 20:49 +08:00 checkpoint:
  this slice stayed in the single-generation runtime owner lane and removed a
  thin constructor wrapper rather than changing stream/background behavior. A
  focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  showed that `build_single_generation_runtime_launch_input(...)` was only a
  pure struct-construction helper used by the runtime launch / stream launch
  family and its test. It did not normalize or validate anything; it merely
  wrapped the already-prepared execution input together with `user_id`.

  The refactor removed the helper and had the background/stream/runtime launch
  call sites construct `SingleGenerationRuntimeLaunchInput` directly. The
  single-generation stream/background launch behavior, prompt-context flow,
  AI config propagation, and response payload shape remain unchanged, but the
  runtime boundary is now one layer flatter and easier to audit.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The remaining warnings are pre-existing workspace-level items outside this
  seam.

- 2026-05-23 21:05 +08:00 checkpoint:
  this slice stayed in the batch-generation runtime launch lane and narrowed
  the create side to dispatch-only ownership. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  confirmed that `launch_owned_batch_generation_create_runtime(...)` no longer
  owns a response payload contract; the write-workflow already returns the
  created response payload from `BatchGenerationCreateOutcome`, so the runtime
  launcher only needs to assemble `BatchGenerationExecutionInput` and dispatch
  it. The create-side return type was therefore collapsed from `Value` to
  `()`, matching the existing resume-side dispatch-only boundary.

  The tests in `chapter_batch_generation_runtime_launch_service.rs` were
  updated to assert side effects instead of a return payload, and the shared
  batch write workflow continued to own the outward response payload assembly.
  No HTTP payload shape, runtime dispatch input, or resume behavior changed.

  Validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_launch_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Remaining warnings are pre-existing workspace-level items outside this seam.

- 2026-05-23 21:18 +08:00 checkpoint:
  this slice stayed in the batch-generation create lane and moved response
  ownership out of the task-command owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that `BatchGenerationCreateOutcome` only needed to carry the created
  task record and the dispatch plan. The actual create response payload is a
  workflow-bound transport concern, so `start_owned_batch_generation_write_workflow()`
  now assembles it directly from `created_task` plus the already-prepared
  chapter list.

  This keeps `create_batch_generation_task_plan()` focused on task creation,
  snapshot persistence, and plan data, while the workflow boundary owns the
  outward JSON response. The batch create response shape and runtime dispatch
  behavior remain unchanged.

  Validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_create_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Remaining warnings are pre-existing workspace-level items outside this seam.

- 2026-05-24 00:08 +08:00 checkpoint:
  this slice stayed in the batch-generation resume lane and completed the same
  ownership narrowing that was already applied to batch create. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that `ResumeBatchGenerationOutcome` did not need to carry a
  prebuilt `response_payload`; the actual outward resume response is a
  workflow-bound transport concern, while the task-command owner only needs
  to return the updated task plus the execution plan.

  The refactor therefore changed `prepare_batch_generation_resume()` to return
  `updated_task` together with `ResumeBatchGenerationPlan`, and
  `resume_owned_batch_generation_write_workflow()` now assembles the outward
  JSON response through `build_resume_batch_generation_response_payload()`.
  This keeps resume response shape and runtime launch behavior unchanged while
  making create/resume ownership symmetric across the batch write lane.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  Note: the default `backend-rs/target/debug/incremental` directory currently
  hits a local Windows file-lock / access-denied issue during incremental
  artifact cleanup, so this validation was run against an isolated target
  directory. Remaining warnings are pre-existing workspace-level items outside
  this seam.

- 2026-05-24 00:24 +08:00 checkpoint:
  this slice stayed in the batch-generation cancel lane and completed the same
  response-owner narrowing already applied to create and resume. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`
  showed that `cancel_batch_generation_task()` only owns terminal-status
  rejection plus cancelled-state persistence. The outward cancel response is a
  workflow-bound transport concern, so the command layer did not need to build
  and return JSON directly.

  The refactor changed `cancel_batch_generation_task()` to return the task
  model after the cancelled-state write, and
  `cancel_owned_batch_generation_task()` now assembles the outward response
  through `build_cancel_batch_generation_response_payload()`. This keeps the
  cancel response shape and error mapper behavior unchanged while making the
  create/resume/cancel command lanes structurally consistent.

  Focused tests were added to keep the terminal-status error message stable in
  the command layer and to keep the workflow error enum shape stable for task
  and domain failures.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slice, the default `backend-rs/target/debug/incremental`
  directory currently hits a local Windows file-lock / access-denied issue
  during incremental cleanup, so this validation was run against an isolated
  target directory. Remaining warnings are pre-existing workspace-level items
  outside this seam.

- 2026-05-24 00:39 +08:00 checkpoint:
  this slice pivoted from the now-thin batch read lane to the adjacent
  single-generation background lane and removed one more real response-owner
  split. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that the background path still kept the outward response payload in
  two internal owners after task creation: the task-command plan carried
  `response_payload`, and the background launch owner carried that payload
  again even though dispatch only needed `task_id` plus runtime input.

  The refactor changed `SingleGenerationBackgroundCreatePlan` to carry the
  created task model instead of a prebuilt response payload, removed
  `response_payload` from `OwnedSingleGenerationBackgroundLaunch`, and
  narrowed `dispatch_owned_single_generation_background_runtime()` to
  dispatch-only behavior. `start_owned_single_generation_background_workflow()`
  now owns the outward background-create JSON response through
  `build_single_generation_background_create_response_payload()`. This keeps
  the background task response shape and runtime launch behavior unchanged
  while aligning the single-generation background lane with the same
  task-command / workflow / launch ownership split now used across the batch
  create/resume/cancel paths.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with recent slices, validation used an isolated target directory because
  the default `backend-rs/target/debug/incremental` path currently hits a
  local Windows file-lock / access-denied issue during incremental cleanup.
  Remaining warnings are pre-existing workspace-level items outside this seam.

- 2026-05-24 00:59 +08:00 checkpoint:
  this slice stayed in the adjacent single-generation lane and narrowed one
  more real launch-contract owner split across background, stream, and
  batch-resume(single) execution paths. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that the same `SingleGenerationRuntimeLaunchInput` contract was still
  being assembled in multiple local owners even though all three paths share
  the same runtime boundary: `user_id + prepared execution_input`.

  The refactor introduced
  `build_single_generation_runtime_launch_input()` as the shared owner for
  that contract and switched the background launch owner, the stream workflow
  path, and the batch-resume single-chapter path to reuse it. This keeps
  runtime behavior unchanged while reducing one more cross-lane seam where
  stream/background/resume(single) could have drifted in field assembly or
  future compatibility handling.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. Remaining warnings are pre-existing workspace-level items outside
  this seam.

- 2026-05-24 01:04 +08:00 checkpoint:
  this slice stayed in the same single-generation runtime lane and narrowed
  one more duplicated interpretation boundary around
  `SingleGenerationRuntimeLaunchInput`. A focused audit of
  `backend-rs/src/services/chapter_single_generation_execution_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the runtime input was still being interpreted twice in parallel:
  one local projection owner exposed `chapter_id/target_word_count` metadata
  for stream and runtime-state consumers, while the execution path separately
  destructured the same launch input again to get the full runtime fields.

  The refactor replaced that split with one shared
  `resolve_single_chapter_generation_runtime_input()` boundary in the
  execution owner. Stream launch and runtime-state now both reuse the resolved
  runtime input for progress metadata and execution dispatch, and
  `execute_single_chapter_generation()` now consumes that resolved input
  directly instead of re-destructuring the launch contract internally. This
  keeps stream progress, runtime-state checkpoint behavior, and chapter
  generation execution semantics unchanged while reducing one more place where
  single-generation runtime fields could drift across adjacent owners.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_execution_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. Remaining warnings are pre-existing workspace-level items outside
  this seam.

- 2026-05-24 01:13 +08:00 checkpoint:
  this slice stayed in the same adjacent single-generation lane and narrowed
  the prepared-request owner boundary so downstream task/workflow owners no
  longer need the full `chapter::Model` when they only consume the chapter
  execution identity fields. A focused audit of
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`,
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that the prepared request still carried the entire chapter model
  across adjacent owners even though the downstream consumers only needed a
  narrow slice: `project_id/chapter_id/chapter_number/title` plus the already
  prepared runtime execution input.

  The refactor introduced `SingleChapterGenerationTarget` as the explicit
  chapter-target projection owned by the prepare layer and changed
  `PreparedSingleChapterGenerationRequest` to carry `chapter_target` instead
  of the full chapter model. Background task planning, initial snapshot
  persistence, single-task chapter payload assembly, and background-create
  response payload assembly now all reuse that explicit target projection.
  This keeps task persistence, snapshot behavior, and outward response payload
  shape unchanged while reducing one more place where unrelated chapter-model
  fields could leak across the single-generation write/runtime boundary.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_chapter_payload_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. Any temporary new dead-code warning introduced during the slice was
  removed before final validation; the remaining warnings are pre-existing
  workspace-level items outside this seam.

- 2026-05-24 01:21 +08:00 checkpoint:
  this slice stayed in the same single-generation background lane but only for
  one more real owner narrowing, not thin-wrapper churn. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed two remaining oversized contracts:
  the background task-plan creator still accepted the whole
  `PreparedSingleChapterGenerationRequest` even though it only used
  `chapter_target + target_word_count`, and the background launch owner still
  accepted the whole `SingleGenerationBackgroundCreatePlan` even though it only
  consumed `created_task_id`.

  The refactor narrowed `create_single_generation_background_task_plan()` to
  accept the explicit `chapter_target` projection plus `target_word_count`,
  and narrowed `build_single_generation_background_launch()` to accept
  `task_id` directly instead of the full plan object. The workflow boundary now
  passes only the data each downstream owner actually consumes. This keeps task
  creation, snapshot persistence, background response payloads, and runtime
  dispatch behavior unchanged while trimming another pair of fake cross-layer
  dependencies from the single-generation background path.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. Any temporary new unused-import warnings introduced during the slice
  were removed before final validation; the remaining warnings are pre-existing
  workspace-level items outside this seam.

  Updated stop-rule assessment:
  the adjacent `single-generation` background lane is now very close to
  saturation. Continue here only if the next move still removes a concrete
  duplicated owner decision; otherwise pivot to a different Rust-owned Phase 5
  seam instead of forcing more wrapper cleanup.

- 2026-05-24 02:03 +08:00 checkpoint:
  this slice pivoted back to the batch-generation read/query lane, but kept
  the stop-rule discipline from the previous checkpoint. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  showed that pushing active/status payload assembly or stream-state projection
  down into the read-context owner would blur two boundaries that are already
  reasonably separated: read-context loading vs payload/stream semantics.

  The remaining real owner split was instead at the route/query orchestration
  boundary: the batch status route still owned a two-step
  `load view context -> build status payload` composition locally, while the
  adjacent active-query and active-list paths already had dedicated query-owner
  services. The refactor therefore introduced
  `chapter_batch_generation_status_query_service.rs` as the query owner for the
  batch status read path and changed the route to delegate directly to
  `load_batch_generation_status_query()`. The shared
  `chapter_batch_generation_task_view_query_service.rs` remains the owner of
  view-context loading plus shared active/status payload projections, while the
  new status-query owner only composes that shared contract into one route-safe
  read boundary.

  This keeps the status payload shape, task-not-found mapping, and route
  transport behavior unchanged while making the batch status route structurally
  consistent with the existing active-query and active-list query lanes.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. The warnings reported during validation are pre-existing
  workspace-level items outside this seam.

  Updated stop-rule assessment:
  the batch-generation read lane still has value, but only where the remaining
  seam is a real query/workflow owner split. Do not push payload or stream
  assembly into `BatchGenerationReadContext` unless a concrete duplicated
  contract appears across multiple consumers.

- 2026-05-24 02:24 +08:00 checkpoint:
  this slice stayed in the same batch-generation read/stream lane and applied
  the same owner-boundary rule that guided the previous status-query slice. A
  focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  showed that the batch status stream route still owned one two-step
  composition locally:
  first `load_required_batch_generation_stream_state()` as the access /
  initial-state gate, then `build_batch_generation_status_stream()` as the
  polling/SSE owner. That split no longer added meaningful clarity at the
  route boundary, because the stream entrypoint already has a dedicated
  service module and the route only needs one read-side stream contract.

  The refactor therefore introduced
  `load_owned_batch_generation_status_stream()` in
  `chapter_batch_generation_status_stream_service.rs` and changed the route to
  delegate directly to that single stream owner. The stream service now owns
  `required initial state load -> stream construction` as one read-side
  streaming boundary, while `chapter_batch_generation_status_view_service.rs`
  remains the owner of loading and shaping `BatchGenerationStreamState`, and
  the existing SSE polling/event semantics stay unchanged.

  This keeps the SSE payload shape, keep-alive policy, task-not-found mapping,
  and polling behavior unchanged while making the stream route structurally
  consistent with the adjacent status-query route: both entrypoints now call a
  single service-owned read boundary instead of composing two internal owners
  directly in the route.

  Focused validation first exposed a bad test assumption in the new stream
  access regression: an empty in-memory SQLite database returns an internal
  table-missing error, not `TaskNotFound`. The regression was corrected to
  assert transparent error propagation from the owned status loader instead of
  relying on nonexistent schema setup.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  As with the previous slices, validation used an isolated target directory
  because the default `backend-rs/target/debug/incremental` path currently
  hits a local Windows file-lock / access-denied issue during incremental
  cleanup. The warnings reported during validation are pre-existing
  workspace-level items outside this seam.

  Updated stop-rule assessment:
  the batch-generation read-side lane is still worth continuing only when the
  remaining move removes a concrete route/query/stream owner split. Avoid
  inventing new read-context wrappers or moving SSE semantics out of their
  current owners unless a real duplicated contract becomes visible.

- 2026-05-24 02:49 +08:00 checkpoint:
  this slice pivoted away from the now-thinning batch read-side route lane and
  moved into the adjacent runtime-launch / dispatch contract boundary, where a
  real duplicated owner split was still visible. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`
  showed that the dispatch owner already defined the runtime execution
  contracts (`BatchGenerationExecutionInput` and
  `ResumeGenerationExecutionInput`), but `runtime_launch_service.rs` was still
  assembling those same contracts locally for batch create, batch resume, and
  single-chapter resume branches.

  The refactor therefore moved contract assembly into the dispatch owner
  through three shared builders:
  `build_batch_generation_execution_input()`,
  `build_resume_batch_generation_execution_input()`, and
  `build_resume_single_generation_execution_input()`.
  `chapter_batch_generation_runtime_launch_service.rs` now narrows to launch
  orchestration plus dispatch selection, instead of hand-assembling the same
  dispatch contracts inline. The single-chapter resume branch now also reuses
  one shared builder path instead of locally chaining the single execution
  input builder and single runtime launch-input builder inside the batch
  runtime-launch owner.

  This keeps create/resume runtime behavior, execution-config handling, and
  outward workflow payloads unchanged while making the dispatch owner the
  single source of truth for batch/single resume execution contract assembly.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  During validation, one test-only unused-import warning introduced by this
  slice was removed before final verification. The remaining warnings are
  pre-existing workspace-level items outside this seam.

  Updated stop-rule assessment:
  continue in the runtime-launch / dispatch lane only when the next move
  removes another concrete shared execution-contract owner split. Do not keep
  extracting wrappers if launch owners are no longer assembling duplicate
  contracts themselves.

- 2026-05-24 03:05 +08:00 checkpoint:
  this slice stayed in the same runtime-launch / dispatch lane and completed
  one more real ownership transfer inside the resume path. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  showed that the previous slice moved execution-contract field assembly into
  the dispatch owner, but `runtime_launch_service.rs` still owned the
  interpretation of `ResumeExecutionPlan` through a local `match` over single
  vs batch resume branches.

  The refactor therefore added
  `build_resume_generation_execution_input()` to
  `chapter_batch_generation_dispatch_service.rs` so the dispatch owner now
  owns the full `ResumeExecutionPlan -> ResumeGenerationExecutionInput`
  conversion, not just the leaf builders for the two branch-specific runtime
  contracts. `chapter_batch_generation_runtime_launch_service.rs` now narrows
  further to:
  `take prepared execution config -> ask dispatch owner for resume execution
  input -> dispatch`.

  This keeps resume runtime behavior unchanged while removing one more place
  where launch orchestration could drift from the shared execution-contract
  owner on branch interpretation or future resume contract changes.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:55 +08:00 checkpoint:
  this slice returned to the adjacent memories write-workflow lane, where the
  read side had already converged project-scoped access/internal failures onto
  one shared query-context owner, but the write side still repeated the same
  outer `ProjectAccess(LoadProjectAccessError) | Internal(String)` boundary
  separately across analyze and delete workflows. A focused audit of
  `backend-rs/src/services/memories_write_workflow_service.rs` and
  `backend-rs/src/api/memories_error_mapper.rs`
  showed that analyze-chapter only added one true extra semantic
  (`CreateTask(CreateChapterAnalysisTaskError)`), while delete-chapter carried
  no extra branch beyond the shared project-access/internal context itself.

  The refactor therefore narrowed this lane to one shared write-context owner:
  `MemoriesProjectWriteContextError` now represents the common
  project-access/internal boundary for memories write workflows,
  `DeleteChapterMemoriesWriteWorkflowError` now aliases that owner directly,
  and `AnalyzeChapterMemoriesWriteWorkflowError` now wraps it through
  `Context(MemoriesProjectWriteContextError)` while keeping `CreateTask(...)`
  as the explicit analyze-only semantic. On the route side,
  `memories_error_mapper.rs` now uses one shared
  `map_memories_project_write_context_error(...)` helper for the common
  transport mapping, while the existing create-task branch continues to reuse
  the chapter-analysis task mapper unchanged.

  This keeps analyze/delete memories payloads, Chinese
  `项目不存在或无权限` not-found detail, internal-detail passthrough, and the
  create-task bad-request mapping unchanged while removing one more duplicated
  outer write-context split across adjacent memories write lanes. Stop-rule
  for this lane: only return if another memories write workflow still repeats
  the same project-access/internal outer boundary without adding a true extra
  semantic; do not continue here for naming-only cleanup once the shared owner
  is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:51 +08:00 checkpoint:
  this slice returned to the adjacent chapter-analysis read-side lane, where
  task-status and analysis-view queries had already converged on the shared
  `ChapterAnalysisQueryContextError` owner, but quality-metrics still exposed
  the same chapter-access/internal boundary through its own
  `LoadAccessibleChapterError` alias. A focused audit of
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_read_workflow_service.rs`, and
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs`
  showed that quality-metrics did not add any route-visible semantic beyond
  that shared context boundary: chapter access failures and internal query
  failures were already transport-compatible with the existing analysis read
  lane.

  The refactor therefore narrowed this lane to one shared analysis query
  owner: `LoadChapterQualityMetricsPayloadError` now aliases
  `ChapterAnalysisQueryContextError`, the quality-metrics service now maps
  chapter lookup failures to `Chapter(...)` and query failures to
  `Internal(...)`, and the route mapper now delegates quality-metrics errors to
  the existing `map_chapter_analysis_query_context_error(...)` helper instead
  of maintaining a parallel `map_load_accessible_chapter_error(...)` path.

  This keeps chapter-quality metrics payloads, chapter not-found/access-denied
  transport, internal-detail passthrough, and the surrounding read-workflow
  signatures unchanged while removing one more duplicated outer query-context
  split from the chapter-analysis read-side group. Stop-rule for this lane:
  only return if another analysis read payload still repeats the same shared
  chapter-access/internal boundary without adding a true extra semantic; do
  not continue here for naming-only symmetry once the shared owner is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_read_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:36 +08:00 checkpoint:
  this slice returned to the adjacent route-compat request lane, where two
  request services still materialized temporary compat-holder structs only to
  acknowledge deprecated route fields and then immediately discard them without
  contributing any service-owned semantic. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  and `backend-rs/src/services/chapter_single_generation_request_service.rs`
  showed that `BatchGenerationRequestCompatFields` and
  `SingleChapterGenerationRequestCompatFields` existed solely to carry ignored
  compatibility input (`enable_mcp`, web-research/story tuning fields, and
  single-generation `enable_analysis`) into no-op consume helpers before the
  route payloads were rebuilt into the real workflow/request owners.

  The refactor therefore narrowed both lanes to direct route-payload
  destructuring. Batch generation now destructures
  `BatchGenerationCreateRouteRequest` once, explicitly ignores compat-only
  fields in place, and forwards only the owned workflow inputs that
  `BatchGenerationCreateWorkflowRequest` actually keeps. Single generation now
  destructures `SingleChapterGenerationRouteRequest` in the same way, ignoring
  the deprecated `enable_analysis` field while preserving
  `SingleChapterGenerationRequest` as the real prepare/workflow owner. This
  keeps route compatibility fields accepted at the API boundary, but removes
  two more compat-holder structs and their empty consume helpers from the
  request boundary.

  This keeps batch-generation create payload behavior, single-generation route
  payload behavior, deprecated field acceptance, normalized
  `enable_analysis/max_retries` semantics, and downstream workflow contracts
  unchanged while shrinking the request boundary to the actual owners in both
  lanes. Stop-rule for this lane: only return if another compat path still
  creates a temporary holder solely to touch ignored route fields before
  rebuilding the real owner; do not continue here for deleting route fields or
  compatibility-sensitive inputs that still need to remain accepted.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_request_compat_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:54 +08:00 checkpoint:
  this slice moved to the chapter read/query lane, where the service layer
  still maintained two parallel outer query owners for the same compatibility-
  sensitive shape and the route mapper mirrored that duplication with separate
  chapter-query and quality-trend not-found/internal branches. A focused audit
  of
  `backend-rs/src/services/chapter_query_service.rs`,
  `backend-rs/src/services/chapter_quality_query_service.rs`,
  `backend-rs/src/services/chapter_crud_read_workflow_service.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs` showed that
  `ChapterQueryPayloadError` and `LoadQualityTrendPayloadError` both exposed
  the same outer shape:
  `NotFound(<resource tag>) | Internal(String)`,
  with only the resource-specific not-found label and route detail text
  differing. The route layer then repeated the same outer `NotFound -> stable
  404 detail / Internal -> passthrough 500 detail` mapping pattern in parallel
  for chapter read endpoints and project quality-trend reads.

  The refactor therefore narrowed this lane to one shared generic owner:
  `ReadQueryPayloadError<TNotFound>` now represents the common outer chapter
  read/query boundary. `ChapterQueryPayloadError` remains as the chapter-scoped
  alias over `ChapterReadNotFound`, while `LoadQualityTrendPayloadError` is now
  a project-scoped alias over `ProjectReadNotFound`. On the route side,
  `chapters_error_mapper.rs` now uses one shared read-query mapper helper and
  keeps the chapter-versus-project not-found response text difference only as
  the injected not-found branch behavior.

  This keeps navigation/can-generate/quality-trend payload shapes, chapter
  versus project not-found detail texts, internal-detail passthrough, and the
  read-workflow signatures unchanged while removing one more duplicated outer
  contract owner split across the chapter read/query service and route layers.
  Stop-rule for this lane: only return if another compatibility-sensitive read
  query outer contract still duplicates the same generic owner pattern; do not
  continue here for naming-only cleanup once the shared outer owner and route
  helper are direct.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_read_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the runtime-launch / dispatch lane is now close to saturation. Continue here
  only if a next step would remove another concrete cross-owner interpretation
  or contract assembly decision, not if it would merely add a wrapper around
  already-shared builders.

- 2026-05-24 03:22 +08:00 checkpoint:
  this slice pivoted from the now-near-saturated runtime-launch lane back to
  the batch create task-command boundary, where one more oversized input
  contract was still visible. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that `create_batch_generation_task_plan()` still accepted the whole
  `BatchGenerationCreateWorkflowRequest` even though the task-command owner
  only consumed a strict subset of fields:
  `start_chapter_number`, `style_id`, `enable_analysis`, and `max_retries`.
  The rest of the workflow request (`count`, `target_word_count`,
  `model_override`) was either already consumed earlier in the create-prep
  path or owned elsewhere.

  The refactor therefore narrowed `create_batch_generation_task_plan()` and
  its internal active-model builder to accept only the explicit fields the
  task-command owner actually uses, and the write-workflow owner now passes
  those values directly instead of forwarding the whole request object. This
  mirrors the earlier single-generation request / plan narrowing work and
  removes another fake dependency where unrelated workflow-request fields could
  leak into the batch create task-command boundary.

  This keeps create behavior, persisted task fields, snapshot creation, and
  outward create workflow payloads unchanged while making the task-command
  boundary more explicit about what it owns.

  Validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the batch create lane only if another downstream owner still
  accepts oversized workflow contracts it does not truly consume. Do not keep
  shrinking signatures once only obvious route/workflow data plumbing remains.

- 2026-05-24 15:05 +08:00 checkpoint:
  this slice stayed in the batch create lane and completed one more real
  contract narrowing step after the earlier workflow-request reduction. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that the create-preparation owner still handed downstream services a
  full `Vec<chapter::Model>` even though the task-command owner and create
  response adapter only consumed the same narrow projection:
  `id`, `chapter_number`, and `title`.

  The refactor therefore introduced an explicit
  `BatchGenerationCreateChapterTarget` projection at the prepare boundary, and
  `PreparedBatchGenerationCreateRequest` now carries
  `Vec<BatchGenerationCreateChapterTarget>` instead of full chapter models.
  The task-command owner continues to derive persisted `chapter_ids` and task
  counts from that narrowed contract, while the create response adapter now
  also reads the same explicit projection for the outward
  `chapters_to_generate` payload.

  This keeps create behavior, task persistence, and outward response payloads
  unchanged while removing one more fake dependency on full ORM chapter models
  in the batch create lane. It also makes the owner boundary clearer about the
  fact that content, summary, status, and other chapter fields are not part of
  this create contract.

  During seam selection, a parallel audit of the single-generation workflow /
  stream / background lane showed that background and stream flows currently
  share the prepared execution contract, but they do not yet expose a stronger
  duplicate-owner problem than the batch create lane. Background launch still
  legitimately owns task persistence and create response concerns that the
  stream path does not share, so that lane was left unchanged for now.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  One test-only compile error caused by an outdated local fixture
  (`build_chapter(...)`) was fixed before the final validation pass. The
  remaining warnings are pre-existing workspace-level items outside this seam.

  Updated stop-rule assessment:
  continue in the batch create lane only if another owner still consumes more
  than the explicit chapter target projection or reinterprets the same create
  semantics independently. Do not force further narrowing if the remaining
  data flow is only straightforward workflow plumbing. For the single-chapter
  lane, continue only when a next move removes a concrete duplicated contract
  owner rather than merely aligning background and stream wrappers.

- 2026-05-24 15:28 +08:00 checkpoint:
  this slice stayed on the same batch create path and narrowed the create
  output contract after the earlier chapter-target projection work. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that `create_batch_generation_task_plan()` still returned a
  `BatchGenerationCreateOutcome` carrying the full persisted
  `created_task` model even though the downstream write-workflow owner only
  consumed:
  `plan.created_task_id` for runtime launch and
  create-response metadata derivable from `created_task_id` plus the prepared
  chapter targets.

  The refactor therefore removed the extra outcome wrapper and narrowed the
  task-command boundary to return `BatchGenerationCreatePlan` directly. The
  write-workflow owner now builds the outward create response from
  `plan.created_task_id` and the already-prepared chapter targets before
  launch, and the create response adapter now accepts the explicit `batch_id`
  instead of the entire task model. Estimated time is derived from the chapter
  target count, which is the same create-time source previously mirrored in
  `created_task.total_chapters`.

  This keeps create behavior, runtime launch, response payload shape, and task
  snapshot persistence unchanged while removing one more fake dependency on the
  persisted task model in the create write-workflow lane.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the batch create lane only if another downstream owner still
  depends on persisted task fields that can be replaced by explicit create
  plan/output contracts. If the next move would only restyle straight-line
  workflow plumbing, stop and pivot to another lane.

- 2026-05-24 16:02 +08:00 checkpoint:
  this slice pivoted from the now-near-saturated batch create lane back to the
  single-chapter background workflow lane, where one more same-shape oversized
  output contract was still present. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that `create_single_generation_background_task_plan()` still returned
  a `SingleGenerationBackgroundCreatePlan` carrying the full persisted
  `created_task` model even though the background workflow owner only consumed:
  `created_task_id` for runtime launch and a small fixed response contract for
  `task_id/chapter_id/status/estimated_time_minutes`.

  The refactor therefore narrowed the single-generation background task-command
  output to explicit response-ready fields:
  `created_task_id`, `chapter_id`, `status`, and
  `estimated_time_minutes`. The background create response adapter now accepts
  those explicit values directly instead of the full task model, and the
  workflow owner builds the response from the narrowed plan before dispatch.

  This keeps single-chapter background task creation behavior, launch
  semantics, and outward response payload shape unchanged while removing one
  more fake dependency on the persisted task model in the single-generation
  background lane.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  One test-only unused import introduced during the slice was removed before
  the final validation pass. The remaining warnings are pre-existing
  workspace-level items outside this seam.

  Updated stop-rule assessment:
  batch create is now a low-signal lane unless another owner still consumes
  persisted task models instead of explicit create contracts. For the
  single-chapter lane, continue only when the next move removes another
  concrete contract owner split rather than attempting to unify background and
  stream wrappers for symmetry alone.

- 2026-05-24 16:24 +08:00 checkpoint:
  this slice stayed in the single-chapter background lane and completed one
  more ownership cleanup inside the create response path. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that the previous slice narrowed the background create plan away from
  the persisted task model, but the task-command owner still carried response
  semantics that it did not truly own:
  `chapter_id`, `status`, and `estimated_time_minutes`.

  Those fields were only consumed by the outward create response builder.
  Runtime launch still only needed `created_task_id`, while the response layer
  already knew the fixed response status (`pending`), fixed create-time
  estimate source (single chapter -> 2 minutes through the shared estimator),
  and the chapter identifier from the already-prepared chapter target in the
  workflow owner.

  The refactor therefore narrowed `SingleGenerationBackgroundCreatePlan` down
  to `created_task_id` only. The background workflow now supplies
  `prepared.chapter_target.chapter_id` directly to the response adapter, and
  the adapter again owns the fixed single-background response semantics for
  `status` and `estimated_time_minutes`.

  This keeps single-chapter background task creation behavior, launch
  semantics, and outward response payload shape unchanged while removing one
  more fake dependency where task-command output was carrying response-only
  metadata.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the single-chapter background lane only if another owner still
  carries response-only semantics or duplicated runtime-contract interpretation.
  Do not keep refactoring this lane once only straight-line workflow plumbing
  remains or if the next move is motivated only by background/stream symmetry.

- 2026-05-24 16:43 +08:00 checkpoint:
  this slice stayed in the single-chapter runtime lane and completed one more
  concrete owner transfer around runtime-input interpretation. A focused audit
  of
  `backend-rs/src/services/chapter_single_generation_execution_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the execution owner already defined the resolved runtime input
  contract
  (`ResolvedSingleChapterGenerationRuntimeInput`), but both the stream launch
  owner and the runtime-state owner were still deciding when to convert
  `SingleGenerationRuntimeLaunchInput` into that resolved execution contract.

  The refactor therefore moved the conversion boundary fully into
  `execute_single_chapter_generation()`. The execution owner now accepts the
  original `SingleGenerationRuntimeLaunchInput` and resolves it internally,
  while:
  - stream launch only reads the one field it truly needs for SSE progress
    reporting (`launch.execution_input.target_word_count`)
  - runtime-state only reads the one field it truly needs for task-state
    preparation (`runtime_input.execution_input.chapter_id`)

  This keeps single-chapter stream behavior, runtime checkpoint flow, and
  chapter generation execution unchanged while removing one more duplicate
  runtime-contract interpretation decision outside the execution owner.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_execution_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the single-chapter runtime lane only if another owner still
  converts or branches on the same runtime launch contract independently.
  Do not keep refactoring this lane once remaining differences are merely due
  to each owner reading the specific field it legitimately needs.

- 2026-05-24 17:22 +08:00 checkpoint:
  this slice pivoted from the now-near-saturated single-chapter runtime lane
  to the batch resume write lane, where one more concrete persisted-model
  dependency was still present. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_task_payload_base_service.rs`
  showed that `prepare_batch_generation_resume()` still returned the full
  `updated_task` ORM model even though the downstream write-workflow owner only
  needed a strict response projection:
  `batch_id`, `project_id`, `status`, `current_chapter_id`,
  `total_chapters`, `completed_chapters`, `created_at`, and task kind for the
  shared response semantics.

  The refactor therefore introduced an explicit
  `ResumeBatchGenerationResponseState` projection at the task-command
  boundary, and the write workflow now consumes that explicit response state
  instead of the whole persisted task model. To avoid duplicating the response
  shape logic, the shared status semantics were also split into reusable
  `batch_generation_task_type()` and `batch_generation_stage_code()` helpers,
  and the shared payload base now exposes
  `build_batch_generation_task_runtime_payload_from_parts()` so the resume
  adapter can keep the exact existing response envelope while no longer
  depending on `batch_generation_task::Model`.

  This keeps resume behavior, runtime launch semantics, and outward response
  payload shape unchanged while removing one more fake dependency on the
  persisted task model in the batch resume lane. The remaining resume data
  flow is now mostly straight-line workflow/runtime plumbing, so this lane
  should only continue if another owner still independently interprets or
  assembles the same resume contract.

- 2026-05-24 18:18 +08:00 checkpoint:
  this slice continued the same batch resume lane and closed the adjacent
  input-side persisted-model dependency that remained after the previous
  response-state narrowing. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that `prepare_batch_generation_resume()` no longer returned the full
  ORM model outward, but the workflow owner still loaded
  `batch_generation_task::Model` and passed that whole model through the
  resume-command, resume-reset, and resumed-checkpoint path even though those
  owners only needed a strict command projection.

  The refactor therefore introduced
  `ResumeBatchGenerationCommandState` as the shared input projection for the
  resume lane. The write workflow now converts the owned task into that
  command state immediately after the access/query boundary; the resume
  command owner, shared resume semantics owner, resumed checkpoint builder,
  and runtime reset path all consume that explicit projection instead of the
  full ORM model. To keep single-vs-batch semantics centralized, shared task
  kind resolution was also split into
  `batch_generation_task_kind(chapter_count, chapter_ids)` inside
  `chapter_batch_generation_status_semantics_service.rs`, and the runtime
  reset now performs its resume-state update through a primary-key
  `ActiveModel` patch plus the already-shared mutation plan instead of
  cloning the full task row.

  This keeps resume eligibility checks, manual-review blocking, malformed
  single-task fallback, pending checkpoint shape, single-vs-batch chapter
  pointer reset behavior, runtime launch semantics, and outward resume
  payloads unchanged while removing one more fake full-model dependency from
  the batch resume lane. At this point the remaining resume lane surface is
  mostly straight-line orchestration; continue here only if another owner is
  still duplicating a compatibility-sensitive resume contract rather than
  simply reading the fields it legitimately needs.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 19:08 +08:00 checkpoint:
  this slice pivoted from the now-near-saturated batch resume lane back to the
  single-generation background write lane, where one more parallel workflow
  contract was still being maintained for no behavior gain. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the background write path still carried a dedicated
  `CreateSingleGenerationBackgroundWorkflowError` even though that enum was
  only a 1:1 restatement of
  `PrepareSingleChapterGenerationRequestError` plus the same internal-string
  forwarding. The route mapper then had to maintain two parallel
  single-generation background error contracts:
  one for stream prepare and one for background prepare.

  The refactor therefore removed
  `CreateSingleGenerationBackgroundWorkflowError` entirely and made
  `start_owned_single_generation_background_workflow()` /
  `start_single_generation_background_write_workflow()` return the shared
  `PrepareSingleChapterGenerationRequestError` directly. The background
  workflow now forwards prepare failures unchanged and wraps task-plan
  insertion failures into the existing `Internal(String)` variant instead of
  introducing a second same-shape workflow error owner. The route error mapper
  now consumes that shared prepare error for both stream and background
  single-generation entrypoints.

  This keeps HTTP status mapping, error detail payloads, single-chapter
  background request handling, task creation, and runtime dispatch behavior
  unchanged while removing one more duplicated write-lane compatibility
  contract. The remaining single-generation background lane should only
  continue if another owner still restates a compatibility-sensitive request /
  response / error contract, not if the next move would merely collapse thin
  dispatch wrappers for symmetry.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 19:31 +08:00 checkpoint:
  this slice stayed adjacent to the same single-generation background write
  lane, but moved one step outward to the route mapper boundary where one
  more duplicated compatibility contract was still being maintained. A focused
  audit of
  `backend-rs/src/api/chapter_batch_generation.rs`
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that after the previous slice both the stream and background
  single-generation entrypoints already returned the same shared
  `PrepareSingleChapterGenerationRequestError`, but the route layer still kept
  two separate mapping helpers:
  `map_single_chapter_generation_request_error(...)` and
  `map_create_single_generation_background_workflow_error(...)`. Their bodies
  were now identical, so the route boundary was still carrying one more fake
  same-shape compatibility branch.

  The refactor therefore removed
  `map_create_single_generation_background_workflow_error(...)` entirely and
  switched the background route to reuse
  `map_single_chapter_generation_request_error(...)`, which already owned the
  shared transport mapping for
  `PrepareSingleChapterGenerationRequestError`.

  This keeps HTTP status codes, `detail` payloads, single-generation route
  behavior, and stream/background compatibility unchanged while removing one
  more duplicate transport contract from the chapter-generation route group.
  At this point the single-generation background/stream error-mapping lane is
  close to saturation; continue only if another owner still restates the same
  prepare/request contract, not if the next move would just collapse a thin
  wrapper for naming symmetry.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-27 19:19 +08:00 checkpoint:
  this slice returned to `backend-rs/src/api/chapter_batch_generation.rs` and
  removed the remaining route-local one-line transfer wrappers for batch create
  and single-chapter generation entrypoints. The route handler now constructs
  `BatchGenerationCreateWorkflowRequest` and the single-chapter prepare request
  inline at the call site, instead of routing through private
  `into_workflow_request()` / `into_request()` helpers that only forwarded the
  same values once.

  This keeps the HTTP payloads, workflow calls, and SSE/background behavior
  unchanged while trimming one more transport-only ownership split from the
  batch route seam. The route file is now thinner and easier to audit: it keeps
  request extraction, delegation, and response mapping, but no longer carries
  a private conversion wrapper layer for the two hot generation entrypoints.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"` and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The remaining warnings are pre-existing workspace-level items outside this
  seam.

  Updated stop-rule assessment:
  continue in `chapter_batch_generation.rs` only if another route-local helper
  still just forwards data once and does not own transport policy, error
  mapping, or request-shape compatibility. If the next move is only cosmetic
  route flattening, pivot back to a service-owned seam instead.

- 2026-05-27 19:40 +08:00 checkpoint:
  this slice stayed in
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  and removed one more fake owner from the batch create workflow lane:
  `PreparedBatchGenerationCreateRequest::total_chapters()` was just a length
  projection helper with a single caller, so the workflow now reads
  `chapters_to_generate.len()` directly at the persistence boundary.

  This keeps task persistence, snapshot creation, runtime launch, and response
  payload behavior unchanged while making the prepared create owner slightly
  thinner. The route tests that had still referenced the removed route-local
  transfer helpers were also updated to construct the explicit request values
  directly, so the route boundary now matches the new inline conversion style.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the batch create workflow lane only if another helper still only
  proxies a single caller without owning a real compatibility contract. If the
  next move is just another length / projection inline, stop and pivot to a
  different owner boundary.

- 2026-05-27 19:44 +08:00 checkpoint:
  this slice stayed in the same batch create workflow lane and removed the
  last small standalone payload helper from the response builder:
  `BatchGenerationCreateChapterTarget::into_payload()` was only used by
  `create_response_payload()`, so the response helper now formats chapter
  payload objects inline at the call site instead of bouncing through a
  one-method owner shell.

  This keeps the response shape, chapter ordering, estimated-time field, and
  task launch behavior unchanged while making the response builder the single
  owner of its chapter payload structure. The batch workflow tests were updated
  to assert against the new inline payload assembly path, so the contract stays
  covered at the service boundary instead of through a removed helper.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  this specific response-payload lane is now close to saturation. Only continue
  here if another helper still owns the same response object shape and can be
  removed without changing runtime or error semantics. If the next move is
  another pure formatting inline, pivot to a different service boundary.

- 2026-05-27 19:51 +08:00 checkpoint:
  this slice stayed in the same batch create workflow lane and removed the
  final dedicated response helper too: `create_response_payload()` was only
  used once, so `start_owned_batch_generation_write_workflow()` now owns the
  batch create response JSON assembly directly after preparing execution
  config.

  This keeps the HTTP response shape, chapter payload ordering, estimated-time
  calculation, and launch behavior unchanged while making the workflow owner
  the single boundary for create-time response assembly. The service tests were
  updated to assert the inline response JSON path directly, so the new owner
  shape is still covered without an extra wrapper layer.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_write_workflow_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the batch create workflow lane is now effectively saturated. Do not keep
  shaving pure response formatting here unless a future audit finds another
  compatibility-sensitive owner split. The next useful move should come from a
  different adjacent service boundary.

- 2026-05-27 19:54 +08:00 checkpoint:
  this slice switched to the adjacent batch query lane and removed one more
  single-consumer wrapper from `backend-rs/src/services/
  chapter_batch_generation_task_view_query_service.rs`:
  `load_active_project_batch_generation_task_view_context()` only wrapped a
  `task -> optional read-context` conversion for one caller, so
  `load_active_batch_generation_query()` now owns that lookup and conversion
  directly.

  This keeps the active-query payload shape, access-check behavior, and list /
  status payload semantics unchanged while shrinking the query boundary to the
  actual transport-facing owner. The query service tests still cover the same
  active-task response and list payload shape, so the behavior remains locked
  down after the helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  continue in the batch query lane only if another helper still only wraps a
  single lookup or payload conversion and does not own a distinct compatibility
  contract. If the next move is just another trivial projection, pivot to the
  status-stream or resume seam instead.

- 2026-05-27 20:00 +08:00 checkpoint:
  this slice switched to the adjacent resume lane and removed the last
  test-only projection helper from
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`:
  `ResumeBatchGenerationResponseState::from_command_state()` only existed to
  mirror a direct `ResumeBatchGenerationCommandState -> response state`
  projection for tests, so the tests now construct the response state inline
  from the real command-state fields instead of routing through a file-local
  helper shell.

  This keeps the resume response shape, reset semantics, manual-review guard,
  and runtime dispatch behavior unchanged while shrinking one more fake owner
  from the resume lane. The focused resume tests still cover both the shared
  projection path and the reset paths, so the contract remains locked down at
  the actual response boundary.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_task_command_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the resume lane still has some shape ownership left, but this particular
  test-only projection is gone. Continue only if another helper still merely
  mirrors a single projection or response field bundle without owning a real
  contract. Otherwise pivot back to the status-stream lane.

- 2026-05-27 20:02 +08:00 checkpoint:
  this slice switched to the adjacent status-stream lane and removed the thin
  JSON event sender helper from
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`:
  `send_json_event()` only wrapped `tx.send(Ok(Event::default().data(...)))`
  for one caller family, so the stream owner now emits JSON events inline at
  the polling and timeout sites instead of routing through a one-method helper.

  This keeps the SSE event shape, poll resolution, timeout behavior, and
  stream cursor semantics unchanged while making the status-stream owner the
  direct boundary for event emission. Focused stream tests still cover the
  pending/close/context conversion cases, so the stream contract remains
  protected after the helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the status-stream lane is thinner now, and the remaining helpers are more
  clearly state/decision owners. Continue only if another helper still acts as
  a pure event wrapper or trivial conversion shell; otherwise pivot to a
  different adjacent boundary.

- 2026-05-27 20:10 +08:00 checkpoint:
  this slice returned to the adjacent batch query lane and removed one more
  single-consumer wrapper from
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`:
  `load_active_user_batch_generation_task_view_contexts()` only wrapped
  `query -> loop over tasks -> read-context assembly` for the list-view owner,
  so `load_active_user_batch_generation_task_list_view()` now owns that query
  and context loading directly.

  This keeps the active-task list payload shape, ordering, and status payload
  semantics unchanged while shrinking the list-view boundary to its actual
  transport-facing owner. Focused query tests still cover the list payload and
  active-query response shape, so the behavior remains locked down after the
  helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the batch query lane is thinner again; continue only if another helper still
  acts as a single-consumer lookup/payload wrapper without owning a distinct
  compatibility contract. If not, pivot back to resume or another adjacent
  service boundary.

- 2026-05-27 20:18 +08:00 checkpoint:
  this slice stayed in the same batch query lane and finished one more adjacent
  route/service owner merge around the status view path. The service helper
  `load_owned_batch_generation_status_task_view()` only wrapped
  `load_required_owned_batch_generation_read_context(...) ->
  into_status_task_payload()` for the route owner, so it has been removed.
  `backend-rs/src/api/chapter_batch_generation.rs` now consumes the required
  read-context directly and builds the status payload from that owner boundary.

  This keeps the status-route payload shape, not-found/internal error mapping,
  and shared read-context semantics unchanged while removing one more
  single-consumer lookup/payload wrapper from the batch status query lane.
  Focused query tests plus the broader chapter-batch-generation API tests still
  cover the same active/status payload behavior after the helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the batch query/status lane is thinner again; only continue here if another
  helper still just forwards a required read-context or payload conversion once
  without owning a distinct compatibility contract. Otherwise pivot back to
  resume or another adjacent boundary.

- 2026-05-27 20:28 +08:00 checkpoint:
  this slice returned to the adjacent batch query lane and removed the last
  remaining query-builder helper from
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`:
  `build_active_batch_generation_task_query()` only constructed the same
  active-task query used by the list-view and active-query owners, so both
  call sites now build the SeaORM query inline and keep the owner boundary
  local to the actual payload builders.

  This keeps the active-task list response, active-query response, ordering,
  and access-check semantics unchanged while removing one more single-purpose
  query wrapper from the batch query lane. Focused query tests plus the
  broader chapter-batch-generation API tests still cover the same list and
  active payload behavior after the helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_task_view_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the batch query lane is close to saturation. Only continue here if another
  helper still exists purely to build a query for a single caller and does not
  own a distinct compatibility contract. Otherwise pivot to resume or another
  adjacent service boundary.

- 2026-05-27 20:35 +08:00 checkpoint:
  this slice returned to the adjacent status-stream lane and removed the final
  stream-poll state-resolution helper from
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`:
  `BatchGenerationStreamPollResolution::from_loaded_state()` only wrapped the
  same pending-state / loaded-state branch decision that the stream loop had to
  make once, so the stream owner now resolves that branch inline at the call
  site.

  This keeps the SSE event shape, not-found fallback, timeout behavior, and
  cursor progression semantics unchanged while removing one more single-purpose
  state wrapper from the status-stream lane. Focused stream tests still cover
  the pending initial state, missing-state close path, and read-context to
  stream-state conversion after the helper removal.

  Validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_status_stream_service -- --nocapture`, and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

  Updated stop-rule assessment:
  the status-stream lane is now significantly thinner. Continue only if another
  helper still acts as a pure branch wrapper or event-state projection shell;
  otherwise pivot to resume or another adjacent boundary.

- 2026-05-24 13:50 +08:00 checkpoint:
  this slice stayed adjacent to the single-generation request lane and closed
  one more fake route/request owner split that no longer carried any
  compatibility value. A focused audit of
  `backend-rs/src/services/chapter_single_generation_request_service.rs`,
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation.rs`
  showed that `SingleChapterGenerationRequestRouteFields` and
  `build_single_chapter_generation_request_from_route_fields(...)` were only
  used inside the request service itself. The real externally relevant
  boundary was already just the route payload plus the compat-only
  `enable_analysis` field consumption; the intermediate route-fields wrapper
  no longer owned any distinct normalization or shared contract.

  The refactor therefore removed the internal
  `SingleChapterGenerationRequestRouteFields` wrapper and collapsed
  `build_single_chapter_generation_request_from_route_payload(...)` into the
  single owner of route-payload -> request conversion. The compat-only field
  consumption boundary remains explicit through
  `SingleChapterGenerationRequestCompatFields`, so the route-compatible input
  shape and ignored legacy field behavior remain unchanged while one more
  no-op same-shape request wrapper disappears.

  This keeps single-generation HTTP payload shape, background/stream request
  preparation, compat-only field consumption, and downstream prepare/runtime
  behavior unchanged while removing one more duplicate request-lane contract
  owner. At this point the single-generation request lane is close to
  saturation; continue here only if another owner still restates a
  compatibility-sensitive request/response/error contract, not if the next
  move would only collapse a thin internal wrapper.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:05 +08:00 checkpoint:
  this slice applied the same request-lane stop-rule one step earlier in the
  batch create compatibility boundary, where one more route/request wrapper
  had become a file-local no-op owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  and
  `backend-rs/src/api/chapter_batch_generation.rs`
  showed that `BatchGenerationCreateRouteFields` and
  `build_batch_generation_create_workflow_request_from_route_fields(...)`
  were only used inside the request-compat service itself. The actual
  externally relevant owners were already:
  - route payload parsing
  - compat-only legacy field consumption
  - normalized workflow-request defaults (`enable_analysis`, `max_retries`)

  The refactor therefore removed the internal
  `BatchGenerationCreateRouteFields` wrapper and collapsed
  `build_batch_generation_create_workflow_request_from_route_payload(...)`
  into the single route-payload -> workflow-request owner. The compat-only
  field consumption boundary remains explicit through
  `BatchGenerationRequestCompatFields`, and the normalized default behavior
  still flows through `build_batch_generation_create_workflow_request(...)`,
  so the route-compatible input shape and create-workflow semantics remain
  unchanged while one more same-shape wrapper disappears.

  This keeps batch create HTTP payload shape, ignored compatibility fields,
  default `enable_analysis` / `max_retries` normalization, route delegation,
  and downstream write-workflow/runtime behavior unchanged while removing one
  more duplicate request-lane contract owner. The batch create request compat
  lane is now also close to saturation; continue here only if another owner
  still restates a compatibility-sensitive request/response/error contract,
  not if the next move would only collapse a thin file-local wrapper.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_request_compat_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:01 +08:00 checkpoint:
  this slice pivoted to the regeneration stream request lane, where the full
  regeneration request contract was still being interpreted by two adjacent
  owners. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`,
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`
  showed that production routing already used the typed entrypoint
  `build_full_chapter_regeneration_stream_request_from_route_payload_typed(...)`,
  but the request service still kept a parallel raw-`Value` builder plus its
  own field-reading helpers for the same full-regeneration contract. That
  meant `selected_suggestion_indices`, `focus_areas`, and
  `preserve_elements` semantics still had two local owner paths even though
  only one route boundary was live in production.

  The first refactor step therefore introduced one shared
  `FullChapterRegenerationStreamRequestFields` owner and moved the common
  field-to-request assembly into a single helper. Both the typed route entry
  and the remaining raw test path now went through the same
  `preserve_elements` / string-list / numeric-string-list interpretation
  contract instead of each maintaining the same parsing shape independently.

  A second focused reference audit then confirmed that the raw
  `build_full_chapter_regeneration_stream_request_from_route_payload(...)`
  path was no longer used by production code and only remained in tests.
  The follow-up refactor therefore removed that raw builder and the helper
  functions that only existed to support it, then updated the request-service
  and prepare-service tests to construct typed
  `FullChapterRegenerationStreamRouteRequest` payloads directly. This leaves
  the full regeneration request contract with one production owner: the typed
  route request boundary.

  This keeps full regeneration HTTP payload compatibility, filtered numeric
  suggestion handling, filtered string-list handling, `preserve_elements`
  defaults, regeneration prompt content, and downstream prepare/workflow
  behavior unchanged while removing one more two-path request contract from
  the regeneration lane. At this point the full regeneration request lane is
  close to saturation; continue here only if another owner still restates a
  compatibility-sensitive request/response/error contract, not if the next
  move would merely reformat already-single-owner prompt preparation.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:06 +08:00 checkpoint:
  this slice stayed adjacent to the regeneration stream lane but moved from
  full-regeneration request parsing into the partial-regeneration prepare
  boundary, where one more request contract was still being re-expanded after
  a request owner already existed. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`,
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that `PartialRegenerationStreamWorkflowRequest` already owned the
  route-to-workflow request shape, but
  `create_partial_regeneration_stream_workflow(...)` still unpacked that
  object into 10+ scalar arguments before handing control to
  `prepare_partial_regeneration_stream(...)`. The prepare owner was therefore
  still indirectly depending on a re-expanded parameter list instead of the
  request contract object itself.

  The refactor therefore narrowed the prepare boundary so
  `prepare_partial_regeneration_stream(...)` now consumes
  `&PartialRegenerationStreamWorkflowRequest` directly. The workflow owner no
  longer reinterprets `selected_text`, range, context, length mode, style,
  or web-research flags as a flattened argument list; it now forwards the
  request object once and only keeps the truly local fields it still needs for
  stream result assembly (`start_position` / `end_position`).

  This keeps partial regeneration HTTP payload shape, context-char defaulting,
  length-mode semantics, style lookup, web-research note assembly, range
  validation, prompt content, and route error mapping unchanged while removing
  one more repeated request-contract interpretation from the regeneration
  write/prepare lane. The partial regeneration request lane is now closer to a
  single-owner shape; continue here only if another owner still restates the
  same compatibility-sensitive request/response/error contract, not if the
  next move would only rename already-direct field forwarding.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:13 +08:00 checkpoint:
  this slice returned to the batch create write-workflow lane, where one more
  request contract was still being decomposed into repeated scalar argument
  groups after a workflow request owner already existed. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  showed that `BatchGenerationCreateWorkflowRequest` already owned the route
  request contract, but the write-workflow owner still unpacked that object
  twice:
  - once into `start_chapter_number` / `count` / `target_word_count` for
    `prepare_batch_generation_create_request(...)`
  - again into `start_chapter_number` / `style_id` / `enable_analysis` /
    `max_retries` for `create_batch_generation_task_plan(...)`

  The refactor therefore narrowed both downstream boundaries so
  `prepare_batch_generation_create_request(...)` and
  `create_batch_generation_task_plan(...)` now consume
  `&BatchGenerationCreateWorkflowRequest` directly. The write-workflow owner
  no longer reinterprets the same batch create request contract into parallel
  scalar groups before prepare/task-plan execution; it now forwards the owned
  request object once and only keeps the fields it truly owns locally
  (`project_id`, `user_id`, and later execution-config preparation).

  This keeps batch create HTTP payload shape, chapter-range validation,
  normalized target-word-count semantics, task persistence shape, route error
  mapping, and runtime launch behavior unchanged while removing one more
  repeated request-contract interpretation from the batch create write lane.
  The batch create request/prepare/task-plan lane is now close to saturation;
  continue here only if another owner still restates a compatibility-sensitive
  request/response/error contract, not if the next move would only rename
  already-direct request forwarding.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:20 +08:00 checkpoint:
  this slice returned to the `projects` consistency write-workflow lane, where
  three sibling workflows still maintained the same error contract shape in
  parallel and the route layer mirrored that duplication with three identical
  HTTP mappers. A focused audit of
  `backend-rs/src/services/project_consistency_write_workflow_service.rs`
  and `backend-rs/src/api/projects.rs` showed that
  `fix_project_organizations_write_workflow(...)`,
  `fix_project_member_counts_write_workflow(...)`, and
  `check_project_consistency_write_workflow(...)` all exposed the same
  two-branch error contract:
  `Context(LoadProjectConsistencyContextError) | Internal(String)`.
  The route layer then repeated the same transport mapping three times even
  though the branch semantics and HTTP behavior were identical.

  The refactor therefore narrowed this lane to a single shared owner:
  `ProjectConsistencyWriteWorkflowError` now represents the write-workflow
  error boundary for all three project-consistency actions, and
  `map_project_consistency_write_workflow_error(...)` now owns the matching
  route-level HTTP translation once. The workflow/service layer no longer
  maintains three parallel enum owners for the same compatibility-sensitive
  contract, and the `projects` route no longer hand-maintains three copies of
  the same mapper logic.

  This keeps `Project not found` transport behavior, internal-detail passthrough,
  `auto_fix` semantics, and all success payload shapes unchanged while removing
  one more repeated error-contract owner split across the service and route
  boundaries. Stop-rule for this lane: only return if a new
  compatibility-sensitive contract duplication is discovered; do not continue
  here for naming-only cleanup or already-direct shared-owner forwarding.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" project_consistency_write_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" projects -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:25 +08:00 checkpoint:
  this slice moved to the chapter-analysis draft route lane, where the service
  layer had already converged on shared owned-draft outer error owners but the
  route error mapper still maintained four parallel outer matches for the same
  contract boundary. A focused audit of
  `backend-rs/src/services/chapter_analysis_draft_service.rs`,
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs`,
  and `backend-rs/src/api/chapter_analysis_routes.rs` showed that:
  - auto-revision load/apply already share the same outer owner via
    `OwnedAutoRevisionDraftPayloadError`
  - candidate load/apply already share the same outer owner via
    `OwnedCandidateDraftPayloadError`
  - but the route layer still repeated the same outer
    `ChapterNotFoundOrAccessDenied | Draft(...) | Internal(...)` mapping shape
    across four public mapper entrypoints

  The refactor therefore narrowed only the outer transport-owner seam:
  `chapter_analysis_draft_error_mapper.rs` now uses one shared helper for the
  auto-revision outer owner and one shared helper for the candidate outer
  owner, while keeping the existing inner load/apply-specific draft mapping
  functions unchanged. This means the route API surface, detail texts,
  selection-mode-sensitive not-found behavior, stale/preview/content failure
  semantics, and status-code behavior all remain unchanged, but the outer
  owned-draft contract is no longer reinterpreted four times at the route
  layer.

  Stop-rule for this lane: only return if another compatibility-sensitive
  contract owner is still duplicated across service and route boundaries. Do
  not keep iterating here for naming-only symmetry once the outer owned-draft
  boundary is direct.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:29 +08:00 checkpoint:
  this slice returned to the regeneration stream workflow lane, where the full
  and partial stream workflows still maintained the same outer transport-facing
  error owner twice even though the only real difference was the inner prepare
  error type. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  `backend-rs/src/api/chapters_error_mapper.rs`, and
  `backend-rs/src/api/chapter_regeneration_routes.rs` showed that both
  `CreateChapterRegenerationStreamWorkflowError` and
  `CreatePartialRegenerationStreamWorkflowError` used the same outer shape:
  `Chapter(LoadAccessibleChapterError) | Prepare(...)`.
  The route layer then repeated the same outer `Chapter -> access mapper /
  Prepare -> inner mapper` match twice, with only the prepare-mapper target
  changing.

  The refactor therefore narrowed this lane to one shared outer owner:
  `CreateRegenerationStreamWorkflowError<TPrepareError>` now represents the
  shared regeneration stream workflow boundary, while
  `CreateChapterRegenerationStreamWorkflowError` and
  `CreatePartialRegenerationStreamWorkflowError` remain as type aliases for the
  full and partial prepare error variants. On the route side,
  `chapters_error_mapper.rs` now uses one shared outer workflow mapper helper
  and keeps only the prepare-specific mapping functions separate.

  This keeps full/partial regeneration route payloads, chapter-access failures,
  invalid-config behavior, partial input/style validation, and SSE stream entry
  semantics unchanged while removing one more duplicated outer contract owner
  split across the workflow and route boundaries. Stop-rule for this lane:
  return only if another compatibility-sensitive outer contract is still
  duplicated across full/partial regeneration owners; do not keep iterating for
  naming-only symmetry once the outer owner is direct and typed.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:32 +08:00 checkpoint:
  this slice moved to the memories query lane, where several project-scoped
  read/query endpoints still maintained the same outer query error owner in
  parallel and the route mapper mirrored that duplication with multiple
  identical branches. A focused audit of
  `backend-rs/src/services/memories_query_service.rs`,
  `backend-rs/src/api/memories_error_mapper.rs`, and
  `backend-rs/src/api/memories.rs` showed that:
  - `LoadProjectMemoriesPayloadError`
  - `SearchProjectMemoriesPayloadError`
  - `LoadUnresolvedForeshadowsPayloadError`
  - `LoadMemoryStatsPayloadError`
  all exposed the same outer shape:
  `ProjectAccess(LoadProjectAccessError) | Internal(String)`.
  The route mapper then repeated the same transport translation four times,
  with `项目不存在或无权限` and internal-detail passthrough behavior staying
  identical in every branch.

  The refactor therefore narrowed this lane to one shared query owner:
  `OwnedProjectMemoriesQueryError` now represents the shared outer error
  contract for those four memories read/query paths, while the existing public
  error names remain as type aliases. On the route side,
  `memories_error_mapper.rs` now uses one shared mapper helper for that outer
  query boundary and keeps only the chapter-analysis query branch separate,
  because it still owns the extra `AnalysisNotFound` semantic.

  This keeps memories list/search/stats/foreshadow payload shapes, project
  access behavior, Chinese not-found detail text, and internal-detail
  passthrough unchanged while removing one more duplicated outer contract owner
  split across the service and route layers. Stop-rule for this lane: only
  return if another compatibility-sensitive outer query contract remains
  duplicated. Do not continue here for naming-only cleanup once the shared
  owner is direct.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_query_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" memories -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:37 +08:00 checkpoint:
  this slice returned to the chapter-analysis draft lane, not for more route
  cleanup, but because one more duplicated outer contract owner still remained
  in the service layer itself. A focused audit of
  `backend-rs/src/services/chapter_analysis_draft_service.rs`,
  `backend-rs/src/services/chapter_analysis_draft_workflow_service.rs`, and
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` showed that the
  previous route-level outer mapper consolidation had reduced transport
  duplication, but the service layer still maintained two parallel owned-draft
  outer error enums:
  `OwnedAutoRevisionDraftPayloadError` and
  `OwnedCandidateDraftPayloadError`.
  Both used the same outer shape:
  `ChapterNotFoundOrAccessDenied | Draft(<draft error>, OwnedDraftSelectionMode) | Internal(String)`,
  with only the inner draft-error type changing. The service layer also kept
  two chapter-access normalization helpers with the same outer behavior.

  The refactor therefore narrowed this lane to a shared service-level owner:
  `OwnedDraftPayloadError<TDraftError>` now represents the common outer owned-
  draft payload boundary, while the auto-revision and candidate variants remain
  as type aliases over their specific inner draft error types. The chapter-
  access normalization path now also routes through one generic helper, and the
  existing route-level outer mappers now consume that shared generic owner
  instead of two separately declared service enums.

  This keeps load/apply workflow signatures, selection-mode-sensitive not-found
  behavior, stale/preview/content failure semantics, chapter-access failure
  behavior, and all route detail/status-code behavior unchanged while removing
  one more duplicated outer contract owner split between the draft service and
  its route mapper. Stop-rule for this lane: only return if another
  compatibility-sensitive owned-draft outer contract remains duplicated beyond
  this generic owner. Do not keep iterating for naming-only symmetry once the
  outer service owner is direct.

  Focused validation passed with
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_workflow_service -- --nocapture`,
  and
  `cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 14:47 +08:00 checkpoint:
  this slice moved to the chapter-crud payload lane, where the service layer
  still maintained two parallel outer payload owners for the same compatibility-
  sensitive shape and the route mapper repeated the same success-message
  transport mapping across project-scoped and chapter-scoped CRUD branches. A
  focused audit of
  `backend-rs/src/services/chapter_crud_workflow_service.rs`,
  `backend-rs/src/services/chapter_crud_read_workflow_service.rs`,
  `backend-rs/src/services/chapter_crud_write_workflow_service.rs`, and
  `backend-rs/src/api/chapter_crud_error_mapper.rs` showed that both
  `ProjectCrudPayloadError` and `ChapterCrudPayloadError` exposed the same outer
  shape:
  `NotFound(<resource tag>) | Internal(String)`,
  with only the not-found label differing. The route layer then translated the
  same outer contract into the same success-message error envelope multiple
  times, except for `list_chapters_by_project_path`, which intentionally keeps a
  `detail`-style not-found response.

  The refactor therefore narrowed this lane to one shared generic owner:
  `CrudPayloadError<TNotFound>` now represents the common outer chapter-crud
  payload boundary, while `ProjectCrudPayloadError` and
  `ChapterCrudPayloadError` remain as type aliases over their specific not-found
  markers. On the route side, `chapter_crud_error_mapper.rs` now uses one shared
  success-message mapper helper for the generic owner and preserves the
  `list_chapters_by_project_path` special-case detail response as an explicit
  route-level exception.

  This keeps create/list/get/update/delete/expansion-plan payload shapes,
  project-versus-chapter not-found semantics, internal-detail passthrough, and
  the `list_chapters_by_project_path` detail envelope unchanged while removing
  one more duplicated outer contract owner split across the chapter-crud service
  and route layers. Stop-rule for this lane: only return if another
  compatibility-sensitive outer CRUD contract duplication is discovered; do not
  continue here for naming-only cleanup once the generic outer owner and shared
  mapper boundary are direct.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_read_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_write_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 16:27 +08:00 checkpoint:
  this slice moved to the project query-context lane, where the service layer
  still maintained two parallel outer query owners for the same compatibility-
  sensitive base contract and the route layer mirrored that duplication with
  separate project-export and project-consistency not-found/internal mapping
  branches. A focused audit of
  `backend-rs/src/services/project_consistency_query_service.rs`,
  `backend-rs/src/services/project_export_query_service.rs`, and
  `backend-rs/src/api/projects.rs` showed that
  `LoadProjectConsistencyContextError` and the base branches inside
  `LoadProjectExportContextError` both exposed the same outer shape:
  `ProjectNotFound | Internal(String)`.
  The export lane then added one true extra semantic,
  `ProjectHasNoChapters`, but otherwise restated the same project-context
  ownership and route-level `Project not found` / internal-detail mapping.

  The refactor therefore narrowed this lane to one shared project query owner:
  `ProjectQueryContextError` now represents the common project-context
  not-found/internal boundary. `LoadProjectConsistencyContextError` remains as
  a type alias over that shared owner, while `LoadProjectExportContextError`
  now wraps the shared context owner through `Context(ProjectQueryContextError)`
  and keeps `ProjectHasNoChapters` as its explicit export-only semantic. On the
  route side, `projects.rs` now uses one shared
  `map_project_query_context_error(...)` helper for the common project-context
  transport mapping and preserves `Project has no chapters` as the export-only
  route exception.

  This keeps export-data/export-txt payload behavior, project-consistency
  access semantics, `Project not found` transport text, `Project has no
  chapters` special-case behavior, and internal-detail passthrough unchanged
  while removing one more duplicated outer contract owner split across the
  project query service and route layers. Stop-rule for this lane: only return
  if another compatibility-sensitive project context boundary still duplicates
  the same `ProjectNotFound | Internal(String)` owner pattern; do not continue
  here for naming-only cleanup once the shared base owner and route helper are
  direct.

  Focused validation passed with
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" project_consistency_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" project_export_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" project_consistency_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" projects -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 19:22 +08:00 checkpoint:
  this slice moved to the shared project-access query lane, where the service
  layer still maintained two parallel outer owners for the same project access
  query contract across memories and active batch generation. A focused audit of
  `backend-rs/src/services/memories_query_service.rs`,
  `backend-rs/src/services/memories_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that `LoadProjectAccessError` and
  `LoadActiveBatchGenerationQueryError` both exposed the same outer shape:
  `NotFoundOrAccessDenied | Internal(String)`,
  with identical project-owned query access semantics and the same transport
  mapping expectations at the route boundary.

  The refactor therefore narrowed this lane to one shared service-level owner:
  `ProjectAccessQueryError` now represents the common project-access query
  boundary and lives in the new
  `backend-rs/src/services/project_access_query_service.rs` source of truth.
  `memories_query_service.rs` keeps `LoadProjectAccessError` as a type alias
  over that shared owner, `memories_write_workflow_service.rs` now routes
  project access checks through `ensure_owned_project_access(...)`, and
  `chapter_batch_generation_active_query_service.rs` now uses the same shared
  owner directly for active batch query access failures. On the route side,
  `chapter_batch_generation_error_mapper.rs` now maps
  `ProjectAccessQueryError` directly and preserves the existing
  project-not-found-or-access-denied transport behavior.

  This keeps memories query/write access behavior, active batch generation
  query payload behavior, `项目不存在或无权限` transport text for memories, and
  the existing project access denied/not-found route status behavior for active
  batch generation unchanged while removing one more duplicated outer contract
  owner split between adjacent project-access query lanes. Stop-rule for this
  lane: only return if another compatibility-sensitive project-access query
  owner still duplicates the same
  `NotFoundOrAccessDenied | Internal(String)` boundary; do not continue here
  for naming-only symmetry once the shared owner is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" project_access_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 19:48 +08:00 checkpoint:
  this slice returned to the batch-generation create write lane, where the
  active-query side had already moved onto the shared project-access query
  owner but the create workflow still kept a separate outer branch for the same
  compatibility-sensitive project access boundary. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the create lane still modeled project access failures as its own
  `ProjectNotFoundOrAccessDenied` variant even though the adjacent active-query
  lane now uses the shared `ProjectAccessQueryError` owner and both routes map
  the same project-not-found-or-access-denied transport contract.

  The refactor therefore narrowed this lane to the same shared owner:
  `CreateBatchGenerationWriteWorkflowError` now wraps
  `ProjectAccess(ProjectAccessQueryError)` instead of keeping a dedicated
  project-access variant, and
  `start_owned_batch_generation_write_workflow(...)` now routes access checks
  through `ensure_owned_project_access(...)`. On the route side,
  `chapter_batch_generation_error_mapper.rs` now maps the wrapped shared owner
  directly while preserving the existing `Project not found or access denied`
  and internal-detail transport behavior for batch-generation create requests.

  This keeps batch-generation create request behavior, invalid-count and
  chapters-not-found semantics, config error behavior, and internal-detail
  passthrough unchanged while removing one more duplicated project-access owner
  from the batch-generation write lane. Stop-rule for this lane: only return if
  another batch-generation write boundary still duplicates the same shared
  project-access contract in a behavior-sensitive way; do not continue here for
  naming-only symmetry once the shared owner is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 20:12 +08:00 checkpoint:
  this slice stayed on the adjacent batch-generation route lane, where the
  service layer had already converged active-query and create-workflow project
  access failures onto `ProjectAccessQueryError` but the route mapper still
  carried two parallel transport branches for the same compatibility-sensitive
  project access contract. A focused audit of
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that both `map_active_batch_generation_query_error(...)` and the
  `CreateBatchGenerationWriteWorkflowError::ProjectAccess(...)` branch inside
  `map_create_batch_generation_workflow_error(...)` repeated the same
  `ProjectAccessQueryError -> project_not_found_or_access_denied / internal`
  translation.

  The refactor therefore narrowed this lane to one route-level owner helper:
  `map_project_access_query_route_error(...)` now represents the shared
  project-access transport mapping for the batch-generation route group, and
  both active-query and create-workflow route paths now delegate to that same
  helper instead of maintaining parallel branches.

  This keeps active batch query responses, batch create workflow responses,
  `Project not found or access denied` detail text, and internal-detail
  passthrough unchanged while removing one more duplicated route contract split
  now that the service layer already shares the same owner. Stop-rule for this
  lane: only return if another behavior-sensitive batch-generation route branch
  still repeats the same shared project-access transport mapping; do not
  continue here for purely cosmetic helper extraction once the direct route
  owner is singular.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 20:43 +08:00 checkpoint:
  this slice moved to the chapter-analysis read-side query-context lane, where
  the service layer still maintained one base access/internal owner for
  analysis-task status and a second parallel owner for chapter-analysis view
  that repeated the same access/internal boundary plus one real extra semantic.
  A focused audit of
  `backend-rs/src/services/chapter_analysis_service.rs`,
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`,
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs`
  showed that `LoadAnalysisTaskStatusError` already owned the shared
  `Chapter(LoadAccessibleChapterError) | Internal(String)` context contract,
  while `LoadChapterAnalysisViewPayloadError` restated that same boundary and
  added only one true view-only semantic: `AnalysisNotFound`.

  The refactor therefore narrowed this lane to one shared analysis query owner:
  `ChapterAnalysisQueryContextError` now represents the common analysis read
  context boundary, `LoadAnalysisTaskStatusError` remains as a type alias over
  that owner, and `LoadChapterAnalysisViewPayloadError` now wraps it through
  `Context(ChapterAnalysisQueryContextError)` while keeping
  `AnalysisNotFound` as the explicit view-only semantic. On the route side,
  `chapter_analysis_query_error_mapper.rs` now uses one shared
  `map_chapter_analysis_query_context_error(...)` helper for the common
  context transport mapping, while `chapters_error_mapper.rs` keeps the task
  status route on the same shared owner and preserves the existing 404/internal
  detail behavior.

  This keeps chapter-analysis read payloads, analysis-task status payloads,
  chapter access denied/not-found transport, `Chapter analysis not found`
  behavior, and internal-detail passthrough unchanged while removing one more
  duplicated read-side outer contract split across adjacent analysis query
  lanes. Stop-rule for this lane: only return if another analysis read boundary
  still duplicates the same shared context contract in a behavior-sensitive
  way; do not continue here for naming-only symmetry once the shared owner is
  direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_read_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:13 +08:00 checkpoint:
  this slice moved to the memories read-side query-context lane, where the
  service layer still maintained one shared outer owner for project-scoped
  memories queries and a second parallel outer owner for chapter-analysis
  lookup that repeated the same project-access/internal boundary plus one real
  extra semantic. A focused audit of
  `backend-rs/src/services/memories_query_service.rs` and
  `backend-rs/src/api/memories_error_mapper.rs`
  showed that list/search/foreshadow/stats queries already shared
  `OwnedProjectMemoriesQueryError` for the common
  `ProjectAccess(LoadProjectAccessError) | Internal(String)` contract, while
  `LoadProjectChapterAnalysisPayloadError` restated that same context boundary
  and added only one true read-only semantic: `AnalysisNotFound`.

  The refactor therefore narrowed this lane to one shared memories query
  context owner: `MemoriesProjectQueryContextError` now represents the common
  project-scoped read context boundary, `OwnedProjectMemoriesQueryError`
  remains as a type alias over that owner, and
  `LoadProjectChapterAnalysisPayloadError` now wraps it through
  `Context(MemoriesProjectQueryContextError)` while keeping
  `AnalysisNotFound` as the explicit chapter-analysis-only semantic. On the
  route side, `memories_error_mapper.rs` now uses one shared
  `map_memories_project_query_context_error(...)` helper for the common
  project-access/internal transport mapping, while the chapter-analysis route
  branch preserves the existing Chinese `该章节还未进行分析` not-found detail.

  This keeps memories list/search/foreshadow/stats payloads, chapter-analysis
  lookup payloads, project access denied/not-found transport, analysis-missing
  detail text, and internal-detail passthrough unchanged while removing one
  more duplicated read-side outer contract split across adjacent memories query
  lanes. Stop-rule for this lane: only return if another memories read
  boundary still duplicates the same shared context contract in a
  behavior-sensitive way; do not continue here for naming-only symmetry once
  the shared owner is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_query_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:23 +08:00 checkpoint:
  this slice moved to the chapter-analysis draft request boundary, where the
  workflow lane still normalized route payload/query data into temporary
  request DTOs and then immediately rebuilt the service-owned
  `OwnedDraftPayloadRequest` contract before calling the draft service. A
  focused audit of
  `backend-rs/src/services/chapter_analysis_draft_request_service.rs` and
  `backend-rs/src/services/chapter_analysis_draft_workflow_service.rs`
  showed that `DraftLookupRequest` / `DraftApplyRequest` did not carry any
  compatibility-sensitive semantic beyond trimmed selector text plus
  `allow_stale`, and that the workflow layer then re-expressed the same data
  into `build_owned_draft_payload_request(...)` for all four draft entrypoints.

  The refactor therefore narrowed this lane to one direct service-owned
  request boundary: the request service now normalizes route query/body input
  straight into `OwnedDraftPayloadRequest`, and the workflow layer now passes
  that owner directly into `load_owned_*_draft_payload(...)` /
  `apply_owned_*_draft_payload(...)` without constructing a temporary
  intermediate DTO first. The existing chapter-access owner, selection-mode
  semantics, stale flag behavior, and route-level error transport all remain
  unchanged.

  This keeps auto-revision and candidate draft lookup/apply payloads,
  selector trimming behavior, explicit-vs-latest selection semantics,
  `allow_stale` handling, and draft error mapping unchanged while removing one
  more no-op request boundary split between route compatibility input and the
  service-owned draft request contract. Stop-rule for this lane: only return
  if another draft request boundary still reconstructs the same
  `OwnedDraftPayloadRequest` semantics through a temporary DTO in a
  behavior-sensitive path; do not continue here for naming-only cleanup once
  the service owner is direct.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_workflow_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 17:30 +08:00 checkpoint:
  this slice moved to two adjacent request-wrapper lanes where the route/read
  boundary still normalized one field into a dedicated request struct and then
  immediately consumed that struct inside one query service without any extra
  compatibility-sensitive semantic. A focused audit of
  `backend-rs/src/services/chapter_analysis_batch_status_request_service.rs`,
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_read_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_list_request_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs`
  showed that `BatchAnalysisStatusRequest` only wrapped the normalized
  `Vec<String>` of deduplicated chapter ids, while
  `ActiveBatchGenerationTaskListRequest` only wrapped the normalized `u64`
  limit. Neither owner carried a second semantic branch, shared error
  behavior, or any contract that was not already represented by the normalized
  primitive input itself.

  The refactor therefore narrowed both lanes to their direct service inputs:
  batch analysis status now passes the normalized `Vec<String>` straight from
  request normalization into the query service, and active batch-generation
  list now passes the normalized `u64` limit straight from route parsing into
  the query service. This keeps chapter-id trimming, deduplication, 200-item
  cap semantics, active-list default/min/max limit normalization, and all
  route/query payload behavior unchanged while removing two more no-op
  request-wrapper owners that did not add durable semantic value.

  This keeps batch-analysis status payloads, active batch-generation list
  payloads, route signatures, and error transport unchanged while shrinking
  the request boundary to one owner per lane. Stop-rule for these lanes: only
  return if another request wrapper still exists solely to hold a normalized
  primitive or collection that is immediately unpacked by one query service;
  do not continue here for arbitrary wrapper deletion once the remaining
  request types carry real multi-field or compatibility semantics.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_batch_status_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_read_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 18:07 +08:00 checkpoint:
  this slice moved to the chapter-regeneration tasks request boundary, where
  the route/query lane still normalized one query field into a dedicated
  request struct and then immediately unpacked that struct inside a single
  query service without any extra compatibility-sensitive semantic. A focused
  audit of
  `backend-rs/src/services/chapter_regeneration_tasks_request_service.rs`,
  `backend-rs/src/services/chapter_regeneration_query_service.rs`, and
  `backend-rs/src/api/chapter_regeneration_routes.rs`
  showed that `RegenerationTasksRequest` only wrapped the normalized `u64`
  limit and that no sibling workflow, mapper, or downstream owner consumed any
  richer contract than that primitive.

  The refactor therefore narrowed this lane to the direct service input:
  route query normalization now returns the normalized `u64` limit directly,
  and the regeneration tasks query service now consumes that primitive instead
  of a one-field wrapper owner. This keeps route query shape, default/min/max
  limit semantics, chapter access checks, regeneration-task payload shape, and
  error transport unchanged while removing one more no-op request-wrapper
  boundary.

  This keeps regeneration tasks payloads, chapter access denied/not-found
  mapping, and route signatures unchanged while shrinking the request boundary
  to one normalized primitive. Stop-rule for this lane: only return if another
  regeneration request wrapper still exists solely to hold one normalized
  primitive or collection that is immediately unpacked by one query/workflow
  owner; do not continue here for arbitrary wrapper deletion once remaining
  request types carry real multi-field compatibility semantics.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_tasks_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 18:14 +08:00 checkpoint:
  this slice moved to the memories unresolved-foreshadows request boundary,
  where the route/query lane still normalized one query field into a dedicated
  request struct and then immediately unpacked that struct inside a single
  query service without any extra compatibility-sensitive semantic. A focused
  audit of
  `backend-rs/src/services/memories_request_service.rs`,
  `backend-rs/src/services/memories_query_service.rs`, and
  `backend-rs/src/api/memories.rs`
  showed that `ForeshadowListRequest` only wrapped the optional
  `current_chapter` filter and that no sibling workflow, mapper, or
  downstream owner consumed any richer contract than that primitive optional
  filter.

  The refactor therefore narrowed this lane to the direct service input:
  route query normalization now returns the optional `current_chapter`
  directly, and the unresolved-foreshadows query service now consumes that
  `Option<i32>` instead of a one-field wrapper owner. This keeps route query
  shape, optional current-chapter filter semantics, unresolved-foreshadow
  payload shape, project-access checks, and error transport unchanged while
  removing one more no-op request-wrapper boundary.

  This keeps unresolved foreshadow payloads, shared memories query error
  mapping, and route signatures unchanged while shrinking the request boundary
  to one optional primitive filter. Stop-rule for this lane: only return if
  another memories request wrapper still exists solely to hold one normalized
  primitive or collection that is immediately unpacked by one query/workflow
  owner; do not continue here for arbitrary wrapper deletion once remaining
  request types carry real multi-field compatibility semantics.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_query_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 18:18 +08:00 checkpoint:
  this slice moved to the single-chapter generation runtime input boundary,
  where the runtime lane still unpacked one launch owner into a second
  temporary struct and then immediately unpacked that struct again before
  constructing the AI runtime call. A focused audit of
  `backend-rs/src/services/chapter_single_generation_execution_service.rs`,
  together with adjacent dispatch/launch/workflow files, showed that
  `ResolvedSingleChapterGenerationRuntimeInput` only re-expressed the same
  fields already present in `SingleGenerationRuntimeLaunchInput` plus its
  nested `SingleChapterGenerationExecutionInput`, and that no sibling owner
  consumed any extra compatibility-sensitive semantic from that intermediate
  runtime struct.

  The refactor therefore narrowed this lane to the direct runtime input:
  single-chapter execution now destructures `SingleGenerationRuntimeLaunchInput`
  directly and passes the extracted fields into the existing AI runtime call,
  instead of rebuilding a temporary resolved owner first. This keeps launch
  payload shape, AI config ownership, provider payload forwarding,
  target-word-count semantics, and runtime behavior unchanged while removing
  one more no-op runtime-wrapper boundary.

  This keeps single-chapter background/stream runtime launch behavior,
  dispatch boundaries, and route-visible semantics unchanged while shrinking
  the runtime input boundary to the direct owner. Stop-rule for this lane:
  only return if another single-generation runtime owner still exists solely
  to restate fields from an already-owned launch/input contract before one
  immediate consumer; do not continue here for arbitrary struct flattening
  once remaining owners carry real dispatch, task, or compatibility semantics.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_execution_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 18:26 +08:00 checkpoint:
  this slice moved to the full chapter-regeneration stream request builder,
  where the request-normalization lane still converted route payload data into
  one temporary local fields owner and then immediately rebuilt the real
  `FullChapterRegenerationStreamRequest` from that owner without any extra
  compatibility-sensitive semantic. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`
  showed that `FullChapterRegenerationStreamRequestFields` existed only as a
  local intermediate container for already-normalized string lists, optional
  text fields, and preserve-element booleans, and that no sibling workflow or
  downstream owner consumed any richer contract from that temporary struct.

  The refactor therefore narrowed this lane to the direct request owner:
  full regeneration route normalization now constructs
  `FullChapterRegenerationStreamRequest` directly from the normalized route
  fields instead of first rebuilding a temporary local fields struct. This
  keeps route payload shape, numeric/string normalization, preserve-elements
  defaults, story-repair field handling, and downstream workflow behavior
  unchanged while removing one more no-op local request-wrapper boundary.

  This keeps full regeneration stream request semantics, route compatibility,
  and downstream workflow inputs unchanged while shrinking the builder
  boundary to the direct owner. Stop-rule for this lane: only return if
  another regeneration request builder still exists solely to restate
  already-normalized local fields into a temporary owner before one immediate
  consumer; do not continue here for arbitrary helper flattening once
  remaining owners carry real multi-stage or compatibility semantics.

  Focused validation passed with
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_request_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`,
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`,
  and
  `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`.
  The warnings reported during validation are pre-existing workspace-level
  items outside this seam.

- 2026-05-24 18:45 +08:00 checkpoint:
  this slice moved to the single-chapter background generation launch
  boundary, where the background workflow still rebuilt one local owner that
  only carried `task_id` plus the already-prepared
  `SingleGenerationRuntimeLaunchInput` before immediately unpacking both
  fields into the shared runtime dispatch helper without any extra
  compatibility-sensitive or transport-specific semantic. A focused audit of
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that `OwnedSingleGenerationBackgroundLaunch` existed only as a local
  wrapper around the already-normalized runtime launch input and that no
  sibling route, workflow, or runtime owner consumed any richer contract from
  that temporary struct.

  The refactor therefore narrowed this lane to the direct runtime launch
  owner: the background launch builder now returns the shared
  `SingleGenerationRuntimeLaunchInput` directly, and the background dispatch
  helper now accepts `task_id` plus that shared runtime input without
  rebuilding a temporary local launch wrapper first. This keeps background
  task creation payloads, task-id ownership, runtime dispatch behavior, and
  single-chapter generation semantics unchanged while removing one more no-op
  local launch-wrapper boundary.

  This keeps single-chapter background generation route behavior, task-plan
  creation, and runtime execution unchanged while shrinking the background
  launch boundary to the direct shared runtime owner. Stop-rule for this lane:
  only return if another single-generation background owner still exists
  solely to restate `task_id` plus an already-owned runtime input before one
  immediate dispatcher; do not continue here for arbitrary launch-helper
  flattening once remaining owners carry real workflow or task semantics.

- 2026-05-24 18:50 +08:00 checkpoint:
  this slice moved one step further on the same single-chapter background
  launch lane, where the dedicated
  `chapter_single_generation_background_launch_service.rs` module had become
  a pure forwarding boundary after the previous launch-wrapper removal. A
  focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  and the now-removed
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`
  showed that the background-launch module no longer owned any task,
  compatibility, transport, or runtime semantic: it only forwarded
  `user_id + execution_input` into the shared
  `build_single_generation_runtime_launch_input()` helper and then forwarded
  `task_id + runtime_input` into the shared single-generation dispatcher.

  The refactor therefore narrowed this lane to the direct shared dispatch
  owner: the background workflow now builds the shared
  `SingleGenerationRuntimeLaunchInput` directly and dispatches it straight
  through `dispatch_single_chapter_generation_runtime()`, and the dedicated
  background-launch service module has been removed. This keeps task-plan
  creation, background task payloads, dispatch timing, and runtime execution
  unchanged while removing one more pure forwarding owner boundary.

  This keeps single-chapter background generation workflow semantics, route
  behavior, and runtime launch behavior unchanged while shrinking the launch
  lane to the shared dispatch owner. Stop-rule for this lane: only return if a
  remaining single-generation helper still exists purely as a pass-through to
  an already-direct shared dispatch/runtime owner; do not continue here for
  arbitrary module deletion once the remaining files each own distinct
  workflow, task, stream, or runtime semantics.

- 2026-05-24 18:53 +08:00 checkpoint:
  this slice moved to the partial-regeneration stream prepare boundary, where
  the active workflow still asked the prepare layer for a local
  `PreparedPartialRegenerationStream` owner and then immediately unpacked that
  owner to rebuild the real `PartialChapterRegenerationStreamInput` consumed by
  the active partial stream service. A focused audit of
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  and `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`
  showed that the active workflow path only needed the final
  `target_words/original_word_count/start_position/end_position/prompt/ai_service`
  stream input, and that `PreparedPartialRegenerationStream` no longer carried
  any extra compatibility or cross-consumer semantic beyond being a temporary
  wrapper around `prepared + ai_service` before one immediate consumer rebuilt
  the active stream input.

  The refactor therefore narrowed this lane to the direct active stream owner:
  `prepare_partial_regeneration_stream()` now returns
  `PartialChapterRegenerationStreamInput` directly, and the partial-regeneration
  workflow now forwards that stream input straight into
  `build_partial_chapter_regeneration_stream()` without reassembling it. As
  part of the same cleanup, the stale
  `backend-rs/src/services/chapter_partial_regeneration_stream_service.rs`
  implementation was removed because it was no longer exported by
  `services/mod.rs` and no active route/workflow used it.

  This keeps partial-regeneration route payloads, request normalization,
  accessibility checks, style-loading behavior, web-research note handling,
  stream event semantics, and finalize behavior unchanged while removing one
  more local prepare-wrapper boundary plus one dead adjacent implementation.
  Stop-rule for this lane: only return if another regeneration prepare owner
  still exists solely to wrap the exact stream/runtime input later rebuilt by
  one immediate workflow consumer; do not continue here for arbitrary prepare
  flattening once remaining owners serve distinct full/partial stream
  contracts or compatibility semantics.

- 2026-05-24 19:03 +08:00 checkpoint:
  this slice moved to the full-regeneration stream prepare boundary, where the
  active workflow still asked the prepare layer for a local
  `PreparedChapterRegenerationStream` owner and then immediately unpacked that
  owner to rebuild the real `FullChapterRegenerationStreamInput` consumed by
  the active full stream service. A focused audit of
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  and `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`
  showed that the active full-regeneration workflow only needed the final
  `task_label/chapter_id/chapter_word_count/prompt/ai_service` stream input,
  and that `PreparedChapterRegenerationStream` no longer carried any extra
  compatibility or cross-consumer semantic beyond being a temporary wrapper
  around `prompt + ai_service` before one immediate workflow consumer rebuilt
  the active full stream input.

  The refactor therefore narrowed this lane to the direct active stream owner:
  `prepare_chapter_regeneration_stream()` now returns
  `FullChapterRegenerationStreamInput` directly, and the full-regeneration
  workflow now forwards that stream input straight into
  `build_full_chapter_regeneration_stream()` without reassembling it. This
  keeps chapter access checks, prompt construction, AI-service preparation,
  task label, chapter payload metadata, stream event semantics, and finalize
  behavior unchanged while removing one more local prepare-wrapper boundary.

  This keeps full-regeneration route payloads, request normalization, stream
  launch behavior, and final payload semantics unchanged while shrinking the
  full stream lane to the direct active input owner. Stop-rule for this lane:
  only return if another regeneration prepare owner still exists solely to
  wrap the exact stream/runtime input later rebuilt by one immediate workflow
  consumer; do not continue here for arbitrary prepare flattening once the
  remaining owners serve distinct contracts or compatibility semantics.

- 2026-05-24 19:07 +08:00 checkpoint:
  this slice moved back to the single-chapter stream launch boundary, where the
  active stream-launch module still exposed a local
  `OwnedSingleChapterGenerationStreamLaunch` owner even though that owner was
  only a direct type alias to the shared
  `SingleGenerationRuntimeLaunchInput` and added no distinct stream semantic.
  A focused audit of
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that the stream workflow built the shared runtime input and then
  immediately re-labeled it through the local alias before handing it to the
  stream-launch helper, and that no sibling route/workflow/runtime owner
  depended on a richer contract than the direct shared runtime input itself.

  The refactor therefore narrowed this lane to the shared runtime owner:
  `launch_owned_single_chapter_generation_stream()` now accepts
  `SingleGenerationRuntimeLaunchInput` directly, and the stream workflow no
  longer spells the local alias when building the launch input. This keeps the
  stream launch payload, progress events, runtime execution, and final SSE
  result behavior unchanged while removing one more fake owner boundary that
  only renamed an already-direct shared contract.

  This keeps single-chapter stream generation route behavior, workflow
  semantics, and runtime launch behavior unchanged while shrinking the stream
  lane to the direct shared runtime owner. Stop-rule for this lane: only
  return if another single-generation stream helper still exists solely to
  rename or restate an already-direct shared runtime input before one
  immediate consumer; do not continue here for arbitrary naming cleanup once
  remaining helpers own real stream or runtime behavior.

- 2026-05-24 19:11 +08:00 checkpoint:
  this slice moved to the single-chapter background task-plan boundary, where
  the task-command layer still returned a local
  `SingleGenerationBackgroundCreatePlan` owner even though that owner only
  wrapped the freshly created `created_task_id` and the background workflow was
  its only consumer. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that the workflow immediately unpacked `plan.created_task_id` to build
  the response payload and to dispatch runtime work, and that no sibling route,
  workflow, or runtime owner depended on a richer contract than the direct
  task id itself.

  The refactor therefore narrowed this lane to the direct task-id owner:
  `create_single_generation_background_task_plan()` now returns the created
  task id directly as `String`, and the background workflow now feeds that task
  id straight into response payload assembly and runtime dispatch without
  rebuilding or unpacking a one-field local plan wrapper. This keeps task row
  creation, snapshot persistence, response payload shape, and runtime dispatch
  behavior unchanged while removing one more single-field internal owner.

  This keeps single-chapter background generation workflow semantics, task
  lifecycle behavior, and response/runtime wiring unchanged while shrinking the
  background task-plan lane to the direct task-id owner. Stop-rule for this
  lane: only return if another single-generation helper still exists solely to
  wrap one created identifier or primitive that is immediately unpacked by one
  workflow consumer; do not continue here for arbitrary return-type
  simplification once remaining task/workflow owners carry distinct semantic
  state.

- 2026-05-24 20:02 +08:00 checkpoint:
  this slice moved to the regeneration stream return boundary, where the full
  and partial stream service modules still exposed local
  `FullChapterRegenerationStream` and `PartialChapterRegenerationStream`
  owners even though both owners were only direct type aliases to the shared
  `OwnedRegenerationStream` returned by the common stream-launch layer. A
  focused audit of
  `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`,
  `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that the full/partial builders already owned the real semantic split
  through their distinct input contracts, initial event plans, and finalize
  closures, while the extra returned aliases only re-labeled the exact shared
  stream type before one immediate workflow consumer passed that stream back to
  the route transport boundary.

  The refactor therefore narrowed this lane to the shared stream owner:
  `build_full_chapter_regeneration_stream()` and
  `build_partial_chapter_regeneration_stream()` now return
  `OwnedRegenerationStream` directly, and the regeneration stream workflow now
  returns that shared stream owner without restating local alias names. This
  keeps route SSE transport shape, initial event sequencing, chunk progress,
  finalize behavior, and error mapping unchanged while removing two more fake
  owner boundaries that only renamed an already-direct shared stream contract.

  This keeps chapter and partial regeneration stream route behavior, workflow
  semantics, and shared launch/runtime behavior unchanged while shrinking the
  stream lane to the direct shared stream owner. Stop-rule for this lane: only
  return if another regeneration stream helper still exists solely to rename an
  already-direct shared launch/stream contract before one immediate consumer;
  do not continue here for arbitrary naming cleanup once remaining owners carry
  distinct input, event-plan, or finalize semantics.

- 2026-05-24 20:18 +08:00 checkpoint:
  this slice moved to the single-chapter stream return boundary, where the
  stream-launch module still exposed a local `SingleChapterGenerationStream`
  owner even though that owner was only a direct type alias to the concrete
  `ReceiverStream<Result<Event, Infallible>>` returned by the launch helper. A
  focused audit of
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  showed that the stream-launch helper already owned the real stream semantic
  through tracker initialization, initial SSE events, runtime execution, and
  result/error emission, while the extra local alias only re-labeled that
  exact stream type before two immediate workflow consumers passed it onward to
  the route transport boundary.

  The refactor therefore narrowed this lane to the direct stream owner:
  `launch_owned_single_chapter_generation_stream()` now returns the concrete
  `ReceiverStream<Result<Event, Infallible>>` directly, and the single-chapter
  stream workflow plus write-workflow now return that shared stream type
  without restating a local alias. This keeps route SSE transport shape,
  progress event sequencing, runtime execution, and final result/error payload
  behavior unchanged while removing one more fake owner boundary that only
  renamed an already-direct stream contract.

  This keeps single-chapter stream generation route behavior, workflow
  semantics, and stream/runtime launch behavior unchanged while shrinking the
  stream lane to the direct stream owner. Stop-rule for this lane: only return
  if another single-generation helper still exists solely to rename an
  already-direct shared stream or runtime contract before one immediate
  consumer; do not continue here for arbitrary naming cleanup once remaining
  helpers carry real request, task, or runtime semantics.

- 2026-05-24 20:31 +08:00 checkpoint:
  this slice moved to the partial-regeneration apply prepare boundary, where
  the apply service still exposed a local `PreparedPartialRegenerateApply`
  owner even though that owner only wrapped the rewritten `new_content` plus
  the unchanged `chapter.word_count`, and the apply workflow was its only
  consumer. A focused audit of
  `backend-rs/src/services/chapter_regeneration_apply_service.rs` showed that
  the prepare helper already owned the real semantic work through narrative
  sanitization, workflow-meta rejection, range validation, and rewritten
  content assembly, while the extra wrapper only re-labeled the resulting
  string and forwarded an `old_word_count` value that the caller already had on
  the loaded chapter model.

  The refactor therefore narrowed this lane to the direct content owner:
  `prepare_partial_regenerate_apply()` now returns the rewritten `String`
  directly, and the apply workflow now uses `chapter.word_count` at the
  response boundary instead of unpacking a local wrapper for that unchanged
  value. This keeps chapter access checks, text sanitization, range
  validation, chapter update behavior, and success/error payload shape
  unchanged while removing one more single-consumer internal owner.

  This keeps partial-regeneration apply route behavior, workflow semantics,
  and chapter update behavior unchanged while shrinking the apply lane to the
  direct rewritten-content owner. Stop-rule for this lane: only return if
  another regeneration apply helper still exists solely to wrap one derived
  content value plus unchanged caller-owned metadata before one immediate
  consumer; do not continue here for arbitrary tuple/return simplification once
  remaining helpers carry real access, sanitize, or persistence semantics.

- 2026-05-24 20:44 +08:00 checkpoint:
  this slice moved to the full-regeneration prepare error boundary, where the
  prepare layer still exposed a local `PrepareChapterRegenerationStreamError`
  owner even though that owner was only a direct type alias to the existing
  `BuildRegenerationAiServiceError` returned by the actual full-regeneration
  prepare path. A focused audit of
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  and `backend-rs/src/api/chapters_error_mapper.rs` showed that the full
  regeneration prepare flow only built prompt text and AI service config, and
  that the only prepare failure shape on this lane was still the direct
  AI-service config error with no added compatibility or branching semantic.

  The refactor therefore narrowed this lane to the direct error owner:
  `prepare_chapter_regeneration_stream()` now returns
  `BuildRegenerationAiServiceError` directly, the regeneration stream workflow
  now aliases its full-prepare branch to that real error owner, and the route
  error mapper now consumes the direct error type without a local rename. This
  keeps chapter access checks, prompt construction, AI-service preparation,
  workflow error branching, and route error payload behavior unchanged while
  removing one more fake error boundary that only renamed an already-direct
  prepare failure contract.

  This keeps full-regeneration stream route behavior, workflow semantics, and
  prepare/config error mapping unchanged while shrinking the prepare lane to
  the direct config-error owner. Stop-rule for this lane: only return if
  another regeneration prepare/helper error still exists solely to rename one
  existing direct failure owner before one immediate consumer; do not continue
  here for arbitrary error alias cleanup once remaining owners add real branch
  or compatibility semantics.

- 2026-05-24 20:57 +08:00 checkpoint:
  this slice moved to the single-generation runtime launch construction
  boundary, where the dispatch layer still exposed a local
  `build_single_generation_runtime_launch_input()` helper even though that
  helper only repackaged `user_id + execution_input` into the already-direct
  `SingleGenerationRuntimeLaunchInput` owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the single-generation workflow and batch-resume lanes
  immediately consumed the same runtime-launch owner without any added
  validation, fallback, or compatibility semantic in the builder itself.

  The refactor therefore narrowed this lane to the direct launch owner:
  `build_single_generation_runtime_launch_input()` has been removed, and the
  single-generation workflow plus batch-resume branch now construct
  `SingleGenerationRuntimeLaunchInput` directly at the call sites before
  dispatch or stream launch. This keeps stream launch behavior, runtime
  dispatch, batch resume routing, and execution-input semantics unchanged while
  removing one more pure construction helper that only restated an already-
  direct owner.

  This keeps single-generation stream/background behavior and batch-resume
  semantics unchanged while shrinking the runtime launch lane to the direct
  owner. Stop-rule for this lane: only return if another single-generation or
  resume helper still exists solely to repack one already-direct runtime owner
  without added validation or compatibility semantics; do not continue here for
  arbitrary constructor inlining once remaining helpers own real branch or
  payload semantics.

- 2026-05-24 21:09 +08:00 checkpoint:
  this slice moved to the batch-resume execution branching boundary, where the
  dispatch layer still exposed local
  `build_resume_single_generation_execution_input()` and
  `build_resume_batch_generation_execution_input()` helpers even though both
  helpers were only single-consumer branch builders used by
  `build_resume_generation_execution_input()`. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the real owner on this lane was already the outer
  `ResumeExecutionPlan -> ResumeGenerationExecutionInput` branch table, while
  the two inner helpers only re-spelled one `SingleChapter` constructor and
  one `Batch` constructor without adding validation, fallback, or shared
  cross-consumer semantics.

  The refactor therefore narrowed this lane to the outer branch owner:
  `build_resume_generation_execution_input()` now constructs both resume enum
  variants directly, and the two inner single-use branch builders have been
  removed. This keeps resume routing, runtime dispatch selection, execution
  config ownership, and batch/single resume behavior unchanged while removing
  two more pure branch helpers that only restated an already-direct branch
  contract.

  This keeps batch-resume semantics and dispatch behavior unchanged while
  shrinking the resume branching lane to the direct outer owner. Stop-rule for
  this lane: only return if another resume helper still exists solely to build
  one branch of an already-direct outer branch owner before one immediate
  caller; do not continue here for arbitrary inlining once remaining helpers
  own shared validation, adaptation, or multi-consumer semantics.

- 2026-05-24 21:21 +08:00 checkpoint:
  this slice moved to the single-generation background task active-model
  construction boundary, where the task-command layer still exposed a local
  `build_single_generation_background_task_active_model_from_prepared_request()`
  helper even though that helper was only a single-consumer adapter over the
  already-shared `build_batch_generation_task_active_model()` owner. A focused
  audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the real semantic owner on this lane was already the shared
  batch-generation task active-model builder together with the explicit
  single-task chapter payload helper, while the local helper only re-spelled
  one call site's fixed constants and direct chapter-target projections before
  one immediate caller inserted the task row.

  The refactor therefore narrowed this lane to the shared active-model owner:
  `create_single_generation_background_task_plan()` now calls
  `build_batch_generation_task_active_model()` directly, and the local
  single-consumer active-model helper has been removed. This keeps task row
  fields, single-task payload shape, snapshot initialization, and background
  generation lifecycle behavior unchanged while removing one more pure adapter
  helper that only restated an already-direct shared construction contract.

  This keeps single-generation background task creation semantics unchanged
  while shrinking the task-command construction lane to the shared active-model
  owner. Stop-rule for this lane: only return if another single-generation
  helper still exists solely to restate one shared batch/task construction
  owner before one immediate caller; do not continue here for arbitrary
  constructor inlining once remaining helpers own task lifecycle or multi-
  consumer adaptation semantics.

- 2026-05-24 21:34 +08:00 checkpoint:
  this slice moved to the batch-generation create task active-model
  construction boundary, where the create task-command layer still exposed a
  local `build_batch_generation_task_active_model_from_create_request()`
  helper even though that helper was only a single-consumer adapter over the
  already-shared `build_batch_generation_task_active_model()` owner plus one
  local `chapter_ids` projection. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  showed that the real semantic owners on this lane were already the shared
  active-model builder and the explicit create-request/prepared-request values,
  while the local helper only reassembled one call site's direct values before
  the task row insert path.

  The refactor therefore narrowed this lane to the shared active-model owner:
  `create_batch_generation_task_plan()` now computes the `chapter_ids` JSON
  payload locally and calls `build_batch_generation_task_active_model()`
  directly, and the local single-consumer create-request adapter helper has
  been removed. This keeps batch task row fields, create-plan payload shape,
  snapshot initialization, and batch generation lifecycle behavior unchanged
  while removing one more pure construction helper that only restated an
  already-direct shared owner.

  This keeps batch-generation create task semantics unchanged while shrinking
  the task-command construction lane to the shared active-model owner.
  Stop-rule for this lane: only return if another batch-generation helper still
  exists solely to restate one shared task construction owner before one
  immediate caller; do not continue here for arbitrary constructor inlining
  once remaining helpers own request normalization, task lifecycle, or
  multi-consumer adaptation semantics.

- 2026-05-24 22:01 +08:00 checkpoint:
  this slice moved to the batch-generation read-side owned-task error
  boundary, where the status-query and status-stream services still exposed
  local `LoadBatchGenerationStatusQueryError` and
  `BatchGenerationStatusStreamAccessError` owners even though both were only
  direct aliases to the existing `LoadOwnedBatchGenerationTaskError`
  contract. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` showed
  that the read-side lane already shared one real error owner through owned
  task lookup and required read-context loading, while the two local aliases
  only re-labeled that exact failure shape before one immediate route mapper
  each forwarded it unchanged.

  The refactor therefore narrowed this lane to the direct owned-task error
  owner: the status-query and status-stream service functions now return
  `LoadOwnedBatchGenerationTaskError` directly, the task-view query service no
  longer restates a local `LoadBatchGenerationTaskViewContextError` alias, and
  the batch-generation route error mapper now accepts the direct owned-task
  error type for both status-query and stream-access mapping. This keeps task
  lookup semantics, status payload behavior, SSE access gating, and route
  error payloads unchanged while removing three more fake read-side error
  boundaries that only renamed an already-direct shared owner.

  This keeps batch-generation status query and stream route behavior unchanged
  while shrinking the read-side access lane to the direct owned-task error
  owner. Stop-rule for this lane: only return if another read-side helper or
  query service still exists solely to rename one direct owned-task/read-
  context error before one immediate consumer; do not continue here for
  broader read-side API renaming once remaining owners add real branch,
  payload, or compatibility semantics.

- 2026-05-24 22:15 +08:00 checkpoint:
  this slice moved to the active batch-generation query access-error
  boundary, where the active-query service still exposed a local
  `LoadActiveBatchGenerationQueryError` owner even though that owner was only
  a direct alias to the existing `ProjectAccessQueryError` contract. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` showed that
  the real error owner on this lane was already the shared project-access
  contract: request access checks and fallback internal string mapping already
  resolved into `ProjectAccessQueryError`, and the route mapper only forwarded
  that exact shape unchanged.

  The refactor therefore narrowed this lane to the direct project-access
  owner: `load_active_batch_generation_query()` now returns
  `ProjectAccessQueryError` directly, and the batch-generation route error
  mapper now accepts that shared owner without a local rename. This keeps
  project access checks, active-task query response payloads, and route error
  payload behavior unchanged while removing one more fake read-side error
  boundary that only renamed an already-direct shared owner.

  This keeps active batch-generation query route behavior unchanged while
  shrinking the read-side access lane to the direct project-access error
  owner. Stop-rule for this lane: only return if another active/read-side
  helper still exists solely to rename one direct project-access or owned-task
  error before one immediate consumer; do not continue here for broader query
  renaming once remaining owners add real branch, payload, or compatibility
  semantics.

- 2026-05-24 22:32 +08:00 checkpoint:
  this slice moved to the active batch-generation task-list internal-error
  boundary, where the active-list query service still exposed a local
  `LoadActiveBatchGenerationTaskListQueryError` owner even though that owner
  only carried one `Internal(String)` variant and the route mapper immediately
  unwrapped that same string into `internal_detail_error(...)`. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs` showed that
  the real owner on this lane was already the direct read-context string error
  returned by `load_active_user_batch_generation_task_view_contexts(...)`, and
  the local enum added no branch, compatibility, or transport semantic before
  one immediate route mapper consumed it.

  The refactor therefore narrowed this lane to the direct internal-error
  owner: `load_owned_active_batch_generation_task_list_query()` now returns
  `Result<Value, String>` directly, and the batch-generation route error
  mapper now accepts that shared string error without the single-variant
  wrapper. This keeps active-task list payloads, route behavior, and internal
  error response payloads unchanged while removing one more fake read-side
  error boundary that only wrapped an already-direct internal failure string.

  This keeps active batch-generation task-list route behavior unchanged while
  shrinking the read-side access lane to the direct internal-error owner.
  Stop-rule for this lane: only return if another read-side helper still
  exists solely to wrap one direct internal failure string or shared access
  error before one immediate consumer; do not continue here for broader
  response/API renaming once remaining owners add real branch, payload, or
  compatibility semantics.

- 2026-05-24 22:49 +08:00 checkpoint:
  this slice moved to the batch-generation task-view context naming boundary,
  where the task-view query service still exposed a local
  `BatchGenerationTaskViewContext` owner even though that owner was only a
  direct alias to the existing `BatchGenerationReadContext` contract. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  showed that the real read-side owner on this lane was already the shared
  `BatchGenerationReadContext` object coming from the read-context service,
  while the task-view alias only re-labeled that exact shape before adjacent
  query helpers and payload builders immediately consumed it unchanged.

  The refactor therefore narrowed this lane to the direct read-context owner:
  the task-view query service, active-query service, and active-list query
  service now all consume `BatchGenerationReadContext` directly, and the local
  `BatchGenerationTaskViewContext` alias has been removed. This keeps
  read-context loading semantics, active/status payload construction, and
  route behavior unchanged while removing one more fake read-side context
  boundary that only renamed an already-direct shared owner.

  This keeps batch-generation read-side query route behavior unchanged while
  shrinking the remaining task-view lane to the direct read-context owner.
  Stop-rule update for this batch read-side track: the obvious fake aliases
  and single-variant wrappers are now largely exhausted; only return here if
  another seam clearly removes a direct owner rename or one-step wrapper
  without weakening payload or semantics ownership. Otherwise switch the next
  Phase 5 slice to single-generation or regeneration-adjacent services.

- 2026-05-24 23:05 +08:00 checkpoint:
  this slice moved to the single-generation execution-input construction
  boundary, where the prepare layer still exposed a local
  `build_single_chapter_generation_execution_input()` helper even though that
  helper only reassembled fields into the already-direct
  `SingleChapterGenerationExecutionInput` owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the real semantic owner on this lane was already the execution-
  input struct itself, while the helper added no validation, fallback, or
  shared branch semantic before the prepare workflow and batch-resume branch
  immediately consumed the same fields.

  The refactor therefore narrowed this lane to the direct execution-input
  owner: `prepare_single_chapter_generation_request()` and the
  single-chapter resume branch inside
  `build_resume_generation_execution_input()` now construct
  `SingleChapterGenerationExecutionInput` directly, and the local pure
  builder helper has been removed. This keeps single-generation prepare
  semantics, batch resume dispatch semantics, runtime launch inputs, and route
  behavior unchanged while removing one more pure construction helper that
  only restated an already-direct shared owner.

  This keeps single-generation and resume behavior unchanged while shrinking
  the execution-input construction lane to the direct owner. Stop-rule for
  this lane: only return if another single-generation or regeneration helper
  still exists solely to reconstruct one already-direct struct owner before an
  immediate consumer; do not continue here for broader constructor inlining
  once remaining helpers own real workflow, task, or compatibility semantics.

- 2026-05-24 23:24 +08:00 checkpoint:
  this slice moved to the partial-regeneration style error boundary, where the
  prepare layer still exposed a local `LoadPartialStyleContentError` owner
  even though that owner only carried one `InvalidStyle(String)` variant and
  every consumer immediately unwrapped that same string into either
  `PreparePartialRegenerationStreamError::Style(...)` or a `BAD_REQUEST`
  detail payload. A focused audit of
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/api/chapters_error_mapper.rs`, and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that the real owner on this lane was already the direct style-detail
  string coming from `WritingStyleService::get_style(...)`, while the local
  enum added no branch, compatibility, or transport semantic before adjacent
  workflow and route mappers consumed it unchanged.

  The refactor therefore narrowed this lane to the direct style-detail owner:
  `load_partial_style_content()` now returns `Result<Option<String>, String>`
  directly, `PreparePartialRegenerationStreamError::Style(...)` now carries
  that string directly, and the regeneration workflow tests plus chapters
  route error mapper now match the direct detail without the single-variant
  wrapper. This keeps partial-regeneration request validation, `BAD_REQUEST`
  status mapping, style-detail text, and regeneration stream workflow
  semantics unchanged while removing one more fake error boundary that only
  wrapped an already-direct shared string owner.

  This keeps partial-regeneration route and workflow behavior unchanged while
  shrinking the style-loading lane to the direct owner. Stop-rule for this
  lane: only return if another regeneration helper still exists solely to wrap
  one direct string/config/input owner before one immediate consumer; do not
  continue here for broader regeneration error renaming once remaining owners
  add real workflow or compatibility semantics.

- 2026-05-24 23:31 +08:00 checkpoint:
  this slice returned to the single-generation prepare lane, where the prepare
  service still exposed two local pure helpers,
  `build_single_chapter_generation_target()` and
  `load_chapter_generation_target()`, even though both helpers only
  reassembled already-direct owner fields before
  `prepare_single_chapter_generation_request()` immediately consumed the
  results. A focused audit of
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs` and
  the adjacent single-generation workflow/dispatch consumers showed that the
  real owners on this lane were already:
  - `SingleChapterGenerationTarget` for the chapter projection contract
  - `normalize_chapter_generation_target_word_count(...)` for the target-word
    normalization contract

  The refactor therefore narrowed this lane to those direct owners:
  `prepare_single_chapter_generation_request()` now constructs
  `SingleChapterGenerationTarget` inline and calls
  `normalize_chapter_generation_target_word_count(...)` directly when building
  `SingleChapterGenerationExecutionInput`, and the two local pure helper
  wrappers have been removed. This keeps single-generation prepare semantics,
  background/stream workflow behavior, runtime launch inputs, and route-facing
  payload behavior unchanged while removing two more pure construction helpers
  that only restated already-direct owner contracts.

  This keeps single-generation behavior unchanged while shrinking the prepare
  construction lane to the direct target/count owners. Stop-rule for this
  lane: only return if another single-generation helper still exists solely to
  restate one already-direct struct or normalization owner before an immediate
  consumer; do not keep inlining prepare/workflow code once the remaining
  helpers own real routing, task-planning, or runtime semantics.

- 2026-05-24 23:46 +08:00 checkpoint:
  this slice moved to the single-generation request construction boundary,
  where the request service still exposed a local
  `build_single_chapter_generation_request()` helper even though that helper
  only reassembled `target_word_count` and `model` into the already-direct
  `SingleChapterGenerationRequest` owner before
  `build_single_chapter_generation_request_from_route_payload()` immediately
  consumed it. A focused audit of
  `backend-rs/src/services/chapter_single_generation_request_service.rs` and
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  showed that the real owner on this lane was already the request struct
  itself, while the route-payload builder still retained the only meaningful
  compat boundary by explicitly consuming the route-only `enable_analysis`
  field.

  The refactor therefore narrowed this lane to the direct request owner:
  `build_single_chapter_generation_request_from_route_payload()` now
  constructs `SingleChapterGenerationRequest` inline and the local pure
  builder helper has been removed. This keeps route payload parsing,
  compat-only field consumption, single-generation write workflow behavior,
  and downstream prepare/runtime semantics unchanged while removing one more
  pure request-construction helper that only restated an already-direct owner.

  This keeps single-generation route and workflow behavior unchanged while
  shrinking the request-construction lane to the direct request owner.
  Stop-rule for this lane: only return if another single-generation request
  helper still exists solely to restate one already-direct request struct
  before an immediate consumer; do not continue here for broader request API
  reshaping once remaining helpers own real compatibility or workflow
  semantics.

- 2026-05-24 23:52 +08:00 checkpoint:
  this slice moved to the partial-regenerate apply request boundary, where the
  regeneration request service still exposed a local
  `build_apply_partial_regenerate_request()` helper even though that helper
  only copied `new_text/start_position/end_position` into the already-direct
  `ApplyPartialRegenerateRequest` owner before
  `build_apply_partial_regenerate_request_from_route_payload()` immediately
  consumed it. A focused audit of
  `backend-rs/src/services/chapter_regeneration_request_service.rs`,
  `backend-rs/src/api/chapter_regeneration_routes.rs`, and
  `backend-rs/src/services/chapter_regeneration_apply_service.rs` showed that
  the real owner on this lane was already the request struct itself: route
  payload parsing still belongs to the route/request boundary, while the apply
  service only consumes the direct owned request accessors.

  The refactor therefore narrowed this lane to the direct request owner:
  `build_apply_partial_regenerate_request_from_route_payload()` now
  constructs `ApplyPartialRegenerateRequest` inline, the local pure builder
  helper has been removed, and the request tests now protect the default
  request contract through `ApplyPartialRegenerateRequest::default()`. This
  keeps partial-regenerate route payload parsing, apply validation semantics,
  access behavior, and response payload behavior unchanged while removing one
  more pure request-construction helper that only restated an already-direct
  owner.

  This keeps partial-regenerate route and apply behavior unchanged while
  shrinking the apply-request lane to the direct request owner. Stop-rule for
  this lane: only return if another regeneration request helper still exists
  solely to restate one already-direct request struct before an immediate
  consumer; do not continue here for broader regeneration request reshaping
  once remaining helpers own real route, validation, or workflow semantics.

- 2026-05-25 00:08 +08:00 checkpoint:
  this slice returned to the regeneration stream request lane, where the
  request service still exposed two local pure builders,
  `build_full_chapter_regeneration_stream_request()` and
  `build_partial_regeneration_stream_workflow_request()`, even though both
  helpers only reassembled already-derived route fields into the already-
  direct owned request structs before one immediate `*_from_route_payload`
  consumer used them. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs` and
  `backend-rs/src/api/chapter_regeneration_routes.rs` showed that the real
  owners on this lane were already:
  - `FullChapterRegenerationStreamRequest` for the full-route typed request
    contract plus preserve-elements filtering/defaulting
  - `PartialRegenerationStreamWorkflowRequest` for the partial-route workflow
    request contract plus downstream context/web-research normalization

  The refactor therefore narrowed this lane to those direct request owners:
  `build_full_chapter_regeneration_stream_request_from_route_payload_typed()`
  now constructs `FullChapterRegenerationStreamRequest` inline after resolving
  `preserve_elements`, and
  `build_partial_regeneration_stream_workflow_request_from_route_payload()`
  now constructs `PartialRegenerationStreamWorkflowRequest` inline. The two
  local pure builder helpers have been removed, while the route-visible typed
  payloads, field filtering, defaults, and normalization accessors remain
  unchanged. This keeps regeneration stream route parsing, prompt-preparation
  inputs, partial web-research/context behavior, and downstream workflow
  semantics unchanged while removing two more pure request-construction
  helpers that only restated already-direct owned request contracts.

  This keeps regeneration stream route and request behavior unchanged while
  shrinking the stream-request lane to the direct request owners. Stop-rule
  for this lane: only return if another regeneration stream request helper
  still exists solely to restate one already-direct request struct before an
  immediate consumer; do not continue here for broader request-accessor or
  route-shape edits once remaining helpers own real filtering, normalization,
  or compatibility semantics.

- 2026-05-24 22:40 +08:00 checkpoint:
  this slice returned to the single-generation write lane after the earlier
  request-builder cleanup. A focused audit of
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/api/chapter_batch_generation.rs` showed that the remaining
  write-workflow layer no longer owned any workflow semantics: both exported
  functions only accepted the route payload, rebuilt the already-direct
  `SingleChapterGenerationRequest` through
  `build_single_chapter_generation_request_from_route_payload()`, and
  immediately forwarded to the real workflow owners
  `start_owned_single_generation_background_workflow()` /
  `create_single_generation_stream_workflow()`.

  The refactor therefore removed that last thin write-workflow shell. The
  chapter-generation routes now build the owned single-generation request
  directly and call the real workflow owners without the extra pass-through
  boundary, and the now-empty
  `chapter_single_generation_write_workflow_service.rs` module has been
  deleted from `services/mod.rs`. This keeps single-generation route payload
  parsing, error mapping, response shape, SSE transport behavior, prepare
  semantics, and runtime launch semantics unchanged while removing one more
  fake owner boundary from the write lane.

  In parallel, the Phase 5 master plan in
  `docs/architecture/rust-strangler-refactor-plan-2026-05-17.zh-CN.md` now
  records the formal three-layer migration accounting rule:
  - API owner remaining: low to medium
  - Python fallback retirement remaining: medium to high
  - schema / migration owner switch remaining: high

  This keeps future progress reports aligned with through-gateway owner,
  fallback, and schema reality instead of using a misleading single
  “remaining percentage”. Stop-rule for this lane: only return if another
  single-generation route/write helper still exists solely to restate one
  already-direct request/workflow owner before an immediate consumer; do not
  continue here once the remaining boundaries own real preparation, task-plan,
  or runtime semantics.

- 2026-05-24 22:48 +08:00 checkpoint:
  this slice returned to the batch-generation create request lane, where
  `chapter_batch_generation_request_compat_service.rs` still exposed a local
  `build_batch_generation_create_workflow_request()` helper even though that
  helper only reassembled already-derived route fields into the already-direct
  `BatchGenerationCreateWorkflowRequest` owner before one immediate
  `build_batch_generation_create_workflow_request_from_route_payload(...)`
  consumer used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the real owner on this lane was already the workflow request
  struct itself, while the route-payload boundary still retained the only
  meaningful compat semantics by explicitly ignoring route-only legacy fields
  and normalizing `enable_analysis` / `max_retries`.

  The refactor therefore narrowed this lane to the direct request owner:
  `build_batch_generation_create_workflow_request_from_route_payload(...)`
  now constructs `BatchGenerationCreateWorkflowRequest` inline, the local pure
  builder helper has been removed, and focused tests now protect both the
  normal populated path and the minimal normalized request contract through
  the route-payload builder itself. This keeps batch-create route payload
  parsing, ignored compat field behavior, downstream create-workflow
  preparation, error mapping, and runtime launch semantics unchanged while
  removing one more pure request-construction helper that only restated an
  already-direct owner.

  This keeps batch-create route and workflow behavior unchanged while
  shrinking the request-construction lane to the direct workflow request
  owner. Stop-rule for this lane: only return if another batch create request
  helper still exists solely to restate one already-direct request struct
  before an immediate consumer; do not continue here for broader compat-field
  policy reshaping once remaining helpers own real normalization,
  preparation, or workflow semantics.

- 2026-05-24 22:51 +08:00 checkpoint:
  this slice moved to the batch-generation dispatch/runtime-launch lane,
  where `chapter_batch_generation_dispatch_service.rs` still exposed a local
  `build_batch_generation_execution_input(...)` helper even though that
  helper only copied `user_id/chapter_ids/target_word_count` together with the
  already-prepared execution config fields into the already-direct
  `BatchGenerationExecutionInput` owner before one immediate consumer used it.
  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that the real owners on this lane were already:
  - `BatchGenerationExecutionInput` for the create-runtime launch contract
  - `build_resume_generation_execution_input(...)` for the still-meaningful
    resume branch split between single-chapter and batch execution plans

  The refactor therefore narrowed this lane to those direct owners:
  `launch_owned_batch_generation_create_runtime(...)` now constructs
  `BatchGenerationExecutionInput` inline because the create launch owner
  already holds the full input fields once, and
  `build_resume_generation_execution_input(...)` now also constructs the batch
  branch inline instead of round-tripping through the removed pure
  constructor. This keeps create/resume runtime launch behavior,
  `PreparedGenerationExecutionConfig` ownership, dispatch behavior, and task
  execution semantics unchanged while removing one more pure execution-input
  helper that only restated an already-direct owner.

  This keeps batch runtime launch behavior unchanged while shrinking the
  create/resume execution-input lane to the direct launch owner plus the
  meaningful resume branch owner. Stop-rule for this lane: only return if
  another batch runtime-launch helper still exists solely to restate one
  already-direct execution input before an immediate consumer; do not
  continue here once the remaining helpers still own real resume branching,
  dispatch, or runtime side effects.

- 2026-05-24 22:54 +08:00 checkpoint:
  this slice returned to the batch-generation read-side status-query lane,
  where `chapter_batch_generation_status_query_service.rs` still exposed a
  local `load_batch_generation_status_query(...)` helper even though that
  helper only loaded the already-direct task-view context through
  `load_required_batch_generation_task_view_context(...)` and immediately
  rebuilt the already-direct status payload through
  `build_batch_generation_status_query_response(...)` before one route
  consumer used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the real
  owners on this lane were already:
  - `load_required_batch_generation_task_view_context(...)` for the read-side
    owned-task context contract
  - `build_batch_generation_status_query_response(...)` for the status payload
    contract

  The refactor therefore removed that thin status-query wrapper. The batch
  status route now calls the task-view context owner directly and immediately
  builds the status payload through the existing task-view payload owner, and
  the now-empty `chapter_batch_generation_status_query_service.rs` module has
  been deleted from `services/mod.rs`. This keeps batch status route payload
  shape, owned-task access semantics, error mapping, and read-side status
  behavior unchanged while removing one more fake query owner boundary.

  This keeps batch status route and read-side behavior unchanged while
  shrinking the status-query lane to the direct context and payload owners.
  Stop-rule for this lane: only return if another read-side status helper
  still exists solely to restate one already-direct query context or payload
  owner before an immediate route consumer; do not continue here once the
  remaining helpers still own real access checks, query composition, or
  response-envelope semantics.

- 2026-05-24 22:59 +08:00 checkpoint:
  this slice stayed in the batch-generation read-side lane and moved to the
  active-task list query path, where
  `chapter_batch_generation_active_list_query_service.rs` still exposed a
  local `load_owned_active_batch_generation_task_list_query(...)` helper even
  though that helper only loaded the already-direct active task-view contexts
  through `load_active_user_batch_generation_task_view_contexts(...)` and
  immediately rebuilt the already-direct list envelope through
  `build_active_batch_generation_task_list_query_response(...)` before one
  route consumer used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the real
  owners on this lane were already:
  - `load_active_user_batch_generation_task_view_contexts(...)` for the
    active-list read-side context loading contract
  - `build_active_batch_generation_task_list_query_response(...)` for the
    `total/items` list envelope contract

  The refactor therefore removed that thin active-list query wrapper. The
  active-task list route now calls the active task-view context owner
  directly, then builds the `total/items` response through the existing list
  envelope owner, and the now-empty
  `chapter_batch_generation_active_list_query_service.rs` module has been
  deleted from `services/mod.rs`. During validation, the first compile attempt
  surfaced one follow-up correction: the list envelope owner also needed to be
  moved into `chapter_batch_generation_task_view_query_service.rs` before the
  route import could be updated cleanly. That fix was applied in the same
  slice, keeping the final owner layout coherent.

  This keeps active-task list route payload shape, normalized limit handling,
  read-side context loading, error mapping, and response-envelope behavior
  unchanged while removing one more fake query owner boundary. Stop-rule for
  this lane: only return if another active-list helper still exists solely to
  restate one already-direct read context or list-envelope owner before an
  immediate route consumer; do not continue here once remaining helpers still
  own real query composition, access policy, or envelope semantics.

- 2026-05-24 23:02 +08:00 checkpoint:
  this slice stayed in the batch-generation read-side lane and narrowed the
  remaining status-view context boundary. After the earlier removal of
  `chapter_batch_generation_status_query_service.rs`, the task-view module
  still exposed one local
  `load_required_batch_generation_task_view_context(...)` helper even though
  that helper only forwarded the owned-task lookup to the already-direct
  read-context owner
  `load_required_owned_batch_generation_read_context(...)` before one route
  consumer used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the real
  owners on this lane were already:
  - `load_required_owned_batch_generation_read_context(...)` for the
    required owned-task read-context contract
  - `build_batch_generation_status_query_response(...)` for the status payload
    contract

  The refactor therefore removed that thin task-view context wrapper. The
  batch status route now calls the read-context owner directly, while the
  task-view module remains responsible for status payload assembly, active
  task payload assembly, list envelope assembly, and active query composition.
  This keeps batch status route payload shape, owned-task access semantics,
  error mapping, and read-side status behavior unchanged while removing one
  more fake read-side owner boundary.

  This keeps batch status route and task-view behavior unchanged while
  shrinking the required-context lane to the direct read-context owner plus
  the direct payload owner. Stop-rule for this lane: only return if another
  task-view helper still exists solely to restate one already-direct
  read-context owner before an immediate route consumer; do not continue here
  once the remaining helpers still own real query composition, payload
  assembly, or response-envelope semantics.

- 2026-05-24 23:06 +08:00 checkpoint:
  this slice moved back to the batch-generation runtime/resume lane, where
  `chapter_batch_generation_resume_semantics_service.rs` still exposed a
  local `resume_runtime_position(...)` helper even though that helper only
  re-packed the already-direct
  `resolve_resume_runtime_semantics(...)` result into a tuple before two
  immediate runtime consumers used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that the real owner on this lane was already
  `resolve_resume_runtime_semantics(...)`, because it directly owns the
  resume checkpoint pointer plus the `include_progress_totals` contract for
  single-chapter vs batch resume behavior.

  The refactor therefore removed `resume_runtime_position(...)`. The runtime
  state reset plan now reads `current_chapter_id/current_chapter_number`
  directly from `resolve_resume_runtime_semantics(...)`, and the focused tests
  now assert the semantics owner directly instead of round-tripping through a
  tuple helper. This keeps batch resume reset behavior, checkpoint pointer
  clearing, and single-chapter pointer preservation unchanged while removing
  one more fake runtime-position wrapper around an already-direct semantics
  owner.

  Focused validation passed:
  `cargo fmt --manifest-path "backend-rs/Cargo.toml"`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_resume_semantics_service -- --nocapture`,
  `cargo test --manifest-path "backend-rs/Cargo.toml"
  chapter_batch_generation_runtime_state_service -- --nocapture`,
  and `cargo check --manifest-path "backend-rs/Cargo.toml"`.

  Stop-rule for this lane: only return if another runtime/resume helper still
  exists solely to restate one already-direct resume semantics owner before an
  immediate consumer; do not continue here once the remaining helpers still
  own real single/batch execution branching, checkpoint stage ownership, or
  runtime snapshot side effects.

- 2026-05-24 23:18 +08:00 checkpoint:
  this slice stayed in the batch-generation runtime/snapshot lane and removed
  two more thin wrappers that only restated already-direct checkpoint owners:
  `build_resume_batch_generation_runtime_checkpoint(...)` in
  `chapter_batch_generation_runtime_checkpoint_service.rs` and
  `persist_new_batch_generation_task_snapshot(...)` in
  `chapter_batch_generation_snapshot_service.rs`. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs`
  showed that the real owners on this lane were already:
  - `resolve_resume_runtime_semantics(...)` plus
    `build_batch_generation_runtime_checkpoint_for_stage(...)` for the resumed
    checkpoint contract
  - `upsert_batch_generation_runtime_snapshot(...)` plus
    `build_batch_generation_runtime_checkpoint_for_stage(...)` for the queued
    task snapshot contract

  The refactor therefore narrowed both call sites to those direct owners.
  `reset_batch_generation_task_for_resume(...)` now resolves resume semantics
  locally and immediately builds the resumed checkpoint through the shared
  stage owner before replacing the runtime snapshot. Likewise,
  `create_batch_generation_task_plan(...)` now directly upserts the queued
  runtime snapshot through the shared queued-stage checkpoint builder instead
  of round-tripping through a route-local persistence wrapper. This keeps
  batch create pending snapshot shape, batch resume reset behavior, progress
  totals semantics, and runtime snapshot persistence unchanged while removing
  two more fake helper boundaries from the runtime/snapshot lane.

  Stop-rule for this lane: only return if another runtime/snapshot helper
  still exists solely to restate one already-direct checkpoint-stage or
  snapshot-write owner before an immediate consumer; do not continue here once
  the remaining helpers still own real stage planning, write-mode behavior, or
  runtime side effects.

- 2026-05-24 23:33 +08:00 checkpoint:
  this slice moved to the active-task list route-input lane, where
  `chapter_batch_generation_active_list_request_service.rs` still exposed a
  local
  `build_active_batch_generation_task_list_request_from_route_query(...)`
  helper even though that helper only unpacked `route_query.limit` and
  immediately forwarded it to the already-direct normalization owner
  `normalize_active_batch_generation_task_list_limit(...)` before one route
  consumer used it. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_list_request_service.rs`
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the real
  owner on this lane was already the limit-normalization helper itself,
  because it directly owns the default/min/max contract for the active-task
  list query.

  The refactor therefore removed that route-shaped wrapper. The batch active
  task list route now passes `query.limit` directly into
  `normalize_active_batch_generation_task_list_limit(...)`, and the focused
  test in the request service now asserts the normalization owner directly
  rather than round-tripping through a route-only helper. This keeps the
  active-task list query default limit, clamping behavior, route query shape,
  and downstream list response behavior unchanged while removing one more fake
  request-boundary owner.

  Stop-rule for this lane: only return if another route-input helper still
  exists solely to unwrap one field and restate an already-direct validation
  or normalization owner before an immediate consumer; do not continue here
  once the remaining helpers still own real route payload contracts or query
  normalization semantics.

- 2026-05-24 23:47 +08:00 checkpoint:
  this slice moved to the single-generation route-input lane inside
  `backend-rs/src/api/chapter_batch_generation.rs`, where the background and
  stream route handlers still each performed their own local
  `build_single_chapter_generation_request_from_route_payload(body)` call
  before immediately forwarding the resulting shared request into the already
  direct single-generation workflow owners. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs` and
  `backend-rs/src/services/chapter_single_generation_request_service.rs`
  showed that the real request-normalization owner on this lane was already
  `build_single_chapter_generation_request_from_route_payload(...)`, while the
  route layer was only duplicating the same transport-to-request conversion in
  two sibling handlers.

  The refactor therefore narrowed that duplication to one route-local helper:
  `build_single_generation_route_request(...)`. Both
  `generate_chapter_content_background(...)` and
  `generate_chapter_content_stream(...)` now reuse the same route-input
  conversion before handing off to their existing workflow owners. This keeps
  the single-generation route payload contract, workflow entrypoint behavior,
  error mapping, and background/SSE runtime behavior unchanged while removing
  one more repeated route-local request conversion from the chapter-generation
  transport lane.

  Stop-rule for this lane: only return if another sibling single-generation
  route still duplicates the same direct transport-to-request conversion before
  an immediate workflow owner; do not continue here once the remaining helpers
  still own meaningful transport branching or workflow selection.

- 2026-05-24 23:56 +08:00 checkpoint:
  this slice stayed in the single-generation create/runtime lane and removed a
  thin pending-snapshot helper from
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`.
  `persist_new_single_generation_task_snapshot(...)` still existed even though
  it had only one caller and only restated two already-direct owners:
  `build_single_generation_runtime_checkpoint_for_stage(...)` for the pending
  checkpoint contract and `upsert_batch_generation_runtime_snapshot(...)` for
  the snapshot write contract. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the task-create lane already held the full pending snapshot
  inputs directly: `task_id`, `chapter_id`, and `chapter_number`.

  The refactor therefore removed
  `persist_new_single_generation_task_snapshot(...)`. The single-generation
  background task create plan now directly upserts the pending runtime
  snapshot through the shared pending-stage checkpoint builder and the shared
  snapshot write owner. This keeps single-generation background task creation,
  pending checkpoint shape, and runtime snapshot persistence unchanged while
  removing one more fake helper boundary from the create/runtime seam.

  Stop-rule for this lane: only return if another single-generation create or
  runtime helper still exists solely to restate one already-direct checkpoint
  stage or snapshot-write owner before an immediate consumer; do not continue
  here once the remaining helpers still own real stage transitions, task
  mutation semantics, or runtime side effects.

- 2026-05-25 00:07 +08:00 checkpoint:
  this slice returned to the batch-generation optional read-context lane,
  where `chapter_batch_generation_read_context_service.rs` still exposed
  `load_owned_batch_generation_read_context(...)` even though that helper had
  only one caller and only restated two already-direct owners:
  `load_owned_task(...)` for optional owned-task lookup and
  `load_batch_generation_read_context_for_task(...)` for snapshot/quality
  context loading. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  showed that the optional stream-state path already held the full inputs
  needed to call those owners directly: `db`, `batch_id`, and `user_id`.

  The refactor therefore removed `load_owned_batch_generation_read_context(...)`.
  `load_batch_generation_stream_state(...)` now performs the optional owned-task
  lookup directly through `load_owned_task(...)`, then immediately loads the
  read context through `load_batch_generation_read_context_for_task(...)`
  before building the stream state. This keeps optional stream access
  behavior, not-found semantics, snapshot/quality context loading, and stream
  state semantics unchanged while removing one more fake optional read-context
  boundary from the batch-generation stream lane.

  Stop-rule for this lane: only return if another optional stream/read helper
  still exists solely to restate one already-direct owned-task lookup or
  read-context owner before an immediate consumer; do not continue here once
  the remaining helpers still own real access semantics, stream-state
  projection, or snapshot/quality composition.

- 2026-05-25 00:18 +08:00 checkpoint:
  this slice moved back to the batch-generation create-plan lane, where
  `chapter_batch_generation_create_task_command_service.rs` still exposed
  `collect_batch_generation_plan_chapter_ids(...)` even though that helper
  only projected `prepared.chapters_to_generate[*].id` into a `Vec<String>`
  before one immediate consumer used it inside
  `create_batch_generation_task_plan(...)`. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  showed that the create-plan owner already held the full source collection and
  that the helper did not own any validation, ordering, or compatibility
  semantics beyond the direct `id.clone()` projection.

  The refactor therefore removed `collect_batch_generation_plan_chapter_ids(...)`.
  `create_batch_generation_task_plan(...)` now projects `chapter_ids` inline
  next to the existing `chapter_id_values` assembly before persisting the task
  and returning the create plan. This keeps create-plan ordering, batch task
  creation behavior, returned `chapter_ids`, and queued snapshot persistence
  unchanged while removing one more fake plan helper boundary from the batch
  create lane.

  Stop-rule for this lane: only return if another create-plan helper still
  exists solely to restate one already-direct field projection before an
  immediate consumer; do not continue here once the remaining helpers still
  own real task-model assembly, validation, or persistence semantics.

- 2026-05-24 23:43 +08:00 checkpoint:
  this slice returned to the single-generation runtime-launch lane, where
  `SingleGenerationRuntimeLaunchInput` was already the real owner of the
  runtime dispatch contract but its construction still appeared as repeated
  struct literals across sibling consumers in
  `chapter_single_generation_workflow_service.rs` and
  `chapter_batch_generation_dispatch_service.rs`. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that those call sites were not adding lane-specific validation or
  branching semantics; they were only restating the same `user_id` +
  `execution_input` assembly before immediately handing the launch object to
  the existing runtime owners.

  The refactor therefore added
  `SingleGenerationRuntimeLaunchInput::new(...)` on the dispatch owner and
  moved both workflow/resume call sites over to that shared constructor. This
  keeps single-generation stream launch behavior, background runtime dispatch,
  resume execution input branching, and the launch-input data contract
  unchanged while removing one more sibling transport assembly seam from the
  runtime-dispatch lane.

  Stop-rule for this lane: only return if another caller still reconstructs
  the single-generation runtime launch contract solely to restate the same
  direct fields before an immediate dispatch/stream owner; do not continue
  here once the remaining helpers still own real workflow branching, runtime
  orchestration, or execution-input semantics.

- 2026-05-24 23:49 +08:00 checkpoint:
  this slice returned to the batch-generation create runtime-launch lane,
  where `BatchGenerationExecutionInput` was already the real owner of the
  batch runtime dispatch contract but the create path inside
  `chapter_batch_generation_runtime_launch_service.rs` still assembled it as a
  local struct literal while the resume path already delegated through the
  dispatch-side builder boundary. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs` and
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  showed that the create launch path was not adding any lane-specific
  semantics; it was only restating `user_id`, `chapter_ids`,
  `target_word_count`, and the prepared execution config before immediately
  forwarding the object to the existing dispatch owner.

  The refactor therefore added `BatchGenerationExecutionInput::new(...)` on
  the dispatch owner and moved both the create launch path and the dispatch
  service's own resume batch arm over to that shared constructor. The
  single-generation stream-launch contract test was also moved to
  `SingleGenerationRuntimeLaunchInput::new(...)` so both launch-input owner
  seams now assert the constructor boundary directly. This keeps batch create
  runtime launch behavior, resume execution branching, explicit execution-config
  flow, and single-generation stream launch semantics unchanged while removing
  one more repeated execution-input assembly seam from the batch launch lane.

  Stop-rule for this lane: only return if another batch create/resume caller
  still reconstructs the same execution-input contract solely to restate
  already-direct fields before an immediate dispatch/launch owner; do not
  continue here once the remaining helpers still own real workflow branching,
  launch orchestration, or runtime execution semantics.

- 2026-05-24 23:58 +08:00 checkpoint:
  this slice returned to the single-generation task-create lane, where
  `chapter_single_generation_task_command_service.rs` still assembled the
  single background task active model inline from `chapter_target` fields even
  though that projection no longer owned any distinct semantics beyond the
  direct single-generation-to-batch-task adaptation. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the same field unpacking pattern appeared both in production code
  and in the focused defaults test, while the real owner on this lane was
  already the single-generation task-command module itself rather than the
  downstream shared batch active-model builder.

  The refactor therefore added
  `build_single_generation_background_task_active_model(...)` as an owner-local
  helper inside `chapter_single_generation_task_command_service.rs`, then moved
  both task creation and the focused active-model defaults test over to that
  shared boundary. This keeps single-generation background task creation,
  chapter payload shape, pending snapshot persistence, and task default fields
  unchanged while removing one more repeated field-projection seam from the
  single task-create lane.

  Stop-rule for this lane: only return if another single-generation task-create
  caller still restates the same `chapter_target` -> active-model projection
  solely to feed the shared batch task builder before an immediate consumer; do
  not continue here once the remaining helpers still own real persistence,
  snapshot, or workflow orchestration semantics.

- 2026-05-25 00:05 +08:00 checkpoint:
  this slice returned to the single-generation stream-workflow lane, where
  `create_single_generation_stream_workflow(...)` still exposed a
  function-returning-`impl Future` wrapper even though that boundary no longer
  owned any deferred branching semantics beyond the direct async workflow body.
  A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs` and
  its sole route caller in `backend-rs/src/api/chapter_batch_generation.rs`
  showed that the workflow only prepared the request, assembled the explicit
  launch input, and returned the owned stream immediately; the extra `async
  move` wrapper no longer represented a meaningful ownership split.

  The refactor therefore narrowed
  `create_single_generation_stream_workflow(...)` to a plain `async fn`
  returning the same stream/result contract directly. This keeps
  single-generation stream request preparation, launch-input assembly, route
  await behavior, SSE payload shape, and runtime semantics unchanged while
  removing one more fake future boundary from the single stream-workflow lane.

  Stop-rule for this lane: only return if another single-generation workflow
  helper still wraps an already-direct async body solely to restate the same
  result contract before one immediate caller; do not continue here once the
  remaining workflow owners still express real branching, orchestration, or
  transport/runtime coordination semantics.

- 2026-05-25 00:09 +08:00 checkpoint:
  this slice returned to the batch-generation create request-compat lane,
  where `chapter_batch_generation_request_compat_service.rs` still kept two
  file-private normalization helpers for `enable_analysis` and `max_retries`
  even though both were only consumed once by
  `build_batch_generation_create_workflow_request_from_route_payload(...)` and
  no longer owned any independent branch semantics beyond the direct default
  contract (`false` / `3`). A focused audit of
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  and its route caller in `backend-rs/src/api/chapter_batch_generation.rs`
  showed that the real owner on this lane was already the route-payload
  request builder itself, while the compatibility policy stayed unchanged:
  ignore legacy route-only fields and finalize the workflow request defaults
  once.

  The refactor therefore removed
  `normalize_batch_generation_enable_analysis(...)` and
  `normalize_batch_generation_max_retries(...)`, then inlined those defaults
  directly inside
  `build_batch_generation_create_workflow_request_from_route_payload(...)`.
  Focused tests now protect the populated path and minimal request contract
  through the real builder owner instead of separate helper-only assertions.
  This keeps batch-create route payload parsing, ignored compat field behavior,
  default `enable_analysis` / `max_retries` semantics, downstream write
  workflow preparation, and runtime launch behavior unchanged while removing
  one more fake normalization boundary from the batch request-compat lane.

  Stop-rule for this lane: only return if another batch create compat helper
  still restates a direct request-builder default or field-finalization
  contract before one immediate consumer; do not continue here once remaining
  helpers still own meaningful normalization policy, compat-field consumption,
  or workflow preparation semantics.

- 2026-05-25 00:15 +08:00 checkpoint:
  this slice returned to the batch-generation status-stream required-read lane,
  where `chapter_batch_generation_status_view_service.rs` still exposed
  `load_required_batch_generation_stream_state(...)` even though that helper
  had only one caller in
  `chapter_batch_generation_status_stream_service.rs` and only restated two
  already-direct owners: required owned read-context loading through
  `load_required_owned_batch_generation_read_context(...)` and stream-state
  assembly through `build_batch_generation_stream_state(...)`. A focused audit
  of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
  showed that the required stream path no longer added any lane-specific error
  translation or composition semantics beyond the direct read-context lookup
  plus state builder call.

  The refactor therefore removed
  `load_required_batch_generation_stream_state(...)`. The owned status-stream
  loader now performs the required read-context lookup directly and then hands
  that context into `build_batch_generation_stream_state(...)`, which remains
  the real owner of stream-state semantic assembly. This keeps owned stream
  access behavior, not-found/internal error mapping, initial stream-state
  semantics, polling behavior, and downstream SSE event resolution unchanged
  while removing one more fake required-read wrapper from the batch
  status-stream lane.

  Stop-rule for this lane: only return if another status-stream helper still
  restates the same required read-context lookup plus immediate
  stream-state-builder call before one direct consumer; do not continue here
  once remaining helpers still own real stream polling, event resolution, or
  read-side semantic composition.

- 2026-05-25 00:25 +08:00 checkpoint:
  this slice revisited the single-generation route request lane after an audit
  of `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`
  showed that `build_active_batch_generation_query_response(...)` still owns a
  real top-level `/active` compat envelope
  (`has_active_task` plus nullable `task`) and therefore should not be treated
  as fake response glue. The next low-risk seam was instead in
  `backend-rs/src/api/chapter_batch_generation.rs`, where
  `build_single_generation_route_request(...)` had become a pure route-local
  rename wrapper with two immediate callers and no added request semantics
  beyond forwarding the body into the existing request owner
  `build_single_chapter_generation_request_from_route_payload(...)`.

  The refactor therefore removed
  `build_single_generation_route_request(...)` and moved both the background
  and stream single-generation route entrypoints to call
  `build_single_chapter_generation_request_from_route_payload(...)` directly.
  This keeps the single-chapter route payload contract, ignored compat-only
  `enable_analysis` field behavior, workflow preparation boundary, background
  dispatch behavior, and stream launch semantics unchanged while removing one
  more zero-semantic forwarding helper from the route lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another route-local helper still
  renames or rewraps the same service-owned request builder for immediate
  sibling callers without adding transport validation or compatibility policy;
  do not continue here once the remaining route code still owns actual HTTP /
  SSE transport wiring or explicit delegation boundaries.

- 2026-05-25 00:32 +08:00 checkpoint:
  this slice returned to the batch-generation status-stream polling lane,
  where `chapter_batch_generation_status_view_service.rs` still exposed
  `load_batch_generation_stream_state(...)` even after the required-read path
  had already been narrowed in the previous checkpoint. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  and `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
  showed that the optional polling path helper had only one caller and no
  longer owned any distinct stream semantics: it only performed the owned-task
  optional lookup through `load_owned_task(...)`, then immediately forwarded
  the task into `load_batch_generation_read_context_for_task(...)` and finally
  into `build_batch_generation_stream_state(...)`.

  The refactor therefore removed `load_batch_generation_stream_state(...)`.
  The polling branch inside `build_batch_generation_status_stream(...)` now
  performs the optional owned-task lookup directly and, when present, hands
  the loaded read-context into `build_batch_generation_stream_state(...)`,
  which remains the real owner of stream-state semantic assembly. This keeps
  owned polling behavior, not-found fallback behavior, initial-state reuse,
  SSE event resolution, and terminal stream handling unchanged while removing
  one more optional wrapper from the batch status-stream lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_view_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another polling helper still
  restates the same optional owned-task lookup plus immediate read-context /
  stream-state-builder call before one direct consumer; do not continue here
  once remaining polling code still owns real retry timing, event batching,
  or terminal stream control semantics.

- 2026-05-25 00:36 +08:00 checkpoint:
  this slice returned to the chapter-generation access lane, where
  `chapter_batch_generation_access_service.rs` still kept
  `verify_project_access(...)` even though that helper had narrowed to a
  single caller inside `load_accessible_chapter_for_generation(...)` and no
  longer owned any independent access policy beyond a direct
  `project_id + user_id` existence check. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_access_service.rs`,
  `backend-rs/src/services/chapter_generation_context_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  showed that the real owner on this lane was already
  `load_accessible_chapter_for_generation(...)`, which still owns the
  externally visible chapter-not-found vs access-denied compatibility
  contract.

  The refactor therefore removed `verify_project_access(...)` and inlined the
  project ownership lookup directly into
  `load_accessible_chapter_for_generation(...)`. This keeps chapter lookup
  behavior, access-denied error mapping, generation-context loading, and
  single-generation request preparation unchanged while removing one more
  single-caller boolean access wrapper from the chapter-generation access
  lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_access_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another chapter-generation access
  helper still restates the same direct project-ownership existence check for
  one immediate consumer without adding distinct compatibility or domain
  semantics; do not continue here once the remaining access helpers still own
  visible error vocabulary or cross-entity loading behavior.

- 2026-05-25 00:52 +08:00 checkpoint:
  this slice returned to the batch-generation response-adapter lane, where
  `chapter_batch_generation_command_payload_adapter_service.rs` still kept
  `build_resume_batch_generation_envelope(...)` even though that helper had
  narrowed to a single production caller inside
  `build_resume_batch_generation_response_payload(...)` and no longer owned
  any branching or compatibility policy beyond re-inserting
  `batch_id / total_chapters / completed_chapters / message` into an already
  built runtime payload map. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  showed that the real owner on this lane was already the resume response
  adapter itself, while the helper only acted as a fake envelope wrapper.

  The refactor therefore removed
  `build_resume_batch_generation_envelope(...)` and inlined those outer resume
  metadata fields directly into
  `build_resume_batch_generation_response_payload(...)`. The helper-only test
  was dropped, and focused tests now protect the shared totals/message
  contract through the real response owner. This keeps resume response shape,
  checkpoint metadata, workflow response assembly, and downstream route
  behavior unchanged while removing one more single-caller envelope wrapper
  from the batch response-adapter lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another response-adapter helper
  still wraps an already-built payload map solely to restate the same outer
  metadata for one immediate caller without adding distinct compatibility or
  branching semantics; do not continue here once the remaining helpers still
  own real payload-contract variation or shared response composition.

- 2026-05-25 00:56 +08:00 checkpoint:
  this slice stayed on the same batch-generation response-adapter lane after
  the resume envelope wrapper removal and revisited the remaining outer
  metadata helper
  `build_batch_generation_task_summary_payload(...)`. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that this helper had also narrowed to a single production caller
  inside `build_cancel_batch_generation_response_payload(...)` and no longer
  owned any branching or compatibility policy beyond assembling
  `batch_id / total_chapters / completed_chapters / message` into a plain map.

  The refactor therefore removed
  `build_batch_generation_task_summary_payload(...)` and inlined those outer
  cancel-response metadata fields directly into
  `build_cancel_batch_generation_response_payload(...)`. The helper-only test
  was dropped, and focused tests now protect the cancel response shape through
  the real response owner. This keeps cancel response payload semantics,
  workflow error ownership, downstream route behavior, and the sibling resume
  adapter contract unchanged while removing one more single-caller summary
  wrapper from the batch response-adapter lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another response-adapter helper
  still wraps a plain metadata map solely to restate the same outer fields for
  one immediate caller without adding distinct compatibility or branching
  semantics; do not continue here once the remaining helpers still own real
  payload-contract variation or shared response composition.

- 2026-05-25 01:03 +08:00 checkpoint:
  this slice returned to the batch-generation active-query read lane after the
  response-adapter signal weakened, but it deliberately avoided the top-level
  `/active` compat envelope because
  `build_active_batch_generation_query_response(...)` still owns the external
  `has_active_task + nullable task` contract. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`
  and `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that the actual remaining fake wrapper on this lane was
  `load_active_project_batch_generation_task_view_context(...)`, which had
  narrowed to a single production caller and no longer owned any shared query
  policy beyond applying the project filter to the already-owned active-task
  base query and then optionally loading read-context for the matched task.

  The refactor therefore removed
  `load_active_project_batch_generation_task_view_context(...)`. The
  project-specific optional task lookup now happens directly inside
  `load_active_batch_generation_query(...)`, which still keeps project access
  verification and the top-level active-query response envelope in the same
  owner. The shared base query builder
  `build_active_batch_generation_task_query(...)` remains in
  `chapter_batch_generation_task_view_query_service.rs` as the true shared
  query owner for active-task list and active-project lookup paths. This keeps
  `/active` response shape, project access error mapping, active-task query
  semantics, and downstream route behavior unchanged while removing one more
  single-caller context wrapper from the active-query read lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another active-query helper still
  restates the same project-specific optional task lookup plus immediate
  read-context load before one direct consumer without owning the top-level
  response envelope or shared base query policy; do not continue here once the
  remaining helpers still own visible compat payload shape or shared active
  query semantics.

- 2026-05-25 01:09 +08:00 checkpoint:
  this slice stayed on the batch-generation active-task list read lane after
  the project-specific active-query wrapper removal, but it deliberately
  avoided the top-level `/active` compat envelope and the shared active-task
  base query owner because those still carry visible response or query
  semantics. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the actual
  remaining fake wrapper on this lane was the route/read-side pair
  `load_active_user_batch_generation_task_view_contexts(...) +
  build_active_batch_generation_task_list_query_response(...)`, which had
  narrowed to one immediate production consumer in
  `list_active_batch_generation_tasks(...)` and no longer owned any shared
  policy beyond:
  - loading the active-task query result
  - resolving read-contexts for those tasks
  - immediately returning the already-owned `{ total, items }` list envelope

  The refactor therefore collapsed that pair into one service-owned helper:
  `load_active_user_batch_generation_task_list_query(...)`. The route now
  delegates directly to this owner, while
  `build_active_batch_generation_task_query(...)` remains the shared base
  query owner and `build_active_batch_generation_task_list_query_response(...)`
  remains the list-envelope owner inside the same service module. This keeps
  the `/chapters/batch-generate/active-tasks` payload shape, limit
  normalization, active-task query semantics, read-context loading behavior,
  and downstream route error mapping unchanged while removing one more
  route-local glue seam from the active-task list read lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another active-task list helper
  still restates the same immediate `query -> read-context load -> existing
  list envelope` flow for one direct consumer without owning the shared base
  query or visible compat response shape; do not continue here once the
  remaining helpers still own real shared query or payload-contract
  semantics.

- 2026-05-25 01:13 +08:00 checkpoint:
  this slice stayed adjacent to the same active-task list read lane but moved
  one layer deeper after the route-local glue had already been collapsed. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that `load_batch_generation_read_contexts_for_tasks(...)` had narrowed
  to a single production caller and no longer owned any shared batching,
  caching, or error semantics beyond iterating the already-owned singular
  helper `load_batch_generation_read_context_for_task(...)` over a `Vec<Task>`.

  The refactor therefore removed
  `load_batch_generation_read_contexts_for_tasks(...)` and pulled that
  immediate loop into
  `load_active_user_batch_generation_task_list_query(...)`, which is the real
  owner of the active-task list query/read pipeline. The singular read-context
  owner remains in `chapter_batch_generation_read_context_service.rs`, while
  the task-list query owner now directly performs `query -> per-task
  read-context load -> existing list envelope` without a fake plural wrapper
  boundary in between. This keeps the active-task list payload shape,
  read-context semantics, snapshot/quality enrichment behavior, and route
  error mapping unchanged while removing one more single-caller collection
  wrapper from the batch-generation read lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another plural read-context helper
  still restates the same immediate singular-owner iteration for one direct
  consumer without adding batching policy, shared error semantics, or visible
  payload-contract behavior; do not continue here once the remaining helpers
  still own real context-loading or cross-query semantics.

- 2026-05-25 01:16 +08:00 checkpoint:
  this slice moved from the active-task list read lane to the adjacent status
  read lane, where the route still kept one immediate glue seam after the
  surrounding read-context and payload owners had already converged. A focused
  audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  showed that `get_batch_generation_status(...)` still did the same two-step
  composition inline:
  - `load_required_owned_batch_generation_read_context(...)`
  - `build_batch_generation_status_query_response(...)`

  That pair had narrowed to one immediate production consumer and no longer
  owned any distinct transport or compatibility policy beyond converting the
  already-owned read-context into the already-owned status payload.

  The refactor therefore introduced one service-owned query helper:
  `load_owned_batch_generation_status_query(...)`. The status route now
  delegates directly to that owner, while
  `load_required_owned_batch_generation_read_context(...)` remains the shared
  owned read-context loader and
  `build_batch_generation_status_query_response(...)` remains the shared status
  payload owner inside the same query module. This keeps the
  `/chapters/batch-generate/{batch_id}/status` payload shape, owned-task error
  vocabulary, read-context semantics, and downstream route error mapping
  unchanged while removing one more route-local `load -> immediate payload`
  glue seam from the batch-generation status read lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another status-read helper still
  restates the same immediate owned-read-context load plus existing payload
  projection for one direct consumer without owning visible transport,
  response-envelope, or stream semantics; do not continue here once the
  remaining helpers still own real shared status-query or access semantics.

- 2026-05-25 01:21 +08:00 checkpoint:
  this slice deliberately avoided returning to the now-thin route/read lane
  and instead moved into the batch-generation status-stream lane, where one
  real shared semantic was still split across the initial load and polling
  branches. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  showed that stream startup and stream polling were both maintaining the same
  owner chain in parallel:
  - owned task lookup
  - read-context load
  - stream-state projection

  The route-independent stream orchestration file therefore still carried two
  local copies of `owned task -> read context -> stream state` semantics even
  though the real reusable contract was the resulting
  `BatchGenerationStreamState`.

  The refactor therefore introduced shared stream-state loaders in
  `chapter_batch_generation_status_view_service.rs`:
  - `load_required_owned_batch_generation_stream_state(...)`
  - `load_optional_owned_batch_generation_stream_state(...)`

  `chapter_batch_generation_status_stream_service.rs` now narrows to polling,
  cursor progression, SSE event emission, and timeout/not-found transport
  behavior. Both the initial stream bootstrap path and the polling fallback
  path now delegate to the shared stream-state owner instead of rebuilding the
  same `load owned task -> load read context -> build stream state` sequence
  inline. This keeps SSE payload shape, timeout/not-found behavior, owned-task
  error vocabulary, and stream terminal semantics unchanged while removing one
  more duplicated read-side semantic chain from the stream lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another stream helper still
  restates the same owned-task/read-context/stream-state chain in multiple
  branches without owning distinct cursor, event-batch, or transport-close
  semantics; do not continue here once the remaining stream helpers still own
  real polling or SSE behavior.

- 2026-05-25 01:26 +08:00 checkpoint:
  this slice moved from the batch-generation stream lane into the adjacent
  single-generation execution-input lane, where one real shared contract was
  still being assembled in parallel by two different owners. A focused audit
  of
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that `SingleChapterGenerationExecutionInput` was no longer just a
  local shape for the normal single-generation workflow. It was already a
  shared execution contract consumed by:
  - direct single-chapter generation prepare/workflow
  - batch resume when the resume target narrows to a single chapter

  But those two lanes were still building the same execution-input fields in
  parallel:
  - `chapter_id`
  - normalized or inherited `target_word_count`
  - `ai_config`
  - `provider_payload`

  The refactor therefore added a real shared constructor:
  `SingleChapterGenerationExecutionInput::new(...)`. The normal
  single-generation prepare path and the batch-resume single-chapter dispatch
  path now both delegate to that same owner instead of each reassembling the
  execution-input struct inline. This keeps single-generation runtime behavior,
  resume execution semantics, provider payload propagation, and the existing
  struct contract unchanged while removing one more duplicated execution-input
  assembly seam across the single/batch boundary.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation execution
  contract is still being assembled inline by multiple production callers
  without owning distinct workflow, dispatch, or runtime semantics; do not
  continue here once the remaining constructors still encode genuinely
  different launch or task-lifecycle policy.

- 2026-05-25 01:29 +08:00 checkpoint:
  this slice stayed on the batch resume / runtime-launch boundary after the
  single-generation execution-input constructor convergence, where one wrapper
  owner had now narrowed to a single-field pass-through between two adjacent
  service owners. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that `ResumeBatchGenerationPlan` no longer carried any distinct
  contract or branching semantics. It only wrapped one field:
  `execution: ResumeExecutionPlan`, and that wrapper was immediately
  destructured by the runtime-launch owner before calling the already-owned
  resume execution-input builder.

  The refactor therefore removed `ResumeBatchGenerationPlan` and let
  `ResumeBatchGenerationOutcome` carry `ResumeExecutionPlan` directly. The
  resume write-workflow owner now forwards that execution plan straight into
  `launch_owned_batch_generation_resume_runtime(...)`, and the runtime-launch
  owner now consumes `ResumeExecutionPlan` directly instead of first unpacking
  a one-field wrapper. This keeps resume response state, resume execution
  semantics, runtime launch behavior, and downstream dispatch unchanged while
  removing one more fake owner boundary from the batch resume lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another resume/write/launch wrapper
  still carries a single immediate field into one direct consumer without
  owning distinct response, execution-target, or dispatch semantics; do not
  continue here once the remaining structs still encode real boundary meaning.

- 2026-05-25 13:42 +08:00 checkpoint:
  this slice stayed on the single-generation / batch-resume dispatch boundary,
  where one shared single-runtime launch contract was still partially owned by
  the batch dispatch lane. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the batch-resume single-chapter branch still knew how to rebuild
  `SingleChapterGenerationExecutionInput` field-by-field from
  `PreparedGenerationExecutionConfig`, even though that launch shape belongs to
  the single-generation runtime owner.

  The refactor therefore moved the
  `execution_config -> SingleGenerationRuntimeLaunchInput` assembly into
  `SingleGenerationRuntimeLaunchInput::from_execution_config(...)` inside the
  single-generation dispatch owner. The batch-resume dispatch branch now only
  forwards `user_id`, `chapter_id`, `target_word_count`, and the prepared
  execution config into that shared single-runtime launch builder instead of
  rebuilding the nested execution-input contract locally. This keeps resume
  target selection, runtime dispatch behavior, provider payload propagation,
  and single-generation execution semantics unchanged while removing one more
  cross-lane contract leak from the batch resume path.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another non-single owner still
  rebuilds `SingleGenerationRuntimeLaunchInput` or its immediate nested
  execution contract from raw config inputs without also owning distinct
  single-runtime dispatch semantics; do not continue here once the remaining
  launch builders still encode real workflow or transport policy.

- 2026-05-25 14:18 +08:00 checkpoint:
  this slice moved back to the single-generation background write lane after a
  fresh file-layout audit confirmed that the current live owner is
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  rather than the older `chapter_single_generation_write_workflow_service.rs`
  name still mentioned in historical checkpoints. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the background workflow was still manually splitting one prepared
  request contract before handing work to the task-command owner:
  - `prepared.chapter_target`
  - `prepared.execution_input.target_word_count`

  That meant the workflow owner still knew how to rebuild the task-plan input
  contract from `PreparedSingleChapterGenerationRequest`, even though the
  actual background task creation semantics already belong to the
  task-command owner.

  The refactor therefore added
  `create_single_generation_background_task_plan_from_prepared_request(...)`
  to the single-generation task-command owner and updated
  `start_owned_single_generation_background_workflow(...)` to delegate the
  prepared request directly into that shared boundary. This keeps background
  task persistence, response payload shape, runtime launch behavior, and
  single-generation execution semantics unchanged while removing one more
  workflow-local contract split from the single-generation write lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another workflow owner still
  decomposes `PreparedSingleChapterGenerationRequest` into immediate
  task-command inputs without also owning distinct task-creation policy; do
  not continue here once the remaining prepared-request consumers still encode
  real launch, transport, or response semantics.

- 2026-05-25 14:36 +08:00 checkpoint:
  this slice stayed on the single-generation background write lane, where one
  single-chapter compat payload owner was still drifting inside the batch
  command payload adapter. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`
  showed that
  `build_single_generation_background_create_response_payload(...)` was only
  consumed by the single-generation background workflow, yet it still lived in
  the batch-generation command payload owner alongside batch create/cancel/
  resume response contracts.

  The refactor therefore moved that compat payload builder into a dedicated
  single-generation payload owner:
  `backend-rs/src/services/chapter_single_generation_payload_adapter_service.rs`.
  The single-generation background workflow now imports the payload from its
  own lane, while the batch command payload adapter narrows back to true batch
  create/cancel/resume response assembly only. This keeps the background
  create payload shape, message text, estimated minutes, and runtime launch
  behavior unchanged while removing one more cross-lane payload-owner drift.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_payload_adapter_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation-only
  compat payload is still owned by a batch or cross-lane adapter without also
  carrying shared batch semantics; do not continue here once the remaining
  payload adapters still encode genuinely shared transport contracts.

- 2026-05-25 15:02 +08:00 checkpoint:
  this slice moved from the single-generation background payload lane into the
  adjacent runtime snapshot lane, where single-chapter checkpoint semantics
  were still colocated inside the batch-generation runtime checkpoint owner. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`,
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the following single-only runtime snapshot contract still lived
  under the batch checkpoint owner:
  - `SingleGenerationSnapshotStage`
  - `SingleGenerationSnapshotPlan`
  - `build_single_generation_runtime_checkpoint_for_stage(...)`
  - the single-chapter `last_message/last_event/progress` semantics

  Those snapshot contracts are only consumed by the single-generation runtime
  state and single-generation background task creation path; they do not carry
  shared batch semantics beyond reusing the persisted snapshot store.

  The refactor therefore moved the single-generation runtime checkpoint owner
  into
  `backend-rs/src/services/chapter_single_generation_runtime_checkpoint_service.rs`.
  The single-generation runtime state and task-command owners now import their
  snapshot semantics from the single lane, while
  `chapter_batch_generation_runtime_checkpoint_service.rs` narrows back to
  true batch-generation checkpoint semantics only. This keeps checkpoint
  payload shape, status vocabulary, progress values, and runtime snapshot
  behavior unchanged while removing one more cross-lane runtime owner split.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation-only
  runtime snapshot or checkpoint semantic is still owned by a batch or
  cross-lane checkpoint module without also encoding shared batch behavior; do
  not continue here once the remaining checkpoint helpers still express
  genuinely shared persistence contracts.

- 2026-05-25 15:19 +08:00 checkpoint:
  this slice stayed on the single-generation background task-creation lane,
  where one single-only task chapter payload owner was still drifting inside a
  batch-generation helper module. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  and the remaining references to
  `backend-rs/src/services/chapter_batch_generation_chapter_payload_service.rs`
  showed that `single_task_chapter_payload_from_parts(...)` was no longer
  shared across batch and single lanes. Its only remaining production caller
  was the single-generation background task-command owner.

  The refactor therefore moved that payload builder into a dedicated single
  lane owner:
  `backend-rs/src/services/chapter_single_generation_task_payload_service.rs`.
  The single-generation task-command owner now imports its chapter snapshot
  payload from the single lane, and the old batch chapter payload shell was
  removed entirely. This keeps the persisted `chapter_ids` payload shape and
  background task creation behavior unchanged while removing one more
  single-only helper from a batch owner boundary.

  Validation for this slice:
  - `rg -n "chapter_batch_generation_chapter_payload_service|single_task_chapter_payload_from_parts|single_generation_task_chapter_payload_from_parts" "backend-rs/src/services"`
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_payload_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation-only task
  payload helper is still owned by a batch or cross-lane module without also
  carrying real shared batch semantics; do not continue here once the
  remaining task payload builders still express genuinely shared persistence
  contracts.

- 2026-05-25 16:02 +08:00 checkpoint:
  this slice stayed adjacent to the batch/single background task-creation
  lane, where one shared persistence contract was still owned by the batch
  create task-command module. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that `build_batch_generation_task_active_model(...)` was no longer a
  batch-create-only helper. It is now consumed by both:
  - batch generation task-plan creation
  - single-generation background task-plan creation

  That builder owns the persisted `batch_generation_tasks` default contract
  (`pending` status, empty failed list, zero retry counter, created/start/end
  timestamps, and optional current chapter pointers). Those semantics are
  shared task-model defaults, not batch-create workflow policy.

  The refactor therefore moved that active-model builder into a dedicated
  shared owner:
  `backend-rs/src/services/chapter_batch_generation_task_model_service.rs`.
  Both batch-create and single-generation task-command owners now import the
  shared persistence builder from that neutral task-model lane, while
  `chapter_batch_generation_create_task_command_service.rs` narrows back to
  batch create plan assembly plus snapshot initialization only. This keeps the
  persisted task row shape and runtime startup behavior unchanged while
  removing one more cross-lane owner drift from the create-task command file.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_model_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another persisted
  `batch_generation_task` creation contract is shared across single/batch
  owners yet still hosted inside one workflow-specific task-command module; do
  not continue here once the remaining task-model helpers still belong to
  true workflow-local policy or runtime mutation semantics.

- 2026-05-25 16:24 +08:00 checkpoint:
  this slice returned to the single-generation route/write-workflow boundary,
  where one route-local request assembly step still remained outside the
  single lane owner. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_request_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  showed that the single background and stream routes still built
  `SingleChapterGenerationRequest` inline before delegating to the single
  workflow owner. That conversion is compatibility-safe but it is still
  write-lane request assembly, and batch generation already owns the
  equivalent route-payload -> workflow request boundary inside
  `chapter_batch_generation_write_workflow_service.rs`.

  The refactor therefore introduced a dedicated single write-workflow owner:
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`.
  The new owner now consumes `SingleChapterGenerationRouteRequest`, performs
  the route-payload -> workflow request conversion once, and delegates to the
  existing single stream/background workflow helpers. The route in
  `chapter_batch_generation.rs` now narrows back to transport/auth wiring plus
  write-workflow delegation only. This keeps request fields, ignored compat
  `enable_analysis` handling, SSE transport shape, background response payload,
  and runtime behavior unchanged while making the single lane boundary
  consistent with batch write-workflow ownership.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_request_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation
  route-compatible request conversion still lives in the route shell without
  also owning transport-only concerns; do not continue here once the remaining
  route-side code is purely transport/auth wiring or the next split only moves
  a trivial passthrough with no ownership gain.

- 2026-05-25 16:47 +08:00 checkpoint:
  this slice moved back to the batch launch/dispatch boundary, where one
  launch-specific construction contract was still hosted inside the dispatch
  owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that two responsibilities were still colocated in the dispatch owner:
  - `BatchGenerationExecutionInput::new(...)`
  - `build_resume_generation_execution_input(...)`

  The `BatchGenerationExecutionInput` and `ResumeGenerationExecutionInput`
  types remain true runtime contracts because downstream runtime/dispatch code
  still consumes them directly. But the construction logic itself is launch
  policy: it converts create/resume execution plans plus prepared execution
  config into explicit runtime inputs before handoff to dispatch. That is no
  longer dispatch-specific behavior.

  The refactor therefore moved the runtime-input construction logic into
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`.
  The launch owner now assembles both create and resume execution inputs
  before dispatch, while
  `chapter_batch_generation_dispatch_service.rs` narrows back to the runtime
  input contract types plus spawn/discriminated dispatch only. This keeps
  runtime payload shape, resume branching semantics, single-vs-batch dispatch,
  and write-workflow behavior unchanged while making the launch/dispatch
  boundary consistent with owner intent.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch runtime-input
  construction helper still lives in dispatch without also owning spawn or
  runtime contract branching; do not continue here once the remaining launch
  code is either true dispatch logic or shared runtime contract surface.

- 2026-05-25 17:06 +08:00 checkpoint:
  this slice returned to the batch create route/write-workflow boundary, where
  one route-local request assembly step still remained in the API shell even
  after the single-generation lane had already narrowed the same pattern. A
  focused audit of `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`
  showed that the batch create route still built
  `BatchGenerationCreateWorkflowRequest` inline before delegating to the batch
  write-workflow owner. That conversion is compatibility-safe, but it is still
  write-lane request assembly rather than transport logic.

  The refactor therefore moved the route-payload -> workflow request handoff
  into `chapter_batch_generation_write_workflow_service.rs`. The batch create
  route now only forwards `BatchGenerationCreateRouteRequest`, while the write
  workflow consumes the route body, resolves the compat request once, and
  continues through the existing prepare/task-plan/launch path. This keeps
  ignored compat fields, default `enable_analysis/max_retries` behavior,
  response payloads, and runtime launch behavior unchanged while making the
  batch lane boundary consistent with the already-narrowed single lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_request_compat_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch-generation
  route-compatible request conversion still lives in the route shell without
  also owning transport-only concerns; do not continue here once the remaining
  route-side code is purely transport/auth wiring or the next split only moves
  a trivial passthrough with no ownership gain.

- 2026-05-25 17:28 +08:00 checkpoint:
  this slice stayed on the batch create task-command lane, where initial
  snapshot persistence still lived inline beside task-row creation. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`
  showed that the create task-command owner still knew two persistence details
  that belong closer to the snapshot owner:
  - how to build the queued runtime checkpoint for a brand-new batch task
  - how to upsert that initial runtime snapshot into the snapshot store

  The task-command owner should still create the task row and return the plan,
  but the initial snapshot write is snapshot persistence policy rather than
  task-plan assembly policy.

  The refactor therefore moved that initialization boundary into
  `chapter_batch_generation_snapshot_service.rs` through:
  - `build_new_batch_generation_task_runtime_snapshot(...)`
  - `persist_new_batch_generation_task_snapshot(...)`

  `chapter_batch_generation_create_task_command_service.rs` now narrows back
  to task active-model creation plus plan projection, while the snapshot owner
  assembles the queued checkpoint and persists it once. This keeps queued
  checkpoint shape, initial `completed/total` values, and snapshot write
  behavior unchanged while removing one more persistence-owner split from the
  task-command lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch create-time snapshot
  initialization semantic still lives in task-command code without also owning
  task-row assembly; do not continue here once the remaining task-command
  logic is strictly task model creation / plan projection or the next split is
  only a thin pass-through.

- 2026-05-25 18:28 +08:00 checkpoint:
  this slice returned to the batch read/query lane, where the active query
  owner still held one read-context loading step that had already converged
  elsewhere into the shared task-view query owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that the active query path still owned four concerns at once:
  - project access gating
  - active-task row selection for one project
  - `BatchGenerationReadContext` loading for the selected task
  - final `has_active_task/task` response envelope assembly

  The access gate and response envelope still belong in
  `chapter_batch_generation_active_query_service.rs`, but the task-row ->
  read-context loading step is the same read-side contract already owned by
  `chapter_batch_generation_task_view_query_service.rs` for status and active
  list paths. Keeping that load inline in the active query owner meant one
  remaining view-context boundary was still split across adjacent read-side
  files.

  The refactor therefore moved the project-scoped active task view-context
  loading into
  `chapter_batch_generation_task_view_query_service.rs` through
  `load_active_project_batch_generation_task_view_context(...)`.
  `chapter_batch_generation_active_query_service.rs` now narrows back to
  project access gating plus active-query response envelope assembly, while
  the shared task-view owner now consistently owns the active/status/list
  read-context loading surface. This keeps the `has_active_task/task`
  payload shape, active-task selection order, project access error contract,
  and quality/checkpoint field assembly unchanged while removing one more
  read-side owner split from the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch read/query path still
  loads `BatchGenerationReadContext` outside the shared task-view owner
  without also owning a distinct transport or stream contract; do not continue
  here once the remaining differences are only route-specific envelopes or
  true access-control concerns.

- 2026-05-25 18:48 +08:00 checkpoint:
  this slice stayed on the batch read/query lane, but after the previous
  read-context consolidation it exposed one more owner-boundary regression:
  the shared task-view owner had also reabsorbed one route-specific list-query
  envelope and query entrypoint. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that
  `chapter_batch_generation_task_view_query_service.rs` was now carrying both:
  - shared task-view responsibilities:
    active-task selection, `BatchGenerationReadContext` loading, item/status
    payload assembly
  - active-list route-specific responsibilities:
    `total/items` response envelope assembly and the user-scoped active-list
    query entrypoint

  That is a real owner drift because the shared task-view file should own the
  reusable read-context + item payload surface, while the active-list route
  shape is a distinct query owner concern that had previously been narrowed
  out of the shared view module.

  The refactor therefore restored a dedicated
  `chapter_batch_generation_active_list_query_service.rs` owner. The shared
  task-view file now narrows back to reusable active/status view-context and
  item payload behavior, while the active-list owner again owns:
  - `build_active_batch_generation_task_list_query_response(...)`
  - `load_active_user_batch_generation_task_list_query(...)`

  This keeps the `total/items` payload shape, active-task ordering, and
  per-item task payload unchanged while making the batch read-side owner split
  consistent again: shared view-context in one module, route-specific active
  list envelope in another.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_active_list_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch query file is again
  mixing shared task-view context ownership with a route-specific response
  envelope or endpoint entrypoint; do not continue here once the remaining
  query differences are only true access gates or distinct route transport
  shapes.

- 2026-05-25 18:38 +08:00 checkpoint:
  this slice continued the same batch read/query lane cleanup after the
  active-list owner was restored, and it confirmed one more matching boundary
  regression on the status-query side. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the shared
  task-view owner still carried one route-specific query responsibility:
  - reusable shared task-view responsibilities:
    active-task selection, `BatchGenerationReadContext` loading, active item
    payload assembly
  - route-specific status-query responsibilities:
    owned task status query entrypoint and final status payload response

  That is the same owner drift shape as the active-list regression: the
  shared task-view file should own reusable view-context behavior, while the
  owned `/status` route contract is a distinct query owner concern.

  The refactor therefore restored a dedicated
  `chapter_batch_generation_status_query_service.rs` owner. The shared
  task-view file now narrows back to reusable task-view context and active
  item payload behavior, while the status-query owner again owns:
  - `build_batch_generation_status_query_response(...)`
  - `load_owned_batch_generation_status_query(...)`

  This keeps the `/status` payload shape, task lookup behavior, and
  route-level error mapping unchanged while making the batch read-side split
  consistent again: shared task-view logic in one module, route-specific
  status query owner in another.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch query surface is still
  mixing reusable task-view context ownership with a route-specific query
  entrypoint or envelope; do not continue here once the remaining differences
  are only true access gates, stream contracts, or transport wiring.

- 2026-05-25 18:46 +08:00 checkpoint:
  this slice moved out of the batch query lane and into the single-generation
  background launch lane, where the workflow owner still carried one explicit
  launch-contract assembly step even though the single runtime dispatch
  contract had already stabilized around `SingleGenerationRuntimeLaunchInput`.
  A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  and `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  showed that the background path still did three launch-owner actions inline
  inside the workflow:
  - convert prepared execution input into `SingleGenerationRuntimeLaunchInput`
  - bind that runtime input to the created task id
  - dispatch the owned background runtime

  The stream lane already has its own launch owner boundary through
  `launch_owned_single_chapter_generation_stream(...)`, so keeping the
  background launch contract inline in the workflow meant the single write lane
  was still splitting launch ownership across workflow and dispatch.

  The refactor therefore introduced a dedicated
  `chapter_single_generation_background_launch_service.rs` owner. The new
  owner now owns:
  - `build_single_generation_background_launch(...)`
  - `dispatch_owned_single_generation_background_runtime(...)`

  `chapter_single_generation_workflow_service.rs` now narrows back to prepare
  request, create task plan, assemble response payload, and hand off the owned
  background launch once. This keeps the background create response payload,
  task creation order, runtime launch input shape, and dispatch behavior
  unchanged while making the single-generation background lane consistent with
  the explicit launch-owner pattern already used elsewhere.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_background_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation background
  launch concern still lives in workflow code without also owning request
  preparation or response assembly; do not continue here once the remaining
  differences are only stream-specific launch behavior or true dispatch
  contract surface.

- 2026-05-25 18:50 +08:00 checkpoint:
  this slice stayed on the single-generation launch lane, but moved from the
  background path to the stream path, where one more explicit launch-contract
  assembly step still lived in the workflow owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`
  showed that the stream workflow still directly converted prepared execution
  input into `SingleGenerationRuntimeLaunchInput` before delegating to the
  stream launch owner. That meant the stream lane still split launch-contract
  ownership across workflow and launch service even after the background lane
  had already been narrowed.

  The refactor therefore moved that conversion into the stream launch owner
  through:
  - `build_single_generation_stream_launch(...)`

  `chapter_single_generation_workflow_service.rs` now narrows back to prepare
  request plus handoff to the owned stream launch, while
  `chapter_single_generation_stream_launch_service.rs` now owns both stream
  launch-contract assembly and stream transport/runtime execution. This keeps
  the SSE event sequence, runtime launch input shape, request validation, and
  stream execution behavior unchanged while making the single-generation launch
  lane more consistent across background and stream paths.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another single-generation launch
  concern still lives in workflow code without also owning request preparation;
  do not continue here once the remaining differences are only true stream
  transport behavior or the shared dispatch contract surface.

- 2026-05-25 18:54 +08:00 checkpoint:
  this slice moved back to the batch stream/status-view lane, but it stayed
  away from stream transport behavior and instead narrowed one remaining
  access/load contract split between the shared read-context owner and the
  stream-state projection owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`
  showed that the shared read-context owner already held the required owned
  loader:
  - `load_required_owned_batch_generation_read_context(...)`

  but the optional owned loader still lived inline inside the stream-state
  owner through the same two-step contract:
  - `load_owned_task(...)`
  - `load_batch_generation_read_context_for_task(...)`

  That meant the owned-task -> read-context boundary was still split across a
  shared read-context service and a stream-specific view owner, even though the
  stream-specific file should only need to turn an already-owned
  `BatchGenerationReadContext` into `BatchGenerationStreamState`.

  The refactor therefore moved the optional owned read-context load into the
  shared owner through:
  - `load_owned_batch_generation_read_context(...)`

  `chapter_batch_generation_status_view_service.rs` now narrows back to stream
  state projection plus optional/required stream-state wrappers over shared
  read-context loaders. This keeps the stream polling behavior, not-found
  semantics, required-vs-optional lookup behavior, and stream-state payload
  semantics unchanged while removing one more shared access/load contract split
  from the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another shared owned-task ->
  read-context loader still lives outside the read-context owner; do not
  continue here once the remaining stream differences are only true state
  projection or stream transport behavior.

- 2026-05-25 20:06 +08:00 checkpoint:
  this slice stayed in the batch generation command/error lane and narrowed one
  remaining outer command-owner split across service and route boundaries. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that:
  - cancel still owned its own outer `Task | Domain(String)` workflow error
  - resume still owned a parallel outer `Task | Domain(...) | Config(String)`
    workflow error
  - the route mapper therefore maintained the same owned-task command routing
    shape more than once even though the outer behavior was identical:
    task lookup failures always mapped through the owned-task route owner, and
    domain/config branches only changed the BAD_REQUEST detail payload source

  The refactor therefore introduced one shared command-error owner in:
  - `backend-rs/src/services/chapter_batch_generation_command_error_service.rs`

  with:
  - `OwnedBatchGenerationTaskCommandError<TDomainError>`
  - `OwnedBatchGenerationTaskCommandConfigError<TDomainError, TConfigError>`

  `chapter_batch_generation_cancel_service.rs` now exposes
  `CancelBatchGenerationWorkflowError` as a type alias over the shared
  `Task | Domain` owner, and
  `chapter_batch_generation_write_workflow_service.rs` now exposes
  `ResumeBatchGenerationWriteWorkflowError` as a type alias over the shared
  `Task | Domain | Config` owner. On the route side,
  `chapter_batch_generation_error_mapper.rs` now routes cancel/resume through
  shared owned-task command mapper helpers instead of hand-maintaining two
  parallel outer matches.

  This keeps cancel/resume HTTP status codes, Chinese/English detail text,
  owned-task not-found behavior, internal-detail passthrough, and runtime
  launch behavior unchanged while removing one more compatibility-sensitive
  outer contract split from the batch command lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch command path still
  duplicates the same owned-task outer command contract across service and
  route owners; do not continue here for naming-only cleanup once the shared
  outer owner is direct and the remaining differences are true domain/config
  semantics.

- 2026-05-25 20:26 +08:00 checkpoint:
  this slice stayed in the batch create write-workflow lane and narrowed one
  remaining transport-boundary leak between the route owner and the workflow
  owner. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the route already had a dedicated compat request builder:
  - `build_batch_generation_create_workflow_request_from_route_payload(...)`

  but the write-workflow owner still accepted the transport-facing
  `BatchGenerationCreateRouteRequest` directly and rebuilt the workflow request
  inside the service boundary. That meant the batch create write-workflow owner
  still depended on an HTTP/compat payload type even though the explicit
  `BatchGenerationCreateWorkflowRequest` contract already existed.

  The refactor therefore moved the route-payload normalization back to the
  transport owner:
  - `api/chapter_batch_generation.rs` now builds
    `BatchGenerationCreateWorkflowRequest` before calling the workflow owner
  - `chapter_batch_generation_write_workflow_service.rs` now accepts only
    `BatchGenerationCreateWorkflowRequest`

  This keeps create request validation, defaulting behavior, error mapper
  behavior, response payload shape, and runtime launch behavior unchanged while
  making the batch create write-workflow boundary consistent with the existing
  “service consumes workflow request, route owns transport payload” rule.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return if another batch write-workflow entry
  point still consumes a route/compat payload type after a stable workflow
  request contract already exists; do not continue here once the remaining
  differences are true workflow semantics rather than transport normalization.

- 2026-05-25 20:40 +08:00 checkpoint:
  this slice returned to the single-generation write lane, not to introduce
  another helper, but to remove one owner layer that had reached zero signal.
  A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_request_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
  showed that the write-workflow module no longer owned any distinct workflow
  semantics:
  - its only callers were the two single-generation routes
  - its only behavior was route-payload normalization through
    `build_single_chapter_generation_request_from_route_payload(...)`
    followed by direct forwarding into the real workflow owner
  - request preparation, task-plan creation, launch-contract assembly, and
    runtime dispatch were already owned elsewhere

  The refactor therefore removed the empty forwarding owner:
  - `chapter_single_generation_write_workflow_service.rs` was deleted
  - `api/chapter_batch_generation.rs` now performs the route-payload ->
    workflow-request conversion directly and calls
    `start_owned_single_generation_background_workflow(...)` /
    `create_single_generation_stream_workflow(...)`
  - `services/mod.rs` no longer exports the deleted module

  This keeps request payload parsing, error mapper behavior, background create
  response shape, SSE stream behavior, and single-generation runtime launch
  semantics unchanged while removing one more no-signal write-lane middle layer
  from the chapter-generation boundary.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: do not recreate a dedicated single-generation
  write-workflow layer unless it regains true workflow ownership beyond route
  payload normalization; route-only transport shaping should stay in the route
  owner once the direct workflow boundary is already stable.

- 2026-05-25 20:56 +08:00 checkpoint:
  this slice moved to the chapter-analysis read lane and removed one more
  no-signal forwarding owner after confirming the underlying query/request
  owners were already stable. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/services/chapter_analysis_read_workflow_service.rs`,
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_query_service.rs`, and
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`
  showed that the read-workflow layer no longer owned any distinct workflow
  semantics:
  - chapter analysis view routes already had direct query error mappers
  - the read-workflow functions only forwarded to owned query services
  - the batch status branch only normalized route payload through
    `build_batch_analysis_status_request_from_route_payload(...)` and then
    directly delegated to the batch query owner

  The refactor therefore removed the empty forwarding owner:
  - `chapter_analysis_read_workflow_service.rs` was deleted
  - `chapter_analysis_routes.rs` now calls
    `load_owned_chapter_analysis_view_payload(...)`,
    `load_owned_chapter_quality_metrics_payload(...)`,
    `load_analysis_task_status_payload(...)`, and
    `load_batch_analysis_task_status_payload(...)` directly
  - the route keeps the route-payload normalization step for batch status via
    `build_batch_analysis_status_request_from_route_payload(...)`
  - `services/mod.rs` no longer exports the deleted module

  This keeps chapter-analysis read payload shapes, status/error mapper
  behavior, batch status normalization, and route transport semantics
  unchanged while removing one more read-lane middle layer that had no real
  ownership beyond forwarding.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: do not restore a dedicated chapter-analysis
  read-workflow layer unless it regains real orchestration semantics beyond
  route payload normalization and direct query delegation; route-owned
  transport shaping plus service-owned query logic is the stable boundary here.

- 2026-05-25 21:10 +08:00 checkpoint:
  this slice stayed in the chapter-analysis lane and removed another
  forwarding-only workflow owner after confirming that request normalization,
  payload ownership, and error ownership were already stable below it. A
  focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/services/chapter_analysis_draft_request_service.rs`,
  `backend-rs/src/services/chapter_analysis_draft_service.rs`, and
  `backend-rs/src/services/chapter_analysis_draft_workflow_service.rs`
  showed that the draft workflow layer no longer owned any real orchestration:
  - its only callers were the four chapter-analysis draft routes
  - its only behavior was route query/body normalization through the draft
    request builders followed by direct delegation into the owned draft
    payload service
  - chapter access gating, draft selection mode ownership, stale handling, and
    outer error contracts were already owned by
    `chapter_analysis_draft_service.rs`

  The refactor therefore removed the empty forwarding owner:
  - `chapter_analysis_draft_workflow_service.rs` was deleted
  - `chapter_analysis_routes.rs` now builds the owned draft request directly
    via the route request builders and calls
    `load_owned_auto_revision_draft_payload(...)`,
    `apply_owned_auto_revision_draft_payload(...)`,
    `load_owned_candidate_draft_payload(...)`, and
    `apply_owned_candidate_draft_payload(...)` directly
  - `services/mod.rs` no longer exports the deleted module

  This keeps route payload trimming/defaulting, chapter-analysis draft payload
  shapes, stale-selection semantics, and route error mapper behavior unchanged
  while removing one more no-signal middle layer from the Rust analysis seam.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: do not reintroduce a dedicated draft workflow layer
  unless it regains real orchestration semantics beyond route-owned transport
  normalization and direct delegation into the owned draft payload service.

- 2026-05-25 21:18 +08:00 checkpoint:
  this slice moved to the chapter CRUD write lane and removed one more
  workflow wrapper after confirming that the route payload builders and the
  CRUD payload owner were already stable. A focused audit of
  `backend-rs/src/api/chapter_crud_routes.rs`,
  `backend-rs/src/services/chapter_crud_request_service.rs`,
  `backend-rs/src/services/chapter_crud_workflow_service.rs`, and
  `backend-rs/src/services/chapter_crud_write_workflow_service.rs`
  showed that the write-workflow layer no longer owned any distinct write
  semantics:
  - its only behavior was route body -> request conversion through the CRUD
    request builders
  - after conversion it only delegated directly into
    `create_chapter_payload(...)`,
    `update_chapter_payload(...)`, and
    `update_expansion_plan_payload(...)`
  - payload assembly, not-found/internal error ownership, and the actual
    chapter CRUD side effects were already owned by
    `chapter_crud_workflow_service.rs`

  The refactor therefore removed the empty forwarding owner:
  - `chapter_crud_write_workflow_service.rs` was deleted
  - `chapter_crud_routes.rs` now builds the CRUD requests directly at the
    transport boundary and calls
    `create_chapter_payload(...)`,
    `update_chapter_payload(...)`, and
    `update_expansion_plan_payload(...)` directly
  - `services/mod.rs` no longer exports the deleted module

  This keeps route payload normalization, CRUD payload shapes, create/update
  status behavior, and route error mapper behavior unchanged while removing
  another no-signal middle layer from the Rust write boundary.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only return here if another CRUD write entry point
  still consumes a route-facing transport payload after a stable service-owned
  request and payload contract already exists; do not keep splitting this lane
  once the remaining behavior is real workflow or domain ownership.

- 2026-05-25 21:31 +08:00 checkpoint:
  this slice stayed in the chapter CRUD lane and removed the remaining
  forwarding-only read workflow after confirming that its route consumers and
  downstream query/payload owners were already stable. A focused audit of
  `backend-rs/src/api/chapter_crud_routes.rs`,
  `backend-rs/src/services/chapter_crud_read_workflow_service.rs`,
  `backend-rs/src/services/chapter_crud_workflow_service.rs`,
  `backend-rs/src/services/chapter_query_service.rs`,
  `backend-rs/src/services/chapter_annotation_query_service.rs`, and
  `backend-rs/src/services/chapter_quality_query_service.rs`
  showed that the read-workflow layer no longer owned any real read-side
  orchestration:
  - every function was a direct delegate into one existing query or payload
    owner
  - no route payload normalization, access-shaping, or response adaptation
    stayed in the read-workflow file
  - list/get CRUD payload ownership already belonged to
    `chapter_crud_workflow_service.rs`, while navigation/can-generate,
    annotations, and quality-trend behavior already belonged to their
    respective query services

  The refactor therefore removed the empty forwarding owner:
  - `chapter_crud_read_workflow_service.rs` was deleted
  - `chapter_crud_routes.rs` now calls
    `list_chapters_payload(...)`,
    `list_chapters_by_project_path_payload(...)`,
    `get_chapter_payload(...)`,
    `load_navigation_payload(...)`,
    `load_annotations_payload(...)`,
    `load_quality_trend_payload(...)`, and
    `load_can_generate_payload(...)` directly
  - `services/mod.rs` no longer exports the deleted module

  This keeps chapter CRUD read payload shapes, route error mapper behavior,
  and read-side access/query semantics unchanged while removing one more
  no-signal middle layer from the Rust route boundary.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: do not restore a dedicated CRUD read-workflow layer
  unless it regains real orchestration semantics beyond direct delegation into
  stable read/query owners.

- 2026-05-25 21:50 +08:00 checkpoint:
  this slice moved to the admin user-management lane and tightened a route
  boundary that was still carrying direct persistence and password-default
  assembly. A focused audit of
  `backend-rs/src/api/admin.rs`,
  `backend-rs/src/services/user_admin_create_user_route_service.rs`, and
  `backend-rs/src/services/user_admin_route_service.rs`
  showed that the `create_user` route still owned too much business behavior:
  - duplicate username lookup lived in the route
  - user row creation, password row creation, default-password fallback, and
    password hashing all lived in the route
  - the route also assembled the final success payload after reloading the
    created user model

  The refactor therefore tightened the owner boundary without changing the
  HTTP contract:
  - `user_admin_create_user_route_service.rs` now owns
    `create_user_payload(...)`
  - that owner now performs duplicate detection, id generation, user/password
    persistence, default-password selection, hashing, and success-payload
    assembly in one place
  - `api/admin.rs` now keeps only admin auth plus
    route-payload -> request conversion before delegating to the owner

  This keeps the `201 Created` response shape, duplicate-user conflict
  behavior, default-password exposure semantics, and admin-only route contract
  unchanged while shrinking one more route-side business seam in Rust.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" admin -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_create_user_route_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: keep future admin-route work focused on concrete
  business logic still sitting in handlers; do not split the remaining routes
  further unless a clear owner boundary or repeated contract drift is visible.

- 2026-05-25 22:03 +08:00 checkpoint:
  this slice stayed in the user-management lane and moved one more concrete
  admin/business write path out of the route handler. A focused audit of
  `backend-rs/src/api/users.rs`,
  `backend-rs/src/services/user_admin_set_admin_route_service.rs`, and
  `backend-rs/src/services/user_admin_route_service.rs`
  showed that the `set_admin` route still owned multiple pieces of business
  behavior beyond transport concerns:
  - self-demotion blocking lived in the route
  - “must keep at least one admin” enforcement lived in the route
  - target-user loading and `is_admin` persistence lived in the route
  - the route assembled the final success payload after performing the update

  The refactor therefore tightened the owner boundary without changing the
  route contract:
  - `user_admin_set_admin_route_service.rs` now owns
    `set_admin_payload(...)`
  - that owner now performs self-demotion blocking, remaining-admin
    protection, target-user loading, `is_admin` update persistence, and
    success-payload assembly in one place
  - `api/users.rs` now keeps only admin auth plus route-payload ->
    request conversion before delegating to the owner

  This keeps the existing request validation, bad-request semantics for
  self-demotion / last-admin protection, and success payload shape unchanged
  while shrinking another route-side business seam in Rust.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" users -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_set_admin_route_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: continue only where a route still clearly mixes
  transport handling with persistence or multi-branch business invariants; do
  not keep splitting user-management handlers once the remaining code is
  mostly orchestration or thin delegation.

- 2026-05-25 22:16 +08:00 checkpoint:
  this slice returned to the admin user-management lane and tightened another
  narrow write-path owner boundary. A focused audit of
  `backend-rs/src/api/admin.rs` and
  `backend-rs/src/services/user_admin_toggle_status_route_service.rs`
  showed that `toggle_user_status` still owned several pieces of business
  logic beyond transport concerns:
  - self-disable blocking lived in the route
  - target-user loading lived in the route
  - `trust_level` mutation for enable/disable lived in the route
  - the route assembled the final success payload after persisting the update

  The refactor therefore tightened the owner boundary without changing the
  route contract:
  - `user_admin_toggle_status_route_service.rs` now owns
    `toggle_user_status_payload(...)`
  - that owner now performs self-disable blocking, target-user loading,
    enable/disable `trust_level` persistence, and success-payload assembly in
    one place
  - `api/admin.rs` now keeps only admin auth plus route-payload ->
    request conversion before delegating to the owner

  This keeps the existing request normalization, bad-request semantics for
  self-disable, not-found handling for the target user, and success payload
  shape unchanged while shrinking another route-side business seam in Rust.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" admin -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_toggle_status_route_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: keep following only concrete route handlers that
  still mix transport concerns with persistence or business invariants; stop
  once the remaining handlers are mostly orchestration or thin owner
  delegation.

- 2026-05-25 22:34 +08:00 checkpoint:
  this slice stayed in the admin user-management lane and tightened the
  remaining route-side update path before touching the heavier delete flow. A
  focused audit of
  `backend-rs/src/api/admin.rs`,
  `backend-rs/src/services/user_admin_update_user_route_service.rs`, and
  `backend-rs/src/services/user_admin_route_service.rs`
  showed that `update_user` still owned too much business behavior:
  - target-user loading lived in the route
  - last-admin-removal protection lived in the route
  - per-field persistence updates lived in the route
  - the route assembled the final success payload after saving the model

  The refactor therefore tightened the owner boundary without changing the
  route contract:
  - `user_admin_update_user_route_service.rs` now owns
    `update_user_payload(...)`
  - that owner now performs target-user loading, last-admin-removal
    protection, field-by-field persistence updates, and success-payload
    assembly in one place
  - `api/admin.rs` now keeps only admin auth plus route-payload ->
    request conversion before delegating to the owner

  This keeps the existing request normalization, bad-request semantics for
  last-admin protection, and update success payload shape unchanged while
  shrinking another route-side business seam in Rust.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" admin -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_update_user_route_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: the remaining useful user-management seam is now
  mostly `delete_user`; do not keep refactoring admin/user routes further
  unless a clear persistence-heavy path still remains in the handler.

- 2026-05-25 03:53 +08:00 checkpoint:
  this slice finished the remaining useful `delete_user` seam in the
  user-management lane and tightened the route boundary without changing the
  two existing delete contracts. A focused audit of
  `backend-rs/src/api/admin.rs`,
  `backend-rs/src/api/users.rs`, and
  `backend-rs/src/services/user_admin_delete_user_route_service.rs`
  showed that the delete logic was still split across two routes even though
  both paths shared the same persistence-heavy owner concerns:
  - self-delete blocking lived in each route
  - target-user loading lived in each route
  - user/password row deletion lived in each route
  - the only meaningful difference was the route-specific business rule and
    response payload shape:
    - admin delete may remove admin users, but must keep at least one admin
    - users delete must reject admin targets and keep the legacy
      `message + user_id` response shell

  The refactor therefore tightened the owner boundary while preserving the
  separate route contracts:
  - `user_admin_delete_user_route_service.rs` now owns the shared delete
    execution path through one internal mode-aware helper
  - `delete_admin_user_payload(...)` now owns the admin delete rule and keeps
    the legacy admin success shell (`success + message`)
  - `delete_standard_user_payload(...)` now owns the non-admin delete rule and
    keeps the legacy users success shell (`message + user_id`)
  - `api/admin.rs` and `api/users.rs` now keep only admin auth plus thin
    delegation into the appropriate owner

  This keeps the self-delete protection, last-admin protection, non-admin-only
  delete rule, not-found handling, and both route-specific response payload
  shapes unchanged while removing the last high-signal persistence seam from
  the current user-management route lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" admin -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" users -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" user_admin_delete_user_route_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: user-management routes are now mostly thin auth /
  request-conversion / workflow delegation shells. Do not keep splitting this
  lane further unless a new route still clearly owns persistence-heavy logic
  or multi-branch business invariants beyond transport concerns.

- 2026-05-25 04:10 +08:00 checkpoint:
  this slice returned to the Phase 5 chapter-generation runtime lane and
  tightened one remaining cross-lane launch owner split between batch resume
  and single-generation dispatch. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`
  showed that the single-dispatch owner was still carrying one constructor
  that it did not actually own:
  - `SingleGenerationRuntimeLaunchInput::from_execution_config(...)` was only
    consumed by batch resume launch
  - the mapping from `PreparedGenerationExecutionConfig` into
    `SingleChapterGenerationExecutionInput` was therefore hanging off the
    single-generation dispatch layer even though the decision to build a
    single-chapter resume launch belongs to the batch resume launch owner
  - this made the single dispatch contract wider than its actual runtime
    ownership and left a small cross-lane adapter seam in the wrong place

  The refactor therefore tightened the owner boundary without changing runtime
  behavior:
  - `chapter_single_generation_dispatch_service.rs` no longer owns the
    `from_execution_config(...)` constructor
  - `chapter_batch_generation_runtime_launch_service.rs` now builds the
    single-chapter resume runtime input inline at the batch launch boundary
    when the resume execution plan resolves to `SingleChapter`
  - single-generation dispatch is now reduced back to its real owner surface:
    explicit runtime input plus background/runtime dispatch

  This keeps batch create/resume runtime behavior, single-chapter resume
  behavior, execution-config semantics, and task-dispatch contracts unchanged
  while removing one more cross-lane launch adapter from the wrong owner.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: do not keep shuffling single/batch generation
  launch helpers unless a boundary still clearly owns a cross-lane adapter or
  duplicate execution-config mapping beyond explicit transport/runtime
  delegation.

- 2026-05-25 04:27 +08:00 checkpoint:
  this slice stayed in the Phase 5 batch command/read-side lane and tightened
  one remaining response-shape owner split in the command payload adapter. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  showed that cancel and resume command payloads were still hand-maintaining
  the same outer task-summary shell in two branches:
  - `batch_id`
  - `total_chapters`
  - `completed_chapters`
  - route-facing message text
  - the surrounding runtime payload stayed different, but the outer summary
    contract itself had become duplicate adapter ownership

  The refactor therefore tightened the owner boundary without changing either
  payload contract:
  - `chapter_batch_generation_command_payload_adapter_service.rs` now owns one
    shared helper for inserting the outer batch task summary fields
  - cancel response payload assembly and resume response payload assembly now
    both delegate that summary shell to the same helper
  - the resume branch still owns its richer runtime/checkpoint payload shape;
    only the repeated outer summary contract was centralized

  This keeps cancel/resume response JSON shapes, summary field names, message
  strings, and runtime metadata unchanged while removing one more duplicate
  command-payload owner from the batch generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_command_payload_adapter_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep refactoring the command payload adapter
  when two or more command branches still duplicate the same route-facing
  contract shell; do not churn the remaining richer runtime payload paths if
  they already reflect distinct owner semantics.

- 2026-05-25 04:42 +08:00 checkpoint:
  this slice stayed in the Phase 5 batch read-side lane and removed one more
  forwarding-only query owner after confirming it no longer carried distinct
  semantics. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  showed that the status-query file had collapsed into a pure pass-through:
  - it loaded the same owned read context already used by adjacent read-side
    query owners
  - it immediately forwarded that context into the shared status payload
    adapter
  - it no longer owned any route-specific access rule, fallback policy, or
    response-shape logic beyond one-line delegation

  The refactor therefore tightened the read-side owner boundary without
  changing the route contract:
  - `chapter_batch_generation_task_view_query_service.rs` now owns both the
    active-task view payload path and the status-task view payload path
  - `chapter_batch_generation_status_query_service.rs` was deleted
  - `api/chapter_batch_generation.rs` now calls
    `load_owned_batch_generation_status_task_view(...)` directly
  - `services/mod.rs` no longer exports the deleted forwarding module

  This keeps the owned-task lookup contract, status payload shape, route error
  mapping, and batch status response behavior unchanged while removing one more
  no-signal read-side wrapper from the batch generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_payload_adapter_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep removing read-side wrappers when they
  have truly collapsed into “load context -> call shared adapter” forwarding.
  Do not merge files that still own distinct stream semantics, payload
  shaping, or access/error behavior.

- 2026-05-25 04:58 +08:00 checkpoint:
  this slice stayed in the same Phase 5 batch read-side lane and removed one
  more list-query wrapper after confirming it had also collapsed into pure
  forwarding. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_list_query_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that the active-list query file no longer owned independent read-side
  semantics:
  - it loaded active task view contexts from the task-view query owner
  - it immediately mapped those contexts through the same active task payload
    builder
  - it only wrapped the resulting list into the stable `{ total, items }`
    transport-facing envelope

  The refactor therefore tightened the read-side owner boundary without
  changing the route contract:
  - `chapter_batch_generation_task_view_query_service.rs` now also owns the
    active-task list view payload and loader
  - `chapter_batch_generation_active_list_query_service.rs` was deleted
  - `api/chapter_batch_generation.rs` now calls
    `load_active_user_batch_generation_task_list_view(...)` directly
  - `services/mod.rs` no longer exports the deleted forwarding module

  This keeps the active-task list response shape, limit normalization, route
  error mapping, and underlying active-task payload contract unchanged while
  removing another no-signal read-side wrapper from the batch generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: batch read-side query ownership is now close to the
  useful limit. Continue only if another module has clearly collapsed into
  payload-agnostic forwarding; otherwise stop before merging files that still
  own distinct access, list-envelope, or stream semantics.

- 2026-05-25 09:08 +08:00 checkpoint:
  this slice moved to the Phase 5 single-generation lane and removed two
  single-owner wrappers after confirming they had both collapsed into
  workflow-local adapter seams. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_background_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_payload_adapter_service.rs`
  showed that both extra files had become single-point facades under the same
  workflow owner:
  - the background launch file only wrapped
    `SingleGenerationRuntimeLaunchInput::new(...)` and immediately delegated
    into `dispatch_single_chapter_generation_runtime(...)`
  - the payload adapter file only built the fixed background create response
    shell and was only consumed by the same background workflow path
  - neither file still owned cross-route reuse, independent runtime policy,
    or a transport contract shared by multiple lanes

  The refactor therefore tightened the owner boundary without changing the
  route contract:
  - `chapter_single_generation_workflow_service.rs` now directly owns the
    background create response shell through
    `build_single_generation_background_create_response_payload(...)`
  - the same workflow owner now directly builds the runtime input through
    `build_single_generation_background_runtime_input(...)`
  - background runtime dispatch now goes straight through
    `dispatch_single_chapter_generation_runtime(...)`
  - `chapter_single_generation_background_launch_service.rs` was deleted
  - `chapter_single_generation_payload_adapter_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted wrappers

  This keeps the single-generation background response JSON shape, task id
  propagation, runtime input contract, and background dispatch behavior
  unchanged while removing two no-signal wrapper modules from the same
  workflow lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep shrinking `chapter_single_generation`
  when a helper has clearly become a single-owner facade or a cross-lane
  adapter hanging off the wrong runtime boundary. Do not merge the remaining
  stream/runtime/task modules if they still own distinct spawn semantics,
  runtime state mutation, or shared execution policy.

- 2026-05-25 09:16 +08:00 checkpoint:
  this slice moved to the Phase 5 regeneration-stream lane and removed two
  workflow-local stream facades after confirming they no longer carried
  reusable owner semantics outside the same workflow boundary. A focused audit
  of
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/services/chapter_regeneration_full_stream_service.rs`,
  and
  `backend-rs/src/services/chapter_regeneration_partial_stream_service.rs`
  showed that the full/partial stream files had both collapsed into
  same-lane wrappers:
  - both input structs were only created by `prepare_service` and only
    consumed by `stream_workflow_service`
  - both files only converted that prepared input into
    `OwnedRegenerationStreamLaunchInput` plus finalization closures, then
    delegated into `build_owned_regeneration_stream(...)`
  - neither file still owned cross-route reuse, request preparation policy,
    shared stream transport semantics, or independent runtime behavior beyond
    the same workflow lane

  The refactor therefore tightened the owner boundary without changing the
  SSE or payload contract:
  - `chapter_regeneration_prepare_service.rs` now owns the workflow-local
    `FullChapterRegenerationStreamInput` and
    `PartialChapterRegenerationStreamInput` types
  - `chapter_regeneration_stream_workflow_service.rs` now directly owns full
    and partial stream launch assembly, including the launch-input helpers for
    initial events and completion messages
  - `chapter_regeneration_full_stream_service.rs` was deleted
  - `chapter_regeneration_partial_stream_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facades

  This keeps the regeneration SSE start/progress/completion messages, final
  result payload semantics, partial-regeneration progress update cadence, and
  full/partial prepare error contracts unchanged while removing two no-signal
  workflow-local wrappers from the regeneration lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep shrinking regeneration stream helpers
  when a file has clearly become a same-workflow facade or a misplaced
  launch-only adapter. Do not merge `stream_launch`, `stream_execution`, or
  text-finalization owners while they still carry shared SSE transport,
  chunk-stream handling, or finalize/sanitization semantics.

- 2026-05-25 09:21 +08:00 checkpoint:
  this slice moved to the Phase 5 analysis-trigger lane and removed one more
  same-workflow trigger facade after confirming it had collapsed into a thin
  prepare/spawn wrapper under the same write-workflow owner. A focused audit of
  `backend-rs/src/services/chapter_analysis_trigger_service.rs`,
  `backend-rs/src/services/chapter_analysis_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapters_error_mapper.rs`
  showed that the trigger file no longer owned an independent boundary:
  - `prepare_chapter_analysis_trigger(...)` only loaded the accessible chapter
    and delegated task creation to the existing runtime owner
  - `dispatch_prepared_chapter_analysis_trigger(...)` only unpacked the same
    prepared ids and spawned `execute_chapter_analysis_background(...)`
  - the facade was only consumed by the same analysis write-workflow path and
    no longer provided cross-lane reuse beyond that owner

  The refactor therefore tightened the owner boundary without changing the
  route contract:
  - `chapter_analysis_write_workflow_service.rs` now directly owns the
    prepared trigger contract, chapter-access check, task creation handoff,
    and background spawn boundary for analysis trigger
  - `PrepareChapterAnalysisTriggerError` now belongs to the same
    write-workflow owner and `chapters_error_mapper.rs` now imports it from
    there
  - `chapter_analysis_trigger_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the analysis trigger response payload, chapter/project validation
  behavior, background task dispatch path, and route error mapping unchanged
  while removing one more no-signal same-workflow wrapper from the analysis
  lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep shrinking analysis helpers when a file
  has clearly become a same-workflow facade or a misplaced launch-only
  adapter. Do not merge `chapter_analysis_runtime_service.rs`,
  `chapter_analysis_query_service.rs`, or read-side payload/query owners while
  they still carry persistent task state, auto-recovery semantics, or shared
  analysis view contracts.

- 2026-05-25 09:26 +08:00 checkpoint:
  this slice returned to the Phase 5 single-generation task-command lane and
  removed one more single-owner payload facade after confirming it had
  collapsed into the same task-command owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_payload_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`
  showed that the payload helper no longer owned an independent boundary:
  - `single_generation_task_chapter_payload_from_parts(...)` was only consumed
    by the same task-command owner that builds the background task active model
  - the helper only projected the minimal `{ id, chapter_number, title }`
    snapshot shape and did not provide cross-lane reuse or a route-facing
    contract shared elsewhere
  - all meaningful semantics for task creation still lived in the surrounding
    task-command owner: task id allocation, snapshot upsert, checkpoint stage,
    and active-model defaults

  The refactor therefore tightened the owner boundary without changing the
  task payload contract:
  - `chapter_single_generation_task_command_service.rs` now directly owns the
    minimal chapter snapshot payload helper together with the background task
    active-model builder
  - payload contract tests now live beside the real task-command owner
  - `chapter_single_generation_task_payload_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the single-generation background task chapter snapshot shape,
  background task active-model defaults, runtime checkpoint initialization, and
  task creation semantics unchanged while removing one more no-signal payload
  wrapper from the single-generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep shrinking single-generation helpers when
  a file has clearly become a same-owner payload/adapter facade. Do not merge
  `chapter_single_generation_stream_launch_service.rs`,
  `chapter_single_generation_execution_service.rs`, or
  `chapter_single_generation_runtime_state_service.rs` while they still carry
  distinct SSE launch semantics, execution-owner behavior, or runtime state
  mutation boundaries.

- 2026-05-25 09:32 +08:00 checkpoint:
  this slice returned to the Phase 5 single-generation stream lane and
  removed one more same-owner runtime-input adapter after confirming it had
  collapsed into the same workflow owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  and
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  showed that the stream launch file no longer owned an independent launch
  assembly boundary:
  - `build_single_generation_stream_launch(...)` only wrapped
    `SingleGenerationRuntimeLaunchInput::new(...)`
  - the helper was only consumed by the same single-generation workflow owner
    that already prepares the request and chooses the SSE launch boundary
  - the stream launch file still owned real SSE launch semantics, but not the
    workflow-local runtime-input assembly

  The refactor therefore tightened the owner boundary without changing the
  SSE or runtime contract:
  - `chapter_single_generation_workflow_service.rs` now directly owns
    `build_single_generation_stream_runtime_input(...)`
  - `create_single_generation_stream_workflow(...)` now constructs
    `SingleGenerationRuntimeLaunchInput` inside the workflow owner before
    delegating to the SSE launch owner
  - `chapter_single_generation_stream_launch_service.rs` no longer exposes the
    deleted `build_single_generation_stream_launch(...)` adapter and now stays
    focused on stream launch orchestration only

  This keeps the single-generation SSE start/progress/result/done behavior,
  request preparation contract, and runtime launch payload shape unchanged
  while removing one more no-signal workflow-local adapter from the stream
  lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Stop-rule for this lane: only keep shrinking single-generation helpers when
  a file has clearly become a same-owner adapter or facade. Do not merge
  `chapter_single_generation_stream_launch_service.rs`,
  `chapter_single_generation_execution_service.rs`, or
  `chapter_single_generation_runtime_state_service.rs` unless a later audit
  proves one of those files no longer owns distinct SSE launch, execution, or
  runtime-state behavior.

- 2026-05-25 09:44 +08:00 checkpoint:
  this slice stayed in the Phase 5 single-generation runtime lane and removed
  one more shared execution adapter after confirming it had collapsed into a
  pure unpack-and-forward seam. A focused audit of
  `backend-rs/src/services/chapter_single_generation_execution_service.rs`,
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the execution file no longer owned an independent semantic
  boundary:
  - `execute_single_chapter_generation(...)` only destructured
    `SingleGenerationRuntimeLaunchInput`
  - after unpacking, it only instantiated `AIService` and forwarded the same
    explicit fields to the already-owned
    `generate_and_persist_chapter_content_with_provider_payload(...)`
    execution boundary
  - the file was only consumed by the stream-launch and runtime-state owners,
    so it no longer provided a distinct cross-lane contract beyond a shared
    adapter layer

  The refactor therefore tightened the owner boundary without changing the
  execution contract:
  - `chapter_single_generation_dispatch_service.rs` now owns
    `execute_owned_single_chapter_generation(...)` beside
    `SingleGenerationRuntimeLaunchInput`, so the launch input owner now also
    owns the one-step unpack/execute handoff
  - `chapter_single_generation_stream_launch_service.rs` and
    `chapter_single_generation_runtime_state_service.rs` now call that shared
    dispatch-owner execution helper directly
  - `chapter_single_generation_execution_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the single-generation stream result behavior, runtime-state
  progression, AI service construction, generated chapter persistence, and
  launch-input payload contract unchanged while removing one more no-signal
  shared adapter from the single-generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_dispatch_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Additional stop-rule confirmation for this round:
  - `chapter_analysis_batch_status_query_service.rs` was reviewed but not
    modified, because it still owns real batch aggregation semantics
    (`project_id` resolution, per-chapter access/load, and item map assembly)
    rather than a no-signal wrapper
  - do not force that lane further unless a later audit finds a genuine
    duplicated owner split instead of meaningful batch query behavior

- 2026-05-25 10:08 +08:00 checkpoint:
  this slice returned to the Phase 5 batch write-runtime lane and removed one
  more same-workflow launch facade after confirming it had collapsed into a
  pure execution-input assembly plus dispatch wrapper. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_runtime_launch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the runtime-launch file no longer owned an independent boundary:
  - `launch_owned_batch_generation_create_runtime(...)` only unpacked the task
    plan, built `BatchGenerationExecutionInput`, and forwarded it to the
    dispatch owner
  - `build_resume_generation_execution_input(...)` only converted the
    workflow-owned `ResumeExecutionPlan` into the dispatch-owned
    `ResumeGenerationExecutionInput`
  - the file was only consumed by the same batch write-workflow owner and did
    not hold route-facing, persistence, or runtime-state semantics beyond that
    shared adapter layer

  The refactor therefore tightened the owner boundary without changing the
  batch runtime contract:
  - `chapter_batch_generation_dispatch_service.rs` now owns
    `BatchGenerationExecutionInput::new(...)`,
    `build_resume_generation_execution_input(...)`,
    `launch_owned_batch_generation_create_runtime(...)`, and
    `launch_owned_batch_generation_resume_runtime(...)` beside the dispatch
    functions and runtime input enums they serve
  - `chapter_batch_generation_write_workflow_service.rs` now imports the launch
    helpers directly from the dispatch owner
  - `chapter_batch_generation_runtime_launch_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps batch create/resume workflow payloads, prepared execution-config
  ownership, resume execution-target branching, and runtime dispatch behavior
  unchanged while removing one more no-signal same-workflow launch wrapper
  from the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch write/runtime helpers when a file has clearly
    become a same-workflow launch or adapter facade
  - do not merge `chapter_batch_generation_runtime_state_service.rs`,
    `chapter_batch_generation_resume_task_command_service.rs`, or
    `chapter_batch_generation_task_view_query_service.rs` while they still own
    runtime mutation, resume domain semantics, or read-side task projection

- 2026-05-25 10:21 +08:00 checkpoint:
  this slice returned to the Phase 5 single-generation request lane and
  removed one more same-workflow request facade after confirming it had
  collapsed into a minimal route-payload adapter. A focused audit of
  `backend-rs/src/services/chapter_single_generation_request_service.rs`,
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation.rs`
  showed that the request file no longer owned an independent boundary:
  - `SingleChapterGenerationRequest` only carried the minimal
    `{ target_word_count, model }` prepare/workflow input
  - `build_single_chapter_generation_request_from_route_payload(...)` only
    discarded the compat-only `enable_analysis` field and projected the same
    two-field request shape
  - the file was only consumed by the same single-generation workflow/prepare
    lane and did not provide cross-lane reuse, transport mapping, or runtime
    semantics beyond that request adapter layer

  The refactor therefore tightened the owner boundary without changing the
  request contract:
  - `chapter_single_generation_prepare_service.rs` now owns the
    `SingleChapterGenerationRequest` domain request type beside the prepare
    boundary that consumes it
  - `chapter_single_generation_workflow_service.rs` now owns the typed
    `SingleChapterGenerationRouteRequest` and the
    `build_single_chapter_generation_request_from_route_payload(...)` adapter
    beside the background/stream workflow entrypoints that consume it
  - `chapter_batch_generation.rs` now imports the typed route request and
    route-payload adapter directly from the single-generation workflow owner
  - `chapter_single_generation_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the single-generation background/stream route payload contract,
  prepare-request fields, compat-only `enable_analysis` discard behavior, and
  downstream workflow/runtime semantics unchanged while removing one more
  no-signal same-workflow request wrapper from the single-generation lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking single-generation helpers when a file has clearly
    become a same-workflow request, payload, or adapter facade
  - do not merge `chapter_single_generation_prepare_service.rs`,
    `chapter_single_generation_stream_launch_service.rs`, or
    `chapter_single_generation_runtime_state_service.rs` while they still own
    request preparation, SSE launch, or runtime mutation semantics

- 2026-05-25 10:33 +08:00 checkpoint:
  this slice returned to the Phase 5 batch create-request lane and removed one
  more same-workflow compat facade after confirming it had collapsed into a
  minimal route-payload adapter. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_request_compat_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_create_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation.rs`
  showed that the compat file no longer owned an independent boundary:
  - `BatchGenerationCreateWorkflowRequest` only carried the minimal create
    workflow fields consumed by the batch create/write lane
  - `build_batch_generation_create_workflow_request_from_route_payload(...)`
    only projected the route payload into that minimal workflow request while
    discarding compat-only fields such as MCP / web-research / story-repair
    knobs that the current Rust workflow does not consume
  - the file was only consumed by the same batch route/create/write lane and
    did not provide cross-lane reuse, transport mapping, or runtime semantics
    beyond that request adapter layer

  The refactor therefore tightened the owner boundary without changing the
  batch create request contract:
  - `chapter_batch_generation_create_service.rs` now owns
    `BatchGenerationCreateWorkflowRequest` beside the create-prepare boundary
    that consumes it
  - `chapter_batch_generation_write_workflow_service.rs` now owns the typed
    `BatchGenerationCreateRouteRequest` and
    `build_batch_generation_create_workflow_request_from_route_payload(...)`
    beside the create write-workflow entrypoint that consumes it
  - `chapter_batch_generation.rs` now imports the typed route request and
    route-payload adapter directly from the batch write-workflow owner
  - `chapter_batch_generation_request_compat_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch create route payload shape, compat-only field discard
  behavior, workflow request defaults (`enable_analysis=false`,
  `max_retries=3`), and downstream create/runtime semantics unchanged while
  removing one more no-signal same-workflow request wrapper from the batch
  lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_create_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch create/write helpers when a file has clearly
    become a same-workflow request, launch, or adapter facade
  - do not merge `chapter_batch_generation_create_service.rs`,
    `chapter_batch_generation_resume_task_command_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    create-prepare semantics, resume domain behavior, or runtime mutation

- 2026-05-25 10:41 +08:00 checkpoint:
  this slice returned to the Phase 5 analysis batch-status lane and removed
  one more same-route request facade after confirming it had collapsed into a
  minimal route-payload normalizer. A focused audit of
  `backend-rs/src/services/chapter_analysis_batch_status_request_service.rs`,
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  and
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`
  showed that the request file no longer owned an independent boundary:
  - `BatchAnalysisStatusRouteRequest` only exposed the typed `chapter_ids`
    input used by the batch status route
  - `build_batch_analysis_status_request_from_route_payload(...)` only trimmed,
    deduplicated, and capped chapter ids before calling the batch query owner
  - the file was only consumed by the same route entrypoint and did not
    provide cross-lane reuse, query semantics, or transport behavior beyond
    that local request normalization layer

  The refactor therefore tightened the owner boundary without changing the
  batch status request contract:
  - `chapter_analysis_routes.rs` now owns `BatchAnalysisStatusRouteRequest`
    and `build_batch_analysis_status_request_from_route_payload(...)` beside
    the route entrypoint that consumes them
  - `chapter_analysis_batch_status_query_service.rs` remains unchanged as the
    real batch aggregation/query owner
  - `chapter_analysis_batch_status_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch analysis route payload shape, chapter-id trim/dedupe
  semantics, 200-item cap, and downstream batch query behavior unchanged while
  removing one more no-signal same-route request wrapper from the analysis
  lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_batch_status_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis helpers when a file has clearly become a
    same-route request adapter or a same-workflow facade
  - do not merge `chapter_analysis_batch_status_query_service.rs`,
    `chapter_analysis_write_workflow_service.rs`, or
    `chapter_analysis_view_query_service.rs` while they still own batch query
    aggregation, write-workflow semantics, or analysis view contracts

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch active-list lane and removed one
  more same-route request facade after confirming it had collapsed into a
  minimal route-query normalizer. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_list_request_service.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that the request file no longer owned an independent boundary:
  - `ActiveBatchGenerationTaskListRouteQuery` only exposed the typed `limit`
    query input used by the active-task list route
  - `normalize_active_batch_generation_task_list_limit(...)` only applied the
    route-local default (`20`) plus clamp semantics (`1..=100`) before
    calling the task-view query owner
  - the file was only consumed by the same route entrypoint and did not
    provide cross-lane reuse, query semantics, or transport behavior beyond
    that local request normalization layer

  The refactor therefore tightened the owner boundary without changing the
  active-task list request contract:
  - `chapter_batch_generation.rs` now owns
    `ActiveBatchGenerationTaskListRouteQuery` and
    `normalize_active_batch_generation_task_list_limit(...)` beside the route
    entrypoint that consumes them
  - `chapter_batch_generation_task_view_query_service.rs` remains unchanged as
    the real read-side task projection/query owner
  - `chapter_batch_generation_active_list_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the active batch-generation task list query shape, default
  request limit, clamp semantics, and downstream task-view query behavior
  unchanged while removing one more no-signal same-route request wrapper from
  the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation helpers when a file has clearly
    become a same-route request adapter, same-workflow facade, or pure
    unpack-and-forward owner shell
  - do not merge `chapter_batch_generation_task_view_query_service.rs`,
    `chapter_batch_generation_runtime_state_service.rs`, or
    `chapter_batch_generation_resume_task_command_service.rs` while they still
    own read-side task projection/query semantics, runtime mutation, or
    resume-domain behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 analysis draft lane and removed one more
  same-route request facade after confirming it had collapsed into a minimal
  route-query/body normalizer. A focused audit of
  `backend-rs/src/services/chapter_analysis_draft_request_service.rs`,
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  and
  `backend-rs/src/services/chapter_analysis_draft_service.rs`
  showed that the request file no longer owned an independent boundary:
  - `AutoRevisionDraftLookupRouteQuery`,
    `CandidateDraftLookupRouteQuery`,
    `AutoRevisionDraftApplyRouteRequest`, and
    `CandidateDraftApplyRouteRequest`
    only exposed typed route inputs for the analysis draft endpoints
  - `normalize_optional_route_string(...)` only trimmed/emptied optional
    selector strings before projecting them into
    `OwnedDraftPayloadRequest`
  - the file was only consumed by the same route entrypoints and did not
    provide cross-lane reuse, draft query/apply semantics, or transport
    behavior beyond that local request normalization layer

  The refactor therefore tightened the owner boundary without changing the
  analysis draft request contract:
  - `chapter_analysis_routes.rs` now owns the typed draft route query/body
    structs plus the `OwnedDraftPayloadRequest` builder helpers beside the
    route entrypoints that consume them
  - `chapter_analysis_draft_service.rs` remains unchanged as the real owned
    draft query/apply owner
  - `chapter_analysis_draft_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the auto-revision/candidate draft route payload shapes, optional
  selector trim behavior, `allow_stale` handling, and downstream draft
  query/apply behavior unchanged while removing one more no-signal same-route
  request wrapper from the analysis lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis helpers when a file has clearly become a
    same-route request adapter or a same-workflow facade
  - do not merge `chapter_analysis_draft_service.rs`,
    `chapter_analysis_batch_status_query_service.rs`, or
    `chapter_analysis_view_query_service.rs` while they still own draft
    query/apply semantics, batch query aggregation, or analysis view
    contracts

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 analysis view lane and removed one more
  same-owner payload facade after confirming it had collapsed into a minimal
  read-side payload assembler. A focused audit of
  `backend-rs/src/services/chapter_analysis_view_payload_adapter_service.rs`
  and
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`
  showed that the payload file no longer owned an independent boundary:
  - `build_chapter_analysis_view_payload(...)` only assembled the final JSON
    payload from already-loaded chapter / analysis / memory / fragment inputs
  - `value_or_null(...)` only normalized `Option<Value>` fields to JSON `null`
    inside that payload assembly step
  - the file was only consumed by the same
    `chapter_analysis_view_query_service.rs` read-side owner and did not
    provide cross-lane reuse, query semantics, or route-facing transport
    behavior beyond that local payload assembly layer

  The refactor therefore tightened the owner boundary without changing the
  chapter analysis view contract:
  - `chapter_analysis_view_query_service.rs` now owns
    `build_chapter_analysis_view_payload(...)` and `value_or_null(...)`
    beside the read-side query boundary that loads the data and consumes the
    payload
  - `chapter_analysis_view_payload_adapter_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the analysis view payload shape, nullable JSON field behavior,
  memory payload projection, checker/draft/quality fragments, and timestamp
  fallback semantics unchanged while removing one more no-signal same-owner
  payload wrapper from the analysis lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis helpers when a file has clearly become a
    same-route request adapter, same-workflow facade, or same-owner payload
    wrapper
  - do not merge `chapter_analysis_view_query_service.rs`,
    `chapter_analysis_draft_service.rs`, or
    `chapter_analysis_batch_status_query_service.rs` while they still own
    chapter analysis read-side query semantics, draft query/apply semantics,
    or batch query aggregation

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch status-view lane and removed one
  more same-owner payload facade after confirming it had collapsed into a
  minimal read-side status/active payload assembler. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_status_payload_adapter_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that the payload file no longer owned an independent boundary:
  - `task_status_payload(...)` only assembled the status-view JSON payload
    from the already-loaded task, runtime state, and quality-status context
  - `active_task_payload(...)` only assembled the active-task JSON payload
    from the same read-side context inputs
  - the shared helpers there only added view-layer fields such as totals,
    timestamps, retry/terminal metadata, and quality payload fragments
    without owning task-query semantics or route-facing transport behavior
  - the file was only consumed by the same
    `chapter_batch_generation_task_view_query_service.rs` read-side owner and
    did not provide cross-lane reuse beyond that local payload assembly layer

  The refactor therefore tightened the owner boundary without changing the
  batch task-view contract:
  - `chapter_batch_generation_task_view_query_service.rs` now owns
    `task_status_payload(...)`, `active_task_payload(...)`, and their shared
    payload field helpers beside the read-side query boundary that loads the
    task view context and consumes those payloads
  - `chapter_batch_generation_status_payload_adapter_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade
  - `chapter_batch_generation_command_payload_adapter_service.rs` remains
    unchanged because it still spans multiple write-side owners
    (`cancel_service` plus `write_workflow_service`) and is not yet a
    single-owner facade

  This keeps the batch active/status payload shapes, total/completed fields,
  runtime checkpoint projection, terminal resume metadata, and quality-status
  payload fragments unchanged while removing one more no-signal same-owner
  payload wrapper from the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation helpers when a file has clearly
    become a same-route request adapter, same-workflow facade, pure
    unpack-and-forward owner shell, or same-owner payload wrapper
  - do not merge `chapter_batch_generation_task_view_query_service.rs`,
    `chapter_batch_generation_command_payload_adapter_service.rs`,
    `chapter_batch_generation_runtime_state_service.rs`, or
    `chapter_batch_generation_resume_task_command_service.rs` while they still
    own read-side task projection/query semantics, multi-owner write response
    payload assembly, runtime mutation, or resume-domain behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch command-response lane and removed
  one more multi-owner payload facade by splitting it back into the owners
  that actually consume each response shell. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_payload_adapter_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`
  showed that the adapter file no longer needed to exist as a shared owner:
  - `build_batch_generation_create_response_payload(...)` and
    `build_resume_batch_generation_response_payload(...)` were only consumed by
    the batch write-workflow owner
  - `build_cancel_batch_generation_response_payload(...)` was only consumed by
    the cancel owner
  - the previous shared helper
    `insert_batch_generation_task_summary_fields(...)` did not represent a
    domain boundary; it only duplicated a tiny response-shell assembly detail
    across the create/resume/cancel write owners

  The refactor therefore tightened the owner boundary without changing the
  batch command response contracts:
  - `chapter_batch_generation_write_workflow_service.rs` now owns the create
    and resume response payload builders plus the tiny shared summary helper
    they need
  - `chapter_batch_generation_cancel_service.rs` now owns the cancel response
    payload builder beside the cancel workflow boundary that consumes it
  - `chapter_batch_generation_command_payload_adapter_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch create response payload, resume response payload,
  cancel response payload, estimated-time rule, checkpoint/runtime metadata,
  and summary field contract unchanged while removing one more no-signal
  response adapter wrapper from the batch write lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation helpers when a file has clearly
    become a same-route request adapter, same-workflow facade, pure
    unpack-and-forward owner shell, same-owner payload wrapper, or a
    multi-owner payload adapter that can be split cleanly back into the
    consuming owners
  - do not merge `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_task_view_query_service.rs`,
    `chapter_batch_generation_runtime_state_service.rs`, or
    `chapter_batch_generation_resume_task_command_service.rs` while they still
    own write-workflow orchestration, read-side task projection/query
    semantics, runtime mutation, or resume-domain behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 chapter-analysis write lane and removed
  one more same-workflow facade after confirming the trigger wrapper had
  collapsed into a thin owner shell around the runtime write boundary. A
  focused audit of
  `backend-rs/src/services/chapter_analysis_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_analysis_runtime_service.rs`,
  `backend-rs/src/api/chapter_analysis_routes.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs` showed that the write-workflow
  file no longer owned an independent boundary:
  - `prepare_chapter_analysis_trigger(...)` only loaded the accessible chapter
    and forwarded it into `create_chapter_analysis_task(...)`
  - `dispatch_prepared_chapter_analysis_trigger(...)` only spawned the already
    existing runtime background executor with prepared `chapter_id/task_id`
  - `trigger_chapter_analysis_write_workflow(...)` only assembled the existing
    create payload, dispatched the prepared trigger, and returned that payload
  - the file was only consumed by the same route/error-mapper lane and did not
    provide cross-owner reuse beyond that local trigger shell

  The refactor therefore tightened the owner boundary without changing the
  chapter-analysis trigger contract:
  - `chapter_analysis_runtime_service.rs` now owns
    `PrepareChapterAnalysisTriggerError`, the minimal
    `PreparedChapterAnalysisTrigger`, the trigger preparation/dispatch helpers,
    and `trigger_chapter_analysis_write_workflow(...)` beside the existing
    task-create, payload-build, and background-runtime functions it already
    coordinated
  - `chapter_analysis_write_workflow_service.rs` was deleted
  - `chapter_analysis_routes.rs` now imports the trigger workflow directly from
    the runtime owner
  - `chapters_error_mapper.rs` now imports the trigger error type from the
    runtime owner
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the analysis trigger response payload, background spawn behavior,
  chapter-access failure mapping, project-missing failure mapping, and runtime
  execution path unchanged while removing one more no-signal same-workflow
  wrapper from the chapter-analysis write lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapters_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter-analysis helpers when a file has clearly
    become a same-route request adapter, same-workflow facade, same-owner
    payload wrapper, or pure unpack-and-forward owner shell
  - do not merge `chapter_analysis_runtime_service.rs`,
    `chapter_analysis_view_query_service.rs`,
    `chapter_analysis_draft_service.rs`, or
    `chapter_analysis_batch_status_query_service.rs` while they still own
    runtime execution/persistence semantics, analysis view query semantics,
    draft query/apply semantics, or batch status aggregation behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch status-stream lane and removed one
  more same-owner stream-state facade after confirming the status-view wrapper
  had collapsed into a local read-context-to-stream-state adapter. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  showed that the status-view file no longer owned an independent boundary:
  - `build_batch_generation_stream_state(...)` only converted the already
    loaded read context plus resolved stream semantics into
    `BatchGenerationStreamState`
  - `load_optional_owned_batch_generation_stream_state(...)` only loaded the
    optional read context and forwarded it into that local state builder
  - `load_required_owned_batch_generation_stream_state(...)` only loaded the
    required read context and forwarded it into the same local state builder
  - the file was only consumed by the same
    `chapter_batch_generation_status_stream_service.rs` stream owner and did
    not provide cross-lane reuse beyond that stream-state assembly layer

  The refactor therefore tightened the owner boundary without changing the
  batch status-stream contract:
  - `chapter_batch_generation_status_stream_service.rs` now owns
    `build_batch_generation_stream_state(...)` plus the optional/required
    stream-state loaders beside the polling stream boundary that consumes them
  - `chapter_batch_generation_status_view_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch status polling behavior, read-context loading path,
  stream progress/message resolution, terminal-kind projection, event-status
  projection, and not-found/timeout stream behavior unchanged while removing
  one more no-signal same-owner stream-state wrapper from the batch lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation stream/read-side helpers when a file
    has clearly become a same-route request adapter, same-workflow facade,
    same-owner payload or stream-state wrapper, or pure unpack-and-forward
    owner shell
  - do not merge `chapter_batch_generation_status_stream_service.rs`,
    `chapter_batch_generation_status_stream_event_service.rs`,
    `chapter_batch_generation_task_view_query_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    polling stream orchestration, stream event projection semantics, read-side
    task payload/query behavior, or runtime mutation/checkpoint behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch create-write lane and removed one
  more same-workflow task-command facade after confirming the create-task-plan
  wrapper had collapsed into a local write-step helper. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_task_model_service.rs`
  showed that the create-task-command file no longer owned an independent
  boundary:
  - `create_batch_generation_task_plan(...)` only projected chapter ids,
    created the batch task active model, inserted the task, persisted the new
    snapshot, and returned the launch plan consumed by the same write lane
  - `BatchGenerationCreatePlan` was only a launch-contract structure for the
    same dispatch owner that immediately consumes it
  - the file was only consumed by the same
    `chapter_batch_generation_write_workflow_service.rs` create workflow and
    `chapter_batch_generation_dispatch_service.rs` launch boundary, without
    separate cross-lane command semantics beyond that local create step

  The refactor therefore tightened the owner boundary without changing the
  batch create contract:
  - `chapter_batch_generation_write_workflow_service.rs` now owns
    `create_batch_generation_task_plan(...)` beside the create request
    preparation, response payload assembly, execution-config loading, and
    launch handoff that already make up the same write workflow
  - `chapter_batch_generation_dispatch_service.rs` now owns
    `BatchGenerationCreatePlan` beside the launch helpers that consume that
    plan
  - `chapter_batch_generation_create_task_command_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch create response payload, task creation path, snapshot
  creation path, chapter-id ordering, runtime launch plan, and dispatch
  behavior unchanged while removing one more no-signal same-workflow wrapper
  from the batch create lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation create/write helpers when a file has
    clearly become a same-route request adapter, same-workflow facade,
    same-owner payload wrapper, same-workflow task-step wrapper, or pure
    unpack-and-forward owner shell
  - do not merge `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_dispatch_service.rs`,
    `chapter_batch_generation_task_view_query_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    write-workflow orchestration, runtime launch branching, read-side task
    payload/query behavior, or runtime mutation/checkpoint behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch command-error lane and removed one
  more shared error-wrapper facade after confirming the generic
  task/domain/config shell no longer owned an independent batch boundary. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_command_error_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the command-error file no longer needed to exist as a shared
  owner:
  - `OwnedBatchGenerationTaskCommandError<TDomainError>` was only a generic
    `Task + Domain` wrapper consumed by the cancel owner
  - `OwnedBatchGenerationTaskCommandConfigError<TDomainError, TConfigError>`
    was only a generic `Task + Domain + Config` wrapper consumed by the resume
    branch of the batch write-workflow owner
  - the file did not own batch-specific domain semantics, route-facing message
    policy, or cross-lane business behavior beyond that generic enum shell

  The refactor therefore tightened the owner boundary without changing the
  batch command error contract:
  - `chapter_batch_generation_cancel_service.rs` now owns
    `CancelBatchGenerationWorkflowError` as its local `Task + Domain` contract
    beside the cancel workflow that produces it
  - `chapter_batch_generation_write_workflow_service.rs` now owns
    `ResumeBatchGenerationWriteWorkflowError` as its local
    `Task + Domain + Config` contract beside the resume workflow that produces
    it
  - `chapter_batch_generation_error_mapper.rs` now maps those owner-local error
    enums directly instead of routing through a generic shared wrapper helper
  - `chapter_batch_generation_command_error_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch cancel error mapping, batch resume error mapping,
  not-found/internal task lookup handling, domain detail policy, and config
  detail policy unchanged while removing one more no-signal shared error shell
  from the batch command lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation command helpers when a file has
    clearly become a same-route request adapter, same-workflow facade,
    same-owner payload wrapper, same-workflow task-step wrapper, shared error
    shell, or pure unpack-and-forward owner shell
  - do not merge `chapter_batch_generation_cancel_service.rs`,
    `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_dispatch_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    cancel-domain behavior, write-workflow orchestration, runtime launch
    branching, or runtime mutation/checkpoint behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the Phase 5 batch cancel lane and removed one more
  same-workflow task-command facade after confirming the cancel command shell
  had collapsed into a local cancel-step helper. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that the task-command file no longer owned an independent boundary:
  - `CancelBatchGenerationCommandState::from_task(...)` only projected the few
    task fields needed by the cancel workflow
  - `cancel_batch_generation_task(...)` only enforced the local terminal-state
    guard, delegated the runtime mutation to
    `finalize_batch_generation_cancelled(...)`, and returned the small cancel
    response plan consumed by the same cancel owner
  - `CancelBatchGenerationResponseState` and `CancelBatchGenerationPlan` were
    only local carrier structs for the same cancel workflow boundary
  - the file was only consumed by
    `chapter_batch_generation_cancel_service.rs` and did not provide cross-lane
    command semantics beyond that local cancel step

  The refactor therefore tightened the owner boundary without changing the
  batch cancel contract:
  - `chapter_batch_generation_cancel_service.rs` now owns
    `CancelBatchGenerationCommandState`, `CancelBatchGenerationResponseState`,
    `CancelBatchGenerationPlan`, and `cancel_batch_generation_task(...)`
    beside the cancel workflow that consumes those pieces
  - `chapter_batch_generation_task_command_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch cancel response payload, terminal-state guard, task
  projection contract, runtime cancellation mutation path, and route-facing
  cancel error behavior unchanged while removing one more no-signal
  same-workflow wrapper from the batch cancel lane.

  Validation for this slice:
  - `cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation cancel helpers when a file has
    clearly become a same-route request adapter, same-workflow facade,
    same-workflow task-step wrapper, same-owner payload wrapper, or pure
    unpack-and-forward owner shell
  - do not merge `chapter_batch_generation_cancel_service.rs`,
    `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_dispatch_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    cancel-domain behavior, write-workflow orchestration, runtime launch
    branching, or runtime mutation/checkpoint behavior

- 2026-05-25 follow-up checkpoint:
  this slice moved back to the Phase 5 batch create write lane and removed one
  more same-workflow create facade after confirming the create request/prepare
  file had collapsed into a local helper owner for one workflow boundary only.
  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_create_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_model_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`
  showed that the create-service file no longer owned an independent cross-lane
  contract:
  - `BatchGenerationCreateWorkflowRequest` was only the batch create
    write-workflow request object consumed by
    `chapter_batch_generation_write_workflow_service.rs`
  - `PrepareBatchGenerationCreateRequestError` only existed to feed the same
    create write-workflow error boundary plus its route mapper tests
  - `BatchGenerationCreateChapterTarget`,
    `PreparedBatchGenerationCreateRequest`, and
    `prepare_batch_generation_create_request(...)` only supported the same
    create write-workflow/task-plan path and did not provide a separate shared
    owner outside that lane
  - the remaining non-production references were only test projections in
    `chapter_batch_generation_task_model_service.rs`

  The refactor therefore tightened the owner boundary without changing the
  batch create contract:
  - `chapter_batch_generation_write_workflow_service.rs` now owns
    `BatchGenerationCreateWorkflowRequest`,
    `PrepareBatchGenerationCreateRequestError`,
    `BatchGenerationCreateChapterTarget`,
    `PreparedBatchGenerationCreateRequest`, and the local chapter-range /
    target-word-count preparation helpers beside the workflow that consumes
    them
  - `chapter_batch_generation_create_service.rs` was deleted
  - `chapter_batch_generation_task_model_service.rs` test helpers now import
    the create-lane projections from the surviving write-workflow owner
  - `chapter_batch_generation_error_mapper.rs` now imports
    `PrepareBatchGenerationCreateRequestError` from the write-workflow owner
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch create HTTP payload contract, create-workflow request
  normalization, chapter-range validation, normalized target-word-count
  semantics, create response payload, task persistence inputs, and route-facing
  error mapping unchanged while removing one more no-signal same-workflow
  create shell from the batch write lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_model_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_error_mapper -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-generation create helpers when a file has
    clearly become a same-workflow request wrapper, same-workflow prepare
    wrapper, same-owner response/payload wrapper, or pure unpack-and-forward
    create-lane shell
  - do not merge
    `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_dispatch_service.rs`,
    `chapter_batch_generation_resume_semantics_service.rs`, or
    `chapter_batch_generation_runtime_state_service.rs` while they still own
    write-workflow orchestration, runtime launch branching, resume semantics,
    or runtime mutation/checkpoint behavior

- 2026-05-25 follow-up checkpoint:
  this slice shifted to the adjacent single-generation runtime lane and removed
  one more same-runtime dispatch facade after confirming the single-generation
  dispatch file had collapsed into a local wrapper around the existing runtime
  owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`
  showed that the dispatch file no longer owned an independent lane:
  - `SingleGenerationRuntimeLaunchInput` was the shared runtime input object
    already consumed by the single-generation runtime owner, stream launch
    owner, workflow owner, and batch resume dispatch branch
  - `dispatch_single_chapter_generation_runtime(...)` only wrapped
    `tokio::spawn` for `execute_single_generation_runtime(...)` and did not
    own separate branching, payload, or persistence semantics
  - `execute_owned_single_chapter_generation(...)` only unpacked the launch
    input and forwarded to the same generation runtime service already used by
    the single-generation runtime owner and the single-generation stream lane

  The refactor therefore tightened the owner boundary without changing the
  single-generation contract:
  - `chapter_single_generation_runtime_state_service.rs` now owns
    `SingleGenerationRuntimeLaunchInput`,
    `dispatch_single_chapter_generation_runtime(...)`, and
    `execute_owned_single_chapter_generation(...)` beside the runtime workflow
    that actually consumes those pieces
  - `chapter_single_generation_dispatch_service.rs` was deleted
  - `chapter_single_generation_workflow_service.rs`,
    `chapter_single_generation_stream_launch_service.rs`, and
    `chapter_batch_generation_dispatch_service.rs` now import the shared
    single-generation runtime input/dispatch helpers from the surviving
    runtime-state owner
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the single-generation background launch contract, stream launch
  contract, batch resume single-chapter branch, runtime snapshot/task mutation
  path, and generation execution behavior unchanged while removing one more
  no-signal same-runtime dispatch shell from the single-generation lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_launch_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_dispatch_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking single-generation helpers when a file has clearly
    become a same-runtime input wrapper, same-workflow request adapter,
    same-stream payload wrapper, or pure unpack-and-forward execution shell
  - do not merge
    `chapter_single_generation_runtime_state_service.rs`,
    `chapter_single_generation_workflow_service.rs`,
    `chapter_single_generation_stream_launch_service.rs`, or
    `chapter_batch_generation_dispatch_service.rs` while they still own
    runtime mutation/checkpoint behavior, background/stream workflow
    orchestration, stream SSE behavior, or batch-vs-single resume branching

- 2026-05-25 follow-up checkpoint:
  this slice stayed in the single-generation background lane and removed one
  more same-workflow task-command facade after confirming the task-command file
  had collapsed into a local task-plan helper for one workflow boundary only.
  A focused audit of
  `backend-rs/src/services/chapter_single_generation_task_command_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  and
  `backend-rs/src/api/chapter_batch_generation.rs`
  showed that the task-command file no longer owned an independent cross-lane
  contract:
  - `single_generation_task_chapter_payload_from_parts(...)` only built the
    single-chapter task snapshot payload consumed by the same background
    workflow lane
  - `build_single_generation_background_task_active_model(...)` only wrapped
    the shared batch-task active-model builder for that same background task
    plan path
  - `create_single_generation_background_task_plan(...)` and
    `create_single_generation_background_task_plan_from_prepared_request(...)`
    were only used by
    `start_owned_single_generation_background_workflow(...)` and did not
    provide an independent owner outside that workflow

  The refactor therefore tightened the owner boundary without changing the
  single-generation background contract:
  - `chapter_single_generation_workflow_service.rs` now owns the background
    task-plan helpers, task chapter snapshot payload builder, and active-model
    builder beside the workflow that consumes them
  - `chapter_single_generation_task_command_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the single-generation background HTTP payload contract, task
  creation defaults, pending runtime snapshot shape, chapter snapshot payload,
  runtime dispatch behavior, and route-facing error mapping unchanged while
  removing one more no-signal same-workflow task-command shell from the
  single-generation background lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking single-generation background helpers when a file has
    clearly become a same-workflow request adapter, same-workflow task-plan
    wrapper, same-owner response/payload wrapper, or pure unpack-and-forward
    background shell
  - do not merge
    `chapter_single_generation_workflow_service.rs`,
    `chapter_single_generation_runtime_state_service.rs`,
    `chapter_single_generation_stream_launch_service.rs`, or
    `chapter_batch_generation.rs` while they still own background/stream
    workflow orchestration, runtime mutation/checkpoint behavior, stream SSE
    behavior, or route transport compatibility

- 2026-05-25 follow-up checkpoint:
  this slice stayed on the single-generation workflow lane after the last
  facade deletions and removed three remaining file-local no-op wrappers that
  no longer carried independent ownership value. A focused audit of
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`,
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`,
  and
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`
  showed that the workflow file still kept three local pass-through shells:
  - `create_single_generation_background_task_plan_from_prepared_request(...)`
    only unpacked the prepared request and forwarded to the same background
    task-plan owner in the same file
  - `build_single_generation_background_runtime_input(...)` only wrapped
    `SingleGenerationRuntimeLaunchInput::new(...)` with `user_id.to_string()`
  - `build_single_generation_stream_runtime_input(...)` only wrapped
    `SingleGenerationRuntimeLaunchInput::new(...)` with identical semantics

  The refactor therefore tightened the owner boundary without changing the
  single-generation workflow contract:
  - `start_owned_single_generation_background_workflow(...)` now calls
    `create_single_generation_background_task_plan(...)` directly with the
    prepared chapter target plus normalized target-word-count
  - background and stream workflow paths now construct
    `SingleGenerationRuntimeLaunchInput` directly at the workflow boundary
    instead of routing through file-local wrapper helpers
  - workflow-local tests now assert the same launch input contract directly
    against `SingleGenerationRuntimeLaunchInput::new(...)`

  This keeps the single-generation background payload, stream launch behavior,
  prepared request consumption, runtime dispatch contract, and route-facing
  error behavior unchanged while removing one more set of no-signal same-file
  wrappers from the workflow lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - do not keep collapsing single-generation workflow internals if the next
    move only rewrites already-direct owner logic without deleting a fake
    boundary or removing repeated compatibility-sensitive interpretation
  - prefer the next Phase 5 slice in an adjacent lane unless another concrete
    same-workflow request/launch/response shell is still visible

- 2026-05-25 follow-up checkpoint:
  this slice returned to the batch write/runtime lane and removed one more
  same-write-lane dispatch facade after confirming the batch dispatch file had
  collapsed into local launch/input helpers for the same write/runtime owners.
  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_dispatch_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`,
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  showed that the dispatch file no longer owned an independent lane:
  - `BatchGenerationExecutionInput` was the concrete runtime input already
    consumed only by the batch runtime owner
  - `dispatch_batch_generation_runtime(...)` only wrapped `tokio::spawn` for
    `execute_batch_generation_runtime(...)` and did not own separate branching
    or persistence semantics
  - `build_resume_generation_execution_input(...)`,
    `launch_owned_batch_generation_create_runtime(...)`, and
    `launch_owned_batch_generation_resume_runtime(...)` only served the same
    write-workflow owner and its adjacent single/batch resume branching

  The refactor therefore tightened the owner boundary without changing the
  batch create/resume contract:
  - `chapter_batch_generation_runtime_state_service.rs` now owns
    `BatchGenerationExecutionInput` and
    `dispatch_batch_generation_runtime(...)` beside the batch runtime executor
    that consumes those pieces
  - `chapter_batch_generation_write_workflow_service.rs` now owns the
    create/resume launch branching and local resume execution-input assembly
    beside the write workflow that consumes those pieces
  - `chapter_batch_generation_dispatch_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the batch create payload, batch resume payload, single-vs-batch
  resume branching, runtime dispatch behavior, execution-config ownership, and
  route-facing error behavior unchanged while removing one more no-signal
  same-write-lane dispatch shell from the batch lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch write/runtime helpers when a file has clearly
    become a same-write-lane launch wrapper, same-runtime input wrapper,
    same-workflow response adapter, or pure unpack-and-forward branch shell
  - do not merge
    `chapter_batch_generation_write_workflow_service.rs`,
    `chapter_batch_generation_runtime_state_service.rs`,
    `chapter_batch_generation_resume_semantics_service.rs`, or
    `chapter_single_generation_runtime_state_service.rs` while they still own
    write-workflow orchestration, runtime mutation/checkpoint behavior, resume
    semantics, or single-generation runtime behavior

- 2026-05-25 follow-up checkpoint:
  this slice returned to the batch active-query lane and removed one more
  same-query-lane facade after confirming the active query file had collapsed
  into access-check plus response-envelope forwarding for the same read/view
  owner. A focused audit of
  `backend-rs/src/services/chapter_batch_generation_active_query_service.rs`,
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`,
  and `backend-rs/src/api/chapter_batch_generation.rs` showed that the active
  query file no longer owned an independent seam:
  - `load_active_batch_generation_query(...)` had only one route caller
  - the file only checked owned project access, delegated active-task context
    loading to the adjacent task-view query owner, and wrapped the result in a
    small `has_active_task/task` envelope
  - its tests only protected the same response-envelope shape and
    `ProjectAccessQueryError` contract already adjacent to the query lane

  The refactor therefore tightened the owner boundary without changing the
  active batch-query contract:
  - `chapter_batch_generation_task_view_query_service.rs` now owns
    `build_active_batch_generation_query_response(...)` and
    `load_active_batch_generation_query(...)` beside the active task-view
    context loader it already used
  - `chapter_batch_generation.rs` now imports the active-query entrypoint from
    the task-view query owner directly
  - `chapter_batch_generation_active_query_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the active batch-generation response envelope, owned-project
  access gate, internal error mapping, and route-facing contract unchanged
  while removing one more no-signal same-query-lane wrapper from the batch
  read/query path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch read/query helpers when a file has clearly
    become a same-query-lane access wrapper, response-envelope shell, or pure
    context-forwarding facade
  - do not merge
    `chapter_batch_generation_task_view_query_service.rs`,
    `chapter_batch_generation_read_context_service.rs`, or
    `chapter_batch_generation_quality_status_service.rs` while they still own
    task-view payload assembly, read-context loading, or quality-status
    semantics as separate behavior boundaries

- 2026-05-25 follow-up checkpoint:
  this slice moved to the chapter quality-metrics query lane and removed one
  more same-read-lane query-context facade after confirming the file had
  collapsed into a single consumer plus field-trimming wrapper for the shared
  analysis read-context owner. A focused audit of
  `backend-rs/src/services/chapter_quality_metrics_query_context_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs`, and
  `backend-rs/src/services/chapter_analysis_read_context_service.rs` showed
  that the query-context file no longer owned an independent seam:
  - `load_chapter_quality_metrics_query_context(...)` had only one consumer
    in the quality-metrics query owner
  - the file only delegated to `load_chapter_analysis_read_context(...)` and
    copied through `candidate_attempt` plus `histories`
  - it did not own extra filtering, auth, payload shaping, or error semantics
    beyond that same read-context boundary

  The refactor therefore tightened the owner boundary without changing the
  quality-metrics query contract:
  - `chapter_quality_metrics_query_service.rs` now reads the shared analysis
    read context directly beside the fragment builder that consumes those
    fields
  - `chapter_quality_metrics_query_context_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps quality-metrics payload assembly, shared read-context loading,
  auth/error ownership, and route-facing behavior unchanged while removing one
  more no-signal same-read-lane field-trimming shell from the analysis/query
  path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis/query helpers when a file has clearly become
    a same-read-lane field-trimming wrapper, same-owner response shell, or
    pure context-forwarding facade
  - do not merge
    `chapter_analysis_read_context_service.rs`,
    `chapter_quality_metrics_query_service.rs`, or
    `chapter_analysis_view_query_service.rs` while they still own shared
    analysis read loading, quality fragment assembly, or analysis-view payload
    composition as separate behavior boundaries

- 2026-05-25 follow-up checkpoint:
  this slice moved to the chapter CRUD/query lane and removed one more
  same-query-lane annotations facade after confirming the file had collapsed
  into access-check plus constant empty-payload forwarding for the same
  chapter query owner. A focused audit of
  `backend-rs/src/services/chapter_annotation_query_service.rs`,
  `backend-rs/src/services/chapter_query_service.rs`, and
  `backend-rs/src/api/chapter_crud_routes.rs` showed that the annotations file
  no longer owned an independent seam:
  - `load_annotations_payload(...)` had only one route caller
  - the file only checked accessible chapter ownership and returned a fixed
    empty annotations/memory-mapping payload
  - its tests only protected that empty payload shape and the
    `LoadAccessibleChapterError` alias already adjacent to the chapter query
    lane

  The refactor therefore tightened the owner boundary without changing the
  annotations query contract:
  - `chapter_query_service.rs` now owns `annotations_payload(...)` and
    `load_annotations_payload(...)` beside the other chapter read/query
    payload helpers
  - `chapter_crud_routes.rs` now imports the annotations query entrypoint from
    the chapter query owner directly
  - `chapters_error_mapper.rs` now reads `LoadAnnotationsPayloadError` from
    the same query owner
  - `chapter_annotation_query_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps the empty annotations payload shape, chapter access gate,
  route-facing error mapping, and CRUD route transport behavior unchanged
  while removing one more no-signal same-query-lane shell from the chapter
  read/query path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter read/query helpers when a file has clearly
    become a same-query-lane access wrapper, constant response shell, or pure
    transport-adjacent field adapter
  - do not merge
    `chapter_query_service.rs`,
    `chapter_quality_query_service.rs`, or
    `chapter_crud_workflow_service.rs` while they still own distinct chapter
    read payloads, project-quality payloads, or write-workflow behavior

- 2026-05-26 follow-up checkpoint:
  this slice moved to the analysis draft lane and removed one more same-lane
  draft query facade after confirming the file had collapsed into local source
  loading plus payload assembly for the same owned-draft owner. A focused
  audit of
  `backend-rs/src/services/chapter_draft_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_draft_service.rs`, and
  `backend-rs/src/services/chapter_draft_source_service.rs` showed that the
  draft query file no longer owned an independent seam:
  - `load_candidate_draft_payload(...)` and
    `load_auto_revision_draft_payload(...)` had only one consumer in the
    owned-draft service
  - the file only loaded source records, mapped local not-found/internal
    errors, and assembled route-facing draft payloads with existing payload
    builders
  - it did not own extra access, selection-mode, or route transport semantics
    beyond the same owned-draft boundary

  The refactor therefore tightened the owner boundary without changing the
  analysis draft contract:
  - `chapter_analysis_draft_service.rs` now owns the candidate/auto-revision
    draft query helpers beside the owned-draft access, selection-mode, and
    error-owner logic that already consumes them
  - `chapter_draft_query_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps candidate-draft payload shape, auto-revision payload shape,
  chapter-access gating, draft selection-mode semantics, and route-facing
  error ownership unchanged while removing one more no-signal same-lane query
  helper from the analysis draft path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis draft helpers when a file has clearly become
    a same-lane source-loading shell, same-owner payload wrapper, or pure
    selection-mode forwarding helper
  - do not merge
    `chapter_analysis_draft_service.rs`,
    `chapter_draft_apply_service.rs`, or
    `chapter_draft_view_payload_service.rs` while they still own owned-draft
    access semantics, draft apply behavior, or payload-shaping behavior as
    separate boundaries

- 2026-05-26 follow-up checkpoint:
  this slice moved to the regeneration route-adjacent request lane and
  removed one more same-route-lane request facade after confirming the file
  had collapsed into a single route caller plus limit normalization shell. A
  focused audit of
  `backend-rs/src/services/chapter_regeneration_tasks_request_service.rs` and
  `backend-rs/src/api/chapter_regeneration_routes.rs` showed that the request
  file no longer owned an independent seam:
  - `RegenerationTasksRouteQuery` and
    `normalize_regeneration_tasks_limit_from_route_query(...)` had only one
    route caller
  - the file only normalized `limit` with default/min/max bounds and did not
    own query execution, auth, payload shaping, or error semantics
  - the normalization is route-adjacent transport interpretation, so the
    honest owner is the regeneration route itself rather than a separate
    service lane

  The refactor therefore tightened the owner boundary without changing the
  regeneration tasks query contract:
  - `chapter_regeneration_routes.rs` now owns
    `RegenerationTasksRouteQuery` and the local limit normalization helper
    beside the route that consumes that transport input
  - `chapter_regeneration_tasks_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps regeneration-task list limit defaults/bounds, route request
  parsing, owned-chapter access behavior, and route-facing error mapping
  unchanged while removing one more no-signal route-adjacent request shell
  from the regeneration path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking regeneration route-adjacent helpers when a file has
    clearly become a same-route-lane typed-query shell, simple bounds
    normalizer, or pure transport field adapter
  - do not merge
    `chapter_regeneration_query_service.rs`,
    `chapter_regeneration_stream_request_service.rs`, or
    `chapter_regeneration_stream_workflow_service.rs` while they still own
    regeneration task payloads, stream request preparation, or stream
    execution/workflow behavior as separate boundaries

- 2026-05-26 follow-up checkpoint:
  this slice returned to the chapter CRUD/query lane and removed one more
  same-query-lane facade after confirming the file had collapsed into a
  single route caller plus project-quality payload forwarding for the same
  chapter query owner. A focused audit of
  `backend-rs/src/services/chapter_quality_query_service.rs`,
  `backend-rs/src/services/chapter_query_service.rs`,
  `backend-rs/src/api/chapter_crud_routes.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs` showed that the quality query
  file no longer owned an independent seam:
  - `load_quality_trend_payload(...)` had only one route caller
  - the file only listed project chapters, mapped local not-found/internal
    errors, and assembled the quality-trend payload already adjacent to the
    chapter read/query lane
  - its tests only protected the error alias contract already meaningful next
    to the shared chapter query owner

  The refactor therefore tightened the owner boundary without changing the
  quality-trend query contract:
  - `chapter_query_service.rs` now owns `quality_trend_payload(...)`,
    `load_quality_trend_payload(...)`, and
    `LoadQualityTrendPayloadError` beside the other chapter read/query payload
    helpers
  - `chapter_crud_routes.rs` now imports the quality-trend query entrypoint
    from the chapter query owner directly
  - `chapters_error_mapper.rs` now reads `LoadQualityTrendPayloadError` from
    the same query owner
  - `chapter_quality_query_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps quality-trend payload shape, project access/not-found mapping,
  route-facing error behavior, and CRUD route transport behavior unchanged
  while removing one more no-signal same-query-lane shell from the chapter
  read/query path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter read/query helpers when a file has clearly
    become a same-query-lane access wrapper, constant response shell, or
    same-owner payload forwarder
  - do not merge
    `chapter_query_service.rs`,
    `chapter_crud_workflow_service.rs`, or
    `chapter_analysis_view_query_service.rs` unless the next file has fully
    collapsed into that same read/query boundary without retaining distinct
    access, payload, or workflow ownership

- 2026-05-26 follow-up checkpoint:
  this slice moved to the analysis query lane and removed one more same-lane
  batch-status facade after confirming the file had collapsed into a single
  route caller plus batch payload assembly around the existing analysis task
  query owner. A focused audit of
  `backend-rs/src/services/chapter_analysis_batch_status_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_query_service.rs`, and
  `backend-rs/src/api/chapter_analysis_routes.rs` showed that the batch-status
  file no longer owned an independent seam:
  - `load_batch_analysis_task_status_payload(...)` had only one route caller
  - the file only normalized the empty response, looped chapter lookups, and
    reused `latest_analysis_task(...)` plus
    `analysis_task_status_payload(...)` from the same analysis-query lane
  - it did not own separate access semantics, background-dispatch behavior,
    or route transport logic beyond that same read/query boundary

  The refactor therefore tightened the owner boundary without changing the
  analysis batch-status contract:
  - `chapter_analysis_query_service.rs` now owns
    `empty_batch_analysis_task_status_payload(...)`,
    `build_batch_analysis_task_status_payload(...)`, and
    `load_batch_analysis_task_status_payload(...)` beside the single-task
    status helpers it already reuses
  - `chapter_analysis_routes.rs` now imports the batch-status query entrypoint
    from the analysis query owner directly
  - `chapter_analysis_batch_status_query_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps batch analysis-status payload shape, per-chapter task fallback,
  route-facing internal error mapping, and transport behavior unchanged while
  removing one more no-signal same-lane batch query shell from the analysis
  read/query path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis query helpers when a file has clearly become
    a same-lane payload shell, response-envelope wrapper, or pure reuse layer
    around the existing task-status/query owner
  - do not merge
    `chapter_analysis_query_service.rs`,
    `chapter_analysis_view_query_service.rs`, or
    `chapter_quality_metrics_query_service.rs` while they still own task
    status semantics, analysis-view composition, or quality-fragment assembly
    as separate behavior boundaries

- 2026-05-26 follow-up checkpoint:
  this slice returned to the regeneration route-adjacent request lane and
  removed one more same-route-lane request facade after confirming the file
  had collapsed into a single route caller plus three-field internal request
  forwarding for the same partial-regeneration apply owner. A focused audit of
  `backend-rs/src/services/chapter_regeneration_request_service.rs`,
  `backend-rs/src/api/chapter_regeneration_routes.rs`, and
  `backend-rs/src/services/chapter_regeneration_apply_service.rs` showed that
  the request file no longer owned an independent seam:
  - `ApplyPartialRegenerateRouteRequest` and
    `build_apply_partial_regenerate_request_from_route_payload(...)` had only
    one route caller
  - the file only copied `new_text/start_position/end_position` into the
    internal request and did not own apply semantics, access checks, payload
    shaping, or error mapping
  - the internal `ApplyPartialRegenerateRequest` type itself had only one
    consumer in the apply owner, so keeping a separate request facade added no
    signal

  The refactor therefore tightened the owner boundary without changing the
  partial-regenerate apply contract:
  - `chapter_regeneration_routes.rs` now owns
    `ApplyPartialRegenerateRouteRequest` and the local route-payload ->
    internal-request builder beside the route that consumes it
  - `chapter_regeneration_apply_service.rs` now owns
    `ApplyPartialRegenerateRequest` beside the apply workflow that consumes
    its normalized getters
  - `chapter_regeneration_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps partial-regenerate request defaults, route request parsing,
  apply payload behavior, and route-facing error mapping unchanged while
  removing one more no-signal route-adjacent request shell from the
  regeneration path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_apply_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking regeneration route-adjacent helpers when a file has
    clearly become a same-route-lane typed payload shell, bounds normalizer,
    or pure transport field adapter
  - do not merge
    `chapter_regeneration_apply_service.rs`,
    `chapter_regeneration_stream_request_service.rs`, or
    `chapter_regeneration_stream_workflow_service.rs` while they still own
    apply semantics, stream request preparation, or stream execution/workflow
    behavior as separate boundaries

- 2026-05-26 follow-up checkpoint:
  this slice returned to the analysis/quality helper lane and removed two
  more same-owner helper facades after confirming each file had collapsed into
  a single service consumer plus local fragment/source derivation for that
  same owner. A focused audit of
  `backend-rs/src/services/chapter_analysis_checker_query_service.rs`,
  `backend-rs/src/services/chapter_analysis_view_query_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_source_service.rs`, and
  `backend-rs/src/services/chapter_quality_metrics_query_service.rs` showed
  that the helper files no longer owned independent seams:
  - `build_chapter_analysis_checker_fragments(...)` and
    `ChapterAnalysisCheckerFragments` had only one consumer in the analysis
    view query owner
  - `resolve_quality_metrics_source(...)` and its local source struct had only
    one consumer in the quality-metrics query owner
  - neither file owned separate route access, error mapping, workflow
    behavior, or cross-lane contracts beyond the same query/helper boundary

  The refactor therefore tightened those owner boundaries without changing the
  analysis-view or quality-metrics contracts:
  - `chapter_analysis_view_query_service.rs` now owns checker fragment parsing
    and the `ChapterAnalysisCheckerFragments` helper struct beside the
    analysis-view assembly that consumes them
  - `chapter_quality_metrics_query_service.rs` now owns quality-metrics source
    resolution and the local resolved-source struct beside the fragment
    builders that consume them
  - `chapter_analysis_checker_query_service.rs` was deleted
  - `chapter_quality_metrics_source_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted helpers

  This keeps checker fragment selection, quality-metrics source precedence,
  analysis-view payload shape, and quality-metrics payload behavior unchanged
  while removing two more no-signal same-owner helper shells from the
  analysis/query path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking analysis/quality helpers when a file has clearly
    become a single-owner fragment parser, source resolver, or local payload
    derivation helper with no independent cross-lane contract
  - do not merge
    `chapter_analysis_view_query_service.rs`,
    `chapter_quality_metrics_query_service.rs`, or
    `chapter_draft_view_payload_service.rs` while they still own analysis-view
    composition, quality fragment assembly, or shared draft payload shaping as
    separate behavior boundaries

- 2026-05-26 follow-up checkpoint:
  this slice returned to the chapter generation / CRUD / quality adapter lane
  and removed three more single-owner helper files after confirming each file
  had collapsed into one consumer plus local data-shaping logic for that same
  owner. A focused audit of
  `backend-rs/src/services/chapter_generation_prompt_params_service.rs`,
  `backend-rs/src/services/chapter_generation_prompt_service.rs`,
  `backend-rs/src/services/chapter_crud_payload_adapter_service.rs`,
  `backend-rs/src/services/chapter_crud_workflow_service.rs`,
  `backend-rs/src/services/chapter_quality_metrics_payload_adapter_service.rs`,
  and `backend-rs/src/services/chapter_quality_metrics_query_service.rs`
  showed that the helper files no longer owned independent seams:
  - prompt-params building had only one consumer in the prompt owner
  - CRUD compat payload shaping had only one consumer in the CRUD workflow
    owner
  - quality-metrics payload shaping had only one consumer in the quality query
    owner
  - none of those files owned separate route access, error mapping, workflow
    branching, or cross-lane contracts beyond the same local owner boundary

  The refactor therefore tightened those owner boundaries without changing the
  generation prompt, CRUD payload, or quality-metrics payload contracts:
  - `chapter_generation_prompt_service.rs` now owns prompt param assembly
    beside prompt-template selection and formatting
  - `chapter_crud_workflow_service.rs` now owns CRUD compat payload shaping
    beside the workflow branches that return those payloads
  - `chapter_quality_metrics_query_service.rs` now owns
    `ChapterQualityMetricsFragments` and final payload assembly beside the
    fragment builders that already consume them
  - `chapter_generation_prompt_params_service.rs` was deleted
  - `chapter_crud_payload_adapter_service.rs` was deleted
  - `chapter_quality_metrics_payload_adapter_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted helpers

  This keeps chapter prompt parameter defaults, CRUD response envelope shape,
  and quality-metrics payload shape unchanged while removing three more
  no-signal same-owner helper shells from the chapter Rust lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter helper files when a file has clearly become a
    single-owner param builder, compat payload shaper, or local response
    envelope helper with no independent cross-lane contract
  - do not merge
    `chapter_generation_prompt_service.rs`,
    `chapter_crud_workflow_service.rs`, or
    `chapter_quality_metrics_query_service.rs` once the next step would only
    flatten still-meaningful owner boundaries instead of removing a true
    single-consumer shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the generation lane and removed two more single-owner
  helper/context shells after confirming both files had collapsed into one
  consumer plus local same-owner derivation logic. A focused audit of
  `backend-rs/src/services/chapter_generation_context_service.rs`,
  `backend-rs/src/services/chapter_generation_runtime_service.rs`,
  `backend-rs/src/services/chapter_generation_prompt_context_service.rs`, and
  `backend-rs/src/services/chapter_generation_prompt_service.rs` showed that
  the helper files no longer owned independent seams:
  - `load_generation_context(...)` and `LoadGenerationContextError` had only
    one consumer in the generation runtime owner
  - `build_prompt_context_params_with_provider_payload(...)` had only one
    consumer in the generation prompt owner
  - neither file owned separate route access, workflow branching, payload
    envelopes, or cross-lane contracts beyond the same local owner boundary

  The refactor therefore tightened those owner boundaries without changing the
  generation runtime or prompt contracts:
  - `chapter_generation_runtime_service.rs` now owns generation context
    loading plus `LoadGenerationContextError` beside the runtime mapping and
    persistence flow that consume them
  - `chapter_generation_prompt_service.rs` now owns provider-context param
    injection plus continuation-point derivation beside template selection and
    prompt formatting
  - `chapter_generation_context_service.rs` was deleted
  - `chapter_generation_prompt_context_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted helpers

  This keeps generation access messages, project/previous-chapter lookup
  behavior, provider payload injection, prompt context defaults, and prompt
  formatting behavior unchanged while removing two more no-signal same-owner
  helper shells from the generation path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking generation helpers when a file has clearly become a
    single-owner context loader, prompt-context injector, or local adapter
    shell with no independent cross-lane contract
  - do not merge
    `chapter_generation_runtime_service.rs` or
    `chapter_generation_prompt_service.rs` once the next step would only
    flatten still-meaningful runtime/prompt owner boundaries instead of
    removing a true single-consumer shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to the CRUD write lane and removed one more request-shell
  facade after confirming the file had collapsed into route-local payload
  parsing plus workflow-local normalized request storage for the same write
  boundary. A focused audit of
  `backend-rs/src/services/chapter_crud_request_service.rs`,
  `backend-rs/src/api/chapter_crud_routes.rs`, and
  `backend-rs/src/services/chapter_crud_workflow_service.rs` showed that the
  request file no longer owned an independent seam:
  - `CreateChapterRouteRequest`, `UpdateChapterRouteRequest`, and
    `UpdateExpansionPlanRouteRequest` had only one route owner in
    `chapter_crud_routes.rs`
  - `CreateChapterRequest`, `UpdateChapterRequest`, and
    `UpdateExpansionPlanRequest` had only one workflow owner in
    `chapter_crud_workflow_service.rs`
  - the file only copied route fields into normalized request objects and did
    not own payload shaping, error mapping, access checks, or service
    branching beyond the same CRUD write lane

  The refactor therefore tightened that owner boundary without changing the
  CRUD route or workflow contract:
  - `chapter_crud_routes.rs` now owns the typed route payload structs and the
    local route-payload -> workflow-request builders beside the routes that
    consume them
  - `chapter_crud_workflow_service.rs` now owns the normalized CRUD request
    structs and their field getters beside the workflow branches that consume
    them
  - `chapter_crud_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps create/update/expansion-plan request parsing, CRUD response
  payload shape, error mapping, and `ChapterService` call semantics unchanged
  while removing one more no-signal same-lane request shell from the chapter
  Rust path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking CRUD request/helpers when a file has clearly become a
    route-local typed payload shell or workflow-local normalized request shell
    with no independent cross-lane contract
  - do not merge
    `chapter_crud_workflow_service.rs` or
    `chapter_crud_routes.rs` once the next step would only flatten still-
    meaningful route/workflow ownership instead of removing a true low-signal
    facade

- 2026-05-26 follow-up checkpoint:
  this slice returned to the regeneration stream lane and removed two more
  no-signal wrapper contracts after confirming they had collapsed into
  single-consumer passthrough fields with no independent behavior value. A
  focused audit of
  `backend-rs/src/services/chapter_regeneration_text_service.rs`,
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`, and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that:
  - `FinalizedPartialRegenerationResult` and
    `FinalizedChapterRegenerationResult` only wrapped a single `payload`
    field, and production consumers immediately unwrapped that payload in the
    stream workflow owner
  - `FullChapterRegenerationStreamInput.task_label` was always set to the same
    `"Chapter Rewrite"` literal and only forwarded into the launch input, so
    keeping it in the prepared input contract added no real seam

  The refactor therefore tightened those regeneration contracts without
  changing stream payload shape or launch behavior:
  - `finalize_partial_regeneration_result(...)` and
    `finalize_chapter_regeneration_result(...)` now return the final JSON
    payload directly instead of wrapping it in one-field result structs
  - `FullChapterRegenerationStreamInput` no longer carries a constant
    `task_label`; the launch owner now owns the single literal where it is
    actually consumed
  - stream workflow closures no longer map through `.payload` wrappers

  This keeps chapter-regeneration result payloads, partial-regeneration result
  payloads, launch initial events, completion messages, and route-facing
  stream behavior unchanged while removing two more no-signal contract
  wrappers from the regeneration path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_text_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking regeneration contracts when a field or wrapper has
    clearly become a single-consumer passthrough with no independent
    validation, payload-shaping, or workflow semantics
  - do not merge
    `chapter_regeneration_prepare_service.rs`,
    `chapter_regeneration_stream_workflow_service.rs`, or
    `chapter_regeneration_stream_launch_service.rs` once the next step would
    only flatten still-meaningful prepare/workflow/launch boundaries instead
    of removing a true low-signal contract shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to the single-generation stream lane and removed one
  more single-consumer launch facade after confirming the file had collapsed
  into one workflow caller plus local SSE orchestration around the existing
  runtime owner. A focused audit of
  `backend-rs/src/services/chapter_single_generation_stream_launch_service.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
  showed that the launch file no longer owned an independent seam:
  - `launch_owned_single_chapter_generation_stream(...)` had only one
    production caller in `chapter_single_generation_workflow_service.rs`
  - the file only wrapped `execute_owned_single_chapter_generation(...)` with
    local SSE start/preparing/generating/result/done orchestration and did not
    own request preparation, route access, payload shaping, or runtime write
    semantics beyond that same stream workflow lane
  - the `SingleGenerationRuntimeLaunchInput` contract itself already belonged
    to the runtime-state owner, so keeping a separate launch facade added no
    signal

  The refactor therefore tightened that owner boundary without changing the
  stream contract:
  - `chapter_single_generation_workflow_service.rs` now owns the stream launch
    SSE orchestration beside the stream workflow that prepares and dispatches
    it
  - `chapter_single_generation_stream_launch_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted facade

  This keeps single-generation stream request preparation, SSE event order,
  progress messages, runtime dispatch behavior, and result payload shape
  unchanged while removing one more no-signal launch shell from the single-
  generation path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking single-generation stream helpers when a file has
    clearly become a single-consumer launch shell or local transport wrapper
    with no independent request/runtime semantics
  - do not merge
    `chapter_single_generation_workflow_service.rs` or
    `chapter_single_generation_runtime_state_service.rs` once the next step
    would only flatten still-meaningful workflow/runtime boundaries instead of
    removing a true low-signal facade

- 2026-05-26 follow-up checkpoint:
  this slice returned to the regeneration launch lane and removed one more
  single-consumer execution helper after confirming the file had collapsed
  into a launch-owner-local stream loop and finalize-error adapter for that
  same lane. A focused audit of
  `backend-rs/src/services/chapter_regeneration_stream_execution_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_launch_service.rs`, and
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`
  showed that the execution file no longer owned an independent seam:
  - `execute_regeneration_text_stream(...)` and
    `emit_regeneration_finalize_error(...)` had one production consumer in the
    regeneration launch owner
  - `RegenerationChunkProgress` only existed to connect the launch owner’s
    progress callback with the same launch-local stream loop
  - the helper did not own request preparation, final payload shaping, route
    access, or workflow branching beyond the same launch boundary

  The refactor therefore tightened that owner boundary without changing the
  regeneration stream contract:
  - `chapter_regeneration_stream_launch_service.rs` now owns chunk streaming,
    finalize-error mapping, and `RegenerationChunkProgress` beside the launch
    orchestration that consumes them
  - `chapter_regeneration_stream_execution_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted helper
  - `chapter_regeneration_stream_workflow_service.rs` now imports
    `RegenerationChunkProgress` from the launch owner instead of the deleted
    helper

  This keeps regeneration SSE chunk emission, finalize-error messages,
  workflow-owned payload finalization, progress callbacks, and route-facing
  stream behavior unchanged while removing one more no-signal execution helper
  shell from the regeneration path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_launch_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking regeneration launch/helpers when a file has clearly
    become a single-consumer execution helper, finalize-error adapter, or
    launch-local callback shell with no independent workflow semantics
  - do not merge
    `chapter_regeneration_stream_launch_service.rs` or
    `chapter_regeneration_stream_workflow_service.rs` once the next step would
    only flatten still-meaningful launch/workflow boundaries instead of
    removing a true low-signal facade

- 2026-05-26 follow-up checkpoint:
  this slice returned to the regeneration route lane and removed one more
  single-route error-mapper shell after confirming the file had collapsed into
  a one-line delegate to the shared chapter-access error mapper. A focused
  audit of `backend-rs/src/api/chapter_regeneration_query_error_mapper.rs`,
  `backend-rs/src/api/chapter_regeneration_routes.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs` showed that the mapper file no
  longer owned an independent seam:
  - `map_regeneration_tasks_query_error(...)` had one caller in the
    regeneration tasks route
  - its entire behavior was delegating the same
    `LoadAccessibleChapterError -> (StatusCode, Json<Value>)` contract already
    owned by `map_load_accessible_chapter_error(...)`
  - the file did not own regeneration-specific payload shaping, status
    branching, or transport semantics beyond that one-line forwarding

  The refactor therefore tightened that route boundary without changing the
  externally visible error contract:
  - `chapter_regeneration_routes.rs` now maps regeneration task query errors
    directly through `map_load_accessible_chapter_error(...)`
  - `chapter_regeneration_query_error_mapper.rs` was deleted
  - `api/mod.rs` no longer exports the deleted mapper

  This keeps regeneration task query status codes, JSON error payload shape,
  and route behavior unchanged while removing one more no-signal route-local
  error-mapper shell from the regeneration path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking regeneration route helpers when a file has clearly
    become a single-route error-mapper, typed transport adapter, or local
    forwarding shell with no independent payload or workflow semantics
  - do not merge
    `chapter_regeneration_routes.rs` or
    `chapter_regeneration_query_service.rs` once the next step would only
    flatten still-meaningful route/query ownership instead of removing a true
    low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the chapter route lane and removed one more group of
  no-signal route error-mapper wrappers after confirming the exported names had
  collapsed into same-file passthrough aliases to already-owned shared
  contracts. A focused audit of
  `backend-rs/src/api/chapter_analysis_query_error_mapper.rs`,
  `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/api/chapter_batch_generation_error_mapper.rs`,
  `backend-rs/src/api/chapter_batch_generation.rs`, and
  `backend-rs/src/api/chapters_error_mapper.rs` showed that:
  - chapter-analysis quality-metrics route mapping and analysis-status route
    mapping were both direct passthroughs to the same
    `ChapterAnalysisQueryContextError -> (StatusCode, Json<Value>)` contract
  - batch-generation active-query, resume-config, status-query, and
    status-stream route mappers were exporting route-local names that only
    forwarded into shared project-access, owned-task, or resume-domain mapper
    helpers in the same file
  - those wrappers did not own route-specific payload shaping, branch
    semantics, or compatibility-sensitive transport behavior beyond the same
    already-tested shared contracts

  The refactor therefore tightened those route boundaries without changing the
  externally visible error contract:
  - `chapter_analysis_routes.rs` now maps quality-metrics and analysis-status
    failures directly through
    `map_chapter_analysis_query_context_error(...)`
  - `chapters_error_mapper.rs` no longer exports the analysis-status-only
    passthrough mapper
  - `chapter_analysis_query_error_mapper.rs` no longer exports the
    quality-metrics-only passthrough mapper
  - `chapter_batch_generation.rs` now maps active-query, owned-task status,
    stream-access, and resume-config failures directly through the shared
    mapper owners already defined in
    `chapter_batch_generation_error_mapper.rs`
  - `chapter_batch_generation_error_mapper.rs` no longer exports those
    extra route-local passthrough names

  This keeps chapter-analysis and batch-generation status codes, JSON error
  payload shapes, and route behavior unchanged while removing one more no-
  signal route-local error-wrapper layer from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking route error helpers when an exported mapper has
    clearly become a same-file passthrough alias to an already-owned shared
    transport contract
  - do not merge route files or shared mapper owners once the next step would
    only rename stable shared mapping boundaries instead of removing a true
    low-signal wrapper

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the route error-mapper lane and removed two more groups
  of same-file passthrough aliases after confirming the exported names had
  collapsed into direct type-alias forwarding to already-owned shared mapper
  contracts. A focused audit of
  `backend-rs/src/api/chapter_crud_error_mapper.rs`,
  `backend-rs/src/api/chapter_crud_routes.rs`,
  `backend-rs/src/api/memories_error_mapper.rs`, and
  `backend-rs/src/api/memories.rs` showed that:
  - CRUD create/list/get/update/delete/expansion-plan route mappers were all
    forwarding directly into the same two shared owners:
    `map_project_crud_success_message_error(...)` and
    `map_chapter_crud_success_message_error(...)`
  - memories project-memories/search/foreshadows/stats/delete route mappers
    were all forwarding directly into the same two shared owners:
    `map_owned_project_memories_query_error(...)` and
    `map_memories_project_write_context_error(...)`
  - those wrapper exports did not own route-specific payload shaping, branch
    semantics, or compatibility-sensitive behavior beyond the already-tested
    shared contracts

  The refactor therefore tightened those route boundaries without changing the
  externally visible error contracts:
  - `chapter_crud_routes.rs` now maps project-owned CRUD errors directly
    through `map_project_crud_success_message_error(...)`
  - `chapter_crud_routes.rs` now maps chapter-owned CRUD errors directly
    through `map_chapter_crud_success_message_error(...)`
  - `chapter_crud_error_mapper.rs` no longer exports the CRUD action-specific
    passthrough aliases for those two shared owners
  - `memories.rs` now maps project query failures directly through
    `map_owned_project_memories_query_error(...)`
  - `memories.rs` now maps delete workflow context failures directly through
    `map_memories_project_write_context_error(...)`
  - `memories_error_mapper.rs` no longer exports the corresponding route-local
    passthrough aliases

  This keeps CRUD and memories status codes, JSON error payload shapes, and
  route behavior unchanged while removing one more no-signal route-local
  error-wrapper layer from the Rust path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking route error helpers when an exported mapper has
    clearly become a same-file passthrough alias to an already-owned shared
    transport contract
  - do not merge route files or shared mapper owners once the next step would
    only rename stable shared mapping boundaries instead of removing a true
    low-signal wrapper

- 2026-05-26 follow-up checkpoint:
  this slice returned to the analysis draft route lane and removed one more
  group of route-only mapper aliases after confirming the exported names had
  collapsed into typed passthrough wrappers over already-owned shared draft
  mapper contracts. A focused audit of
  `backend-rs/src/api/chapter_analysis_draft_error_mapper.rs` and
  `backend-rs/src/api/chapter_analysis_routes.rs` showed that:
  - `map_owned_auto_revision_draft_load_error(...)`
  - `map_owned_auto_revision_draft_apply_error(...)`
  - `map_owned_candidate_draft_load_error(...)`
  - `map_owned_candidate_draft_apply_error(...)`
  were all direct wrappers over the same two shared owners:
  `map_owned_auto_revision_draft_error(...)` and
  `map_owned_candidate_draft_error(...)`
  plus the already-owned inner draft mappers
  (`map_auto_revision_draft_*` / `map_candidate_draft_*`)
  - the four exported wrappers did not own route-specific payload shaping,
    selection-mode branching, or compatibility-sensitive behavior beyond
    forwarding the typed owned-draft aliases into those shared owners

  The refactor therefore tightened that route boundary without changing the
  externally visible error contract:
  - `chapter_analysis_routes.rs` now maps owned draft load/apply failures
    directly through `map_owned_auto_revision_draft_error(...)` or
    `map_owned_candidate_draft_error(...)` with the existing inner draft
    mapper supplied explicitly
  - `chapter_analysis_draft_error_mapper.rs` no longer exports the four
    route-only passthrough aliases

  This keeps draft load/apply status codes, JSON error payload shapes,
  selection-mode-sensitive not-found details, stale handling, and route
  behavior unchanged while removing one more no-signal route-local
  error-wrapper layer from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_error_mapper -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking route draft helpers when an exported mapper has
    clearly become a typed passthrough alias to an already-owned shared draft
    transport contract
  - do not merge route files or the shared draft mapper owners once the next
    step would only rename stable mapping boundaries instead of removing a
    true low-signal wrapper

- 2026-05-26 follow-up checkpoint:
  this slice returned to the memories route/query lane and removed one more
  route-adjacent request shell after confirming the file had collapsed into
  one route caller set plus one query-owner consumer set inside the same lane.
  A focused audit of
  `backend-rs/src/services/memories_request_service.rs`,
  `backend-rs/src/api/memories.rs`, and
  `backend-rs/src/services/memories_query_service.rs` showed that:
  - `MemoryListRouteQuery`, `SearchMemoriesRouteRequest`, and
    `ForeshadowListRouteQuery` had only one route owner in `memories.rs`
  - `MemoryListRequest` and `SearchMemoriesRequest` had only one query owner
    in `memories_query_service.rs`
  - the deleted file only normalized route fields into those query-owner
    request structs and did not own independent access, payload shaping,
    workflow branching, or cross-lane semantics beyond the same memories
    read-side boundary

  The refactor therefore tightened that owner boundary without changing the
  memories route or query contract:
  - `memories.rs` now owns the typed route query/body structs and the local
    route -> query-request normalization helpers beside the routes that
    consume them
  - `memories_query_service.rs` now owns `MemoryListRequest` and
    `SearchMemoriesRequest` beside the query functions that consume those
    normalized getters
  - `memories_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted shell

  This keeps memories list/search/filter defaults, current-chapter forwarding,
  query payload shapes, and route behavior unchanged while removing one more
  no-signal same-lane request shell from the Rust path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" memories_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking memories route/query helpers when a file has clearly
    become a route-local typed request shell or query-local normalized request
    shell with no independent cross-lane contract
  - do not merge `memories.rs`, `memories_query_service.rs`, or
    `memories_write_workflow_service.rs` once the next step would only flatten
    still-meaningful route/query/write boundaries instead of removing a true
    low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to the chapter regeneration stream lane and removed the
  remaining same-lane request shell after confirming the file had collapsed
  into transport structs plus route -> normalized-request builders with no
  independent workflow or cross-lane ownership. A focused audit of
  `backend-rs/src/api/chapter_regeneration_routes.rs`,
  `backend-rs/src/services/chapter_regeneration_prepare_service.rs`,
  `backend-rs/src/services/chapter_regeneration_stream_workflow_service.rs`,
  and the deleted
  `backend-rs/src/services/chapter_regeneration_stream_request_service.rs`
  showed that:
  - `FullChapterRegenerationStreamRouteRequest` and
    `PartialRegenerationStreamRouteRequest` only belonged to the route layer
    and were consumed by the two chapter regeneration stream endpoints
  - `FullChapterRegenerationStreamRequest` and
    `PartialRegenerationStreamWorkflowRequest` only belonged to the prepare /
    workflow lane and were consumed by prepare + workflow owners
  - the deleted file no longer owned access checks, payload shaping beyond
    route normalization, stream launch semantics, or compatibility-sensitive
    behavior outside this same regeneration boundary

  The refactor therefore tightened those owners without changing the stream
  contract:
  - `chapter_regeneration_routes.rs` now owns the typed transport structs and
    local route -> normalized-request builders beside the stream routes
  - `chapter_regeneration_prepare_service.rs` now owns the normalized request
    structs beside the prompt/input preparation logic that consumes them
  - `chapter_regeneration_stream_workflow_service.rs` now imports the
    normalized request owner directly from prepare
  - `chapter_regeneration_stream_request_service.rs` was deleted
  - `services/mod.rs` no longer exports the deleted shell

  This keeps full/partial regeneration defaults, preserve-element parsing,
  partial context normalization, web-research flags, stream route behavior,
  and prompt-preparation inputs unchanged while removing one more no-signal
  same-lane request shell from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_stream_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter regeneration stream helpers when a file has
    clearly become a route-local transport shell or a prepare-local normalized
    request shell with no independent stream / workflow ownership
  - do not merge `chapter_regeneration_routes.rs`,
    `chapter_regeneration_prepare_service.rs`,
    `chapter_regeneration_stream_workflow_service.rs`, or
    `chapter_regeneration_stream_launch_service.rs` once the next step would
    only flatten still-meaningful route / prepare / workflow / launch
    boundaries instead of removing a true low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to the single-chapter generation lane and removed one
  more route-local request shell after confirming the route transport struct
  had collapsed into one route caller set plus one prepare/workflow consumer
  set inside the same generation boundary. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`, and
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  showed that:
  - `SingleChapterGenerationRouteRequest` was only consumed by the two
    single-chapter generation entrypoints in `chapter_batch_generation.rs`
  - `SingleChapterGenerationRequest` already belonged to the prepare/workflow
    lane and was the real normalized contract consumed by both background and
    stream workflows
  - the route builder in
    `chapter_single_generation_workflow_service.rs` did not own workflow
    branching, runtime launch semantics, or compatibility-sensitive behavior
    beyond discarding the route-only `enable_analysis` field and forwarding
    `target_word_count` plus `model`

  The refactor therefore tightened that boundary without changing the route or
  workflow contract:
  - `chapter_batch_generation.rs` now owns
    `SingleChapterGenerationRouteRequest` and the local
    route -> normalized-request builder beside the two routes that consume it
  - `chapter_single_generation_workflow_service.rs` no longer owns the
    transport struct or route-local builder and now narrows to background/stream
    workflow orchestration plus runtime launch

  This keeps single-chapter background and stream payload shapes, ignored
  `enable_analysis` compatibility semantics, request defaults, and workflow
  behavior unchanged while removing one more no-signal route-local request
  shell from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking single-chapter generation helpers when a file has
    clearly become a route-local transport shell or a workflow-local passthrough
    shell with no independent prepare/runtime ownership
  - do not merge `chapter_batch_generation.rs`,
    `chapter_single_generation_prepare_service.rs`, or
    `chapter_single_generation_workflow_service.rs` once the next step would
    only flatten still-meaningful route / prepare / workflow boundaries
    instead of removing a true low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the chapter batch-generation route/write lane and
  removed one more route-local request shell after confirming the batch-create
  transport struct had collapsed into one route caller set plus one write-
  workflow consumer set inside the same generation boundary. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs` and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that:
  - `BatchGenerationCreateRouteRequest` was only consumed by the
    `create_batch_generate` route in `chapter_batch_generation.rs`
  - `BatchGenerationCreateWorkflowRequest` already belonged to the write
    workflow lane and was the real normalized contract consumed by prepare,
    task-plan creation, and runtime launch orchestration
  - the route builder in
    `chapter_batch_generation_write_workflow_service.rs` did not own workflow
    branching, runtime launch semantics, or compatibility-sensitive behavior
    beyond forwarding the transport fields and discarding the route-only
    compat fields (`enable_mcp`, `enable_web_research`, `web_research_query`,
    `creative_mode`, `story_focus`, `plot_stage`, `story_creation_brief`,
    `quality_preset`, `quality_notes`, `story_repair_summary`,
    `story_repair_targets`, `story_preserve_strengths`)

  The refactor therefore tightened that boundary without changing the route or
  write-workflow contract:
  - `chapter_batch_generation.rs` now owns `BatchGenerationCreateRouteRequest`
    and the local route -> normalized-request builder beside the batch-create
    route that consumes it
  - `chapter_batch_generation_write_workflow_service.rs` no longer owns the
    transport struct or route-local builder and now narrows to normalized
    write-workflow preparation, task-plan creation, and runtime launch

  This keeps batch-create payload shape, ignored compat-only field semantics,
  request defaults, and runtime behavior unchanged while removing one more
  no-signal route-local request shell from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking batch-create helpers when a file has clearly become a
    route-local transport shell or a write-local passthrough shell with no
    independent prepare/runtime ownership
  - do not merge `chapter_batch_generation.rs` or
    `chapter_batch_generation_write_workflow_service.rs` once the next step
    would only flatten still-meaningful route / write-workflow boundaries
    instead of removing a true low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the chapter CRUD lane and removed one more group of
  route-only request-builder passthroughs after confirming the exported helper
  names had collapsed into scalar forwarding wrappers over already-owned CRUD
  request structs. A focused audit of
  `backend-rs/src/api/chapter_crud_routes.rs` and
  `backend-rs/src/services/chapter_crud_workflow_service.rs` showed that:
  - `build_create_chapter_request(...)`,
    `build_update_chapter_request(...)`, and
    `build_update_expansion_plan_request(...)` were only consumed by
    `chapter_crud_routes.rs`
  - the true reusable owner in the workflow layer was the request struct
    contract itself (`CreateChapterRequest`, `UpdateChapterRequest`,
    `UpdateExpansionPlanRequest`), not the route-only scalar forwarding
    wrapper names
  - those builders did not own workflow branching, payload shaping, or
    compatibility-sensitive behavior beyond copying route-local fields into the
    already-owned CRUD request structs

  The refactor therefore tightened that boundary without changing the CRUD
  contract:
  - `chapter_crud_routes.rs` now owns the local route -> request conversion
    and builds CRUD requests through request-type constructors beside the
    routes that consume them
  - `chapter_crud_workflow_service.rs` no longer exports the three
    route-only builder helpers and now narrows to request-type ownership plus
    CRUD payload workflows

  This keeps chapter CRUD payload shapes, field defaults, update optionality,
  expansion-plan behavior, and workflow semantics unchanged while removing one
  more no-signal route-only builder layer from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter CRUD helpers when an exported helper has
    clearly become a route-only scalar passthrough to an already-owned CRUD
    request struct contract
  - do not merge `chapter_crud_routes.rs` or
    `chapter_crud_workflow_service.rs` once the next step would only flatten
    still-meaningful route / workflow boundaries instead of removing a true
    low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the chapter analysis draft lane and removed one more
  exported request-builder alias after confirming the service-owned helper had
  collapsed into a thin constructor wrapper over an already-owned request
  struct. A focused audit of
  `backend-rs/src/api/chapter_analysis_routes.rs` and
  `backend-rs/src/services/chapter_analysis_draft_service.rs` showed that:
  - `build_owned_draft_payload_request(...)` was only used by
    `chapter_analysis_routes.rs` route-local request builders plus service
    unit tests
  - the true reusable owner in the analysis draft lane was the request struct
    contract itself (`OwnedDraftPayloadRequest`), not the exported helper name
  - the builder did not own workflow branching, payload shaping, or
    compatibility-sensitive behavior beyond deriving `selection_mode` from
    whether `selector` exists and carrying `allow_stale`

  The refactor therefore tightened that boundary without changing the analysis
  draft contract:
  - `OwnedDraftPayloadRequest` now owns its constructor through
    `OwnedDraftPayloadRequest::new(...)`
  - `chapter_analysis_routes.rs` now builds owned draft requests directly via
    the request-type constructor beside the route-local query/body adapters
  - `chapter_analysis_draft_service.rs` no longer exports the standalone
    route-consumed builder helper and now narrows to request ownership plus
    owned draft payload workflows

  This keeps draft lookup/apply payload shapes, explicit-vs-latest selection
  semantics, `allow_stale` behavior, and error mapping unchanged while
  removing one more no-signal request-builder alias from the Rust chapter
  path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter analysis draft helpers when an exported helper
    has clearly become a route-only constructor alias over an already-owned
    request struct contract
  - do not merge `chapter_analysis_routes.rs` or
    `chapter_analysis_draft_service.rs` once the next step would only flatten
    still-meaningful route / service ownership instead of removing a true
    low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the chapter regeneration apply lane and removed one
  more route-local passthrough helper after confirming the local builder had
  collapsed into a single constructor call with no independent ownership. A
  focused audit of `backend-rs/src/api/chapter_regeneration_routes.rs` and
  `backend-rs/src/services/chapter_regeneration_apply_service.rs` showed that:
  - `build_apply_partial_regenerate_request_from_route_payload(...)` was only
    used by the `apply_partial_regenerate` route plus the route-local helper
    test
  - the true reusable owner in this lane was already the request struct
    contract itself (`ApplyPartialRegenerateRequest`), not the extra local
    builder name
  - the helper did not own any normalization, compatibility fallback, or
    workflow branching beyond forwarding three optional route fields into
    `ApplyPartialRegenerateRequest::new(...)`

  The refactor therefore tightened that boundary without changing the
  regeneration-apply contract:
  - `chapter_regeneration_routes.rs` now constructs
    `ApplyPartialRegenerateRequest` directly inside the route through the
    request-type constructor
  - the route-local test now asserts the same payload contract through
    `ApplyPartialRegenerateRequest::new(...)`
  - the extra route-local helper has been removed entirely

  This keeps partial-regenerate apply payload shape, default positions,
  chapter access behavior, and write-workflow semantics unchanged while
  removing one more no-signal passthrough layer from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_apply_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter regeneration apply helpers when the next move
    removes a true route-only passthrough layer or constructor alias
  - do not merge `chapter_regeneration_routes.rs` or
    `chapter_regeneration_apply_service.rs` once the next step would only
    flatten still-meaningful route / apply ownership instead of removing a
    true low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to the chapter batch-generation route/write lane and
  removed two more route-local builder aliases after confirming both route
  helpers had collapsed into thin constructors over already-owned request
  structs. A focused audit of
  `backend-rs/src/api/chapter_batch_generation.rs`,
  `backend-rs/src/services/chapter_single_generation_prepare_service.rs`, and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that:
  - `build_single_chapter_generation_request_from_route_payload(...)` was only
    used by the background and stream single-chapter generation routes
  - `build_batch_generation_create_workflow_request_from_route_payload(...)`
    was only used by the batch-create route plus route-local contract tests
  - the true reusable owners in these lanes were already the request struct
    contracts themselves (`SingleChapterGenerationRequest` and
    `BatchGenerationCreateWorkflowRequest`), not the extra route-local helper
    names
  - those helpers did not own workflow branching, payload shaping, or
    compatibility-sensitive behavior beyond copying route-local fields and
    applying the already-owned defaults for `enable_analysis` / `max_retries`

  The refactor therefore tightened those boundaries without changing the
  chapter-generation or batch-create contracts:
  - `SingleChapterGenerationRequest` now owns its constructor through
    `SingleChapterGenerationRequest::new(...)`
  - `BatchGenerationCreateWorkflowRequest` now owns its constructor through
    `BatchGenerationCreateWorkflowRequest::new(...)`, including the existing
    `enable_analysis=false` and `max_retries=3` defaults
  - `chapter_batch_generation.rs` now constructs both request types directly
    inside the consuming routes and no longer carries the two route-local
    builder helpers
  - route-local contract tests now assert the same payload semantics through
    the request-type constructors

  This keeps single-chapter generation payload shape, batch-create payload
  shape, ignored compat-only route fields, default analysis/retry semantics,
  and downstream workflow behavior unchanged while removing two more
  no-signal route-local builder layers from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking chapter batch-generation route/write helpers when the
    next move removes a true route-only constructor alias or passthrough shell
  - do not merge `chapter_batch_generation.rs`,
    `chapter_single_generation_prepare_service.rs`, or
    `chapter_batch_generation_write_workflow_service.rs` once the next step
    would only flatten still-meaningful route / request-owner / write-workflow
    boundaries instead of removing a true low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the route transport lane and batch-compressed the
  remaining free builder helpers inside the chapter analysis and chapter
  regeneration route groups after confirming those helpers had become
  route-owned adapter details rather than independent reusable owners. A
  focused audit of `backend-rs/src/api/chapter_analysis_routes.rs`,
  `backend-rs/src/api/chapter_regeneration_routes.rs`, and the affected
  prepare-service tests showed that:
  - the analysis route file still carried free helper functions for
    `BatchAnalysisStatusRouteRequest`, auto-revision draft query/body, and
    candidate draft query/body even though each helper was only used by its
    matching route call sites plus route-local tests
  - the regeneration route file still carried free helper functions for
    full-regeneration request assembly, partial-regeneration request assembly,
    and task-limit normalization even though those helpers were only used by
    route-local call sites plus route/prepare tests
  - those helpers did not own workflow branching or cross-module semantics;
    they only adapted transport fields into already-owned downstream request
    contracts or normalized one route-local scalar

  The refactor therefore tightened the route transport boundary without
  changing analysis/regeneration behavior:
  - `BatchAnalysisStatusRouteRequest` now owns `into_chapter_ids()`
  - analysis draft route query/body transport types now own
    `into_owned_draft_request()`
  - `RegenerationTasksRouteQuery` now owns `normalized_limit()`
  - `FullChapterRegenerationStreamRouteRequest` now owns
    `into_workflow_request()` together with its preserve-element parsing helpers
  - `PartialRegenerationStreamRouteRequest` now owns
    `into_workflow_request()`
  - route handlers and route-local tests now call those route-owned methods
    directly instead of going through extra top-level free helper functions
  - `chapter_regeneration_prepare_service.rs` tests were updated to follow the
    same route-owned conversion path, so the old free helper did not need to be
    restored as a compatibility alias

  This keeps batch-analysis status payloads, draft selection semantics,
  regeneration stream payload shape, preserve-element parsing defaults, and
  regeneration task-limit normalization unchanged while removing another group
  of no-signal free builder layers from the Rust chapter path.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking route transport helpers when the next move removes a
    true route-owned free adapter layer or scalar-normalization shell
  - do not merge `chapter_analysis_routes.rs` or
    `chapter_regeneration_routes.rs` once the next step would only flatten
    still-meaningful route transport types instead of removing a true
    low-signal shell

- 2026-05-26 follow-up checkpoint:
  this slice returned to `chapter_batch_generation.rs` and finished aligning
  its remaining route transport helpers with the same transport-owned style now
  used by the chapter analysis and chapter regeneration routes. A focused audit
  of `backend-rs/src/api/chapter_batch_generation.rs` showed that:
  - `normalize_active_batch_generation_task_list_limit(...)` was only used by
    the active-task list route plus route-local tests
  - the batch-create and single-chapter generation route payload structs still
    relied on route-local inline request assembly even though the true request
    owners were already `BatchGenerationCreateWorkflowRequest` and
    `SingleChapterGenerationRequest`
  - those remaining route-local helpers did not own workflow branching or
    compatibility-sensitive behavior; they only normalized one route-local
    scalar or forwarded transport fields into already-owned request contracts

  The refactor therefore tightened the batch-generation route transport
  boundary without changing behavior:
  - `ActiveBatchGenerationTaskListRouteQuery` now owns `normalized_limit()`
  - `BatchGenerationCreateRouteRequest` now owns `into_workflow_request()`
  - `SingleChapterGenerationRouteRequest` now owns `into_request()`
  - the batch-create, background-generate, stream-generate, and active-task
    list routes now call those route-owned methods directly
  - route-local tests now assert the same transport semantics through the
    route-owned methods instead of standalone helper names

  This keeps batch-create payload shape, active-task list limit normalization,
  ignored compat-only route fields, single-chapter generation payload shape,
  and downstream workflow behavior unchanged while removing the last obvious
  route-local scalar/request helper layer from `chapter_batch_generation.rs`.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation.rs` when the next move
    removes a true route transport helper layer rather than flattening still-
    meaningful status/stream/runtime ownership
  - do not merge `chapter_batch_generation.rs` into adjacent service owners
    once the remaining work is mostly runtime/status semantics instead of
    route transport cleanup

- 2026-05-26 follow-up checkpoint:
  this slice re-audited the current batch read-side files before making
  another seam cut and explicitly corrected for stale planning references.
  The current repository no longer contains
  `backend-rs/src/services/chapter_batch_generation_status_view_service.rs`,
  so the next useful low-risk move had to be selected from the real
  `chapter_batch_generation_*` service set instead of continuing to chase an
  outdated owner name.

  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
  showed that:
  - `base_task_payload(...)` was a single-consumer shell used only by
    `build_shared_task_view_payload(...)`
  - the helper did not own any branching, compatibility behavior, or
    cross-file contract; it only appended a fixed set of shared task-view
    fields after the runtime payload had already been built
  - `insert_shared_task_view_fields(...)` only carried quality-status field
    insertion, so its previous name overstated ownership relative to the
    actual view payload boundary

  The refactor therefore tightened the task-view payload owner without
  changing behavior:
  - `build_shared_task_view_payload(...)` now owns the shared base field
    assembly directly instead of round-tripping through
    `base_task_payload(...)`
  - `insert_shared_task_view_fields(...)` was renamed to the narrower
    `insert_quality_status_fields(...)` so the helper name matches its actual
    responsibility
  - status and active task payload builders still share the same runtime
    payload base and quality-status fields, but the file no longer maintains
    one extra no-signal pass-through layer for the shared view payload

  This keeps batch status payload shape, active-task payload shape, terminal
  semantics, runtime checkpoint fields, and quality-status fields unchanged
  while removing one more single-consumer passthrough shell from the batch
  read-side lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_task_view_query_service.rs`
    when the next move removes another true single-consumer shell or fake
    owner split
  - do not split `chapter_batch_generation_task_payload_base_service.rs` or
    `chapter_batch_generation_runtime_checkpoint_service.rs` unless a future
    audit finds a concrete passthrough helper rather than a real shared owner

- 2026-05-26 follow-up checkpoint:
  this slice continued the Phase 5 write-lane tightening by re-auditing the
  current `chapter_batch_generation_write_workflow_service.rs` create path and
  only removing helper layers that were clearly single-consumer shells.

  A focused audit showed that:
  - `build_batch_generation_create_chapter_target(...)` was only used when
    projecting `chapter::Model` into the create-lane target view owned by
    `BatchGenerationCreateChapterTarget`
  - the helper did not own branching or cross-file compatibility behavior; it
    was just a constructor-shaped free function beside the target type
  - `validate_batch_generation_count(...)` was only called by
    `load_chapters_for_batch_generation_range(...)`, so it behaved as a thin
    single-consumer guard shell rather than a reusable validation owner

  The refactor therefore tightened the batch create write boundary without
  changing behavior:
  - `BatchGenerationCreateChapterTarget` now owns the chapter projection
    through `from_model(...)` instead of relying on a separate free helper
  - `load_chapters_for_batch_generation_range(...)` now performs its invalid
    count guard inline instead of round-tripping through a dedicated
    single-caller validator helper
  - create-lane tests were updated to assert the same projection behavior
    through the type-owned constructor boundary

  This keeps create request validation semantics, chapter target projection,
  batch-create response payload shape, and runtime launch behavior unchanged
  while removing two more low-signal helper layers from the batch write lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:46 +08:00 checkpoint:
  this slice tightened the stream-state owner boundary between
  `chapter_batch_generation_read_context_service.rs` and
  `chapter_batch_generation_stream_semantics_service.rs`.
  A focused review showed that `BatchGenerationReadContext::into_stream_state()`
  still manually unpacked task status plus checkpoint semantics and then
  rebuilt `BatchGenerationStreamState` field-by-field, even though the true
  owner of those semantics was already the stream-state layer itself.

  The refactor therefore moved stream-state construction into the stream-state
  owner:
  - `BatchGenerationStreamState` now owns
    `from_task_state(task, workflow_runtime_state)`
  - `BatchGenerationReadContext::into_stream_state()` now delegates directly
    to that owner instead of reassembling status/completed/progress/message/
    event-status/terminal-kind inline
  - focused tests now cover stream-state construction at the stream-semantics
    layer directly

  This keeps stream progress/message/event-status/terminal-kind semantics and
  read-context driven stream behavior unchanged while removing one more
  field-by-field owner split from the batch read/stream lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:43 +08:00 checkpoint:
  this slice returned to the batch create write-workflow lane and tightened
  the response item projection inside
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`.
  A focused review showed that `PreparedBatchGenerationCreateRequest` already
  owned the create response envelope, but the
  `chapters_to_generate` item projection still lived inline inside
  `create_response_payload(...)`.

  The refactor therefore pulled that item projection into the same owner:
  - `PreparedBatchGenerationCreateRequest` now owns
    `chapters_to_generate_payload()`
  - `create_response_payload(...)` now composes the response from that owned
    projection plus the existing estimated-time contract
  - focused tests now cover the chapter-target payload list projection
    directly

  This keeps batch create response shape, chapter target item shape, and
  estimated-time semantics unchanged while removing one more small inline
  projection branch from the batch create workflow owner.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:40 +08:00 checkpoint:
  this slice returned to the batch status stream lane and tightened the event
  resolution contract between the stream-event owner and the polling loop. A
  focused review of
  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
  showed that the event owner still returned a struct with two loosely coupled
  control fields:
  - `should_close`
  - `next_cursor`

  The polling loop then had to reinterpret that pair to decide whether to
  continue or close. That left one small ownership split where the event layer
  knew the lifecycle outcome, but the stream loop still had to decode it.

  The refactor therefore made the event owner return an explicit control
  result:
  - replaced the old event-batch struct with
    `BatchGenerationStreamEventResolution`
  - the resolution now directly expresses either
    `Continue { events, next_cursor }` or `Close { events }`
  - the polling loop now only sends events and matches on the resolution,
    without reinterpreting separate `should_close/next_cursor` fields

  This keeps progress/result/error/done event payloads, terminal close
  behavior, and cursor advancement semantics unchanged while removing one more
  small owner split from the batch stream polling lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:33 +08:00 checkpoint:
  this slice moved to the adjacent batch cancel workflow and aligned its
  response-state ownership with the freshly tightened resume lane. A focused
  audit of
  `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`
  showed that `CancelBatchGenerationCommandState` still owned
  `into_response_state(...)`, while `CancelBatchGenerationResponseState`
  already owned the final payload projection. That split made the command
  state responsible for a response concern even though the response owner was
  already present in the same file.

  The refactor therefore moved the projection into the response owner:
  - `CancelBatchGenerationResponseState` now owns
    `from_command_state(...)`
  - `cancel_batch_generation_task(...)` now returns the response state through
    that owner directly after the runtime cancellation write succeeds
  - command-state construction remains limited to projecting task fields used
    by cancel validation and persistence

  This keeps cancel validation, terminal-status rejection text, cancelled
  runtime checkpoint persistence, and the public cancel response payload shape
  unchanged while removing another same-file response contract split in the
  batch command workflow lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:35 +08:00 checkpoint:
  this slice tightened the active batch task list view in
  `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`.
  A focused review showed that the query function still mapped
  `BatchGenerationReadContext` values into active-task payloads before handing
  them to `ActiveBatchGenerationTaskListView`, so the view owner only wrapped
  an already-projected `Vec<Value>` and did not own the full list-envelope
  contract.

  The refactor moved the item projection into the list view owner:
  - `ActiveBatchGenerationTaskListView` now stores
    `Vec<BatchGenerationReadContext>`
  - `into_payload()` now owns both active-task item projection and
    `total/items` envelope assembly
  - `load_active_user_batch_generation_task_list_view(...)` now loads contexts
    and hands them to the view owner directly

  This keeps active-list response shape, active-task item payload shape,
  quality status fields, and omission of status-only fields unchanged while
  removing a small query/view ownership split in the batch read-side lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_write_workflow_service.rs`
    when the next move removes another constructor alias, single-consumer
    guard shell, or pure response-shape passthrough
  - do not split create/resume runtime dispatch owners unless a future audit
    shows a concrete fake boundary rather than a real workflow handoff

- 2026-05-26 follow-up checkpoint:
  this slice moved from the batch create lane into the current
  `chapter_single_generation_workflow_service.rs` background-create path and
  tightened the single-chapter write owner without touching runtime
  dispatch/prepare behavior.

  A focused audit showed that:
  - `build_single_generation_background_create_response_payload(...)`,
    `single_generation_task_chapter_payload_from_parts(...)`, and
    `build_single_generation_background_task_active_model(...)` were all only
    used for one ownership boundary: turning
    `SingleChapterGenerationTarget` into the background task snapshot /
    response payload required by the write workflow
  - those helpers did not own branching or cross-file compatibility logic;
    they were constructor-shaped free helpers adjacent to the true target
    owner
  - the background-create workflow already depends on
    `SingleChapterGenerationTarget`, so keeping the projection logic outside
    the type created unnecessary write-lane noise

  The refactor therefore tightened the single-generation workflow boundary
  without changing behavior:
  - `SingleChapterGenerationTarget` now owns
    `background_create_response_payload(...)`
  - `SingleChapterGenerationTarget` now owns `task_chapter_payload()`
  - `SingleChapterGenerationTarget` now owns
    `build_background_task_active_model(...)`
  - background-create workflow assembly and tests now use those type-owned
    methods directly instead of free helper names

  This keeps single-chapter background response payload shape, task snapshot
  content, active-model defaults, runtime checkpoint persistence, and
  background dispatch behavior unchanged while removing another cluster of
  constructor-alias helpers from the Rust single-generation write lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_single_generation_workflow_service.rs` when
    the next move removes another true constructor alias, single-consumer
    passthrough shell, or fake response-assembly split
  - do not split stream launch / runtime execution owners unless a future
    audit finds a concrete fake boundary rather than a real workflow/runtime
    handoff

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the same single-generation background-create lane and
  removed the remaining single-caller task-plan shell inside
  `chapter_single_generation_workflow_service.rs`.

  A focused audit showed that:
  - `create_single_generation_background_task_plan(...)` had only one caller:
    `start_owned_single_generation_background_workflow(...)`
  - the helper no longer represented a meaningful reusable owner after the
    chapter payload / active-model / response payload assembly had already been
    moved onto `SingleChapterGenerationTarget`
  - its remaining responsibility was just a short inline sequence:
    `task_id generation -> task insert -> pending snapshot persist -> created
    id return`

  The refactor therefore tightened the background write workflow owner without
  changing behavior:
  - `start_owned_single_generation_background_workflow(...)` now owns the
    background task creation and pending snapshot persistence sequence directly
  - `create_single_generation_background_task_plan(...)` was removed as a
    single-consumer workflow shell
  - the workflow still returns the same response payload, persists the same
    pending checkpoint, and dispatches the same runtime input after task
    creation succeeds

  This keeps single-chapter background task creation semantics, pending
  checkpoint persistence, response payload shape, and runtime dispatch
  behavior unchanged while removing one more no-signal workflow helper layer
  from the Rust single-generation lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_single_generation_workflow_service.rs` when
    the next move removes another true single-consumer shell around stream
    launch or background dispatch sequencing
  - do not split the stream launch owner unless a future audit shows a fake
    boundary rather than a real async runtime orchestration owner

- 2026-05-26 follow-up checkpoint:
  this slice returned to the batch read-side lane and removed the remaining
  `BatchGenerationReadContext -> payload` passthrough wrappers inside
  `chapter_batch_generation_task_view_query_service.rs`.

  A focused audit showed that:
  - `build_active_batch_generation_task_view_payload(...)` only unpacked
    `BatchGenerationReadContext` and forwarded its fields to
    `active_task_payload(...)`
  - `build_batch_generation_status_task_view_payload(...)` only unpacked
    `BatchGenerationReadContext` and forwarded its fields to
    `task_status_payload(...)`
  - neither helper owned response envelope logic, access semantics, query
    behavior, or any compatibility-sensitive branching; they were just
    context-shaped passthrough shells inside the same file

  The refactor therefore tightened the task-view query owner without changing
  behavior:
  - owned-status query now builds its payload directly through
    `task_status_payload(...)`
  - active-query response and active-list response now build their item payload
    directly through `active_task_payload(...)`
  - the two context passthrough wrappers were removed, leaving the file with
    one fewer fake owner split between context loading and payload assembly

  This keeps owned status payload shape, active-task payload shape,
  active-query response shape, and active-list response shape unchanged while
  removing another pair of no-signal read-side wrapper layers from the batch
  generation seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_task_view_query_service.rs`
    when the next move removes another true context passthrough shell or fake
    same-file owner split
  - do not flatten the remaining active-query/list/status response owners if
    the next step would only move envelope shapes around without reducing
    boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch stream read-side lane and removed the
  remaining stream-state loading wrappers inside
  `chapter_batch_generation_status_stream_service.rs`.

  A focused audit showed that:
  - `build_batch_generation_stream_state(...)` only converted
    `BatchGenerationReadContext` into `BatchGenerationStreamState` and had no
    other consumer or branching semantics beyond that owner boundary
  - `load_optional_owned_batch_generation_stream_state(...)` only wrapped
    `load_owned_batch_generation_read_context(...)` and immediately converted
    the returned context into stream state for the polling loop
  - `load_required_owned_batch_generation_stream_state(...)` only wrapped
    `load_required_owned_batch_generation_read_context(...)` and immediately
    converted the returned context into stream state for initial stream access

  The refactor therefore tightened the stream read owner without changing
  behavior:
  - `BatchGenerationReadContext` now owns `into_stream_state()`
  - status stream polling now loads the optional read context directly and
    converts it to stream state inline
  - initial owned stream access now loads the required read context directly
    and converts it to stream state inline
  - the three free helper shells were removed, leaving
    `chapter_batch_generation_status_stream_service.rs` with orchestration plus
    direct owner calls only

  This keeps stream not-found behavior, terminal event behavior, timeout
  behavior, status semantics, and initial stream access behavior unchanged
  while removing another cluster of no-signal stream-state wrappers from the
  batch generation seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_status_stream_service.rs`
    when the next move removes another true polling-adjacent passthrough shell
    without flattening event orchestration ownership
  - do not merge stream polling with event-batch ownership if the next step
    would blur the boundary between polling cadence and event semantics

- 2026-05-26 follow-up checkpoint:
  this slice returned to the batch write lane and tightened the remaining
  response-shape ownership in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `build_batch_generation_create_response_payload(...)` only depended on
    `PreparedBatchGenerationCreateRequest` data plus the created batch id, so
    it was effectively a create-request-owned response projection
  - `build_resume_batch_generation_response_payload(...)` only depended on
    `ResumeBatchGenerationResponseState`, so it was effectively a
    response-state-owned projection rather than a reusable free helper
  - the remaining `estimate_batch_generation_task_minutes(...)` helper had no
    meaning outside create-response projection once the response shape stayed
    within `PreparedBatchGenerationCreateRequest`

  The refactor therefore tightened the batch write response owner without
  changing behavior:
  - `PreparedBatchGenerationCreateRequest` now owns
    `estimated_task_minutes(...)`
  - `PreparedBatchGenerationCreateRequest` now owns
    `create_response_payload(...)`
  - `ResumeBatchGenerationResponseState` now owns
    `into_response_payload()`
  - create/resume workflow entrypoints now call those owner methods directly
    instead of routing through standalone free response builders

  This keeps batch-create response payload shape, estimated-time semantics,
  resume response payload shape, shared checkpoint metadata, and runtime
  dispatch behavior unchanged while removing another group of free
  response-shape helpers from the Rust batch write seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_write_workflow_service.rs`
    when the next move removes another true owner-local projection shell
    rather than flattening runtime handoff logic
  - do not collapse create/resume dispatch sequencing into response owners if
    the next step would blur the boundary between response projection and
    runtime launch orchestration

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the same batch write lane and tightened the remaining
  resume execution conversion boundary in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `build_resume_generation_execution_input(...)` had only one caller:
    `launch_batch_generation_resume_runtime(...)`
  - the helper existed only to convert `ResumeExecutionPlan` into the local
    `ResumeGenerationExecutionInput` enum, so its data ownership already
    belonged to the resume plan itself rather than a separate free function
  - leaving the conversion as a free helper no longer reduced complexity after
    response projection had already been moved onto
    `ResumeBatchGenerationResponseState`

  The refactor therefore tightened the resume execution owner without changing
  behavior:
  - `ResumeExecutionPlan` now owns `into_runtime_launch_input(...)`
  - resume runtime launch now calls the plan-owned conversion directly instead
    of routing through `build_resume_generation_execution_input(...)`
  - the focused tests now assert resume execution conversion through the plan
    owner boundary rather than the old free helper name

  This keeps batch/single resume dispatch selection, runtime launch input
  payloads, provider config handoff, and resume runtime behavior unchanged
  while removing another no-signal conversion shell from the Rust batch write
  seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_write_workflow_service.rs`
    when the next move removes another true plan/response owner shell without
    flattening runtime dispatch orchestration
  - do not merge the remaining dispatch launch functions unless a future audit
    shows they are fake boundaries rather than real orchestration owners

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the same batch write lane and removed the remaining
  free launch-helper shells around runtime dispatch in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `launch_batch_generation_create_runtime(...)` had only one caller and only
    converted `BatchGenerationCreatePlan` into `BatchGenerationExecutionInput`
    before dispatch
  - `launch_batch_generation_resume_runtime(...)` had only one caller and only
    delegated `ResumeExecutionPlan -> ResumeGenerationExecutionInput ->
    runtime dispatch`
  - after the prior slices moved response and resume-execution conversion onto
    their real owners, these free functions had become owner-local orchestration
    shells rather than meaningful shared boundaries

  The refactor therefore tightened the runtime launch owner without changing
  behavior:
  - `BatchGenerationCreatePlan` now owns `dispatch_runtime(...)`
  - `ResumeGenerationExecutionInput` now owns `dispatch(...)`
  - `ResumeExecutionPlan` now owns `dispatch_runtime(...)`
  - create/resume workflow entrypoints now call those owner methods directly
    instead of routing through standalone launch helpers

  This keeps batch create dispatch behavior, resume dispatch selection,
  execution-config handoff, batch/single runtime input payloads, and runtime
  side effects unchanged while removing another group of free orchestration
  helpers from the Rust batch write seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_write_workflow_service.rs`
    when the next move removes another true owner-local shell rather than
    collapsing runtime semantics into the workflow file
  - do not flatten the remaining write-lane owners if the next step would only
    move code around without reducing boundary noise or fake ownership

- 2026-05-26 follow-up checkpoint:
  this slice moved to the single-chapter stream workflow lane and tightened
  the remaining owner-local SSE initialization shell in
  `chapter_single_generation_workflow_service.rs`.

  A focused audit showed that:
  - `launch_owned_single_chapter_generation_stream(...)` still hard-coded the
    same initial tracker sequence directly inside the `tokio::spawn` closure:
    `start -> preparing -> generating`
  - those two initial progress events were not a real runtime-orchestration
    boundary; they were just a local stream-state projection owned entirely by
    the single-chapter workflow file
  - the repository already uses the same ownership style in the regeneration
    stream lane, where initial SSE steps are represented explicitly and then
    applied to one local tracker owner

  The refactor therefore tightened the single-chapter stream owner without
  changing behavior:
  - `OwnedSingleGenerationInitialEvent` now describes the workflow-local
    initial tracker steps
  - `owned_single_generation_initial_events(...)` now owns the standard
    preparing/generating initialization sequence for single-chapter stream
    launch
  - `apply_owned_single_generation_initial_event(...)` now owns how those
    local steps advance `SseProgress`
  - `launch_owned_single_chapter_generation_stream(...)` now keeps only stream
    orchestration plus execution/result/error dispatch, while the initial
    event sequence is emitted through the local owner boundary

  This keeps single-chapter stream start behavior, initial progress ordering,
  generating progress semantics, runtime execution, final result emission, and
  error emission unchanged while removing one more owner-local inline shell
  from the Rust chapter-generation stream seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_single_generation_workflow_service.rs` when
    the next move removes another true owner-local shell around stream launch
    without flattening runtime execution ownership
  - do not split `execute_owned_single_chapter_generation(...)` or move stream
    result/error orchestration unless a future audit shows another fake
    boundary instead of a real async runtime owner

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the single-chapter runtime-state lane and tightened
  the repeated task-mutation persistence shell in
  `chapter_single_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `prepare_single_generation_runtime(...)`,
    `finalize_single_generation_success(...)`, and
    `finalize_single_generation_failure(...)` each repeated the same
    `find task -> convert to active model -> apply mutation plan -> update`
    persistence sequence before writing their respective runtime snapshots
  - that repeated block did not own stage-specific semantics by itself; the
    real stage differences already lived in
    `resolve_single_generation_task_mutation_plan(...)`
  - leaving the duplicated persistence shell in three branches created another
    fake same-file owner split inside the single-chapter runtime-state lane

  The refactor therefore tightened the runtime-state owner without changing
  behavior:
  - `persist_single_generation_task_mutation_plan(...)` now owns the shared
    task-update persistence path for a resolved mutation plan
  - `persist_single_generation_task_stage(...)` now owns the
    `stage -> mutation plan -> persisted task update` handoff
  - preparing, success, and failure runtime-state functions now delegate their
    task update through that shared owner and keep only stage-specific snapshot
    writes plus control-flow sequencing

  This keeps single-chapter runtime status transitions, chapter pointer
  updates, completed/error timestamps, retry reset semantics, and snapshot
  progression unchanged while removing one more repeated owner-local
  persistence shell from the Rust single-chapter runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_single_generation_runtime_state_service.rs`
    when the next move removes another true repeated persistence shell or fake
    same-file owner split around task/snapshot state updates
  - do not flatten `execute_single_generation_runtime(...)` result branching or
    merge runtime snapshot sequencing into one monolithic helper unless a
    future audit shows another fake boundary instead of a real runtime-state
    owner

- 2026-05-26 follow-up checkpoint:
  this slice returned to the batch write lane and tightened the remaining
  chapter-id projection shell in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `create_batch_generation_task_plan(...)` still rebuilt two local
    projections from `PreparedBatchGenerationCreateRequest`:
    `Vec<String>` chapter ids for dispatch and `Value::Array` chapter id
    payload for task persistence
  - both projections depended only on
    `PreparedBatchGenerationCreateRequest.chapters_to_generate`
  - that conversion no longer represented a real workflow/task-plan owner
    boundary, because the prepared request already owned the chapter target
    collection and no caller outside the file consumed a separate projection
    helper

  The refactor therefore tightened the prepared-request owner without changing
  behavior:
  - `PreparedBatchGenerationCreateRequest` now owns `chapter_ids()`
  - `PreparedBatchGenerationCreateRequest` now owns `chapter_id_payload()`
  - `create_batch_generation_task_plan(...)` now delegates both projections to
    the prepared-request owner and keeps only task-plan creation sequencing

  This keeps batch task creation payload shape, persisted chapter id ordering,
  runtime dispatch chapter ordering, target word-count behavior, and response
  payload behavior unchanged while removing one more owner-local projection
  shell from the Rust batch write seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_write_workflow_service.rs`
    when the next move removes another true prepared-owner projection shell or
    fake same-file owner split
  - do not flatten the remaining task-plan creation or runtime-dispatch
    sequencing if the next step would only reshuffle orchestration code

- 2026-05-26 follow-up checkpoint:
  this slice moved to the single-chapter prepare lane and tightened the
  remaining request-owned projection shell in
  `chapter_single_generation_prepare_service.rs`.

  A focused audit showed that:
  - `prepare_single_chapter_generation_request(...)` still built
    `SingleChapterGenerationTarget` inline from the loaded chapter model
  - the same function also still assembled
    `PreparedSingleChapterGenerationRequest` inline from
    `chapter_target + normalized_target_word_count + execution_config`
  - both projections were request-owned data shaping steps and did not carry
    independent access semantics, config loading semantics, or runtime
    ownership beyond the prepare boundary itself

  The refactor therefore tightened the single-chapter prepare owner without
  changing behavior:
  - `SingleChapterGenerationTarget` now owns `from_model(...)`
  - `PreparedSingleChapterGenerationRequest` now owns `new(...)`
  - `prepare_single_chapter_generation_request(...)` now keeps only the
    access/config loading sequence plus normalized request handoff through the
    local prepare owner boundary

  This keeps accessible chapter loading behavior, execution-config loading,
  normalized target word-count behavior, prepared execution-input shape, and
  chapter target projection unchanged while removing one more owner-local
  request projection shell from the Rust single-chapter prepare seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_single_generation_prepare_service.rs` when
    the next move removes another true request-owned projection shell or fake
    same-file owner split
  - do not flatten access loading and execution-config loading into one
    monolithic helper if the next step would only reshuffle the prepare
    sequence without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch runtime-state lane and tightened the
  repeated task-mutation persistence shell in
  `chapter_batch_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `prepare_batch_generation_runtime(...)`,
    `finalize_batch_generation_cancelled(...)`,
    `mark_batch_generation_chapter_started(...)`,
    `finalize_batch_generation_success(...)`, and
    `finalize_batch_generation_failure(...)` each repeated the same
    `find task -> convert to active model -> apply mutation plan -> update`
    persistence sequence before writing their stage-specific runtime snapshot
  - the real stage semantics already lived in
    `resolve_batch_generation_task_mutation_plan(...)`
  - that left the file with another same-file fake owner split, where five
    branches repeated identical persistence mechanics without adding new
    behavior

  The refactor therefore tightened the batch runtime-state owner without
  changing behavior:
  - `persist_batch_generation_task_mutation_plan(...)` now owns the shared
    task-update persistence path for a resolved mutation plan
  - `persist_batch_generation_task_stage(...)` now owns the
    `stage -> mutation plan -> persisted task update` handoff
  - preparing, cancelled, chapter-started, success, and failure runtime-state
    functions now delegate their task update through that shared owner and
    keep only stage-specific snapshot writes plus control-flow sequencing

  This keeps batch runtime status transitions, chapter pointer updates,
  completed/error timestamps, total/completed chapter counters, and runtime
  snapshot progression unchanged while removing one more repeated owner-local
  persistence shell from the Rust batch runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_runtime_state_service.rs`
    when the next move removes another true repeated persistence shell or fake
    same-file owner split around task/snapshot state updates
  - do not flatten the main batch runtime loop or merge snapshot sequencing
    into one monolithic helper unless a future audit shows another fake
    boundary instead of a real runtime-state owner

- 2026-05-26 follow-up checkpoint:
  this slice moved to the batch cancel-command lane and tightened the
  remaining same-file plan/response shell in
  `chapter_batch_generation_cancel_service.rs`.

  A focused audit showed that:
  - `CancelBatchGenerationPlan` only wrapped one field:
    `response_state: CancelBatchGenerationResponseState`
  - `build_cancel_batch_generation_response_payload(...)` only projected
    `CancelBatchGenerationResponseState` into the final compat payload and had
    no external reuse
  - both owners existed only inside the same file, so they no longer carried a
    meaningful boundary beyond one extra layer of indirection in the cancel
    flow

  The refactor therefore tightened the cancel-command owner without changing
  behavior:
  - `CancelBatchGenerationResponseState` now owns
    `from_command_state(...)`
  - `CancelBatchGenerationResponseState` now owns
    `into_response_payload()`
  - `cancel_batch_generation_task(...)` now returns the response state
    directly instead of a one-field `CancelBatchGenerationPlan`
  - `cancel_owned_batch_generation_task(...)` now delegates final payload
    projection through the response-state owner boundary

  This keeps cancel status validation, runtime cancellation side effects,
  response payload shape, batch totals, and user-facing cancel message
  unchanged while removing one more same-file fake owner split from the Rust
  batch command seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_cancel_service.rs` when the
    next move removes another true same-file response shell or fake owner
    split
  - do not flatten task loading, status validation, and runtime cancellation
    into one helper if the next step would only reshuffle the command
    orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice moved to the batch resume-command lane and tightened the
  remaining same-file execution conversion shell in
  `chapter_batch_generation_resume_task_command_service.rs`.

  A focused audit showed that:
  - `build_resume_execution_plan(...)` only converted
    `ResumeBatchGenerationCommandState + user_id` into the local
    `ResumeExecutionPlan`
  - the helper had no external reuse and no independent ownership beyond the
    command state that already carried the resume semantics inputs
  - leaving it as a free function kept one more same-file conversion shell in
    the resume lane after response-state ownership and runtime reset semantics
    were already separated elsewhere

  The refactor therefore tightened the resume-command owner without changing
  behavior:
  - `ResumeBatchGenerationCommandState` now owns
    `into_resume_execution_plan(...)`
  - `prepare_batch_generation_resume(...)` now delegates execution conversion
    directly through the command-state owner boundary
  - focused tests now assert plan construction through the command-state owner
    instead of the old free helper name

  This keeps resume status validation, manual-review blocking, target-word
  normalization, single/batch execution target selection, runtime reset
  sequencing, and resume response-state behavior unchanged while removing one
  more same-file conversion shell from the Rust batch command seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_resume_task_command_service.rs`
    when the next move removes another true same-file conversion shell or fake
    owner split
  - do not flatten status validation, manual-review blocking, runtime reset,
    and response projection into one helper if the next step would only
    reshuffle the resume orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice stayed in the batch resume-command lane and tightened the
  remaining same-file response-state projection shell in
  `chapter_batch_generation_resume_task_command_service.rs`.

  A focused audit showed that:
  - `ResumeBatchGenerationResponseState::from_command_state(...)` only
    projected the live command state into the local response-state owner
  - `ResumeBatchGenerationResponseState::from_reset_command_state(...)` only
    projected the reset command state into the same response-state owner
  - both conversions were only consumed inside the same file, so keeping them
    as free constructors left another fake same-file owner split after the
    execution-plan conversion had already been tightened

  The refactor therefore tightened the resume-command owner without changing
  behavior:
  - `ResumeBatchGenerationCommandState` now owns `response_state()`
  - `ResumeBatchGenerationCommandState` now owns `reset_response_state()`
  - `prepare_batch_generation_resume(...)` now delegates response projection
    through the command-state owner boundary for both the normal resume path
    and the reset snapshot path

  This keeps resume status validation, manual-review blocking, reset snapshot
  sequencing, response payload shape, completed/total chapter reporting, and
  user-facing resume message behavior unchanged while removing one more
  same-file response projection shell from the Rust batch command seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_resume_task_command_service.rs`
    when the next move removes another true same-file projection shell or fake
    owner split
  - do not flatten status validation, manual-review blocking, reset
    sequencing, and response projection into one helper if the next step would
    only reshuffle the resume orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch read-side task-view lane and tightened
  the remaining read-context projection shell across
  `chapter_batch_generation_read_context_service.rs` and
  `chapter_batch_generation_task_view_query_service.rs`.

  A focused audit showed that:
  - `task_view_query_service` still owned `active_task_payload(...)` and
    `task_status_payload(...)`, even though both functions only projected the
    already-loaded `BatchGenerationReadContext` fields into read-side payloads
  - the same file also kept `build_shared_task_view_payload(...)` and
    `insert_quality_status_fields(...)` as local helper shells that existed
    only to unpack `task + workflow_runtime_state + quality_status_context`
    from the same read-context owner
  - `read_context_service` still used a free
    `build_batch_generation_read_context(...)` constructor instead of letting
    the read-context owner own its own creation boundary

  The refactor therefore tightened the batch read-context owner without
  changing behavior:
  - `BatchGenerationReadContext` now owns `new(...)`
  - `BatchGenerationReadContext` now owns the shared task-view projection
    mechanics through local owner methods
  - `BatchGenerationReadContext` now owns
    `into_active_task_payload(...)`
  - `BatchGenerationReadContext` now owns
    `into_status_task_payload(...)`
  - `chapter_batch_generation_task_view_query_service.rs` now keeps query
    loading plus response envelope assembly only, and delegates the actual
    task payload projection through the read-context owner boundary

  This keeps active-task payload shape, status-task payload shape,
  checkpoint/runtime metadata projection, quality-status fields,
  terminal/can-resume fields, and query response envelopes unchanged while
  removing one more same-file read-side projection shell from the Rust batch
  task-view seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch read-side task-view files when the next move
    removes another true read-context projection shell or fake owner split
  - do not flatten query loading, owned-access validation, and response
    envelope assembly into one helper if the next step would only reshuffle
    the read-side orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the batch read-context lane and tightened the
  remaining stream-state projection shell across
  `chapter_batch_generation_read_context_service.rs` and
  `chapter_batch_generation_status_stream_service.rs`.

  A focused audit showed that:
  - `chapter_batch_generation_status_stream_service.rs` still owned
    `BatchGenerationReadContext::into_stream_state(...)` inline inside the
    stream orchestration file
  - that projection only converted the already-loaded read-context owner into
    `BatchGenerationStreamState` using existing stream semantics, and had no
    independent polling or transport ownership of its own
  - leaving the conversion in the stream file kept one more read-side fake
    owner split after task payload projections had already been moved back to
    the read-context owner

  The refactor therefore tightened the batch read-context owner without
  changing behavior:
  - `BatchGenerationReadContext` now owns `into_stream_state(...)`
  - stream-state focused assertions now live with the read-context owner tests
    instead of the stream orchestration file
  - `chapter_batch_generation_status_stream_service.rs` now keeps only owned
    access loading, polling cadence, SSE send orchestration, and cursor/event
    handling

  This keeps batch stream payload shape, progress/message/event-status
  semantics, terminal-kind resolution, owned-task access behavior, and stream
  polling/timeout behavior unchanged while removing one more read-context
  projection shell from the Rust batch status-stream seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch read-context / status-stream files when the
    next move removes another true read-side projection shell or fake owner
    split
  - do not flatten owned access loading, polling cadence, timeout handling,
    and SSE send orchestration into one helper if the next step would only
    reshuffle the stream control path

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch cancel-command lane and tightened the
  remaining same-file response-state projection shell in
  `chapter_batch_generation_cancel_service.rs`.

  A focused audit showed that:
  - `CancelBatchGenerationResponseState::from_command_state(...)` still only
    projected the local `CancelBatchGenerationCommandState` into the response
    state owner
  - the conversion had no external reuse and no independent ownership beyond
    the command state that already carried the required response fields
  - that left one more fake same-file owner split in the cancel lane, even
    after the one-field plan shell had already been removed

  The refactor therefore tightened the cancel-command owner without changing
  behavior:
  - `CancelBatchGenerationCommandState` now owns `into_response_state()`
  - `cancel_batch_generation_task(...)` now delegates final response-state
    projection through the command-state owner boundary
  - focused response-state / payload tests now assert projection through the
    command-state owner instead of the old response-state constructor

  This keeps cancel status validation, runtime cancellation side effects,
  response payload shape, completed/total chapter reporting, and user-facing
  cancel message behavior unchanged while removing one more same-file response
  projection shell from the Rust batch cancel-command seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking `chapter_batch_generation_cancel_service.rs` when the
    next move removes another true same-file projection shell or fake owner
    split
  - do not flatten task loading, status validation, runtime cancellation, and
    response projection into one helper if the next step would only reshuffle
    the cancel orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch resume-semantics lane and tightened the
  remaining same-file semantics shell across
  `chapter_batch_generation_resume_semantics_service.rs`,
  `chapter_batch_generation_resume_task_command_service.rs`, and
  `chapter_batch_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `resolve_resume_runtime_semantics(...)` and
    `resolve_resume_execution_target(...)` were still free semantics helpers
    even though they only derived owner-local decisions from
    `ResumeBatchGenerationCommandState`
  - the file also kept the chapter-id parsing and intermediate semantics
    decision flow outside the command-state owner, leaving another same-file
    fake owner split after response-state and execution-plan projections had
    already been moved closer to the command owner
  - downstream consumers in resume command preparation and runtime reset logic
    already depended only on the semantics result, not on the helper location

  The refactor therefore tightened the resume command/semantics owner without
  changing behavior:
  - `ResumeBatchGenerationCommandState` now owns `parse_batch_chapter_ids()`
  - `ResumeBatchGenerationCommandState` now owns `resolve_semantics()`
  - `ResumeBatchGenerationCommandState` now owns
    `resolve_runtime_semantics()`
  - `ResumeBatchGenerationCommandState` now owns
    `resolve_execution_target()`
  - resume command preparation and runtime reset/checkpoint code now delegate
    directly through the command-state owner boundary instead of calling free
    semantics helpers

  This keeps single-vs-batch resume classification, malformed-single fallback
  behavior, resume execution target selection, runtime checkpoint reset
  semantics, and response payload behavior unchanged while removing one more
  same-file semantics shell from the Rust batch resume seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch resume semantics / command files when the
    next move removes another true same-file semantics shell or fake owner
    split
  - do not flatten invalid-status checks, manual-review blocking, runtime
    reset sequencing, and execution-plan construction into one helper if the
    next step would only reshuffle the resume orchestration path

- 2026-05-26 follow-up checkpoint:
  this slice moved into the batch runtime-checkpoint lane and tightened the
  remaining same-file checkpoint planning shell in
  `chapter_batch_generation_runtime_checkpoint_service.rs`.

  A focused audit showed that:
  - `resolve_batch_generation_snapshot_plan(...)` only translated
    `BatchGenerationSnapshotStage` plus the already-known chapter progress
    inputs into a local `BatchGenerationSnapshotPlan`
  - `build_batch_generation_runtime_checkpoint_from_plan(...)` only projected
    that local plan into the final checkpoint payload shape
  - both helpers were consumed only inside the same file, so they left a fake
    owner split between the snapshot stage enum, the local checkpoint plan,
    and the final payload builder

  The refactor therefore tightened the runtime-checkpoint owner without
  changing behavior:
  - `BatchGenerationSnapshotStage` now owns `resolve_plan(...)`
  - `BatchGenerationSnapshotPlan` now owns `build_checkpoint(...)`
  - `build_batch_generation_runtime_checkpoint_for_stage(...)` keeps the
    external compatibility contract and now delegates through those owner
    methods instead of same-file free helpers

  This keeps queued/resumed/preparing/running/completed/cancelled/failed
  checkpoint semantics, progress totals behavior, current chapter message
  behavior, and checkpoint payload shape unchanged while removing one more
  same-file planning/projection shell from the Rust batch runtime-checkpoint
  seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch runtime-checkpoint files when the next move
    removes another true same-file checkpoint planning/projection shell or
    fake owner split
  - do not flatten stage semantics, progress calculation, and final payload
    assembly into one helper if the next step would only reshuffle local
    checkpoint orchestration without clarifying ownership

- 2026-05-26 follow-up checkpoint:
  this slice moved into the batch task-payload-base lane and tightened the
  remaining same-file runtime payload builder chain across
  `chapter_batch_generation_task_payload_base_service.rs`,
  `chapter_batch_generation_read_context_service.rs`, and
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `build_batch_generation_task_runtime_payload_from_state(...)` only derived
    checkpoint metadata from task state and immediately forwarded into
    `build_batch_generation_task_runtime_payload(...)`
  - `build_batch_generation_task_runtime_payload(...)` then only forwarded the
    same owner-local fields into
    `build_batch_generation_task_runtime_payload_from_parts(...)`
  - downstream read-side and resume-response callers only needed the final
    payload base contract, not the intermediate same-file builder chain

  The refactor therefore tightened the batch task-payload owner without
  changing behavior:
  - `BatchGenerationTaskRuntimePayloadBase` now owns the batch runtime payload
    base contract
  - the payload owner now exposes `new(...)`, `from_task(...)`,
    `from_task_state(...)`, and `into_payload()`
  - read-context payload projection and resume response payload assembly now
    delegate through the payload owner boundary instead of chaining local free
    builders

  This keeps task runtime payload shape, stage/execution metadata injection,
  created-at projection, checkpoint override behavior, read-side task payload
  assembly, and resume response payload behavior unchanged while removing one
  more same-file builder shell from the Rust batch task-payload seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch task-payload-base files when the next move
    removes another true same-file payload projection/builder shell or fake
    owner split
  - do not flatten read-side payload enrichment, resume response assembly,
    and checkpoint metadata injection into one helper if the next step would
    only reshuffle payload composition without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the batch snapshot-write lane and tightened the
  remaining same-file write-mode projection shell in
  `chapter_batch_generation_snapshot_service.rs`.

  A focused audit showed that:
  - `resolve_batch_generation_snapshot_runtime_state_for_write(...)` only
    expressed the runtime-state merge-vs-replace decision that already belongs
    to `BatchGenerationSnapshotWriteMode`
  - `apply_batch_generation_snapshot_write_mode(...)` only applied that same
    write-mode choice back onto the snapshot active model, including the
    replace-mode quality-field reset
  - both helpers were consumed only inside the same file, so they left
    another fake owner split between the write-mode enum and the snapshot
    mutation behavior it already defined

  The refactor therefore tightened the snapshot write-mode owner without
  changing behavior:
  - `BatchGenerationSnapshotWriteMode` now owns `resolve_runtime_state(...)`
  - `BatchGenerationSnapshotWriteMode` now owns `apply_to_active_model(...)`
  - `write_batch_generation_runtime_snapshot(...)` now delegates runtime-state
    resolution and active-model mutation directly through the write-mode owner
    boundary instead of same-file free helpers

  This keeps merge-vs-replace runtime snapshot semantics, resume-time quality
  reset behavior, new-snapshot insert shape, and runtime snapshot write entry
  contracts unchanged while removing one more same-file projection shell from
  the Rust batch snapshot-write seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch snapshot-write files when the next move
    removes another true same-file write-mode projection shell or fake owner
    split
  - do not flatten snapshot loading, insert/update branching, and runtime
    state persistence into one helper if the next step would only reshuffle
    the write orchestration path without clarifying ownership

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch runtime-state mutation lane and
  tightened the remaining same-file reset-plan / mutation-plan owner split in
  `chapter_batch_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `resolve_resume_batch_generation_task_reset_plan(...)` only projected
    `ResumeBatchGenerationCommandState` into the local
    `ResumeBatchGenerationTaskResetPlan` owner
  - `resolve_batch_generation_task_mutation_plan(...)` only expressed the
    mutation semantics that already belong to `BatchGenerationTaskStage`
  - `apply_batch_generation_task_mutation_plan(...)` only applied the local
    `BatchGenerationTaskMutationPlan` owner back onto the active model
  - all three helpers were same-file only, so they left another fake owner
    split inside the runtime mutation lane after the snapshot/checkpoint
    ownership had already been tightened

  The refactor therefore tightened the runtime mutation owners without
  changing behavior:
  - `ResumeBatchGenerationTaskResetPlan` now owns `from_command_state(...)`
  - `BatchGenerationTaskStage` now owns `resolve_mutation_plan(...)`
  - `BatchGenerationTaskMutationPlan` now owns `apply_to_active_model(...)`
  - resume reset and task-stage persistence paths now delegate directly
    through those owners instead of chaining same-file free helpers

  This keeps resume reset semantics, task status/stage mutation behavior,
  started/completed timestamp policy, failed/cancelled task mutation rules,
  and runtime write entry contracts unchanged while removing one more
  same-file owner split from the Rust batch runtime-state seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch runtime-state files when the next move
    removes another true same-file reset/mutation projection shell or fake
    owner split
  - do not flatten runtime stage persistence, snapshot writes, and the main
    execution loop into one helper if the next step would only reshuffle the
    orchestration path without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the batch runtime-state persistence lane and tightened
  the remaining same-file stage-persistence passthrough shell in
  `chapter_batch_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `persist_batch_generation_task_mutation_plan(...)` only persisted the
    already-owned `BatchGenerationTaskMutationPlan` onto the task active model
  - `persist_batch_generation_task_stage(...)` only chained
    `BatchGenerationTaskStage -> resolve_mutation_plan(...) -> persist plan`
    without adding independent ownership or behavior
  - both helpers were same-file only, so they left another fake owner split
    in the runtime-state persistence lane after reset-plan and mutation-plan
    ownership had already been tightened

  The refactor therefore tightened the runtime persistence owners without
  changing behavior:
  - `BatchGenerationTaskMutationPlan` now owns `persist_for_task(...)`
  - `BatchGenerationTaskStage` now owns `persist_for_task(...)`
  - runtime state write entrypoints now delegate task-stage persistence
    directly through those owners instead of routing through same-file free
    persistence helpers

  This keeps preparing/cancelled/started/succeeded/failed task persistence
  semantics, status/timestamp mutation behavior, and runtime-state write entry
  contracts unchanged while removing one more same-file passthrough shell from
  the Rust batch runtime-state seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch runtime-state files when the next move
    removes another true same-file persistence passthrough shell or fake
    owner split
  - do not flatten stage persistence, snapshot persistence, and the runtime
    execution loop into one helper if the next step would only reshuffle the
    orchestration path without clarifying ownership

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch write-workflow lane and tightened two
  remaining same-file create/resume passthrough shells in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `prepare_batch_generation_create_request(...)` only projected
    `BatchGenerationCreateWorkflowRequest` plus the already-loaded chapter
    range into the local prepared-request owner
  - `ResumeExecutionPlan::dispatch_runtime(...)` only chained
    `into_runtime_launch_input(...).dispatch(...)` without adding
    independent ownership or behavior
  - both helpers were same-file only, so they left low-signal owner splits in
    the write-workflow lane after response payload and execution-config
    ownership had already been tightened

  The refactor therefore tightened the write-workflow owners without changing
  behavior:
  - `BatchGenerationCreateWorkflowRequest` now owns `prepare(...)`
  - the standalone `prepare_batch_generation_create_request(...)` free helper
    has been removed
  - resume dispatch now goes directly through
    `into_runtime_launch_input(...).dispatch(...)`
  - `ResumeExecutionPlan::dispatch_runtime(...)` has been removed as a thin
    passthrough shell

  This keeps batch create request preparation semantics, normalized target
  word count behavior, chapter-range projection, resume runtime launch
  behavior, and create/resume write-workflow entry contracts unchanged while
  removing two more same-file passthrough shells from the Rust batch
  write-workflow seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch write-workflow files when the next move
    removes another true same-file create/resume passthrough shell or fake
    owner split
  - do not flatten project access, task creation, snapshot persistence, and
    runtime dispatch into one helper if the next step would only reshuffle
    write-workflow orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice stayed on the batch read-side / quality-context lane and
  tightened the remaining same-file quality-status projection shell across
  `chapter_batch_generation_quality_status_service.rs` and
  `chapter_batch_generation_read_context_service.rs`.

  A focused audit showed that:
  - `build_quality_status_context(...)` only projected snapshot/runtime-state
    inputs into the local `BatchGenerationQualityStatusContext` owner
  - `chapter_batch_generation_read_context_service.rs` still kept a local
    `insert_quality_status_fields(...)` helper even though it only reprojected
    the same `BatchGenerationQualityStatusContext` owner into payload fields
  - both helpers were local owner shells, so the read-side payload lane still
    split quality-context construction and payload projection across two files
    after the task payload base had already been tightened

  The refactor therefore tightened the quality-context owner without changing
  behavior:
  - `BatchGenerationQualityStatusContext` now owns
    `from_snapshot_and_runtime_state(...)`
  - `BatchGenerationQualityStatusContext` now owns `insert_into_payload(...)`
  - `chapter_batch_generation_read_context_service.rs` no longer keeps a
    local quality-field projection helper and now delegates quality payload
    insertion directly through the context owner boundary

  This keeps latest quality metrics projection, quality summary projection,
  active story repair payload extraction, read-side active/status payload
  shapes, and terminal payload semantics unchanged while removing one more
  same-file projection shell from the Rust batch read-side seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch read-side / quality-context files when the
    next move removes another true same-file quality-status projection shell
    or fake owner split
  - do not flatten read-context loading, terminal semantics, and stream-state
    conversion into one helper if the next step would only reshuffle
    orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the single-generation runtime-state lane and tightened
  the remaining same-file task-stage / mutation-plan owner split in
  `chapter_single_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - `resolve_single_generation_task_mutation_plan(...)` only expressed the
    mutation semantics that already belong to `SingleGenerationTaskStage`
  - `apply_single_generation_task_mutation_plan(...)` only applied the local
    `SingleGenerationTaskMutationPlan` owner back onto the active model
  - `persist_single_generation_task_mutation_plan(...)` and
    `persist_single_generation_task_stage(...)` only chained stage -> plan ->
    task persistence without adding independent ownership
  - all four helpers were same-file only, so the single-generation runtime
    lane still kept the same fake owner split that had already been removed
    from the batch runtime-state files

  The refactor therefore tightened the single-generation runtime owners
  without changing behavior:
  - `SingleGenerationTaskStage` now owns `resolve_mutation_plan(...)`
  - `SingleGenerationTaskStage` now owns `persist_for_task(...)`
  - `SingleGenerationTaskMutationPlan` now owns `apply_to_active_model(...)`
  - `SingleGenerationTaskMutationPlan` now owns `persist_for_task(...)`
  - runtime prepare/success/failure write entrypoints now delegate task-state
    persistence directly through those owners instead of same-file free
    helper chains

  This keeps preparing/completed/failed task persistence semantics,
  timestamp/error mutation behavior, single-generation runtime write entry
  contracts, and snapshot write ordering unchanged while removing one more
  same-file owner split from the Rust single-generation runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the single-generation runtime-state file when the
    next move removes another true same-file stage/mutation/persistence shell
    or fake owner split
  - do not flatten runtime snapshot writes, generation execution, and the
    main runtime loop into one helper if the next step would only reshuffle
    orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the analysis task-state lane and tightened the
  remaining same-file task-stage / mutation-plan owner split in
  `chapter_analysis_task_state_service.rs`.

  A focused audit showed that:
  - `resolve_analysis_task_mutation_plan(...)` only expressed the mutation
    semantics that already belong to `AnalysisTaskStage`
  - `apply_analysis_task_state(...)` only applied the local
    `AnalysisTaskMutationPlan` owner onto the active model
  - `apply_analysis_task_state_by_id(...)` only chained stage -> plan ->
    task persistence without adding independent ownership
  - all three helpers were same-file only, so the analysis task-state lane
    still kept the same fake owner split already removed from the batch and
    single-generation runtime-state files

  The refactor therefore tightened the analysis task-state owners without
  changing behavior:
  - `AnalysisTaskStage` now owns `resolve_mutation_plan(...)`
  - `AnalysisTaskMutationPlan` now owns `apply_to_active_model(...)`
  - `AnalysisTaskMutationPlan` now owns `persist_for_task(...)`
  - public create/apply/persist entrypoints now delegate through those owners
    instead of same-file free helper chains

  This keeps analysis task created/running/completed/failed/auto-recovered
  mutation semantics, timestamp behavior, and state persistence entry
  contracts unchanged while removing one more same-file owner split from the
  Rust analysis task-state seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_task_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the analysis task-state file when the next move
    removes another true same-file stage/mutation/persistence shell or fake
    owner split
  - do not flatten task query recovery, runtime execution, and route payload
    assembly into one helper if the next step would only reshuffle ownership
    without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice combined one single-generation checkpoint seam and one analysis
  task-state wrapper cleanup to keep the refactor lane moving at high speed
  without widening the write-path risk surface.

  A focused audit showed that:
  - `resolve_single_generation_snapshot_plan(...)` only expressed snapshot
    semantics that already belong to `SingleGenerationSnapshotStage`
  - `build_single_generation_runtime_checkpoint(...)` only projected the
    already-owned `SingleGenerationSnapshotPlan` back into the checkpoint
    payload shape
  - `chapter_analysis_task_state_service.rs` still kept
    `apply_analysis_task_state(...)` as a thin wrapper even after the stage
    and mutation-plan owners had already been tightened
  - both seams were local and behavior-preserving, so they were safe to fold
    into the same wave without creating cross-file orchestration risk

  The refactor therefore tightened these owners without changing behavior:
  - `SingleGenerationSnapshotStage` now owns `resolve_plan(...)`
  - `SingleGenerationSnapshotPlan` now owns `build_checkpoint(...)`
  - `build_single_generation_runtime_checkpoint_for_stage(...)` now delegates
    directly through those owners instead of same-file free helpers
  - `apply_analysis_task_state(...)` has been removed, and tests now exercise
    `AnalysisTaskStage -> resolve_mutation_plan(...) ->
    AnalysisTaskMutationPlan::apply_to_active_model(...)` directly

  This keeps single-generation snapshot payload semantics, progress/status
  mapping, timestamp injection, word-count projection, and analysis
  task-state mutation behavior unchanged while removing one more same-file
  owner split and one dead thin wrapper from the Rust seam inventory.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_checkpoint_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_task_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for these lanes:
  - only keep shrinking the single-generation checkpoint and analysis
    task-state files when the next move removes another true same-file owner
    split or dead wrapper
  - do not flatten runtime checkpoint writes, task query recovery, and route
    response assembly into one helper if the next step would only reshuffle
    ownership without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch write-workflow create lane and tightened
  two remaining same-file create-path free helpers in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - `load_chapters_for_batch_generation_range(...)` only expressed the chapter
    range loading semantics that already belong to
    `BatchGenerationCreateWorkflowRequest`
  - `create_batch_generation_task_plan(...)` only projected the same request
    owner plus the already-prepared chapter targets into the local
    `BatchGenerationCreatePlan`
  - both helpers were same-file only and only served the create workflow, so
    they left another fake owner split in the batch write-workflow lane after
    create preparation and resume dispatch had already been tightened

  The refactor therefore tightened the batch create workflow owner without
  changing behavior:
  - `BatchGenerationCreateWorkflowRequest` now owns
    `load_chapters_for_generation_range(...)`
  - `BatchGenerationCreateWorkflowRequest` now owns `create_task_plan(...)`
  - `start_owned_batch_generation_write_workflow(...)` now delegates create
    preparation and task-plan creation directly through the request owner
    instead of same-file free helper chains

  This keeps batch chapter-range selection semantics, invalid-count and
  no-chapter error behavior, task creation defaults, snapshot persistence,
  normalized target word count behavior, and create response/runtime dispatch
  contracts unchanged while removing two more same-file free helpers from the
  Rust batch write-workflow seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch write-workflow create lane when the next
    move removes another true same-file create-path free helper or fake owner
    split
  - do not flatten project access, execution-config loading, response
    assembly, and runtime dispatch into one helper if the next step would
    only reshuffle orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the analysis query auto-recovery lane and tightened the
  remaining same-file auto-recovery plan / execution split in
  `chapter_analysis_query_service.rs`.

  A focused audit showed that:
  - `resolve_analysis_task_auto_recovery_plan(...)` only expressed timeout
    recovery semantics that already belong to `AnalysisTaskAutoRecoveryPlan`
  - `recover_analysis_task_if_needed(...)` only applied that same plan back
    onto the persisted task state without adding independent ownership
  - both helpers were same-file only, so the analysis query lane still kept
    a small fake owner split between timeout recovery planning and recovery
    execution

  The refactor therefore tightened the analysis auto-recovery owner without
  changing behavior:
  - `AnalysisTaskAutoRecoveryPlan` now owns `resolve(...)`
  - `AnalysisTaskAutoRecoveryPlan` now owns `recover_task_if_needed(...)`
  - `analysis_task_status_payload(...)` now delegates timeout recovery
    directly through the plan owner instead of same-file free helper chains

  This keeps running/pending timeout thresholds, retry-aware timeout
  expansion, auto-recovery error messages, persisted failed-state transition,
  and analysis task status payload behavior unchanged while removing one more
  same-file owner split from the Rust analysis query seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the analysis query auto-recovery lane when the next
    move removes another true same-file recovery-plan / execution split or
    fake owner split
  - do not flatten chapter access checks, task lookup, batch payload
    assembly, and auto-recovery semantics into one helper if the next step
    would only reshuffle orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the regeneration prepare lane and tightened the
  remaining same-file length-mode / length-plan owner split in
  `chapter_regeneration_prepare_service.rs`.

  A focused audit showed that:
  - `normalize_partial_regeneration_length_mode(...)` only expressed the mode
    normalization semantics that already belong to
    `PartialRegenerationLengthMode`
  - `resolve_partial_regeneration_length_plan(...)` only expressed the
    requirement/target-word plan that already belongs to the normalized
    length-mode owner
  - `build_partial_length_requirement(...)` and
    `calculate_partial_target_words(...)` only reprojected the same local plan
    owner through free helper chaining
  - the whole chain was same-file only, so the regeneration prepare lane
    still kept a small fake owner split after the runtime-state and query
    lanes had already been tightened

  The refactor therefore tightened the regeneration length-strategy owners
  without changing behavior:
  - `PartialRegenerationLengthMode` now owns `normalize(...)`
  - `PartialRegenerationLengthMode` now owns `resolve_plan(...)`
  - `PartialRegenerationLengthPlan` now owns `requirement()` and
    `target_words()`
  - public requirement/target-word helpers now delegate directly through those
    owners instead of same-file free helper chains

  This keeps similar/expand/condense/custom length semantics, custom fallback
  behavior, prompt requirement text, target-word calculations, and partial
  regeneration prepare behavior unchanged while removing one more same-file
  owner split from the Rust regeneration prepare seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the regeneration prepare lane when the next move
    removes another true same-file mode/plan split or fake owner split
  - do not flatten prompt assembly, style lookup, web-research note building,
    and input-range validation into one helper if the next step would only
    reshuffle orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the chapter generation runtime lane and tightened the
  remaining same-file persistence-plan / error-mapping owner split in
  `chapter_generation_runtime_service.rs`.

  A focused audit showed that:
  - `prepare_generated_chapter_persistence_plan(...)` only expressed the
    persistence semantics that already belong to
    `GeneratedChapterPersistencePlan`
  - `persist_generated_chapter_content(...)` only applied that same plan back
    onto the active chapter model without adding independent ownership
  - `map_load_generation_context_error(...)` only projected the local
    `LoadGenerationContextError` owner into the runtime message contract
  - all three helpers were same-file only, so the generation runtime lane
    still kept another small fake owner split after the surrounding
    chapter-generation seams had already been tightened

  The refactor therefore tightened the chapter generation runtime owners
  without changing behavior:
  - `GeneratedChapterPersistencePlan` now owns `prepare(...)`
  - `GeneratedChapterPersistencePlan` now owns `persist(...)`
  - `LoadGenerationContextError` now owns `into_runtime_message(...)`
  - the runtime entrypoint now delegates persistence and context-error mapping
    directly through those owners instead of same-file free helper chains

  This keeps chapter content trimming, word-count calculation, draft-status
  persistence, updated-at stamping, context error message behavior, and the
  generate-and-persist runtime contract unchanged while removing one more
  same-file owner split from the Rust chapter generation runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the chapter generation runtime lane when the next
    move removes another true same-file persistence-plan / error-owner split
    or fake owner split
  - do not flatten context loading, prompt construction, provider execution,
    and persistence orchestration into one helper if the next step would only
    reshuffle ownership without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the batch task-view query lane and tightened the
  remaining same-file list/query response owner split in
  `chapter_batch_generation_task_view_query_service.rs`.

  A focused audit showed that:
  - `build_active_batch_generation_task_list_view_payload(...)` only wrapped a
    local list-view payload shape around already prepared item payloads
  - `build_active_batch_generation_query_response(...)` only wrapped an
    optional `BatchGenerationReadContext` into the active-task response shape
  - both helpers were same-file only and already had implicit owner shapes, so
    the read/query lane still kept a small fake owner split between view state
    and payload projection

  The refactor therefore tightened the batch task-view owners without
  changing behavior:
  - `ActiveBatchGenerationTaskListView` now owns the list-view payload through
    `into_payload()`
  - `ActiveBatchGenerationQueryView` now owns the active-task response payload
    through `into_payload()`
  - list/query read entrypoints now delegate directly through those view
    owners instead of same-file free payload helpers

  This keeps active-task query semantics, list payload totals, optional active
    task response shape, and read-context payload projection behavior
    unchanged while removing one more same-file owner split from the Rust
    batch task-view query seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch task-view query lane when the next move
    removes another true same-file list/query response owner split or fake
    owner split
  - do not flatten task query construction, read-context loading, project
    access checks, and payload projection into one helper if the next step
    would only reshuffle orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the chapter quality-metrics query lane and tightened the
  remaining same-file source/summary/payload owner split in
  `chapter_quality_metrics_query_service.rs`.

  A focused audit showed that:
  - `resolve_quality_metrics_source(...)` only expressed the source-selection
    semantics that already belong to `ResolvedQualityMetricsSource`
  - `build_quality_metrics_summary(...)` only reprojected the same resolved
    metrics into two summary variants that already belong to the quality
    source / fragment owners
  - `build_chapter_quality_metrics_payload(...)` only wrapped the local
    `ChapterQualityMetricsFragments` owner into the top-level response shape
  - all three helpers were same-file only, so the quality-metrics read-side
    lane still kept a small fake owner split after the surrounding chapter
    analysis and runtime seams had already been tightened

  The refactor therefore tightened the chapter quality-metrics owners without
  changing behavior:
  - `ResolvedQualityMetricsSource` now owns `resolve(...)`,
    `latest_history_metrics(...)`, `build_summary(...)`, and
    `formatted_generated_at()`
  - `ChapterAnalysisQualityFragments` now owns the analysis-side projection
    through `from_resolved_source(...)`
  - `ChapterQualityMetricsFragments` now owns the quality-metrics-side
    projection and top-level payload through `from_resolved_source(...)` and
    `into_payload()`
  - read entrypoints and focused tests now delegate directly through those
    owners instead of same-file free helper chains

  This keeps candidate-attempt priority, history fallback semantics,
  generated-at formatting, quality summary field exposure, and the chapter
  quality-metrics response shape unchanged while removing one more same-file
  owner split from the Rust quality-metrics query seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_quality_metrics_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the quality-metrics query lane when the next move
    removes another true same-file source/summary/payload owner split or fake
    owner split
  - do not flatten read-context loading, chapter access, and analysis-view
    composition into one helper if the next step would only reshuffle
    orchestration without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the chapter analysis-view query lane and tightened the
  remaining same-file checker/view-payload owner split in
  `chapter_analysis_view_query_service.rs`.

  A focused audit showed that:
  - checker-result parsing and checker-created-at projection already belonged
    to `ChapterAnalysisCheckerFragments`, but were still split across free
    helper chaining
  - the top-level analysis-view response shape already had a stable implicit
    owner, while `build_chapter_analysis_view_payload(...)` only wrapped
    chapter/analysis/memory/fragments into that one local payload projection
  - all of these helpers were same-file only, so the analysis-view read-side
    lane still kept a small fake owner split after the adjacent
    quality-metrics and draft-fragment seams had already been tightened

  The refactor therefore tightened the chapter analysis-view owners without
  changing behavior:
  - `ChapterAnalysisCheckerFragments` now owns checker parsing and
    checker-created-at projection through `from_histories(...)` and
    `parse_result(...)`
  - `ChapterAnalysisViewPayload` now owns the top-level analysis-view response
    shape through `into_payload()`
  - the query entrypoint and focused tests now delegate directly through those
    owners instead of same-file free helper chains

  This keeps checker-history filtering, checker-result extraction,
  checker-created-at fallback behavior, memory payload projection, draft /
  quality fragment exposure, and the chapter analysis-view response shape
  unchanged while removing one more same-file owner split from the Rust
  analysis-view query seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the analysis-view query lane when the next move
    removes another true same-file checker/view-payload owner split or fake
    owner split
  - do not flatten analysis loading, memory query, read-context loading, and
    cross-fragment orchestration into one helper if the next step would only
    reshuffle ownership without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved back to the batch write-workflow lane and tightened the
  remaining same-file task-plan / chapter-payload owner split in
  `chapter_batch_generation_write_workflow_service.rs`.

  A focused audit showed that:
  - create-task persistence semantics were already represented by
    `BatchGenerationCreatePlan`, but task creation still lived on the request
    owner through `create_task_plan(...)`
  - chapter list projection for the create response already belonged to the
    chapter-target owner, but was still rebuilt inline inside
    `PreparedBatchGenerationCreateRequest::create_response_payload(...)`
  - both helpers were same-file only, so the batch write-workflow lane still
    kept another small fake owner split after execution-config ownership had
    already been narrowed in earlier slices

  The refactor therefore tightened the batch write-workflow owners without
  changing behavior:
  - `BatchGenerationCreatePlan` now owns task creation and snapshot bootstrap
    through `create(...)`
  - `PreparedBatchGenerationCreateRequest` now exposes `total_chapters()` so
    the plan owner consumes the prepared request contract directly instead of
    recomputing counts ad hoc
  - `BatchGenerationCreateChapterTarget` now owns its response projection
    through `into_payload()`
  - the create workflow entrypoint now delegates task-plan persistence and
    chapter payload projection directly through those owners instead of
    same-file free helper chains

  This keeps batch create request defaults, chapter ordering, chapter-id
  payload shape, snapshot bootstrap behavior, estimated-time calculation, and
  the batch-generation create response shape unchanged while removing one more
  same-file owner split from the Rust batch write-workflow seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the batch write-workflow lane when the next move
    removes another true same-file plan/payload owner split or fake owner
    split
  - do not flatten project access, execution-config preparation, resume
    dispatch selection, and runtime launch orchestration into one helper if
    the next step would only reshuffle ownership without reducing boundary
    noise

- 2026-05-26 follow-up checkpoint:
  this slice moved to the single generation workflow lane and tightened the
  remaining same-file background-launch / initial-event owner split in
  `chapter_single_generation_workflow_service.rs`.

  A focused audit showed that:
  - background task launch semantics were already built from the prepared
    single-generation owner contract, but task active-model creation,
    checkpoint bootstrap, response payload projection, and runtime launch
    dispatch were still spread inline across the workflow entrypoint
  - initial SSE event semantics were already represented by
    `OwnedSingleGenerationInitialEvent`, but sequence construction and event
    application still lived in two free helper functions
  - all of these helpers were same-file only, so the single workflow lane
    still kept another small fake owner split after request preparation had
    already been narrowed in earlier slices

  The refactor therefore tightened the single generation workflow owners
  without changing behavior:
  - `PreparedSingleChapterGenerationRequest` now owns background launch-plan
    assembly through `into_background_launch_plan(...)`
  - `SingleGenerationBackgroundLaunchPlan` now owns task persistence and
    checkpoint bootstrap through `persist(...)`
  - `SingleGenerationBackgroundLaunchOutcome` now owns runtime dispatch plus
    final response handoff through `dispatch(...)`
  - `OwnedSingleGenerationInitialEvent` now owns both initial event sequence
    construction and tracker application through `sequence(...)` and `apply(...)`
  - the background workflow entrypoint and focused tests now delegate
    directly through those owners instead of same-file free helper chains

  This keeps single-generation background response fields, task active-model
  defaults, checkpoint bootstrap semantics, runtime launch contract, initial
  SSE progress behavior, and the single workflow response/stream shape
  unchanged while removing one more same-file owner split from the Rust single
  generation workflow seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the single generation workflow lane when the next
    move removes another true same-file background-launch / stream-event owner
    split or fake owner split
  - do not flatten request preparation, runtime execution, result projection,
    and stream orchestration into one helper if the next step would only
    reshuffle ownership without reducing boundary noise

- 2026-05-26 follow-up checkpoint:
  this slice moved forward into the single generation runtime lane and
  tightened the remaining same-file runtime-lifecycle owner split in
  `chapter_single_generation_runtime_state_service.rs`.

  A focused audit showed that:
  - runtime lifecycle semantics were still spread across four free helper
    functions: preparing state bootstrap, finalizing snapshot persistence,
    success finalization, and failure finalization
  - these helpers were all same-file only and already consumed the same
    `task_id/chapter_id/snapshot stage` contracts, so the runtime lane still
    kept another small fake owner split after mutation-plan ownership had
    already been narrowed earlier
  - `execute_single_generation_runtime(...)` therefore still mixed lifecycle
    state transitions with the main execution flow instead of delegating the
    state/result persistence boundary through one local owner

  The refactor therefore tightened the single generation runtime owners
  without changing behavior:
  - `SingleGenerationRuntimeOutcome` now owns lifecycle bootstrap through
    `prepare_runtime(...)`
  - `SingleGenerationRuntimeOutcome` now owns final result persistence
    through `persist(...)`
  - checkpoint writes for preparing / generating / finalizing / completed /
    failed states now flow through one owner-local
    `persist_checkpoint(...)` boundary
  - `execute_single_generation_runtime(...)` now keeps the main
    `prepare -> execute -> map outcome -> persist outcome` flow and no longer
    manually coordinates four separate same-file lifecycle helpers

  This keeps preparing/generating/finalizing/completed/failed checkpoint
  stages, task mutation semantics, completed-chapter updates, failure message
  persistence, and the single generation runtime execution contract unchanged
  while removing one more same-file owner split from the Rust single
  generation runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

  Updated stop-rule for this lane:
  - only keep shrinking the single generation runtime lane when the next move
    removes another true same-file lifecycle/result owner split or fake owner
    split
  - do not flatten runtime dispatch, AI execution, persistence result mapping,
    and task-state mutation into one helper if the next step would only
    reshuffle ownership without reducing boundary noise

- 2026-05-26 23:00 +08:00 checkpoint:
  this slice returned to the batch resume command/write lane and removed one
  more response-owner split that no longer carried compatibility value. A
  focused audit of
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the resume command owner already produced the full
  `ResumeBatchGenerationResponseState`, but the actual response payload
  projection still lived in the write-workflow file. In the same lane,
  `response_state()` / `reset_response_state()` were still attached to
  `ResumeBatchGenerationCommandState` even though the real owner of those
  shapes was the response-state object itself.

  The refactor therefore moved the response projection boundary back into the
  command owner:
  - `ResumeBatchGenerationResponseState` now owns
    `from_command_state(...)`, `reset_for_resume(...)`, and
    `into_response_payload()`
  - `prepare_batch_generation_resume(...)` now returns the reset response
    state through that response owner directly
  - `chapter_batch_generation_write_workflow_service.rs` now only consumes the
    prepared response payload and no longer owns the resume transport-payload
    shape

  This keeps batch-resume HTTP payload shape, checkpoint metadata shape,
  stage/execution-mode semantics, single-vs-batch resume reset behavior, and
  downstream runtime dispatch behavior unchanged while removing one more
  same-lane response contract split between command preparation and workflow
  orchestration. Stop-rule for this lane remains the same: continue only if a
  remaining helper still owns a real compatibility-sensitive contract, not if
  the next move would only repackage direct dispatch wiring.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:04 +08:00 checkpoint:
  this slice stayed on the same batch resume write-workflow lane and removed
  one more same-file dispatch shell that no longer carried a distinct contract.
  A focused audit of
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  showed that the resume path still split one local owner into two layers:
  - `ResumeExecutionPlan::into_runtime_launch_input(...)`
  - `ResumeGenerationExecutionInput::dispatch(...)`

  Those two steps were both same-file only, route-invisible, and already
  consumed the full explicit execution config and task id. The intermediate
  `ResumeGenerationExecutionInput` enum was therefore just a transport-free
  shell between one plan owner and the final runtime dispatch call.

  The refactor therefore tightened the resume dispatch boundary:
  - removed the intermediate `ResumeGenerationExecutionInput` enum entirely
  - `ResumeExecutionPlan` now owns direct runtime dispatch through
    `dispatch(db, task_id, execution_config)`
  - `resume_owned_batch_generation_write_workflow(...)` now keeps the simpler
    `prepare -> load config -> dispatch -> return response payload` flow

  This keeps batch-resume runtime branch selection, single-vs-batch dispatch
  behavior, explicit execution-config handoff, and external HTTP response
  shape unchanged while removing one more fake owner split from the Rust batch
  resume write-workflow seam. Updated stop-rule for this lane: continue only
  if the next move removes another true compatibility-sensitive owner split,
  not if it merely renames direct dispatch wiring or touches runtime internals.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:52 +08:00 checkpoint:
  this slice returned to the batch runtime resume lane and tightened one more
  same-file owner split inside
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`.
  A focused audit showed that
  `reset_batch_generation_task_for_resume(...)` still did two pieces of
  owner-local work after `ResumeBatchGenerationTaskResetPlan` had already been
  introduced:
  - it re-ran `task.resolve_runtime_semantics()` only to recover resume
    checkpoint inputs that were already implied by the reset decision
  - it still hand-built the resumed runtime checkpoint inline, so the reset
    plan only owned active-model reset fields while snapshot-reset semantics
    remained split across the outer function

  The refactor therefore narrowed the resume reset owner boundary:
  - `ResumeBatchGenerationTaskResetPlan` now also owns
    `include_progress_totals`
  - `ResumeBatchGenerationTaskResetPlan` now exposes
    `build_resume_checkpoint(total_chapters)` so the resumed checkpoint shape
    is produced by the same reset owner that decides current chapter position
    and progress reset semantics
  - `reset_batch_generation_task_for_resume(...)` now keeps the simpler
    `prepare reset plan -> apply mutation -> persist resumed snapshot` flow
    and no longer reparses runtime semantics or assembles the resume
    checkpoint inline
  - focused tests now verify the cleared batch resume snapshot through
    `reset_plan.build_resume_checkpoint(...)` instead of bypassing the new
    owner boundary

  This keeps single-vs-batch resume position semantics, pending checkpoint
  shape, progress reset behavior, and resume snapshot persistence behavior
  unchanged while removing one more fake owner split from the Rust batch
  runtime reset seam. Updated stop-rule for this lane: continue only if the
  next step removes another true owner split between reset-plan semantics and
  persisted runtime snapshot/task mutation behavior, not if it only repackages
  direct persistence calls.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-26 23:59 +08:00 checkpoint:
  this slice stayed on the same
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
  lane and removed another same-file runtime owner split between task-stage
  mutation and snapshot-stage persistence.

  A focused audit showed that the runtime file had already separated
  task-state mutation semantics into `BatchGenerationTaskStage`, but the outer
  runtime entrypoints still hand-owned the snapshot side of the same stage
  transition:
  - `prepare_batch_generation_runtime(...)`
  - `finalize_batch_generation_cancelled(...)`
  - `mark_batch_generation_chapter_started(...)`
  - `finalize_batch_generation_success(...)`
  - `finalize_batch_generation_failure(...)`

  Each of those functions still repeated the same second half:
  - choose the matching `BatchGenerationSnapshotStage`
  - call `build_batch_generation_runtime_checkpoint_for_stage(...)`
  - call `upsert_batch_generation_runtime_snapshot(...)`

  The refactor therefore narrowed snapshot ownership inside the runtime lane:
  - `BatchGenerationSnapshotStage` now owns
    `persist_for_task(db, task_id, current_chapter_id, current_chapter_number, completed_chapters, total_chapters)`
  - the five runtime lifecycle entrypoints now keep the simpler
    `persist task mutation -> persist matching snapshot stage` flow
  - those entrypoints no longer hand-build the checkpoint payload inline after
    the task-stage owner has already established the runtime transition

  This keeps queued/preparing/started/succeeded/cancelled/failed snapshot
  payload shapes, progress semantics, checkpoint stage vocabulary, and runtime
  persistence behavior unchanged while removing one more fake owner split from
  the batch runtime seam. Updated stop-rule for this lane: continue only if
  the next move removes another real same-file ownership split between runtime
  lifecycle stage choice and persistence behavior, not if it only wraps direct
  helper forwarding.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:03 +08:00 checkpoint:
  this slice left the runtime-state lane before diminishing returns and moved
  to the adjacent resume response / payload-base lane. A focused audit showed
  that
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  still hand-owned part of the runtime payload boundary even after response
  state ownership had already been narrowed:
  - `ResumeBatchGenerationResponseState::into_response_payload()` still
    resolved `stage_code` and `execution_mode` itself
  - it still called `checkpoint_with_runtime_metadata(...)` directly and
    patched `chapter_id` inline
  - it still called `BatchGenerationTaskRuntimePayloadBase::new(...)` with
    fully prepared runtime payload parts, so the payload-base owner was not
    yet the single source of truth for "runtime parts -> compat payload"

  The refactor therefore narrowed the runtime payload assembly boundary:
  - `BatchGenerationTaskRuntimePayloadBase` now owns
    `from_runtime_parts(...)`
  - this helper resolves `stage_code` / `execution_mode`, applies optional
    checkpoint overrides, and builds the runtime payload base from explicit
    business fields
  - `ResumeBatchGenerationResponseState::into_response_payload()` now only
    passes the resume business fields plus the `chapter_id` checkpoint
    override, instead of manually assembling runtime metadata first
  - focused tests in `chapter_batch_generation_task_payload_base_service.rs`
    now verify the new owner path directly

  This keeps batch-resume HTTP payload shape, pending stage semantics,
  execution-mode literals, checkpoint override behavior, and outer
  `message/total_chapters/completed_chapters` compat fields unchanged while
  removing one more same-lane payload owner split between the resume command
  owner and the shared payload-base owner. Updated stop-rule for this lane:
  continue only if the next step removes another real compatibility-sensitive
  split between explicit task/runtime fields and payload-base assembly, not if
  it only renames direct JSON field insertion.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:07 +08:00 checkpoint:
  this slice stayed on the same payload-base/read-context lane and narrowed
  the shared task-view payload boundary between
  `backend-rs/src/services/chapter_batch_generation_read_context_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_task_payload_base_service.rs`.

  A focused audit showed that `BatchGenerationReadContext::shared_task_view_payload()`
  still hand-owned the common view projection after runtime payload-base
  ownership had already been tightened:
  - it called `BatchGenerationTaskRuntimePayloadBase::from_task_state(...)`
    and then manually inserted the repeated view fields
    `total/completed/current_chapter_number/started_at/completed_at/error_message`
  - both `into_status_task_payload()` and `into_active_task_payload()` depended
    on that local projection shell, so the shared read-context owner still
    contained one more duplicate "task view base" contract instead of reusing
    one payload-base owner

  The refactor therefore narrowed task-view payload ownership:
  - `BatchGenerationTaskRuntimePayloadBase` now owns
    `from_task_view_state(task, workflow_runtime_state)`
  - this helper extends the shared runtime payload with the common task-view
    fields once, in the payload-base lane
  - `BatchGenerationReadContext::shared_task_view_payload()` now delegates to
    that owner directly instead of rebuilding the same common view fields inline
  - focused tests in the payload-base owner now verify the shared task-view
    projection directly, while existing read-context and task-view query tests
    continue to confirm the status/active query shapes stay stable

  This keeps active/status task response fields, checkpoint metadata,
  started/completed timestamps, current chapter numbering, and read-side
  terminal semantics unchanged while removing one more same-lane view payload
  split between read-context assembly and the shared payload-base owner.
  Updated stop-rule for this lane: continue only if the next step removes
  another real compatibility-sensitive split between shared task view fields
  and payload-base projection, not if it only wraps direct payload insertion.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:11 +08:00 checkpoint:
  this slice moved back to the batch resume lane and removed one more semantic
  owner split between
  `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
  and
  `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`.

  A focused audit showed that both files were still re-explaining the same
  "resume reset" contract from the same command state:
  - `ResumeBatchGenerationResponseState::reset_for_resume(...)` re-derived the
    reset status/current chapter/completed-chapter behavior for single vs batch
  - `ResumeBatchGenerationTaskResetPlan::from_command_state(...)` separately
    re-derived the reset runtime position and progress-total semantics for the
    same single-vs-batch resume decision
  - the actual compatibility-sensitive owner of that branching logic was still
    the resume semantics lane, but the reset semantics themselves were split
    across two downstream consumers

  The refactor therefore narrowed shared reset ownership:
  - `ResumeBatchGenerationCommandState` now owns
    `resolve_reset_semantics()`
  - the new `ResumeResetSemantics` contract centralizes:
    `status/current_chapter_id/current_chapter_number/include_progress_totals/completed_chapters`
  - `ResumeBatchGenerationResponseState::reset_for_resume(...)` now consumes
    that shared reset contract instead of manually branching on task kind
  - `ResumeBatchGenerationTaskResetPlan::from_command_state(...)` now also
    consumes the same reset contract instead of separately re-reading runtime
    semantics and hardcoding reset counters

  This keeps single-chapter resume position preservation, batch resume pointer
  clearing, malformed-single fallback behavior, pending reset status, and
  resumed checkpoint progress semantics unchanged while removing one more
  compatibility-sensitive semantic split between resume response assembly and
  runtime reset persistence. Updated stop-rule for this lane: continue only if
  the next step removes another real shared semantic split across resume
  consumers, not if it only repackages already-shared data without reducing
  downstream interpretation.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:17 +08:00 checkpoint:
  this slice stayed on the quality/read-context lane and finished removing a
  now-redundant compatibility shell after terminal-status ownership had
  already moved into
  `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`.

  A focused audit showed that
  `BatchGenerationTerminalStatusContext` had already become the real owner of
  terminal read-side semantics, but the old `terminal_semantics(...) -> tuple`
  wrapper still remained:
  - `BatchGenerationReadContext` no longer consumed the tuple wrapper after the
    previous slice
  - only tests still referenced it, so the old helper had become a dead
    compatibility shell around the new owner
  - `cargo check` had started surfacing this as an unused function, which was
    a concrete sign that the old owner split was no longer carrying value

  The refactor therefore completed that seam cleanup:
  - removed the obsolete `terminal_semantics(...)` wrapper entirely
  - updated the remaining quality-status test coverage to assert directly
    through `BatchGenerationTerminalStatusContext::from_task(...)`
  - preserved `manual_review_label(...)` and terminal payload insertion
    through the explicit shared owner only

  This keeps manual-review labels, completed/cancelled terminal labels,
  review-required semantics, and resume eligibility semantics unchanged while
  removing one more dead compatibility shell from the Rust batch read-side
  semantics lane. Updated stop-rule for this lane: continue only if the next
  step removes another still-live owner split across quality/read-context/
  stream consumers, not if it just deletes already-isolated helpers without
  collapsing a real shared contract.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:24 +08:00 checkpoint:
  this slice moved to the adjacent stream/status lane and removed another
  still-live owner split between status semantics resolution and terminal SSE
  event assembly.

  A focused audit showed that
  `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
  still explained the same status value through several parallel helpers:
  - `default_stream_progress(status)`
  - `default_stream_message(status)`
  - `map_batch_generation_stream_event_status(status)`
  - `batch_generation_stream_terminal_kind(status)`
  - `resolve_batch_generation_stream_semantics(...)`

  At the same time,
  `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
  still reinterpreted terminal semantics inside
  `BatchGenerationStreamState::terminal_events()` by branching again on
  completed/failed/cancelled.

  The refactor therefore narrowed stream-status ownership in two connected
  steps:
  - introduced one internal resolved-status owner in the stream semantics
    lane, so `pending/running/completed/failed/cancelled/unknown` now resolve
    default progress, default message, event-status literal, and terminal-kind
    through a single contract before
    `BatchGenerationStreamSemantics` is built
  - moved terminal SSE event assembly onto
    `BatchGenerationStreamTerminalKind::build_events(...)`, so the event layer
    now consumes the parsed terminal kind instead of re-deriving one more
    completed/failed/cancelled branch from stream state
  - updated focused tests to assert the new status owner contract directly
    while keeping existing progress/result/error/done event assertions intact

  This keeps SSE payload shapes, progress fallback behavior, completed/failed/
  cancelled terminal event semantics, and stream close behavior unchanged
  while removing one more compatibility-sensitive split across stream status
  interpretation and terminal event assembly. Updated stop-rule for this lane:
  continue only if the next step removes another real shared contract across
  stream cursor/state/event consumers, not if it only repackages already-owned
  terminal payload literals.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:28 +08:00 checkpoint:
  this slice switched to the adjacent single-generation write/runtime lane and
  removed one more duplicated handoff contract between prepared request
  ownership and runtime launch ownership.

  A focused audit showed that
  `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
  still explained the same `prepared request -> runtime launch input` contract
  in two separate places:
  - `create_single_generation_stream_workflow(...)` directly called
    `SingleGenerationRuntimeLaunchInput::new(user_id, prepared.execution_input)`
  - `SingleGenerationBackgroundLaunchPlan::persist(...)` re-created the same
    runtime launch input again after persistence, even though the execution
    input was already owned by `PreparedSingleChapterGenerationRequest`

  The refactor therefore narrowed that write/runtime handoff boundary:
  - `PreparedSingleChapterGenerationRequest` now owns
    `into_runtime_launch_input(user_id)`
  - the stream workflow now delegates runtime launch construction to that
    prepared-request owner instead of open-coding it in the workflow entrypoint
  - the background launch plan now stores `runtime_input` directly, so the
    persistence step no longer reassembles the same launch contract after task
    creation
  - focused tests now assert the prepared owner path directly and confirm the
    background launch plan still carries the same runtime launch payload

  This keeps single-generation background response payloads, stream launch
  behavior, persisted task/snapshot semantics, and runtime dispatch input
  contents unchanged while removing one more same-lane owner split between
  request preparation and runtime launch handoff. Updated stop-rule for this
  lane: continue only if the next step removes another real duplicated
  write/runtime handoff or task-launch contract, not if it only shortens local
  forwarding helpers without collapsing shared semantics.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:32 +08:00 checkpoint:
  this slice returned to the batch create write lane and removed one more
  duplicated owner split between prepared response assembly and runtime
  dispatch handoff.

  A focused audit showed that
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  still stitched together the batch-create result in the top-level workflow:
  - `PreparedBatchGenerationCreateRequest` already owned the create response
    payload contract through `create_response_payload(...)`
  - `BatchGenerationCreatePlan` already owned the runtime dispatch contract
    through `dispatch_runtime(...)`
  - but `start_owned_batch_generation_write_workflow(...)` still manually
    combined those two owners by separately building `response_payload`,
    dispatching the runtime plan, and returning the outer value itself

  The refactor therefore narrowed that create outcome boundary:
  - introduced `BatchGenerationCreateOutcome` as the explicit owner of
    `response_payload + runtime_plan`
  - `PreparedBatchGenerationCreateRequest` now owns
    `into_create_outcome(plan)`, so the prepared request lane produces the
    compatibility response plus runtime handoff bundle in one place
  - `BatchGenerationCreateOutcome::dispatch(...)` now owns
    `dispatch runtime -> return response payload`, so the top-level workflow no
    longer re-stitches that same handoff inline
  - focused tests now assert the prepared create-outcome owner directly

  This keeps batch-create HTTP payload shape, estimated-time semantics,
  chapter-list ordering, created task id wiring, and runtime dispatch behavior
  unchanged while removing one more same-lane split between prepared payload
  ownership and runtime launch handoff. Updated stop-rule for this lane:
  continue only if the next step removes another real duplicated
  create/resume dispatch or response/outcome contract, not if it only wraps a
  single direct call without collapsing shared semantics.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:35 +08:00 checkpoint:
  this slice stayed on the same batch write lane and removed one more
  duplicated owner split from the resume path between domain outcome handling
  and runtime dispatch handoff.

  A focused audit showed that
  `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
  still hand-owned the resume workflow stitching even though the domain layer
  had already produced the two meaningful owners:
  - `prepare_batch_generation_resume(...)` already returned
    `ResumeBatchGenerationOutcome { execution, response_state }`
  - `ResumeExecutionPlan` already owned the runtime dispatch contract through
    its `dispatch(...)` method
  - but `resume_owned_batch_generation_write_workflow(...)` still manually
    unpacked `execution` and `response_state`, built the outer response payload
    itself, and then separately called dispatch

  The refactor therefore narrowed that resume outcome boundary:
  - introduced `ResumeBatchGenerationWriteOutcome` as the explicit owner of
    `response_payload + execution + task_id`
  - `ResumeBatchGenerationWriteOutcome::from_domain_outcome(...)` now owns the
    domain-outcome -> write-workflow outcome conversion, including
    `response_state.into_response_payload()`
  - `ResumeBatchGenerationWriteOutcome::dispatch(...)` now owns
    `dispatch execution -> return response payload`, so the top-level resume
    workflow no longer re-stitches the same handoff inline
  - focused tests now assert the resume write outcome owner directly

  This keeps batch-resume HTTP payload shape, created task id wiring, single
  vs batch execution dispatch behavior, and runtime resume semantics unchanged
  while removing one more same-lane split between domain outcome handling and
  runtime dispatch handoff. Updated stop-rule for this lane: continue only if
  the next step removes another real duplicated resume/create response or
  execution-handoff contract, not if it only wraps already-isolated direct
  calls.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:38 +08:00 checkpoint:
  this slice left the now-near-saturated write-workflow envelope lane and
  returned to the adjacent resume semantics lane to remove another still-live
  shared semantic split across execution and runtime consumers.

  A focused audit showed that
  `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
  still interpreted the same single-vs-batch task kind in two downstream
  passes:
  - `resolve_runtime_semantics()` first matched a local intermediate
    `ResumeBatchGenerationSemantics`
  - `resolve_execution_target()` then matched that same intermediate again to
    derive the execution branch
  - both consumers were reading the same parsed task-kind decision, but the
    owner still exposed the semantic in two separate projection paths instead
    of one shared resolved contract

  The refactor therefore narrowed that resume semantic boundary:
  - introduced `ResumeResolvedTaskSemantics` as the shared owner of
    `execution_target + runtime_semantics`
  - `resolve_task_semantics()` now performs the single/batch interpretation
    once and returns both consumer-facing projections together
  - `resolve_runtime_semantics()` and `resolve_execution_target()` now delegate
    to that shared resolved contract instead of each re-matching the same
    branch shape
  - focused tests now assert the shared resolved-task owner directly, while
    existing resume-task-command and runtime-state tests continue to verify
    the external resume/reset behavior remains stable

  This keeps malformed-single fallback behavior, single-chapter resume
  position preservation, batch progress-total reset semantics, and execution
  target selection unchanged while removing one more compatibility-sensitive
  split across the resume semantics lane. Updated stop-rule for this lane:
  continue only if the next step removes another real shared semantic split
  between resume execution/reset/response consumers, not if it only renames
  already-isolated projection helpers.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:42 +08:00 checkpoint:
  this slice stayed on the same resume semantics lane and removed one more
  split between parsing an execution target and validating whether that target
  is actually resumable.

  A focused audit showed that the resume flow still split one semantic
  contract across two files:
  - `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
    already owned `resolve_execution_target()` as the parsed single-vs-batch
    result
  - `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    then re-owned the next step by checking whether the parsed target was
    actually resumable (`Some(chapter_id)` / non-empty `chapter_ids`) before
    building `ResumeExecutionPlan`
  - this meant the legality of the execution target still lived outside the
    semantics owner even though it was part of the same single-vs-batch target
    interpretation contract

  The refactor therefore narrowed that execution-target boundary:
  - introduced `ResumeExecutionSelection` as the "already validated, resumable
    target" owner
  - `ResumeBatchGenerationCommandState::resolve_execution_selection()` now
    turns `ResumeExecutionTarget` into either a valid `SingleChapter` /
    `Batch` selection or `None`
  - `into_resume_execution_plan(...)` now consumes that validated selection
    instead of re-checking `Option<String>` and empty chapter lists itself
  - focused tests now assert the new semantics owner directly, while existing
    command/runtime tests continue to confirm malformed-single fallback and
    reset behavior remain unchanged

  This keeps `NoChaptersToResume` behavior, malformed-single batch fallback,
  single-chapter resume dispatch, and batch resume dispatch semantics
  unchanged while removing one more shared semantic split across the resume
  lane. Updated stop-rule for this lane: continue only if the next step
  removes another still-live shared contract between response state,
  execution plan, and reset plan consumers, not if it only renames already
  isolated helper shapes.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:47 +08:00 checkpoint:
  this slice stayed on the same resume lane and finished collapsing the
  remaining hardcoded reset fields into the shared reset contract.

  A focused audit showed that `ResumeResetSemantics` still owned only part of
  the actual "resume reset" boundary:
  - `backend-rs/src/services/chapter_batch_generation_resume_semantics_service.rs`
    already owned reset `status/current_chapter/current_chapter_number/
    include_progress_totals/completed_chapters`
  - but `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    still hardcoded the remaining reset fields locally as
    `failed_chapters = []` and `current_retry_count = 0`
  - this left one real reset contract split across the semantics owner and the
    runtime reset-plan owner

  The refactor therefore completed that shared reset boundary:
  - extended `ResumeResetSemantics` to also own
    `failed_chapters` and `current_retry_count`
  - `resolve_reset_semantics()` now returns the full reset contract, not just
    the position/progress subset
  - `ResumeBatchGenerationTaskResetPlan::from_command_state(...)` now consumes
    those reset fields directly from the shared owner instead of hardcoding
    them again in the runtime lane
  - focused tests now assert the additional reset fields through the shared
    semantics owner while existing runtime-state tests continue to verify the
    persisted reset behavior remains unchanged

  This keeps batch/single resume pointer clearing, retry reset behavior,
  failed-chapter clearing, pending status semantics, and resumed checkpoint
  behavior unchanged while removing one more still-live shared reset split.
  Updated stop-rule for this lane: continue only if the next step removes
  another real shared contract across response-state, execution-plan, and
  reset-plan consumers; otherwise leave the resume lane and switch back to the
  next highest-signal seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:52 +08:00 checkpoint:
  this slice switched out of the nearly-saturated resume lane and returned to
  the single-generation workflow lane to remove one still-live shared
  stream-terminal contract.

  A focused audit of the single-generation workflow showed that the stream
  path still split one real ownership boundary:
  - `backend-rs/src/services/chapter_single_generation_workflow_service.rs`
    already owned the initial SSE sequence through
    `OwnedSingleGenerationInitialEvent`
  - but the terminal branch still lived as an inline `match` inside
    `launch_owned_single_chapter_generation_stream(...)`, where success
    manually emitted `complete -> result -> done` and failure manually emitted
    `error`
  - this meant the stream terminal contract still lived in workflow control
    flow instead of a single owner type, unlike the batch stream/status lanes
    already refactored earlier

  The refactor therefore narrowed that workflow/stream boundary:
  - introduced `OwnedSingleGenerationStreamTerminalEvent` as the owner of
    success-vs-failure terminal semantics
  - `from_generation_result(...)` now converts runtime output into the owned
    terminal contract in one place
  - `emit(...)` now owns the actual SSE terminal sequence:
    success emits `complete -> result -> done`, failure emits `error`
  - `launch_owned_single_chapter_generation_stream(...)` now only runs the
    shared initial sequence and delegates terminal emission to the new owner
  - added focused tests that assert both success and failure terminal
    contract shapes directly

  This keeps single-generation stream startup, progress semantics, response
  payload shape, final `done` event ordering, and error emission behavior
  unchanged while removing another still-live workflow-layer contract split.
  Updated next-step guidance for this lane: continue only if the next change
  removes another real shared handoff across prepared request, runtime launch,
  stream launch, or initial/terminal event ownership; otherwise switch to the
  next highest-signal seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 00:56 +08:00 checkpoint:
  this slice stayed adjacent to the single-generation lane and removed one
  remaining route-local request ownership split without touching runtime or
  workflow behavior.

  A focused audit showed that the single-chapter request contract was still
  split across the transport boundary:
  - `backend-rs/src/api/chapter_batch_generation.rs` still owned the
    `SingleChapterGenerationRouteRequest -> SingleChapterGenerationRequest`
    conversion through a local `into_request()` helper
  - the actual owned request type already belonged to
    `backend-rs/src/services/chapter_single_generation_prepare_service.rs`
  - this left a small but still-live route-local request assembly contract in
    the API layer, even though the route only needed to forward the standard
    domain request into the existing background/stream workflows

  The refactor therefore narrowed that transport/preparation boundary:
  - introduced
    `build_single_chapter_generation_request_from_route_payload(...)` in
    `chapter_single_generation_prepare_service.rs`
  - `SingleChapterGenerationRouteRequest::into_request()` now delegates to the
    service-owned builder instead of constructing the request shape directly
  - added a focused prepare-service test that asserts the route-payload ->
    request contract at the owner boundary
  - existing API tests continue to assert the route payload shape and behavior
    remain unchanged

  This keeps single-chapter background generation, single-chapter stream
  generation, ignored compat-only route fields (`enable_analysis`), and error
  mapping semantics unchanged while removing one more route-local owner split.
  Updated next-step guidance for this lane: it is now close to saturation, so
  stay here only if the next slice removes another real shared contract across
  prepare/workflow/runtime boundaries; otherwise switch to the next
  highest-signal batch/runtime seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:00 +08:00 checkpoint:
  this slice switched back to the higher-signal batch/runtime lane and
  narrowed one still-live polling/close contract in the batch status stream
  reader.

  A focused audit showed that the batch status stream still split one control
  boundary across the polling loop and event owner:
  - `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
    already owned stream event batches and terminal close semantics once a
    `BatchGenerationStreamState` existed
  - but `backend-rs/src/services/chapter_batch_generation_status_stream_service.rs`
    still owned a parallel control-flow contract for
    `initial pending state -> loaded state -> not found close`
    directly inside the polling loop
  - this left the stream reader with an inline ownership split for “what
    state do we continue with vs when do we emit a close error and return”
    even though the surrounding stream layer had already been moving toward
    explicit owner types

  The refactor therefore narrowed that polling boundary:
  - introduced `BatchGenerationStreamPollResolution` as the owner of
    `Continue { state }` vs `Close { event }`
  - `from_loaded_state(...)` now owns the initial-state preference and
    missing-state close decision in one place
  - the polling loop now only asks the owner for the next resolution, emits
    a close event when required, and otherwise continues with the resolved
    stream state
  - added focused tests that assert both
    `pending initial state -> continue` and
    `missing loaded state -> 404 close` behavior directly

  This keeps batch status SSE payloads, cursor/event batching, terminal
  event ordering, and timeout behavior unchanged while removing one more
  shared stream-reader control split. Updated next-step guidance for this
  lane: continue here if the next slice removes another real split across
  stream polling, read-context loading, or runtime-state ownership; otherwise
  switch to the next highest-signal runtime-write seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:05 +08:00 checkpoint:
  this slice stayed on the higher-signal batch/runtime lane and narrowed one
  still-live per-chapter runtime outcome contract inside the batch execution
  loop.

  A focused audit showed that the batch runtime loop still owned too much of
  the per-step outcome interpretation inline:
  - `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    already had separate owners for preparing, started, success, cancelled,
    and failed persistence/write boundaries
  - but `execute_batch_generation_runtime(...)` still directly interpreted the
    full chain of
    `task gate -> cancelled -> chapter lookup missing/load error ->
    generation error -> success`
    through several local `match` / `if` branches
  - this left the runtime loop as a mixed owner of both orchestration and the
    shared “what kind of step outcome is this, and what failure metadata does
    it carry” contract

  The refactor therefore narrowed that runtime-step boundary:
  - introduced `BatchGenerationRuntimeStepOutcome` as the owner of
    `Continue`, `Succeeded`, `Cancelled`, and `Failed`
  - `resolve_next_step(...)` now owns the task gate, cancelled short-circuit,
    and chapter lookup result mapping
  - `resolve_generation_result(...)` now owns success-vs-generation-failure
    mapping, including the final failure metadata contract
  - `execute_batch_generation_runtime(...)` now mostly orchestrates:
    resolve step -> optionally persist started -> run generation ->
    consume resolved outcome through the existing finalize helpers
  - added a focused runtime-state test that asserts
    generation-result -> runtime-step-outcome mapping directly

  This keeps batch runtime dispatch, chapter-start persistence, success/fail
  checkpoint writes, cancel short-circuit behavior, and failure-kind semantics
  unchanged while removing one more shared runtime-write control split from
  the main loop. Updated next-step guidance for this lane: continue here if
  the next slice removes another real contract between runtime step
  resolution, finalize persistence, and task/snapshot writes; otherwise switch
  to the next highest-signal query/read-side seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:09 +08:00 checkpoint:
  this slice moved to the read-side query lane and narrowed one still-live
  active-task query projection contract shared by list and project-active
  reads.

  A focused audit showed that the active batch query layer still duplicated
  one read-side projection boundary:
  - `backend-rs/src/services/chapter_batch_generation_task_view_query_service.rs`
    already owned the query envelopes for
    `total/items` and `has_active_task/task`
  - but both
    `load_active_user_batch_generation_task_view_contexts(...)` and
    `load_active_project_batch_generation_task_view_context(...)`
    still independently owned the same
    `task query result -> BatchGenerationReadContext projection`
    process
  - this left the task-view query layer with two parallel owners for the same
    “convert queried tasks into read contexts / maybe first context” contract

  The refactor therefore narrowed that query-projection boundary:
  - introduced `ActiveBatchGenerationReadQueryOutcome` as the owner of the
    queried-task -> read-context projection result
  - `from_tasks(...)` now owns the shared task-to-context loading loop in one
    place
  - list reads now consume `into_contexts()`
  - project-active reads now consume `into_optional_context()`
  - added focused tests that assert both
    `contexts -> optional first context` and
    `empty contexts -> None`
    behavior directly at the new owner boundary

  This keeps active-task query filtering, payload envelope shapes, project
  access checks, and read-context payload semantics unchanged while removing
  one more read-side projection split from the query layer. Updated next-step
  guidance for this lane: continue here only if the next slice removes
  another real shared contract across read-context payload assembly, query
  projection, or stream/state views; otherwise switch back to the next
  highest-signal remaining seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:12 +08:00 checkpoint:
  this slice stayed on the read-side lane and narrowed one still-live shared
  task-view payload assembly contract inside the batch read-context owner.

  A focused audit showed that `BatchGenerationReadContext` still split one
  real view-assembly boundary across two parallel payload methods:
  - `into_status_task_payload(...)` and `into_active_task_payload(...)` both
    owned the same base task-view payload plus quality-context insertion
  - only the status view then layered on retry / failed-chapters /
    terminal-context fields
  - this left the read-context owner with duplicated assembly flow for the
    common “base payload + quality overlay” contract, with only a small
    mode-specific delta

  The refactor therefore narrowed that view-assembly boundary:
  - introduced `BatchGenerationTaskViewPayloadKind` as the owner of
    active-vs-status payload mode
  - added `into_task_view_payload(kind)` so the shared
    `base payload + quality overlay` assembly now lives in one place
  - status payload now only contributes its mode-specific fields
    (`current_retry_count`, `max_retries`, `failed_chapters`,
    terminal-context)
  - active payload now delegates through the same owner without reassembling
    the shared parts separately
  - added focused tests that assert both status-mode and active-mode payload
    behavior directly at the new shared owner boundary

  This keeps status payload fields, active payload fields, quality overlays,
  stream-state construction, and terminal semantics unchanged while removing
  one more duplicated read-side assembly split from `BatchGenerationReadContext`.
  Updated next-step guidance for this lane: continue here only if the next
  slice removes another real contract across read-context loading, stream
  state construction, or stream event resolution; otherwise switch to the
  next highest-signal remaining seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch payload-base lane, removed one more one-hop
  runtime-payload owner, and then stopped again before the lane degraded into
  local field forwarding only.

  A focused audit showed that runtime payload assembly still crossed an
  unnecessary intermediate owner:
  - `build_batch_generation_task_runtime_payload_from_task(...)` only forwarded
    the task fields into the lower-level runtime payload constructor
  - `build_batch_generation_task_runtime_payload_from_task_state(...)` was the
    only production caller of that helper
  - the temporary task-based wrapper had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the task-state payload builder itself, not the
    temporary task-based wrapper

  The refactor therefore narrowed that batch payload-base boundary:
  - removed `build_batch_generation_task_runtime_payload_from_task`
  - `build_batch_generation_task_runtime_payload_from_task_state(...)` now
    directly constructs the runtime payload from the task fields and checkpoint
    data
  - the related unit test now asserts the direct runtime-payload path instead
    of the deleted helper

  This keeps runtime payload fields, checkpoint metadata, stage code, and
  execution-mode semantics unchanged while removing one more duplicate owner
  from the batch payload-base lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch task-view-query lane, flattened one more
  one-hop query flow, and then stopped again before the lane degraded into
  local JSON assembly reshuffling only.

  A focused audit showed that active-task query flow still carried unnecessary
  temporary one-hop values:
  - the active-task list path first accumulated read contexts, then immediately
    remapped them into active payload items
  - the project-scoped active-task query first wrapped the loaded read context
    in an `Option`, then immediately unpacked it into the final response
  - these intermediate values had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the query flow itself, not those temporary
    locals

  The refactor therefore narrowed that task-view-query boundary:
  - `load_active_user_batch_generation_task_list_view(...)` now accumulates the
    query results directly into the final payload item vector path
  - `load_active_batch_generation_query(...)` now directly maps the optional
    task model into the final response payload without a second temporary
    context wrapper

  This keeps active-task query filtering, ordering, payload shape, and access
  semantics unchanged while removing one more duplicate owner from the batch
  task-view-query lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch read-context lane again, removed one more
  one-hop task-view payload owner, and then stopped again before the lane
  degraded into local field reshuffling only.

  A focused audit showed that read-context payload building still crossed an
  unnecessary intermediate owner:
  - `BatchGenerationReadContext::build_task_view_payload()` and
    `into_task_view_payload()` only wrapped the common task-view projection
    used by the public status/active payload methods
  - production code immediately consumed that wrapper inside the public
    projection methods
  - the temporary wrapper had no independent transport contract or lifecycle
    beyond one-hop grouping
  - the real owner boundary was the public read-context projection methods
    themselves, not the temporary task-view payload wrapper

  The refactor therefore narrowed that batch read-context boundary:
  - removed `BatchGenerationReadContext::build_task_view_payload`
  - removed `BatchGenerationReadContext::into_task_view_payload`
  - `into_status_task_payload()` now directly builds the task-view payload and
    attaches terminal/status-only fields
  - `into_active_task_payload()` now directly builds the task-view payload and
    returns it as the active envelope

  This keeps read-side payload content, quality-status merging, terminal field
  injection, and stream-state projection unchanged while removing one more
  duplicate owner from the batch read-context lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch cancel lane again, removed one more
  one-hop cancel-response owner, and then stopped again before the lane
  degraded into local payload-string reshuffling only.

  A focused audit showed that cancel response assembly still crossed an
  unnecessary intermediate owner:
  - `build_cancel_batch_generation_response_payload(...)` only wrapped the
    shared summary payload builder with the fixed cancel message
  - `cancel_owned_batch_generation_task(...)` was the only production consumer
    of that wrapper
  - the temporary response wrapper had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the cancel workflow itself, not the temporary
    response wrapper

  The refactor therefore narrowed that batch cancel boundary:
  - removed `build_cancel_batch_generation_response_payload`
  - `cancel_owned_batch_generation_task(...)` now calls the shared summary
    payload builder directly with the cancel message
  - cancel-service tests were updated to assert the direct summary payload
    builder path instead of the deleted wrapper

  This keeps cancel validation, terminal-status rejection, runtime
  finalization, response shape, and shared summary semantics unchanged while
  removing one more duplicate owner from the batch cancel lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch cancel lane, removed one more one-hop
  progress-summary owner, and then stopped again before the lane degraded into
  local payload constructor reshuffling only.

  A focused audit showed that cancel response assembly still crossed an
  unnecessary intermediate owner:
  - `BatchGenerationCommandProgressSummary::from_task(...)` only converted a
    batch task model into the shared progress summary used by cancel response
    payloads
  - `cancel_batch_generation_task(...)` was the only production consumer of
    that helper
  - the temporary projection helper had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the cancel task function itself, not the
    temporary summary helper

  The refactor therefore narrowed that batch cancel boundary:
  - removed `BatchGenerationCommandProgressSummary::from_task`
  - `cancel_batch_generation_task(...)` now directly constructs the shared
    summary from the task fields
  - `chapter_batch_generation_cancel_service.rs` tests were updated to build
    the summary directly where needed

  This keeps cancel validation, terminal-status rejection, runtime
  finalization, response shape, and shared progress-summary semantics unchanged
  while removing one more duplicate owner from the batch cancel lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch resume lane, removed one more one-hop
  execution-plan owner, and then stopped again before the lane degraded into
  local test-only reshuffling only.

  A focused audit showed that resume preparation still crossed an unnecessary
  intermediate owner:
  - `ResumeBatchGenerationCommandState::into_resume_execution_plan(...)` only
    converted the resolved single/batch selection plus normalized target word
    count into a resume execution plan
  - `prepare_batch_generation_resume(...)` was the only production consumer of
    that helper
  - the temporary execution-plan helper had no independent transport contract
    or lifecycle beyond one-hop grouping
  - the real owner boundary was the preparation function itself, not the
    temporary execution-plan helper

  The refactor therefore narrowed that batch resume boundary:
  - removed `ResumeBatchGenerationCommandState::into_resume_execution_plan`
  - `prepare_batch_generation_resume(...)` now directly resolves the execution
    selection and constructs the resume execution plan inline
  - the related tests were updated to validate the direct plan construction
    path instead of the deleted helper

  This keeps resume validation, target-word normalization, manual-review
  blocking, execution selection, and reset behavior unchanged while removing
  one more duplicate owner from the batch resume lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:16 +08:00 checkpoint:
  this slice started with a fast stop-rule check on the stream-event lane,
  found that it was near saturation, and then switched immediately to a
  still-live read-context lookup seam with higher signal.

  A focused audit showed that `chapter_batch_generation_read_context_service.rs`
  still duplicated one owned-task lookup contract:
  - `load_owned_batch_generation_read_context(...)` and
    `load_required_owned_batch_generation_read_context(...)`
    both owned the same
    `owned task lookup -> read context load`
    path
  - the only real difference was whether a missing owned task should become
    `None` or `LoadOwnedBatchGenerationTaskError::TaskNotFound`
  - this left the read-context owner with two parallel loaders for the same
    underlying lookup/result contract

  The refactor therefore narrowed that lookup boundary:
  - introduced `OwnedBatchGenerationReadContextLoad` as the owner of
    `Found(context)` vs `Missing`
  - `OwnedBatchGenerationReadContextLoad::load(...)` now owns the shared
    `owned task -> read context` loading path in one place
  - optional read-context lookup now consumes `into_optional_context()`
  - required read-context lookup now consumes `into_required_context()`
    to translate `Missing` into the existing `TaskNotFound` error contract
  - added focused tests that assert both
    `Found -> optional context` and
    `Missing -> required TaskNotFound`
    behavior directly at the new owner boundary

  This keeps owned-task access behavior, read-context contents, status/active
  payload semantics, stream-state construction, and required-vs-optional
  route behavior unchanged while removing one more duplicated lookup split
  from the read-context lane. Updated next-step guidance for this lane:
  continue only if another real contract remains across read-context loading,
  stream-state construction, or downstream event resolution; otherwise leave
  this lane and switch to the next highest-signal remaining seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:21 +08:00 checkpoint:
  this slice re-ran the narrow stop-rule on the
  `status_stream_service + read_context_service` pair, found one remaining
  shared stream-state load contract, and narrowed it immediately.

  A focused audit showed that the status stream still split one
  read-context-to-stream-state boundary across two paths:
  - the polling path loaded optional owned read context and then mapped it to
    `context.into_stream_state()`
  - the initial stream bootstrap path loaded required owned read context and
    then mapped it to `into_stream_state()` separately
  - this left `chapter_batch_generation_status_stream_service.rs` with two
    parallel owners for the same
    `owned read context -> stream state`
    contract, differing only by optional-vs-required result semantics

  The refactor therefore narrowed that stream-state load boundary:
  - introduced `OwnedBatchGenerationStreamStateLoad` as the owner of
    `Found(state)` vs `Missing`
  - `load_optional(...)` now owns the shared optional
    `owned read context -> stream state` path for polling
  - `load_required(...)` now owns the required
    `owned read context -> stream state` path for initial stream bootstrap
  - polling now consumes `into_optional_state()`
  - the initial stream loader now consumes the shared required owner instead
    of rebuilding the mapping locally
  - added a focused test that asserts
    `Found(state) -> optional stream state`
    directly at the new owner boundary

  This keeps status-stream polling semantics, required stream bootstrap
  behavior, not-found close handling, event batching, and timeout behavior
  unchanged while removing one more duplicated stream-state load split from
  the read/stream lane. Updated stop-rule for this area: this lane is now
  very close to saturation, and the next step should leave it unless another
  clearly shared non-trivial contract is still visible.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:28 +08:00 checkpoint:
  this slice left the now near-saturated read/stream lane and moved back to a
  higher-signal batch command/payload contract shared by cancel and resume
  responses.

  A focused audit showed that batch command responses still duplicated one
  real outer summary contract:
  - `backend-rs/src/services/chapter_batch_generation_cancel_service.rs`
    owned a local response payload for
    `batch_id + total_chapters + completed_chapters + message`
  - `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    also rebuilt the same outer summary fields inline after assembling the
    runtime payload base
  - this left two separate owners for the same command-summary envelope,
    differing only by the message literal and whether the resume path first
    added runtime fields

  The refactor therefore narrowed that command-summary boundary:
  - introduced `BatchGenerationCommandSummaryPayload` in
    `chapter_batch_generation_task_payload_base_service.rs` as the owner of
    the shared outer response contract
  - the new owner now supports both `into_payload()` for summary-only command
    responses and `extend_payload(...)` for commands like resume that first
    build a richer runtime payload
  - cancel now emits its response by consuming that shared summary owner once
  - resume now reuses the same summary owner to layer
    `batch_id / total_chapters / completed_chapters / message`
    onto the existing runtime payload, instead of inserting those fields
    inline
  - added focused tests in the payload-base service that assert both
    standalone summary payload construction and extension of an existing
    payload map

  This keeps cancel payload shape, resume payload shape, runtime checkpoint
  fields, task-type/status semantics, and message literals unchanged while
  removing one more shared response-envelope split from the batch
  command/write lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across batch cancel/resume/write payload ownership; otherwise move to the
  next highest-signal batch runtime/write seam such as
  `chapter_batch_generation_write_workflow_service.rs` or adjacent dispatch
  handoff owners.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:34 +08:00 checkpoint:
  this slice stayed on the higher-signal batch write/runtime lane and narrowed
  one still-live workflow handoff contract shared by create and resume.

  A focused audit showed that the batch write workflow still owned two
  parallel outcome wrappers for the same handoff shape:
  - `BatchGenerationCreateOutcome` owned
    `response_payload + runtime_plan -> dispatch -> return payload`
  - `ResumeBatchGenerationWriteOutcome` owned
    `response_payload + execution/task_id -> dispatch -> return payload`
  - both wrappers represented the same workflow-level contract:
    the write owner prepares a response payload, carries a dispatch plan,
    launches runtime, and returns the payload unchanged
  - this left `chapter_batch_generation_write_workflow_service.rs` with
    duplicated owner structure across create and resume, even though the only
    real variation was the dispatch plan kind

  The refactor therefore narrowed that workflow handoff boundary:
  - introduced `BatchGenerationWriteWorkflowOutcome` as the shared owner of
    `response_payload + dispatch_plan`
  - introduced `BatchGenerationWriteDispatchPlan` with explicit
    `Create { user_id, runtime_plan }` and
    `Resume { task_id, execution }` variants
  - create now produces the shared workflow outcome through
    `PreparedBatchGenerationCreateRequest::into_create_outcome(...)`
  - resume now produces the same shared workflow outcome through
    `BatchGenerationWriteWorkflowOutcome::from_resume_domain_outcome(...)`
  - runtime launch now happens once through the shared
    `dispatch_plan.dispatch(...)` boundary instead of being maintained across
    two separate wrapper types
  - updated focused tests so they assert the new shared dispatch-plan owner
    directly for both create and resume paths

  This keeps create payload shape, resume payload shape, runtime dispatch
  targets, execution-config handoff, and single-vs-batch resume behavior
  unchanged while removing one more duplicated write/runtime owner split from
  the batch workflow lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across batch write payload creation, dispatch-plan assembly, or execution
  config handoff; otherwise move to the next highest-signal batch runtime or
  payload seam adjacent to `chapter_batch_generation_write_workflow_service.rs`.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:38 +08:00 checkpoint:
  this slice stayed on the same batch write/runtime lane and narrowed one
  deeper runtime-launch contract still split beneath the workflow handoff.

  A focused audit showed that even after unifying the workflow outcome, the
  actual runtime launch mapping was still owned by two parallel boundaries:
  - create still mapped its dispatch request through
    `BatchGenerationCreatePlan::dispatch_runtime(...)`
  - resume still mapped its dispatch request through
    `ResumeExecutionPlan::dispatch(...)`
  - both paths represented the same underlying contract:
    write-layer dispatch request + prepared execution config should resolve to
    one explicit runtime launch plan, which is then dispatched once
  - this left the write/runtime lane with one more owner split below the
    shared workflow outcome

  The refactor therefore narrowed that runtime-launch boundary:
  - introduced `BatchGenerationRuntimeLaunchPlan` with explicit
    `Batch { task_id, execution_input }` and
    `SingleChapter { task_id, launch_input }` variants
  - renamed the workflow handoff owner to
    `BatchGenerationWriteDispatchRequest`, making it explicit that this is a
    pre-launch request shape rather than the final runtime plan
  - `BatchGenerationWriteDispatchRequest` now resolves once into
    `into_runtime_launch_plan(execution_config)`
  - create now maps through
    `BatchGenerationCreatePlan::into_runtime_launch_plan(...)`
  - resume now maps through
    `ResumeExecutionPlan::into_runtime_launch_plan(...)`
  - runtime dispatch now happens once at
    `BatchGenerationRuntimeLaunchPlan::dispatch(db)`, so create and resume no
    longer maintain separate runtime-start owner paths
  - updated focused tests to assert the renamed shared dispatch-request owner
    for both create and resume outcomes

  This keeps create payload shape, resume payload shape, execution-config
  semantics, single-vs-batch resume dispatch behavior, and background runtime
  launch targets unchanged while removing one more duplicated runtime-start
  split from the batch write/runtime lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across create payload assembly, chapter-target projection, or execution
  config preparation; otherwise move to the next highest-signal batch payload
  or command seam adjacent to this workflow owner.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:41 +08:00 checkpoint:
  this slice stayed on the same write/runtime lane and narrowed one remaining
  create-request projection contract inside the batch create owner.

  A focused audit showed that `PreparedBatchGenerationCreateRequest` still
  owned three parallel projections over the same `chapters_to_generate` data:
  - `chapter_ids()`
  - `chapter_id_payload()`
  - `chapters_to_generate_payload()`
  - all three were derived from the same chapter-target list, but each path
    rebuilt its own view independently
  - this left the create-request owner with a small but real payload/projection
    split that could drift if chapter-target shape changed again

  The refactor therefore narrowed that create-projection boundary:
  - introduced `BatchGenerationCreateChapterProjection` as the owner of
    `chapter_ids + chapter_id_payload + chapter_payloads`
  - `from_targets(...)` now derives those projections once from the prepared
    chapter target list
  - `PreparedBatchGenerationCreateRequest::chapter_projection()` now provides
    the shared projection owner
  - `chapter_ids()`, `chapter_id_payload()`, and
    `chapters_to_generate_payload()` now all delegate through that owner
    instead of each rebuilding their own projection path
  - added a focused test that asserts the shared projection owner keeps
    `ids / id-payload / chapter-payloads` aligned

  This keeps create response payload shape, persisted chapter-id payload
  shape, create-plan chapter ordering, and chapter-target response content
  unchanged while removing one more repeated projection split from the batch
  create workflow lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across create request preparation, response payload assembly, or create-plan
  persistence handoff; otherwise switch to the next highest-signal batch
  command/payload seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:49 +08:00 checkpoint:
  this slice intentionally left the near-saturated batch write/runtime lane
  and switched back to the adjacent batch command/payload seam for a more
  meaningful shared contract.

  A focused stop-rule check showed that `chapter_batch_generation_write_workflow_service.rs`
  was now close to diminishing returns:
  - the remaining edits in that lane were trending toward thin wrapping rather
    than true ownership reduction
  - meanwhile the command/payload lane still had one real split:
    cancel and resume both combined the same
    `batch_id + total_chapters + completed_chapters`
    progress contract with different outer message/payload shapes, but the
    progress summary itself still had no dedicated owner

  The refactor therefore switched lanes and narrowed that command-progress
  boundary:
  - introduced `BatchGenerationCommandProgressSummary` in
    `chapter_batch_generation_task_payload_base_service.rs` as the owner of
    `batch_id / total_chapters / completed_chapters`
  - `BatchGenerationCommandSummaryPayload` now builds from
    `from_progress_summary(...)` instead of owning both progress fields and
    message assembly from scratch
  - `CancelBatchGenerationResponseState` now holds the shared progress summary
    instead of duplicating the three progress fields directly
  - `ResumeBatchGenerationResponseState` now also holds the shared progress
    summary and updates that owner during reset-for-resume rather than
    separately mutating a raw completed count field
  - resume runtime payload assembly now reads the batch id from the shared
    progress owner while summary/message layering still happens through the
    summary payload owner
  - updated focused tests across cancel, resume, payload-base, and
    write-workflow fixtures so they assert the new progress-summary owner
    without changing the external response payload contract

  This keeps cancel response payloads, resume response payloads, batch ids,
  chapter totals, completed counts, runtime checkpoint payloads, and
  write-workflow resume fixtures unchanged while removing one more duplicated
  task-progress summary split from the batch command/payload lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across cancel/resume command state, progress summary, or response payload
  assembly; otherwise run a fresh stop-rule and switch to the next
  highest-signal adjacent seam.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 01:54 +08:00 checkpoint:
  this slice stayed in the batch command/payload lane for one final narrow
  audit and found one remaining shared upstream projection worth narrowing
  before the lane reaches saturation.

  A focused audit showed that after introducing
  `BatchGenerationCommandProgressSummary`, the summary owner itself was still
  being populated through duplicate upstream projections:
  - cancel still built progress fields manually from the task in local code
  - resume still built progress fields manually from command state
  - the new progress-summary owner existed, but its task-side source
    projection was not yet owned in one place
  - this was still a real contract because `task -> batch_id/total/completed`
    is a reusable projection boundary, not just message formatting

  The refactor therefore narrowed that upstream progress-summary boundary:
  - added `BatchGenerationCommandProgressSummary::from_task(...)` so the
    shared owner can now be derived directly from the task model once
  - `CancelBatchGenerationResponseState::from_task(...)` now consumes that
    shared projection owner instead of rebuilding the three fields locally
  - cancel workflow now validates through the command-state path but returns
    the response payload from the task-derived summary owner, making the
    task-side projection a true production path rather than a parallel test
    helper
  - after this change, `CancelBatchGenerationResponseState::from_command_state(...)`
    remains only as residual test-facing structure, which is a strong
    stop-rule signal that this lane is close to saturation

  This keeps cancel response payload shape, resume payload shape, persisted
  cancel semantics, command validation behavior, and task-progress values
  unchanged while removing one more duplicated task-to-summary projection
  split from the batch command/payload lane.

  Updated next-step guidance for this lane:
  this lane is now close to saturation. Unless another clearly shared
  non-trivial contract is visible immediately, the next step should switch to
  the next adjacent high-signal seam instead of continuing to thin-wrap the
  remaining helpers here.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 02:20 +08:00 checkpoint:
  this slice switched out of the saturated command/payload lane and returned
  to the adjacent resume semantics/runtime-state seam for one small
  compatibility-safe ownership cleanup.

  A focused audit showed that the batch resume path still carried one real
  split owner for the same reset contract:
  - `chapter_batch_generation_resume_semantics_service.rs` already owned
    `resolve_reset_semantics()` with the reset-time pointer, totals, failed
    chapter reset, and retry reset semantics
  - but `chapter_batch_generation_runtime_state_service.rs` still rebuilt a
    parallel `ResumeBatchGenerationTaskResetPlan` wrapper only to pass the same
    fields forward into resumed checkpoint construction
  - this meant `include_progress_totals`, current chapter pointer, and reset
    progress values were still split across two local owners before reaching
    `BatchGenerationSnapshotStage::Resumed`

  The refactor therefore narrowed that resume-reset boundary:
  - added `ResumeResetSemantics::build_resume_checkpoint(...)` in
    `chapter_batch_generation_resume_semantics_service.rs` so the same owner
    now carries both reset semantics and resumed checkpoint assembly
  - removed the parallel `ResumeBatchGenerationTaskResetPlan` wrapper from
    `chapter_batch_generation_runtime_state_service.rs`
  - runtime-state resume reset now consumes
    `ResumeBatchGenerationCommandState::resolve_reset_semantics()` directly for
    both task mutation and snapshot replacement
  - added focused tests in the resume-semantics service for single/batch
    resumed checkpoint payload construction, and updated runtime-state tests to
    assert the runtime path still respects that shared reset-semantic owner

  This keeps batch resume payload shape, single-chapter resume pointer
  behavior, batch resume pointer clearing, resumed checkpoint totals behavior,
  failed-chapter reset, and retry reset unchanged while removing one more
  duplicate owner split from the resume semantics/runtime-state lane.

  Updated next-step guidance for this lane:
  this lane is now again near a stop-rule point. Continue here only if another
  concrete multi-consumer resume contract is visible immediately; otherwise
  switch to the next highest-signal write/workflow seam instead of reopening
  snapshot/write internals.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 02:33 +08:00 checkpoint:
  this slice returned to the batch write/workflow lane and narrowed one more
  still-live handoff owner between workflow outcome assembly and runtime
  dispatch.

  A focused audit showed that `chapter_batch_generation_write_workflow_service.rs`
  already had a unified outer workflow outcome, but it still kept an extra
  parallel dispatch-request owner:
  - create first built `BatchGenerationWriteDispatchRequest::Create {
    user_id, runtime_plan }`
  - resume first built `BatchGenerationWriteDispatchRequest::Resume {
    task_id, execution }`
  - both branches then immediately converted that intermediate wrapper into the
    same `BatchGenerationRuntimeLaunchPlan`
  - this left one thin but still real duplicated handoff contract between the
    workflow owner and the runtime launch owner

  The refactor therefore narrowed that workflow-to-runtime handoff boundary:
  - `BatchGenerationWriteWorkflowOutcome` now owns the final
    `BatchGenerationRuntimeLaunchPlan` directly instead of a separate
    `BatchGenerationWriteDispatchRequest`
  - removed the now-redundant `BatchGenerationWriteDispatchRequest` enum and
    its conversion layer
  - create now resolves its launch plan while building the workflow outcome
  - resume now also resolves its launch plan while building the workflow
    outcome, so dispatch is reduced to `launch_plan.dispatch(db)`
  - focused tests were updated to assert the new direct launch-plan owner for
    both create and resume outcome construction

  This keeps batch create response payloads, batch resume response payloads,
  launch behavior, execution config ownership, single-chapter resume fallback,
  and batch runtime dispatch semantics unchanged while removing one more
  intermediate owner from the batch write/workflow lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared non-trivial contract remains
  across create/resume workflow preparation or runtime launch assembly;
  otherwise switch again instead of flattening the remaining code into
  low-signal cleanup.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 02:43 +08:00 checkpoint:
  this slice left the write/workflow lane before it turned into thin cleanup
  and took one narrow command-response projection seam in the adjacent resume
  command lane.

  A focused audit showed that `chapter_batch_generation_resume_task_command_service.rs`
  still kept two local owners for the same base response-state projection:
  - `from_command_state(...)` already mapped the shared
    `command_state -> summary/project/status/current_chapter/task_kind`
    contract
  - `reset_for_resume(...)` then rebuilt part of that same projection before
    overriding only the reset-specific fields
  - the real difference between the two branches was only the injected
    summary and the reset-time `status/current_chapter_id`

  The refactor therefore narrowed that response-state projection boundary:
  - added `from_command_state_with_summary(...)` as the shared owner of the
    base `command_state -> response_state` projection
  - `from_command_state(...)` now delegates to that shared owner
  - `reset_for_resume(...)` now also delegates to the same owner and only
    applies the true reset-specific field overrides afterward
  - focused tests now assert that the shared projection owner can be reused
    with an injected summary without changing the returned response payload
    contract

  This keeps resume response payload shape, batch id, project id, task type,
  checkpoint fields, reset status semantics, and reset completed-count behavior
  unchanged while removing one more duplicate response-state projection split
  from the resume command lane.

  Updated next-step guidance for this lane:
  this resume command lane is now again close to a stop-rule point. Continue
  here only if another multi-consumer semantic owner is immediately visible;
  otherwise switch back to a higher-signal lane rather than polishing local
  wrappers.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 02:52 +08:00 checkpoint:
  this slice switched to the read/query lane and removed one remaining
  mid-layer wrapper from the active-task read path before it turned into
  broader view cleanup.

  A focused audit showed that `chapter_batch_generation_task_view_query_service.rs`
  still kept a dedicated `ActiveBatchGenerationReadQueryOutcome` owner only to
  hold and immediately unwrap the same `Vec<BatchGenerationReadContext>`:
  - task-list loading used it only to recover `Vec<context>`
  - project-active loading used it only to recover `Option<context>`
  - the true shared contract was already just `tasks -> read contexts`, while
    the extra outcome wrapper did not own any distinct payload semantics

  The refactor therefore narrowed that read-side assembly boundary:
  - removed `ActiveBatchGenerationReadQueryOutcome`
  - introduced `load_batch_generation_read_contexts_for_tasks(...)` as the
    direct shared owner of `Vec<task> -> Vec<BatchGenerationReadContext>`
  - task-list and project-active read paths now both consume that same helper
    directly and apply only their real final projection step (`Vec<context>` or
    `.next()`)
  - focused tests were updated to assert the simplified optional-context path
    without changing active-query or task-list payload contracts

  This keeps active task list payloads, active project query payloads, status
  task payloads, quality-field projection, and access error mapping unchanged
  while removing one more no-op wrapper from the read/query lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared read-side contract remains
  across context loading or final payload projection; otherwise switch again
  rather than flattening the rest of the read path into low-signal cleanup.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 03:02 +08:00 checkpoint:
  this slice stayed in the adjacent read-context lane long enough to remove
  one more no-op ownership wrapper, then stopped again before drifting into
  broad payload cleanup.

  A focused audit showed that `chapter_batch_generation_read_context_service.rs`
  still kept `OwnedBatchGenerationReadContextLoad` as a dedicated wrapper for
  only two states:
  - `Found(BatchGenerationReadContext)`
  - `Missing`
  - both public callers immediately converted that wrapper back into either
    `Option<BatchGenerationReadContext>` or
    `LoadOwnedBatchGenerationTaskError::TaskNotFound`
  - the wrapper therefore did not own any distinct read-context semantics; it
    only re-encoded `Option<context>` as another enum

  The refactor therefore narrowed that owned-read boundary:
  - removed `OwnedBatchGenerationReadContextLoad`
  - added `load_owned_batch_generation_read_context_inner(...)` as the direct
    shared owner of `owned task -> Option<BatchGenerationReadContext>`
  - `load_owned_batch_generation_read_context(...)` now returns that optional
    context directly
  - `load_required_owned_batch_generation_read_context(...)` now maps the same
    optional result straight to `TaskNotFound` without the intermediate wrapper
  - focused tests were updated to assert the simplified optional/required
    conversion path while preserving the same owned-read behavior

  This keeps owned-task lookup behavior, read-context payload construction,
  stream-state projection, snapshot loading, and required/missing error mapping
  unchanged while removing one more no-op wrapper from the owned read-context
  lane.

  Updated next-step guidance for this lane:
  this read-context lane is again near a stop-rule point. Continue here only if
  another shared semantic owner is clearly duplicated; otherwise switch back to
  a higher-signal write or command lane instead of flattening remaining local
  structure.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 03:12 +08:00 checkpoint:
  this slice switched back to the command lane and cleaned up one remaining
  duplicate cancel-response projection before the lane hit another stop-rule.

  A focused audit showed that `chapter_batch_generation_cancel_service.rs`
  still carried two parallel owners for the same response-state summary
  projection:
  - `from_task(...)` already produced the production response state from the
    shared `BatchGenerationCommandProgressSummary::from_task(...)`
  - `from_command_state(...)` still rebuilt the same summary fields from
    `batch_id / total_chapters / completed_chapters`
  - both branches then fed the same `into_response_payload()` path, so the
    true shared contract was the progress summary itself, not two separate
    response-state constructors

  The refactor therefore narrowed that cancel response-state boundary:
  - added `from_progress_summary(...)` as the shared response-state owner
  - `from_task(...)` now delegates to that owner through
    `BatchGenerationCommandProgressSummary::from_task(...)`
  - test-only `from_command_state(...)` now also delegates to the same shared
    summary owner instead of rebuilding fields inline
  - focused tests now assert the new shared progress-summary path directly

  This keeps cancel response payload shape, cancel command validation, task
  progress values, and runtime cancellation behavior unchanged while removing
  one more duplicate summary projection from the command lane.

  Updated next-step guidance for this lane:
  the cancel/command lane is again close to saturation. Continue here only if a
  clearly shared non-trivial owner remains immediately visible; otherwise
  switch back to a higher-signal write/runtime seam instead of polishing local
  constructors.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 03:36 +08:00 checkpoint:
  this slice moved back to the single-generation write/runtime lane and
  removed one remaining duplicated handoff field before the lane drifted into
  thin wrapper cleanup.

  A focused audit showed that the single-generation prepared/runtime boundary
  still carried the same `chapter_id` in two parallel owners:
  - `PreparedSingleChapterGenerationRequest.chapter_target.chapter_id` already
    owned the target chapter identity for background task creation and
    checkpoint assembly
  - `SingleChapterGenerationExecutionInput.chapter_id` duplicated that same
    identity only so downstream runtime launch could read it again
  - the true runtime handoff contract was therefore:
    `chapter target identity + execution config`, not two separate owners each
    holding the same `chapter_id`

  The refactor therefore narrowed that handoff boundary:
  - removed `chapter_id` from `SingleChapterGenerationExecutionInput`
  - moved explicit chapter identity ownership to
    `SingleGenerationRuntimeLaunchInput.chapter_id`
  - `PreparedSingleChapterGenerationRequest::into_runtime_launch_input(...)`
    now derives the launch input from the prepared chapter target instead of
    relying on a duplicated nested field
  - `into_background_launch_plan(...)` now builds the runtime launch input
    from the same chapter-target owner used for task/checkpoint assembly
  - batch resume's `ResumeExecutionPlan::SingleChapter` path was updated to
    pass the chapter identity at the outer runtime-launch boundary as well
  - focused tests were updated across single prepare/workflow/runtime and
    batch write workflow to assert the new owner split

  This keeps single-generation request payloads, background task creation,
  runtime execution behavior, checkpoint persistence, stream launch behavior,
  and batch-resume single-chapter dispatch unchanged while removing one more
  duplicated field from the single-generation runtime handoff contract.

  Updated next-step guidance for this lane:
  this single-generation lane is now closer to a stop-rule point. Continue
  here only if another clearly shared write/runtime contract is still owned in
  parallel across prepare/workflow/runtime boundaries; otherwise switch back
  to a higher-signal shared runtime/workflow seam instead of flattening local
  launch/result wrappers.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 04:02 +08:00 checkpoint:
  this slice switched back to the batch create write/runtime lane and removed
  one remaining duplicated execution-handoff contract before the lane drifted
  into wrapper-only cleanup.

  A focused audit showed that the batch create path still kept
  `chapter_ids/target_word_count` in two parallel owners:
  - `PreparedBatchGenerationCreateRequest` already owned the ordered chapter
    projection and normalized target word count used for response payload
    assembly and task creation
  - `BatchGenerationCreatePlan` copied those same values again only so the
    create workflow could rebuild runtime launch input after persistence
  - the true post-persistence contract for the create plan was therefore only
    the newly created `task_id`, while the chapter projection and execution
    config still belonged to the prepared request owner

  The refactor therefore narrowed that create handoff boundary:
  - removed `chapter_ids` and `target_word_count` from
    `BatchGenerationCreatePlan`
  - `BatchGenerationCreatePlan` now only carries `created_task_id`
  - `PreparedBatchGenerationCreateRequest::into_create_outcome(...)` now uses
    the prepared request owner to build both the create response payload and
    the final batch runtime launch plan
  - added a workflow-local `PreparedBatchGenerationCreateRequest::into_runtime_launch_plan(...)`
    owner so the ordered chapter ids plus normalized target word count are
    assembled once from the prepared request instead of being re-copied through
    the persisted plan layer
  - focused tests were updated to assert the narrower create-plan contract

  This keeps batch create response payloads, chapter ordering, normalized word
  count behavior, persistence semantics, and runtime dispatch behavior
  unchanged while removing one more duplicated execution contract from the
  batch create workflow lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared workflow/runtime contract still
  spans create vs resume or single vs batch launch ownership. If the next move
  is only renaming or flattening local wrapper types, switch lanes again
  instead of forcing more low-signal cleanup.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 04:18 +08:00 checkpoint:
  this slice moved into the chapter-analysis write/runtime lane and removed
  one remaining duplicated task-create response contract before that lane
  drifted into routine wrapper cleanup.

  A focused audit showed that chapter-analysis task creation still rebuilt the
  same `task_id/chapter_id -> pending payload` contract in multiple places:
  - `build_chapter_analysis_task_create_payload(...)` owned the raw create
    payload shape
  - `PreparedChapterAnalysisTrigger` separately held the same `task_id` and
    `chapter_id` fields only so the trigger workflow could immediately rebuild
    that same payload again before dispatch
  - `enqueue_chapter_analysis_task(...)` also created the same response
    contract from separate `task.id + chapter_id` inputs
  - the true shared contract was therefore the task-create response state
    itself, not a loose helper plus duplicated field pairs

  The refactor therefore narrowed that analysis create boundary:
  - introduced `ChapterAnalysisTaskCreateState` as the shared owner of
    `task_id + chapter_id + pending response payload`
  - `PreparedChapterAnalysisTrigger` now carries that shared create state
    instead of separate `task_id/chapter_id` fields
  - trigger workflow response assembly now goes through
    `PreparedChapterAnalysisTrigger::create_payload()`
  - dispatch unpacks the same shared create state when launching the
    background analysis task
  - `enqueue_chapter_analysis_task(...)` now also returns the shared create
    state payload instead of calling a parallel raw helper
  - focused tests were updated to assert the new create-state owner directly

  This keeps chapter-analysis task-create payloads, trigger error mapping,
  background dispatch behavior, and enqueue response semantics unchanged while
  removing one more duplicated response contract from the analysis workflow
  lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared write/runtime contract still
  spans trigger preparation, response assembly, and dispatch ownership. If the
  next move is only flattening local structs without reducing duplicated
  semantics, switch back to a higher-signal seam instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 04:31 +08:00 checkpoint:
  this slice returned to the resume/write-workflow lane and removed one more
  duplicated task-identity owner before the lane drifted into thin wrapper
  cleanup.

  A focused audit showed that resume workflow output still carried the same
  task identity in two parallel owners:
  - `ResumeBatchGenerationOutcome.task_id` held the resumed task id directly
  - `ResumeBatchGenerationResponseState.summary.batch_id()` already carried the
    same task identity as part of the shared progress-summary owner
  - `BatchGenerationWriteWorkflowOutcome::from_resume_domain_outcome(...)`
    only needed one source of truth to build the runtime launch plan task id,
    so the extra top-level `task_id` field no longer owned distinct semantics

  The refactor therefore narrowed that resume handoff boundary:
  - removed top-level `task_id` from `ResumeBatchGenerationOutcome`
  - `prepare_batch_generation_resume(...)` now returns only
    `execution + response_state`
  - resume write workflow now derives the runtime launch `task_id` from
    `response_state.summary.batch_id()` instead of consuming a parallel outcome
    field
  - focused tests were updated to assert the narrower resume outcome contract

  This keeps resume response payloads, resume execution selection, task reset
  semantics, and runtime launch behavior unchanged while removing one more
  duplicate task-identity owner from the resume workflow lane.

  Updated next-step guidance for this lane:
  the resume lane is now again close to a stop-rule point. Continue here only
  if another clearly shared workflow/runtime contract remains duplicated across
  execution selection, response state, and launch ownership. If the next move
  is only flattening local wrappers, switch lanes again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 04:42 +08:00 checkpoint:
  this slice stayed in the chapter-analysis task-create lane long enough to
  remove one more duplicated create-state projection, then stopped again
  before it turned into routine local cleanup.

  A focused audit showed that chapter-analysis task creation still split the
  same `analysis_task::Model -> ChapterAnalysisTaskCreateState` mapping across
  two outer callers:
  - `create_chapter_analysis_task(...)` created and returned the inserted task
    model
  - `prepare_chapter_analysis_trigger(...)` immediately rebuilt
    `ChapterAnalysisTaskCreateState` from `task.id + chapter_id`
  - `enqueue_chapter_analysis_task(...)` repeated the same projection again
    from the same inserted task id plus chapter id
  - the true shared post-insert contract was therefore the create-state owner
    itself, not the raw task model

  The refactor therefore narrowed that task-create boundary:
  - `create_chapter_analysis_task(...)` now returns
    `ChapterAnalysisTaskCreateState` directly instead of `analysis_task::Model`
  - the task-create owner now maps inserted task id plus chapter id into the
    shared create state once, immediately after persistence
  - `prepare_chapter_analysis_trigger(...)` now consumes that create state
    directly instead of rebuilding it
  - `enqueue_chapter_analysis_task(...)` now also returns the same create
    state payload without a second local projection step
  - this required only a local visibility adjustment and import cleanup; no
    route or dispatch behavior changed

  This keeps chapter-analysis task persistence, create payload shape, trigger
  preparation, enqueue response semantics, and background dispatch behavior
  unchanged while removing one more duplicated post-insert mapping from the
  analysis task-create lane.

  Updated next-step guidance for this lane:
  this task-create lane is now close to a stop-rule point. Continue here only
  if another clearly shared contract still spans task creation, trigger
  preparation, and dispatch ownership. If the next move is only flattening
  local structs, switch lanes again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 04:54 +08:00 checkpoint:
  this slice stayed one more step in the chapter-analysis dispatch lane and
  removed another duplicated handoff split before that lane hit the stop-rule
  again.

  A focused audit showed that chapter-analysis background dispatch still
  unpacked and re-expanded the same create-state owner immediately after
  preparation:
  - `PreparedChapterAnalysisTrigger` already owned
    `ChapterAnalysisTaskCreateState`
  - `dispatch_prepared_chapter_analysis_trigger(...)` destructured that shared
    state back into raw `task_id/chapter_id`
  - `execute_chapter_analysis_background(...)` then accepted those raw fields
    as separate parameters, recreating a second local contract from the same
    owner
  - the true dispatch handoff was therefore still the create-state owner
    itself, not the two loose strings

  The refactor therefore narrowed that dispatch boundary:
  - `dispatch_prepared_chapter_analysis_trigger(...)` now forwards
    `ChapterAnalysisTaskCreateState` directly into background execution
  - `execute_chapter_analysis_background(...)` now accepts the shared create
    state and reads `task_id/chapter_id` from that owner inside the runtime
    body instead of at the dispatch boundary
  - a temporary visibility warning was resolved by keeping the background
    executor module-private, so no new public contract was introduced

  This keeps chapter-analysis trigger payloads, background dispatch behavior,
  task-state transitions, and analysis persistence unchanged while removing one
  more duplicated handoff split from the analysis dispatch lane.

  Updated next-step guidance for this lane:
  the analysis dispatch lane is now close to saturation. Continue here only if
  another clearly shared runtime handoff still spans trigger preparation and
  execution ownership. If the next move is only flattening local flow without
  removing a duplicated contract, switch lanes again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 05:12 +08:00 checkpoint:
  this slice returned to the single-generation background launch lane, removed
  one more duplicated runtime handoff owner, and then stopped again before the
  lane turned into thin local cleanup.

  A focused audit showed that single background launch still carried the same
  post-persist handoff across two adjacent owners:
  - `SingleGenerationBackgroundLaunchPlan::persist(...)` inserted the task,
    persisted the pending checkpoint, and built the final response payload
  - `SingleGenerationBackgroundLaunchOutcome` then held only
    `response_payload + task_id + runtime_input`
  - `SingleGenerationBackgroundLaunchOutcome::dispatch(...)` immediately
    forwarded `task_id + runtime_input` into
    `dispatch_single_chapter_generation_runtime(...)` and returned the same
    response payload unchanged
  - the true owner boundary was therefore still the launch plan itself, not a
    one-hop outcome wrapper

  The refactor therefore narrowed that background-launch boundary:
  - removed `SingleGenerationBackgroundLaunchOutcome`
  - `SingleGenerationBackgroundLaunchPlan` now owns the full
    `persist + runtime dispatch + response payload` handoff through
    `persist_and_dispatch(...)`
  - `start_owned_single_generation_background_workflow(...)` now consumes the
    final response payload directly from the launch owner instead of staging a
    second post-persist outcome contract

  This keeps single-chapter background task creation, pending checkpoint
  persistence, response payload shape, and runtime dispatch behavior unchanged
  while removing one more duplicate `task_id/runtime_input` owner from the
  single-generation launch lane.

  Updated next-step guidance for this lane:
  the single-generation background-launch lane is now again close to a
  stop-rule point. Continue here only if another clearly shared owner still
  spans persisted task creation and runtime launch semantics. If the next move
  is only flattening local flow or pass-through wrappers, switch to a new
  runtime/write seam instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 05:34 +08:00 checkpoint:
  this slice moved into the batch-generation write/runtime lane, removed one
  more duplicated post-workflow owner, and then stopped again before the file
  devolved into local flattening only.

  A focused audit showed that batch write workflow still carried the same
  launch handoff across two adjacent owners:
  - create flow built `BatchGenerationWriteWorkflowOutcome` with
    `response_payload + launch_plan`
  - resume flow rebuilt the same owner through
    `BatchGenerationWriteWorkflowOutcome::from_resume_domain_outcome(...)`
  - `BatchGenerationWriteWorkflowOutcome::dispatch(...)` then immediately
    forwarded `launch_plan` into runtime dispatch and returned the same
    response payload unchanged
  - the true owner boundary was therefore the create/resume workflow itself,
    not a shared one-hop outcome wrapper

  The refactor therefore narrowed that write/runtime boundary:
  - removed `BatchGenerationWriteWorkflowOutcome`
  - create flow now dispatches directly through
    `PreparedBatchGenerationCreateRequest::dispatch_create_workflow(...)`
  - resume flow now dispatches directly through
    `dispatch_resume_workflow(...)`
  - tests were narrowed to assert the real remaining owners:
    create response payload + launch-plan contract, and resume execution-plan
    -> launch-plan contract

  This keeps batch create response payloads, batch resume response payloads,
  runtime launch behavior, and single-chapter/batch execution selection
  unchanged while removing one more duplicate `response_payload + launch_plan`
  owner from the batch write workflow lane.

  Updated next-step guidance for this lane:
  the batch write/runtime lane is now again closer to a stop-rule point.
  Continue here only if another clearly shared workflow/runtime contract still
  spans create/resume orchestration and launch ownership. If the next move is
  only collapsing local pass-through flow, switch to a new seam instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 05:49 +08:00 checkpoint:
  this slice stayed near the batch runtime/write boundary, narrowed one more
  duplicated execution-config contract, and then stopped again before the lane
  turned into field reshuffling without owner payoff.

  A focused audit showed that batch runtime launch still flattened the same
  prepared execution-config owner across two adjacent boundaries:
  - `BatchGenerationExecutionInput::new(...)` accepted
    `PreparedGenerationExecutionConfig`
  - it immediately split that owner into top-level `ai_config` and
    `provider_payload` fields on `BatchGenerationExecutionInput`
  - `execute_batch_generation_runtime(...)` then reassembled the same
    execution-config semantics by reading those two fields together to build
    `AIService` and runtime prompt context
  - the true owner boundary was therefore still the prepared execution-config
    object itself, not the flattened pair on the runtime input contract

  The refactor therefore narrowed that runtime-input boundary:
  - `BatchGenerationExecutionInput` now keeps
    `PreparedGenerationExecutionConfig` as one nested owner instead of
    flattening `ai_config + provider_payload`
  - batch runtime execution now unpacks the nested execution-config at the
    runtime boundary itself before creating `AIService` and invoking chapter
    generation
  - launch-plan tests were updated to assert the nested execution-config
    contract instead of a flatter batch-runtime input shape

  This keeps batch runtime launch behavior, provider payload propagation, AI
  config usage, and create/resume launch semantics unchanged while removing
  one more duplicated execution-config projection from the batch runtime lane.

  Updated next-step guidance for this lane:
  continue here only if another clearly shared create/resume/runtime contract
  still spans multiple owners. If the next move is only moving fields between
  adjacent structs without shrinking a real handoff boundary, switch seams
  again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 06:05 +08:00 checkpoint:
  this slice mirrored the same execution-config seam in the single-generation
  lane, aligned the single prepare/runtime/workflow contract with the batch
  lane, and then stopped before the lane degraded into local field churn.

  A focused audit showed that single-generation still flattened the same
  prepared execution-config owner across three adjacent boundaries:
  - `PreparedSingleChapterGenerationRequest::new(...)` accepted
    `PreparedGenerationExecutionConfig`
  - it immediately split that owner into top-level `ai_config` and
    `provider_payload` fields on `SingleChapterGenerationExecutionInput`
  - `execute_owned_single_chapter_generation(...)` then reassembled those same
    semantics by reading both fields together to build `AIService` and runtime
    prompt context
  - the true owner boundary was therefore still the prepared execution-config
    object itself, not the flattened pair on the single runtime input contract

  The refactor therefore narrowed that single-generation boundary:
  - `SingleChapterGenerationExecutionInput` now keeps
    `PreparedGenerationExecutionConfig` as one nested owner instead of
    flattening `ai_config + provider_payload`
  - single-generation runtime execution now unpacks the nested
    execution-config at the runtime boundary itself before creating
    `AIService` and invoking chapter generation
  - prepare/runtime/workflow tests were updated to assert the nested
    execution-config contract instead of the flatter single-runtime input
    shape
  - batch resume single-chapter launch was also updated to use the new
    `SingleChapterGenerationExecutionInput::new(target_word_count, execution_config)`
    owner boundary so single/batch launch paths stay aligned

  This keeps single-generation request preparation, background/stream runtime
  launch behavior, provider payload propagation, AI config usage, and
  single-chapter resume launch semantics unchanged while removing one more
  duplicated execution-config projection from the single-generation lane.

  Updated next-step guidance for this lane:
  the single-generation execution-config lane is now close to saturation.
  Continue here only if another clearly shared create/prepare/runtime contract
  still spans multiple owners. If the next move is only moving adjacent fields
  without shrinking a real handoff boundary, switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 06:19 +08:00 checkpoint:
  this slice moved into the cancel command-response lane, removed one more
  one-hop response owner, and then stopped again before the lane collapsed
  into purely local cleanup.

  A focused audit showed that cancel response assembly still carried the same
  summary contract across two adjacent owners:
  - `CancelBatchGenerationResponseState` held only one field:
    `summary: BatchGenerationCommandProgressSummary`
  - `CancelBatchGenerationResponseState::into_response_payload()` then
    immediately forwarded that same summary into
    `BatchGenerationCommandSummaryPayload::from_progress_summary(...).into_payload()`
  - `cancel_owned_batch_generation_task(...)` built the response-state owner
    from task data and then consumed it immediately for the same payload shape
  - the true owner boundary was therefore still the shared progress-summary
    payload helper, not the one-hop cancel response-state wrapper

  The refactor therefore narrowed that cancel-response boundary:
  - removed `CancelBatchGenerationResponseState`
  - cancel workflow now builds `BatchGenerationCommandProgressSummary`
    directly from the task and returns payload through
    `build_cancel_batch_generation_response_payload(...)`
  - cancel tests were narrowed to assert the real remaining owners:
    shared progress-summary input plus final cancel payload shape

  This keeps cancel validation, terminal-status rejection, cancel response
  payload shape, and shared summary semantics unchanged while removing one more
  duplicate response owner from the cancel lane.

  Updated next-step guidance for this lane:
  the cancel response lane is now close to saturation. Continue here only if
  another clearly shared task/payload contract still spans command-state and
  response ownership. If the next move is only flattening local helpers,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 06:32 +08:00 checkpoint:
  this slice moved into the batch read-side query lane, removed two more
  one-hop query-view owners, and then stopped again before the lane devolved
  into route-envelope polishing only.

  A focused audit showed that active-query read-side response assembly still
  carried two adjacent wrappers that owned no distinct semantics:
  - `ActiveBatchGenerationTaskListView` held only
    `contexts: Vec<BatchGenerationReadContext>`
  - `ActiveBatchGenerationTaskListView::into_payload()` immediately mapped the
    same contexts into `items` and wrapped them in `{ total, items }`
  - `ActiveBatchGenerationQueryView` held only
    `task: Option<BatchGenerationReadContext>`
  - `ActiveBatchGenerationQueryView::into_payload()` immediately forwarded the
    same optional context into the compat envelope
    `{ has_active_task, task }`
  - the true owner boundaries were therefore the direct list/query payload
    helpers themselves, not the two one-hop view structs

  The refactor therefore narrowed that read-side query boundary:
  - removed `ActiveBatchGenerationTaskListView`
  - removed `ActiveBatchGenerationQueryView`
  - list query now returns payload through
    `build_active_batch_generation_task_list_payload(...)`
  - active query now returns payload through
    `build_active_batch_generation_query_payload(...)`
  - focused tests were updated to assert the direct helper-owned envelopes
    instead of the intermediate view wrappers

  This keeps active-task list payloads, active project query payloads,
  read-context -> task payload conversion, and project-access behavior
  unchanged while removing two more duplicate read-side response owners from
  the active-query lane.

  Updated next-step guidance for this lane:
  the active-query read-side lane is now again close to saturation. Continue
  here only if another clearly shared read-context/payload contract still spans
  multiple owners. If the next move is only collapsing tiny envelope wrappers,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 06:47 +08:00 checkpoint:
  this slice moved into the shared command-summary payload lane, removed one
  more one-hop summary owner, and then stopped again before the lane degraded
  into helper renaming or local payload flattening only.

  A focused audit showed that batch command response assembly still carried
  the same summary contract across an unnecessary intermediate owner:
  - `BatchGenerationCommandSummaryPayload` held only the shared
    `BatchGenerationCommandProgressSummary` semantics plus the outer message
    field before immediately turning them into a payload map
  - both resume and cancel response paths consumed that owner only through
    `into_payload()` / `extend_payload()` style forwarding
  - tests in the base, resume, and cancel lanes were also asserting the
    intermediate summary owner instead of the real remaining payload boundary
  - the true owner was therefore the shared summary-payload helper itself, not
    the temporary struct wrapper

  The refactor therefore narrowed that shared command-summary boundary:
  - removed `BatchGenerationCommandSummaryPayload`
  - `chapter_batch_generation_task_payload_base_service.rs` now owns the
    direct helper boundary through
    `extend_batch_generation_command_summary_payload(...)` and
    `build_batch_generation_command_summary_payload(...)`
  - resume response assembly now extends the shared payload directly instead of
    creating an intermediate summary struct owner first
  - cancel response assembly now also builds the final payload directly from
    the same shared helper
  - focused tests were updated to assert the helper-owned payload contract
    instead of the deleted intermediate owner

  This keeps shared `batch_id/message/completed_chapters/total_chapters`
  payload shape, cancel/resume response behavior, and progress-summary
  semantics unchanged while removing one more duplicate owner from the batch
  command-response lane.

  Updated next-step guidance for this lane:
  the shared command-summary payload lane is now close to saturation.
  Continue here only if another clearly shared summary/payload contract still
  spans multiple owners. If the next move is only helper naming or tiny payload
  flattening, switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 07:03 +08:00 checkpoint:
  this slice moved into the batch status-stream loading lane, removed another
  one-hop state-load owner, and then stopped again before the stream lane
  devolved into event-envelope polishing only.

  A focused audit showed that owned stream-state loading still carried an
  intermediate wrapper with no distinct semantics:
  - `OwnedBatchGenerationStreamStateLoad` encoded only `Found(state)` vs
    `Missing`
  - `load_optional()` immediately converted read-context into
    `BatchGenerationStreamState`
  - the polling loop then immediately consumed that wrapper only through
    `into_optional_state()`
  - `load_required()` was also colocated on the same wrapper even though it
    returned the final `BatchGenerationStreamState` directly
  - the true owner boundary was therefore the direct
    `Option<BatchGenerationStreamState>` / required `BatchGenerationStreamState`
    loader helpers, not the temporary enum

  The refactor therefore narrowed that stream-loading boundary:
  - removed `OwnedBatchGenerationStreamStateLoad`
  - added direct helpers:
    `load_optional_owned_batch_generation_stream_state(...)` and
    `load_required_owned_batch_generation_stream_state(...)`
  - polling now consumes the optional stream state directly through
    `.await.ok().flatten()` instead of creating an intermediate enum owner
  - initial stream loading now also depends on the direct required-state helper
  - focused tests were updated to assert direct read-context -> stream-state
    conversion and the required-loader error contract instead of the deleted
    intermediate enum

  This keeps owned-task access control, stream polling behavior, not-found
  closure semantics, initial stream bootstrap, and SSE payload shapes unchanged
  while removing one more duplicate owner from the batch status-stream lane.

  Updated next-step guidance for this lane:
  the status-stream loading lane is now closer to saturation. Continue here
  only if another clearly shared state/cursor/event contract still spans
  multiple owners. If the next move is only cursor or event helper polishing,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 07:16 +08:00 checkpoint:
  this slice moved back into the batch read-side payload lane, removed one more
  one-hop payload kind owner, and then stopped again before the lane degraded
  into pure read-side field shuffling.

  A focused audit showed that read-context payload assembly still carried a
  temporary kind split with no distinct ownership:
  - `BatchGenerationTaskViewPayloadKind` existed only to choose between
    `Active` and `Status`
  - `into_task_view_payload(kind)` always built the same shared base payload
    and quality-status fields first
  - the only semantic difference was whether status-only terminal/retry fields
    were appended afterward
  - `into_status_task_payload()` and `into_active_task_payload()` therefore
    still depended on a one-hop kind owner instead of being the true payload
    boundaries themselves

  The refactor therefore narrowed that read-side payload boundary:
  - removed `BatchGenerationTaskViewPayloadKind`
  - extracted the shared payload assembly into
    `into_shared_task_view_payload()`
  - `into_active_task_payload()` now returns the shared payload directly
  - `into_status_task_payload()` now owns the explicit status-only append step
    for retry / failed-chapter / terminal fields
  - focused tests were updated to assert the direct active/status payload
    boundaries instead of the deleted intermediate kind dispatch

  This keeps active-task payload shape, status-task payload shape,
  quality-status propagation, terminal metadata, and stream-state conversion
  unchanged while removing one more duplicate owner from the batch read-side
  payload lane.

  Updated next-step guidance for this lane:
  the read-side payload lane is now again close to saturation. Continue here
  only if another clearly shared payload boundary still spans multiple owners.
  If the next move is only reshuffling local field inserts, switch seams again
  instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 07:31 +08:00 checkpoint:
  this slice moved into the batch terminal-status payload lane, removed one
  more one-hop terminal context owner, and then stopped again before the lane
  degraded into local label/message polishing only.

  A focused audit showed that terminal status payload assembly still crossed an
  unnecessary intermediate owner:
  - `BatchGenerationTerminalStatusContext` existed only to resolve
    `terminal_reason / terminal_label / review_required / can_resume`
    from task status and failed-chapter semantics
  - the production read-side path immediately consumed that owner only through
    `insert_into_payload(...)`
  - `manual_review_label(...)` remained the true reusable semantic helper,
    while the terminal context struct itself added no independent lifecycle or
    transport boundary
  - the real remaining owner boundary was therefore the direct terminal payload
    extension helper, not the temporary context struct

  The refactor therefore narrowed that terminal-status boundary:
  - removed `BatchGenerationTerminalStatusContext`
  - added direct helper
    `insert_batch_generation_terminal_status_payload(...)`
  - `chapter_batch_generation_read_context_service.rs` now appends terminal
    fields through the direct helper instead of `from_task(...).insert_into_payload(...)`
  - quality-status tests were updated to assert payload output directly instead
    of the deleted terminal context owner

  This keeps manual-review detection, terminal reason/label semantics,
  read-side status payload shape, and resume gating compatibility unchanged
  while removing one more duplicate owner from the batch terminal-status lane.

  Updated next-step guidance for this lane:
  the terminal-status payload lane is now close to saturation. Continue here
  only if another clearly shared terminal semantic still spans multiple owners.
  If the next move is only tweaking field names, labels, or local payload
  insertion order, switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 07:44 +08:00 checkpoint:
  this slice moved into the batch stream-semantics owner lane, removed one more
  one-hop semantics struct, and then stopped again before the stream lane
  degraded into event-shape polishing only.

  A focused audit showed that stream state construction still crossed an
  unnecessary intermediate owner:
  - `resolve_batch_generation_stream_semantics(...)` built
    `progress / message / event_status / terminal_kind`
    into `BatchGenerationStreamSemantics`
  - `BatchGenerationStreamState::from_task_state(...)` immediately unpacked
    that struct and copied the same four fields into the final stream-state
    owner
  - the semantics struct therefore had no independent lifecycle, transport
    contract, or reuse boundary beyond that single unpack step
  - the real owner boundary was the direct stream-state field resolver, not the
    temporary semantics wrapper

  The refactor therefore narrowed that stream-semantics boundary:
  - removed `BatchGenerationStreamSemantics`
  - replaced `resolve_batch_generation_stream_semantics(...)` with direct helper
    `resolve_batch_generation_stream_state_fields(...)`
  - `BatchGenerationStreamState::from_task_state(...)` now consumes the tuple
    result directly and owns the final stream-state field assembly in one step
  - focused tests were updated to assert the direct field-resolver contract
    instead of the deleted intermediate semantics struct

  This keeps progress fallback behavior, message fallback behavior,
  `event_status`, terminal-kind semantics, stream-state construction, and all
  SSE event shapes unchanged while removing one more duplicate owner from the
  batch stream lane.

  Updated next-step guidance for this lane:
  the stream-semantics lane is now closer to saturation. Continue here only if
  another clearly shared state/event boundary still spans multiple owners. If
  the next move is only reshuffling terminal event helpers or cursor fields,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 08:02 +08:00 checkpoint:
  this slice moved into the batch create-workflow projection lane, removed one
  more one-hop chapter projection owner, and then stopped again before the
  create lane degraded into local payload micro-polish only.

  A focused audit showed that create-workflow chapter preparation still crossed
  an unnecessary intermediate owner:
  - `PreparedBatchGenerationCreateRequest` already owned the canonical
    `chapters_to_generate`
  - `chapter_projection()` immediately wrapped those same targets into
    `BatchGenerationCreateChapterProjection`
  - that projection was then only used to re-expose
    `chapter_ids / chapter_id_payload / chapter_payloads`
  - the projection struct therefore had no independent lifecycle or business
    meaning beyond one-hop expansion of the prepared request owner
  - the true owner boundary was the prepared create request itself, not the
    temporary projection wrapper

  The refactor therefore narrowed that create-workflow boundary:
  - removed `BatchGenerationCreateChapterProjection`
  - `PreparedBatchGenerationCreateRequest` now directly derives
    `chapter_ids()`, `chapter_id_payload()`, and
    `chapters_to_generate_payload()` from its owned chapter targets
  - focused tests were updated to assert direct target projection through the
    prepared request instead of the deleted intermediate projection owner

  This keeps create response payload shape, chapter ordering, chapter id
  payload shape, launch-plan input assembly, and create workflow runtime
  behavior unchanged while removing one more duplicate owner from the batch
  create lane.

  Updated next-step guidance for this lane:
  the create-workflow projection lane is now closer to saturation. Continue
  here only if another clearly shared create/workflow boundary still spans
  multiple owners. If the next move is only local payload formatting or clone
  cleanup, switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 08:14 +08:00 checkpoint:
  this slice moved into the batch resume workflow boundary lane, removed one
  more one-hop domain outcome owner, and then stopped again before the resume
  lane degraded into local response-state flattening only.

  A focused audit showed that resume preparation still crossed an unnecessary
  outer owner:
  - `prepare_batch_generation_resume(...)` produced
    `ResumeBatchGenerationOutcome`
  - that outcome held only `execution` plus `response_state`
  - production write-workflow code immediately destructured that owner in its
    only call site and forwarded the two inner values separately
  - the temporary outcome struct therefore had no independent lifecycle,
    transport contract, or semantic reuse beyond one-hop grouping
  - the real owner boundary was the pair
    `(ResumeExecutionPlan, ResumeBatchGenerationResponseState)`, not the
    temporary outer wrapper

  The refactor therefore narrowed that resume boundary:
  - removed `ResumeBatchGenerationOutcome`
  - `prepare_batch_generation_resume(...)` now returns
    `(ResumeExecutionPlan, ResumeBatchGenerationResponseState)` directly
  - `resume_owned_batch_generation_write_workflow(...)` now receives the pair
    directly and forwards both values to `dispatch_resume_workflow(...)`
  - `dispatch_resume_workflow(...)` now consumes the direct pair instead of
    destructuring the deleted intermediate outcome owner

  This keeps resume validation, reset semantics, response payload shape,
  execution-plan selection, and runtime launch behavior unchanged while
  removing one more duplicate owner from the batch resume lane.

  Updated next-step guidance for this lane:
  the outer resume outcome lane is now closer to saturation. Continue here
  only if another clearly shared resume/workflow boundary still spans multiple
  owners. If the next move is only flattening the inner response-state fields,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 08:27 +08:00 checkpoint:
  this slice moved into the batch resume semantics lane, removed one more
  one-hop shared semantics owner, and then stopped again before the lane
  degraded into local field reshuffling only.

  A focused audit showed that resume semantics still crossed an unnecessary
  temporary owner:
  - `resolve_task_semantics()` built `ResumeResolvedTaskSemantics`
  - that struct only grouped `execution_target` plus `runtime_semantics`
  - production code then immediately consumed it only through
    `resolve_runtime_semantics()` or `resolve_execution_target()`
  - the temporary shared semantics struct therefore had no independent
    lifecycle or transport meaning beyond one-hop grouping of two direct
    consumer boundaries
  - the real owner boundaries were the direct
    `resolve_runtime_semantics()` and `resolve_execution_target()` functions,
    not the temporary wrapper

  The refactor therefore narrowed that resume-semantics boundary:
  - removed `ResumeResolvedTaskSemantics`
  - removed `resolve_task_semantics()`
  - `resolve_runtime_semantics()` now directly owns the single/batch runtime
    branch semantics
  - `resolve_execution_target()` now directly owns the single/batch execution
    target branch semantics
  - focused tests were updated to assert the direct runtime/execution
    boundaries instead of the deleted shared wrapper

  This keeps resume reset semantics, resume execution target resolution,
  malformed-single fallback behavior, and runtime checkpoint behavior unchanged
  while removing one more duplicate owner from the batch resume lane.

  Updated next-step guidance for this lane:
  the resume semantics lane is now closer to saturation. Continue here only if
  another clearly shared resume semantic still spans multiple owners. If the
  next move is only local field movement inside `ResumeRuntimeSemantics` or
  `ResumeResetSemantics`, switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 08:40 +08:00 checkpoint:
  this slice moved into the batch runtime-checkpoint lane, removed one more
  one-hop snapshot plan owner, and then stopped again before the lane degraded
  into local message/string polishing only.

  A focused audit showed that runtime checkpoint generation still crossed an
  unnecessary intermediate owner:
  - `BatchGenerationSnapshotStage::resolve_plan(...)` built a
    `BatchGenerationSnapshotPlan`
  - `build_batch_generation_runtime_checkpoint_for_stage(...)` then immediately
    consumed that plan only through `plan.build_checkpoint(...)`
  - the temporary plan struct had no independent lifecycle, persistence, or
    cross-layer contract beyond one-hop grouping of checkpoint fields
  - the real owner boundary was the stage-specific checkpoint builder itself,
    not the temporary snapshot plan wrapper

  The refactor therefore narrowed that runtime-checkpoint boundary:
  - removed `BatchGenerationSnapshotPlan`
  - replaced `resolve_plan(...)->build_checkpoint(...)` with direct
    `BatchGenerationSnapshotStage::build_checkpoint(...)`
  - each stage branch now directly constructs the final pending/runtime
    checkpoint value without a temporary plan owner

  This keeps queue/resume/preparing/running/completed/cancelled/failed
  checkpoint semantics, progress clamping, message selection, and runtime-state
  integration unchanged while removing one more duplicate owner from the batch
  checkpoint lane.

  Updated next-step guidance for this lane:
  the runtime-checkpoint lane is now closer to saturation. Continue here only
  if another clearly shared checkpoint owner still spans multiple boundaries.
  If the next move is only tweaking per-stage strings or local branch layout,
  switch seams again instead.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the single-generation workflow lane, removed two more
  one-hop prepared-request wrappers, and then stopped again before the lane
  degraded into local field reshuffling only.

  A focused audit showed that single-generation workflow still crossed two
  unnecessary prepared-request wrappers:
  - `PreparedSingleChapterGenerationRequest::into_runtime_launch_input(...)`
    only forwarded the prepared chapter target id plus execution input into the
    single-runtime launch constructor
  - `PreparedSingleChapterGenerationRequest::into_background_runtime_parts(...)`
    only grouped the prepared task model, pending checkpoint, chapter target,
    and runtime launch input for its two call sites
  - production code immediately destructured both wrappers at the workflow
    boundary and forwarded the inner values separately
  - these wrappers therefore had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the workflow code itself, not the temporary
    prepared-request wrappers

  The refactor therefore narrowed that single-generation workflow boundary:
  - removed `PreparedSingleChapterGenerationRequest::into_runtime_launch_input`
  - removed `PreparedSingleChapterGenerationRequest::into_background_runtime_parts`
  - `create_single_generation_stream_workflow(...)` now directly constructs the
    runtime launch input from the prepared owner fields
  - `start_owned_single_generation_background_workflow(...)` now directly
    constructs the task model, pending checkpoint, and runtime launch input
    from the prepared owner fields
  - focused tests were updated to assert the direct workflow boundaries instead
    of the deleted prepared-request wrappers

  This keeps request normalization, accessible-chapter loading, background task
  shape, pending checkpoint semantics, runtime launch behavior, and response
  payload shape unchanged while removing two more duplicate owners from the
  single-generation lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch write-workflow lane, removed two more
  one-hop launch-plan wrappers, and then stopped again before the lane
  degraded into local branch reshuffling only.

  A focused audit showed that batch write-workflow still crossed two
  unnecessary launch-plan wrappers:
  - `PreparedBatchGenerationCreateRequest::into_runtime_launch_plan(...)`
    only forwarded the prepared chapter ids plus normalized target word count
    into the batch runtime launch constructor
  - `ResumeExecutionPlan::into_runtime_launch_plan(...)` only mapped the
    already-resolved resume execution branch into a runtime launch plan
  - production code immediately consumed both wrappers at the workflow
    boundary and forwarded the inner values separately
  - these wrappers therefore had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the workflow code itself, not the temporary
    launch-plan wrappers

  The refactor therefore narrowed that batch write-workflow boundary:
  - removed `PreparedBatchGenerationCreateRequest::into_runtime_launch_plan`
  - removed `ResumeExecutionPlan::into_runtime_launch_plan`
  - `create_owned_batch_generation_write_workflow(...)` now directly constructs
    the batch runtime launch plan from the prepared owner fields
  - `resume_owned_batch_generation_write_workflow(...)` now directly maps the
    resolved resume execution branch into the runtime launch plan
  - focused tests were updated to assert the direct workflow boundaries instead
    of the deleted launch-plan wrappers
  - a now-unused `PreparedGenerationExecutionConfig` type alias in this file
    was removed as part of the same cleanup

  This keeps batch create/resume payloads, launch plan shape, runtime dispatch
  behavior, and response contract unchanged while removing two more duplicate
  owners from the batch write lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch status-stream lane, removed one more
  one-hop poll-resolution owner, and then stopped again before the lane
  degraded into local branch reshuffling only.

  A focused audit showed that status polling still crossed an unnecessary
  intermediate owner:
  - `BatchGenerationStreamPollResolution` only wrapped the current stream
    state vs not-found close path inside one polling loop
  - production code immediately unpacked that wrapper and forwarded the inner
    state/event separately
  - the temporary resolution enum had no independent transport contract or
    lifecycle beyond one-hop grouping
  - the real owner boundary was the polling loop itself, not the temporary
    poll-resolution wrapper

  The refactor therefore narrowed that status-stream boundary:
  - removed `BatchGenerationStreamPollResolution`
  - `build_batch_generation_status_stream(...)` now directly handles the
    pending-state branch, loaded-state branch, and not-found close path inline
  - focused tests were updated to assert the direct polling behavior instead
    of the deleted wrapper

  This keeps stream polling, not-found handling, event emission, and terminal
  close behavior unchanged while removing one more duplicate owner from the
  batch status-stream lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-27 09:xx +08:00 checkpoint:
  this slice moved into the batch read-context lane, removed one more
  one-hop shared payload owner, and then stopped again before the lane
  degraded into local payload polishing only.

  A focused audit showed that read-context payload building still crossed an
  unnecessary intermediate owner:
  - `shared_task_view_payload()` only built the common task-view payload map
    used by both status and active payload projections
  - `into_shared_task_view_payload()` then immediately consumed that helper and
    only attached quality-status fields before the two public projection paths
  - the temporary shared payload wrapper had no independent transport contract
    or lifecycle beyond one-hop grouping
  - the real owner boundary was the read-context projection methods themselves,
    not the temporary shared payload wrapper

  The refactor therefore narrowed that batch read-context boundary:
  - removed `shared_task_view_payload()`
  - removed `into_shared_task_view_payload()`
  - `into_status_task_payload()` now directly builds the base task-view payload
    before attaching terminal/status-only fields
  - `into_active_task_payload()` now directly builds the base task-view payload
    before returning it as the active response envelope
  - focused tests were updated implicitly by preserving the same payload shape

  This keeps read-side payload content, status-only field injection, quality
  status merging, and stream-state projection unchanged while removing one more
  duplicate owner from the batch read-context lane.

  Validation for this slice:
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo fmt --manifest-path "backend-rs/Cargo.toml"`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:/Users/yanc/.codex/memories/mumu-rs-target'; cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the single-generation entry lane, removed the last thin
  request constructor, and then confirmed the route-to-service chain still
  compiles cleanly after the seam closure.

  A focused audit showed that the single-generation entry path still carried
  one unnecessary constructor hop:
  - `SingleChapterGenerationRequest::new(...)` only forwarded route fields into
    the request struct
  - the route layer immediately consumed that constructor and never relied on a
    separate validation or translation boundary
  - the request type already behaved as a plain data carrier, so the thin
    constructor had no independent contract
  - removing it keeps the route and prepare service aligned on the same plain
    struct boundary

  The refactor therefore narrowed that single-generation entry boundary:
  - removed `SingleChapterGenerationRequest::new`
  - removed the now-empty impl blocks around the single-generation request,
    execution input, and prepared-request carriers
  - `SingleChapterGenerationRouteRequest::into_generation_request(...)` now
    builds `SingleChapterGenerationRequest` directly
  - the prepare service continues to normalize target word count and assemble
    the execution input inline, without extra wrapper hops

  This keeps route payload shape, request normalization, prepared-request
  assembly, and downstream workflow behavior unchanged while removing one more
  duplicate owner from the single-generation lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch write-workflow and single runtime-launch
  lanes, removed a pair of thin request/launch constructors, and then
  re-validated the route-to-service chain after restoring the necessary
  shared helpers.

  A focused audit showed that the batch write lane and single runtime launch
  lane still carried a few thin ownership hops:
  - `BatchGenerationCreateWorkflowRequest::new(...)` only normalized route
    fields into a plain request carrier
  - `SingleGenerationRuntimeLaunchInput::new(...)` only wrapped already
    prepared launch data into a plain struct
  - the batch write-workflow helper methods for chapter id / payload assembly
    were still the real owner boundary for response and dispatch shapes
  - the runtime launch boundary was already explicit enough to carry a plain
    struct literal directly

  The refactor therefore narrowed those lanes:
  - removed `BatchGenerationCreateWorkflowRequest::new`
  - removed `SingleGenerationRuntimeLaunchInput::new`
  - route and workflow call sites now build both carriers directly with struct
    literals or a `from_route_fields(...)` helper
  - `PreparedBatchGenerationCreateRequest` continues to own chapter id and
    chapter payload projection, keeping response and dispatch shapes stable
  - shared estimate helpers were restored to the preparation owner so the
    test contract stayed unchanged

  This keeps batch create/resume payloads, single-runtime launch data, and
  downstream execution behavior unchanged while removing more duplicate owner
  hops from the generation lanes.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch payload-base lane, removed one more one-hop
  payload adapter, and then revalidated that command-summary payload shape
  stayed unchanged for cancel/resume flows.

  A focused audit showed that one helper in the payload-base lane had already
  degraded into a pure forwarding owner:
  - `build_batch_generation_command_summary_payload(...)` already owned the
    summary payload shape
  - `extend_batch_generation_command_summary_payload(...)` only copied the
    same four fields into an existing map and was no longer a meaningful
    reusable boundary
  - resume could append those fields inline after building its runtime payload
    without changing any external contract

  The refactor therefore narrowed that payload-base boundary:
  - removed `extend_batch_generation_command_summary_payload(...)`
  - inlined the summary field insertion into
    `build_batch_generation_command_summary_payload(...)`
  - resume now appends summary fields directly to its locally owned payload map
  - cleaned a duplicate `#[test]` attribute and an unused `Map` import in the
    payload-base test module while touching the file

  This keeps cancel/resume summary payload shape and runtime payload behavior
  unchanged while removing one more duplicate owner from the payload-base lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_payload_base_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch command-summary lane, removed the last thin
  summary constructor, and then revalidated the cancel/resume payload paths
  after making the summary fields crate-visible for direct struct literals.

  A focused audit showed that the shared command-summary wrapper was still a
  thin owner hop:
  - `BatchGenerationCommandProgressSummary::new(...)` only packed batch id and
    chapter counts into a data carrier
  - cancel and resume call sites already knew those exact fields, so the thin
    constructor did not add a meaningful translation boundary
  - the summary payload helpers are the real owner for response shape
  - direct struct literals keep the shared summary contract explicit without
    changing payload shape

  The refactor therefore narrowed that batch command-summary boundary:
  - removed `BatchGenerationCommandProgressSummary::new`
  - made the summary fields `pub(crate)` so cancel/resume and tests can build
    the carrier directly
  - cancel and resume workflows now construct the summary with struct literals
  - the existing payload-builder helpers continue to own the response shape

  This keeps cancel/resume progress payloads unchanged while removing one more
  duplicate owner from the batch command-summary lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch stream-event lane, removed the last thin
  cursor constructor, and then revalidated the poll/event path without
  changing stream semantics or emitted payloads.

  A focused audit showed that `BatchGenerationStreamCursor::new()` was only a
  one-hop default-value wrapper:
  - it only initialized empty status/message plus sentinel progress/completed
    values
  - both the status-stream poller and the stream-event tests already knew
    those exact initial values
  - the real ownership boundary is `resolve_event_batch(...)`, not the thin
    default constructor

  The refactor therefore narrowed that stream-event boundary:
  - removed `BatchGenerationStreamCursor::new()`
  - made the cursor fields `pub(crate)` so local stream owners can build the
    struct directly
  - status-stream runtime and event tests now initialize the cursor with
    explicit struct literals

  This keeps stream polling, dedup cursor behavior, terminal close behavior,
  and SSE payload shape unchanged while removing one more duplicate owner from
  the stream-event lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch status-stream poll lane, removed one more
  one-hop branch owner, and then revalidated that pending-first polling and
  not-found close behavior stayed unchanged.

  A focused audit showed that the poll loop still carried one unnecessary
  ownership split:
  - `pending_state.is_some()` plus `take().expect(...)` introduced a two-step
    branch around the initial state handoff
  - `loaded_state` then introduced a second one-use local wrapper before the
    not-found close branch
  - neither temporary owner added a reusable contract beyond one loop turn

  The refactor therefore narrowed that poll boundary:
  - replaced `pending_state.is_some()` + `take().expect(...)` with a direct
    `if let Some(state) = pending_state.take()` handoff
  - removed the one-use `loaded_state` local and matched the loaded read
    result directly in the poll branch
  - kept poll ordering, not-found close behavior, timeout behavior, and
    downstream SSE event emission unchanged

  This keeps pending-first delivery, read-context reload semantics, and stream
  close behavior unchanged while removing one more duplicate owner from the
  status-stream poll lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice intentionally moved back from the stream lane to the batch
  write-workflow lane, removed one more pure forwarding helper, and then
  revalidated that create-response timing fields stayed unchanged.

  A focused audit showed that one helper in the write lane had already become
  a zero-value forwarding owner:
  - the free `estimated_task_minutes(...)` function only forwarded to
    `PreparedBatchGenerationCreateRequest::estimated_task_minutes(...)`
  - all real meaning already lived on the prepared-request owner
  - the extra wrapper did not provide a reusable boundary or compatibility
    contract

  The refactor therefore narrowed that write-workflow boundary:
  - removed the free `estimated_task_minutes(...)` helper
  - create-response assembly and tests now call
    `PreparedBatchGenerationCreateRequest::estimated_task_minutes(...)`
    directly

  This keeps batch create response shape and estimated time calculation
  unchanged while removing one more duplicate owner from the batch write lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch stream-semantics lane, removed the tuple-only
  state-field helper, and then revalidated that stream-state ownership still
  produces the same progress/message/status semantics.

  A focused audit showed that
  `resolve_batch_generation_stream_state_fields(...)` was only a one-hop tuple
  wrapper:
  - it only computed progress/message/event-status/terminal-kind and returned
    them immediately to `BatchGenerationStreamState::from_task_state(...)`
  - no other production owner consumed that tuple contract
  - the real owner boundary is the stream-state projection itself, not the
    intermediate tuple

  The refactor therefore narrowed that stream-semantics boundary:
  - removed `resolve_batch_generation_stream_state_fields(...)`
  - moved checkpoint-fallback progress/message resolution inline into
    `BatchGenerationStreamState::from_task_state(...)`
  - kept `BatchGenerationResolvedStreamStatus` as the semantic owner for
    default progress/message plus event-status/terminal-kind mapping
  - updated tests to assert the stream-state owner result directly instead of
    the deleted tuple contract

  This keeps stream-state projection, progress fallback, terminal detection,
  and downstream SSE event behavior unchanged while removing one more duplicate
  owner from the stream-semantics lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch stream-event lane, removed two terminal-only
  event wrappers, and then revalidated that terminal event order and payload
  shape stayed unchanged.

  A focused audit showed that two event builders were only one-hop wrappers:
  - `build_batch_generation_result_event(...)` was only used by
    `BatchGenerationStreamState::terminal_events()`
  - `build_batch_generation_failed_event(...)` was also only used by
    `terminal_events()`
  - neither function exposed a reusable transport boundary beyond the local
    terminal-event owner

  The refactor therefore narrowed that terminal-event boundary:
  - removed `build_batch_generation_result_event(...)`
  - removed `build_batch_generation_failed_event(...)`
  - `BatchGenerationStreamState::terminal_events()` now builds completed and
    failed terminal payloads directly
  - tests now assert the terminal-event owner result instead of the deleted
    wrapper functions

  This keeps progress/result/error event shape, terminal event order, and
  stream close semantics unchanged while removing more duplicate owners from
  the stream-event lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch stream-event and poll lanes, removed three
  single-consumer error-event wrappers, and then revalidated that not-found,
  timeout, and cancelled SSE payloads stayed unchanged.

  A focused audit showed that three error-event helpers had already degraded
  into single-consumer wrappers:
  - `build_batch_generation_cancelled_event()` was only used inside
    `BatchGenerationStreamState::terminal_events()`
  - `build_batch_generation_not_found_event()` was only used by the
    status-stream poll not-found branch
  - `build_batch_generation_timeout_event()` was only used by the
    status-stream timeout branch
  - none of these helpers provided a reusable owner boundary beyond their
    single local consumer

  The refactor therefore narrowed those stream-event boundaries:
  - removed `build_batch_generation_cancelled_event()`
  - removed `build_batch_generation_not_found_event()`
  - removed `build_batch_generation_timeout_event()`
  - `terminal_events()` now builds the cancelled payload directly
  - `build_batch_generation_status_stream(...)` now builds not-found and
    timeout payloads directly in the owning poll branches

  This keeps cancelled/not-found/timeout SSE payload shape and stream close
  semantics unchanged while removing more duplicate owners from the stream
  event lanes.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved back to the batch write-workflow lane, removed one more
  single-consumer payload wrapper, and then revalidated that batch task
  creation and response payloads stayed unchanged.

  A focused audit showed that `chapter_id_payload()` had already degraded into
  a one-hop wrapper:
  - it only projected `chapter_ids()` into `Value::Array(...)`
  - the production path only consumed that JSON payload inside
    `create_batch_generation_task(...)`
  - the real owner boundary is the task creation path and prepared-request id
    projection, not the intermediate wrapper itself

  The refactor therefore narrowed that batch write-workflow boundary:
  - removed `PreparedBatchGenerationCreateRequest::chapter_id_payload()`
  - `create_batch_generation_task(...)` now builds the `chapter_ids` JSON array
    directly at the task-model write owner
  - tests now assert the owned chapter-id payload shape directly instead of the
    deleted wrapper

  This keeps batch task persistence payload shape, create response behavior,
  runtime dispatch inputs, and chapter id ordering unchanged while removing one
  more duplicate owner from the batch write lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch write-workflow lane, removed one more
  create-response-only payload wrapper, and then revalidated that the batch
  create response shape stayed unchanged.

  A focused audit showed that `chapter_payloads()` had already degraded into a
  one-hop response-only wrapper:
  - it only projected `chapters_to_generate` into the create response JSON
    array shape
  - the production path only consumed that projection inside
    `start_owned_batch_generation_write_workflow(...)`
  - the real owner boundary is the create response payload assembly, not the
    prepared-request helper itself

  The refactor therefore narrowed that batch write-workflow boundary:
  - removed `PreparedBatchGenerationCreateRequest::chapter_payloads()`
  - `start_owned_batch_generation_write_workflow(...)` now builds the
    `chapters_to_generate` JSON array directly at the create-response owner
  - tests now assert the owned create-response chapter payload shape directly
    instead of the deleted wrapper

  This keeps batch create response payload fields, chapter ordering, runtime
  dispatch inputs, estimated-time calculation, and task creation behavior
  unchanged while removing one more duplicate owner from the batch write lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch runtime lane, removed one more zero-behavior
  owner shell, and then revalidated that runtime dispatch and resume/reset
  semantics stayed unchanged.

  A focused audit showed that `BatchGenerationExecutionInput` no longer owned
  any internal construction or behavior:
  - its previous constructor had already been removed in an earlier slice
  - the remaining `impl BatchGenerationExecutionInput {}` block was empty
  - the empty impl did not provide a reusable contract, compatibility seam, or
    semantic owner boundary

  The refactor therefore narrowed that runtime-state boundary:
  - removed the empty `impl BatchGenerationExecutionInput {}`
  - kept all runtime input construction at the existing write/runtime owners
    that already instantiate the struct directly

  This keeps runtime dispatch input shape, batch resume/reset behavior,
  checkpoint progression, and downstream task mutation semantics unchanged
  while removing one more no-signal owner shell from the runtime lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch resume-command lane, removed one more
  duplicated execution-plan assembly path, and then revalidated that resume
  dispatch branching and error semantics stayed unchanged.

  A focused audit showed that `ResumeExecutionPlan` construction had already
  become duplicated owner work:
  - `prepare_batch_generation_resume(...)` rebuilt the same
    `ResumeExecutionSelection -> ResumeExecutionPlan` mapping inline
  - the tests also rebuilt that mapping locally to assert the same contract
  - the real owner boundary is the execution-plan type itself, not each
    consumer repeating the same selection-to-plan assembly

  The refactor therefore narrowed that resume-command boundary:
  - added `ResumeExecutionPlan::from_command_state(...)` as the single plan
    owner for `command_state + user_id -> execution plan`
  - `prepare_batch_generation_resume(...)` now consumes that owner directly
  - tests now assert the shared plan owner result instead of rebuilding the
    mapping inline

  This keeps malformed-single fallback behavior, target-word-count
  normalization, single-vs-batch resume dispatch selection, reset semantics,
  and resume response payload behavior unchanged while removing one more
  duplicate owner from the resume command lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch resume-semantics lane, removed one more
  target-only intermediate owner, and then revalidated that resume selection
  behavior stayed unchanged.

  A focused audit showed that `ResumeExecutionTarget` and
  `resolve_execution_target()` had already degraded into a pure intermediate
  state:
  - production code only consumed that target enum inside
    `resolve_execution_selection()`
  - no external workflow, runtime, or payload owner depended on the
    intermediate target contract
  - tests were also largely asserting the same intermediate state instead of
    the real surviving selection/runtime/reset semantics

  The refactor therefore narrowed that resume-semantics boundary:
  - removed `ResumeExecutionTarget`
  - removed `resolve_execution_target()`
  - `resolve_execution_selection()` now resolves directly from task kind plus
    chapter-id parsing
  - tests now assert the surviving execution-selection owner and the adjacent
    runtime/reset semantics instead of the deleted middle state

  This keeps malformed-single fallback behavior, batch chapter-id parsing,
  single-vs-batch resume selection, runtime semantics, and reset checkpoint
  semantics unchanged while removing one more duplicate owner from the resume
  semantics lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed on the batch resume-semantics lane, removed one more
  single-consumer parsing helper, and then revalidated that resume selection
  behavior stayed unchanged.

  A focused audit showed that `parse_batch_chapter_ids()` had already
  degraded into a one-consumer helper:
  - production code only consumed it inside `resolve_execution_selection()`
  - no runtime, workflow, or payload owner depended on a separate
    chapter-id-parsing boundary
  - the real owner contract is the resumable execution-selection result, not
    a file-local parser helper sitting underneath it

  The refactor therefore narrowed that resume-semantics boundary:
  - removed `parse_batch_chapter_ids()`
  - `resolve_execution_selection()` now owns the batch chapter-id parsing
    inline beside the single-vs-batch selection branch it serves

  This keeps malformed-single fallback behavior, batch chapter-id extraction,
  single-vs-batch resume selection, runtime semantics, and reset checkpoint
  semantics unchanged while removing one more duplicate owner from the resume
  semantics lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_semantics_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch read-side query lane, removed one more
  list-only intermediate owner, and then revalidated that active-task list
  payloads stayed unchanged.

  A focused audit showed that `load_active_user_batch_generation_task_list_view(...)`
  still owned a no-signal intermediate list:
  - it first collected `Vec<BatchGenerationReadContext>`
  - then immediately remapped that list into active-task payload values
  - the real owner boundary is the final list-view payload assembly, not a
    temporary read-context vector sitting between query and payload output

  The refactor therefore narrowed that read-side query boundary:
  - removed the intermediate `Vec<BatchGenerationReadContext>` owner from the
    active-list query path
  - the query now converts each loaded read context directly into its active
    payload while assembling the final `items` list
  - also removed the now-unused `BatchGenerationReadContext` import from the
    query file

  This keeps active batch-generation list payload shape, item ordering,
  read-context loading semantics, and downstream active-task response
  contracts unchanged while removing one more duplicate owner from the
  read-side query lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved back to the batch stream-event lane, removed one more
  single-consumer progress-event wrapper, and then revalidated that SSE
  progress payloads stayed unchanged.

  A focused audit showed that `build_batch_generation_progress_event(...)`
  had already degraded into a one-consumer wrapper:
  - production code only consumed it inside
    `BatchGenerationStreamCursor::resolve_event_batch(...)`
  - the real owner boundary is the event-batch resolver that decides
    continue-vs-close and assembles the emitted event sequence
  - the standalone progress-event builder no longer carried an independent
    transport contract beyond that local owner

  The refactor therefore narrowed that stream-event boundary:
  - removed `build_batch_generation_progress_event(...)`
  - `BatchGenerationStreamCursor::resolve_event_batch(...)` now builds the
    progress event directly beside the cursor-diff and terminal-event logic
  - tests now assert the progress event through the surviving event-batch
    owner result instead of the deleted wrapper

  This keeps SSE progress payload shape, cursor change detection, terminal
  event ordering, and close-vs-continue stream semantics unchanged while
  removing one more duplicate owner from the stream-event lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice returned to the batch read-side query lane, removed one more
  active-query transition owner, and then revalidated that active-task query
  payloads stayed unchanged.

  A focused audit showed that `load_active_batch_generation_query(...)` still
  carried a small transition owner around the optional task payload:
  - it matched `Option<task>` directly into two separate JSON response shells
  - the real owner boundary is the final query response payload, not the
    branch-local JSON object duplication
  - the optional active-task payload itself is enough as the local transition
    value before final response assembly

  The refactor therefore narrowed that read-side query boundary:
  - `load_active_batch_generation_query(...)` now first resolves
    `Option<Value>` for the active task payload
  - the final response JSON is assembled once from `task_payload.is_some()`
    plus the optional payload value

  This keeps active-task query response shape, read-context loading semantics,
  project-access behavior, and downstream payload contracts unchanged while
  removing one more duplicate owner from the read-side query lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch snapshot/checkpoint lane, removed one more
  queued-snapshot-only wrapper, and then revalidated that initial runtime
  snapshot payloads stayed unchanged.

  A focused audit showed that
  `build_new_batch_generation_task_runtime_snapshot(...)` had already
  degraded into a one-consumer wrapper:
  - production code only consumed it inside
    `persist_new_batch_generation_task_snapshot(...)`
  - all actual checkpoint semantics already lived in
    `BatchGenerationSnapshotStage::Queued` via
    `build_batch_generation_runtime_checkpoint_for_stage(...)`
  - the standalone queued snapshot helper no longer carried an independent
    runtime or persistence contract beyond that single call site

  The refactor therefore narrowed that snapshot boundary:
  - removed `build_new_batch_generation_task_runtime_snapshot(...)`
  - `persist_new_batch_generation_task_snapshot(...)` now builds the queued
    runtime snapshot directly from the queued stage owner
  - tests now assert the queued stage owner result directly instead of the
    deleted wrapper

  This keeps initial batch runtime snapshot shape, queued checkpoint fields,
  snapshot persistence behavior, and downstream write/runtime semantics
  unchanged while removing one more duplicate owner from the snapshot lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed in the batch snapshot/runtime lane, removed one more
  local transition owner from snapshot writes, and then revalidated that
  merge-vs-replace runtime-state behavior stayed unchanged.

  A focused audit showed that
  `BatchGenerationSnapshotWriteMode::resolve_runtime_state(...)` had already
  degraded into a thin local wrapper:
  - production code only consumed it inside
    `BatchGenerationSnapshotWriteMode::apply_to_active_model(...)` and the
    new-snapshot insert path in
    `write_batch_generation_runtime_snapshot(...)`
  - it no longer owned an independent cross-call contract beyond choosing
    between merge and replace at those two local write points
  - the real shared semantic owner is still
    `merge_batch_generation_runtime_state(...)`, while replace mode is just
    "use the incoming state as-is"

  The refactor therefore narrowed that snapshot write boundary:
  - removed `BatchGenerationSnapshotWriteMode::resolve_runtime_state(...)`
  - `apply_to_active_model(...)` now resolves merge-vs-replace inline beside
    the quality-field reset and `updated_at` write
  - the insert branch in
    `write_batch_generation_runtime_snapshot(...)` now also resolves
    merge-vs-replace inline before creating the new active model
  - tests now assert the surviving merge owner directly and keep replace-mode
    behavior covered through the remaining write-mode paths

  This keeps snapshot runtime-state payload shape, merge semantics,
  replace-on-resume semantics, quality-field clearing, and persistence
  behavior unchanged while removing one more duplicate owner from the
  snapshot/runtime lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the single-chapter workflow lane, removed one more
  local launch-input transition owner, and then revalidated that stream and
  background generation entry contracts stayed unchanged.

  A focused audit showed that
  `build_single_generation_runtime_launch_input(...)` had already degraded
  into a thin file-local wrapper:
  - production code only consumed it inside
    `create_single_generation_stream_workflow(...)` and
    `start_owned_single_generation_background_workflow(...)`
  - it no longer owned an independent cross-call contract beyond destructuring
    `PreparedSingleChapterGenerationRequest` and assembling
    `SingleGenerationRuntimeLaunchInput`
  - the real owner boundaries are the stream/background workflow entrypoints
    that already decide how the prepared request is forwarded into runtime

  The refactor therefore narrowed that single-generation workflow boundary:
  - removed `build_single_generation_runtime_launch_input(...)`
  - stream workflow now destructures the prepared request inline and builds
    `SingleGenerationRuntimeLaunchInput` directly before launching SSE runtime
  - background workflow now does the same inline before task creation and
    runtime dispatch
  - tests now assert the surviving stream/background owner paths directly
    instead of the deleted transition wrapper

  This keeps single-chapter request preparation, background task payload
  shape, SSE stream launch behavior, runtime dispatch inputs, and error
  contracts unchanged while removing one more duplicate owner from the
  single-generation workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed in the single-chapter runtime-checkpoint lane, removed
  one more file-local plan wrapper, and then revalidated that checkpoint
  payloads for every stage stayed unchanged.

  A focused audit showed that `SingleGenerationSnapshotPlan` plus
  `SingleGenerationSnapshotStage::resolve_plan()` had already degraded into a
  local transition owner:
  - production code only used them through
    `build_single_generation_runtime_checkpoint_for_stage(...)`
  - the plan struct no longer owned an independent shared contract beyond
    forwarding stage constants into the final checkpoint JSON builder
  - the real semantic owner is the stage enum itself, because stage selection
    already determines phase/progress/status/event/message as one unit

  The refactor therefore narrowed that single-generation checkpoint boundary:
  - removed `SingleGenerationSnapshotPlan`
  - removed `SingleGenerationSnapshotStage::resolve_plan()`
  - `SingleGenerationSnapshotStage::build_checkpoint(...)` now resolves the
    stage constants inline and builds the final checkpoint payload directly
  - tests now assert payload results through the surviving public checkpoint
    builder instead of the deleted intermediate plan owner

  This keeps single-chapter checkpoint payload shape, stage vocabulary,
  progress values, event names, terminal semantics, and optional word-count
  behavior unchanged while removing one more duplicate owner from the
  single-generation runtime-checkpoint lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch runtime-checkpoint lane, removed one more
  shared-field base wrapper, and then revalidated that pending/running/
  terminal checkpoint payloads stayed unchanged.

  A focused audit showed that `build_batch_runtime_checkpoint_base(...)` had
  already degraded into a thin file-local helper:
  - production code only consumed it inside
    `build_batch_generation_runtime_checkpoint(...)` and
    `build_pending_batch_generation_runtime_checkpoint(...)`
  - it no longer owned an independent shared contract beyond copying the
    common JSON fields into a temporary object
  - the real semantic owners are still the specialized pending/runtime
    checkpoint builders together with the stage enum that selects between them

  The refactor therefore narrowed that batch checkpoint boundary:
  - removed `build_batch_runtime_checkpoint_base(...)`
  - `build_batch_generation_runtime_checkpoint(...)` now assembles its common
    JSON payload inline before applying `completed/total`
  - `build_pending_batch_generation_runtime_checkpoint(...)` now also
    assembles the pending JSON payload inline before optionally applying
    progress totals

  This keeps batch checkpoint payload shape, shared timestamp fields,
  pending/resume differences, completed/total behavior, stage vocabulary, and
  terminal checkpoint semantics unchanged while removing one more duplicate
  owner from the batch runtime-checkpoint lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed in the batch runtime-checkpoint lane, removed one more
  stage-local runtime builder, and then revalidated that started/progress/
  terminal checkpoint payloads stayed unchanged.

  A focused audit showed that
  `build_batch_generation_runtime_checkpoint(...)` had already degraded into a
  stage-local specialized helper:
  - production code only consumed it inside
    `BatchGenerationSnapshotStage::build_checkpoint(...)`
  - unlike the pending builder, it no longer served any cross-file shared
    contract
  - the real owner boundary is the stage enum itself, because the stage
    branch already decides runtime phase/progress/status/event/message and the
    `completed/total` checkpoint fields as one semantic unit

  The refactor therefore narrowed that batch checkpoint boundary:
  - removed `build_batch_generation_runtime_checkpoint(...)`
  - `BatchGenerationSnapshotStage::build_checkpoint(...)` now owns a local
    runtime-checkpoint builder closure for the running/completed/cancelled/
    failed branches
  - kept `build_pending_batch_generation_runtime_checkpoint(...)` intact
    because it is still consumed by
    `chapter_batch_generation_resume_task_command_service.rs` as a shared
    resume checkpoint contract

  This keeps batch runtime checkpoint payload shape, pending-vs-runtime owner
  boundaries, `completed/total` behavior, progress/status/event values, and
  terminal semantics unchanged while removing one more duplicate owner from
  the batch runtime-checkpoint lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch resume-command lane, removed one more
  file-local execution-plan wrapper, and then revalidated that resume
  eligibility, execution selection, and response payloads stayed unchanged.

  A focused audit showed that
  `ResumeExecutionPlan::from_command_state(...)` had already degraded into a
  thin local transition helper:
  - production code only consumed it inside
    `prepare_batch_generation_resume(...)`
  - the real shared semantic owner for chapter selection is
    `ResumeBatchGenerationCommandState::resolve_execution_selection()`
  - the remaining step from selection to `ResumeExecutionPlan` was only local
    request forwarding plus `target_word_count` normalization

  The refactor therefore narrowed that resume-command boundary:
  - removed `ResumeExecutionPlan::from_command_state(...)`
  - `prepare_batch_generation_resume(...)` now normalizes
    `target_word_count` and converts the resolved execution selection into the
    final `ResumeExecutionPlan` inline
  - tests now assert the surviving selection owner directly instead of the
    deleted local plan wrapper

  This keeps resume status gating, manual-review blocking, execution
  selection, target-word-count normalization, task reset behavior, and resume
  response payload shape unchanged while removing one more duplicate owner
  from the batch resume-command lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch runtime-state lane, removed one more
  mutation-plan persistence wrapper, and then revalidated that task-field
  mutation semantics and runtime checkpoint writes stayed unchanged.

  A focused audit showed that
  `BatchGenerationTaskMutationPlan::persist_for_task(...)` had already
  degraded into a thin local transition helper:
  - production code only consumed it through `BatchGenerationTaskStage`
    call sites inside this file
  - it no longer owned an independent reusable contract beyond "load task ->
    apply resolved mutation plan -> persist active model"
  - the real semantic owners are still
    `BatchGenerationTaskStage::resolve_mutation_plan(...)` and
    `BatchGenerationTaskMutationPlan::apply_to_active_model(...)`

  The refactor therefore narrowed that runtime-state boundary:
  - removed `BatchGenerationTaskMutationPlan::persist_for_task(...)`
  - `BatchGenerationTaskStage` now owns `persist_for_task(...)` directly,
    taking the stage inputs and resolving the mutation plan internally before
    persisting
  - runtime entrypoints now delegate persistence through the stage owner
    instead of first materializing a plan and then forwarding it into a second
    persistence wrapper

  This keeps batch task status transitions, timestamp semantics, error-message
  updates, current-chapter tracking, resume-reset writes, and downstream
  runtime checkpoint persistence unchanged while removing one more duplicate
  owner from the batch runtime-state lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:30 +08:00 checkpoint:
  this slice continued the same terminal runtime migration by folding the last
  top-level patch choreography into a more complete shared Rust terminal patch
  contract, so the entrypoint function now behaves much more like a thin
  contract assembler than a branch-by-branch semantics owner.

  A focused audit confirmed the next remaining owner split:
  - after the previous slice, five quality runtime sections were already
    patched through a shared owner, but the top-level terminal patch fields
    still lived inline inside `build_quality_gate_blocked_runtime_state_patch(...)`
  - `active_story_repair_payload` mutation was also still coordinated directly
    from the same entrypoint
  - that meant the builder had become thinner, but it still partially owned the
    terminal patch contract instead of delegating the full patch composition to
    shared helpers

  The refactor therefore completed the next real migration seam:
  - introduced a more complete shared terminal patch contract in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`:
      1. `build_manual_review_terminal_runtime_patch_contract(...)`
      2. `apply_terminal_quality_runtime_patch_contract(...)`
      3. `insert_terminal_active_story_repair_payload(...)`
  - rewired `build_quality_gate_blocked_runtime_state_patch(...)` so it now:
      1. builds top-level terminal patch fields from one shared contract helper
      2. delegates quality runtime section rewrites to the existing shared
         patch-semantics owner
      3. delegates `active_story_repair_payload` terminal mutation to a shared
         helper
  - added a focused runtime-state test that directly proves the more complete
    contract builder applies:
      - top-level terminal fields
      - shared quality section rewrites
      - active story-repair terminal mutation

  This is a real Python-to-Rust migration step because the batch quality
  terminal runtime patch is now substantially closer to a single shared Rust
  contract across:
  - top-level runtime patch fields
  - five quality runtime sections
  - active story-repair payload mutation
  - lower-level gate/history/context normalization

  The entrypoint function is materially closer to a thin Rust-owned assembly
  layer, instead of retaining Python-era responsibility for coordinating the
  terminal patch field-by-field.

  Explicitly not migrated in this slice:
  - persistence schema changes
  - non-terminal quality flows
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:26 +08:00 checkpoint:
  this slice continued the same runtime-patch migration by lifting the
  remaining five-layer terminal rewrite orchestration into a shared Rust patch
  semantics owner, instead of leaving `build_quality_gate_blocked_runtime_state_patch(...)`
  as a large coordinator that still replayed each branch manually.

  A focused audit confirmed the next remaining owner split:
  - after the previous slices, normalization for
    `quality_metrics_summary` / `latest_quality_metrics` /
    `quality_metrics_history` / `quality_metrics_summary_state` /
    `quality_history_context`
    had shared low-level helpers, but
    `build_quality_gate_blocked_runtime_state_patch(...)` still manually
    orchestrated each field one by one
  - that meant the “which terminal quality sections must be patched together”
    contract still lived inline inside the patch builder, even though the
    lower-level normalization steps were already shared

  The refactor therefore completed the next real migration seam:
  - introduced a higher-level patch semantics owner in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`:
      1. `apply_terminal_quality_runtime_patch_sections(...)`
      2. `insert_normalized_terminal_quality_payload_field(...)`
  - rewired `build_quality_gate_blocked_runtime_state_patch(...)` so the
    five-field terminal rewrite for:
      - `quality_metrics_summary`
      - `latest_quality_metrics`
      - `quality_metrics_history`
      - `quality_metrics_summary_state`
      - `quality_history_context`
    is now delegated to one shared patch-semantics owner
  - kept `active_story_repair_payload` on the existing shared terminal field
    helper, so the runtime patch builder is now much thinner and closer to a
    pure composition layer
  - added a focused runtime-state test that directly proves the shared patch
    owner applies manual-review semantics across all five quality runtime
    sections in one call

  This is a real Python-to-Rust migration step because the batch quality
  terminal runtime patch is now materially closer to a single Rust-owned
  pipeline:
  - top-level terminal patch assembly
  - five quality runtime section rewrites
  - shared nested normalization helpers
  - shared active story-repair terminal mutation

  The system is materially closer to a full Rust-owned terminal patch contract,
  rather than a large builder replaying Python-era branch choreography even
  after individual field normalizers were extracted.

  Explicitly not migrated in this slice:
  - persistence schema changes
  - non-terminal quality flows
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:23 +08:00 checkpoint:
  this slice continued the same runtime-persistence migration by collapsing the
  remaining terminal normalization logic for quality history/context into shared
  Rust owners, instead of keeping those transformations inline inside one large
  patch builder.

  A focused audit confirmed the next remaining owner split:
  - `build_quality_gate_blocked_runtime_state_patch(...)` still normalized the
    last item of `quality_metrics_history` inline
  - it still normalized `quality_history_context.recent_metrics[*]` inline and
    separately rebuilt `quality_gate_counts` /
    `recent_manual_review_count` / `recent_auto_repair_count`
  - those branches were already applying the same Rust-owned terminal decision,
    but the normalization rules were still embedded locally in the runtime
    patch builder instead of being owned by shared helper logic

  The refactor therefore completed the next real migration seam:
  - introduced shared terminal normalization helpers in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`:
      1. `normalize_terminal_quality_history(...)`
      2. `normalize_terminal_quality_history_context(...)`
  - rewired the `quality_metrics_history` branch inside
    `build_quality_gate_blocked_runtime_state_patch(...)` to consume the shared
    history helper instead of locally rewriting only the last metric
  - rewired the `quality_history_context` branch inside the same patch builder
    to consume the shared context helper instead of locally rewriting metrics
    plus rebuilding terminal counters/count maps inline
  - added a focused runtime-state test proving the shared history/context
    helpers emit the expected `failed/manual_review/<label>` contract and
    recompute counts consistently

  This is a real Python-to-Rust migration step because the batch quality
  terminal persistence semantics now have shared Rust owners across:
  - nested quality-gate normalization
  - failed chapter entry persistence
  - active story-repair payload terminal mutation
  - `quality_metrics_history` terminal rewrite
  - `quality_history_context` terminal rewrite and counter rebuild

  The runtime write path is materially closer to a single Rust-owned terminal
  semantics pipeline, rather than one large patch builder replaying Python-era
  transformations branch by branch.

  Explicitly not migrated in this slice:
  - broader persistence schema changes
  - non-terminal quality updates
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:12 +08:00 checkpoint:
  this slice fixed a deeper batch snapshot owner gap: Rust terminal quality
  patches now propagate onto the snapshot’s first-class quality columns, not
  only into `workflow_runtime_state`.

  A focused read/write audit confirmed the higher-value owner bug:
  - previous slices had already made Rust normalize terminal manual-review
    semantics inside `workflow_runtime_state`
  - but read-side consumers such as
    `BatchGenerationQualityStatusContext::from_snapshot_and_runtime_state(...)`
    resolve:
    - `snapshot.latest_quality_metrics`
    - `snapshot.quality_metrics_summary`
    directly from snapshot columns, not from the runtime-state shadow copies
  - meanwhile `chapter_batch_generation_snapshot_service.rs` merge writes only
    updated `workflow_runtime_state`; it did not sync those top-level quality
    columns from incoming runtime patches
  - that meant Rust could appear to own terminal quality-gate semantics in the
    runtime-state JSON while the actual read-side source of truth still served
    stale retry-era summary/metrics payloads from the snapshot columns

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_snapshot_service.rs`
    so `BatchGenerationSnapshotWriteMode::apply_to_active_model(...)` now also
    syncs optional quality fields from incoming runtime-state payload keys into
    the snapshot’s first-class columns:
    - `latest_quality_metrics`
    - `quality_metrics_history`
    - `quality_metrics_summary`
  - added
    `apply_optional_quality_field(...)` so merge/replace writes can explicitly
    project runtime-patch quality payloads onto the real snapshot read-side
    columns, including `null -> clear` behavior
  - added focused tests covering:
    - merge-mode syncing of quality columns
    - replace-mode syncing of quality columns after clear
    - direct helper behavior for `null` field clearing

  This is a real Python-to-Rust migration step because Rust no longer only
  mutates a shadow runtime JSON blob; it now updates the same snapshot columns
  that current read-side quality-status consumers actually prioritize. That
  closes a real owner gap between runtime write-side normalization and read-side
  terminal semantics resolution.

  Explicitly not migrated in this slice:
  - fresh quality summary recomputation from new generated content
  - candidate/reviser repair pipeline ownership
  - broader cutover of snapshot quality ownership away from existing analysis
    write paths that still populate these columns

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:18 +08:00 checkpoint:
  this slice moved one more fresh-quality execution owner from Python-shaped
  gaps into Rust: batch follow-up analysis runtime snapshots now carry fresh
  `latest_quality_metrics`, not only a derived quality summary.

  A focused analysis-success audit confirmed the next higher-value owner gap:
  - Rust already rebuilt a fresh batch `quality_metrics_summary` from the
    latest `plot_analysis` row after chapter follow-up analysis completed
  - but the same runtime path did not build and persist a companion fresh
    `latest_quality_metrics` payload
  - that meant the batch runtime could advance with:
    - updated summary-level repair guidance
    - but missing fresh latest metrics for downstream read-side/runtime owners
  - the resulting shape was still weaker than the read-side contract used
    elsewhere, where both `quality_metrics_summary` and
    `latest_quality_metrics` may participate in later interpretation

  The refactor therefore completed the next real Rust migration seam:
  - added
    `build_current_chapter_latest_quality_metrics_from_plot_analysis(...)` to
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so Rust can reconstruct a fresh latest-metrics payload directly from the
    newest persisted `plot_analysis` record
  - extended
    `build_batch_generation_runtime_state_payload_from_current_quality(...)`
    so the post-analysis runtime snapshot now persists:
    - fresh `quality_metrics_summary`
    - fresh `latest_quality_metrics`
    together through the same Rust owner path
  - rewired
    `build_batch_generation_current_chapter_quality_runtime_snapshot(...)`
    to include that fresh latest-metrics payload in the snapshot patch written
    immediately after analysis success
  - added focused contract coverage in
    `should_build_batch_runtime_state_payload_with_fresh_latest_quality_metrics`

  This is a real Python-to-Rust migration step because Rust now owns another
  piece of post-analysis execution data, not only a summary projection. The
  batch runtime snapshot can now expose both fresh summary guidance and fresh
  latest metrics from the Rust-owned analysis-success path.

  Explicitly not migrated in this slice:
  - a full fresh quality-column persistence contract beyond the existing
    snapshot synchronization path
  - retry decision recomputation from newly refreshed latest metrics
  - candidate/reviser repair pipeline ownership

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:07 +08:00 checkpoint:
  this slice extended the same terminal quality-gate owner path into the
  quality summary payloads themselves, so Rust now normalizes
  `quality_metrics_summary` and `latest_quality_metrics` when batch generation
  stops for terminal manual review.

  A focused runtime audit confirmed the remaining owner gap:
  - previous slices had already aligned terminal semantics across:
    - top-level runtime snapshot fields
    - `active_story_repair_payload`
    - `quality_history_context.recent_metrics[*].quality_gate`
    - `quality_history_context` aggregate counters
  - but `quality_metrics_summary` and `latest_quality_metrics` could still
    preserve the original retry-era quality gate object produced from fresh
    analysis, for example:
    - `status = "warning"`
    - `decision = "auto_repair"`
    - `label = "建议继续修复"`
  - that meant the same runtime snapshot could still mix:
    - terminal manual-review runtime state
    - retry-era summary-level quality gate semantics

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `build_quality_gate_blocked_runtime_state_patch(...)` in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so the terminal manual-review patch now also reloads and rewrites:
    - `quality_metrics_summary.quality_gate`
    - `latest_quality_metrics.quality_gate`
    to:
    - `status = "failed"`
    - `decision = "manual_review"`
    - `label = <terminal manual review label>`
  - added
    `normalize_terminal_quality_gate_payload(...)` as a focused helper so this
    summary-level normalization stays explicit and local to the runtime owner
    path
  - expanded the existing terminal patch contract test so summary-level gate
    normalization is asserted together with repair payload and history-context
    normalization

  This is a real Python-to-Rust migration step because Rust now owns another
  slice of write-side truth for the batch quality-gate terminal lane. The
  runtime snapshot no longer relies on readers to reconcile terminal task state
  with retry-era summary payloads produced earlier in the analysis pipeline.

  Explicitly not migrated in this slice:
  - fresh quality summary recomputation from newly generated chapter output
  - candidate/reviser repair pipeline ownership
  - full parity for every Python-side quality-gate event/progress payload

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:04 +08:00 checkpoint:
  this slice extended the same terminal quality-gate normalization into the
  derived aggregate fields of `quality_history_context`, so Rust now aligns the
  nested per-metric gate semantics and the history-level gate counters/counts
  through the same terminal write path.

  A focused runtime audit confirmed the remaining owner gap:
  - the previous slice already rewrote
    `quality_history_context.recent_metrics[*].quality_gate` to terminal
    `manual_review` semantics
  - but aggregate fields derived from those same recent metrics could still
    retain retry-era values, for example:
    - `quality_gate_counts.auto_repair = 1`
    - `recent_manual_review_count = 0`
    - `recent_auto_repair_count = 1`
  - that meant the same runtime snapshot could simultaneously claim:
    - per-metric gate state = terminal manual review
    - aggregate gate counts = retryable auto repair
  - so one more owner seam remained: Rust needed to normalize not only the
    nested gate payloads, but also the aggregate counters derived from them

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `build_quality_gate_blocked_runtime_state_patch(...)` in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so after rewriting `recent_metrics[*].quality_gate`, Rust now also
    recalculates and rewrites:
    - `quality_gate_counts`
    - `recent_manual_review_count`
    - `recent_auto_repair_count`
    from the same terminalized recent metric decisions
  - added
    `increment_quality_gate_terminal_counts(...)` as a focused helper to keep
    this terminal normalization explicit and local to the runtime owner path
  - expanded the existing terminal patch contract test so aggregate fields are
    asserted together with top-level, repair-payload, and recent-metric gate
    semantics

  This is a real Python-to-Rust migration step because Rust now owns another
  slice of write-side truth for the batch quality-gate terminal lane. The
  snapshot no longer relies on readers to reconcile terminalized recent metrics
  with stale retry-era aggregate counters.

  Explicitly not migrated in this slice:
  - fresh quality summary recomputation from newly produced chapter output
  - candidate/reviser repair pipeline ownership
  - full parity for every Python-side quality-gate event/progress payload

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:01 +08:00 checkpoint:
  this slice extended the same terminal quality-gate owner path into
  `quality_history_context`, so Rust now normalizes the nested recent quality
  history gate semantics when batch generation stops for manual review.

  A focused runtime audit confirmed the remaining owner gap:
  - the previous slice already aligned:
    - top-level runtime snapshot terminal fields
    - nested `active_story_repair_payload` terminal fields
  - but `quality_history_context.recent_metrics[*].quality_gate` could still
    preserve retry-era semantics such as:
    - `status = "warning"`
    - `decision = "auto_repair"`
    - `label = "建议继续修复"`
  - that meant the outer runtime snapshot could declare terminal
    `manual_review / quality_blocked` while recent quality history still looked
    like the chapter was only in a retryable repair state

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `build_quality_gate_blocked_runtime_state_patch(...)` in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so when a terminal manual-review stop is written, Rust now also:
    - reloads `quality_history_context` from runtime state
    - rewrites each `recent_metrics[*].quality_gate` payload to:
      - `status = "failed"`
      - `decision = "manual_review"`
      - `label = <terminal manual review label>`
  - kept the patch compatibility-safe:
    - no full recomputation of history aggregation
    - only the nested terminal gate semantics are normalized in place
  - expanded focused contract coverage in
    `should_build_quality_gate_blocked_runtime_state_patch_with_terminal_repair_payload`
    so top-level fields, repair payload fields, and history-context gate fields
    are asserted together

  This is a real Python-to-Rust migration step because Rust now owns one more
  layer of terminal runtime-state normalization for the batch quality-gate
  lane. The snapshot no longer needs downstream readers to reconcile a
  terminal task state with retry-era recent metric gate semantics.

  Explicitly not migrated in this slice:
  - recomputation of aggregate count fields such as
    `recent_manual_review_count` / `recent_auto_repair_count`
  - candidate/reviser repair pipeline ownership
  - full parity for all Python-side quality-gate progress/event payloads

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 10:58 +08:00 checkpoint:
  this slice pushed the same quality-gate terminal lane one step deeper into
  Rust-owned runtime state: when batch generation stops for manual review,
  Rust now also rewrites the nested active repair payload to match the terminal
  state instead of leaving stale retry-era repair semantics inside it.

  A focused runtime audit confirmed the remaining owner drift:
  - the previous slice already made Rust persist an explicit
    `manual_review / quality_blocked` failed-chapter entry
  - the top-level runtime snapshot had already been rewritten to:
    - `quality_gate_decision = "manual_review"`
    - `quality_gate_label = <terminal label>`
    - `phase = "quality_blocked"`
  - but `active_story_repair_payload` could still retain stale nested fields
    from the prior retry lane, for example:
    - `quality_gate_decision = "auto_repair"`
    - `quality_gate_label = "建议继续修复"`
    - `phase = "repair_pending"`
  - that meant Rust could declare a terminal quality-gate state at the
    snapshot top level while still preserving a retry-shaped repair payload
    underneath, leaving one more terminal owner gap for read-side consumers
    and future runtime recovery logic

  The refactor therefore completed the next real Rust migration seam:
  - added
    `build_quality_gate_blocked_runtime_state_patch(...)` to
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so the quality-gate blocked runtime patch now:
    - updates the top-level analysis/quality-gate terminal fields
    - reloads the current `active_story_repair_payload` from runtime state
    - rewrites its nested terminal semantics to:
      - `quality_gate_decision = "manual_review"`
      - `quality_gate_label = <terminal label>`
      - `phase = "quality_blocked"`
  - rewired
    `maybe_stop_batch_generation_for_quality_gate_manual_review(...)`
    to use that shared terminal-state patch builder instead of only writing
    the top-level fields inline
  - added focused contract coverage in
    `should_build_quality_gate_blocked_runtime_state_patch_with_terminal_repair_payload`

  This is a real Python-to-Rust migration step because Rust no longer only
  decides to stop and persist a failed task. It now also owns one more layer of
  runtime-state normalization for the terminal repair lane itself, keeping the
  nested repair payload aligned with the same terminal quality-gate meaning
  that the runtime already writes at the top level.

  Explicitly not migrated in this slice:
  - deeper terminal cleanup/parity for `quality_history_context`
  - candidate/reviser repair pipeline ownership
  - full parity for all Python-side quality-gate progress/event payloads

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 10:55 +08:00 checkpoint:
  this slice moved one more quality-gate terminal-state owner from
  Python-shaped interpretation into Rust-owned persisted runtime facts:
  exhausted auto-repair now lands as an explicit manual-review failed entry,
  not only as a read-side inference.

  A focused runtime audit confirmed the remaining owner gap:
  - Rust already owned the runtime decision boundary for:
    - `manual_review -> stop`
    - `auto_repair -> retry same chapter`
    - retry-time repaired compat/provider/research input restoration
  - read-side quality semantics already knew how to interpret exhausted
    `auto_repair` budget as an effective terminal `manual_review`
  - but the runtime write path still persisted the terminal failure through the
    same generic failed-chapter entry shape used by non-quality-gate failures
  - that meant one important runtime fact stayed implicit:
    after retries are exhausted, the persisted failed entry itself did not
    explicitly declare:
    - `quality_gate_decision = "manual_review"`
    - `quality_gate_label = <exhausted repair label>`
    - `phase = "quality_blocked"`

  The refactor therefore completed the next real Rust migration seam:
  - added
    `build_quality_gate_blocked_failed_chapter_entry(...)` to
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so quality-gate terminal failures now carry explicit terminal semantics in
    the persisted failed-chapter entry
  - added
    `BatchGenerationRuntimePersistencePlan::failed_quality_gate_blocked(...)`
    so the quality-gate blocked lane can keep the same task/checkpoint failure
    path while specializing the failed entry contract
  - rewired
    `maybe_stop_batch_generation_for_quality_gate_manual_review(...)`
    to use that explicit quality-gate terminal persistence helper instead of
    the generic failure builder
  - added focused contract coverage in
    `should_build_quality_gate_blocked_failed_chapter_entry_with_terminal_semantics`

  This is a real Python-to-Rust migration step because Rust no longer relies
  only on retry-budget-aware readers to reinterpret an exhausted auto-repair
  failure as manual review. The runtime write path itself now persists that
  terminal quality-gate meaning into the failed entry, which narrows one more
  semantic gap between runtime execution ownership and read-side reporting.

  Explicitly not migrated in this slice:
  - full rewrite of nested `active_story_repair_payload` terminal cleanup on
    exhausted auto-repair
  - candidate/reviser repair pipeline ownership
  - Python parity for every quality-gate-related event transport message

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 10:50 +08:00 checkpoint:
  this slice closed the next real batch retry-runtime seam from Python-shaped
  semantics into Rust-owned success-state cleanup: retry success now explicitly
  clears stale repair-pending control fields from the runtime snapshot.

  A focused runtime audit confirmed the owner drift:
  - Rust had already taken ownership of `manual_review -> stop`,
    `auto_repair -> retry same chapter`, and retry-time
    provider/research/prompt input restoration
  - but after a repaired retry succeeded, the batch runtime snapshot still only
    wrote generic "analysis completed" fields and did not explicitly override
    prior retry-control markers such as:
    - `phase = "repair_pending"`
    - `quality_gate_decision = "auto_repair"`
    - `quality_gate_label = <retry label>`
  - because runtime snapshot writes are merge-based, those stale fields could
    survive into the post-success state unless Rust explicitly cleared them
  - that meant the execution owner gap was no longer "can Rust retry", but
    "can Rust authoritatively declare that the retry-repair lane has completed"

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so `build_batch_generation_analysis_completed_snapshot(...)` now explicitly
    clears the top-level retry-control fields on analysis success:
    - `quality_gate_decision = null`
    - `quality_gate_label = null`
    - `phase = null`
  - added contract coverage in
    `should_build_completed_batch_generation_analysis_snapshot`
    to keep that cleanup behavior explicit and regression-resistant

  This is a real Python-to-Rust migration step because Rust now owns not only
  the decision to enter the repair retry lane, but also the decision to exit
  that lane cleanly after success. The batch runtime snapshot no longer relies
  on downstream readers to infer whether `repair_pending` is stale; Rust
  actively writes the terminal-success cleanup boundary itself.

  Explicitly not migrated in this slice:
  - exhausted auto-repair -> terminal manual-review write-state cleanup parity
  - full candidate/reviser repair plan ownership
  - retry-success-time refresh of deeper nested repair payload internals beyond
    the top-level control-state cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 06:05 +08:00 checkpoint:
  this slice moved the generated-chapter history persistence seam from Python
  into Rust on the real chapter generation success path.

  A focused runtime audit confirmed the next owner bottleneck:
  - Rust batch/runtime logic had already started consuming and refreshing
    story-repair runtime state
  - but `backend-rs/src/services/chapter_generation_runtime_service.rs`
    still only updated the `chapters` table after AI generation success
  - read-side quality/runtime consumers (`chapter_quality_metrics_query_service`
    and downstream story-repair restore logic) rely on `generation_history`
    payloads, especially `generated_content.quality_metrics`
  - without a Rust-owned generation-history write contract, later migration of
    current-chapter quality refresh and quality-gate retry ownership remains
    blocked

  The refactor therefore completed the next real Rust migration seam:
  - rewired
    `backend-rs/src/services/chapter_generation_runtime_service.rs`
    so chapter content persistence and generated history insertion now happen
    inside the same Rust-owned transaction
  - introduced a Rust-owned `chapter_generation_v1` generation-history model
    contract for direct chapter generation success payloads
  - added a compatible `generated_content` envelope with:
      1. `content`
      2. `quality_metrics: null`
      3. `content_applied: true`
      4. `attempt_state: "generated_from_runtime"`
  - kept the scope deliberately minimal: this slice does not fabricate quality
    metrics it does not yet own, but it ensures Rust now owns the persistence
    boundary that future fresh quality metrics can attach to

  This is a real Python-to-Rust migration step because Rust is no longer only
  writing chapter body state; it now also produces the runtime history artifact
  that downstream quality/status/repair readers consume. That shifts the
  generation-success persistence owner boundary toward Rust and unblocks the
  next migration seam for fresh current-chapter quality state.

  Explicitly not migrated in this slice:
  - fresh current-chapter quality metrics computation or persistence
  - current-chapter-quality branch of success-path story-repair refresh
  - full quality-gate retry / manual-review execution ownership

  Validation for this slice:
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
- 2026-05-30 06:34 +08:00 checkpoint:
  this slice pushed the batch success path one step deeper into real
  Python-to-Rust runtime ownership by letting Rust feed current-chapter quality
  context back into the batch runtime snapshot after analysis completes.

  A focused owner audit confirmed the remaining gap after the previous slice:
  - Rust already owned chapter-content persistence and generation-history write
    on successful chapter generation
  - Rust batch runtime already owned retry/backoff, prompt repair-state restore,
    and recent-history-derived story-repair refresh
  - but after current-chapter follow-up analysis succeeded, Rust still only
    persisted "analysis completed" progress metadata into the batch snapshot
  - that meant the next chapter's runtime repair input still could not consume
    any fresh current-chapter quality context produced by Rust itself

  The refactor therefore completed the next real runtime seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    with a Rust-owned current-chapter-quality summary builder sourced from the
    latest persisted `plot_analysis`
  - added a minimal batch quality summary contract that derives:
      1. `overall_score`
      2. `repair_guidance.summary`
      3. `repair_targets`
      4. `preserve_strengths`
      5. `focus_areas`
      6. minimal `quality_gate`
      7. `quality_runtime_context.recent_metrics`
  - rewired batch follow-up analysis success so Rust now:
      1. loads the latest chapter `plot_analysis`
      2. builds a current-chapter quality summary
      3. merges that summary into `active_story_repair_payload`
      4. persists the refreshed runtime snapshot before later success-path
         story-repair evolution continues

  This is a real Python-to-Rust migration step because Rust no longer stops at
  "analysis finished"; it now converts its own current-chapter analysis output
  into next-step runtime state that downstream batch prompt/repair logic can
  consume. That starts moving the `current_chapter_quality` success-path owner
  from Python into Rust.

  Explicitly not migrated in this slice:
  - full Python-parity `compute_story_quality_metrics` logic
  - fresh quality metrics write-back into `generation_history.generated_content`
  - full quality-gate retry / manual-review execution ownership

  Validation for this slice:
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
- 2026-05-30 06:56 +08:00 checkpoint:
  this slice moved fresh current-chapter quality persistence one step further
  by writing Rust-generated analysis quality payloads back into
  `generation_history`, instead of leaving them only in `plot_analysis` and
  batch runtime snapshot state.

  A focused seam audit after the previous slice confirmed the next blocker:
  - Rust batch runtime could already derive a minimal current-chapter quality
    summary from `plot_analysis` and feed that summary back into batch snapshot
  - but read-side quality consumers still primarily resolve
    `latest_quality_metrics` from `generation_history.generated_content` and
    candidate attempts
  - that meant Rust had started owning current-chapter quality inside runtime
    state, while the durable chapter-quality read contract was still partially
    behind the old Python persistence pattern

  The refactor therefore completed the next real persistence seam:
  - exposed reusable helpers in
    `backend-rs/src/services/chapter_generation_runtime_service.rs`
    so Rust can rebuild the `chapter_generation_v1` history payload with a real
    `quality_metrics` object instead of only `null`
  - added
    `update_latest_generated_chapter_history_quality_metrics(...)` to locate the
    latest generated-chapter history row and overwrite its `generated_content`
    payload with Rust-owned quality metrics
  - added a minimal quality payload builder in
    `backend-rs/src/services/chapter_analysis_runtime_service.rs` sourced from
    analysis scores/suggestions so Rust now derives:
      1. `overall_score`
      2. per-dimension scores
      3. `repair_guidance`
      4. `quality_gate`
      5. `quality_runtime_context`
  - rewired chapter analysis persist flow so, after plot analysis/memory sync
    succeeds, Rust writes that quality payload back into the latest
    `chapter_generation_v1` history row for the same chapter

  This is a real Python-to-Rust migration step because fresh chapter quality is
  no longer trapped in Rust-only side tables or transient batch snapshot state;
  it is now persisted into the same durable history channel that Rust quality
  readers already consume. That reduces another remaining Python ownership edge
  around quality-state durability and read-model input.

  Explicitly not migrated in this slice:
  - full Python-parity story runtime contract serialization
  - complete `chapter_generation_quality_v1` envelope parity
  - full quality-gate retry / manual-review execution owner

  Validation for this slice:
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved back to the single-generation runtime-state lane, removed
  one more mutation-plan persistence wrapper, and then revalidated that
  single-chapter task-field mutations and runtime checkpoint writes stayed
  unchanged.

  A focused audit showed that
  `SingleGenerationTaskMutationPlan::persist_for_task(...)` had the same
  degradation pattern as the batch runtime-state lane:
  - production code only consumed it through
    `SingleGenerationTaskStage::persist_for_task(...)`
  - it no longer owned an independent reusable contract beyond
    "load task -> apply resolved mutation plan -> persist active model"
  - the real semantic owners are still
    `SingleGenerationTaskStage::resolve_mutation_plan(...)` and
    `SingleGenerationTaskMutationPlan::apply_to_active_model(...)`

  The refactor therefore narrowed that single-generation runtime-state
  boundary:
  - removed `SingleGenerationTaskMutationPlan::persist_for_task(...)`
  - `SingleGenerationTaskStage::persist_for_task(...)` now loads the task,
    resolves the mutation plan internally, applies it to the active model, and
    persists in one owner path
  - runtime outcome transitions still delegate through the stage owner without
    changing status/timestamp/error/current-chapter semantics

  This keeps single-chapter task status transitions, timestamp semantics,
  error-message updates, completed-chapter tracking, current-chapter writes,
  and downstream runtime checkpoint persistence unchanged while removing one
  more duplicate owner from the single-generation runtime-state lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed in the single-generation workflow lane, removed one more
  single-consumer terminal-event wrapper, and then revalidated that SSE
  terminal ordering and payload shapes stayed unchanged.

  A focused audit showed that
  `emit_owned_single_generation_stream_terminal_event(...)` had already
  degraded into a one-consumer wrapper:
  - production code only consumed it inside
    `launch_owned_single_chapter_generation_stream(...)`
  - the real owner boundary is the stream-launch function that already owns
    the full SSE event sequence from start/preparing/generating through the
    final success-or-error emission
  - the standalone terminal helper no longer carried an independent stream
    contract beyond forwarding the runtime result into a local event sequence

  The refactor therefore narrowed that single-generation workflow boundary:
  - removed `emit_owned_single_generation_stream_terminal_event(...)`
  - `launch_owned_single_chapter_generation_stream(...)` now handles success
    and failure terminal SSE emission inline beside the earlier progress-event
    sends
  - tests now validate the surviving stream owner paths and no longer depend
    on the deleted wrapper

  This keeps single-generation SSE terminal event ordering, success payload
  shape, failure error event shape, runtime dispatch flow, and stream owner
  behavior unchanged while removing one more duplicate owner from the
  single-generation workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice stayed in the single-generation runtime-outcome lane, removed
  one more file-local checkpoint forwarding wrapper, and then revalidated
  that runtime stage writes stayed unchanged.

  A focused audit showed that
  `SingleGenerationRuntimeOutcome::persist_checkpoint(...)` had already
  degraded into a file-local forwarding helper:
  - production code only consumed it inside
    `SingleGenerationRuntimeOutcome::prepare_runtime(...)` and
    `SingleGenerationRuntimeOutcome::persist(...)`
  - it no longer owned an independent contract beyond forwarding
    `(stage, chapter_id, chapter_number, word_count)` into
    `build_single_generation_runtime_checkpoint_for_stage(...)` plus
    `upsert_batch_generation_runtime_snapshot(...)`
  - the real owner boundaries are the runtime-outcome transition methods that
    decide when each checkpoint stage should be written

  The refactor therefore narrowed that single-generation runtime-outcome
  boundary:
  - removed `SingleGenerationRuntimeOutcome::persist_checkpoint(...)`
  - `prepare_runtime(...)` now writes the preparing and generating checkpoints
    inline beside the preparing task-state mutation
  - `persist(...)` now writes finalizing/completed/failed checkpoints inline
    beside the completed/failed task-state mutations

  This keeps single-generation runtime checkpoint payload shape, stage order,
  success/failure branching, task-state mutation ordering, and snapshot
  persistence behavior unchanged while removing one more duplicate owner from
  the single-generation runtime-outcome lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice returned to the batch snapshot lane, removed one more
  single-consumer active-model builder, and then revalidated that new-snapshot
  insert payloads stayed unchanged.

  A focused audit showed that
  `build_new_batch_generation_snapshot_active_model(...)` had already
  degraded into a one-consumer builder:
  - production code only consumed it inside
    `write_batch_generation_runtime_snapshot(...)` when no existing snapshot
    row was found
  - it no longer owned an independent reusable contract beyond copying the
    insert fields into `batch_generation_snapshot::ActiveModel`
  - the real owner boundary is the write function itself, because it already
    decides merge-vs-replace runtime state, insert-vs-update path, and insert
    timestamps together

  The refactor therefore narrowed that snapshot boundary:
  - removed `build_new_batch_generation_snapshot_active_model(...)`
  - `write_batch_generation_runtime_snapshot(...)` now builds the new active
    model inline in the insert branch beside the resolved runtime-state write
  - tests now assert the surviving new-snapshot active-model contract directly
    without depending on the deleted builder helper

  This keeps batch snapshot insert payload shape, quality-field defaults,
  merge-vs-replace runtime-state semantics, and created/updated timestamp
  writes unchanged while removing one more duplicate owner from the batch
  snapshot lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_snapshot_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 09:xx +08:00 checkpoint:
  this slice moved to the batch owned-task query lane, removed one more
  optional-to-required wrapper, and then revalidated that task-not-found and
  internal error contracts stayed unchanged at the call sites.

  A focused audit showed that `load_required_owned_task(...)` had already
  degraded into a thin transition helper:
  - the real data owner is `load_owned_task(...)`, which performs the actual
    lookup plus user-ownership filter
  - `load_required_owned_task(...)` only forwarded the `Option` result into a
    `TaskNotFound` / `Internal` error shape
  - its production call sites already had their own domain-level error enums,
    so keeping the required wrapper as a separate layer no longer reduced any
    meaningful ownership complexity

  The refactor therefore narrowed that owned-task query boundary:
  - removed `load_required_owned_task(...)`
  - `chapter_batch_generation_cancel_service.rs` now converts the optional
    owned-task query result into the existing task error shape inline
  - `chapter_batch_generation_write_workflow_service.rs` now does the same for
    resume-owned-batch workflow loading
  - `load_owned_task(...)` remains the shared owner of database lookup plus
    ownership filtering semantics

  This keeps owned-task lookup behavior, `TaskNotFound` vs `Internal` error
  mapping, cancel workflow error shape, and resume write-workflow task error
  behavior unchanged while removing one more duplicate owner from the batch
  query lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice returned to the batch read/stream seam, moved the shared
  read-context to stream-state projection into the read-context owner, and
  then revalidated that route and SSE behavior stayed unchanged.

  A focused audit showed that the stream-state projection had become a thin
  repeated transition:
  - `chapter_batch_generation.rs` built the initial SSE state by manually
    passing `context.task` plus `context.workflow_runtime_state.as_ref()` into
    `BatchGenerationStreamState::from_task_state(...)`
  - `chapter_batch_generation_status_stream_service.rs` repeated the same
    conversion after reloading read context during poll iterations
  - the real data owner is `BatchGenerationReadContext`, because it already
    owns the combined task row plus optional runtime snapshot state

  The refactor therefore narrowed that read/stream boundary:
  - added `BatchGenerationReadContext::into_stream_state(self)`
  - `chapter_batch_generation.rs` now derives the initial stream state through
    the read-context owner instead of rebuilding the projection inline
  - `chapter_batch_generation_status_stream_service.rs` now does the same when
    converting reloaded context into the next poll state
  - focused tests now assert the surviving owner contract through
    `BatchGenerationReadContext::into_stream_state()`

  This keeps batch status route payloads, SSE event shape/order, poll timeout
  behavior, task-not-found stream error behavior, and stream semantic
  resolution unchanged while removing one more duplicated owner from the batch
  read/stream lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 11:08 +08:00 checkpoint:
  this slice stayed on the batch status-stream-event seam, removed one
  stream-event-only progress helper, and then revalidated that stream-event
  resolution and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_event_service.rs` still kept one
  low-signal event-local helper:
  - `BatchGenerationStreamState::events()` already owned the true event-batch
    assembly boundary
  - `BatchGenerationStreamState::progress_event()` no longer exposed any
    independent seam to other modules; it only built the progress payload
    before immediate consumption by that same `events()` owner
  - keeping that helper left one extra event-only owner inside the
    stream-event boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that status-stream-event boundary:
  - removed `BatchGenerationStreamState::progress_event()`
  - inlined progress-event payload assembly directly into
    `BatchGenerationStreamState::events()`

  This keeps progress-event payload shape, terminal-event ordering, event-batch
  resolution, cursor advancement semantics, and downstream chapter-batch
  stream behavior unchanged while making the event seam more cutover-ready.
  Smoke comparison and rollback review are easier because the event-batch
  owner now directly owns the trivial progress payload it emits immediately in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 11:03 +08:00 checkpoint:
  this slice stayed on the batch status-stream seam, removed two stream-local
  system-event payload helpers, and then revalidated that stream polling
  behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service.rs` still kept two
  low-signal local payload helpers:
  - `task_not_found_stream_payload()` only built one error payload before
    immediate `Event::data(...)` emission in the same stream owner
  - `timed_out_stream_payload()` only built one timeout payload before
    immediate `Event::data(...)` emission in that same stream owner
  - keeping both helpers left extra stream-only payload owners inside the
    polling boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that status-stream boundary:
  - removed `task_not_found_stream_payload()`
  - removed `timed_out_stream_payload()`
  - inlined both system-event payload assemblies directly into the stream
    owner and aligned focused tests with the direct stream-owned contract

  This keeps stream error payload shape, polling close behavior, timeout
  semantics, stream-state conversion, and downstream chapter-batch stream
  behavior unchanged while making the stream seam more cutover-ready. Smoke
  comparison and rollback review are easier because the stream owner now
  directly owns the trivial system-event payloads it emits immediately in that
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 10:58 +08:00 checkpoint:
  this slice stayed on the batch read-context seam, removed one remaining
  read-side failed-chapters carrier split, and then revalidated that
  read-side payload behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_read_context_service.rs` still kept one
  low-signal tuple-carried projection:
  - `BatchGenerationReadContext::into_payload_parts()` already owned the true
    shared read-side payload boundary
  - `failed_chapters` no longer exposed any independent seam to other modules;
    it was only being split out as its own tuple member before immediate
    status-payload assembly by the same read-context owner
  - keeping that extra tuple member left one more read-only carrier split
    inside the payload boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that read-side payload boundary:
  - changed `into_payload_parts()` to return only `(task, payload)`
  - moved `failed_chapters` lookup directly into
    `into_status_task_payload()` from the returned task projection
  - adjusted focused tests to assert the direct task-owned failed-chapters
    contract instead of the removed tuple-carried projection

  This keeps read-side task payload shape, failed-chapter exposure in status
  payloads, active payload omissions, stream-state conversion, quality context
  insertion, and error mapping unchanged while making the read seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  read-context owner now directly consumes the task-owned failed-chapters
  state instead of carrying it through an extra tuple split in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 10:55 +08:00 checkpoint:
  this slice stayed on the single-generation prepare/background seam, removed
  the intermediate task-chapter payload helper, and then revalidated that
  single-generation background assembly and workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_prepare_service.rs` still kept one low-signal
  local projection helper:
  - `SingleChapterGenerationTarget::background_task_active_model(...)`
    already owned the true background task active-model boundary
  - `SingleChapterGenerationTarget::task_chapter_payload()` no longer exposed
    any independent seam to other modules; it only projected the chapter id /
    number / title payload before immediate consumption by that same
    background task active-model owner
  - keeping that wrapper left one extra background-only projection owner
    inside the prepare boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single-generation prepare boundary:
  - removed `SingleChapterGenerationTarget::task_chapter_payload()`
  - moved chapter task-payload assembly directly into
    `SingleChapterGenerationTarget::background_task_active_model(...)`
  - updated focused prepare tests to assert the background task active-model
    owner contract directly

  This keeps single-generation background task payload shape, checkpoint
  persistence inputs, response payload shape, workflow runtime input, and
  downstream single-generation workflow behavior unchanged while making the
  background seam more cutover-ready. Smoke comparison and rollback review are
  easier because the background task active-model owner now directly owns the
  trivial chapter-payload projection it consumes immediately in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 10:49 +08:00 checkpoint:
  this slice stayed on the batch create workflow seam, removed two
  create-only projection helpers, and then revalidated that batch create
  workflow behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept two
  low-signal local projection helpers:
  - `batch_generation_create_total_chapters(...)` only returned
    `chapters_to_generate.len() as i32`
  - `batch_generation_create_chapter_ids(...)` only projected chapter target
    ids before immediate chapter-id payload assembly, runtime-input assembly,
    and focused test assertions
  - keeping both helpers left extra create-only projection owners inside the
    workflow boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch create workflow boundary:
  - removed `batch_generation_create_total_chapters(...)`
  - removed `batch_generation_create_chapter_ids(...)`
  - inlined total-chapter and chapter-id projection directly into the create
    workflow owner and focused tests that consume those projections

  This keeps batch create response payload shape, runtime-input content,
  task active-model assembly, snapshot persistence, and downstream batch
  runtime dispatch behavior unchanged while making the create seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  create workflow owner now directly owns the trivial projections it consumes
  immediately in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 10:44 +08:00 checkpoint:
  this slice stayed on the single-generation runtime persistence seam,
  removed the intermediate persistence-plan owner, and then revalidated that
  single-generation runtime persistence behavior and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal internal carrier owner:
  - `SingleGenerationTaskStage` already owned the true task-mutation
    semantics for preparing/completed/failed runtime stages
  - `SingleGenerationRuntimePersistencePlan` no longer exposed any
    independent seam to other modules; it only repackaged task stage,
    checkpoint stage, chapter identity, and optional error/word-count fields
    before immediate persistence
  - keeping that wrapper left one extra persistence-only owner inside the
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that runtime persistence boundary:
  - removed `SingleGenerationRuntimePersistencePlan`
  - added `SingleGenerationTaskStage::persist_with_checkpoint(...)`
  - updated success/failure persistence helpers to delegate directly to the
    task-stage owner with explicit checkpoint stages
  - adjusted focused tests to assert the direct helper contract without the
    removed carrier owner

  This keeps single-generation task status mutation, checkpoint stage order,
  snapshot persistence, runtime failure handling, and downstream chapter-batch
  behavior unchanged while making the runtime seam more cutover-ready. Smoke
  comparison and rollback review are easier because the task-stage owner now
  directly owns the task-mutation -> checkpoint-persist edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 10:38 +08:00 checkpoint:
  this slice stayed on the analysis draft route seam, removed the remaining
  route-local draft forwarding shells, and then revalidated that owned draft
  selector normalization and analysis route behavior stayed unchanged.

  A focused audit showed that `chapter_analysis_routes.rs` still kept four
  low-signal route-local adapters:
  - `AutoRevisionDraftLookupRouteQuery::into_owned_draft_request()` only
    forwarded `history_id` into
    `OwnedDraftPayloadRequest::from_route_selector(...)`
  - `CandidateDraftLookupRouteQuery::into_owned_draft_request()` only
    forwarded `attempt_id` into the same owner
  - `AutoRevisionDraftApplyRouteRequest::into_owned_draft_request()` and
    `CandidateDraftApplyRouteRequest::into_owned_draft_request()` only
    forwarded route payload fields into that same owner with `allow_stale`
  - keeping those wrappers left extra route-only forwarding owners at the
    transport boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that analysis draft route boundary:
  - removed all four route-local `into_owned_draft_request()` helpers
  - updated the draft query/apply routes to delegate directly to
    `OwnedDraftPayloadRequest::from_route_selector(...)`
  - adjusted focused route tests to assert the shared owned-draft request
    contract directly

  This keeps query/body payload shape, draft selector normalization, draft
  apply/load behavior, and error mapping unchanged while making the analysis
  route seam more cutover-ready. Smoke comparison and rollback review are
  easier because the transport layer now hands draft selectors directly to the
  existing request owner in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 10:33 +08:00 checkpoint:
  this slice stayed on the regeneration stream route seam, removed two
  route-local forwarding shells, and then revalidated that regeneration
  request normalization and route-side behavior stayed unchanged.

  A focused audit showed that `chapter_regeneration_routes.rs` still kept two
  low-signal route-local adapters:
  - `FullChapterRegenerationStreamRouteRequest::into_workflow_request()` only
    forwarded route payload fields into
    `FullChapterRegenerationStreamRequest::from_route_payload(...)`
  - `PartialRegenerationStreamRouteRequest::into_workflow_request()` only
    forwarded route payload fields into
    `PartialRegenerationStreamWorkflowRequest::from_route_payload(...)`
  - keeping both wrappers left extra route-only forwarding owners at the
    transport boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that regeneration stream route boundary:
  - removed `FullChapterRegenerationStreamRouteRequest::into_workflow_request()`
  - removed
    `PartialRegenerationStreamRouteRequest::into_workflow_request()`
  - updated both stream routes to delegate directly to the existing
    prepare-service request owners
  - adjusted route and prepare focused tests to assert the shared
    `from_route_payload(...)` request-owner contract directly

  This keeps HTTP payload shape, request normalization, stream workflow
  dispatch, SSE semantics, and error mapping unchanged while making the
  regeneration route seam more cutover-ready. Smoke comparison and rollback
  review are easier because the transport layer now hands route payloads
  directly to the existing request owners in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
- 2026-05-29 10:25 +08:00 checkpoint:
  this slice returned to the chapter-regeneration apply lane, removed one more
  route-local request constructor split, and then revalidated that partial
  regenerate apply behavior and route contracts stayed unchanged.

  A focused audit showed that `chapter_regeneration_routes.rs` still kept one
  route-local request assembly seam for `POST /chapters/{chapter_id}/apply-partial-regenerate`:
  - the route still called `ApplyPartialRegenerateRequest::new(...)` directly
    with the raw route payload fields
  - the downstream `ApplyPartialRegenerateRequest` owner already defined the
    actual apply-request boundary, including default accessor behavior for
    missing start/end positions
  - keeping the constructor call in the route left one extra
    transport-adjacent request assembly split in front of the real apply owner
    without improving cutover review or rollback auditability

  The refactor therefore narrowed that apply/request boundary:
  - added `ApplyPartialRegenerateRequest::from_route_payload(...)` to
    `chapter_regeneration_apply_service.rs`
  - updated `chapter_regeneration_routes.rs` so the route now delegates its
    request assembly directly to the apply-request owner
  - updated focused tests to assert the route-facing request constructor
    contract through the owned builder

  This keeps partial regenerate apply payload shape, start/end default
  semantics, chapter-access error mapping, and downstream apply behavior
  unchanged while making the regeneration seam more cutover-ready. Smoke
  comparison and rollback review are easier because the apply-request owner
  now defines the complete route-payload contract and the route is thinner at
  that entrypoint.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_apply_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
- 2026-05-29 10:20 +08:00 checkpoint:
  this slice returned to the chapter-crud query lane, removed one remaining
  route-local list-query owner, and then revalidated that chapter list query
  behavior and route contracts stayed unchanged.

  A focused audit showed that `chapter_crud_routes.rs` still kept one
  route-local request boundary for `GET /chapters`:
  - `ListQuery` still owned the raw `project_id` query field at the route
    boundary
  - the downstream `chapter_crud_workflow_service.rs` already owned the
    actual chapter-list payload behavior, so keeping the project-id request
    boundary in the route left one extra transport-adjacent owner in front of
    the real CRUD workflow owner
  - that split did not improve payload shape or error mapping, but it did
    require cutover review to audit the same list-request contract across both
    the route and workflow layers

  The refactor therefore narrowed that query/request boundary:
  - added `ListChaptersRequest` to
    `chapter_crud_workflow_service.rs`
  - added `ListChaptersRequest::from_route_payload(...)`
  - changed `list_chapters_payload(...)` to consume the service-owned request
    type instead of a raw `project_id`
  - updated `chapter_crud_routes.rs` so the route now forwards `project_id`
    into the CRUD workflow request owner before delegating

  This keeps chapter list payload shape, project-id query semantics, and route
  error mapping unchanged while making the chapter CRUD seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  CRUD workflow owner now defines the list request contract and the route is
  thinner at that entrypoint.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
- 2026-05-29 10:14 +08:00 checkpoint:
  this slice returned to the chapter-batch query lane, removed one remaining
  route-local list-limit normalization owner, and then revalidated that the
  active task list query behavior and chapter-batch route contracts stayed
  unchanged.

  A focused audit showed that `chapter_batch_generation.rs` still kept one
  route-local query boundary for the active-task list entrypoint:
  - `ActiveBatchGenerationTaskListRouteQuery::normalized_limit()` still owned
    the default plus clamp semantics for `limit`
  - the downstream
    `chapter_batch_generation_task_view_query_service.rs` already owned the
    actual active-task read semantics, so keeping limit normalization in the
    route left one extra transport-adjacent owner in front of the real query
    boundary
  - that split did not improve payload shape, auth, or error mapping, but it
    did require cutover review to audit the same request contract across both
    the route and task-view query owner

  The refactor therefore narrowed that query/request boundary:
  - added `ActiveBatchGenerationTaskListQueryRequest` to
    `chapter_batch_generation_task_view_query_service.rs`
  - moved default/clamp normalization into
    `ActiveBatchGenerationTaskListQueryRequest::from_route_limit(...)`
  - changed `load_active_user_batch_generation_task_list_view(...)` to consume
    that service-owned request type
  - updated `chapter_batch_generation.rs` so the route now forwards the raw
    `limit` into the query-owner request boundary

  This keeps active batch-task list payload shape, limit default/clamp
  behavior, active-status filtering, and route error mapping unchanged while
  making the chapter-batch seam more cutover-ready. Smoke comparison and
  rollback review are easier because the query owner now defines the list
  request contract and the route is thinner at that entrypoint.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 10:10 +08:00 checkpoint:
  this slice stayed on the chapter-regeneration query seam, removed one
  remaining route-local query-normalization owner, and then revalidated that
  regeneration task query behavior and route contracts stayed unchanged.

  A focused audit showed that `chapter_regeneration_routes.rs` still kept one
  route-local query boundary for the regeneration task list entrypoint:
  - `RegenerationTasksRouteQuery::normalized_limit()` still owned the default
    plus clamp semantics for `limit`
  - the downstream `chapter_regeneration_query_service.rs` already owned the
    actual accessible-chapter gate and regeneration-task read semantics, so
    keeping limit normalization in the route left one extra transport-adjacent
    owner in front of the real query boundary
  - that split did not improve auth, payload shape, or error mapping, but it
    did require cutover review to audit the same request contract across both
    the route and query layers

  The refactor therefore narrowed that query/request boundary:
  - added `RegenerationTasksQueryRequest` to
    `chapter_regeneration_query_service.rs`
  - moved default/clamp normalization into
    `RegenerationTasksQueryRequest::from_route_limit(...)`
  - changed `load_owned_regeneration_tasks_payload(...)` to consume that
    service-owned request type
  - updated `chapter_regeneration_routes.rs` so the route now forwards the raw
    `limit` into the query-owner request boundary

  This keeps regeneration task payload shape, limit default/clamp behavior,
  accessible-chapter error mapping, and downstream query semantics unchanged
  while making the regeneration seam more cutover-ready. Smoke comparison and
  rollback review are easier because the query owner now defines the list
  request contract and the route is thinner at that entrypoint.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
- 2026-05-29 10:06 +08:00 checkpoint:
  this slice moved to the adjacent chapter-crud route/request lane, removed
  one more group of route-local request builders, and then revalidated that
  chapter CRUD request contracts and route behavior stayed unchanged.

  A focused audit showed that `chapter_crud_routes.rs` still kept three
  route-local request assembly seams:
  - `build_create_chapter_request_from_route_payload(...)`
  - `build_update_chapter_request_from_route_payload(...)`
  - `build_update_expansion_plan_request_from_route_payload(...)`
  - each helper only projected route payload fields into the already-existing
    workflow request owners
    `CreateChapterRequest`, `UpdateChapterRequest`, and
    `UpdateExpansionPlanRequest`
  - keeping those builders in the route left one extra transport-adjacent
    owner boundary in front of the CRUD workflow owner without improving
    cutover review or rollback auditability

  The refactor therefore narrowed that chapter-crud request boundary:
  - added `CreateChapterRequest::from_route_payload(...)` to
    `chapter_crud_workflow_service.rs`
  - added `UpdateChapterRequest::from_route_payload(...)`
  - added `UpdateExpansionPlanRequest::from_route_payload(...)`
  - removed the three route-local request-builder helpers from
    `chapter_crud_routes.rs`
  - updated the route handlers to delegate request assembly directly to the
    workflow request owners

  This keeps create/update/expansion-plan payload shape, CRUD workflow input
  semantics, and route error mapping unchanged while making the chapter CRUD
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because the workflow request owners now define the route-payload projection
  contract and the route is thinner across all three write entrypoints.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_crud_routes -- --nocapture`
- 2026-05-29 10:01 +08:00 checkpoint:
  this slice moved to the adjacent chapter-regeneration request lane, removed
  one more route-local workflow-request projection split, and then
  revalidated that regeneration stream request contracts and route behavior
  stayed unchanged.

  A focused audit showed that `chapter_regeneration_routes.rs` still kept two
  route-owned request assembly seams:
  - `FullChapterRegenerationStreamRouteRequest::into_workflow_request()` still
    owned the JSON payload -> workflow request projection, including
    preserve-elements parsing and string-list filtering
  - `PartialRegenerationStreamRouteRequest::into_workflow_request()` still
    owned the raw route field -> workflow request projection for partial
    regeneration
  - both projections were immediately forwarded into the regeneration prepare
    owner, so keeping them in the route left one extra transport-adjacent
    owner in front of the real prepare boundary without improving cutover
    review or rollback auditability

  The refactor therefore narrowed that prepare/request boundary:
  - added `FullChapterRegenerationStreamRequest::from_route_payload(...)` to
    `chapter_regeneration_prepare_service.rs`
  - added `PartialRegenerationStreamWorkflowRequest::from_route_payload(...)`
    to the same prepare owner
  - moved the preserve-elements parsing and route-payload field projection
    into those service-owned request builders
  - updated `chapter_regeneration_routes.rs` so the route now delegates its
    request assembly directly to the prepare owner

  This keeps full/partial regeneration payload shape, preserve-elements
  defaults, stream request normalization, and route error mapping unchanged
  while making the regeneration seam more cutover-ready. Smoke comparison and
  rollback review are easier because the prepare owner now defines both route
  request contracts and the route is thinner across the regeneration stream
  entrypoints.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_routes -- --nocapture`
- 2026-05-29 09:58 +08:00 checkpoint:
  this slice stayed on the chapter-analysis draft request seam, removed one
  more route-local selector-normalization owner, and then revalidated that
  draft payload request behavior and chapter-analysis route contracts stayed
  unchanged.

  A focused audit showed that `chapter_analysis_routes.rs` still kept one
  low-signal route-local owner split across the auto-revision and candidate
  draft entrypoints:
  - four route-local request builders still owned selector trimming and
    empty-string filtering before immediately forwarding into
    `OwnedDraftPayloadRequest::new(...)`
  - the actual selector semantics already belonged to the
    `OwnedDraftPayloadRequest` owner, including latest-vs-explicit selection
    mode and `allow_stale`
  - keeping selector normalization in the route meant cutover review still
    had to audit the same request-shaping contract across both the route and
    the draft service

  The refactor therefore narrowed that draft request boundary:
  - added `OwnedDraftPayloadRequest::from_route_selector(...)` to
    `chapter_analysis_draft_service.rs`
  - moved selector trim/empty filtering into that service-owned request
    builder while preserving `allow_stale` and selection-mode semantics
  - updated all four draft route request adapters in
    `chapter_analysis_routes.rs` to delegate directly to the new owned
    request builder
  - removed the now-unused route-local `normalize_optional_route_string(...)`
    helper

  This keeps auto-revision and candidate draft payload shape, selector
  fallback semantics, stale-apply handling, and route error mapping unchanged
  while making the chapter-analysis route seam more cutover-ready. Smoke
  comparison and rollback review are easier because the draft request owner
  now defines the complete selector normalization contract and the route is
  thinner across all four draft entrypoints.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_draft_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 09:56 +08:00 checkpoint:
  this slice moved to the adjacent chapter-analysis query/route lane, removed
  one remaining route-local request-normalization owner, and then revalidated
  that batch analysis status behavior and chapter-analysis route contracts
  stayed unchanged.

  A focused audit showed that `chapter_analysis_routes.rs` still kept one
  real route-local ownership split at the batch analysis status entrypoint:
  - `BatchAnalysisStatusRouteRequest` still owned the chapter-id
    limit/trim/dedup normalization inline at the route boundary
  - the downstream `chapter_analysis_query_service.rs` already owned the
    actual batch analysis status query semantics, so keeping that request
    normalization in the route left one extra transport-adjacent owner in
    front of the real query boundary
  - that split did not improve auth, transport mapping, or payload behavior,
    but it did make cutover review audit both route and query files for the
    same request-shaping contract

  The refactor therefore narrowed that query/read boundary:
  - added `BatchAnalysisStatusRequest` to
    `chapter_analysis_query_service.rs`
  - moved the route-facing chapter-id normalization into
    `BatchAnalysisStatusRequest::from_route_chapter_ids(...)`
  - changed `load_batch_analysis_task_status_payload(...)` to consume the
    service-owned request type instead of raw route-owned `Vec<String>`
  - updated `chapter_analysis_routes.rs` so the route now forwards raw
    `chapter_ids` into the service-owned request boundary

  This keeps batch analysis status payload shape, chapter-id cap/trim/dedup
  behavior, route error mapping, and downstream analysis-task recovery/query
  semantics unchanged while making the chapter-analysis seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  query owner now defines the batch status request contract and the route is
  closer to transport-only delegation.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 09:52 +08:00 checkpoint:
  this slice stayed on the batch status-stream route/read seam, removed one
  remaining route-local owner split, and then revalidated that stream access
  behavior, SSE keep-alive transport, and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that `chapter_batch_generation.rs` still kept one
  real route/service ownership split at the status-stream entrypoint:
  - the route still owned
    `load_owned_batch_generation_read_context(...) ->
    BatchGenerationStreamState::from_task_state(...) ->
    build_batch_generation_status_stream(...)`
    as a local three-step composition
  - that composition did not add transport-specific value beyond the final
    `Sse::new(...).keep_alive(...)` boundary, because the read-context load and
    initial stream-state assembly are both service-owned semantics
  - keeping that split meant cutover review still had to audit both the route
    and the stream service to verify the initial stream-read contract

  The refactor therefore narrowed that route/read boundary:
  - added `load_owned_batch_generation_status_stream(...)` to
    `chapter_batch_generation_status_stream_service.rs`
  - moved initial read-context loading plus
    `BatchGenerationStreamState::from_task_state(...)` assembly into that new
    service-owned entrypoint
  - updated `chapter_batch_generation.rs` so the route now delegates directly
    to the owned stream loader and only keeps transport wiring

  This keeps task-not-found vs internal error mapping, initial stream-state
  semantics, downstream polling/event behavior, and SSE keep-alive transport
  unchanged while making the batch status-stream seam more cutover-ready.
  Smoke comparison and rollback review are easier because the route is now
  transport-only for the stream entrypoint and the service owns the complete
  read-context -> initial-state -> stream construction edge.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 04:02 +08:00 checkpoint:
  this slice stayed on the chapter analysis view lane, removed the
  intermediate view payload shell, and then revalidated that analysis view
  payload behavior and analysis route behavior stayed unchanged.

  A focused audit showed that
  `chapter_analysis_view_query_service.rs` still kept one low-signal internal
  owner split:
  - `load_chapter_analysis_view_payload(...)` already owned the true view-side
    read/query boundary, including analysis row loading, memories loading,
    checker/draft/quality fragment assembly, and created-at fallback logic
  - `ChapterAnalysisViewPayload` only wrapped those already-prepared values
    before immediate `.into_payload()` conversion in the same file
  - keeping that wrapper left one extra payload-only owner inside the
    analysis view boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that analysis view boundary:
  - removed `ChapterAnalysisViewPayload`
  - replaced the view shell with a direct file-local helper
    `build_chapter_analysis_view_payload(...)`
  - updated focused tests to assert the direct payload builder contract

  This keeps chapter analysis view payload shape, checker/draft/quality
  fragment insertion, created-at fallback semantics, and route behavior
  unchanged while making the analysis view seam more cutover-ready. Smoke
  comparison and rollback review are easier because the read/query owner now
  directly owns the final payload projection edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 04:29 +08:00 checkpoint:
  this slice stayed on the chapter analysis query lane, removed the
  intermediate auto-recovery plan shell, and then revalidated that analysis
  task status payload behavior and analysis route behavior stayed unchanged.

  A focused audit showed that
  `chapter_analysis_query_service.rs` still kept one low-signal internal
  owner split:
  - `analysis_task_status_payload(...)` already owned the true query-side
    task status boundary, including the no-task payload branch, current-time
    capture, auto-recovery decision, optional state mutation, and final
    status payload projection
  - `AnalysisTaskAutoRecoveryPlan` only wrapped `should_recover` plus
    `error_message` before immediate recovery execution in the same file
  - keeping that wrapper left one extra plan-only owner inside the analysis
    query boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that analysis query boundary:
  - removed `AnalysisTaskAutoRecoveryPlan`
  - added the direct file-local helper
    `resolve_analysis_task_auto_recovery_error(...)`
  - added the direct file-local helper
    `recover_analysis_task_if_needed(...)`
  - updated focused tests to assert the direct auto-recovery error contract

  This keeps analysis task timeout thresholds, auto-recovery error wording,
  `auto_recovered` semantics, task status payload shape, and route behavior
  unchanged while making the analysis query seam more cutover-ready. Smoke
  comparison and rollback review are easier because the query owner now
  directly owns the auto-recovery decision -> mutation -> payload edge in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 04:36 +08:00 checkpoint:
  this slice stayed on the single-generation runtime lane, removed the
  intermediate task-mutation plan shell, and then revalidated that runtime
  task mutation behavior and single-generation workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `SingleGenerationTaskStage` already owned the true runtime task-mutation
    boundary, including status selection, timestamp policy, retry reset, and
    current chapter update semantics
  - `SingleGenerationTaskMutationPlan` only wrapped those already-decided
    mutation fields before immediate `apply_to_active_model(...)` execution in
    the same file
  - keeping that wrapper left one extra mutation-plan owner inside the single
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single runtime boundary:
  - removed `SingleGenerationTaskMutationPlan`
  - moved mutation policy helpers directly onto `SingleGenerationTaskStage`
  - moved active-model mutation application directly onto
    `SingleGenerationTaskStage::apply_to_active_model(...)`
  - updated focused tests to assert the stage-owned mutation contract

  This keeps single-generation task status transitions, timestamp updates,
  retry reset semantics, chapter pointer updates, runtime checkpoint order,
  and workflow behavior unchanged while making the single runtime seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  task-stage owner now directly owns the mutation-policy -> active-model edge
  in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 16:34 +08:00 checkpoint:
  this slice moved the pre-generation relevant-memory consumer seam from the
  Python context owner into the Rust provider-payload owner, then revalidated
  that the chapter-generation research payload lane still compiled and its
  focused tests stayed green.

  A focused audit showed that
  `chapter_generation_research_payload_service.rs` still stopped at the
  web-research write seam:
  - `build_single_chapter_research_provider_payload(...)` already sat on the
    true shared entry point reused by single-generation, regeneration, and
    batch runtime per-chapter provider payload construction
  - Rust had already gained a real vector-memory owner and search path, but
    the provider payload still left `relevant_memories` as placeholder `[]`
    unless future work filled it somewhere else
  - keeping that gap meant Python `chapter_context_service._get_relevant_memories_enhanced()`
    still owned the pre-generation memory retrieval responsibility, so the
    migration had not yet crossed the runtime consumption boundary

  The refactor therefore narrowed that runtime-consumer boundary:
  - added `compose_single_chapter_generation_memory_query(...)` to build the
    generation query from chapter title/number plus compat story context
  - added `build_single_chapter_relevant_memories_payload(...)` to run Rust
    vector-memory retrieval, apply similarity thresholding, dedupe, coverage
    selection, and prompt-line formatting
  - injected the resulting prompt block into
    `PromptContextProviderPayload.relevant_memories` before the web-research
    branch, so memory retrieval is no longer blocked by the web-research
    feature flag
  - kept the existing web-research asset/archive/memory write-back lane
    unchanged

  This keeps the research-provider entry shape stable while moving one real
  generation-time memory/context consumer from Python ownership into Rust
  ownership. Single-generation, regeneration, and batch runtime now all pick
  up the same Rust-side `relevant_memories` payload seam through their
  existing shared provider builder.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:17 +08:00 checkpoint:
  this slice continued the shared provider-payload migration and moved the
  chapter-career context owner from template-only placeholder state into the
  Rust runtime provider owner, then revalidated the provider/prompt path with
  focused tests and `cargo check`.

  A focused audit showed that `chapter_careers` was still a real migration
  gap:
  - the generation templates already consumed `{chapter_careers}` across
    one-to-many and one-to-one chapter generation flows
  - Python chapter-context assembly still treated career detail as a dedicated
    generation-time context block
  - but Rust `PromptContextProviderPayload` did not even carry that field yet,
    so the shared runtime owner could not supply it at all

  The refactor therefore narrowed that provider-context boundary further:
  - added `chapter_careers` to `PromptContextProviderPayload` and its prompt
    param conversion / placeholder contract
  - added `build_single_chapter_careers_payload(...)` in the shared research
    payload owner to load Rust `career` and `character_career` models and
    format a generation-ready career summary block
  - injected that career block into the shared provider builder used by
    single-generation, regeneration, and batch per-chapter runtime
  - updated affected provider-payload test literals and added a focused helper
    test for career prompt-line formatting

  This keeps the outward prompt/template contract stable while moving another
  real chapter-context responsibility from Python ownership into the Rust
  generation runtime owner.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_context_provider_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:10 +08:00 checkpoint:
  this slice stayed on the Rust-owned pre-generation memory-retrieval seam
  and moved the query-construction owner closer to the original Python
  runtime behavior, then revalidated that the shared research payload lane
  still compiled and its focused tests stayed green.

  A focused audit showed that the previous Rust memory migration still kept
  one meaningful behavior gap versus Python `build_memory_query_text(...)`:
  - Rust `relevant_memories` retrieval already owned vector search, ranking,
    thresholding, and prompt formatting
  - but its query text still only consumed chapter title plus compat story
    brief/focus/stage, so it did not yet use the newly Rust-owned
    `characters_info` and `foreshadow_reminders` context blocks
  - keeping that gap meant Python-style context-guided memory retrieval had
    not fully crossed the runtime query-construction boundary, even after the
    earlier write/search/provider migrations

  The refactor therefore narrowed that memory-query boundary:
  - expanded `compose_single_chapter_generation_memory_query(...)` so Rust now
    extracts query-focus lines from provider-owned `characters_info` and
    `foreshadow_reminders`
  - threaded those context blocks through
    `build_single_chapter_relevant_memories_payload(...)`
  - kept the existing vector-search path, thresholding, selection, and prompt
    payload shape unchanged

  This keeps the outward provider payload contract stable while moving more of
  the context-guided memory retrieval behavior into the Rust runtime owner,
  reducing another remaining dependency on Python chapter-context logic.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:06 +08:00 checkpoint:
  this slice continued the pre-generation provider-payload migration and
  moved two more runtime context consumers from the Python chapter-context
  owner into the Rust provider owner, then revalidated that the shared
  research payload lane still compiled and its focused tests stayed green.

  A focused audit showed that
  `chapter_generation_research_payload_service.rs` still left two additional
  high-signal provider fields as placeholders:
  - `PromptContextProviderPayload.characters_info` was still `[]`, even
    though single-generation, regeneration, and batch per-chapter runtime all
    already reused this one provider builder
  - `PromptContextProviderPayload.foreshadow_reminders` was also still `[]`,
    so Python chapter-context assembly still owned that generation-time
    continuity reminder responsibility
  - keeping those placeholders meant the runtime had only partially crossed
    the pre-generation context boundary, even after the earlier
    `relevant_memories` migration

  The refactor therefore narrowed that provider-context boundary further:
  - added `build_single_chapter_characters_info_payload(...)` to load Rust
    character models and format a generation-ready character summary block
  - added `build_single_chapter_foreshadow_reminders_payload(...)` to load
    unresolved foreshadow rows and assemble must-resolve / overdue / upcoming
    reminder sections directly in Rust
  - injected both results into
    `PromptContextProviderPayload.characters_info` and
    `PromptContextProviderPayload.foreshadow_reminders` inside the shared
    provider builder before the web-research branch
  - added focused helper tests for character prompt-line formatting and
    foreshadow reminder formatting

  This keeps the provider payload shape stable while moving more of the
  generation-time context assembly out of Python ownership and into the Rust
  runtime owner that is already shared by single-generation, regeneration,
  and batch per-chapter execution.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 04:40 +08:00 checkpoint:
  this slice stayed on the single-generation prepare/workflow lane, removed
  the intermediate prepared-request carrier, and then revalidated that
  prepare-side target/config assembly and single-generation workflow behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_prepare_service.rs` plus
  `chapter_single_generation_workflow_service.rs` still kept one low-signal
  lane-local owner split:
  - `prepare_single_chapter_generation_request(...)` already owned the true
    prepare boundary, including accessible chapter loading, chapter-target
    projection, target-word-count normalization, and execution-config
    preparation
  - `PreparedSingleChapterGenerationRequest` only wrapped
    `chapter_target + execution_input` before immediate unpacking by the
    workflow lane for stream launch or background launch
  - keeping that wrapper left one extra carrier owner across the same
    single-generation lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that prepare/workflow boundary:
  - removed `PreparedSingleChapterGenerationRequest`
  - changed `prepare_single_chapter_generation_request(...)` to return the
    real `(SingleChapterGenerationTarget, SingleChapterGenerationExecutionInput)`
    parts directly
  - replaced prepared-owner methods with direct workflow helpers
    `into_runtime_launch_input(...)` and `dispatch_background_generation(...)`
  - updated focused tests to assert the direct request-parts and workflow
    helper contracts

  This keeps single-generation target projection, execution-config content,
  pending checkpoint semantics, background response payload shape, runtime
  launch input shape, and workflow behavior unchanged while making the
  single-generation lane more cutover-ready. Smoke comparison and rollback
  review are easier because the prepare owner now directly hands off the real
  chapter-target and execution-input parts consumed by the workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 04:47 +08:00 checkpoint:
  this slice stayed on the batch-generation runtime lane, removed the
  intermediate task-mutation plan shell, and then revalidated that runtime
  task mutation behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `BatchGenerationTaskStage` already owned the true runtime task-mutation
    boundary, including status resolution, timestamp policy, retry reset,
    chapter pointer updates, and total-chapter update semantics
  - `BatchGenerationTaskMutationPlan` only wrapped those already-decided
    mutation fields before immediate `apply_to_active_model(...)` execution
    in the same file
  - keeping that wrapper left one extra mutation-plan owner inside the batch
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch runtime boundary:
  - removed `BatchGenerationTaskMutationPlan`
  - moved mutation policy helpers directly onto `BatchGenerationTaskStage`
  - moved active-model mutation application directly onto
    `BatchGenerationTaskStage::apply_to_active_model(...)`
  - updated focused tests to assert the stage-owned mutation contract

  This keeps batch task status transitions, timestamp updates, retry reset
  semantics, chapter pointer updates, total-chapter propagation, runtime
  checkpoint order, and chapter-batch behavior unchanged while making the
  batch runtime seam more cutover-ready. Smoke comparison and rollback review
  are easier because the task-stage owner now directly owns the
  mutation-policy -> active-model edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 04:55 +08:00 checkpoint:
  this slice stayed on the batch-generation create lane, removed the
  intermediate prepared-create carrier, and then revalidated that create-side
  target assembly and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one
  low-signal lane-local owner split:
  - `BatchGenerationCreateWorkflowRequest::prepare(...)` already owned the
    true create-side prepare boundary, including chapter-range loading,
    chapter-target projection, and target-word-count normalization
  - `PreparedBatchGenerationCreateRequest` only wrapped
    `normalized_target_word_count + chapters_to_generate` before immediate
    unpacking by the write workflow lane for response payload assembly, task
    active-model assembly, and runtime dispatch input assembly
  - keeping that wrapper left one extra carrier owner inside the same batch
    create lane without improving cutover review or rollback auditability

  The refactor therefore narrowed that batch create boundary:
  - removed `PreparedBatchGenerationCreateRequest`
  - changed `prepare(...)` to return the real
    `(normalized_target_word_count, chapters_to_generate)` parts directly
  - replaced prepared-owner methods with direct file-local batch create
    helpers for chapter ids, payload projection, response payload, and
    workflow dispatch
  - updated focused tests across write-workflow/task-model consumers to assert
    the direct create-parts contract

  This keeps batch create target projection, target-word-count normalization,
  create response payload shape, task active-model assembly, runtime dispatch
  input shape, and chapter-batch behavior unchanged while making the batch
  create seam more cutover-ready. Smoke comparison and rollback review are
  easier because the prepare owner now directly hands off the real create
  parts consumed by the workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 05:00 +08:00 checkpoint:
  this slice stayed on the batch-generation resume runtime lane, removed the
  intermediate resume-reset plan shell, and then revalidated that resume
  reset behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `reset_batch_generation_task_for_resume(...)` already owned the true
    resume-reset boundary, including reset-semantics resolution, failed
    chapter clearing, reset task mutation, and resume checkpoint replace
    behavior
  - `BatchGenerationResumeResetPlan` only wrapped
    `task_id + active + checkpoint` before immediate `persist(...)`
    execution in the same file
  - keeping that wrapper left one extra reset-plan owner inside the batch
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that resume runtime boundary:
  - removed `BatchGenerationResumeResetPlan`
  - moved reset active-model assembly directly into
    `reset_batch_generation_task_for_resume(...)`
  - moved resume checkpoint replace directly into the same reset owner
  - kept reset mutation semantics on `BatchGenerationTaskStage::ResumeReset`

  This keeps resume reset semantics, failed-chapter clearing, current chapter
  reset rules, retry reset, resume checkpoint replace behavior, and
  chapter-batch behavior unchanged while making the batch resume runtime seam
  more cutover-ready. Smoke comparison and rollback review are easier because
  the reset owner now directly owns the command-state -> active/checkpoint ->
  persist edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 05:08 +08:00 checkpoint:
  this slice stayed on the batch-generation runtime step lane, removed the
  intermediate step-result shell, and then revalidated that per-step runtime
  persistence behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `execute_batch_generation_step(...)` already owned the true step
    execution boundary, including task cancellation detection, chapter load
    failure handling, chapter-start checkpoint persistence, generation
    success/failure branching, and next-progress resolution
  - `BatchGenerationStepResult` only wrapped
    `BatchGenerationRuntimePersistencePlan + BatchGenerationStepOutcome`
    before immediate `persist(...)` execution in the same file
  - keeping that wrapper left one extra result-only owner inside the batch
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch runtime step boundary:
  - removed `BatchGenerationStepResult`
  - added direct file-local helper
    `persist_batch_generation_step_outcome(...)`
  - moved each cancelled / missing-chapter / load-error / success /
    generation-failure branch to assemble its
    `BatchGenerationRuntimePersistencePlan` inline inside
    `execute_batch_generation_step(...)` before returning the real
    `BatchGenerationStepOutcome`
  - kept the runtime step contract test coverage on the surviving direct
    persistence + outcome edge

  This keeps batch runtime step ordering, progress advancement, cancel/fail
  stop semantics, chapter-start persistence ordering, runtime persistence
  semantics, and chapter-batch observable behavior unchanged while making the
  batch runtime seam more cutover-ready. Smoke comparison and rollback review
  are easier because the step owner now directly owns the branch decision ->
  persistence plan -> returned step outcome edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 05:18 +08:00 checkpoint:
  this slice stayed on the single-generation runtime terminal lane, removed
  the intermediate runtime-outcome shell, and then revalidated that terminal
  runtime persistence behavior and single-generation workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `execute_single_generation_runtime(...)` already owned the true runtime
    terminal boundary, including preparation persistence, owned generation
    execution, success/failure branching, and terminal write ordering
  - `SingleGenerationRuntimeOutcome` only wrapped
    `chapter_id + chapter_number + word_count` or `chapter_id +
    error_message` before immediate `persist(...)` execution in the same file
  - keeping that wrapper left one extra outcome-only owner inside the single
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single runtime terminal boundary:
  - removed `SingleGenerationRuntimeOutcome`
  - added direct file-local helper
    `persist_single_generation_runtime_success(...)`
  - added direct file-local helper
    `persist_single_generation_runtime_failure(...)`
  - moved the final success/failure persistence handoff directly into
    `execute_single_generation_runtime(...)`
  - updated focused tests to assert the surviving direct helper-owned
    persistence contract

  This keeps single-generation preparing/finalizing/completed/failed
  checkpoint ordering, task-stage mutation semantics, word-count and
  chapter-number propagation, failure persistence, and workflow-observable
  behavior unchanged while making the single runtime seam more cutover-ready.
  Smoke comparison and rollback review are easier because the runtime owner
  now directly owns the generation result -> persistence helper edge in that
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 05:31 +08:00 checkpoint:
  this slice stayed on the batch-generation resume command lane, removed the
  intermediate execution-plan shell, and then revalidated that resume
  dispatch behavior, response payload behavior, and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still kept one
  low-signal internal owner split:
  - `prepare_batch_generation_resume(...)` already owned the true resume
    command boundary, including resumable selection resolution,
    target-word-count normalization, reset persistence triggering, and final
    response payload projection
  - `ResumeExecutionPlan` only wrapped
    `ResumeExecutionSelection + task_id + user_id + target_word_count`
    before immediate `dispatch(...)` branching in the same lane
  - keeping that wrapper left one extra forwarding-only owner inside the
    resume command boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch resume command boundary:
  - removed `ResumeExecutionPlan`
  - changed `prepare_batch_generation_resume(...)` to return the real
    `(ResumeExecutionSelection, target_word_count, response_payload)` parts
    directly
  - added direct file-local helper
    `dispatch_resumed_batch_generation_execution(...)`
  - updated the write workflow to dispatch from the real selection plus the
    prepared execution config without recreating an extra carrier owner
  - updated focused tests to assert the surviving selection/dispatch-helper
    contract

  This keeps resume eligibility gating, manual-review blocking, reset
  semantics, response payload shape, target-word-count normalization, runtime
  dispatch branching, and chapter-batch observable behavior unchanged while
  making the resume seam more cutover-ready. Smoke comparison and rollback
  review are easier because the command owner now directly owns the resume
  selection -> runtime dispatch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 05:42 +08:00 checkpoint:
  this slice stayed on the batch-generation cancel/runtime lane, removed the
  intermediate cancelled-finalize forwarding shell, and then revalidated that
  cancel response behavior, cancelled persistence behavior, and chapter-batch
  behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `BatchGenerationRuntimePersistencePlan::cancelled(...).persist(...)`
    already owned the true cancelled persistence boundary, including task
    stage mutation, cancelled checkpoint projection, and timestamped snapshot
    writes
  - `finalize_batch_generation_cancelled(...)` only forwarded
    `task_id + completed_chapters + total_chapters` into that same
    persistence owner before immediate await by the cancel command lane
  - keeping that helper left one extra forwarding-only owner across the
    cancel/runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that cancel/runtime boundary:
  - removed `finalize_batch_generation_cancelled(...)`
  - changed the cancel command owner to call
    `BatchGenerationRuntimePersistencePlan::cancelled(...).persist(...)`
    directly
  - promoted the cancelled persistence owner visibility only as far as needed
    for sibling service reuse, without changing the runtime write contract

  This keeps cancel eligibility checks, cancel response payload shape,
  cancelled checkpoint semantics, task status mutation, and chapter-batch
  observable behavior unchanged while making the cancel seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  cancel owner now directly owns the cancel decision -> cancelled persistence
  edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_cancel_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 05:51 +08:00 checkpoint:
  this slice stayed on the batch-generation stream/read entry lane, removed
  the intermediate initial-state loader shell, and then revalidated that
  stream bootstrap behavior, polling behavior, and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service.rs` still kept one
  low-signal internal owner split:
  - `load_owned_batch_generation_read_context(...)` already owned the true
    stream bootstrap read boundary, including ownership validation, task
    loading, snapshot/runtime-state loading, and read-context assembly
  - `BatchGenerationStreamState::from_task_state(...)` already owned the true
    stream semantic projection boundary
  - `load_owned_batch_generation_stream_initial_state(...)` only chained those
    two owners together for one production route call before immediate stream
    construction
  - keeping that helper left one extra forwarding-only owner on the route ->
    read -> stream seam without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that stream/read entry boundary:
  - removed `load_owned_batch_generation_stream_initial_state(...)`
  - changed the batch status stream route to call the real read-context owner
    directly, then project `BatchGenerationStreamState` from the returned
    task/runtime parts inline at the route boundary
  - removed the corresponding test-only loader contract shell from the stream
    service suite

  This keeps owned stream access checks, initial stream state projection,
  polling refresh behavior, task-not-found/timed-out system event payloads,
  SSE event semantics, and chapter-batch observable behavior unchanged while
  making the stream bootstrap seam more cutover-ready. Smoke comparison and
  rollback review are easier because the route now directly owns the read
  owner -> stream projection edge it immediately consumes.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 06:00 +08:00 checkpoint:
  this slice stayed on the single-generation stream workflow lane, removed
  the intermediate runtime-launch input builder shell, and then revalidated
  that stream launch behavior, workflow behavior, and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one
  low-signal internal owner split:
  - `create_single_generation_stream_workflow(...)` already owned the true
    stream-workflow boundary, including request preparation, runtime launch
    input assembly, and stream launch handoff
  - `into_runtime_launch_input(...)` only wrapped
    `chapter_target + execution_input + user_id` into
    `SingleGenerationRuntimeLaunchInput` before immediate consumption by the
    same stream launch owner
  - keeping that helper left one extra forwarding-only owner inside the
    single stream workflow lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single stream workflow boundary:
  - removed `into_runtime_launch_input(...)`
  - changed `create_single_generation_stream_workflow(...)` to construct the
    real `SingleGenerationRuntimeLaunchInput` inline at the workflow owner
    boundary
  - updated focused tests to assert the surviving runtime-parts launch-input
    contract directly

  This keeps single-generation request preparation, stream launch input shape,
  SSE progress/result/error behavior, downstream runtime execution, and
  chapter-batch observable behavior unchanged while making the single stream
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because the workflow owner now directly owns the prepared request -> runtime
  launch input edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 06:09 +08:00 checkpoint:
  this slice stayed on the single-generation background workflow lane,
  removed the intermediate background-dispatch shell, and then revalidated
  that background workflow behavior, runtime dispatch behavior, and
  chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one
  low-signal internal owner split:
  - `start_owned_single_generation_background_workflow(...)` already owned
    the true background-workflow boundary, including task id allocation,
    request preparation, pending checkpoint assembly, response payload
    projection, task active-model assembly, and runtime dispatch handoff
  - `dispatch_background_generation(...)` only wrapped those same lane-local
    values before immediate insert/snapshot-write/dispatch execution in the
    same file
  - keeping that helper left one extra forwarding-only owner inside the
    single background workflow lane without improving cutover review or
    rollback auditability

  The refactor therefore narrowed that single background workflow boundary:
  - removed `dispatch_background_generation(...)`
  - changed `start_owned_single_generation_background_workflow(...)` to own
    the runtime input assembly, task insert, pending checkpoint write, and
    runtime dispatch directly
  - kept focused tests on the surviving background runtime input, active-model
    defaults, and prepared-owner contract

  This keeps single-generation background response payload shape, pending
  checkpoint semantics, task defaults, runtime dispatch behavior, and
  chapter-batch observable behavior unchanged while making the background
  workflow seam more cutover-ready. Smoke comparison and rollback review are
  easier because the workflow owner now directly owns the prepared request ->
  task insert/checkpoint/runtime dispatch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 06:18 +08:00 checkpoint:
  this slice stayed on the batch-generation create/write lane, removed the
  intermediate chapter-id payload shell, and then revalidated that create
  response behavior, task seed behavior, and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one
  low-signal internal owner split:
  - `batch_generation_create_chapter_ids(...)` already owned the true ordered
    chapter-id projection from `chapters_to_generate`
  - `dispatch_batch_generation_create_workflow(...)` already owned the true
    create-workflow dispatch boundary that immediately consumed those ordered
    ids for runtime input and task active-model assembly
  - `batch_generation_create_chapter_id_payload(...)` only wrapped the same
    ordered ids into `Value::Array` before immediate consumption in the same
    create/write lane
  - keeping that helper left one extra payload-only owner inside the batch
    create boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch create/write boundary:
  - removed `batch_generation_create_chapter_id_payload(...)`
  - changed `dispatch_batch_generation_create_workflow(...)` to derive the
    `Value::Array` chapter-id payload directly from the already-owned ordered
    `chapter_ids`
  - updated focused tests to assert the surviving ordered-id -> payload
    contract directly

  This keeps create response payload shape, chapter-id ordering, task
  active-model assembly, runtime input shape, and chapter-batch observable
  behavior unchanged while making the batch create seam more cutover-ready.
  Smoke comparison and rollback review are easier because the create workflow
  owner now directly owns the ordered chapter ids -> task payload edge in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 06:27 +08:00 checkpoint:
  this slice stayed on the batch-generation create response lane, removed the
  intermediate chapters-to-generate payload shell, and then revalidated that
  create response behavior, runtime seed behavior, and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one
  low-signal internal owner split:
  - `batch_generation_create_response_payload(...)` already owned the true
    create response boundary, including the response message, estimated-time
    derivation, and `chapters_to_generate` response field assembly
  - `batch_generation_create_chapters_to_generate_payload(...)` only mapped
    the same `chapters_to_generate` list into `Vec<Value>` before immediate
    consumption by that response owner
  - keeping that helper left one extra payload-only owner inside the batch
    create response boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that batch create response boundary:
  - removed `batch_generation_create_chapters_to_generate_payload(...)`
  - changed `batch_generation_create_response_payload(...)` to project the
    `chapters_to_generate` response array directly at the response owner
    boundary
  - updated focused tests to assert the surviving response-array contract
    directly

  This keeps create response payload shape, chapter-id / title /
  chapter-number projection, estimated-time semantics, runtime seed behavior,
  and chapter-batch observable behavior unchanged while making the batch
  create seam more cutover-ready. Smoke comparison and rollback review are
  easier because the response owner now directly owns the create-targets ->
  response array edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 03:53 +08:00 checkpoint:
  this slice switched to the chapter analysis runtime lane, removed the
  intermediate trigger shell, and then revalidated that analysis task create
  payload behavior and analysis route behavior stayed unchanged.

  A focused audit showed that
  `chapter_analysis_runtime_service.rs` still kept one low-signal internal
  owner split:
  - `ChapterAnalysisTaskCreateState` already owned the true analysis task
    create boundary, including `task_id/chapter_id` plus the create payload
  - `PreparedChapterAnalysisTrigger` only wrapped that create state before
    immediate payload projection and background dispatch in the same file
  - keeping that wrapper left one extra trigger-only owner inside the
    analysis runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that analysis runtime boundary:
  - removed `PreparedChapterAnalysisTrigger`
  - changed `prepare_chapter_analysis_trigger(...)` to return
    `ChapterAnalysisTaskCreateState` directly
  - changed `dispatch_prepared_chapter_analysis_trigger(...)` to dispatch the
    real create-state owner directly
  - updated focused tests to assert the flattened create-state contract

  This keeps analysis task create payload shape, background dispatch target,
  task create semantics, and error mapping unchanged while making the
  analysis runtime seam more cutover-ready. Smoke comparison and rollback
  review are easier because the create-state owner now directly owns the only
  remaining trigger edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_routes -- --nocapture`
- 2026-05-29 03:42 +08:00 checkpoint:
  this slice stayed on the single runtime seam, removed the intermediate
  outcome-to-persistence forwarding shell, and then revalidated that
  single-generation runtime persistence behavior and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `SingleGenerationRuntimeOutcome` already owned the true runtime terminal
    branch between success and failure
  - `into_persistence_plans()` only expanded that outcome into one or two
    `SingleGenerationRuntimePersistencePlan` values before immediate
    persistence in the same file
  - keeping that wrapper left one extra forwarding-only owner inside the
    single runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single runtime boundary:
  - removed `SingleGenerationRuntimeOutcome::into_persistence_plans()`
  - moved finalizing/completed/failed persistence branching directly into
    `SingleGenerationRuntimeOutcome::persist(...)`
  - adjusted focused tests to assert the outcome-owned persistence contract
    instead of the intermediate plan vector

  This keeps single-generation finalizing/completed/failed checkpoint order,
  task-stage mutation semantics, downstream generation execution, and error
  behavior unchanged while making the single runtime seam more cutover-ready.
  Smoke comparison and rollback review are easier because the outcome owner
  now directly owns the final runtime persistence edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 03:31 +08:00 checkpoint:
  this slice stayed on the batch resume command seam, removed the
  intermediate resume launch shell, and then revalidated that resume-side
  payload behavior, runtime dispatch behavior, and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still kept one
  low-signal internal owner split:
  - `ResumeExecutionPlan` already owned the real single-vs-batch runtime
    dispatch boundary
  - `PreparedBatchGenerationResumeLaunchPlan` only repackaged
    `execution/task_id/response_payload` before immediate dispatch from the
    write-workflow owner
  - keeping that wrapper left one extra launch-only owner inside the resume
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that resume command boundary:
  - removed `PreparedBatchGenerationResumeLaunchPlan`
  - moved `task_id` ownership directly onto `ResumeExecutionPlan`
  - changed `prepare_batch_generation_resume(...)` to return
    `(ResumeExecutionPlan, Value)`
  - updated the write-workflow resume path to dispatch the execution owner
    directly and return the response payload separately
  - adjusted focused tests to assert the flattened execution/payload contract

  This keeps resume-domain validation, reset semantics, response payload
  shape, execution-config threading, and downstream single/batch runtime
  dispatch behavior unchanged while making the resume seam more cutover-ready.
  Smoke comparison and rollback review are easier because the resume owner now
  directly owns the only remaining runtime launch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 03:18 +08:00 checkpoint:
  this slice stayed on the single runtime seam, removed the intermediate
  preparation carrier owner, and then revalidated that single-generation
  runtime lifecycle behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split:
  - `SingleGenerationTaskStage` already owned the true task-stage mutation
    boundary for preparing/completed/failed runtime transitions
  - `SingleGenerationRuntimePreparationPlan` only carried `chapter_id`
    before immediately persisting the preparing task mutation plus the
    preparing/generating checkpoints
  - keeping that wrapper left one extra preparation-only owner inside the
    single runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single runtime boundary:
  - removed `SingleGenerationRuntimePreparationPlan`
  - added
    `SingleGenerationTaskStage::persist_runtime_preparation(...)`
  - updated `execute_single_generation_runtime(...)` to call the real
    task-stage owner directly for preparing/generating checkpoint persistence
  - adjusted focused tests to assert the checkpoint builder contract directly

  This keeps single-generation preparing/generating checkpoint order,
  task-stage mutation semantics, downstream generation execution, and error
  behavior unchanged while making the single runtime seam more cutover-ready.
  Smoke comparison and rollback review are easier because the task-stage owner
  now directly owns the preparation persistence edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 03:02 +08:00 checkpoint:
  this slice stayed on the batch create write-workflow seam, removed the
  intermediate create launch owner, and then revalidated that create-side
  response payload behavior, snapshot persistence, and downstream batch
  runtime dispatch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one
  low-signal internal owner split:
  - `PreparedBatchGenerationCreateRequest` already owned the true create-side
    boundary, including normalized target word count and ordered chapter
    targets
  - `BatchGenerationCreateLaunchPlan` no longer exposed any independent seam
    to other modules; it only repackaged task create inputs, response payload,
    and runtime dispatch inputs before immediate insertion and dispatch
  - keeping that wrapper left one extra launch-only owner inside the create
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that create-side boundary:
  - removed `BatchGenerationCreateLaunchPlan`
  - added `PreparedBatchGenerationCreateRequest::dispatch_create_workflow(...)`
    so the prepared owner now directly assembles task create input, persists
    the snapshot, dispatches runtime input, and returns the route response
  - updated focused tests to assert the prepared-owner dispatch contract
    directly

  This keeps task create payload shape, snapshot persistence, runtime input
  content, downstream batch runtime dispatch behavior, and error mapping
  unchanged while making the batch create seam more cutover-ready. Smoke
  comparison and rollback review are easier because the create launch owner
  now directly owns the only remaining task and runtime-input carrier state in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:29 +08:00 checkpoint:
  this slice stayed on the batch runtime-step seam, removed the intermediate
  step-plan owner, and then revalidated that runtime-step behavior and
  chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one
  low-signal internal owner split inside the runtime loop:
  - `execute_batch_generation_step(...)` already owned the true per-step
    runtime boundary, including task re-check, chapter lookup, chapter-start
    persistence, generation call, and final step-result persistence
  - `BatchGenerationStepPlan` no longer exposed any independent seam to other
    modules; it only repackaged `chapter_id/progress` before immediate
    destructuring in the same runtime-step owner
  - keeping that wrapper left one extra runtime-step-only owner inside the
    loop without improving cutover review or rollback auditability

  The refactor therefore narrowed that runtime-step boundary:
  - removed `BatchGenerationStepPlan`
  - changed `execute_batch_generation_step(...)` to consume
    `chapter_id` plus `BatchGenerationStepProgress` directly
  - updated focused tests to assert the surviving explicit step-call contract
    directly

  This keeps runtime loop control flow, cancel re-check behavior, chapter-load
  failure behavior, generation failure behavior, step persistence semantics,
  and error mapping unchanged while making the runtime-step seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  runtime-step owner now directly owns the complete chapter-id/progress ->
  step execution edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:26 +08:00 checkpoint:
  this slice stayed on the batch resume launch seam, removed the
  intermediate prepared-resume plan owner, and then revalidated that resume
  launch behavior, write-workflow behavior, and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still kept one
  low-signal internal owner split:
  - `PreparedBatchGenerationResumeLaunchPlan` already owned the true resume
    launch boundary, including execution selection, task id, response payload,
    and dispatch behavior
  - `PreparedBatchGenerationResumePlan` no longer exposed any independent seam
    to other modules; it only wrapped that launch owner under one `launch`
    field before immediate consumption by the write-workflow owner
  - keeping that wrapper left one extra plan-only owner inside the resume
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that resume launch boundary:
  - removed `PreparedBatchGenerationResumePlan`
  - changed `prepare_batch_generation_resume(...)` to return
    `PreparedBatchGenerationResumeLaunchPlan` directly
  - updated `chapter_batch_generation_write_workflow_service.rs` to consume
    the launch owner directly
  - adjusted focused tests to assert the surviving launch-plan contract
    without the deleted `launch` wrapper

  This keeps resume response payload shape, execution selection semantics,
  runtime dispatch targets, write-workflow integration, and error mapping
  unchanged while making the resume seam more cutover-ready. Smoke comparison
  and rollback review are easier because the write-workflow owner now consumes
  the true launch boundary directly instead of stepping through one extra
  wrapper plan.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:21 +08:00 checkpoint:
  this slice stayed on the batch resume command seam, removed the
  intermediate resume task-context owner, and then revalidated that resume
  payload behavior, resume dispatch behavior, write-workflow behavior, and
  chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still kept one
  low-signal internal owner split:
  - `PreparedBatchGenerationResumeLaunchPlan` already owned the true resume
    launch boundary, including the selected execution path plus the final
    task identifier and response payload
  - `ResumeTaskContext` no longer exposed any independent seam to other
    modules; it only repackaged `task_id/response_payload` before immediate
    dispatch and return from the launch owner
  - keeping that wrapper left one extra context-only owner inside the resume
    command boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that resume command boundary:
  - removed `ResumeTaskContext`
  - flattened `task_id` and `response_payload` directly onto
    `PreparedBatchGenerationResumeLaunchPlan`
  - updated focused tests to assert the surviving launch-plan-owned fields
    directly

  This keeps resume response payload shape, single-vs-batch execution
  selection, runtime dispatch targets, write-workflow integration, and error
  mapping unchanged while making the resume seam more cutover-ready. Smoke
  comparison and rollback review are easier because the launch owner now
  directly owns the complete execution -> dispatch -> response edge in that
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:18 +08:00 checkpoint:
  this slice stayed on the batch status-stream polling seam, removed the
  intermediate stream system-event owner, and then revalidated that
  not-found/timed-out SSE behavior plus chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service.rs` still kept one
  low-signal internal owner split:
  - the status-stream polling loop already owned the true system-event send
    boundary for `task not found` and `timed out`
  - `BatchGenerationStatusStreamSystemEvent` no longer exposed any
    independent seam to other modules; it only repackaged two payload shapes
    before immediate `Event::default().data(...to_string())` sends
  - keeping that enum left one extra system-event-only owner inside the batch
    stream boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that batch stream boundary:
  - removed `BatchGenerationStatusStreamSystemEvent`
  - replaced it with direct owner functions:
    `task_not_found_stream_payload()` and
    `timed_out_stream_payload()`
  - updated focused tests to assert the surviving payload-owner functions
    directly

  This keeps not-found SSE payload shape, timed-out SSE payload shape, stream
  polling behavior, cursor/event resolution behavior, and error mapping
  unchanged while making the batch stream seam more cutover-ready. Smoke
  comparison and rollback review are easier because the polling owner now
  directly owns the complete system-event payload -> SSE send edge in that
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:15 +08:00 checkpoint:
  this slice stayed on the single-chapter prepare/workflow seam, removed the
  intermediate background-projection owner, and then revalidated that
  single-chapter background behavior, stream behavior, and chapter-batch
  behavior stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_prepare_service.rs` still kept one low-signal
  internal owner split:
  - `SingleChapterGenerationTarget` already owned the true single-target
    projection boundary, including task chapter payload, pending checkpoint,
    background response payload, and background task active-model assembly
  - `SingleChapterGenerationBackgroundProjectionPlan` no longer exposed any
    independent seam to other modules; it only repackaged
    `task/checkpoint/response_payload` before immediate background launch
    assembly in the workflow owner
  - keeping that wrapper left one extra projection-only owner inside the
    single prepare/workflow boundary without improving cutover review or
    rollback auditability

  The refactor therefore narrowed that single prepare/workflow boundary:
  - removed `SingleChapterGenerationBackgroundProjectionPlan`
  - updated `chapter_single_generation_workflow_service.rs` to consume
    `SingleChapterGenerationTarget`'s direct owner functions:
    `pending_checkpoint()`, `background_response_payload()`, and
    `background_task_active_model()`
  - updated focused tests to assert the surviving target-owned background
    parts directly instead of a deleted projection wrapper

  This keeps single-chapter background task payload shape, checkpoint payload
  shape, runtime snapshot write content, runtime dispatch targets, stream
  behavior, and error mapping unchanged while making the single
  prepare/workflow seam more cutover-ready. Smoke comparison and rollback
  review are easier because the target/workflow boundary now directly owns the
  complete target -> background parts -> background launch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:09 +08:00 checkpoint:
  this slice stayed on the single-chapter workflow seam, removed the
  intermediate runtime-launch plan owner, and then revalidated that
  single-chapter stream/background behavior and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one low-signal
  internal owner split:
  - `PreparedSingleChapterGenerationRequest` already owned the true prepared
    single-generation boundary, including the loaded chapter target plus
    explicit execution input
  - `SingleGenerationRuntimeLaunchPlan` no longer exposed any independent seam
    to other modules; it only repackaged `chapter_target/runtime_input` before
    immediate stream launch or background launch projection
  - keeping that wrapper left one extra launch-only owner inside the single
    workflow boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that single workflow boundary:
  - removed `SingleGenerationRuntimeLaunchPlan`
  - replaced `into_runtime_launch_plan()` with
    `into_runtime_launch_input()`
  - moved background launch assembly directly onto
    `PreparedSingleChapterGenerationRequest::into_background_launch_plan(...)`
  - updated focused tests to assert the surviving prepared-request-owned
    launch contracts directly

  This keeps single-chapter stream payload behavior, background task payload
  shape, runtime dispatch targets, prepared request semantics, and error
  mapping unchanged while making the single workflow seam more cutover-ready.
  Smoke comparison and rollback review are easier because the prepared request
  owner now directly owns the complete prepared -> runtime/background launch
  edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:05 +08:00 checkpoint:
  this slice stayed on the batch status-stream event seam, removed the
  intermediate stream-event plan owner, and then revalidated that SSE
  progress/terminal behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_event_service.rs` still kept one
  low-signal internal owner split:
  - `BatchGenerationStreamCursor::resolve_event_batch(...)` already owned the
    true cursor -> event-batch resolution boundary
  - `BatchGenerationStreamEventPlan` no longer exposed any independent seam to
    other modules; it only repackaged `events/should_close` before immediate
    branching into `Continue` vs `Close`
  - keeping that wrapper left one extra event-only owner inside the stream
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that stream-event boundary:
  - removed `BatchGenerationStreamEventPlan`
  - replaced `into_event_plan()` with `events()` plus
    `should_close_stream()`
  - updated cursor resolution to branch directly on the state-owned close
    semantic
  - adjusted focused tests to assert the surviving state-owned event and close
    contracts directly

  This keeps SSE progress payload shape, terminal event categories, terminal
  close timing, cursor advancement semantics, and error payload behavior
  unchanged while making the stream seam more cutover-ready. Smoke comparison
  and rollback review are easier because the cursor/state boundary now owns
  the complete state -> events -> close decision edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice stayed in the same batch read-side lane, introduced one shared
  owned read-context loader, and then revalidated that route and stream error
  contracts stayed unchanged.

  A focused audit showed that the owned-task read chain had become duplicated:
  - `chapter_batch_generation.rs` repeated the same
    `load_owned_task(...) -> TaskNotFound/Internal -> load read context`
    sequence in both status query and status stream entrypoints
  - `chapter_batch_generation_status_stream_service.rs` repeated the same
    owned-task lookup plus read-context load inside the stream poll loop
  - the real owner boundary is the read-context lane itself, because it now
    owns both the task row projection and the snapshot-derived read state

  The refactor therefore narrowed that read-side loading boundary:
  - added `load_owned_batch_generation_read_context(...)` to
    `chapter_batch_generation_read_context_service.rs`
  - `chapter_batch_generation.rs` now uses that owner directly for both the
    status payload path and the initial SSE state path
  - `chapter_batch_generation_status_stream_service.rs` now uses the same
    owner while polling instead of reassembling the lookup chain inline
  - the external error shape remains
    `LoadOwnedBatchGenerationTaskError::{TaskNotFound, Internal}`

  This keeps status-route payloads, SSE event shape/order, task-not-found and
  internal error mapping, and stream poll fallback behavior unchanged while
  removing one more duplicated read-side owner from the batch query/stream
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice moved back to the batch create write-workflow lane, consolidated
  duplicate create-response projection under the prepared request owner, and
  then revalidated that create payloads and runtime dispatch inputs stayed
  unchanged.

  A focused audit showed that `PreparedBatchGenerationCreateRequest` already
  owned the normalized target word count and ordered chapter target list, but
  production code and tests still rebuilt two projections from the same owner:
  - `chapters_to_generate` JSON payload was assembled inline from
    `prepared.chapters_to_generate`
  - the outer `"batch_id" / "message" / "chapters_to_generate" /
    "estimated_time_minutes"` create response envelope was also assembled
    inline beside runtime dispatch

  The refactor therefore narrowed that write-workflow boundary:
  - added `PreparedBatchGenerationCreateRequest::chapters_to_generate_payload()`
  - added `PreparedBatchGenerationCreateRequest::create_response_payload(...)`
  - `start_owned_batch_generation_write_workflow(...)` now builds the create
    response through the prepared owner instead of reassembling the same JSON
    shape inline
  - focused tests now assert those surviving owner projections directly

  This keeps batch create route payload shape, chapter ordering, estimated
  minutes, runtime dispatch input, and create response contract unchanged
  while removing one more duplicated write-side projection from the batch
  create workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice stayed in the same batch create write-workflow lane, removed the
  last single-consumer task-creation wrapper, and then revalidated that task
  insert, snapshot initialization, and create response behavior stayed
  unchanged.

  A focused audit showed that `create_batch_generation_task(...)` had already
  degraded into a thin one-caller wrapper:
  - production code only called it from
    `start_owned_batch_generation_write_workflow(...)`
  - it no longer owned an independent reusable contract beyond forwarding the
    prepared chapter ids, normalized target word count, and route-derived task
    metadata into `build_batch_generation_task_active_model(...)`
  - the real owner boundary is the create workflow itself, because it already
    owns project access, prepared request loading, execution-config loading,
    create response assembly, and runtime dispatch

  The refactor therefore narrowed that write-workflow boundary:
  - removed `create_batch_generation_task(...)`
  - `start_owned_batch_generation_write_workflow(...)` now performs the task
    active-model build, insert, and initial snapshot persistence inline
    beside the surviving create-response and runtime-dispatch owners
  - internal insert/snapshot failures still map to the same
    `CreateBatchGenerationWriteWorkflowError::Internal(...)` contract

  This keeps batch create route payload shape, chapter-id ordering, snapshot
  initialization behavior, runtime dispatch input, and internal error mapping
  unchanged while removing one more fake-shared write-side owner from the
  batch create workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 10:xx +08:00 checkpoint:
  this slice moved to the single-generation workflow lane, consolidated the
  repeated prepared-request to runtime-launch projection under the prepared
  owner, and then revalidated that stream/background behavior stayed
  unchanged.

  A focused audit showed that
  `PreparedSingleChapterGenerationRequest -> SingleGenerationRuntimeLaunchInput`
  had become a thin repeated transition:
  - `create_single_generation_stream_workflow(...)` destructured the prepared
    owner and rebuilt the same runtime launch input inline
  - `start_owned_single_generation_background_workflow(...)` did the same
    before creating the background task and dispatching runtime work
  - tests also repeated the same projection contract instead of asserting one
    surviving owner boundary

  The refactor therefore narrowed that single-generation boundary:
  - added
    `PreparedSingleChapterGenerationRequest::into_runtime_launch_input(user_id)`
  - stream workflow now converts the prepared request through that owner
  - background workflow now does the same while preserving the existing task
    creation, checkpoint initialization, response payload, and runtime
    dispatch flow
  - focused tests now assert the surviving prepared-owner projection directly

  This keeps single-generation request payloads, stream launch behavior,
  background task creation, checkpoint initialization, SSE event shape/order,
  and runtime dispatch input unchanged while removing one more duplicate owner
  from the single-generation workflow lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:39 +08:00 checkpoint:
  this slice stayed in the batch runtime finalize/result lane, pulled the
  finalize persistence handoff under an explicit step-result owner, and then
  revalidated that batch runtime behavior, shared chapter-generation runtime
  behavior, and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one real
  owner split after the step-progress move:
  - `execute_batch_generation_step(...)` still manually chose between
    `finalize_batch_generation_cancelled`, `finalize_batch_generation_success`,
    and `finalize_batch_generation_failure`
  - each branch rebuilt the next `BatchGenerationStepOutcome` separately from
    the persistence plan it had just chosen
  - this meant finalize semantics were still split across “which persistence
    plan do we write?” and “which step outcome do we return?” instead of one
    explicit handoff owner

  The refactor therefore narrowed that batch finalize boundary:
  - introduced `BatchGenerationStepResult` to own
    `persistence_plan + outcome`
  - moved cancelled / missing-chapter / load-error / success /
    generation-failure result assembly under that explicit owner
  - updated `execute_batch_generation_step(...)` to return through
    `BatchGenerationStepResult::persist(...)` instead of separately selecting
    finalize helper calls and step outcomes inline
  - added a focused step-result contract test in the batch runtime-state suite

  This keeps batch chapter ordering, completed-count advancement, cancel /
  failure semantics, checkpoint persistence ordering, shared chapter-generation
  write semantics, and chapter-batch observable behavior unchanged while
  making the batch finalize/result seam more cutover-ready. Smoke comparison
  and rollback review are easier because one explicit step-result owner now
  carries both the persisted runtime decision and the returned step outcome.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
- 2026-05-29 01:33 +08:00 checkpoint:
  this slice stayed in the batch runtime execution lane, pulled the
  step-progress handoff under an explicit progress owner, and then
  revalidated that batch runtime behavior, shared chapter-generation runtime
  behavior, and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one real
  owner split in the step/result handoff:
  - `BatchGenerationStepPlan` flat-carried `completed`
  - `BatchGenerationStepOutcome::Continue` then flat-carried the updated
    `completed` value again
  - the outer runtime loop separately kept `total_chapters` and rebuilt the
    next step from `completed + session.total_chapters`, so one logical
    progress contract was still scattered across the plan, outcome, and loop

  The refactor therefore narrowed that execution boundary:
  - introduced `BatchGenerationStepProgress` to own
    `completed + total_chapters`
  - changed `BatchGenerationStepPlan` to expose `chapter_id + progress`
  - changed `BatchGenerationStepOutcome::Continue` to return
    `progress: BatchGenerationStepProgress`
  - updated the runtime loop to carry one explicit progress owner across step
    execution instead of recomputing the next boundary from flat integers
  - added focused progress-contract coverage in the batch runtime-state suite

  This keeps batch chapter ordering, completed-count advancement, cancel /
  failure semantics, runtime persistence calls, shared chapter-generation
  write semantics, and chapter-batch observable behavior unchanged while
  making the batch execution seam more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit progress contract now
  crosses the step plan -> step outcome -> loop handoff instead of being
  restated in parallel values.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
- 2026-05-29 01:27 +08:00 checkpoint:
  this slice stayed in the batch runtime-state lane, pulled the runtime
  persistence fields under an explicit task-context owner, and then
  revalidated that batch runtime behavior, shared chapter-generation runtime
  behavior, and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one real
  owner split in the persistence boundary:
  - `BatchGenerationRuntimePersistencePlan` still flat-carried
    `current_chapter_id / current_chapter_number / completed_chapters /
    total_chapters / error_message`
  - those fields always traveled together as one runtime task-state snapshot
    across `preparing / cancelled / chapter_started / chapter_succeeded /
    failed` persistence variants
  - this left the main batch runtime lane with the same duplicated
    context-shape problem that had already been removed from the single
    runtime lane

  The refactor therefore narrowed that batch runtime boundary:
  - introduced `BatchGenerationRuntimeTaskContext` to own the shared runtime
    task-state fields
  - changed `BatchGenerationRuntimePersistencePlan` to expose
    `task_context + task_stage + checkpoint_stage`
  - rewired all persistence constructors (`preparing`, `cancelled`,
    `chapter_started`, `chapter_succeeded`, `failed`) to build that explicit
    owner instead of restating the fields inline
  - added a focused task-context contract test in the batch runtime-state
    suite

  This keeps batch task status transitions, checkpoint stage ordering, cancel
  / failure semantics, completed-count handling, shared chapter-generation
  write semantics, and chapter-batch observable behavior unchanged while
  making the batch runtime seam more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit runtime task context now
  crosses the persistence boundary instead of being restated in each plan
  variant.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
- 2026-05-29 01:21 +08:00 checkpoint:
  this slice stayed in the single runtime-state lane, pulled the runtime
  result fields under an explicit task-context owner, and then revalidated
  that single runtime behavior, shared chapter-generation runtime behavior,
  and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one real
  owner split in the runtime outcome -> persistence handoff:
  - `SingleGenerationRuntimeOutcome` carried `chapter_id / chapter_number /
    word_count / error_message` as flat variant fields
  - `SingleGenerationRuntimePersistencePlan` then repeated the same runtime
    fields as another flat boundary before writing task state and checkpoint
  - this meant the single runtime lane still duplicated the same generated
    task context across two adjacent owners instead of reusing one explicit
    handoff contract

  The refactor therefore narrowed that single runtime boundary:
  - introduced `SingleGenerationRuntimeTaskContext` to own
    `chapter_id / chapter_number / word_count / error_message`
  - changed `SingleGenerationRuntimeOutcome` to carry that explicit context
    for both success and failure branches
  - changed `SingleGenerationRuntimePersistencePlan` to carry
    `task_context + task_stage + checkpoint_stage`
  - updated focused tests to validate the runtime outcome/persistence contract
    through the shared task-context owner

  This keeps runtime task status transitions, checkpoint stage ordering,
  generated chapter metadata, shared chapter-generation write semantics, and
  single/batch observable behavior unchanged while making the single runtime
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because one explicit runtime task context now crosses the outcome ->
  persistence boundary instead of being restated in parallel structs.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:16 +08:00 checkpoint:
  this slice stayed in the single background workflow lane, pulled the
  background task persistence context under an explicit task-context owner,
  and then revalidated that single background/stream behavior and
  chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one real owner
  split on the background path:
  - `SingleGenerationBackgroundLaunchPlan` exposed `task_id`, `task`,
    `checkpoint`, and `response_payload` directly next to `runtime_input`
  - those four fields all belonged to the same background task persistence /
    response boundary and were only consumed together during dispatch
  - this left the single background lane with the same flat dispatch context
    shape that had already been removed from batch create and batch resume

  The refactor therefore narrowed that single background boundary:
  - introduced `SingleGenerationBackgroundTaskContext` to own `task_id`,
    `task`, `checkpoint`, and `response_payload`
  - changed `SingleGenerationBackgroundLaunchPlan` to expose
    `task_context + runtime_input`
  - updated background dispatch logic and focused tests to read task,
    checkpoint, and response state through the explicit task-context owner

  This keeps single background response payload shape, snapshot persistence
  order, runtime dispatch semantics, SSE stream behavior, and downstream
  single/batch generation behavior unchanged while making the single
  background workflow seam more cutover-ready. Smoke comparison and rollback
  review are easier because the background task boundary is now explicit
  instead of spread across launch-plan fields.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:12 +08:00 checkpoint:
  this slice stayed in the shared chapter-generation runtime lane, pulled the
  generated write persistence context under the write-outcome owner, and then
  revalidated that single and batch runtime behavior stayed unchanged.

  A focused audit showed that
  `chapter_generation_runtime_service.rs` still kept one real owner split in
  the core write path:
  - `GeneratedChapterWriteOutcome::prepare(...)` already depended on the full
    `chapter_model` to derive the response payload and the persisted draft
    status
  - but `GeneratedChapterWriteOutcome::persist(...)` still required
    `chapter_model` to be passed back in from the outer runtime context
  - this left the shared runtime handoff split across
    `prepare(chapter_model, response)` and `persist(db, chapter_model)` even
    though both halves belonged to the same write outcome owner

  The refactor therefore narrowed that shared runtime boundary:
  - changed `GeneratedChapterWriteOutcome` to own the prepared
    `chapter_model` together with the generated response payload
  - simplified `persist(...)` so it now consumes only `self + db`
  - updated the runtime path and focused test to assert the write-outcome
    owner now carries its own persisted chapter context

  This keeps prompt assembly, AI generation order, generated payload shape,
  persisted chapter status/content/word-count semantics, and single/batch
  runtime behavior unchanged while making the shared chapter-generation
  runtime seam more cutover-ready. Smoke comparison and rollback review are
  easier because the generated write outcome now owns the full write handoff
  instead of relying on a second outer-context argument.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
- 2026-05-29 01:00 +08:00 checkpoint:
  this slice stayed in the batch resume launch lane, pulled the prepared
  resume dispatch boundary under explicit launch/task-context owners, and then
  revalidated that batch resume workflow and runtime state behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still exposed one
  real owner split after the runtime/reset work:
  - `PreparedBatchGenerationResumePlan` still carried `execution`, `task_id`,
    and `response_payload` directly even though those values only mattered at
    the launch/dispatch boundary
  - `into_runtime_dispatch_plan(...)` therefore lived one layer too high and
    the prepared plan still exposed the prepared dispatch seam instead of a
    narrower launch owner
  - this left resume write workflow and command-service tests reviewing three
    separate resume launch fields instead of one explicit launch contract

  The refactor therefore narrowed that batch resume boundary:
  - introduced `ResumeTaskContext` to own `task_id` and `response_payload`
  - introduced `PreparedBatchGenerationResumeLaunchPlan` to own
    `execution + task_context`
  - changed `PreparedBatchGenerationResumePlan` to expose only `launch`
  - moved `into_runtime_dispatch_plan(...)` onto the explicit launch owner
  - updated resume write workflow and focused tests to dispatch through
    `resume_plan.launch.into_runtime_dispatch_plan(...)`

  This keeps HTTP payload shape, resume response payload fields, runtime
  dispatch semantics, task lifecycle semantics, and single/batch selection
  behavior unchanged while making the batch resume seam more cutover-ready.
  Smoke comparison and rollback review are easier because the prepared resume
  plan no longer exposes raw dispatch boundary fields directly.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:08 +08:00 checkpoint:
  this slice stayed in the batch create write-workflow lane, pulled the
  dispatch-only task/response fields under an explicit task-context owner, and
  then revalidated that batch create workflow behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one real
  owner split in the create lane:
  - `BatchGenerationCreateLaunchPlan` exposed `task_id`, `task`,
    `total_chapters`, and `response_payload` directly even though those values
    only mattered at dispatch time
  - `BatchGenerationCreateDispatchPlan` then repeated the same boundary fields
    next to `runtime_input`, so launch/dispatch review still had to track four
    parallel task-context members
  - this was the same seam shape already removed from the resume lane, so
    keeping it here would leave cutover audit inconsistent

  The refactor therefore narrowed that batch create boundary:
  - introduced `BatchGenerationCreateTaskContext` to own `task_id`, `task`,
    `total_chapters`, and `response_payload`
  - changed `BatchGenerationCreateLaunchPlan` to expose
    `task_context + runtime_dispatch_seed`
  - changed `BatchGenerationCreateDispatchPlan` to expose
    `task_context + runtime_input`
  - updated dispatch logic and focused tests to read task/response state
    through the explicit task-context owner

  This keeps create response payload shape, task persistence semantics,
  snapshot persistence order, runtime dispatch semantics, and downstream batch
  generation behavior unchanged while making the create write-workflow seam
  more cutover-ready. Smoke comparison and rollback review are easier because
  the task-context boundary is now explicit instead of spread across launch and
  dispatch structs.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:46 +08:00 checkpoint:
  this slice stayed in the single prepare lane, pulled the remaining
  intermediate background projection helpers back under the target owner, and
  then revalidated that single background projection contracts and chapter-batch
  smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_prepare_service.rs` still kept one real but
  low-signal owner split around the single-target background projection:
  - `SingleChapterGenerationTarget::into_background_projection_plan(...)`
    already owned the full target -> background projection boundary
  - but `task_chapter_payload()`, `pending_checkpoint()`,
    `background_response_payload()`, and `background_task_active_model()` were
    still exposed as `pub(crate)` intermediate helpers even though no external
    caller needed to own those sub-projections directly
  - this left the single prepare lane with several extra projection access
    points that widened cutover audit scope without adding compatibility value

  The refactor therefore narrowed that single prepare boundary:
  - pulled `task_chapter_payload()`, `pending_checkpoint()`,
    `background_response_payload()`, and `background_task_active_model()` back
    to private target-owner helpers
  - kept `SingleChapterGenerationTarget::into_background_projection_plan(...)`
    as the only public background projection owner for the target -> task /
    checkpoint / response contract

  This keeps single background task payload shape, pending checkpoint payload,
  background response payload, and downstream workflow/runtime behavior
  unchanged while making the single prepare seam more cutover-ready. Smoke
  comparison and rollback review are easier because one explicit target owner
  now carries the full background projection contract.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:02 +08:00 checkpoint:
  this slice stayed in the single runtime-state lane, pulled the runtime
  preparation stage sequence under one explicit owner, and then revalidated
  that single runtime dispatch and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one real
  owner split around runtime preparation:
  - `execute_single_generation_runtime(...)` still owned the pre-execution
    preparing/generating persistence sequence inline
  - `SingleGenerationRuntimeOutcome` separately owned the terminal persistence
    plans, so the runtime lane still split one lifecycle contract across the
    executor and the outcome owner
  - this widened cutover review because the runtime state lane still required
    auditing both the executor body and the terminal persistence owner to
    understand the full stage transition contract

  The refactor therefore narrowed that runtime preparation boundary:
  - added `SingleGenerationRuntimePreparationPlan::new(...).persist(...)` as
    the explicit owner for the runtime preparing/generating stage sequence
  - updated `execute_single_generation_runtime(...)` to delegate the
    pre-execution task/checkpoint persistence to that owner before running the
    generation body

  This keeps single runtime task status transitions, checkpoint stage order,
  terminal persistence behavior, and downstream workflow/runtime semantics
  unchanged while making the single runtime-state seam more cutover-ready.
  Smoke comparison and rollback review are easier because one explicit plan now
  carries the full runtime preparation contract instead of the executor body
  assembling it inline.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:18 +08:00 checkpoint:
  this slice stayed in the single-chapter runtime lane, pulled the
  context-owned prompt/generate/persist handoff under one explicit runtime
  context owner, and then revalidated that single runtime behavior and
  chapter-batch smoke tests stayed unchanged.

  A focused audit showed that `chapter_generation_runtime_service.rs` still
  kept one real owner split:
  - `load_generation_context(...)` already owned the chapter/project/previous
    chapter lookup boundary
  - but `generate_and_persist_chapter_content_with_provider_payload(...)`
    still consumed that loaded context manually by rebuilding the prompt,
    calling the AI service, and then preparing/persisting the write outcome in
    the outer runtime function
  - this meant cutover review still had to audit both the loaded context
    contract and one extra runtime-local execution chain for the same
    single-chapter generation boundary

  The refactor therefore narrowed that runtime context boundary:
  - added `ChapterGenerationRuntimeContext` as the explicit owner for the
    loaded chapter/project/previous-chapter execution context
  - moved prompt construction and AI-response -> persistence handoff under
    `ChapterGenerationRuntimeContext::generate_and_persist(...)`
  - updated
    `generate_and_persist_chapter_content_with_provider_payload(...)` to
    delegate the full post-load execution chain to the context owner

  This keeps chapter access loading, project lookup, previous-chapter
  selection, prompt payload assembly, AI generation behavior, persisted draft
  updates, and response payload shape unchanged while making the
  single-chapter runtime seam more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit runtime context owner now
  carries the loaded context plus execution handoff contract.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:31 +08:00 checkpoint:
  this slice stayed in the batch runtime-state lane, pulled the repeated
  task-mutation plus checkpoint-snapshot write boundary under one explicit
  persistence owner, and then revalidated that batch runtime behavior and
  chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one real
  owner split around batch runtime persistence:
  - `prepare_batch_generation_runtime(...)`,
    `finalize_batch_generation_cancelled(...)`,
    `mark_batch_generation_chapter_started(...)`,
    `finalize_batch_generation_success(...)`, and
    `finalize_batch_generation_failure(...)` each manually replayed the same
    outer persistence shape: task mutation first, then runtime checkpoint
    snapshot write
  - the task mutation semantics already belonged to `BatchGenerationTaskStage`,
    but the final stage -> checkpoint write handoff was still spread across
    five separate functions
  - this widened cutover review because the batch runtime lane still required
    auditing several near-identical persistence branches to confirm one shared
    lifecycle contract

  The refactor therefore narrowed that batch runtime persistence boundary:
  - added `BatchGenerationRuntimePersistencePlan` as the explicit owner for
    task-stage mutation plus runtime-checkpoint snapshot persistence
  - moved the preparing/cancelled/chapter-started/chapter-succeeded/failed
    runtime branches to construct and persist one explicit plan instead of
    replaying the same double-write shape inline

  This keeps batch task status transitions, checkpoint payload shape, resume
  semantics, chapter-start/finalize stage order, and downstream runtime
  behavior unchanged while making the batch runtime-state seam more
  cutover-ready. Smoke comparison and rollback review are easier because one
  explicit plan now carries the repeated persistence contract instead of five
  separate outer branches assembling it inline.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
- 2026-05-29 01:40 +08:00 checkpoint:
  this slice stayed in the batch runtime execute-loop lane, pulled the
  single-step cancel/load/generate/finalize branching under one explicit step
  owner, and then revalidated that batch runtime behavior and chapter-batch
  smoke tests stayed unchanged.

  A focused audit showed that
  `execute_batch_generation_runtime(...)` still kept one real owner split even
  after the shared persistence-plan move:
  - the outer loop still owned the full per-chapter step contract: task
    cancelled check, chapter lookup, missing/load-error failure mapping,
    chapter-start persistence, generation execution, success finalize, and
    generation-error finalize
  - this meant the runtime lane still split one chapter-step lifecycle across
    the outer loop and multiple helper calls, so cutover review still had to
    audit a long branchy loop body to confirm step behavior

  The refactor therefore narrowed that execute-loop boundary:
  - added `BatchGenerationStepOutcome` as the explicit per-step result owner
  - added `execute_batch_generation_step(...)` to own cancelled/load-failure/
    generation-success/generation-failure branching for one chapter iteration
  - updated `execute_batch_generation_runtime(...)` so the outer loop now only
    iterates chapter ids, forwards explicit inputs, and updates `completed`
    from the returned step outcome

  This keeps batch task cancellation behavior, missing/load failure mapping,
  chapter-start/finalize order, generation execution behavior, checkpoint
  payload shape, and downstream runtime semantics unchanged while making the
  batch runtime loop seam more cutover-ready. Smoke comparison and rollback
  review are easier because one explicit step owner now carries the single
  chapter-step lifecycle instead of the outer loop assembling every branch
  inline.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
- 2026-05-29 01:49 +08:00 checkpoint:
  this slice stayed in the batch resume-reset lane, pulled the reset active
  model update plus resume-checkpoint replace boundary under one explicit plan
  owner, and then revalidated that resume semantics and chapter-batch smoke
  tests stayed unchanged.

  A focused audit showed that
  `reset_batch_generation_task_for_resume(...)` still kept one real owner
  split:
  - the function still owned the full resume-reset write sequence inline:
    resolve reset semantics, build an active model patch, apply the
    `ResumeReset` task mutation plan, persist the task update, build the reset
    checkpoint, and replace the runtime snapshot
  - the reset semantics already existed as a shared owner, but the final
    active-model + checkpoint persistence handoff was still assembled manually
    at the outer runtime-state boundary
  - this widened cutover review because resume reset still required auditing a
    multi-step write function instead of one explicit reset owner contract

  The refactor therefore narrowed that resume-reset boundary:
  - added `BatchGenerationResumeResetPlan::from_command_state(...).persist(...)`
    as the explicit owner for the reset task patch plus snapshot replace
    contract
  - updated `reset_batch_generation_task_for_resume(...)` to delegate the full
    reset write sequence to that owner instead of replaying it inline

  This keeps resume reset semantics, pending task patch shape, failed-chapters
  clearing, chapter-pointer reset behavior, resume checkpoint payload shape,
  and downstream resume dispatch behavior unchanged while making the batch
  resume lane more cutover-ready. Smoke comparison and rollback review are
  easier because one explicit reset plan now carries the full write boundary
  instead of the outer helper assembling it inline.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:58 +08:00 checkpoint:
  this slice stayed in the batch runtime session lane, pulled the shared
  generation inputs under one explicit runtime-session owner, and then
  revalidated that batch runtime behavior and chapter-batch smoke tests stayed
  unchanged.

  A focused audit showed that
  `execute_batch_generation_runtime(...)` still kept one real shared-input
  owner split:
  - the outer runtime function still owned execution-config destructuring,
    `AIService` construction, provider-payload retention, target-word-count
    retention, user-id retention, and total-chapter calculation
  - `execute_batch_generation_step(...)` already owned the per-chapter runtime
    branching, but the outer function still carried the shared generation
    session state that every step needed
  - this meant cutover review still had to audit one extra outer setup layer
    to understand the stable per-run execution contract

  The refactor therefore narrowed that runtime-session boundary:
  - added `BatchGenerationRuntimeSession::from_execution_input(...)` as the
    explicit owner for shared `AIService/user_id/target_word_count/
    provider_payload/total_chapters` runtime inputs
  - updated `execute_batch_generation_runtime(...)` to build one session owner
    plus chapter-id list and delegate per-chapter work through that session
  - updated `execute_batch_generation_step(...)` to consume the shared runtime
    session instead of receiving each shared input separately

  This keeps batch execution config flow, `AIService` creation behavior,
  provider-payload propagation, target-word-count handling, chapter iteration
  order, per-step runtime branching, and downstream runtime semantics
  unchanged while making the batch runtime lane more cutover-ready. Smoke
  comparison and rollback review are easier because one explicit session owner
  now carries the stable per-run execution inputs instead of the outer runtime
  helper assembling them inline.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
- 2026-05-29 02:06 +08:00 checkpoint:
  this slice stayed in the batch runtime step-input lane, pulled the per-step
  chapter-id/completed counter contract under one explicit step-plan owner,
  and then revalidated that batch runtime behavior and chapter-batch smoke
  tests stayed unchanged.

  A focused audit showed that `execute_batch_generation_step(...)` still kept
  one real input-owner split even after the runtime-session move:
  - the step function still accepted `chapter_id` and `completed` as separate
    scalar inputs while consuming the shared runtime session independently
  - the outer loop therefore still assembled one more ad hoc per-step contract
    inline instead of handing off one explicit step-input object
  - this kept the per-chapter step boundary slightly wider than needed for
    cutover review because the step input shape was still split across the
    loop body and the step function signature

  The refactor therefore narrowed that step-input boundary:
  - added `BatchGenerationStepPlan` as the explicit owner for one chapter-step
    input contract (`chapter_id` + `completed`)
  - updated `execute_batch_generation_step(...)` to consume that plan instead
    of separate scalar inputs
  - updated the outer loop to construct one explicit step plan per iteration
    before delegating to the step owner

  This keeps chapter iteration order, completed-count advancement, cancelled /
  missing / load-failure / generation-failure branching, and downstream
  runtime semantics unchanged while making the batch runtime lane more
  cutover-ready. Smoke comparison and rollback review are easier because one
  explicit plan now carries the per-step input contract instead of the loop
  assembling it as separate ad hoc values.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 16:44 +08:00 checkpoint:
  this slice stayed in the same single-generation workflow lane, narrowed the
  background-write owner into one explicit launch-plan boundary, and then
  revalidated that task creation, checkpoint initialization, response payload,
  and runtime dispatch behavior stayed unchanged.

  A focused audit showed that
  `start_owned_single_generation_background_workflow(...)` still mixed two
  different owner responsibilities after the earlier runtime-launch projection
  cleanup:
  - request preparation already produced the real prepared owner
  - but the background workflow still rebuilt task payload, pending snapshot,
    response payload, and runtime dispatch input inline from scattered
    prepared fields
  - this made the background path harder to compare against route-group
    cutover assets because the write-side owner boundary was still implicit

  The refactor therefore narrowed that background workflow boundary:
  - added `SingleGenerationBackgroundLaunchPlan`
  - added
    `PreparedSingleChapterGenerationRequest::into_background_launch_plan(...)`
  - background workflow now performs:
    `prepare request -> build explicit launch plan -> insert task ->
    persist pending snapshot -> dispatch runtime -> return response`
  - stream ownership and runtime-state ownership remain unchanged

  This keeps single-generation background route payload shape, pending task
  contract, checkpoint initialization, estimated minutes, and runtime dispatch
  input unchanged while making the write-side launch contract explicit in one
  place. That directly improves `chapters` cutover readiness because the
  background generation branch is now easier to audit as one Rust-owned write
  boundary instead of a sequence of route-adjacent field reconstructions.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`

- 2026-05-28 22:11 +08:00 checkpoint:
  this slice returned to the batch status stream lane, removed the read-side
  stream-state projection owner from `BatchGenerationReadContext`, and then
  revalidated that route status queries and SSE polling behavior stayed
  unchanged.

  A focused audit showed that `BatchGenerationReadContext::into_stream_state()`
  had become an owner drift point:
  - the read-context service already owned task/snapshot loading plus
    status/active payload assembly
  - but the same read-context type also projected
    `BatchGenerationStreamState`, even though only the batch status route and
    the status-stream poller consumed that semantic
  - this made stream cutover review harder because the stream owner boundary
    was split across read-context and stream services

  The refactor therefore narrowed that stream/read boundary:
  - removed `BatchGenerationReadContext::into_stream_state()`
  - added `build_stream_state_from_read_context(...)` inside
    `chapter_batch_generation_status_stream_service.rs`
  - the status stream poller now converts loaded read-context values through
    that stream-local owner
  - the batch status route now builds its initial
    `BatchGenerationStreamState` directly through
    `BatchGenerationStreamState::from_task_state(...)`

  This keeps batch status payloads, SSE polling semantics, terminal event
  behavior, and route error mapping unchanged while making stream-state
  projection belong to the actual stream consumers only. That improves
  `chapters` cutover readiness because stream smoke, rollback review, and
  fallback shrink analysis can now reason about read payload ownership and
  stream ownership separately without crossing an extra helper boundary.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`

- 2026-05-28 22:14 +08:00 checkpoint:
  this slice stayed in the same batch status stream lane, removed the last
  route-local initial stream-state projection from
  `chapter_batch_generation.rs`, and then revalidated that SSE polling
  behavior and route contracts stayed unchanged.

  A focused audit showed that the previous slice had already moved loaded
  read-context projection into the stream service, but the route still owned a
  second copy of the same stream-state semantic for the initial poll value:
  - `stream_batch_generation_status(...)` loaded owned read-context
  - then it directly called
    `BatchGenerationStreamState::from_task_state(...)` inline
  - meanwhile the poller path already used
    `build_stream_state_from_read_context(...)` inside the stream owner

  The refactor therefore finished that stream-owner consolidation:
  - made `build_stream_state_from_read_context(...)` a public(crate)
    stream-owner helper
  - the batch status route now also uses that same helper for the initial
    state instead of reconstructing stream semantics inline
  - initial-state and poll-state projection now share one owner boundary

  This keeps batch status route payloads, SSE keep-alive behavior, polling
  semantics, terminal event behavior, and route error mapping unchanged while
  making the route more transport-only. That improves `chapters` cutover
  readiness because stream smoke and rollback review can now treat the route
  as wiring only and audit all stream-state projection semantics in one Rust
  owner.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`

- 2026-05-28 22:17 +08:00 checkpoint:
  this slice returned to the batch-create route/request lane, moved the
  route-body to workflow-request projection back under the route request
  owner, and then revalidated that batch-create payload compatibility and
  write-workflow behavior stayed unchanged.

  A focused audit showed that batch create still had one lingering route-origin
  constructor living in the workflow service:
  - `chapter_batch_generation.rs` accepted the full compatibility route body
  - but it then called
    `BatchGenerationCreateWorkflowRequest::from_route_fields(...)`
  - that meant route-only defaulting and compatibility-field dropping still
    lived in the write-workflow owner instead of the transport/request owner

  The refactor therefore narrowed that request boundary:
  - added `BatchGenerationCreateRouteRequest::into_workflow_request()`
  - `create_batch_generate(...)` now converts the route body through that
    route-owned projection directly
  - removed `BatchGenerationCreateWorkflowRequest::from_route_fields(...)`
    from the write-workflow service
  - write-workflow tests now assert the minimal transport-free workflow
    contract through direct struct construction instead of a route-origin
    helper

  This keeps batch-create request payload shape, compatibility-field
  acceptance, default `enable_analysis/max_retries` semantics, write-workflow
  behavior, and response payloads unchanged while making the route/request
  boundary more honest about where route-only fields are interpreted and
  discarded. That improves `chapters` cutover readiness because the route is
  now the only owner of compatibility request projection, while the workflow
  service consumes a fully transport-free request contract.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`

- 2026-05-28 22:20 +08:00 checkpoint:
  this slice stayed in the batch-create write-workflow lane, introduced one
  explicit create launch-plan owner, and then revalidated that task creation,
  snapshot initialization, response payload assembly, and runtime dispatch
  behavior stayed unchanged.

  A focused audit showed that `start_owned_batch_generation_write_workflow(...)`
  still spread one real write-side contract across several local steps:
  - prepared create request owned chapter targets and normalized word count
  - but task active-model construction, total-chapter counting, response
    payload assembly, and runtime dispatch input were still re-derived inline
    in the workflow function
  - this made the batch-create write path harder to review as one cutover
    boundary than the newer single-generation background launch-plan lane

  The refactor therefore narrowed that write-side boundary:
  - added `BatchGenerationCreateLaunchPlan`
  - added `BatchGenerationCreateRuntimeDispatchSeed`
  - added
    `PreparedBatchGenerationCreateRequest::into_create_launch_plan(...)`
  - batch create now performs:
    `prepare request -> build explicit launch plan -> insert task ->
    persist snapshot -> load execution config -> dispatch runtime -> return
    response`

  This keeps batch-create payload shape, chapter ordering, snapshot
  initialization, runtime dispatch input, and response payloads unchanged
  while making task creation and dispatch assembly belong to one explicit
  write owner. That improves `chapters` cutover readiness because the create
  write path is now easier to audit, smoke, and compare against rollback
  boundaries without chasing the same contract across several inline locals.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`

- 2026-05-28 22:25 +08:00 checkpoint:
  this slice returned to the single-generation background-write lane, moved
  target-derived payload/checkpoint/response semantics under the chapter-target
  owner, and then revalidated that background task creation, checkpoint
  initialization, response payload shape, and runtime dispatch stayed
  unchanged.

  A focused audit showed that the background launch-plan boundary was already
  explicit, but it still manually rebuilt three values directly from
  `chapter_target` fields:
  - task chapter payload JSON
  - pending checkpoint payload
  - background response envelope
  - that meant `SingleGenerationBackgroundLaunchPlan` still knew too much
    about how a chapter target projects into the background-write transport
    contract

  The refactor therefore narrowed that owner split:
  - added `SingleChapterGenerationTarget::task_chapter_payload()`
  - added `SingleChapterGenerationTarget::pending_checkpoint()`
  - added
    `SingleChapterGenerationTarget::background_response_payload(...)`
  - `chapter_single_generation_workflow_service.rs` now consumes those target
    owner helpers instead of hand-building the same three projections inline

  This keeps single-generation background route payload shape, pending
  checkpoint semantics, task chapter payload order/content, and runtime
  dispatch input unchanged while making chapter-target derived write-side
  semantics belong to one explicit owner. That improves `chapters` cutover
  readiness because the single-generation background lane is now easier to
  audit as: request prep owner -> target owner -> launch-plan owner -> runtime
  owner, with less field-level duplication in the workflow seam.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`

- 2026-05-28 22:28 +08:00 checkpoint:
  this slice stayed in the same single-generation background-write lane,
  moved background task active-model derivation under the chapter-target owner,
  and then revalidated that pending task persistence defaults, checkpoint
  initialization, response payload shape, and runtime dispatch stayed
  unchanged.

  A focused audit showed that the previous slice had already moved three
  target-derived payloads under `SingleChapterGenerationTarget`, but one
  remaining write-side projection still lived in the workflow seam:
  - `SingleGenerationBackgroundLaunchPlan` no longer hand-built checkpoint or
    response payload
  - but it still constructed the background task active-model inline from the
    same chapter-target fields plus the normalized word-count input
  - this left the workflow owner partially responsible for both transport
    coordination and target-derived persistence shape

  The refactor therefore narrowed that owner split again:
  - added `SingleChapterGenerationTarget::background_task_active_model(...)`
  - `chapter_single_generation_workflow_service.rs` now consumes that target
    owner helper instead of rebuilding the single-chapter task model inline
  - the background launch-plan owner is now closer to pure orchestration over
    target-derived inputs rather than field-by-field persistence assembly

  This keeps single-generation background task defaults, current chapter
  pointer, target word count, pending response payload, checkpoint semantics,
  and runtime dispatch input unchanged while making target-derived persistence
  semantics belong to one explicit owner. That improves `chapters` cutover
  readiness because the background write lane is now easier to review as a
  sequence of owner boundaries instead of a workflow seam that still contains
  partial persistence-shape assembly.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 22:35 +08:00 checkpoint:
  this slice stayed in the single-generation runtime-launch lane, collapsed the
  last duplicated runtime-input projection into one prepared-request owner, and
  then revalidated that background task creation, stream launch wiring, and
  runtime dispatch behavior stayed unchanged.

  A focused audit showed that the previous background-write slices had already
  moved most target-derived write semantics under the chapter-target owner, but
  one runtime dispatch contract still had two owners:
  - `into_runtime_launch_input(...)` built `SingleGenerationRuntimeLaunchInput`
    for the stream lane
  - `into_background_launch_plan(...)` rebuilt the same runtime-input shape for
    the background lane before dispatch
  - this meant the same runtime launch contract could still drift between the
    stream and background paths even though both ultimately fed the same Rust
    runtime owner

  The refactor therefore narrowed that runtime-launch boundary:
  - added `into_runtime_launch_input_parts(...)` on
    `PreparedSingleChapterGenerationRequest`
  - this helper now returns the `chapter_target` plus the single owned
    `SingleGenerationRuntimeLaunchInput` projection together
  - `into_runtime_launch_input(...)` now delegates to that shared owner
  - `into_background_launch_plan(...)` now reuses the same shared runtime-input
    projection instead of rebuilding it inline

  This keeps single-generation stream launch payloads, background task
  persistence defaults, checkpoint initialization, runtime dispatch input, and
  response payloads unchanged while making the runtime launch contract belong
  to one explicit prepared-request owner. That improves `chapters` cutover
  readiness because the single-generation lane now has one narrower handoff for
  both stream and background execution, which makes smoke comparison and
  rollback review easier across the two entry paths.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 22:40 +08:00 checkpoint:
  this slice stayed in the batch-resume write lane, moved resume execution ->
  runtime launch projection under the execution-plan owner, and then
  revalidated that resume response payloads, reset semantics, and runtime
  dispatch behavior stayed unchanged.

  A focused audit showed that the resume write workflow still carried one real
  owner split after the previous launch-plan cleanup:
  - `prepare_batch_generation_resume(...)` already owned the semantic branch
    between single-chapter and batch resume execution
  - but `resume_owned_batch_generation_write_workflow(...)` still matched on
    `ResumeExecutionPlan` and manually assembled two different runtime launch
    input shapes inline
  - this meant the write-workflow owner still knew single-vs-batch runtime
    field projection details even though that contract already belonged to the
    resume execution-selection owner

  The refactor therefore narrowed that resume-dispatch boundary:
  - added `ResumeRuntimeLaunchPlan`
  - added `ResumeExecutionPlan::into_runtime_launch_plan(...)`
  - the resume command owner now converts semantic resume selection plus
    prepared execution config into the exact runtime launch contract
  - the write workflow now only prepares execution config, asks the execution
    owner for a runtime launch plan, and dispatches the returned launch input

  This keeps resume gating, response payload shape, checkpoint reset semantics,
  target word count normalization, single-vs-batch dispatch choice, and route
  error mapping unchanged while making the batch resume lane more cutover-ready.
  Smoke comparison and rollback review are now easier because the same owner
  that decides resume execution shape also owns the launch contract that feeds
  runtime dispatch.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 22:42 +08:00 checkpoint:
  this slice stayed in the batch-create write lane, moved create dispatch-seed
  -> execution-input projection under the dispatch-seed owner, and then
  revalidated that task creation, snapshot initialization, create response
  payloads, and runtime dispatch behavior stayed unchanged.

  A focused audit showed that the create write workflow still had one lingering
  field-level launch assembly after the recent resume-lane cleanup:
  - `PreparedBatchGenerationCreateRequest::into_create_launch_plan(...)`
    already owned the create task model, response payload, and runtime dispatch
    seed contract
  - but `start_owned_batch_generation_write_workflow(...)` still manually
    rebuilt `BatchGenerationExecutionInput` inline from
    `runtime_dispatch_seed.user_id/chapter_ids/target_word_count`
  - this meant the create workflow still knew the runtime execution-input field
    shape even though those fields already belonged to the dispatch-seed owner

  The refactor therefore narrowed that create-dispatch boundary:
  - added `BatchGenerationCreateRuntimeDispatchSeed::into_execution_input(...)`
  - the create workflow now only prepares execution config and asks the
    dispatch-seed owner for the final runtime execution input
  - create and resume write lanes are now more structurally aligned: the owner
    that carries launch semantics also owns the final runtime-input projection

  This keeps batch-create payload shape, estimated time semantics, snapshot
  initialization, target word count normalization, runtime dispatch input, and
  route error mapping unchanged while making the create lane more cutover-ready.
  Smoke comparison and rollback review are easier because the create write
  workflow now keeps less knowledge about runtime field assembly and stays
  closer to pure orchestration.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-28 22:45 +08:00 checkpoint:
  this slice stayed in the batch-resume command lane, replaced the anonymous
  resume tuple handoff with one explicit prepared resume plan owner, and then
  revalidated that resume payloads, reset semantics, and runtime dispatch
  behavior stayed unchanged.

  A focused audit showed that the recent resume-dispatch cleanup still left one
  implicit contract in place:
  - `prepare_batch_generation_resume(...)` already owned the semantic trio of
    execution selection, resumed task id, and response payload
  - but it returned those three values as an anonymous tuple
  - `resume_owned_batch_generation_write_workflow(...)` therefore depended on
    positional unpacking instead of consuming one explicit resume handoff owner

  The refactor therefore narrowed that command/write boundary:
  - added `PreparedBatchGenerationResumePlan`
  - `prepare_batch_generation_resume(...)` now returns that explicit plan
    instead of `(execution, task_id, response_payload)`
  - the write workflow now consumes `resume_plan.execution`,
    `resume_plan.task_id`, and `resume_plan.response_payload` through one
    named contract

  This keeps resume gating, response payload shape, reset checkpoint semantics,
  single-vs-batch launch selection, target word count normalization, and route
  error mapping unchanged while making the batch resume lane more cutover-ready.
  Smoke comparison and rollback review are easier because the command owner now
  exposes one explicit handoff artifact instead of a positional tuple.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 22:48 +08:00 checkpoint:
  this slice stayed in the single-generation background-write lane, moved the
  background task id handoff fully under the launch-plan owner, and then
  revalidated that task creation, checkpoint initialization, background
  response payloads, and runtime dispatch behavior stayed unchanged.

  A focused audit showed that the background launch plan was already close to a
  full owner boundary, but one persistence-coupled handoff still leaked back to
  the workflow:
  - `into_background_launch_plan(...)` already owned the response payload,
    checkpoint payload, task active-model, and runtime input around one
    generated `task_id`
  - but after insert, the workflow still switched back to `created_task.id` for
    snapshot persistence and runtime dispatch
  - this left the workflow partially dependent on the inserted model return
    shape instead of consuming one explicit background execution plan

  The refactor therefore narrowed that background-write boundary:
  - added `task_id` to `SingleGenerationBackgroundLaunchPlan`
  - `into_background_launch_plan(...)` now keeps that explicit task id beside
    the task active-model, checkpoint, runtime input, and response payload
  - the workflow now uses `launch_plan.task_id` for snapshot persistence and
    runtime dispatch instead of reading the inserted model id back out

  This keeps background task response payload shape, checkpoint semantics,
  task persistence defaults, runtime dispatch input, and route error mapping
  unchanged while making the single-generation background lane more cutover-ready.
  Smoke comparison and rollback review are easier because the launch owner now
  exposes one explicit task-id handoff for the whole write path.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
- 2026-05-28 22:50 +08:00 checkpoint:
  this slice stayed in the shared single-generation launch lane, replaced the
  anonymous `chapter_target + runtime_input` tuple with one explicit runtime
  launch plan owner, and then revalidated that stream launch, background task
  creation, checkpoint persistence, and runtime dispatch behavior stayed
  unchanged.

  A focused audit showed that the single-generation write/stream cleanup still
  left one implicit shared contract between the two entry paths:
  - `into_runtime_launch_input_parts(...)` already owned the shared handoff
    from prepared request to `chapter_target` plus `runtime_input`
  - but it returned those two values as an anonymous tuple
  - both `into_runtime_launch_input(...)` and
    `into_background_launch_plan(...)` therefore depended on positional tuple
    unpacking instead of consuming one explicit shared launch owner

  The refactor therefore narrowed that single-generation boundary:
  - added `SingleGenerationRuntimeLaunchPlan`
  - replaced `into_runtime_launch_input_parts(...)` with
    `into_runtime_launch_plan(...)`
  - the stream lane now consumes `runtime_launch_plan.runtime_input`
  - the background lane now consumes the named `chapter_target` and
    `runtime_input` fields from the same explicit launch owner

  This keeps single-generation stream payloads, background task defaults,
  checkpoint semantics, runtime dispatch input, and route error mapping
  unchanged while making the shared single-generation lane more cutover-ready.
  Smoke comparison and rollback review are easier because the common handoff is
  now one named plan instead of a positional tuple.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
- 2026-05-28 22:53 +08:00 checkpoint:
  this slice stayed in the batch-create write lane, moved the create task id
  handoff fully under the launch-plan owner, and then revalidated that task
  creation, snapshot initialization, create response payloads, and runtime
  dispatch behavior stayed unchanged.

  A focused audit showed that the recent create-dispatch cleanup still left one
  persistence-coupled handoff leaking back to the workflow:
  - `into_create_launch_plan(...)` already owned the create response payload,
    task active-model, total-chapter count, and runtime dispatch seed around a
    generated `task_id`
  - but after insert, the workflow still switched back to `created_task.id` for
    snapshot persistence and runtime dispatch
  - this left the workflow partially dependent on the inserted model return
    shape instead of consuming one explicit create execution plan

  The refactor therefore narrowed that batch-create boundary:
  - added `task_id` to `BatchGenerationCreateLaunchPlan`
  - `into_create_launch_plan(...)` now keeps that explicit task id beside the
    task active-model, response payload, total chapters, and dispatch seed
  - the workflow now uses `launch_plan.task_id` for snapshot persistence and
    runtime dispatch instead of reading the inserted model id back out

  This keeps batch-create payload shape, estimated time semantics, snapshot
  initialization, runtime dispatch input, and route error mapping unchanged
  while making the create lane more cutover-ready. Smoke comparison and
  rollback review are easier because the launch owner now exposes one explicit
  task-id handoff for the whole write path.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-28 23:01 +08:00 checkpoint:
  this slice stayed in the batch-create write lane, pulled the repeated chapter
  projection handoff under one explicit owner, and then revalidated that create
  task persistence, response payload shape, and runtime dispatch behavior stayed
  unchanged.

  A focused audit showed that the recent create-lane cleanup still left one
  repeated field-projection seam spread across three consumers:
  - `PreparedBatchGenerationCreateRequest` still projected `chapter_ids()` for
    runtime dispatch
  - then rebuilt `chapter_id_payload` for task persistence
  - and separately rebuilt `chapters_to_generate_payload()` for the create
    response payload
  - this meant the same chapter boundary had no single owner for the runtime /
    task / response projections that all come from one prepared request

  The refactor therefore narrowed that batch-create boundary:
  - added `BatchGenerationCreateChapterProjectionPlan`
  - `PreparedBatchGenerationCreateRequest::into_chapter_projection_plan(...)`
    now owns `chapter_ids`, `chapter_id_payload`,
    `chapters_to_generate_payload`, and `total_chapters`
  - `into_create_launch_plan(...)` now consumes that single projection owner
    for task active-model creation, response payload construction, and runtime
    dispatch seed setup

  This keeps batch-create payload shape, chapter ordering, estimated time
  semantics, snapshot initialization, runtime dispatch input, and route error
  mapping unchanged while making the create lane more cutover-ready. Smoke
  comparison and rollback review are easier because one explicit projection
  owner now defines the chapter handoff consumed by all three downstream paths.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_model_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:06 +08:00 checkpoint:
  this slice returned to the single-generation write lane, moved the background
  launch assembly under the shared runtime launch owner, and then revalidated
  that stream launch, background task creation, checkpoint persistence, and
  runtime dispatch behavior stayed unchanged.

  A focused audit showed that the earlier single-generation cleanup still left
  one owner split inside the background path:
  - `PreparedSingleChapterGenerationRequest::into_runtime_launch_plan(...)`
    already owned the shared `chapter_target + runtime_input` handoff for the
    stream and background lanes
  - but `into_background_launch_plan(...)` still lived on the prepared request
    owner and internally re-derived the runtime launch plan before assembling
    task/checkpoint/response/runtime dispatch data
  - this left the background lane partially bypassing the shared runtime launch
    owner instead of consuming it directly

  The refactor therefore narrowed that single-generation boundary:
  - moved `into_background_launch_plan(...)` from
    `PreparedSingleChapterGenerationRequest` onto
    `SingleGenerationRuntimeLaunchPlan`
  - background workflow startup now flows through
    `prepare_single_chapter_generation_request(...) ->
    into_runtime_launch_plan(...) -> into_background_launch_plan(...)`
  - the focused workflow test now asserts the real background launch-plan owner
    instead of reconstructing task/checkpoint/runtime pieces manually

  This keeps single-generation background payload shape, task defaults,
  checkpoint semantics, runtime dispatch input, and route error mapping
  unchanged while making the shared single-generation lane more cutover-ready.
  Smoke comparison and rollback review are easier because both stream and
  background now consume the same explicit runtime launch owner before diverging
  into their transport-specific execution paths.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
- 2026-05-28 23:10 +08:00 checkpoint:
  this slice returned to the batch-resume write lane, moved runtime dispatch
  assembly under the prepared resume owner, and then revalidated that resume
  response payloads, task-id routing, and single-vs-batch runtime dispatch
  behavior stayed unchanged.

  A focused audit showed that the earlier resume cleanup still left one owner
  split in the write workflow:
  - `PreparedBatchGenerationResumePlan` already owned the resumed execution
    selection, `task_id`, and final `response_payload`
  - but the workflow still unpacked `resume_plan.execution` separately and
    called `into_runtime_launch_plan(...)` itself before dispatching with
    `resume_plan.task_id`
  - this meant the runtime dispatch contract was still partially assembled
    outside the explicit resume owner

  The refactor therefore narrowed that batch-resume boundary:
  - added `PreparedBatchGenerationResumeDispatchPlan`
  - added
    `PreparedBatchGenerationResumePlan::into_runtime_dispatch_plan(...)`
  - resume workflow startup now flows through
    `prepare_batch_generation_resume(...) -> into_runtime_dispatch_plan(...)`
    before branching to single-chapter vs batch runtime dispatch
  - the focused resume-command test now asserts the real dispatch-plan owner
    instead of only asserting the inner execution launch enum

  This keeps resume payload shape, task-id handoff, execution-config use,
  runtime dispatch target selection, and route error mapping unchanged while
  making the resume lane more cutover-ready. Smoke comparison and rollback
  review are easier because one explicit prepared-resume owner now defines the
  dispatch contract consumed by the write workflow.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:15 +08:00 checkpoint:
  this slice returned to the batch-create write lane, moved runtime dispatch
  assembly under the create launch owner, and then revalidated that create task
  persistence, snapshot initialization, response payload shape, and runtime
  dispatch behavior stayed unchanged.

  A focused audit showed that the earlier batch-create cleanup still left one
  owner split in the write workflow:
  - `BatchGenerationCreateLaunchPlan` already owned `task_id`, task
    active-model, total chapters, response payload, and runtime dispatch seed
  - but the workflow still unpacked `runtime_dispatch_seed` itself and called
    `into_execution_input(...)` outside the owner before dispatching
  - this meant the final create dispatch contract was still partially assembled
    in the workflow instead of being consumed as one explicit owner

  The refactor therefore narrowed that batch-create boundary:
  - added `BatchGenerationCreateDispatchPlan`
  - added `BatchGenerationCreateLaunchPlan::into_runtime_dispatch_plan(...)`
  - the create workflow now prepares execution config once, converts the launch
    owner into one dispatch owner, and then consumes only that owner for task
    insert, snapshot initialization, runtime dispatch, and response return
  - the focused workflow test now asserts the real dispatch-plan owner instead
    of only asserting the inner runtime-dispatch seed projection

  This keeps batch-create payload shape, task defaults, chapter ordering,
  snapshot initialization, runtime dispatch input, and route error mapping
  unchanged while making the create lane more cutover-ready. Smoke comparison
  and rollback review are easier because one explicit create-dispatch owner now
  defines the contract consumed by the write workflow after launch preparation.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_model_service -- --nocapture`
- 2026-05-28 23:18 +08:00 checkpoint:
  this slice returned to the single-generation prepare/background lane, pulled
  the background task/checkpoint/response projection under one explicit target
  owner, and then revalidated that background task creation, checkpoint
  persistence, response payload shape, and runtime launch behavior stayed
  unchanged.

  A focused audit showed that the earlier single-generation launch cleanup
  still left one field-level projection split on the chapter target owner:
  - `SingleChapterGenerationTarget` still exposed three separate background
    helpers: `pending_checkpoint()`, `background_response_payload(...)`, and
    `background_task_active_model(...)`
  - `SingleGenerationRuntimeLaunchPlan::into_background_launch_plan(...)`
    consumed those three helpers separately when assembling the background lane
  - this meant the target-specific background contract still had no one owner
    for the task/checkpoint/response projection consumed by the launch plan

  The refactor therefore narrowed that single-generation boundary:
  - added `SingleChapterGenerationBackgroundProjectionPlan`
  - added
    `SingleChapterGenerationTarget::into_background_projection_plan(...)`
  - the background launch plan now consumes that one target-owned projection
    plan instead of independently assembling task/checkpoint/response fields
  - the focused prepare-service test now asserts the real background
    projection owner directly

  This keeps single-generation background payload shape, task defaults,
  checkpoint semantics, runtime launch input, and route error mapping unchanged
  while making the background lane more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit target-owned projection now
  defines the background task/checkpoint/response contract consumed by the
  write workflow.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:27 +08:00 checkpoint:
  this slice returned to the single-generation runtime lane, pulled the
  success/failure persistence steps under one explicit runtime persistence
  owner, and then revalidated that task mutation, checkpoint writes, and
  runtime dispatch behavior stayed unchanged.

  A focused audit showed that the earlier single-generation cleanup still left
  one real runtime seam split inside `SingleGenerationRuntimeOutcome`:
  - `prepare_runtime(...)` already owned the "preparing -> generating"
    checkpoint transition
  - but `persist(...)` still hard-coded success/failure branches that directly
    mixed task mutation and checkpoint persistence in place
  - this meant the final runtime persistence contract still had no explicit
    owner for the stage/checkpoint/error metadata consumed by the write path

  The refactor therefore narrowed that single-generation runtime boundary:
  - added `SingleGenerationRuntimePersistencePlan`
  - added `SingleGenerationRuntimeOutcome::into_persistence_plans(...)`
  - `persist(...)` now only iterates the explicit persistence-plan owner(s)
    instead of constructing task/checkpoint writes inline per branch
  - the focused runtime-state test now asserts the real persistence plan owner
    derived from success/failure outcomes

  This keeps single-generation runtime checkpoint stages, task status mutation,
  word-count/chapter-number propagation, failure error persistence, and runtime
  dispatch behavior unchanged while making the runtime lane more cutover-ready.
  Smoke comparison and rollback review are easier because the outcome-derived
  persistence contract is now one explicit owner instead of two in-branch write
  paths.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:32 +08:00 checkpoint:
  this slice moved to the shared chapter-generation runtime writer, narrowed
  the AI-output projection plus chapter-persistence seam under one explicit
  write outcome owner, and then revalidated that single-generation response
  payload shape and chapter content persistence behavior stayed unchanged.

  A focused audit showed that `chapter_generation_runtime_service` still kept
  one real owner split in the shared write lane:
  - `GeneratedChapterPersistencePlan::prepare(...)` projected the cleaned
    content, word-count, chapter identity, and response payload fields into
    `result`
  - `persist(...)` then wrote those fields and returned that same `result`
    separately
  - this meant the runtime write contract was still expressed as "persistence
    plan + response result" instead of one explicit owner consumed by the
    shared write path

  The refactor therefore narrowed that shared runtime boundary:
  - replaced `GeneratedChapterPersistencePlan` with
    `GeneratedChapterWriteOutcome`
  - made that owner explicitly carry the response payload projection consumed
    after persistence
  - kept `persist(...)` as the only write step while returning the owner-held
    response payload after the DB update
  - updated the focused runtime test to assert the new write outcome owner
    directly

  This keeps generated chapter payload shape, chapter status transition,
  cleaned content persistence, word-count propagation, and single-generation
  workflow behavior unchanged while making the shared runtime write seam more
  cutover-ready. Smoke comparison and rollback review are easier because one
  explicit write outcome now owns both the persisted chapter projection and the
  returned response contract.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-28 23:37 +08:00 checkpoint:
  this slice moved to the batch-generation read-side owner, pulled the shared
  active/status base payload assembly under one explicit read payload plan, and
  then revalidated that status polling, active-task list views, and terminal
  task payload fields stayed unchanged.

  A focused audit showed that `chapter_batch_generation_read_context_service`
  still kept one real owner split across the read-side payload exits:
  - `into_status_task_payload(...)` and `into_active_task_payload(...)` both
    rebuilt the same base task view payload from task state plus runtime
    snapshot state
  - both exits also independently re-applied the same quality-status context
    decoration before diverging into status-only vs active-only fields
  - this meant the read-side contract for batch task payload assembly was still
    expressed as two partially duplicated exits instead of one shared owner

  The refactor therefore narrowed that read-side boundary:
  - added `BatchGenerationReadPayloadPlan`
  - added `BatchGenerationReadContext::into_payload_plan(...)`
  - active/status payload exits now both consume that one shared payload-plan
    owner, and only add their distinct fields after the base payload exists
  - the focused read-context tests now assert the new payload-plan owner
    directly

  This keeps active-task payload shape, status payload shape, retry counters,
  failed-chapter projection, quality-status decoration, and stream/query
  behavior unchanged while making the batch read-side seam more cutover-ready.
  Smoke comparison and rollback review are easier because one explicit
  read-payload owner now defines the shared task payload contract consumed by
  status view, active-task query, and stream-state readers.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
- 2026-05-28 23:40 +08:00 checkpoint:
  this slice stayed in the batch-generation query/read lane, pulled the active
  list/query response envelope under explicit query view owners, and then
  revalidated that active-task lookup, list payload shape, and status-stream
  consumers stayed unchanged.

  A focused audit showed that `chapter_batch_generation_task_view_query_service`
  still kept one real response-owner split across the read/query exits:
  - the active-task list path still hand-built `{ total, items }` inline after
    collecting shared read-context payloads
  - the active-task query path still hand-built `{ has_active_task, task }`
    inline after resolving the optional active-task payload
  - this meant the query/read envelope contract was still expressed as two
    route-adjacent inline JSON builders instead of explicit owner objects

  The refactor therefore narrowed that query-side boundary:
  - added `ActiveBatchGenerationTaskListView`
  - added `ActiveBatchGenerationQueryView`
  - both query exits now convert into explicit response-payload owners instead
    of building the outer JSON envelope inline
  - the focused query-service tests now assert both owner contracts directly

  This keeps active batch-generation list payload shape, active-task query
  payload shape, read-context consumption, and downstream stream/query behavior
  unchanged while making the query/read seam more cutover-ready. Smoke
  comparison and rollback review are easier because one explicit owner now
  defines each outer response envelope consumed by active-task list and active
  project query flows.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
- 2026-05-28 23:43 +08:00 checkpoint:
  this slice stayed in the batch-generation stream/event lane, pulled the
  progress-plus-terminal event assembly under one explicit stream event plan
  owner, and then revalidated that SSE event shape, poll-close behavior, and
  chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_event_service` still kept one real
  event-contract split:
  - `resolve_event_batch(...)` still hand-built the progress event inline
  - `terminal_events()` separately built the terminal event list
  - the close/continue decision then depended on that split assembly instead
    of consuming one explicit stream event contract owner

  The refactor therefore narrowed that stream/event boundary:
  - added `BatchGenerationStreamEventPlan`
  - added `BatchGenerationStreamState::progress_event()`
  - added `BatchGenerationStreamState::into_event_plan()`
  - `resolve_event_batch(...)` now consumes the unified event plan owner
    instead of assembling progress and terminal payloads itself
  - the focused stream-event tests now assert the new event-plan owner
    directly

  This keeps batch status-stream progress payload shape, completed result/done
  events, failed/cancelled error events, and stream close semantics unchanged
  while making the stream/event seam more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit event-plan owner now defines
  the emitted SSE batch consumed by the poll loop.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:47 +08:00 checkpoint:
  this slice stayed at the status-stream/runtime-read handoff, pulled the
  stream-level not-found and timeout exits under one explicit system-event
  owner, and then revalidated that stream polling, SSE error payload shape,
  and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service` still kept one real
  stream-level contract split:
  - normal progress/result/done/error event batches already flowed through the
    shared stream-event owner path
  - but the "task not found" and "stream timed out" exits still hand-built
    JSON payloads inline inside the poll loop
  - this meant stream system-error semantics still lived in route-adjacent loop
    code instead of one explicit stream owner

  The refactor therefore narrowed that status-stream boundary:
  - added `BatchGenerationStatusStreamSystemEvent`
  - added `BatchGenerationStatusStreamSystemEvent::payload()`
  - the poll loop now emits not-found and timeout payloads through that shared
    system-event owner instead of building the JSON inline
  - the focused status-stream tests now assert both system-event payload
    contracts directly

  This keeps stream task-not-found payload shape, stream timeout payload shape,
  status polling semantics, and downstream SSE behavior unchanged while making
  the status-stream seam more cutover-ready. Smoke comparison and rollback
  review are easier because one explicit owner now defines the non-runtime
  system-error events emitted by the poll loop.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:52 +08:00 checkpoint:
  this slice stayed at the read-state / stream-state handoff, moved stream
  state projection under the read-context owner, and then revalidated that the
  route consumer, status stream, and chapter-batch smoke tests stayed
  unchanged.

  A focused audit showed that the read-side handoff still kept one real owner
  split:
  - `BatchGenerationReadContext` already owned the task plus
    `workflow_runtime_state`
  - but `chapter_batch_generation_status_stream_service` still exposed
    `build_stream_state_from_read_context(...)` and therefore retained the
    read-context -> stream-state projection boundary
  - the route consumer in `api/chapter_batch_generation.rs` also still
    imported that helper directly, so the projection contract had not yet been
    fully pulled under the read-side owner

  The refactor therefore narrowed that handoff boundary:
  - added `BatchGenerationReadContext::into_stream_state()`
  - removed the standalone `build_stream_state_from_read_context(...)` helper
    from `chapter_batch_generation_status_stream_service.rs`
  - updated the status stream poll loop to consume `context.into_stream_state()`
  - updated `api/chapter_batch_generation.rs` to consume the read-context owner
    directly when building the initial stream state
  - added focused read-context tests for the new stream-state owner contract

  This keeps status stream payload shape, stream polling behavior, route
  transport behavior, and stream-state semantics unchanged while making the
  read/stream seam more cutover-ready. Smoke comparison and rollback review are
  easier because one explicit read-context owner now defines the transition
  from persisted task/snapshot state into stream-state semantics.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-28 23:57 +08:00 checkpoint:
  this slice stayed in the active-task read/query lane, pulled the repeated
  task -> read-context -> active-payload projection under one shared loader,
  and then revalidated that active list/query payloads and chapter-batch smoke
  tests stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_task_view_query_service` still kept one real
  projection split:
  - the active-task list path and the active-task query path both repeated the
    same `task -> load_batch_generation_read_context_for_task(...) ->
    into_active_task_payload()` chain
  - the outer response envelopes had already been ownerized in the previous
    slice, but the shared active-payload read projection itself still lived in
    two separate exits
  - this meant cutover review still had to audit two identical read-model
    assembly paths instead of one explicit owner

  The refactor therefore narrowed that active-query boundary:
  - added shared `load_active_task_payload(...)`
  - active-task list and active-task project query now both consume that one
    shared loader before building their existing response envelopes
  - focused query-service tests now cover the shared active-payload projection
    contract explicitly

  This keeps active-task payload shape, active list/query response shape, and
  downstream read/query behavior unchanged while making the active-task
  read-model seam more cutover-ready. Smoke comparison and rollback review are
  easier because one explicit loader now defines the shared active-payload
  projection consumed by both query exits.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_task_view_query_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:06 +08:00 checkpoint:
  this slice stayed at the status/stream route consumer boundary, pulled the
  repeated read-context -> status payload / stream-state projection under
  service-owned loaders, and then revalidated that route transport behavior
  and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that `api/chapter_batch_generation.rs` still kept one
  real consumer split:
  - `get_batch_generation_status` still loaded owned read context and then
    projected it into the status payload at the route edge
  - `stream_batch_generation_status` still loaded owned read context and then
    projected it into the initial stream state at the route edge
  - the read-context owner already had the necessary task plus runtime/snapshot
    semantics, so keeping those final projections in the route left transport
    code holding one more read-side ownership boundary than needed

  The refactor therefore narrowed that route consumer boundary:
  - added `load_owned_batch_generation_status_payload(...)` under
    `chapter_batch_generation_read_context_service.rs`
  - added `load_owned_batch_generation_stream_initial_state(...)` under
    `chapter_batch_generation_status_stream_service.rs`
  - updated `api/chapter_batch_generation.rs` status/stream routes to consume
    those service-owned loaders directly instead of projecting from read
    context inline

  This keeps status payload shape, SSE initialization behavior, route auth and
  error mapping, and downstream stream polling semantics unchanged while
  making the status/stream read-side seam more cutover-ready. Smoke comparison
  and rollback review are easier because the route is now transport-only for
  both status and stream initial-state reads.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:06 +08:00 checkpoint:
  this slice stayed in the batch resume write lane, pulled the remaining
  single/batch runtime dispatch branch under the prepared resume dispatch
  owner, and then revalidated that resume responses and chapter-batch smoke
  tests stayed unchanged.

  A focused audit showed that `chapter_batch_generation_write_workflow_service`
  still kept one real owner split after the previous resume-plan work:
  - `prepare_batch_generation_resume(...)` already returned an explicit
    prepared dispatch owner with `runtime_launch`, `task_id`, and
    `response_payload`
  - but the write workflow still matched on `ResumeRuntimeLaunchPlan` itself
    and chose between single-chapter runtime dispatch and batch runtime
    dispatch at the outer workflow boundary
  - this meant cutover review still had to audit both the prepared resume
    owner and one extra workflow-local dispatch branch for the same contract

  The refactor therefore narrowed that resume dispatch boundary:
  - added `PreparedBatchGenerationResumeDispatchPlan::dispatch(...)` to own the
    final `ResumeRuntimeLaunchPlan` branching
  - updated `resume_owned_batch_generation_write_workflow(...)` to consume that
    owner directly instead of branching on runtime launch inline
  - removed the now-unused route-local resume dispatch imports from the write
    workflow service

  This keeps resume gating, prepared execution config flow, response payload
  shape, task-id ownership, and runtime dispatch behavior unchanged while
  making the resume write seam more cutover-ready. Smoke comparison and
  rollback review are easier because one explicit prepared dispatch owner now
  carries both runtime launch selection and final payload handoff.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:12 +08:00 checkpoint:
  this slice stayed in the batch create write lane, pulled the remaining
  task insert / snapshot seed / runtime dispatch consumption under the create
  dispatch owner, and then revalidated that create responses and chapter-batch
  smoke tests stayed unchanged.

  A focused audit showed that `chapter_batch_generation_write_workflow_service`
  still kept one real owner split on the create path:
  - `PreparedBatchGenerationCreateRequest` already narrowed to an explicit
    create launch plan and then to an explicit create dispatch plan
  - but `start_owned_batch_generation_write_workflow(...)` still consumed that
    dispatch owner manually by performing task insert, initial snapshot
    creation, runtime dispatch, and final response handoff at the outer
    workflow boundary
  - this meant cutover review still had to audit both the prepared create
    dispatch owner and one extra workflow-local execution branch for the same
    launch contract

  The refactor therefore narrowed that create dispatch boundary:
  - added `BatchGenerationCreateDispatchPlan::dispatch(...)` to own the final
    create-path insert / snapshot / runtime dispatch sequence
  - updated `start_owned_batch_generation_write_workflow(...)` to consume that
    owner directly instead of replaying the execution steps inline

  This keeps project-access gating, prepared execution config flow, task-id
  ownership, response payload shape, initial snapshot semantics, and runtime
  dispatch behavior unchanged while making the create write seam more
  cutover-ready. Smoke comparison and rollback review are easier because one
  explicit prepared dispatch owner now carries the full create-path execution
  handoff.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:17 +08:00 checkpoint:
  this slice stayed in the single-chapter background write lane, pulled the
  remaining task insert / runtime snapshot seed / runtime dispatch consumption
  under the background launch owner, and then revalidated that single-chapter
  background responses and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one real owner
  split on the background path:
  - `PreparedSingleChapterGenerationRequest` already narrowed to an explicit
    runtime launch plan and then to an explicit background launch plan
  - but `start_owned_single_generation_background_workflow(...)` still
    consumed that owner manually by performing task insert, initial snapshot
    creation, runtime dispatch, and final response handoff at the outer
    workflow boundary
  - this meant cutover review still had to audit both the prepared background
    launch owner and one extra workflow-local execution branch for the same
    contract

  The refactor therefore narrowed that background dispatch boundary:
  - added `SingleGenerationBackgroundLaunchPlan::dispatch(...)` to own the
    final background-path insert / snapshot / runtime dispatch sequence
  - updated `start_owned_single_generation_background_workflow(...)` to
    consume that owner directly instead of replaying the execution steps inline

  This keeps chapter-access gating, prepared execution config flow, task-id
  ownership, response payload shape, pending checkpoint semantics, and runtime
  dispatch behavior unchanged while making the single-chapter background write
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because one explicit prepared background owner now carries the full runtime
  handoff.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:22 +08:00 checkpoint:
  this slice stayed in the single-chapter stream launch lane, pulled the
  remaining prepared-request -> runtime-stream handoff under the runtime
  launch owner, and then revalidated that single-chapter stream behavior and
  chapter-batch smoke tests stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one real owner
  split on the stream path:
  - `PreparedSingleChapterGenerationRequest` already narrowed to an explicit
    runtime launch plan
  - but `create_single_generation_stream_workflow(...)` still consumed that
    owner manually by extracting `runtime_input` and then calling the stream
    launcher at the outer workflow boundary
  - this meant cutover review still had to audit both the prepared runtime
    launch owner and one extra workflow-local stream handoff for the same
    contract

  The refactor therefore narrowed that stream launch boundary:
  - added `SingleGenerationRuntimeLaunchPlan::into_stream(...)` to own the
    final runtime-input -> stream handoff
  - updated `create_single_generation_stream_workflow(...)` to consume the
    runtime launch owner directly instead of extracting launch input inline

  This keeps chapter-access gating, prepared execution config flow, SSE keep
  alive behavior, stream event order, and runtime execution behavior unchanged
  while making the single-chapter stream seam more cutover-ready. Smoke
  comparison and rollback review are easier because one explicit runtime
  launch owner now carries the final stream handoff.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 00:29 +08:00 checkpoint:
  this slice stayed in the route request lane, pulled the default-value and
  minimal-request assembly for batch-create and single-generation requests
  under the service request owners, and then revalidated that route payload
  contracts and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that `api/chapter_batch_generation.rs` still kept one
  real request-owner split:
  - `BatchGenerationCreateRouteRequest::into_workflow_request()` still owned
    the batch-create defaulting and route-payload -> workflow-request assembly
  - `SingleChapterGenerationRouteRequest::into_generation_request()` still
    owned the single-generation route-payload -> request assembly
  - those request shapes are consumed as service-layer contracts, so keeping
    their last-mile construction in the route left transport code holding one
    more domain request boundary than needed

  The refactor therefore narrowed that route request boundary:
  - added `BatchGenerationCreateWorkflowRequest::from_route_payload(...)` so
    the batch-create request owner now owns its own defaulting and minimal
    route-to-domain assembly contract
  - added `SingleChapterGenerationRequest::from_route_payload(...)` so the
    single-generation request owner now owns its own route-to-domain assembly
    contract
  - updated `chapter_batch_generation.rs` to call those service-owned
    constructors instead of rebuilding the request shapes inline

  This keeps route payload shape, default values, write-workflow inputs, and
  downstream single/batch generation behavior unchanged while making the route
  request seam more cutover-ready. Smoke comparison and rollback review are
  easier because the service request owners now define their own transport
  assembly contracts instead of the route layer doing that work.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
- 2026-05-29 00:35 +08:00 checkpoint:
  this slice stayed in the route transport lane, removed the remaining
  route-local pure-forward request wrappers, and then revalidated that route
  payload contracts and chapter-batch smoke tests stayed unchanged.

  A focused audit showed that `api/chapter_batch_generation.rs` still kept one
  low-signal but real owner split even after the previous request-owner move:
  - `BatchGenerationCreateRouteRequest::into_workflow_request()` no longer
    owned any defaulting or domain semantics; it only forwarded fields into
    `BatchGenerationCreateWorkflowRequest::from_route_payload(...)`
  - `SingleChapterGenerationRouteRequest::into_generation_request()` likewise
    no longer owned any domain semantics; it only forwarded fields into
    `SingleChapterGenerationRequest::from_route_payload(...)`
  - keeping those wrappers in the route still left transport code owning two
    extra indirection points without adding any compatibility value

  The refactor therefore narrowed that transport boundary:
  - removed the route-local `into_workflow_request()` wrapper from
    `BatchGenerationCreateRouteRequest`
  - removed the route-local `into_generation_request()` wrapper from
    `SingleChapterGenerationRouteRequest`
  - updated the live route handlers and route-focused tests to call the
    service-owned request constructors directly

  This keeps route payload shape, request default values, downstream workflow
  inputs, and single/batch generation behavior unchanged while making the
  route layer more transport-only. Smoke comparison and rollback review are
  easier because the service request owners now define the only remaining
  request assembly boundary.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
- 2026-05-29 00:40 +08:00 checkpoint:
  this slice stayed in the single workflow lane, removed the remaining
  pure-forward runtime-launch-input wrapper, and then revalidated that single
  stream/background workflow behavior and chapter-batch smoke tests stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one real but
  low-signal owner split:
  - `PreparedSingleChapterGenerationRequest::into_runtime_launch_plan(...)`
    already owned the full prepared-request -> runtime-launch boundary
  - but `PreparedSingleChapterGenerationRequest::into_runtime_launch_input(...)`
    no longer owned any extra semantics; it only returned
    `into_runtime_launch_plan(...).runtime_input`
  - this meant the single workflow lane still had one fake-shared input owner
    that added indirection without helping cutover review

  The refactor therefore narrowed that single workflow boundary:
  - removed `PreparedSingleChapterGenerationRequest::into_runtime_launch_input(...)`
  - updated the stream workflow path and focused tests to consume
    `into_runtime_launch_plan(...).runtime_input` or `into_stream(...)`
    directly from the real launch owner

  This keeps prepared execution config flow, single stream/background payloads,
  stream event order, and runtime execution behavior unchanged while making the
  single workflow seam more cutover-ready. Smoke comparison and rollback
  review are easier because the prepared runtime-launch owner is now the only
  launch boundary in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:05 +08:00 checkpoint:
  this slice stayed in the batch runtime lane, removed the remaining
  runtime-local pure-forward chapter-started facade, and then revalidated
  that batch runtime state transitions and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one real but
  low-signal owner split after the previous step-result consolidation:
  - `mark_batch_generation_chapter_started(...)` was only called once inside
    `execute_batch_generation_step(...)`
  - it no longer owned any distinct runtime semantics; it only forwarded to
    `BatchGenerationRuntimePersistencePlan::chapter_started(...).persist(...)`
  - keeping that wrapper left an extra fake owner in the runtime hot path
    without improving cutover review or rollback auditability

  The refactor therefore narrowed that batch runtime boundary:
  - removed `mark_batch_generation_chapter_started(...)`
  - updated `execute_batch_generation_step(...)` to persist the
    `chapter_started(...)` plan directly from the real persistence owner

  This keeps batch task lifecycle semantics, checkpoint progression, error
  mapping, and runtime generation behavior unchanged while making the runtime
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because the chapter-start persistence edge now stays on the single real
  persistence owner path.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:12 +08:00 checkpoint:
  this slice stayed in the batch runtime lane, removed the remaining
  runtime-local pure-forward prepare facade, and then revalidated that batch
  runtime bootstrap behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_runtime_state_service.rs` still kept one more
  low-signal internal owner split after the chapter-started cleanup:
  - `prepare_batch_generation_runtime(...)` was only called once inside
    `execute_batch_generation_runtime(...)`
  - it no longer owned any distinct semantics; it only forwarded to
    `BatchGenerationRuntimePersistencePlan::preparing(...).persist(...)`
  - keeping that wrapper left an extra fake owner at the runtime entry seam
    without improving cutover review or rollback auditability

  The refactor therefore narrowed that runtime bootstrap boundary:
  - removed `prepare_batch_generation_runtime(...)`
  - updated `execute_batch_generation_runtime(...)` to persist the
    `preparing(...)` plan directly from the real persistence owner

  This keeps batch task lifecycle semantics, preparing checkpoint behavior,
  downstream generation execution, and error mapping unchanged while making
  the runtime seam more cutover-ready. Smoke comparison and rollback review
  are easier because the prepare-stage persistence edge now stays on the
  single real persistence owner path.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:25 +08:00 checkpoint:
  this slice stayed on the batch resume-to-runtime seam, removed the
  intermediate runtime-dispatch plan owner, and then revalidated that resume
  dispatch behavior and chapter-batch workflow behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_resume_task_command_service.rs` still kept one
  low-signal but real owner split:
  - `PreparedBatchGenerationResumeLaunchPlan` already owned the prepared
    resume execution boundary
  - `PreparedBatchGenerationResumeDispatchPlan` no longer added any new
    semantics; it only wrapped
    `execution.into_runtime_launch_plan(execution_config)` plus the same
    `task_context`, then immediately dispatched
  - keeping that intermediate plan left one extra dispatch-only owner between
    the resume command seam and the runtime seam without helping cutover
    review or rollback auditability

  The refactor therefore narrowed that resume dispatch boundary:
  - removed `PreparedBatchGenerationResumeDispatchPlan`
  - replaced `PreparedBatchGenerationResumeLaunchPlan::into_runtime_dispatch_plan(...)`
    with `PreparedBatchGenerationResumeLaunchPlan::dispatch(...)`
  - updated `chapter_batch_generation_write_workflow_service.rs` to dispatch
    directly from the launch owner
  - adjusted focused contract tests to assert the launch owner and runtime
    launch owner directly

  This keeps resume payload shape, task reset semantics, runtime selection,
  downstream single/batch runtime dispatch behavior, and error mapping
  unchanged while making the resume seam more cutover-ready. Smoke comparison
  and rollback review are easier because the prepared resume launch owner now
  owns the only remaining dispatch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:37 +08:00 checkpoint:
  this slice stayed on the single stream workflow seam, removed the
  intermediate stream-launch wrapper, and then revalidated that single stream
  behavior and chapter-batch workflow behavior stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one low-signal
  owner split after the earlier runtime-launch cleanup:
  - `SingleGenerationRuntimeLaunchPlan` still exposed `into_stream(...)`
  - that helper no longer owned any distinct workflow semantics; it only
    forwarded `runtime_input` into
    `launch_owned_single_chapter_generation_stream(...)`
  - keeping that wrapper left one extra stream-only owner between the
    prepared launch seam and the actual runtime stream launcher without
    helping cutover review or rollback auditability

  The refactor therefore narrowed that single stream boundary:
  - removed `SingleGenerationRuntimeLaunchPlan::into_stream(...)`
  - updated `create_single_generation_stream_workflow(...)` to pass the
    prepared `runtime_input` directly into
    `launch_owned_single_chapter_generation_stream(...)`

  This keeps stream event order, stream payload shape, prepared execution
  config flow, and downstream runtime behavior unchanged while making the
  single stream seam more cutover-ready. Smoke comparison and rollback review
  are easier because the prepared runtime-launch owner now exposes the only
  remaining stream launch edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 01:49 +08:00 checkpoint:
  this slice stayed on the single background workflow seam, removed the
  intermediate background task-context owner, and then revalidated that
  single background workflow behavior and chapter-batch workflow behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one low-signal
  owner split after the stream-launch cleanup:
  - `SingleGenerationBackgroundLaunchPlan` already owned the true background
    launch boundary
  - but the plan still nested a `SingleGenerationBackgroundTaskContext`
    wrapper that only carried `task_id/task/checkpoint/response_payload`
    straight through to the same launch owner
  - keeping that wrapper left one extra background-only owner between the
    prepared background projection seam and the actual dispatch seam without
    improving cutover review or rollback auditability

  The refactor therefore narrowed that single background boundary:
  - removed `SingleGenerationBackgroundTaskContext`
  - flattened `task_id/task/checkpoint/response_payload` directly onto
    `SingleGenerationBackgroundLaunchPlan`
  - updated the focused background launch-plan contract test to assert the
    flattened owner fields directly

  This keeps background response payload shape, task insert semantics,
  checkpoint persistence, runtime dispatch behavior, and error mapping
  unchanged while making the single background seam more cutover-ready. Smoke
  comparison and rollback review are easier because the background launch
  owner now directly owns the only remaining task/checkpoint dispatch edge in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:02 +08:00 checkpoint:
  this slice stayed on the batch create write-workflow seam, removed the
  intermediate create dispatch-plan owner, and then revalidated that batch
  create workflow behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept one
  low-signal but real owner split after the earlier resume dispatch cleanup:
  - `BatchGenerationCreateLaunchPlan` already owned the prepared create
    launch boundary
  - `BatchGenerationCreateDispatchPlan` no longer added any new semantics; it
    only wrapped `runtime_dispatch_seed.into_execution_input(execution_config)`
    plus the same `task_context`, then immediately inserted the task,
    persisted the snapshot, and dispatched
  - keeping that intermediate plan left one extra dispatch-only owner between
    the create launch seam and the runtime seam without helping cutover
    review or rollback auditability

  The refactor therefore narrowed that batch create dispatch boundary:
  - removed `BatchGenerationCreateDispatchPlan`
  - replaced `BatchGenerationCreateLaunchPlan::into_runtime_dispatch_plan(...)`
    with `BatchGenerationCreateLaunchPlan::dispatch(...)`
  - updated the create workflow entrypoint to dispatch directly from the
    launch owner
  - adjusted the focused contract test to assert the launch-plan runtime seed
    and runtime execution input directly

  This keeps create response payload shape, task insert semantics, snapshot
  persistence, runtime input assembly, downstream batch runtime dispatch
  behavior, and error mapping unchanged while making the batch create seam
  more cutover-ready. Smoke comparison and rollback review are easier because
  the prepared create launch owner now owns the only remaining dispatch edge
  in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:13 +08:00 checkpoint:
  this slice stayed on the batch create launch seam, flattened the remaining
  create-only carrier owners into the real launch owner, and then
  revalidated that batch create workflow behavior and chapter-batch behavior
  stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_write_workflow_service.rs` still kept two
  low-signal carrier owners even after the dispatch-plan cleanup:
  - `BatchGenerationCreateLaunchPlan` already owned the true create launch
    boundary
  - `BatchGenerationCreateTaskContext` only carried
    `task_id/task/total_chapters/response_payload` straight through to that
    same launch owner
  - `BatchGenerationCreateRuntimeDispatchSeed` only carried
    `user_id/chapter_ids/target_word_count` straight through to the same
    launch owner before immediate runtime-input assembly
  - keeping both wrappers left extra create-only owners inside the launch
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that batch create launch boundary:
  - removed `BatchGenerationCreateTaskContext`
  - removed `BatchGenerationCreateRuntimeDispatchSeed`
  - flattened task metadata, response payload, runtime chapter ids, user id,
    and target word count directly onto `BatchGenerationCreateLaunchPlan`
  - updated focused contract tests to assert the flattened launch owner and
    explicit `BatchGenerationExecutionInput` assembly directly

  This keeps create response payload shape, task active-model construction,
  snapshot persistence, runtime input content, downstream batch runtime
  dispatch behavior, and error mapping unchanged while making the batch create
  seam more cutover-ready. Smoke comparison and rollback review are easier
  because the create launch owner now directly owns the only remaining task
  and runtime-input carrier state in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:24 +08:00 checkpoint:
  this slice stayed on the shared generation runtime seam, removed the
  intermediate generated-chapter write owner, and then revalidated that
  shared runtime write behavior and chapter-batch behavior stayed unchanged.

  A focused audit showed that `chapter_generation_runtime_service.rs` still
  kept one low-signal internal owner split:
  - `ChapterGenerationRuntimeContext` already owned the true runtime
    generation boundary, including prompt assembly and the loaded chapter /
    project / previous-chapter context
  - `GeneratedChapterWriteOutcome` no longer exposed any independent seam to
    other modules; it only repackaged the generated text plus chapter write
    fields before immediate persistence
  - keeping that wrapper left one extra write-only owner inside the shared
    runtime boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that shared runtime boundary:
  - removed `GeneratedChapterWriteOutcome`
  - moved generated-result assembly into
    `ChapterGenerationRuntimeContext::build_generated_result(...)`
  - moved chapter write persistence into
    `ChapterGenerationRuntimeContext::persist_generated_result(...)`
  - updated focused tests to assert the runtime-context-owned generated result
    contract directly

  This keeps generated chapter payload shape, draft-status persistence,
  prompt-context behavior, downstream single/batch runtime behavior, and
  error mapping unchanged while making the shared generation runtime seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  runtime context now owns the complete generate -> result -> persist edge in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 02:36 +08:00 checkpoint:
  this slice stayed on the batch read-context seam, removed the intermediate
  read-payload plan owner, and then revalidated that read-side payload
  behavior, status-stream behavior, and chapter-batch behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_batch_generation_read_context_service.rs` still kept one
  low-signal internal owner split:
  - `BatchGenerationReadContext` already owned the true read-side boundary,
    including task model, runtime state, and quality context
  - `BatchGenerationReadPayloadPlan` no longer exposed any independent seam to
    other modules; it only repackaged `task/failed_chapters/payload` before
    immediate status-payload or active-payload assembly
  - keeping that wrapper left one extra payload-only owner inside the read
    boundary without improving cutover review or rollback auditability

  The refactor therefore narrowed that read-side boundary:
  - removed `BatchGenerationReadPayloadPlan`
  - replaced `into_payload_plan()` with `into_payload_parts()`
  - updated status-payload and active-payload assembly to consume the
    read-context-owned tuple directly
  - adjusted focused tests to assert the read-context-owned payload parts
    contract directly

  This keeps read-side task payload shape, status payload terminal fields,
  active payload omissions, stream-state conversion, quality context insertion,
  and error mapping unchanged while making the read seam more cutover-ready.
  Smoke comparison and rollback review are easier because the read context now
  owns the complete task/runtime-state -> payload parts edge in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation -- --nocapture`
- 2026-05-29 11:18 +08:00 checkpoint:
  this slice stayed on the single-generation stream seam, removed the
  generated-result response projection helper, and then revalidated that
  single-generation stream behavior and shared runtime behavior stayed
  unchanged.

  A focused audit showed that `chapter_generation_runtime_service.rs` still
  kept one low-signal transport projection inside the single-generation lane:
  - `GeneratedChapterResult` is still the real shared runtime owner boundary
    across generation, persistence, and downstream consumers
  - `GeneratedChapterResult::response_payload()` did not expose an
    independent seam; it only rewrapped those same transport fields into JSON
    before immediate SSE emission inside
    `chapter_single_generation_workflow_service.rs`
  - keeping that helper left one extra payload-only projection split across
    the shared runtime and stream owner without improving cutover review or
    rollback auditability

  The refactor therefore narrowed that single-generation stream boundary:
  - removed `GeneratedChapterResult::response_payload()`
  - moved success-path SSE payload assembly directly into
    `launch_owned_single_chapter_generation_stream(...)`
  - updated focused tests to assert generated-result transport fields and the
    stream-owner payload assembly directly

  This keeps generated chapter SSE payload shape, runtime persistence
  behavior, task lifecycle, terminal stream sequencing, and error mapping
  unchanged while making the single-generation seam more cutover-ready. Smoke
  comparison and rollback review are easier because the stream owner now owns
  the only remaining generated-result -> SSE payload projection in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 11:25 +08:00 checkpoint:
  this slice stayed on the single-generation runtime-state seam, removed the
  success/failure persistence forwarding wrappers, and then revalidated that
  runtime-state behavior and single-generation workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept two
  low-signal runtime-state wrappers:
  - `SingleGenerationTaskStage::persist_with_checkpoint(...)` already owned
    the true runtime-state persistence boundary for task mutation plus
    snapshot checkpoint writes
  - `persist_single_generation_runtime_success(...)` only forwarded completed
    runtime fields into that same owner for `Finalizing` and `Completed`
    checkpoints
  - `persist_single_generation_runtime_failure(...)` only forwarded failed
    runtime fields into that same owner for the `Failed` checkpoint
  - keeping both wrappers left extra result-only forwarding owners inside the
    runtime-state lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that runtime-state boundary:
  - removed `persist_single_generation_runtime_success(...)`
  - removed `persist_single_generation_runtime_failure(...)`
  - moved the completed/failed checkpoint persistence sequence directly into
    `execute_single_generation_runtime(...)`
  - updated focused tests to assert the stage-owner persistence contract
    directly instead of holding helper references

  This keeps single-generation runtime task status mutation, checkpoint stage
  progression, snapshot persistence behavior, workflow launch semantics, and
  error mapping unchanged while making the runtime-state seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  runtime-state owner now directly owns the only remaining result -> task
  persistence transition in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 11:28 +08:00 checkpoint:
  this slice stayed on the single-generation background-workflow seam,
  removed the task-id forwarding carrier, and then revalidated that
  background workflow behavior and prepared target-owner behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_workflow_service.rs` still kept one low-signal
  background-only carrier:
  - `task_id` already owned the true background task identity boundary for
    checkpoint persistence, task insert, runtime dispatch, and response
    payload assembly
  - `background_task_id = task_id.clone()` did not expose an independent seam;
    it only forwarded the same value into
    `background_response_payload(...)` and
    `background_task_active_model(...)` before immediate consumption
  - keeping that extra alias left one more create-only carrier inside the
    background workflow lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that background workflow boundary:
  - removed `background_task_id`
  - passed `&task_id` directly into
    `chapter_target.background_response_payload(...)`
  - passed `task_id.clone()` directly into
    `chapter_target.background_task_active_model(...)`

  This keeps single-generation background response payload shape, pending
  checkpoint persistence, task insert semantics, runtime dispatch behavior,
  and error mapping unchanged while making the background workflow seam
  slightly more cutover-ready. Smoke comparison and rollback review are
  easier because the workflow owner now carries one less fake-shared task-id
  alias in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_prepare_service -- --nocapture`
- 2026-05-29 11:32 +08:00 checkpoint:
  this slice stayed on the batch status-stream event seam, removed the
  terminal-close forwarding helper, and then revalidated that stream-event
  resolution and status-stream behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_event_service.rs` still kept one
  low-signal event-resolution wrapper:
  - `BatchGenerationStreamCursor::resolve_event_batch(...)` already owned the
    true stream close/continue decision boundary for cursor advancement
  - `BatchGenerationStreamState::should_close_stream()` did not expose an
    independent seam; it only forwarded `terminal_kind.is_some()` back into
    that same cursor resolution owner
  - keeping that helper left one more terminal-check split across the state
    and cursor owners without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that stream-event boundary:
  - removed `BatchGenerationStreamState::should_close_stream()`
  - moved the terminal-close predicate directly into
    `BatchGenerationStreamCursor::resolve_event_batch(...)`
  - updated focused tests to assert terminal-kind ownership directly instead
    of routing through the removed helper

  This keeps batch stream progress/result/error event payloads, cursor
  advancement behavior, terminal close semantics, and status-stream polling
  behavior unchanged while making the stream-event seam more cutover-ready.
  Smoke comparison and rollback review are easier because the cursor
  resolution owner now directly owns the only remaining close/continue branch
  in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
- 2026-05-29 11:35 +08:00 checkpoint:
  this slice stayed on the batch status-stream polling seam, removed the
  loaded-state option wrapper, and then revalidated that status-stream
  polling and stream-event behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service.rs` still kept one
  low-signal polling carrier:
  - the polling owner already owned the true read-context reload boundary for
    `load_owned_batch_generation_read_context(...)` plus immediate
    `BatchGenerationStreamState::from_task_state(...)` conversion
  - `loaded_state: Option<BatchGenerationStreamState>` did not expose an
    independent seam; it only wrapped `Ok(context)` into `Some(state)` and
    `Err(_)` into `None` before immediate `match` unwrapping in the same
    owner
  - keeping that wrapper left one more read-reload carrier inside the
    polling lane without improving cutover review or rollback auditability

  The refactor therefore narrowed that polling boundary:
  - removed `loaded_state`
  - moved the `Ok(context)` -> stream-state conversion directly into the
    polling owner branch
  - kept the `Err(_)` -> 404 system-event close path directly in the same
    branch without an intermediate option wrapper

  This keeps batch status-stream polling cadence, read-context reload
  behavior, 404 system-event payload shape, cursor/event resolution, and
  timeout semantics unchanged while making the polling seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  polling owner now directly owns the entire reload -> state-or-404 branch in
  that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
- 2026-05-29 11:39 +08:00 checkpoint:
  this slice stayed on the batch read-context payload seam, removed the
  cloned task carrier from payload-parts assembly, and then revalidated that
  read-side payload behavior and status-stream behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_read_context_service.rs` still kept one
  low-signal payload carrier:
  - `BatchGenerationReadContext::into_payload_parts(...)` already owned the
    true read-side boundary for task-view payload assembly plus quality-status
    insertion
  - the local `task = self.task.clone()` did not expose an independent seam;
    it only duplicated the same task so the owner could both borrow `self.task`
    for payload building and then return the cloned task immediately after
  - keeping that clone left one more read-side carrier inside the payload
    assembly boundary without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that read-context payload boundary:
  - destructured `BatchGenerationReadContext` directly into
    `task/workflow_runtime_state/quality_status_context`
  - built the shared payload from `&task`
  - returned the same moved `task` instead of a cloned carrier copy

  This keeps read-side task payload shape, quality-status insertion,
  terminal-status payload enrichment, stream-state conversion, and
  status-stream behavior unchanged while making the read-context seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  payload-parts owner now carries one less fake-shared task copy in that
  lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
- 2026-05-29 11:44 +08:00 checkpoint:
  this slice stayed on the batch read-context terminal-payload seam, removed
  the failed-chapters forwarding carrier, and then revalidated that read-side
  terminal payload behavior and quality-status behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_read_context_service.rs` still kept one
  low-signal terminal-payload carrier:
  - `BatchGenerationReadContext::into_status_task_payload(...)` already owned
    the true read-side status payload boundary for terminal-status enrichment
    and payload field insertion
  - the local `failed_chapters = task.failed_chapters.clone()` did not expose
    an independent seam; it only cloned the same task field so the owner
    could insert it into payload and then immediately pass the same value to
    `insert_batch_generation_terminal_status_payload(...)`
  - keeping that variable left one more terminal-payload forwarding carrier
    inside the read-context lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that terminal-payload boundary:
  - removed the local `failed_chapters` carrier
  - inserted `task.failed_chapters.clone()` directly into payload
  - passed `Some(&task.failed_chapters)` directly into the quality-status
    terminal owner

  This keeps read-side terminal payload shape, failed-chapters field content,
  manual-review label resolution, terminal-status semantics, and quality
  status behavior unchanged while making the read-context seam more
  cutover-ready. Smoke comparison and rollback review are easier because the
  status-payload owner now carries one less fake-shared failed-chapters alias
  in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_read_context_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
- 2026-05-29 11:47 +08:00 checkpoint:
  this slice stayed on the single-generation runtime-state completion seam,
  removed the finalizing-result carrier, and then revalidated that
  runtime-state behavior and single-generation workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal completion carrier:
  - `execute_single_generation_runtime(...)` already owned the true completed
    checkpoint sequencing boundary for `Finalizing -> Completed`
  - the local `finalizing_result` did not expose an independent seam; it only
    stored the result of `persist_with_checkpoint(...Finalizing...)` before an
    immediate `match` in the same owner
  - keeping that variable left one more completion-only carrier inside the
    runtime-state lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that completion boundary:
  - removed `finalizing_result`
  - matched directly on the `Finalizing` checkpoint persistence result
  - kept the `Finalizing -> Completed` sequencing and the failed path
    unchanged

  This keeps single-generation runtime checkpoint progression, task status
  mutation, workflow launch semantics, and error mapping unchanged while
  making the runtime-state seam more cutover-ready. Smoke comparison and
  rollback review are easier because the completion owner now carries one
  less fake-shared result carrier in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 11:51 +08:00 checkpoint:
  this slice stayed on the batch status-stream send seam, removed the
  duplicated event-send forwarding branches, and then revalidated that
  status-stream polling and stream-event behavior stayed unchanged.

  A focused audit showed that
  `chapter_batch_generation_status_stream_service.rs` still kept one
  low-signal send duplication seam:
  - the polling owner already owned the true event delivery boundary after
    `cursor.resolve_event_batch(...)`
  - both `Continue` and `Close` branches repeated the same
    `for event in events { tx.send(...event.to_string()) }` forwarding logic
    before diverging only on `cursor = next_cursor` versus `return`
  - keeping that duplication left one more repeated send path inside the same
    owner without improving cutover review or rollback auditability

  The refactor therefore narrowed that event-delivery boundary:
  - introduced one file-local `send_stream_events(...)` helper
  - moved the repeated `events -> SSE send` forwarding into that helper
  - kept the `Continue`/`Close` branch ownership limited to cursor advance vs
    stream close semantics

  This keeps batch status-stream polling cadence, event payload shape,
  cursor advancement behavior, and terminal close semantics unchanged while
  making the stream send seam more cutover-ready. Smoke comparison and
  rollback review are easier because the polling owner now has one explicit
  event-delivery edge instead of two duplicated forwarding branches.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
- 2026-05-29 11:55 +08:00 checkpoint:
  this slice stayed on the single-generation runtime-state execution seam,
  removed the generation-result carrier, and then revalidated that
  runtime-state behavior and single-generation workflow behavior stayed
  unchanged.

  A focused audit showed that
  `chapter_single_generation_runtime_state_service.rs` still kept one
  low-signal execution carrier:
  - `execute_single_generation_runtime(...)` already owned the true
    generate-result branching boundary for completed vs failed persistence
  - the local `generation_result` did not expose an independent seam; it only
    stored the result of `execute_owned_single_chapter_generation(...).await`
    before an immediate `match` in the same owner
  - keeping that variable left one more execution-only carrier inside the
    runtime-state lane without improving cutover review or rollback
    auditability

  The refactor therefore narrowed that execution boundary:
  - removed `generation_result`
  - matched directly on
    `execute_owned_single_chapter_generation(db, runtime_input).await`
  - kept the success and failure persistence branches unchanged

  This keeps single-generation runtime checkpoint sequencing, task status
  mutation, workflow launch semantics, and error mapping unchanged while
  making the runtime-state seam more cutover-ready. Smoke comparison and
  rollback review are easier because the execution owner now carries one less
  fake-shared runtime result carrier in that lane.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_workflow_service -- --nocapture`
- 2026-05-29 17:17 +08:00 checkpoint:
  this slice stayed on the shared chapter-context provider seam, moved more
  Python-owned chapter-context assembly into Rust-owned provider payload
  builders, and then revalidated that prompt injection and research payload
  behavior stayed unchanged.

  A focused audit showed that the shared provider payload had already become
  the right owner boundary for chapter-generation context reused by single
  generation, regeneration, and batch per-chapter runtime:
  - `chapter_generation_prompt_context_provider_service.rs` already owned the
    shared provider payload contract and placeholder/default assembly
  - `chapter_generation_research_payload_service.rs` already owned the real
    runtime data assembly for prompt/provider context
  - Python `chapter_context_service.py` still had chapter-context lanes that
    Rust had not yet truly owned, especially `chapter_careers` and
    `recent_chapters_context`

  The refactor therefore narrowed that chapter-context boundary:
  - added `chapter_careers` to the shared provider payload contract and prompt
    params
  - introduced a Rust-owned `build_single_chapter_careers_payload(...)`
    builder backed by `career` / `character_career` / `character`
  - added `recent_chapters_context` to the shared provider payload contract
    and prompt params
  - introduced a Rust-owned
    `build_single_chapter_recent_context_payload(...)` builder that loads the
    previous chapter plans/summaries and formats the recent-context block
  - injected both fields through
    `build_single_chapter_research_provider_payload(...)` so single
    generation, regeneration, and batch per-chapter runtime all reuse the
    same Rust-owned provider payload

  This keeps prompt placeholder shape, batch runtime payload forwarding, and
  prompt assembly behavior unchanged while making the shared provider payload
  the real owner of more Python chapter-context runtime responsibilities.
  Smoke comparison and rollback review are easier because these context
  fields no longer depend on scattered route/runtime fallback assembly.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_context_provider_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:32 +08:00 checkpoint:
  this slice stayed on the shared chapter-context provider seam, moved the
  `previous_chapter_summary` owner from prompt-local chapter fallback logic
  into the Rust shared provider builder, and then revalidated that prompt
  assembly and research payload behavior stayed unchanged.

  A focused audit showed that `previous_chapter_summary` was still one of the
  remaining fake-Rust seams:
  - Python `chapter_context_service.py` already treated this field as a real
    runtime-owned context lane with priority `story_memories.chapter_summary`
    first and `chapter.summary` second
  - Rust `chapter_generation_prompt_service.rs` still filled
    `previous_chapter_summary` locally from `previous_chapter.summary`
  - keeping that fallback inside prompt assembly meant the field was not yet
    truly owned by the shared provider payload boundary that already carried
    `characters_info`, `chapter_careers`, `recent_chapters_context`,
    `foreshadow_reminders`, and `relevant_memories`

  The refactor therefore narrowed that context-assembly boundary:
  - added `previous_chapter_summary` to
    `PromptContextProviderPayload` and its placeholder/default prompt params
  - introduced Rust-owned
    `build_single_chapter_previous_summary_payload(...)` in
    `chapter_generation_research_payload_service.rs`
  - made the new builder load the previous chapter by chapter number, then
    prefer `story_memories.memory_type == "chapter_summary"` and fall back to
    `chapter.summary`, matching the Python lane semantics more closely
  - injected the field through
    `build_single_chapter_research_provider_payload(...)`
  - removed the old prompt-local
    `previous_chapter.and_then(|item| item.summary.clone())` summary fallback
    from `chapter_generation_prompt_service.rs`
  - updated prompt/runtime test fixtures so all shared payload construction
    points carry the new field explicitly

  This keeps template placeholder names, previous-chapter content injection,
  and prompt/runtime flow shape unchanged while making
  `previous_chapter_summary` a real Rust-owned shared provider seam instead of
  a prompt-local fallback. Smoke comparison and rollback review are easier
  because the summary field now follows the same owner boundary as the rest of
  the migrated chapter-context runtime payload.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_context_provider_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:40 +08:00 checkpoint:
  this slice stayed on the shared chapter-context provider seam, moved the
  Python-owned `character_arc_snapshot` runtime assembly into the Rust
  research payload owner, and then revalidated that generation-memory query
  composition and relevant-memory prompt assembly stayed unchanged.

  A focused audit showed that `character_arc_snapshot` was still one of the
  remaining real Python chapter-context seams:
  - Python `chapter_context_service.py` already treated it as a true runtime
    context lane built from character current state plus recent
    `character_event` / `plot_point` memories
  - that same Python lane also influenced `build_memory_query_text(...)`
    during memory retrieval and was merged back into `relevant_memories`
    before prompt compaction
  - Rust had already moved `characters_info`, `chapter_careers`,
    `recent_chapters_context`, `previous_chapter_summary`,
    `foreshadow_reminders`, and `relevant_memories` into the shared provider
    builder, but `character_arc_snapshot` still had no Rust runtime owner

  The refactor therefore narrowed that chapter-context boundary:
  - introduced a Rust-owned `build_character_arc_snapshot(...)` formatter plus
    `build_single_chapter_character_arc_snapshot_payload(...)` loader in
    `chapter_generation_research_payload_service.rs`
  - made the Rust owner aggregate non-organization characters with current
    state / non-active status and recent `character_event` / `plot_point`
    memories, then emit a compact `【角色弧光快照】` block
  - extended
    `compose_single_chapter_generation_memory_query(...)` so Rust generation
    memory retrieval now consumes `chapter_careers` and the newly owned
    `character_arc_snapshot` alongside `characters_info` and
    `foreshadow_reminders`
  - updated `build_single_chapter_relevant_memories_payload(...)` so the Rust
    memory lane now merges the arc snapshot back into the returned
    `relevant_memories` block, matching the Python continuity/reference
    behavior more closely
  - injected the new runtime-owned arc snapshot through the shared provider
    build path without introducing another scattered route/runtime fallback

  This keeps prompt placeholder shape unchanged while making another real
  Python chapter-context runtime responsibility Rust-owned: the same service
  now owns character-arc assembly, memory-query context enrichment, and the
  merged reference block used downstream by single generation, regeneration,
  and batch per-chapter runtime. Smoke comparison and rollback review are
  easier because this character-continuity lane now follows the same shared
  builder boundary as the rest of the migrated chapter context.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_research_payload_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:42 +08:00 checkpoint:
  this slice stayed on the previous-chapter continuation seam, moved the
  Rust continuation excerpt behavior closer to the Python ending-info owner,
  and then revalidated that prompt assembly behavior stayed unchanged.

  A focused audit showed that `continuation_point` still had one clear
  Python/Rust semantic drift:
  - Python `chapter_context_service.py` already owned the continuation anchor
    as `prev_chapter.content.strip()` and then the **last** 500 characters
  - Rust `chapter_generation_prompt_service.rs` still treated
    `continuation_point` as the **first** 300 characters of the previous
    chapter content
  - keeping that difference meant the Rust prompt lane was still pulling the
    wrong side of the previous chapter body for continuation, which weakens
    true cutover confidence even when the field names already match

  The refactor therefore narrowed that continuation boundary:
  - updated `continuation_point(...)` in
    `chapter_generation_prompt_service.rs` to trim previous content and keep
    the trailing 500 characters instead of the leading 300 characters
  - updated the focused prompt test to assert the Rust continuation excerpt
    now matches the Python-style tail-content behavior

  This keeps template placeholder names and previous-chapter prompt wiring
  unchanged while making the Rust continuation excerpt semantics materially
  closer to the Python runtime owner. Smoke comparison and rollback review are
  easier because this lane no longer hides a simple but meaningful behavioral
  drift in how the continuation anchor is extracted.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:46 +08:00 checkpoint:
  this slice stayed on the previous-chapter continuation seam, moved the
  Rust `previous_chapter_content` behavior closer to the Python ending-info
  owner, and then revalidated that prompt assembly behavior stayed unchanged.

  A focused audit showed that one more simple but important Python/Rust drift
  still remained in the previous-chapter lane:
  - Python `chapter_context_service.py` treated the previous-chapter content
    reference as the trimmed **last** 500 characters of the previous chapter
  - Rust `chapter_generation_prompt_service.rs` still injected the **entire**
    previous chapter body into `previous_chapter_content`
  - even after fixing `continuation_point`, keeping full-body injection in
    this field meant the one-to-one continuation template could still receive
    a much wider and less Python-like prompt context than intended

  The refactor therefore narrowed that continuation boundary:
  - introduced one local `previous_chapter_content(...)` owner in
    `chapter_generation_prompt_service.rs`
  - made the Rust field follow the same trim + trailing-500-chars behavior as
    the Python previous-chapter ending-info lane
  - strengthened the focused prompt test so it now proves the prompt contains
    the tail 500 characters and does not rely on the whole previous chapter
    body being injected

  This keeps template placeholder names and prompt wiring unchanged while
  making the Rust one-to-one continuation context materially closer to the
  Python runtime owner. Smoke comparison and rollback review are easier
  because `previous_chapter_content` no longer hides a whole-body-vs-tail
  semantic drift.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 17:49 +08:00 checkpoint:
  this slice stayed on the previous-chapter continuation seam, narrowed the
  prompt-local previous-chapter assembly boundary, and then revalidated that
  prompt behavior stayed unchanged.

  A focused audit showed that even after aligning Python semantics for
  `continuation_point` and `previous_chapter_content`, the previous-chapter
  lane still had one structural weakness:
  - `chapter_generation_prompt_service.rs` still owned two separate local
    helper calls for previous-chapter prompt assembly
  - that meant the previous-chapter continuation context was still spread
    across multiple local prompt-value insertions instead of one explicit
    owner boundary
  - keeping the lane split would make the next step of moving more previous-
    chapter logic into a shared Rust context owner harder to review and easier
    to drift again

  The refactor therefore narrowed that prompt boundary:
  - introduced one local `PreviousChapterPromptContext` owner struct in
    `chapter_generation_prompt_service.rs`
  - introduced one
    `build_previous_chapter_prompt_context(previous_chapter)` assembly helper
    that owns both `continuation_point` and `previous_chapter_content`
  - changed prompt param assembly so the downstream prompt builder consumes
    the aggregated owner result instead of calling separate local helpers

  This keeps placeholder names and rendered prompt content unchanged while
  making the previous-chapter lane more cutover-ready. Smoke comparison and
  rollback review are easier because the prompt boundary now has one explicit
  previous-chapter owner instead of multiple local value derivations.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 18:14 +08:00 checkpoint:
  this slice continued the same previous-chapter seam, but moved the
  prepared previous-chapter prompt context one step upward from prompt-local
  ownership into the Rust runtime owner boundary.

  A focused audit showed that one more structural Python/Rust cutover gap
  still remained after introducing `PreviousChapterPromptContext`:
  - `chapter_generation_prompt_service.rs` already had one explicit
    previous-chapter context object, but prompt assembly still accepted
    `previous_chapter: Option<&chapter::Model>` at its public boundary
  - that meant the prompt service still needed to understand chapter-model
    shape and previous-chapter existence instead of only consuming prepared
    prompt context
  - keeping that coupling would make later runtime cutover review weaker,
    because the seam would still be split between runtime context loading and
    prompt-local chapter inspection

  The refactor therefore tightened the runtime owner boundary:
  - changed `build_prompt_with_provider_payload(...)` and
    `build_prompt_params_with_provider_payload(...)` in
    `backend-rs/src/services/chapter_generation_prompt_service.rs` so they no
    longer accept `Option<&chapter::Model>` and instead consume:
    `PreviousChapterPromptContext` plus an explicit
    `has_previous_chapter: bool`
  - made `PreviousChapterPromptContext` and
    `build_previous_chapter_prompt_context(...)` `pub(crate)` so the runtime
    owner can prepare the context once and pass it downward
  - updated `backend-rs/src/services/chapter_generation_runtime_service.rs`
    so `ChapterGenerationRuntimeContext` now stores
    `previous_chapter_prompt_context` as part of its owned runtime state and
    prepares it during `load_generation_context(...)`
  - updated focused tests in both services so they now construct prompt input
    through the prepared-context builder instead of the old
    `previous_chapter` prompt-local path

  This is a real Python-to-Rust migration step because the prompt lane is now
  closer to a pure prepared-context consumer, which matches the direction of
  Rust owning the real runtime chapter-context assembly instead of letting
  prompt code keep re-inspecting raw chapter models. Smoke comparison and
  rollback review are easier because the previous-chapter prompt seam now has
  a clearer owner handoff: runtime prepares, prompt consumes.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_prompt_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 18:36 +08:00 checkpoint:
  this slice moved one more real chapter-context runtime responsibility from
  Python `chapter_context_service.py` into the Rust generation lane:
  context compaction before prompt rendering.

  A focused audit showed that even after Rust had already taken ownership of
  several chapter-context fields, one important runtime behavior was still
  effectively Python-owned:
  - Python `chapter_context_service.py` did not only assemble fields such as
    `recent_chapters_context`, `chapter_careers`,
    `foreshadow_reminders`, `relevant_memories`,
    `previous_chapter_summary`, and previous-chapter tail context
  - it also applied target-word-count and mode-sensitive context compaction
    before prompt injection, which directly controls prompt density and what
    survives when context grows too large
  - Rust prompt/runtime already consumed the migrated fields, but without a
    Rust-owned compaction boundary the final runtime behavior still was not a
    true Python-to-Rust cutover for this lane

  The refactor therefore introduced a new Rust runtime owner:
  - added
    `backend-rs/src/services/chapter_generation_context_compaction_service.rs`
    as a dedicated Rust context-compaction seam
  - implemented Rust-owned compaction rules for the highest-signal fields that
    Python previously compacted before generation:
    `recent_chapters_context`, `chapter_careers`,
    `foreshadow_reminders`, `relevant_memories`,
    `previous_chapter_summary`, `continuation_point`,
    `previous_chapter_content`, and `characters_info`
  - kept the compaction profile mode-sensitive in the same direction as
    Python: `one-to-one` keeps recent-chapter context disabled while
    `one-to-many` allows bounded recent-context carryover
  - wired that compaction seam into
    `backend-rs/src/services/chapter_generation_runtime_service.rs` so prompt
    rendering now consumes Rust-compacted provider payload + previous-chapter
    prompt context at the final runtime boundary

  This is a real Python-to-Rust migration step because Rust now owns not just
  chapter-context field construction, but also one of the key runtime quality
  controls that Python used before prompt formatting. Smoke comparison and
  rollback review are easier because the generation lane now has a clear
  Rust-owned “prepare -> compact -> render prompt” boundary instead of
  leaving prompt-density behavior split across languages.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_context_compaction_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_generation_runtime_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 18:52 +08:00 checkpoint:
  this slice stayed on the same “real runtime ownership” track and pushed the
  Rust chapter-context seam deeper into regeneration execution, instead of
  leaving regeneration on a research-note-only compatibility path.

  A focused audit showed that one high-value cutover gap still remained even
  after Rust-owned chapter-context field assembly and Rust-owned compaction
  were both in place:
  - `chapter_regeneration_prepare_service.rs` already rebuilt a real Rust
    `provider_payload` through
    `build_single_chapter_research_provider_payload(...)`
  - but full and partial regeneration prompts still consumed only
    `web_research_note` plus external/reference assets, while ignoring the
    Rust-owned `previous_chapter_summary`, `recent_chapters_context`,
    `characters_info`, `chapter_careers`, `foreshadow_reminders`, and
    `relevant_memories`
  - keeping regeneration on that reduced prompt surface meant Rust had not yet
    completed a true runtime cutover for this lane; chapter-context assembly
    existed, but the regeneration prompts still did not actually use it

  The refactor therefore completed the next seam:
  - upgraded `build_regeneration_prompt(...)` so full regeneration now
    consumes the Rust-owned chapter-context payload directly, including recent
    planning context, previous-chapter summary, character sheet, career sheet,
    foreshadow reminders, and relevant memories
  - upgraded `build_partial_regeneration_prompt(...)` and
    `prepare_partial_regeneration_input(...)` so partial regeneration also
    consumes the Rust-owned chapter-context payload instead of only style note
    + web research note
  - applied `compact_generation_context(...)` before building both full and
    partial regeneration prompts, so the same Rust-owned compaction boundary
    now controls regeneration prompt density as well as single-chapter
    generation
  - added focused tests proving that regeneration prompts now surface the
    Rust-owned context payload instead of dropping it on the floor

  This is a real Python-to-Rust migration step because Rust regeneration now
  does not merely reuse the research fetch/archive lane; it also actually
  consumes the Rust-owned chapter-context runtime payload when constructing the
  rewrite prompts. Smoke comparison and rollback review are easier because
  regeneration has moved from “Rust gathers context but doesn’t really use it”
  to “Rust gathers, compacts, and injects that context into the prompt”.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_regeneration_prepare_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 19:05 +08:00 checkpoint:
  this slice stayed on the real runtime parity track and closed one more
  generation-path semantic drift: batch generation was still dropping the
  compat-driven prompt overrides even after Rust had already migrated the
  provider payload and prompt-context seam.

  A focused audit showed one concrete cutover gap in the batch lane:
  - batch runtime already rebuilt a real chapter-scoped Rust provider payload
    and already called the shared generation runtime
  - but `chapter_batch_generation_runtime_state_service.rs` still passed
    `ChapterGenerationPromptOverrides::default()` into
    `generate_and_persist_chapter_content_with_provider_payload(...)`
  - that meant batch generation silently discarded the compat/runtime prompt
    semantics that single generation already preserved, including
    `narrative_perspective`, `creative_mode`, `story_focus`, `plot_stage`,
    `story_creation_brief`, `quality_*`, `web_research_*`, and
    `story_repair_*`

  The refactor therefore removed that semantic split:
  - promoted
    `build_prompt_overrides_from_compat_options(...)` in
    `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
    to a shared `pub(crate)` owner instead of keeping it single-runtime-local
  - updated
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so batch generation now reuses the exact same compat-to-prompt-overrides
    mapping before calling the shared Rust generation runtime
  - added a focused batch test that proves the reused owner preserves
    narrative mode, creative controls, quality controls, web-research fields,
    and repair/preserve fields

  This is a real Python-to-Rust migration step because batch generation now no
  longer stops at “Rust can generate a chapter with a provider payload”; it
  also preserves the same runtime prompt semantics that the single-generation
  Rust lane already owned. Smoke comparison and rollback review are easier
  because single and batch generation now share one prompt-override owner
  instead of silently diverging.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 19:21 +08:00 checkpoint:
  this slice moved another real Python runtime owner into Rust by closing the
  `manual_chapter_analysis_execution_service.py` memory-write gap. Before this
  change, Rust chapter analysis already owned AI invocation, `plot_analysis`
  persistence, foreshadow sync, character state sync, organization-state sync,
  and analysis task state transitions, but it still did not replace the
  chapter-scoped `story_memories` rows or rebuild the vector memory index that
  Python manual analysis execution continued to own.

  The refactor therefore completed the next high-value seam:
  - added Rust-side analysis memory extraction in
    `backend-rs/src/services/chapter_analysis_runtime_service.rs`, mirroring the
    Python `plot_analyzer.extract_memories_from_analysis(...)` semantics for
    `chapter_summary`, strong `hook`, `foreshadow`, important `plot_point`,
    `character_event`, and high-level conflict-derived memories
  - added Rust-side chapter memory replacement after `plot_analysis` persist:
    delete existing chapter `story_memories`, delete the chapter-scoped vector
    index entries, insert the new relational memories, and upsert their vector
    records
  - kept the migrated runtime behavior aligned with Python by fixing text
    position tracking to use character offsets instead of UTF-8 byte offsets,
    which matters for downstream annotation / memory-position consumers
  - added focused tests for text-position parity and analysis memory extraction
    coverage so this owner no longer lives only in Python runtime behavior

  This is a real Python-to-Rust migration step because chapter analysis in Rust
  now owns not only the structured analysis result and downstream state-sync
  side effects, but also the chapter memory replacement workflow that feeds the
  relational memory table and vector retrieval path. That removes one of the
  remaining substantive reasons for Python `manual_chapter_analysis_execution`
  to stay in the critical runtime path.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_character_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 19:41 +08:00 checkpoint:
  this slice closed another real Python runtime lane by migrating the
  `backend/app/api/memories.py` chapter-analysis execution path toward the
  Rust-owned analysis runtime instead of leaving it as a Python-only
  synchronous analysis endpoint.

  A focused audit showed a concrete owner split still remained:
  - Python `POST /api/memories/projects/{project_id}/analyze-chapter/{chapter_id}`
    executed chapter analysis synchronously and returned the final
    `analysis + memories_count + foreshadow_stats` payload
  - Rust `POST /memories/projects/{project_id}/analyze-chapter/{chapter_id}`
    only created a background analysis task, which meant the same product
    capability still had two different runtime owners and two different
    response semantics
  - after the previous slice moved analysis persistence, memory replacement,
    and foreshadow sync into Rust, keeping this memories lane on Python no
    longer reflected a missing Rust capability; it was just an unclosed owner
    boundary

  The refactor therefore completed the next seam:
  - promoted the Rust chapter-analysis runtime so
    `persist_chapter_analysis_result(...)` now returns structured post-persist
    results (`analysis`, `memories_count`, `foreshadow_stats`) instead of
    behaving only as an async task helper
  - added a synchronous Rust `analyze_chapter_now(...)` entrypoint in
    `backend-rs/src/services/chapter_analysis_runtime_service.rs` that reuses
    the same Rust-owned prompt, AI invocation, plot-analysis persist, memory
    replacement, and foreshadow-sync path
  - updated
    `backend-rs/src/services/memories_write_workflow_service.rs` so the Rust
    memories analyze route now executes the real Rust analysis runtime and
    returns the final synchronous result payload instead of only returning a
    created-task envelope
  - preserved the separate `/chapters/{chapter_id}/analyze` async task flow,
    so this slice narrows the memories analyze owner without collapsing the
    existing explicit background-task API shape

  This is a real Python-to-Rust migration step because the memories analyze
  lane no longer depends on Python to produce the final chapter analysis
  result. Rust now owns both:
  - the async analysis-task route family
  - the synchronous memories analyze execution lane

  That reduces one more concrete reason for the old Python memories analysis
  endpoint to remain in the critical runtime path, and it moves the system
  closer to one analysis runtime owner instead of parallel Python/Rust
  execution semantics.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" memories_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" memories_error_mapper -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 19:45 +08:00 checkpoint:
  this slice did not yet cut a Python generation-path caller over to Rust, but
  it moved a required owner capability into the Rust analysis runtime so the
  next real cutover can happen without keeping the Python
  `manual_chapter_analysis_execution_service.py` override path as a blocking
  dependency.

  A focused audit showed the remaining high-value Python runtime owner is now
  concentrated in generation-adjacent follow-up analysis flows:
  - Python stream generation, compat generation entrypoints, and batch
    generation analysis helpers still call
    `manual_chapter_analysis_execution_service.execute_chapter_analysis_background(...)`
  - those callers do not only need the base chapter analysis path; they also
    rely on generation-follow-up overrides such as
    `chapter_content_override` / `chapter_word_count_override`
  - before this slice, Rust analysis runtime could analyze persisted chapter
    content and the memories lane, but it did not yet expose an equivalent
    override-aware execution surface for the generation follow-up path

  The refactor therefore completed the next cutover-readiness seam:
  - added `ChapterAnalysisRuntimeOverrides` in
    `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  - added `build_analysis_runtime_chapter_model(...)` so Rust can analyze an
    effective chapter model built from the persisted chapter plus temporary
    content / word-count overrides
  - upgraded the synchronous Rust analysis entrypoint to
    `analyze_chapter_now_with_overrides(...)`, while preserving the existing
    simpler `analyze_chapter_now(...)` wrapper for callers that do not need
    override behavior
  - added focused tests proving the effective chapter model correctly prefers
    override content and override word count while preserving the persisted
    chapter identity fields

  This is a real Python-to-Rust migration step because it moves one more
  generation-follow-up analysis capability into the Rust owner instead of
  leaving it exclusive to the Python execution service. The next seam can now
  target actual generation/batch follow-up callers and wire them to Rust
  without first re-implementing override semantics in a hurry.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 19:54 +08:00 checkpoint:
  this slice completed the next real owner cutover in the generation runtime
  path instead of only preparing helper capability.

  A focused audit confirmed the missing seam:
  - Rust single-chapter generation runtime and Rust batch-generation runtime
    already owned chapter content generation and persistence
  - but `compat_options.enable_analysis` was still only carried through runtime
    state and task payloads; it did not trigger real Rust follow-up analysis
  - that meant Python had already lost some analysis ownership, but the most
    valuable "generate -> analyze generated content" runtime lane still lacked
    a Rust-native owner path

  The refactor therefore completed the next real Rust migration seam:
  - added `build_generated_chapter_analysis_overrides(...)` and
    `analyze_generated_chapter_follow_up(...)` in
    `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  - wired
    `backend-rs/src/services/chapter_single_generation_runtime_state_service.rs`
    so successful single-chapter generation now directly invokes the Rust
    follow-up analysis owner when `enable_analysis == true`
  - wired
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so each successful batch-generated chapter now directly invokes the same
    Rust follow-up analysis owner when `enable_analysis == true`
  - reused the existing override-aware Rust analysis runtime, so generated
    chapter content / word count are analyzed immediately without falling back
    to Python `manual_chapter_analysis_execution_service.py` semantics for this
    lane
  - kept follow-up analysis failures non-fatal to generation task completion,
    matching the existing "analysis is post-generation side effect" boundary
    instead of turning analysis errors into generation hard-failures

  This is a real Python-to-Rust migration step because the Rust-owned single
  and batch generation runtimes now own:
  - generate chapter content
  - persist chapter content
  - analyze the generated result with Rust runtime overrides

  That removes another concrete class of runtime behavior from the old Python
  generation-follow-up owner surface and narrows the remaining migration work
  to stream finalize / compat route shells and residual Python-triggered
  background analysis entrypoints.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 20:11 +08:00 checkpoint:
  this slice continued the same owner cutover pattern into the Rust
  single-chapter stream lane, which was still one of the remaining real
  runtime gaps after background and batch generation had already moved to Rust
  follow-up analysis.

  A focused audit confirmed the missing seam:
  - Rust `/chapters/{chapter_id}/generate-stream` already owned request
    preparation, streaming progress transport, chapter generation, and final
    content SSE result emission
  - but unlike Rust background generation and Rust batch generation, the stream
    workflow still ignored `compat_options.enable_analysis`
  - that left one more "generate -> analyze generated content" path without a
    Rust-native follow-up owner, even though the same analysis override surface
    was already available in the shared Rust analysis runtime

  The refactor therefore completed the next real Rust migration seam:
  - wired
    `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`
    so successful stream generation now directly invokes
    `analyze_generated_chapter_follow_up(...)` when `enable_analysis == true`
  - reused the same generated-result override path already used by the Rust
    background and batch generation lanes, instead of introducing a
    stream-only analysis implementation
  - preserved the current stream transport contract: analysis remains a
    non-fatal post-generation side effect and does not change the existing
    SSE success payload shape

  This is a real Python-to-Rust migration step because the Rust single-chapter
  stream lane now also owns:
  - generate chapter content
  - emit stream success result
  - trigger Rust follow-up chapter analysis on the generated content

  That removes another concrete reason for Python stream finalize /
  compatibility layers to remain the only owner of stream-follow-up analysis
  semantics, and it narrows the remaining migration work further toward
  compatibility shells and residual Python-triggered analysis wrappers.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 20:33 +08:00 checkpoint:
  this slice converted the newly completed Rust single-chapter stream owner
  into explicit phase5 cutover assets, so the migration state no longer relies
  on oral knowledge or source-only evidence.

  A focused gateway-asset audit confirmed the missing governance seam:
  - `chapters/{chapter_id}/generate-background` already had Rust owner and
    Python fallback probes in the strangler manifest
  - but `chapters/{chapter_id}/generate-stream` was still missing from the
    gateway probe manifest, route-group cutover checklist, and parity matrix
  - that meant the real Rust follow-up-analysis migration completed in the
    stream lane still had no corresponding cutover evidence, which weakened
    future fallback-shrink and rollback reviews for the `chapters` group

  The follow-up therefore closed the governance seam by:
  - adding `chapters-generate-stream-auth-guard-rust` and
    `chapters-generate-stream-auth-guard-python-fallback` to
    `deploy/strangler-gateway-probes.json`
  - updating
    `docs/architecture/rust-route-group-ownership-and-cutover-checklist-2026-05-19.zh-CN.md`
    so the `chapters` route group now explicitly treats single-chapter
    `generate-stream` as part of the P0 owner/fallback evidence chain
  - updating
    `docs/architecture/rust-python-api-parity-matrix-2026-05-19.zh-CN.md`
    so parity and fallback sections now reflect that the stream lane has a
    real same-path gateway proof instead of only source-level Rust ownership

  This matters for the actual Python-to-Rust migration because it upgrades the
  stream lane from "Rust code exists" to "Rust owner is governable and
  rollback-ready", which is necessary before shrinking Python fallback on the
  `chapters` route group any further.
- 2026-05-29 21:02 +08:00 checkpoint:
  this slice continued from the stream follow-up-analysis cutover and moved one
  more real stream-finalize responsibility from Python semantics into the Rust
  owner path.

  A focused payload/consumer audit confirmed the next runtime seam:
  - the Rust single-chapter stream lane already owned generation, persistence,
    and follow-up analysis execution
  - but its final SSE `result` payload still exposed only
    `chapter_id/chapter_number/title/content/word_count`
  - the frontend's single-chapter generation workflow still consumes
    `analysis_task_id` and `content_source`, so the missing task creation /
    finalize payload semantics kept part of the old Python stream finalize
    value alive

  The refactor therefore completed the next real Rust migration seam:
  - exposed Rust chapter-analysis task preparation / dispatch helpers from
    `backend-rs/src/services/chapter_analysis_runtime_service.rs`
  - rewired
    `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`
    so successful stream generation now creates a real Rust `analysis_task`
    record and asynchronously dispatches the Rust background analysis runner,
    instead of only doing synchronous follow-up analysis without task identity
  - upgraded the stream terminal payload builder so the Rust `result` event now
    returns real finalize fields already backed by Rust owner state:
    `analysis_task_id`, `saved_word_count`, `chapter_status`,
    `content_applied`, and `content_source`

  This is a real Python-to-Rust migration step because the Rust stream lane now
  owns not only "generate and analyze", but also "create the follow-up analysis
  task identity that the frontend tracks", which directly reduces the remaining
  semantic value of Python `stream/finalize_service.py`.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 20:19 +08:00 checkpoint:
  this slice closed a real stream-contract gap that still diluted the value of
  the just-migrated Rust analysis task owner.

  A focused Rust/frontend contract audit confirmed the mismatch:
  - the frontend single-chapter generation workflow listens for a top-level SSE
    payload where `message.type === "analysis_started"` and extracts
    `task_id` directly from that event
  - the Rust single-chapter stream lane had already started creating a real
    Rust `analysis_task_id`, but it was incorrectly emitting the
    `analysis_started` payload by wrapping it inside `sse_result(...)`
  - that meant the Rust stream owner had the right runtime state but still did
    not expose it on the exact event contract the frontend actually consumes

  The refactor therefore completed the next real Rust seam closure:
  - added `sse_json(...)` to
    `backend-rs/src/utils/sse.rs` so Rust can emit non-`result` structured SSE
    payloads without faking them as terminal result envelopes
  - rewired
    `backend-rs/src/services/chapter_single_generation_stream_workflow_service.rs`
    so `analysis_started` now ships as a first-class SSE event payload instead
    of a nested `result` event
  - kept the already migrated Rust `analysis_task_id` creation / dispatch
    semantics intact, while making the event shape match the frontend's real
    parser contract

  This matters for the actual Python-to-Rust migration because the Rust stream
  lane now owns not only the analysis task identity, but also the exact
  analysis-start signaling contract that the frontend tracks in real time.
  That removes one more remaining reason for Python stream semantics to stay in
  the loop for single-chapter interactive generation.

  The same audit also reconfirmed the next higher-value Python owner still
  remaining:
  - `backend/app/services/batch_generation_analysis_service.py` still owns a
    richer batch-only analysis helper contract, including Python-side
    `analysis_started` task-stream publication and Python-side analysis task
    creation / retry loop semantics
  - Rust batch runtime currently owns generated-content follow-up analysis via
    `analyze_generated_chapter_follow_up(...)`, but does not yet mirror that
    explicit batch helper contract as a named runtime seam

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_stream_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" utils::sse -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 21:34 +08:00 checkpoint:
  this slice moved the next real batch-generation analysis seam from Python
  helper ownership into the Rust batch runtime and stream owner path.

  A focused batch-runtime / batch-stream audit confirmed the remaining Python
  owner value:
  - Rust batch generation already owned chapter generation, persistence,
    task/snapshot progression, and same-process follow-up analysis execution
    through `analyze_generated_chapter_follow_up(...)`
  - but Python `backend/app/services/batch_generation_analysis_service.py`
    still owned the explicit batch-facing analysis helper semantics:
    publish `analysis_started`, create an analysis task identity, and expose
    that task identity back to stream consumers
  - Rust batch stream was still polling snapshot state without any analysis
    task identity in runtime state, so this batch-only semantic slice remained
    a concrete reason for the Python helper to stay valuable

  The refactor therefore completed the next real Rust migration seam:
  - rewired
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so successful batch chapter generation now first attempts to create a real
    Rust chapter analysis task through
    `prepare_chapter_analysis_trigger(...)`, persists batch runtime metadata
    (`analysis_task_id`, `analysis_task_message`, chapter identity, progress)
    into the Rust batch snapshot, and asynchronously dispatches the Rust
    background analysis runner
  - preserved the existing Rust fallback path:
    if explicit analysis task preparation fails, batch runtime still falls back
    to the already-migrated Rust `analyze_generated_chapter_follow_up(...)`
  - extended
    `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
    and
    `backend-rs/src/services/chapter_batch_generation_status_stream_event_service.rs`
    so the Rust batch stream now emits a first-class `analysis_started` event
    and includes `analysis_task_id` in the terminal `result` payload

  This is a real Python-to-Rust migration step because the Rust batch owner now
  controls not only "analyze after generation", but also "create and expose
  the analysis task identity and batch-facing analysis start event". That
  directly reduces the remaining runtime value of
  `batch_generation_analysis_service.py`.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 21:52 +08:00 checkpoint:
  this slice hardened the newly migrated Rust batch-analysis owner so its
  runtime metadata lifecycle no longer leaks stale chapter analysis state
  across batch-step boundaries.

  A focused post-migration audit found a real follow-up seam:
  - the previous Rust cutover already persisted `analysis_task_id` and exposed
    `analysis_started` from the batch stream
  - but batch runtime checkpoints for `queued`, `resumed`, `preparing`,
    `chapter_start`, `cancelled`, and `failed` were still preserving whatever
    analysis metadata had been merged into the snapshot earlier
  - that meant once one chapter emitted `analysis_started`, later batch-step
    checkpoints could continue carrying the old analysis identity until a new
    one overwrote it, which risks stale cross-chapter stream semantics

  The refactor therefore completed the next Rust owner hardening seam:
  - updated
    `backend-rs/src/services/chapter_batch_generation_runtime_checkpoint_service.rs`
    so non-terminal batch runtime checkpoints explicitly clear
    `analysis_task_id`, `analysis_task_message`,
    `analysis_started_chapter_id`, `analysis_started_chapter_number`,
    `analysis_started_at`, and `analysis_task_progress`
  - kept `chapter_succeeded` runtime state merge behavior intact, so the batch
    stream still exposes the current chapter's real analysis task identity
    after Rust creates it
  - added targeted tests proving queued / resumed / started / cancelled /
    failed checkpoints do not retain stale analysis task metadata

  This matters for the actual Python-to-Rust migration because a real owner is
  not only about feature presence; it must also own cleanup and lifecycle
  correctness. Without this hardening, Python compat paths would remain the
  safer source of truth for batch analysis event semantics.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_checkpoint_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_status_stream_event_service -- --nocapture`
- 2026-05-29 22:18 +08:00 checkpoint:
  this slice moved one of the last high-value Python batch-analysis contracts
  into the Rust batch runtime: analysis retry / failure propagation.

  A focused Python-vs-Rust audit confirmed the remaining owner gap:
  - Python `backend/app/services/batch_generation_analysis_service.py` still
    returned a `(bool, error)` contract to the batch execution pipeline
  - Python `backend/app/services/batch_generation_retry_service.py` used that
    contract to terminate the whole batch when chapter analysis failed after
    three retries
  - Rust batch runtime already owned analysis task identity and
    `analysis_started`, but it still treated follow-up analysis as effectively
    fire-and-forget, which meant Python remained the only owner of
    "analysis failure can fail the batch"

  The refactor therefore completed the next real Rust migration seam:
  - extracted
    `execute_prepared_chapter_analysis_trigger(...)` from
    `backend-rs/src/services/chapter_analysis_runtime_service.rs` so Rust now
    has a reusable synchronous execution surface for a prepared analysis task,
    instead of only background spawn semantics
  - rewired
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so batch follow-up analysis now:
      1. creates a Rust analysis task
      2. executes it synchronously through the Rust analysis runtime
      3. retries up to 3 times with exponential backoff
      4. persists retry/error metadata into batch snapshot state
      5. fails the batch task when analysis still fails after the final retry
  - preserved the Rust fallback path for direct
    `analyze_generated_chapter_follow_up(...)` execution when explicit analysis
    task preparation is unavailable

  This is a real Python-to-Rust migration step because the Rust batch runtime
  now owns not only the analysis task identity and stream event, but also the
  actual success/failure contract that determines whether analysis failure
  should stop the batch. That directly shrinks the remaining runtime value of
  Python batch analysis helper + retry wiring.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_analysis_runtime_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 23:08 +08:00 checkpoint:
  this slice started moving one more real request/runtime seed from implicit
  Python ownership into explicit Rust snapshot ownership: manual story-repair
  request guidance.

  A focused Python-vs-Rust runtime-state audit confirmed the next safe seam:
  - Rust batch/single generation already persisted
    `batch_request_runtime_state.compat_options`, and those compat options
    already carried manual `story_repair_summary`, `story_repair_targets`, and
    `story_preserve_strengths`
  - but that manual repair intent still only existed as nested request fields;
    Rust snapshot/read-side state exposed `active_story_repair_payload` only
    after later runtime derivation, which left the initial pending/resume
    runtime seed semantically thinner than the Python task-runtime path
  - fully migrating Python `StoryRepairPayload` / derived quality guidance is
    still too wide for this slice, but the manual request subset is already a
    stable, Rust-consumable contract

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
    so `BatchGenerationRequestRuntimeState` now builds a normalized manual
    `active_story_repair_payload` snapshot when compat options include explicit
    story-repair request guidance
  - kept the existing request-runtime-state payload compatible while adding a
    top-level runtime-state owner field that Rust read-side consumers already
    understand
  - rewired
    `backend-rs/src/services/chapter_single_generation_write_workflow_service.rs`
    to use the same shared runtime-state constructor, so single-chapter
    background snapshot seeds now also carry the explicit manual repair intent
    through the same Rust owner path
  - cleaned the adjacent batch runtime test module by removing obsolete helper
    debris left over from the previous fake-owner boundary shape

  This is a real Python-to-Rust migration step because manual story-repair
  request semantics no longer need to wait for later Python-side runtime-state
  derivation before they become visible in task snapshot/read-side state.
  Rust now owns the initial persisted request seed for that subset in both
  batch and single-chapter background lanes.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_single_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-29 23:32 +08:00 checkpoint:
  this slice extended the same story-repair runtime-state seam into the batch
  resume path, so Rust now owns one more piece of repair-state recovery logic
  that previously depended on Python task-runtime behavior.

  A focused Python-vs-Rust audit confirmed the remaining owner gap:
  - Python `task_workflow_runtime_service.sync_task_story_repair_state(...)`
    is still the canonical owner of normalizing and persisting
    `active_story_repair_payload` into task runtime snapshots
  - Python batch resume logic also rebuilds repair state by first preferring
    the persisted `active_story_repair_payload` and otherwise re-deriving it
    from the resumable story-repair request state
  - Rust already owned the request seed from the previous slice, but resume
    reset still rewrote the pending snapshot without explicitly carrying that
    repair runtime payload back into the new snapshot

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so resume-reset can accept an explicit runtime-state seed and merge it into
    the rebuilt pending checkpoint before writing the resumed snapshot
  - rewired
    `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    so Rust resume now:
      1. prefers an existing persisted `active_story_repair_payload`
      2. falls back to rehydrating the same payload from Rust-owned manual
         request runtime seed when the snapshot does not already have one
      3. persists that payload through the same Rust resume-reset owner path
  - rewired
    `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
    so the resume entrypoint passes both the source snapshot runtime state and
    the parsed request runtime state into the Rust resume domain owner before
    dispatch

  This is a real Python-to-Rust migration step because batch resume no longer
  depends entirely on Python task-runtime helpers to restore the
  `active_story_repair_payload` portion of task runtime state. Rust now owns
  the recovery rule for that serialized repair-state subset: preserve existing
  payload first, otherwise rehydrate from Rust-owned request seed.

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_write_workflow_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 04:19 +08:00 checkpoint:
  this slice moved one more real chapter-execution runtime owner from Python
  into Rust: generic batch generation retry with backoff.

  A focused Python-vs-Rust runtime audit confirmed the next highest-signal
  seam:
  - Python `backend/app/services/batch_generation_retry_service.py` already
    owns a chapter-level `while retry_count <= task.max_retries` execution
    loop for generic generation/provider/setup exceptions
  - Rust `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    had already absorbed failure persistence and follow-up analysis retry, but
    the main chapter generation step still executed only once and failed
    terminally on the first provider/setup/runtime error
  - the full Python quality-gate retry lane is still wider than the current
    Rust runtime contract, because Rust does not yet own the complete
    quality-gate plan / repair payload / candidate regeneration pipeline
  - therefore the safe next move is not to fake full parity, but to migrate
    the generic exception retry owner first

  The refactor therefore completed the next real Rust migration seam:
  - extended
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so `execute_batch_generation_step(...)` now runs as a chapter-level retry
    loop instead of a single attempt
  - added Rust-owned retry helpers for:
    - retry boundary resolution (`should_retry_batch_generation_attempt`)
    - exponential backoff seconds (`batch_generation_retry_backoff_seconds`)
    - retry waiting snapshot payload construction
  - rewired runtime persistence so retry attempts now update:
    - task `current_retry_count`
    - retry waiting snapshot metadata
    - chapter-start persistence with the current attempt count
  - preserved the existing Rust failure source-of-truth contract:
    only when retries are exhausted does Rust append the terminal
    `failed_chapters` entry and mark the task as failed

  This is a real Python-to-Rust migration step because Rust now owns one more
  piece of true runtime control flow rather than only read-side semantics or
  terminal failure formatting. The batch runtime can now decide, for generic
  chapter execution failures, whether the task should wait-and-retry or stop
  terminally. That directly shrinks the remaining runtime value of Python's
  generic retry loop.

  Explicitly not migrated in this slice:
  - full quality-gate retry loop ownership
  - repair payload / candidate regeneration retry plan parity
  - retry event transport parity with all Python-side progress messages

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 05:02 +08:00 checkpoint:
  this slice moved another real runtime owner from Python into Rust:
  per-chapter story-repair prompt input refresh for the next batch step.

  A focused Python-vs-Rust runtime audit confirmed the owner gap:
  - Python batch runtime does not only own retry/stop decisions; after each
    chapter it also evolves the effective story-repair runtime state that the
    next chapter generation attempt consumes
  - Rust had already persisted `active_story_repair_payload`,
    `quality_metrics_summary`, and manual/recent-history repair seeds into the
    batch snapshot, but `execute_batch_generation_step(...)` still built prompt
    overrides from the session-start compat seed only
  - that meant Rust held richer runtime state in snapshot/read-side form
    without actually letting the chapter-generation runtime consume it on the
    next step, so one piece of prompt-driving runtime ownership still stayed
    effectively Python-shaped

  The refactor therefore completed the next real Rust migration seam:
  - added
    `restore_batch_generation_runtime_compat_options_from_runtime_state(...)`
    to
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so Rust can reconstruct effective story-repair compat options from its own
    persisted batch runtime state
  - added
    `resolve_runtime_compat_options_for_batch_generation_step(...)` so each
    chapter step reloads the latest batch snapshot runtime state before prompt
    override construction
  - rewired `execute_batch_generation_step(...)` so prompt overrides are now
    derived from the restored runtime compat options instead of the original
    session-start compat seed
  - kept the fallback contract safe: if no runtime snapshot repair payload is
    present, Rust falls back to the base request compat options unchanged

  This is a real Python-to-Rust migration step because the next chapter's
  generation prompt is now driven by Rust-owned runtime snapshot state rather
  than only by the original request seed. Rust is no longer just persisting
  repair guidance for read-side consumers; it now feeds that state back into
  the actual execution path that decides how the next chapter is generated.

  Explicitly not migrated in this slice:
  - full quality-gate retry plan ownership
  - runtime-time recomputation of new repair payloads from freshly produced
    chapter quality metrics
  - Python parity for candidate-attempt persistence during repair retries

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 05:34 +08:00 checkpoint:
  this slice pushed the same runtime lane one step further: Rust now refreshes
  batch story-repair runtime state on the success path instead of only
  consuming previously seeded state.

  A focused Python-vs-Rust runtime audit confirmed the remaining owner gap:
  - Python batch runtime refreshes story-repair state after successful chapter
    completion so the next chapter can consume a newly evolved repair context
  - Rust had already learned how to read the latest `active_story_repair_payload`
    from batch snapshot before each chapter step, but the snapshot itself still
    depended on task creation/resume-time seed state only
  - Rust does not yet own a full “fresh current-chapter quality metrics ->
    repair payload -> next retry plan” pipeline, because the current batch
    generation write path still lacks a complete runtime-time quality metrics
    persistence contract for brand-new chapter output
  - but Rust already has a stable `recent_history_summary` owner path, and
    Python also uses recent-history-derived repair state as one branch of its
    success-path evolution logic

  The refactor therefore completed the next real Rust migration seam:
  - exposed reusable batch runtime-state builders in
    `backend-rs/src/services/chapter_batch_generation_write_workflow_service.rs`
    so runtime code can rebuild batch story-repair payloads through the same
    compatibility-safe owner logic as create/resume flows
  - added
    `refresh_batch_generation_runtime_story_repair_state(...)` to
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so Rust runtime now:
      1. reloads the current batch runtime snapshot
      2. preserves the current active runtime repair payload as explicit state
      3. recomputes recent-history-derived batch repair summary for the next
         chapter boundary
      4. persists the refreshed runtime story-repair payload and
         `quality_metrics_summary` back into the batch snapshot
  - rewired the chapter success path so this refresh happens immediately before
    the task advances to the next chapter

  This is a real Python-to-Rust migration step because Rust no longer only
  consumes story-repair runtime state that was seeded earlier; it now also
  evolves that runtime state on the batch success path. The next chapter's
  prompt input is therefore influenced by a Rust-refreshed runtime snapshot,
  which reduces another piece of Python's remaining execution-time owner role.

  Explicitly not migrated in this slice:
  - fresh current-chapter quality metrics to runtime payload recomputation
  - full quality-gate retry plan / candidate regeneration parity
  - current-chapter-quality branch of Python's post-success story-repair state

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:28 +08:00 checkpoint:
  this slice moved one more real batch quality owner from Python into Rust:
  `quality_metrics_history` is no longer just a dormant snapshot column on the
  Rust side; Rust now starts maintaining the chapter-quality event history that
  later quality summary and repair context logic depend on.

  A focused owner audit confirmed the gap:
  - Python's `task_quality_snapshot_service.py` does not only persist
    `latest_quality_metrics`; it appends each fresh quality event into
    `quality_metrics_history`, caps the history window at 20 entries, and then
    advances batch summary state from that history
  - Rust had already learned to persist fresh `latest_quality_metrics` and
    `quality_metrics_summary` after batch chapter analysis success, and the
    snapshot layer already mirrored `quality_metrics_history` into independent
    snapshot columns when present
  - but Rust still did not own the event-history write itself, so one real
    quality runtime fact lane remained Python-shaped: fresh chapter analysis
    events were not entering Rust-owned history at all

  The refactor therefore completed the next real migration seam:
  - added batch runtime helper logic in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    to append fresh `latest_quality_metrics` into
    `quality_metrics_history` and trim the history window to 20 entries
  - rewired the batch current-chapter quality snapshot builder so it now reads
    the existing snapshot/runtime history and emits the updated history back
    through Rust-owned runtime snapshot persistence
  - extended the quality-gate terminal patch path so a terminal
    `manual_review` decision also normalizes the tail item inside
    `quality_metrics_history`, preventing the persisted history from keeping a
    stale pre-terminal gate decision while top-level latest/summary already say
    `manual_review`

  This is a real Python-to-Rust migration step because Rust now owns one more
  piece of execution-time quality fact persistence rather than only
  read-side/latest-field formatting. Fresh batch analysis output now enters the
  Rust-owned quality event history, and terminal quality-gate transitions also
  reconcile that history from the Rust side.

  Explicitly not migrated in this slice:
  - Python's incremental `summary_state` owner logic
  - rebuilding `quality_metrics_summary` from Rust-owned history instead of
    directly from the latest plot analysis
  - broader batch quality cache/runtime parity beyond snapshot persistence

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:37 +08:00 checkpoint:
  this slice pushed the same batch quality lane one step deeper: Rust no
  longer only appends fresh batch quality events into
  `quality_metrics_history`; it now also starts deriving batch
  `quality_metrics_summary` from that Rust-owned history instead of treating
  the latest plot-analysis summary as the only owner.

  A focused owner audit confirmed the next gap:
  - Python's quality snapshot flow does not stop at persisting history; after
    appending the latest event it advances summary state and rebuilds the
    effective summary from that accumulated history
  - Rust had already completed the previous slice where fresh
    `latest_quality_metrics` enters `quality_metrics_history`, but
    `build_batch_generation_runtime_state_payload_from_current_quality(...)`
    still wrote `quality_metrics_summary` directly from the current chapter's
    plot-analysis-derived one-shot summary
  - that left one more meaningful owner gap: Rust owned the event history
    persistence, but the summary still did not truly flow from that history

  The refactor therefore completed the next real migration seam:
  - reused the existing Rust quality-domain aggregation helper
    `aggregate_story_repair_quality_summaries(...)` from
    `backend-rs/src/services/chapter_story_repair_quality_context_service.rs`
    instead of creating a parallel batch-only summary implementation
  - added a history-to-summary adapter in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so fresh batch quality history is converted into an aggregated
    `quality_metrics_summary` through Rust-owned aggregation logic
  - preserved the Python-compatible history storage contract
    (`history.append(...)`, oldest -> newest), while adapting the order only at
    aggregation time because the reused Rust helper expects newest-first input
  - rewired `quality_history_context` emission so it now follows the
    history-derived summary rather than the one-shot plot-analysis payload

  This is a real Python-to-Rust migration step because Rust now owns not only
  the persistence of batch quality events, but also the next layer of derived
  runtime state that prompt repair and quality-gate decisions consume. The
  batch summary is now materially closer to a Rust-owned “history -> summary”
  pipeline rather than a direct mirror of the latest plot analysis row.

  Explicitly not migrated in this slice:
  - Python's incremental cached `summary_state` parity
  - complete batch quality cache parity beyond snapshot/runtime persistence
  - any broader route/read-side refactor outside this runtime owner seam

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:50 +08:00 checkpoint:
  this slice continued the same Python-to-Rust quality-owner migration by
  moving the intermediate batch quality aggregation state into Rust.

  A focused audit showed the remaining gap after the previous slice:
  - Python's batch quality snapshot owner does not only persist history and
    rebuild the final summary; it also owns an intermediate `_summary_state`
    that advances incrementally as each fresh quality event arrives
  - after the prior Rust slice, `backend-rs` already owned
    `quality_metrics_history` and could derive `quality_metrics_summary` from
    that history, but it still recomputed the effective summary from history on
    demand and did not yet own the incremental aggregation state itself
  - that meant one meaningful runtime owner still remained Python-shaped:
    Rust had the facts and the derived summary, but not the intermediate
    running aggregation state that the Python runtime advances per event

  The refactor therefore completed the next real migration seam:
  - added Rust-side quality summary state helpers in
    `backend-rs/src/services/chapter_story_repair_quality_context_service.rs`
    to:
      1. build `quality_metrics_summary_state` from history
      2. advance that state incrementally from an appended event plus optional
         dropped history item
      3. rebuild `quality_metrics_summary` from the state while keeping the
         existing history-derived fallback safe
  - rewired
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so batch current-quality runtime persistence now writes and advances
    `quality_metrics_summary_state` alongside
    `quality_metrics_history` / `quality_metrics_summary`
  - kept the migration small and compatibility-safe by storing the new state
    inside batch runtime snapshot payload only, instead of expanding database
    columns in the same slice
  - preserved terminal quality-gate behavior without widening route/read-side
    scope, so this remains a runtime-owner migration rather than a broad
    cleanup pass

  This is a real Python-to-Rust migration step because Rust now owns not only
  the batch quality event facts and the summary output, but also the
  intermediate aggregation state that evolves between those two. That pushes
  the quality runtime materially closer to a full Rust-owned
  `event -> summary_state -> summary` pipeline.

  Explicitly not migrated in this slice:
  - complete Python parity for every batch quality summary metric family
  - any dedicated persisted column for `quality_metrics_summary_state`
  - broader read-side consumption of the new intermediate state

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_story_repair_quality_context_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 11:55 +08:00 checkpoint:
  this slice extended the same batch quality-owner migration across the
  resume/recover boundary so Rust does not lose its newly owned intermediate
  quality aggregation state after a task restart.

  A focused audit confirmed the next owner gap:
  - the previous Rust slice already introduced
    `quality_metrics_summary_state` and made the active batch runtime advance
    it alongside `quality_metrics_history` and `quality_metrics_summary`
  - but `build_resume_runtime_state_seed(...)` in
    `chapter_batch_generation_resume_task_command_service.rs` still restored
    only `quality_metrics_summary` and `quality_history_context` from snapshot
    state
  - that meant a resumed task would re-enter runtime with the final summary
    but without the intermediate state/history owner that Rust had just learned
    to maintain, weakening the migration at exactly the recover boundary where
    runtime ownership matters most

  The refactor therefore completed the next real migration seam:
  - rewired
    `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    so resume runtime seed now restores:
      1. `quality_metrics_history`
      2. `quality_metrics_summary_state`
      3. existing `quality_metrics_summary` / `quality_history_context`
  - preserved compatibility by sourcing the new state from runtime snapshot
    payload, without expanding read-side or database-column scope in the same
    patch
  - added focused resume tests that prove Rust now keeps quality history and
    intermediate summary state intact when rebuilding runtime state for resumed
    batch tasks

  This is a real Python-to-Rust migration step because Rust quality ownership
  now survives the resume/recover boundary. The batch runtime no longer falls
  back to a “summary only” shape after restart; it re-enters with the same
  history + intermediate state needed to keep advancing the Rust-owned
  `event -> summary_state -> summary` pipeline.

  Explicitly not migrated in this slice:
  - broader read-side consumers of `quality_metrics_summary_state`
  - dedicated persisted database columns for the new intermediate state
  - complete Python parity for every quality-summary metric family

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:20 +08:00 checkpoint:
  this slice continued the same “real Python-to-Rust migration” track by
  removing one more duplicated semantics island around failed batch quality
  terminal-state interpretation.

  A focused audit confirmed that failed-task terminal semantics were still
  split across multiple Rust services:
  - `chapter_batch_generation_quality_status_service.rs` computed
    `manual_review / retry / error` terminal payload semantics for read-side
    responses by combining failed chapter entries with runtime quality context
  - `chapter_batch_generation_stream_semantics_service.rs` independently
    recomputed the same `manual_review / retry` distinctions to build stream
    terminal state and fallback messages
  - both paths were already using Rust-owned quality context, but they still
    owned parallel copies of the decision tree, which is exactly the kind of
    Python-era semantics duplication that causes drift during migration

  The refactor therefore completed the next real migration seam:
  - introduced a shared failed-terminal semantics owner in
    `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
    via:
      1. `BatchGenerationFailedTerminalKind`
      2. `BatchGenerationFailedTerminalSemantics`
      3. `resolve_failed_terminal_semantics(...)`
  - rewired `insert_batch_generation_terminal_status_payload(...)` to consume
    that shared resolver instead of locally reimplementing the decision tree
  - rewired
    `backend-rs/src/services/chapter_batch_generation_stream_semantics_service.rs`
    so stream-state terminal kind / terminal label / fallback message logic now
    consumes the same shared failed-terminal semantics owner
  - kept payload shapes and stream event behavior stable; this slice changed
    ownership of the decision logic, not the externally visible contract

  This is a real Python-to-Rust migration step because Rust now has a single
  owner for failed batch quality terminal semantics across both read-side and
  stream-side consumers. The system is materially closer to a true
  service-owned runtime model, instead of multiple services replaying the same
  Python-era decision tree in parallel.

  Explicitly not migrated in this slice:
  - batch execution-time quality gate decisions themselves
  - database persistence shape changes
  - broader route compression or unrelated read-side cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_stream_semantics_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:17 +08:00 checkpoint:
  this slice pushed the same failed-quality semantics migration one step deeper
  into the actual runtime-control path, instead of stopping at read-side and
  stream-side consumers.

  A focused audit confirmed the next remaining owner split:
  - the previous slice already made read-side terminal payloads and stream
    terminal state consume one shared failed-terminal semantics owner
  - but execution-time quality gate handling in
    `chapter_batch_generation_runtime_state_service.rs` still computed
    `manual_review` and `retry` branches directly from quality-context helpers
  - and resume blocking in
    `chapter_batch_generation_resume_task_command_service.rs` still owned an
    independent “manual review blocker” decision path
  - that meant Rust still had multiple parallel branches for the same
    `failed_chapters + quality_context + retry_budget -> terminal semantics`
    decision, which is exactly the Python-era seam we want to collapse

  The refactor therefore completed the next real migration seam:
  - extended the shared owner in
    `backend-rs/src/services/chapter_batch_generation_quality_status_service.rs`
    with `resolve_failed_terminal_semantics_from_sources(...)`, so callers that
    only have source facts do not need to reconstruct or duplicate the decision
    tree around a full task wrapper
  - rewired
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    so execution-time quality gate stop/retry routing now consumes that shared
    source-level resolver, then filters the result into the
    `ManualReview` / `Retry` branches
  - rewired
    `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    so resume manual-review blocking now consumes the same shared resolver
    instead of separately checking failed-chapter labels plus quality-context
    blockers
  - preserved external behavior; this slice changed decision ownership, not the
    HTTP/SSE/runtime payload contract

  This is a real Python-to-Rust migration step because failed batch quality
  semantics now have one shared Rust owner across:
  - read-side terminal payloads
  - stream terminal state
  - execution-time quality gate stop/retry routing
  - resume manual-review blocking

  The system is materially closer to a true Rust-owned runtime decision chain,
  rather than multiple services replaying the same Python-era branching rules
  from different entry points.

  Explicitly not migrated in this slice:
  - persistence-shape changes for quality terminal semantics
  - broader route compression
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_quality_status_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:17 +08:00 checkpoint:
  this slice continued the same runtime-owner migration by pushing shared batch
  quality terminal semantics one step deeper into persistence/runtime patch
  assembly, instead of leaving the final write-shape normalization duplicated
  across multiple local builders.

  A focused audit confirmed the next remaining owner split:
  - `build_quality_gate_blocked_failed_chapter_entry(...)` still hand-wrote
    `quality_gate_decision` / `quality_gate_label` / `phase`
  - `normalize_terminal_quality_gate_payload(...)` still hand-wrote
    `status` / `decision` / `label` into nested `quality_gate`
  - `build_quality_gate_blocked_runtime_state_patch(...)` separately applied
    similar manual-review semantics again when patching
    `active_story_repair_payload`
  - all of these were already implementing the same Rust-owned
    `manual_review + quality_blocked + failed gate` terminal shape, but they
    still duplicated the final persistence/write contract in parallel

  The refactor therefore completed the next real migration seam:
  - introduced shared runtime-persistence helpers in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`:
      1. `apply_manual_review_terminal_fields(...)`
      2. `apply_manual_review_terminal_quality_gate(...)`
  - rewired `build_quality_gate_blocked_failed_chapter_entry(...)` to consume
    the shared terminal field helper instead of locally writing the same
    `manual_review/quality_blocked` contract
  - rewired `normalize_terminal_quality_gate_payload(...)` to consume the
    shared nested quality-gate helper instead of locally writing
    `failed/manual_review/<label>`
  - rewired the `active_story_repair_payload` branch inside
    `build_quality_gate_blocked_runtime_state_patch(...)` to consume the same
    shared terminal field helper
  - added a focused runtime-state test that directly proves both shared helpers
    emit the expected manual-review terminal semantics

  This is a real Python-to-Rust migration step because the batch quality
  terminal write-shape is now owned by one shared Rust implementation across:
  - failed chapter entry persistence
  - runtime snapshot terminal patch assembly
  - nested quality-gate normalization
  - active story-repair payload terminal mutation

  The system is materially closer to a true Rust-owned persistence/runtime
  contract, rather than multiple builders replaying the same Python-era final
  shape independently.

  Explicitly not migrated in this slice:
  - broader persistence column changes
  - non-terminal quality mutation paths
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:34 +08:00 checkpoint:
  this slice continued the same runtime-owner migration by pushing the batch
  quality-gate repair retry write contract into one shared Rust owner, instead
  of leaving the retry transition to hand-assemble the same top-level runtime
  patch inline.

  A focused retry-path audit confirmed the next duplicated owner seam:
  - `maybe_retry_batch_generation_for_quality_gate_repair(...)` still
    hand-built the same retry transition fields inline when moving a blocked
    chapter from `manual_review` into `repair_pending`
  - the fields
    `analysis_task_message` / `analysis_task_progress` /
    `analysis_last_error` / `quality_gate_decision` /
    `quality_gate_label` / `phase`
    already represented one Rust-owned retry state contract, but their final
    runtime snapshot write-shape still lived as local JSON assembly inside the
    retry branch
  - this meant terminal manual-review patching had already become shared Rust
    ownership, while the adjacent auto-repair retry transition still carried a
    smaller duplicated write contract

  The refactor therefore completed the next real migration seam:
  - introduced shared retry runtime patch ownership in
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    via `build_retry_quality_runtime_patch_contract(...)`
  - rewired
    `maybe_retry_batch_generation_for_quality_gate_repair(...)`
    so the quality-gate repair retry snapshot no longer hand-builds the same
    top-level patch fields inline, and instead persists the shared Rust-owned
    contract
  - added focused runtime-state coverage that proves the shared retry patch
    builder emits the expected retry transition shape

  This is a real Python-to-Rust migration step because the batch quality
  runtime now owns both sides of the quality-gate transition boundary in Rust:
  - manual-review terminal persistence/patch contracts
  - repair retry transition contracts back into `repair_pending`

  The system is materially closer to a single Rust-owned runtime transition
  model, rather than parallel branches replaying adjacent Python-era patch
  shapes independently.

  Explicitly not migrated in this slice:
  - deeper retry-path nested payload normalization
  - non-quality retry flows
  - unrelated warning cleanup

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
- 2026-05-30 12:05 +08:00 checkpoint:
  this slice tightened the same batch quality-owner migration around the last
  two runtime-boundary inconsistencies that still let Python-era semantics leak
  through restart and terminal manual-review transitions.

  A focused runtime-owner audit confirmed two gaps:
  - `build_quality_gate_blocked_runtime_state_patch(...)` already normalized
    terminal `manual_review` output for
    `quality_metrics_summary` / `latest_quality_metrics` /
    `quality_metrics_history` / `quality_history_context`, but the newer
    Rust-owned `quality_metrics_summary_state` could still keep a stale
    `recent_history[*].quality_gate`, leaving the intermediate aggregation
    state out of sync with the final terminal decision
  - `build_resume_runtime_state_seed(...)` restored
    `quality_metrics_summary_state` only when it already existed inside runtime
    state payload; older snapshots that carried `quality_metrics_history` +
    `quality_metrics_summary` but no state would still resume without the
    Rust-owned intermediate aggregation owner

  The refactor therefore completed the next real migration seam:
  - rewired
    `backend-rs/src/services/chapter_batch_generation_resume_task_command_service.rs`
    so resume seed now prefers runtime
    `quality_metrics_summary_state`, but falls back to rebuilding it from
    Rust-owned `quality_metrics_history` when the snapshot comes from an older
    shape
  - tightened
    `backend-rs/src/services/chapter_batch_generation_runtime_state_service.rs`
    test coverage so terminal `manual_review` normalization now proves
    `quality_metrics_summary_state.recent_history` is rewritten to the same
    `failed/manual_review/<label>` contract as summary/history/context
  - added focused resume tests that prove a task restarted from history-only
    quality runtime data re-enters with a reconstructed
    `quality_metrics_summary_state`

  This is a real Python-to-Rust migration step because Rust now owns the batch
  quality runtime consistently across both:
  - terminal quality-blocked/manual-review transitions
  - resume/recover transitions from older snapshot shapes

  The runtime no longer depends on preexisting snapshot state to preserve its
  intermediate quality aggregation owner; it can reconstruct and continue the
  Rust-owned `history -> summary_state -> summary` pipeline by itself.

  Explicitly not migrated in this slice:
  - broader read-side consumers of `quality_metrics_summary_state`
  - dedicated persisted database columns for the intermediate state
  - full Python parity for every batch quality-summary metric family

  Validation for this slice:
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_resume_task_command_service -- --nocapture`
  - `cargo test --manifest-path "backend-rs/Cargo.toml" chapter_batch_generation_runtime_state_service -- --nocapture`
  - `cargo check --manifest-path "backend-rs/Cargo.toml"`
